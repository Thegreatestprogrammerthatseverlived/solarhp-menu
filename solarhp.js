"use strict";

(() => {
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __commonJS = (e, t) => function () {
    try {
      if (!t) {
        (0, e[__getOwnPropNames(e)[0]])((t = {
          exports: {}
        }).exports, t);
      }
      return t.exports;
    } catch (e) {
      t = 0;
      throw e;
    }
  };
  var require_solarhp = __commonJS({
    "solarhp.ts"(exports, module) {
      var __decorate = exports && exports.__decorate || function (e, t, o, n) {
        var i;
        var a = arguments.length;
        var l = a < 3 ? t : n === null ? n = Object.getOwnPropertyDescriptor(t, o) : n;
        if (typeof Reflect == "object" && typeof Reflect.decorate == "function") {
          l = Reflect.decorate(e, t, o, n);
        } else {
          for (var r = e.length - 1; r >= 0; r--) {
            if (i = e[r]) {
              l = (a < 3 ? i(l) : a > 3 ? i(t, o, l) : i(t, o)) || l;
            }
          }
        }
        if (a > 3 && l) {
          Object.defineProperty(t, o, l);
        }
        return l;
      };
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp2;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Android;
      var Android2;
      function raise(e) {
        const t = new Error(e);
        t.name = "Il2CppError";
        t.stack = t.stack?.replace(/^(Il2Cpp)?Error/, "[0m[38;5;9mil2cpp[0m")?.replace(/\n    at (.+) \((.+):(.+)\)/, "[3m[2m")?.concat("[0m");
        throw t;
      }
      function warn(e) {
        globalThis.console.log(`[38;5;11mil2cpp[0m: ${e}`);
      }
      function ok(e) {
        globalThis.console.log(`[38;5;10mil2cpp[0m: ${e}`);
      }
      function inform(e) {
        globalThis.console.log(`[38;5;12mil2cpp[0m: ${e}`);
      }
      function decorate(e, t, o = Object.getOwnPropertyDescriptors(e)) {
        for (const n in o) {
          o[n] = t(e, n, o[n]);
        }
        Object.defineProperties(e, o);
        return e;
      }
      function getter(e, t, o, n) {
        globalThis.Object.defineProperty(e, t, n?.(e, t, {
          get: o,
          configurable: true
        }) ?? {
          get: o,
          configurable: true
        });
      }
      function cyrb53(e) {
        let t = 3735928559;
        let o = 1103547991;
        for (let n, i = 0; i < e.length; i++) {
          n = e.charCodeAt(i);
          t = Math.imul(t ^ n, 2654435761);
          o = Math.imul(o ^ n, 1597334677);
        }
        t = Math.imul(t ^ t >>> 16, 2246822507);
        t ^= Math.imul(o ^ o >>> 13, 3266489909);
        o = Math.imul(o ^ o >>> 16, 2246822507);
        o ^= Math.imul(t ^ t >>> 13, 3266489909);
        return (o & 2097151) * 4294967296 + (t >>> 0);
      }
      function exportsHash(e) {
        return cyrb53(e.enumerateExports().sort((e, t) => e.name.localeCompare(t.name)).map(t => t.name + t.address.sub(e.base)).join(""));
      }
      function lazy(e, t, o) {
        const n = o.get;
        if (!n) {
          throw new Error("@lazy can only be applied to getter accessors");
        }
        o.get = function () {
          const e = n.call(this);
          Object.defineProperty(this, t, {
            value: e,
            configurable: o.configurable,
            enumerable: o.enumerable,
            writable: false
          });
          return e;
        };
        return o;
      }
      (function (e) {
        function t(t) {
          const o = e.exports.resolveInternalCall(Memory.allocUtf8String("UnityEngine.Application::" + t));
          const n = new NativeFunction(o, "pointer", []);
          if (n.isNull()) {
            return null;
          } else {
            return new e.String(n()).asNullable()?.content ?? null;
          }
        }
        e.application = {
          get dataPath() {
            return t("get_persistentDataPath");
          },
          get identifier() {
            return t("get_identifier") ?? t("get_bundleIdentifier") ?? Process.mainModule.name;
          },
          get version() {
            return t("get_version") ?? exportsHash(e.module).toString(16);
          }
        };
        getter(e, "unityVersion", () => {
          try {
            const o = e.$config.unityVersion ?? t("get_unityVersion");
            if (o != null) {
              return o;
            }
          } catch (e) {}
          for (const t of e.module.enumerateRanges("r--").concat(Process.getRangeByAddress(e.module.base))) {
            for (let {
              address: e
            } of Memory.scanSync(t.base, t.size, "69 6c 32 63 70 70")) {
              while (e.readU8() != 0) {
                e = e.sub(1);
              }
              const t = UnityVersion.find(e.add(1).readCString());
              if (t != null) {
                return t;
              }
            }
          }
          raise("couldn't determine the Unity version, please specify it manually");
        }, lazy);
        getter(e, "unityVersionIsBelow201830", () => UnityVersion.lt(e.unityVersion, "2018.3.0"), lazy);
        getter(e, "unityVersionIsBelow202120", () => UnityVersion.lt(e.unityVersion, "2021.2.0"), lazy);
      })(Il2Cpp ||= {});
      Il2Cpp2 = Il2Cpp ||= {};
      Il2Cpp2.boxed = function (e, t) {
        const o = {
          int8: "System.SByte",
          uint8: "System.Byte",
          int16: "System.Int16",
          uint16: "System.UInt16",
          int32: "System.Int32",
          uint32: "System.UInt32",
          int64: "System.Int64",
          uint64: "System.UInt64",
          char: "System.Char",
          intptr: "System.IntPtr",
          uintptr: "System.UIntPtr"
        };
        const n = typeof e == "boolean" ? "System.Boolean" : typeof e == "number" ? o[t ?? "int32"] : e instanceof Int64 ? "System.Int64" : e instanceof UInt64 ? "System.UInt64" : e instanceof NativePointer ? o[t ?? "intptr"] : raise(`Cannot create boxed primitive using value of type '${typeof e}'`);
        const i = Il2Cpp2.corlib.class(n ?? raise(`Unknown primitive type name '${t}'`)).alloc();
        (i.tryField("m_value") ?? i.tryField("_pointer") ?? raise(`Could not find primitive field in class '${n}'`)).value = e;
        return i;
      };
      (function (e) {
        e.$config = {
          moduleName: undefined,
          unityVersion: undefined,
          exports: {}
        };
      })(Il2Cpp ||= {});
      (function (e) {
        function t(t) {
          e.corlib.class("System.IO.Directory").method("CreateDirectory").invoke(e.string(t));
        }
        function o() {
          warn("this api will be removed in a future release, please use `npx frida-il2cpp-bridge dump` instead");
        }
        e.dump = function (n, i) {
          n = n ?? `${e.application.identifier}_${e.application.version}.cs`;
          t(i = i ?? e.application.dataPath ?? Process.getCurrentDir());
          const a = `${i}/${n}`;
          const l = new File(a, "w");
          for (const t of e.domain.assemblies) {
            inform(`dumping ${t.name}...`);
            for (const e of t.image.classes) {
              l.write(`${e}\n\n`);
            }
          }
          l.flush();
          l.close();
          ok(`dump saved to ${a}`);
          o();
        };
        e.dumpTree = function (n, i = false) {
          n = n ?? `${e.application.dataPath ?? Process.getCurrentDir()}/${e.application.identifier}_${e.application.version}`;
          if (!i && function (t) {
            return e.corlib.class("System.IO.Directory").method("Exists").invoke(e.string(t));
          }(n)) {
            raise(`directory ${n} already exists - pass ignoreAlreadyExistingDirectory = true to skip this check`);
          }
          for (const o of e.domain.assemblies) {
            inform(`dumping ${o.name}...`);
            const e = `${n}/${o.name.replaceAll(".", "/")}.cs`;
            t(e.substring(0, e.lastIndexOf("/")));
            const i = new File(e, "w");
            for (const e of o.image.classes) {
              i.write(`${e}\n\n`);
            }
            i.flush();
            i.close();
          }
          ok(`dump saved to ${n}`);
          o();
        };
      })(Il2Cpp ||= {});
      (function (e) {
        e.installExceptionListener = function (t = "current") {
          const o = e.exports.threadGetCurrent();
          return Interceptor.attach(e.module.getExportByName("__cxa_throw"), function (n) {
            if (t != "current" || e.exports.threadGetCurrent().equals(o)) {
              inform(new e.Object(n[0].readPointer()));
            }
          });
        };
      })(Il2Cpp ||= {});
      (function (e) {
        function t(t, o, n) {
          const i = e.$config.exports?.[t]?.() ?? e.module.findExportByName(t) ?? e.memorySnapshotExports[t];
          const a = new NativeFunction(i ?? NULL, o, n);
          if (a.isNull()) {
            return new Proxy(a, {
              get(e, t) {
                const o = e[t];
                if (typeof o == "function") {
                  return o.bind(e);
                } else {
                  return o;
                }
              },
              apply() {
                if (i == null) {
                  raise(`couldn't resolve export ${t}`);
                } else if (i.isNull()) {
                  raise(`export ${t} points to NULL IL2CPP library has likely been stripped, obfuscated, or customized`);
                }
              }
            });
          } else {
            return a;
          }
        }
        e.exports = {
          get alloc() {
            return t("zlbROoiarnm", "pointer", ["size_t"]);
          },
          get arrayGetLength() {
            return t("YnIqjPAeKMS", "uint32", ["pointer"]);
          },
          get arrayNew() {
            return t("pgdSUYkWMJr", "pointer", ["pointer", "uint32"]);
          },
          get assemblyGetImage() {
            return t("_uaqbSnBXIa", "pointer", ["pointer"]);
          },
          get classForEach() {
            return t("QUZQwDuenGT", "void", ["pointer", "pointer"]);
          },
          get classFromName() {
            return t("nYeRoAQlLVk", "pointer", ["pointer", "pointer", "pointer"]);
          },
          get classFromObject() {
            return t("UgYuZCiuHZE", "pointer", ["pointer"]);
          },
          get classGetArrayClass() {
            return t("YasSFVLtEvu", "pointer", ["pointer", "uint32"]);
          },
          get classGetArrayElementSize() {
            return t("HfkZSFpBwVF", "int", ["pointer"]);
          },
          get classGetAssemblyName() {
            return t("UpSR_YaJCtl", "pointer", ["pointer"]);
          },
          get classGetBaseType() {
            return t("_EvpdArAmnF", "pointer", ["pointer"]);
          },
          get classGetDeclaringType() {
            return t("WKn_tKspYDy", "pointer", ["pointer"]);
          },
          get classGetElementClass() {
            return t("UDbZbvxzblI", "pointer", ["pointer"]);
          },
          get classGetFieldFromName() {
            return t("MPOHSfxIBNp", "pointer", ["pointer", "pointer"]);
          },
          get classGetFields() {
            return t("rOgtHbCJOHy", "pointer", ["pointer", "pointer"]);
          },
          get classGetFlags() {
            return t("pKz_mGx_oej", "int", ["pointer"]);
          },
          get classGetImage() {
            return t("vvMARHqAeKl", "pointer", ["pointer"]);
          },
          get classGetInstanceSize() {
            return t("fTBdTeJRpKu", "int32", ["pointer"]);
          },
          get classGetInterfaces() {
            return t("XFZFz_darUM", "pointer", ["pointer", "pointer"]);
          },
          get classGetMethodFromName() {
            return t("RGutFUB_gvX", "pointer", ["pointer", "pointer", "int"]);
          },
          get classGetMethods() {
            return t("TgisPILmYXu", "pointer", ["pointer", "pointer"]);
          },
          get classGetName() {
            return t("HbwEb_zoK_E", "pointer", ["pointer"]);
          },
          get classGetNamespace() {
            return t("QVzdiSaisXV", "pointer", ["pointer"]);
          },
          get classGetNestedClasses() {
            return t("QZIoMgVvjf_", "pointer", ["pointer", "pointer"]);
          },
          get classGetParent() {
            return t("NKxjgk_dOiY", "pointer", ["pointer"]);
          },
          get classGetStaticFieldData() {
            return t("kDZkxxdBLRc", "pointer", ["pointer"]);
          },
          get classGetValueTypeSize() {
            return t("pTNZxpZJzic", "int32", ["pointer", "pointer"]);
          },
          get classGetType() {
            return t("cNgaIdXPQVp", "pointer", ["pointer"]);
          },
          get classHasReferences() {
            return t("LZYmAOcldok", "bool", ["pointer"]);
          },
          get classInitialize() {
            return t("FWblMkQaNuJ", "void", ["pointer"]);
          },
          get classIsAbstract() {
            return t("vHwyXOZKmqk", "bool", ["pointer"]);
          },
          get classIsAssignableFrom() {
            return t("fIiyiwqBClg", "bool", ["pointer", "pointer"]);
          },
          get classIsBlittable() {
            return t("UggviuhnaNc", "bool", ["pointer"]);
          },
          get classIsEnum() {
            return t("irRAPcuPmZd", "bool", ["pointer"]);
          },
          get classIsGeneric() {
            return t("YEcIFRlDeGe", "bool", ["pointer"]);
          },
          get classIsInflated() {
            return t("flVXyTHWACz", "bool", ["pointer"]);
          },
          get classIsInterface() {
            return t("LtQIujXiPQI", "bool", ["pointer"]);
          },
          get classIsSubclassOf() {
            return t("OZMxZxTOhcn", "bool", ["pointer", "pointer", "bool"]);
          },
          get classIsValueType() {
            return t("gzFfF_XYXBu", "bool", ["pointer"]);
          },
          get domainGetAssemblyFromName() {
            return t("AGoNHOgrGZj", "pointer", ["pointer", "pointer"]);
          },
          get domainGet() {
            return t("ewITDoKjSbY", "pointer", []);
          },
          get domainGetAssemblies() {
            return t("KAuvyGCWZal", "pointer", ["pointer", "pointer"]);
          },
          get fieldGetClass() {
            return t("CRsxeDQDMrG", "pointer", ["pointer"]);
          },
          get fieldGetFlags() {
            return t("aIXtWVyRPQW", "int", ["pointer"]);
          },
          get fieldGetName() {
            return t("glzjhAhskPN", "pointer", ["pointer"]);
          },
          get fieldGetOffset() {
            return t("cnalSlCqicr", "int32", ["pointer"]);
          },
          get fieldGetStaticValue() {
            return t("ZZtRVbsIVYG", "void", ["pointer", "pointer"]);
          },
          get fieldGetType() {
            return t("oaeqZmzMbDt", "pointer", ["pointer"]);
          },
          get fieldSetStaticValue() {
            return t("UevIyjTnuzl", "void", ["pointer", "pointer"]);
          },
          get free() {
            return t("kYLqUQDUlkF", "void", ["pointer"]);
          },
          get gcCollect() {
            return t("NAKEJPKirvl", "void", ["int"]);
          },
          get gcCollectALittle() {
            return t("aIXvAhf_yLH", "void", []);
          },
          get gcDisable() {
            return t("VbjzdjUCGWJ", "void", []);
          },
          get gcEnable() {
            return t("qAcXxVcCPNu", "void", []);
          },
          get gcGetHeapSize() {
            return t("pFqvtRzOERS", "int64", []);
          },
          get gcGetMaxTimeSlice() {
            return t("ZMFQNIiOipD", "int64", []);
          },
          get gcGetUsedSize() {
            return t("JFQDrhupaBF", "int64", []);
          },
          get gcHandleGetTarget() {
            return t("CrYrZpkeaaf", "pointer", ["uint32"]);
          },
          get gcHandleFree() {
            return t("jPWOeIVFGLS", "void", ["uint32"]);
          },
          get gcHandleNew() {
            return t("OsXzWDUIvIW", "uint32", ["pointer", "bool"]);
          },
          get gcHandleNewWeakRef() {
            return t("OOzvTC_XbNd", "uint32", ["pointer", "bool"]);
          },
          get gcIsDisabled() {
            return t("PDsRPUDxrjw", "bool", []);
          },
          get gcIsIncremental() {
            return t("XIQyVRmCROd", "bool", []);
          },
          get gcSetMaxTimeSlice() {
            return t("qgiMEBirTbD", "void", ["int64"]);
          },
          get gcStartIncrementalCollection() {
            return t("Olgn_dzAreE", "void", []);
          },
          get gcStartWorld() {
            return t("HjFuHFXlnwG", "void", []);
          },
          get gcStopWorld() {
            return t("YdNhBxnaoQh", "void", []);
          },
          get getCorlib() {
            return t("eUPqbGVaXIY", "pointer", []);
          },
          get imageGetAssembly() {
            return t("oQuELENSelm", "pointer", ["pointer"]);
          },
          get imageGetClass() {
            return t("ZEpVtNyyOCK", "pointer", ["pointer", "uint"]);
          },
          get imageGetClassCount() {
            return t("blKAcnmYlXB", "uint32", ["pointer"]);
          },
          get imageGetName() {
            return t("rCrwzAsBwWv", "pointer", ["pointer"]);
          },
          get initialize() {
            return t("KQQobkahLcB", "void", ["pointer"]);
          },
          get livenessAllocateStruct() {
            return t("orfnYfFUOKN", "pointer", ["pointer", "int", "pointer", "pointer", "pointer"]);
          },
          get livenessCalculationBegin() {
            return t("il2cpp_unity_liveness_calculation_begin", "pointer", ["pointer", "int", "pointer", "pointer", "pointer", "pointer"]);
          },
          get livenessCalculationEnd() {
            return t("il2cpp_unity_liveness_calculation_end", "void", ["pointer"]);
          },
          get livenessCalculationFromStatics() {
            return t("YDEIUmHSeSs", "void", ["pointer"]);
          },
          get livenessFinalize() {
            return t("RltZcmgVyDa", "void", ["pointer"]);
          },
          get livenessFreeStruct() {
            return t("GYwOvIYuVkt", "void", ["pointer"]);
          },
          get memorySnapshotCapture() {
            return t("qEnocNzysOD", "pointer", []);
          },
          get memorySnapshotFree() {
            return t("BNRtyQsRRxB", "void", ["pointer"]);
          },
          get memorySnapshotGetClasses() {
            return t("il2cpp_memory_snapshot_get_classes", "pointer", ["pointer", "pointer"]);
          },
          get memorySnapshotGetObjects() {
            return t("il2cpp_memory_snapshot_get_objects", "pointer", ["pointer", "pointer"]);
          },
          get methodGetClass() {
            return t("AQpvtNhMywP", "pointer", ["pointer"]);
          },
          get methodGetFlags() {
            return t("vXQMAxPcioX", "uint32", ["pointer", "pointer"]);
          },
          get methodGetName() {
            return t("RdKgQJfA_Fq", "pointer", ["pointer"]);
          },
          get methodGetObject() {
            return t("ZYXZxcrkrYB", "pointer", ["pointer", "pointer"]);
          },
          get methodGetParameterCount() {
            return t("HNnargsOyMI", "uint8", ["pointer"]);
          },
          get methodGetParameterName() {
            return t("zYqibxkmhyQ", "pointer", ["pointer", "uint32"]);
          },
          get methodGetParameters() {
            return t("il2cpp_method_get_parameters", "pointer", ["pointer", "pointer"]);
          },
          get methodGetParameterType() {
            return t("YOkFJacjdvD", "pointer", ["pointer", "uint32"]);
          },
          get methodGetReturnType() {
            return t("aTFWVYLLxWn", "pointer", ["pointer"]);
          },
          get methodIsGeneric() {
            return t("vKNzgNLPvXc", "bool", ["pointer"]);
          },
          get methodIsInflated() {
            return t("EQEYVVIabOE", "bool", ["pointer"]);
          },
          get methodIsInstance() {
            return t("GvicpGfzItC", "bool", ["pointer"]);
          },
          get monitorEnter() {
            return t("fCgrOeqKBOM", "void", ["pointer"]);
          },
          get monitorExit() {
            return t("zMdabSFMgua", "void", ["pointer"]);
          },
          get monitorPulse() {
            return t("XSkypOy_YfV", "void", ["pointer"]);
          },
          get monitorPulseAll() {
            return t("UWDGIMygvqE", "void", ["pointer"]);
          },
          get monitorTryEnter() {
            return t("yLGRpudYLBk", "bool", ["pointer", "uint32"]);
          },
          get monitorTryWait() {
            return t("PGHTxzaOLoL", "bool", ["pointer", "uint32"]);
          },
          get monitorWait() {
            return t("YLzLMHGWlVy", "void", ["pointer"]);
          },
          get objectGetClass() {
            return t("DYSSFTVys_a", "pointer", ["pointer"]);
          },
          get objectGetVirtualMethod() {
            return t("UQxqzJcOzGI", "pointer", ["pointer", "pointer"]);
          },
          get objectInitialize() {
            return t("IQDCicMrAAh", "void", ["pointer", "pointer"]);
          },
          get objectNew() {
            return t("rvBzgPARXUp", "pointer", ["pointer"]);
          },
          get objectGetSize() {
            return t("tkYhNKvQzea", "uint32", ["pointer"]);
          },
          get objectUnbox() {
            return t("usGeLgJXLXs", "pointer", ["pointer"]);
          },
          get resolveInternalCall() {
            return t("wRphLKlGyAK", "pointer", ["pointer"]);
          },
          get stringGetChars() {
            return t("JbKXFKofDbk", "pointer", ["pointer"]);
          },
          get stringGetLength() {
            return t("tzhsKMk_erj", "int32", ["pointer"]);
          },
          get stringNew() {
            return t("HayoWStoOTy", "pointer", ["pointer"]);
          },
          get valueTypeBox() {
            return t("UXpYaFznhgg", "pointer", ["pointer", "pointer"]);
          },
          get threadAttach() {
            return t("YkLhnLonIZx", "pointer", ["pointer"]);
          },
          get threadDetach() {
            return t("Rj_ORUfTKjy", "void", ["pointer"]);
          },
          get threadGetAttachedThreads() {
            return t("il2cpp_thread_get_all_attached_threads", "pointer", ["pointer"]);
          },
          get threadGetCurrent() {
            return t("lKQVpyePKDk", "pointer", []);
          },
          get threadIsVm() {
            return t("liJGJwHegtr", "bool", ["pointer"]);
          },
          get typeEquals() {
            return t("oQgGfpIk_Le", "bool", ["pointer", "pointer"]);
          },
          get typeGetClass() {
            return t("nTTXqbPqucV", "pointer", ["pointer"]);
          },
          get typeGetName() {
            return t("MKrQf_BbDNm", "pointer", ["pointer"]);
          },
          get typeGetObject() {
            return t("__aggEgsdVR", "pointer", ["pointer"]);
          },
          get typeGetTypeEnum() {
            return t("eSFdf_oAcwB", "int", ["pointer"]);
          }
        };
        decorate(e.exports, lazy);
        getter(e, "memorySnapshotExports", () => new CModule("#include <stdint.h>\n#include <string.h>\n\ntypedef struct Il2CppManagedMemorySnapshot Il2CppManagedMemorySnapshot;\ntypedef struct Il2CppMetadataType Il2CppMetadataType;\n\nstruct Il2CppManagedMemorySnapshot\n{\n  struct Il2CppManagedHeap\n  {\n    uint32_t section_count;\n    void * sections;\n  } heap;\n  struct Il2CppStacks\n  {\n    uint32_t stack_count;\n    void * stacks;\n  } stacks;\n  struct Il2CppMetadataSnapshot\n  {\n    uint32_t type_count;\n    Il2CppMetadataType * types;\n  } metadata_snapshot;\n  struct Il2CppGCHandles\n  {\n    uint32_t tracked_object_count;\n    void ** pointers_to_objects;\n  } gc_handles;\n  struct Il2CppRuntimeInformation\n  {\n    uint32_t pointer_size;\n    uint32_t object_header_size;\n    uint32_t array_header_size;\n    uint32_t array_bounds_offset_in_header;\n    uint32_t array_size_offset_in_header;\n    uint32_t allocation_granularity;\n  } runtime_information;\n  void * additional_user_information;\n};\n\nstruct Il2CppMetadataType\n{\n  uint32_t flags;\n  void * fields;\n  uint32_t field_count;\n  uint32_t statics_size;\n  uint8_t * statics;\n  uint32_t base_or_element_type_index;\n  char * name;\n  const char * assembly_name;\n  uint64_t type_info_address;\n  uint32_t size;\n};\n\nuintptr_t\nil2cpp_memory_snapshot_get_classes (\n    const Il2CppManagedMemorySnapshot * snapshot, Il2CppMetadataType ** iter)\n{\n  const int zero = 0;\n  const void * null = 0;\n\n  if (iter != NULL && snapshot->metadata_snapshot.type_count > zero)\n  {\n    if (*iter == null)\n    {\n      *iter = snapshot->metadata_snapshot.types;\n      return (uintptr_t) (*iter)->type_info_address;\n    }\n    else\n    {\n      Il2CppMetadataType * metadata_type = *iter + 1;\n\n      if (metadata_type < snapshot->metadata_snapshot.types +\n                              snapshot->metadata_snapshot.type_count)\n      {\n        *iter = metadata_type;\n        return (uintptr_t) (*iter)->type_info_address;\n      }\n    }\n  }\n  return 0;\n}\n\nvoid **\nil2cpp_memory_snapshot_get_objects (\n    const Il2CppManagedMemorySnapshot * snapshot, uint32_t * size)\n{\n  *size = snapshot->gc_handles.tracked_object_count;\n  return snapshot->gc_handles.pointers_to_objects;\n}\n"), lazy);
      })(Il2Cpp ||= {});
      (function (e) {
        e.is = function (t) {
          return o => o instanceof e.Class ? t.isAssignableFrom(o) : t.isAssignableFrom(o.class);
        };
        e.isExactly = function (t) {
          return o => o instanceof e.Class ? o.equals(t) : o.class.equals(t);
        };
      })(Il2Cpp ||= {});
      (function (e) {
        e.gc = {
          get heapSize() {
            return e.exports.gcGetHeapSize();
          },
          get isEnabled() {
            return !e.exports.gcIsDisabled();
          },
          get isIncremental() {
            return !!e.exports.gcIsIncremental();
          },
          get maxTimeSlice() {
            return e.exports.gcGetMaxTimeSlice();
          },
          get usedHeapSize() {
            return e.exports.gcGetUsedSize();
          },
          set isEnabled(t) {
            if (t) {
              e.exports.gcEnable();
            } else {
              e.exports.gcDisable();
            }
          },
          set maxTimeSlice(t) {
            e.exports.gcSetMaxTimeSlice(t);
          },
          choose(t) {
            const o = [];
            const n = new NativeCallback((t, n) => {
              for (let i = 0; i < n; i++) {
                o.push(new e.Object(t.add(i * Process.pointerSize).readPointer()));
              }
            }, "void", ["pointer", "int", "pointer"]);
            if (e.unityVersionIsBelow202120) {
              const o = new NativeCallback(() => {}, "void", []);
              const i = e.exports.livenessCalculationBegin(t, 0, n, NULL, o, o);
              e.exports.livenessCalculationFromStatics(i);
              e.exports.livenessCalculationEnd(i);
            } else {
              const o = new NativeCallback((t, o) => t.isNull() || o.compare(0) != 0 ? e.alloc(o) : (e.free(t), NULL), "pointer", ["pointer", "size_t", "pointer"]);
              this.stopWorld();
              const i = e.exports.livenessAllocateStruct(t, 0, n, NULL, o);
              e.exports.livenessCalculationFromStatics(i);
              e.exports.livenessFinalize(i);
              this.startWorld();
              e.exports.livenessFreeStruct(i);
            }
            return o;
          },
          collect(t) {
            e.exports.gcCollect(t < 0 ? 0 : t > 2 ? 2 : t);
          },
          collectALittle() {
            e.exports.gcCollectALittle();
          },
          startWorld: () => e.exports.gcStartWorld(),
          startIncrementalCollection: () => e.exports.gcStartIncrementalCollection(),
          stopWorld: () => e.exports.gcStopWorld()
        };
      })(Il2Cpp ||= {});
      Android2 = Android ||= {};
      getter(Android2, "apiLevel", () => {
        const e = function (e) {
          const t = Process.findModuleByName("libc.so")?.findExportByName("__system_property_get");
          if (t) {
            const o = new NativeFunction(t, "void", ["pointer", "pointer"]);
            const n = Memory.alloc(92).writePointer(NULL);
            o(Memory.allocUtf8String(e), n);
            return n.readCString() ?? undefined;
          }
        }("ro.build.version.sdk");
        if (e) {
          return parseInt(e);
        } else {
          return null;
        }
      }, lazy);
      var NativeStruct = NativeStruct !== undefined ? NativeStruct : class {
        constructor(e) {
          if (e instanceof NativePointer) {
            this.handle = e;
          } else {
            this.handle = e.handle;
          }
        }
        equals(e) {
          return this.handle.equals(e.handle);
        }
        isNull() {
          return this.handle.isNull();
        }
        asNullable() {
          if (this.isNull()) {
            return null;
          } else {
            return this;
          }
        }
      };
      var UnityVersion;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      var Il2Cpp;
      function addFlippedEntries(e) {
        return Object.keys(e).reduce((e, t) => {
          e[e[t]] = t;
          return e;
        }, e);
      }
      function readNativeIterator(e) {
        const t = [];
        const o = Memory.alloc(Process.pointerSize);
        let n = e(o);
        while (!n.isNull()) {
          t.push(n);
          n = e(o);
        }
        return t;
      }
      function readNativeList(e) {
        const t = Memory.alloc(Process.pointerSize);
        const o = e(t);
        if (o.isNull()) {
          return [];
        }
        const n = new Array(t.readInt());
        for (let e = 0; e < n.length; e++) {
          n[e] = o.add(e * Process.pointerSize).readPointer();
        }
        return n;
      }
      function recycle(e) {
        return new Proxy(e, {
          cache: new Map(),
          construct(e, t) {
            const o = t[0].toUInt32();
            if (!this.cache.has(o)) {
              this.cache.set(o, new e(t[0]));
            }
            return this.cache.get(o);
          }
        });
      }
      NativePointer.prototype.offsetOf = function (e, t) {
        t ??= 512;
        for (let o = 0; t > 0 ? o < t : o < -t; o++) {
          if (e(t > 0 ? this.add(o) : this.sub(o))) {
            return o;
          }
        }
        return null;
      };
      (function (e) {
        const t = /(6\d{3}|20\d{2}|\d)\.(\d)\.(\d{1,2})(?:[abcfp]|rc){0,2}\d?/;
        function o(e, o) {
          const n = e.match(t);
          const i = o.match(t);
          for (let e = 1; e <= 3; e++) {
            const t = Number(n?.[e] ?? -1);
            const o = Number(i?.[e] ?? -1);
            if (t > o) {
              return 1;
            }
            if (t < o) {
              return -1;
            }
          }
          return 0;
        }
        e.find = function (e) {
          return e?.match(t)?.[0];
        };
        e.gte = function (e, t) {
          return o(e, t) >= 0;
        };
        e.lt = function (e, t) {
          return o(e, t) < 0;
        };
      })(UnityVersion ||= {});
      (function (e) {
        function t(t, o, n) {
          switch (n.enumValue) {
            case e.Type.Enum.BOOLEAN:
              return t.writeS8(+o);
            case e.Type.Enum.BYTE:
              return t.writeS8(o);
            case e.Type.Enum.UBYTE:
              return t.writeU8(o);
            case e.Type.Enum.SHORT:
              return t.writeS16(o);
            case e.Type.Enum.USHORT:
              return t.writeU16(o);
            case e.Type.Enum.INT:
              return t.writeS32(o);
            case e.Type.Enum.UINT:
              return t.writeU32(o);
            case e.Type.Enum.CHAR:
              return t.writeU16(o);
            case e.Type.Enum.LONG:
              return t.writeS64(o);
            case e.Type.Enum.ULONG:
              return t.writeU64(o);
            case e.Type.Enum.FLOAT:
              return t.writeFloat(o);
            case e.Type.Enum.DOUBLE:
              return t.writeDouble(o);
            case e.Type.Enum.NINT:
            case e.Type.Enum.NUINT:
            case e.Type.Enum.POINTER:
            case e.Type.Enum.STRING:
            case e.Type.Enum.ARRAY:
            case e.Type.Enum.NARRAY:
              return t.writePointer(o);
            case e.Type.Enum.VALUE_TYPE:
              Memory.copy(t, o, n.class.valueTypeSize);
              return t;
            case e.Type.Enum.OBJECT:
            case e.Type.Enum.CLASS:
            case e.Type.Enum.GENERIC_INSTANCE:
              if (o instanceof e.ValueType) {
                Memory.copy(t, o, n.class.valueTypeSize);
                return t;
              } else {
                return t.writePointer(o);
              }
          }
          raise(`couldn't write value ${o} to ${t} using an unhandled or unknown type ${n.name} (${n.enumValue}), please file an issue`);
        }
        e.alloc = function (t = Process.pointerSize) {
          return e.exports.alloc(t);
        };
        e.free = function (t) {
          return e.exports.free(t);
        };
        e.read = function (t, o) {
          switch (o.enumValue) {
            case e.Type.Enum.BOOLEAN:
              return !!t.readS8();
            case e.Type.Enum.BYTE:
              return t.readS8();
            case e.Type.Enum.UBYTE:
              return t.readU8();
            case e.Type.Enum.SHORT:
              return t.readS16();
            case e.Type.Enum.USHORT:
              return t.readU16();
            case e.Type.Enum.INT:
              return t.readS32();
            case e.Type.Enum.UINT:
              return t.readU32();
            case e.Type.Enum.CHAR:
              return t.readU16();
            case e.Type.Enum.LONG:
              return t.readS64();
            case e.Type.Enum.ULONG:
              return t.readU64();
            case e.Type.Enum.FLOAT:
              return t.readFloat();
            case e.Type.Enum.DOUBLE:
              return t.readDouble();
            case e.Type.Enum.NINT:
            case e.Type.Enum.NUINT:
              return t.readPointer();
            case e.Type.Enum.POINTER:
              return new e.Pointer(t.readPointer(), o.class.baseType);
            case e.Type.Enum.VALUE_TYPE:
              return new e.ValueType(t, o);
            case e.Type.Enum.OBJECT:
            case e.Type.Enum.CLASS:
              return new e.Object(t.readPointer());
            case e.Type.Enum.GENERIC_INSTANCE:
              if (o.class.isValueType) {
                return new e.ValueType(t, o);
              } else {
                return new e.Object(t.readPointer());
              }
            case e.Type.Enum.STRING:
              return new e.String(t.readPointer());
            case e.Type.Enum.ARRAY:
            case e.Type.Enum.NARRAY:
              return new e.Array(t.readPointer());
          }
          raise(`couldn't read the value from ${t} using an unhandled or unknown type ${o.name} (${o.enumValue}), please file an issue`);
        };
        e.write = t;
        e.fromFridaValue = function o(n, i) {
          if (globalThis.Array.isArray(n)) {
            const a = Memory.alloc(i.class.valueTypeSize);
            const l = i.class.fields.filter(e => !e.isStatic);
            for (let i = 0; i < l.length; i++) {
              const r = o(n[i], l[i].type);
              t(a.add(l[i].offset).sub(e.Object.headerSize), r, l[i].type);
            }
            return new e.ValueType(a, i);
          }
          if (!(n instanceof NativePointer)) {
            if (i.enumValue == e.Type.Enum.BOOLEAN) {
              return !!n;
            } else if (i.enumValue == e.Type.Enum.VALUE_TYPE && i.class.isEnum) {
              return o([n], i);
            } else {
              return n;
            }
          }
          if (i.isByReference) {
            return new e.Reference(n, i);
          }
          switch (i.enumValue) {
            case e.Type.Enum.POINTER:
              return new e.Pointer(n, i.class.baseType);
            case e.Type.Enum.STRING:
              return new e.String(n);
            case e.Type.Enum.CLASS:
            case e.Type.Enum.GENERIC_INSTANCE:
            case e.Type.Enum.OBJECT:
              return new e.Object(n);
            case e.Type.Enum.ARRAY:
            case e.Type.Enum.NARRAY:
              return new e.Array(n);
            default:
              return n;
          }
        };
        e.toFridaValue = function t(o) {
          if (typeof o == "boolean") {
            return +o;
          }
          if (o instanceof e.ValueType) {
            if (o.type.class.isEnum) {
              return o.field("value__").value;
            }
            {
              const e = o.type.class.fields.filter(e => !e.isStatic).map(e => t(e.bind(o).value));
              if (e.length == 0) {
                return [0];
              } else {
                return e;
              }
            }
          }
          return o;
        };
      })(Il2Cpp ||= {});
      (function (e) {
        function t() {
          const [e, t] = o();
          return Process.findModuleByName(e) ?? Process.findModuleByName(t ?? e) ?? (Process.platform == "darwin" ? Process.findModuleByAddress(DebugSymbol.fromName("il2cpp_init").address) : undefined) ?? undefined;
        }
        function o() {
          if (e.$config.moduleName) {
            return [e.$config.moduleName];
          }
          switch (Process.platform) {
            case "linux":
              return [Android.apiLevel ? "libil2cpp.so" : "GameAssembly.so"];
            case "windows":
              return ["GameAssembly.dll"];
            case "darwin":
              return ["UnityFramework", "GameAssembly.dylib"];
          }
          raise(`${Process.platform} is not supported yet`);
        }
        getter(e, "module", () => t() ?? raise("Could not find IL2CPP module"));
        e.initialize = async function (n = false) {
          const i = t() ?? (await new Promise(e => {
            const [t, n] = o();
            const i = setTimeout(() => {
              warn(`after 10 seconds, IL2CPP module '${t}' has not been loaded yet, is the app running?`);
            }, 10000);
            const a = Process.attachModuleObserver({
              onAdded(o) {
                if (o.name == t || n && o.name == n) {
                  clearTimeout(i);
                  setImmediate(() => {
                    e(o);
                    a.detach();
                  });
                }
              }
            });
          }));
          Reflect.defineProperty(e, "module", {
            value: i
          });
          return !!e.exports.getCorlib().isNull() && (await new Promise(t => {
            const o = Interceptor.attach(e.exports.initialize, {
              onLeave() {
                o.detach();
                if (n) {
                  t(true);
                } else {
                  setImmediate(() => t(false));
                }
              }
            });
          }));
        };
      })(Il2Cpp ||= {});
      (function (e) {
        e.perform = async function t(o, n = "bind") {
          let i = null;
          try {
            const a = await e.initialize(n == "main");
            if (n == "main" && !a) {
              return t(() => e.mainThread.schedule(o), "free");
            }
            if (e.currentThread == null) {
              i = e.domain.attach();
            }
            if (n == "bind" && i != null) {
              Script.bindWeak(globalThis, () => i?.detach());
            }
            const l = o();
            if (l instanceof Promise) {
              return await l;
            } else {
              return l;
            }
          } catch (e) {
            Script.nextTick(e => {
              throw e;
            }, e);
            return Promise.reject(e);
          } finally {
            if (n == "free" && i != null) {
              i.detach();
            }
          }
        };
      })(Il2Cpp ||= {});
      (function (e) {
        e.Tracer = class {
          #e = {
            depth: 0,
            buffer: [],
            history: new Set(),
            flush: () => {
              if (this.#e.depth == 0) {
                const e = `\n${this.#e.buffer.join("\n")}\n`;
                if (this.#t) {
                  inform(e);
                } else {
                  const t = cyrb53(e);
                  if (!this.#e.history.has(t)) {
                    this.#e.history.add(t);
                    inform(e);
                  }
                }
                this.#e.buffer.length = 0;
              }
            }
          };
          #o = e.mainThread.id;
          #t = false;
          #n;
          #i = [];
          #a;
          #l;
          #r;
          #s;
          #c;
          #d;
          #u;
          #m;
          constructor(e) {
            this.#n = e;
          }
          thread(e) {
            this.#o = e.id;
            return this;
          }
          verbose(e) {
            this.#t = e;
            return this;
          }
          domain() {
            this.#a = e.domain;
            return this;
          }
          assemblies(...e) {
            this.#l = e;
            return this;
          }
          classes(...e) {
            this.#r = e;
            return this;
          }
          methods(...e) {
            this.#s = e;
            return this;
          }
          filterAssemblies(e) {
            this.#c = e;
            return this;
          }
          filterClasses(e) {
            this.#d = e;
            return this;
          }
          filterMethods(e) {
            this.#u = e;
            return this;
          }
          filterParameters(e) {
            this.#m = e;
            return this;
          }
          and() {
            const e = e => {
              if (this.#m != null) {
                for (const t of e.parameters) {
                  if (this.#m(t)) {
                    this.#i.push(e);
                    break;
                  }
                }
              } else {
                this.#i.push(e);
              }
            };
            const t = t => {
              for (const o of t) {
                e(o);
              }
            };
            const o = o => {
              if (this.#u != null) {
                for (const t of o.methods) {
                  if (this.#u(t)) {
                    e(t);
                  }
                }
              } else {
                t(o.methods);
              }
            };
            const n = e => {
              for (const t of e) {
                o(t);
              }
            };
            const i = e => {
              if (this.#d != null) {
                for (const t of e.image.classes) {
                  if (this.#d(t)) {
                    o(t);
                  }
                }
              } else {
                n(e.image.classes);
              }
            };
            const a = e => {
              for (const t of e) {
                i(t);
              }
            };
            if (this.#s) {
              t(this.#s);
            } else if (this.#r) {
              n(this.#r);
            } else if (this.#l) {
              a(this.#l);
            } else if (this.#a) {
              (e => {
                if (this.#c != null) {
                  for (const t of e.assemblies) {
                    if (this.#c(t)) {
                      i(t);
                    }
                  }
                } else {
                  a(e.assemblies);
                }
              })(this.#a);
            }
            this.#l = undefined;
            this.#r = undefined;
            this.#s = undefined;
            this.#c = undefined;
            this.#d = undefined;
            this.#u = undefined;
            this.#m = undefined;
            return this;
          }
          attach() {
            for (const e of this.#i) {
              if (!e.virtualAddress.isNull()) {
                try {
                  this.#n(e, this.#e, this.#o);
                } catch (e) {
                  switch (e.message) {
                    case /unable to intercept function at \w+; please file a bug/.exec(e.message)?.input:
                    case "already replaced this function":
                      break;
                    default:
                      throw e;
                  }
                }
              }
            }
          }
        };
        e.trace = function (t = false) {
          return new e.Tracer(t ? (t, o, n) => {
            const i = t.relativeVirtualAddress.toString(16).padStart(8, "0");
            const a = +!t.isStatic | +e.unityVersionIsBelow201830;
            t.revert();
            const l = new NativeCallback(function (...l) {
              if (this.threadId == n) {
                const n = t.isStatic ? undefined : new e.Parameter("this", -1, t.class.type);
                const r = n ? [n].concat(t.parameters) : t.parameters;
                o.buffer.push(`[2m0x${i}[0m ${" ".repeat(o.depth++)}[35m${t.class.type.name}::[1m${t.name}[0m[0m(${r.map(t => `[32m${t.name}[0m = [31m${e.fromFridaValue(l[t.position + a], t.type)}[0m`).join(", ")})`);
              }
              const r = t.nativeFunction(...l);
              if (this.threadId == n) {
                o.buffer.push(`[2m0x${i}[0m ${" ".repeat(--o.depth)}[33m${t.class.type.name}::[1m${t.name}[0m[0m${r == null ? "" : ` = [36m${e.fromFridaValue(r, t.returnType)}`}[0m`);
                o.flush();
              }
              return r;
            }, t.returnType.fridaAlias, t.fridaSignature);
            Interceptor.replace(t.virtualAddress, l);
          } : (e, t, o) => {
            const n = e.relativeVirtualAddress.toString(16).padStart(8, "0");
            Interceptor.attach(e.virtualAddress, {
              onEnter() {
                if (this.threadId == o) {
                  t.buffer.push(`[2m0x${n}[0m ${" ".repeat(t.depth++)}[35m${e.class.type.name}::[1m${e.name}[0m[0m`);
                }
              },
              onLeave() {
                if (this.threadId == o) {
                  t.buffer.push(`[2m0x${n}[0m ${" ".repeat(--t.depth)}[33m${e.class.type.name}::[1m${e.name}[0m[0m`);
                  t.flush();
                }
              }
            });
          });
        };
        e.backtrace = function (t) {
          const o = e.domain.assemblies.flatMap(e => e.image.classes.flatMap(e => e.methods.filter(e => !e.virtualAddress.isNull()))).sort((e, t) => e.virtualAddress.compare(t.virtualAddress));
          const n = e => {
            let t = 0;
            let n = o.length - 1;
            while (t <= n) {
              const i = Math.floor((t + n) / 2);
              const a = o[i].virtualAddress.compare(e);
              if (a == 0) {
                return o[i];
              }
              if (a > 0) {
                n = i - 1;
              } else {
                t = i + 1;
              }
            }
            return o[n];
          };
          return new e.Tracer((o, i, a) => {
            Interceptor.attach(o.virtualAddress, function () {
              if (this.threadId == a) {
                const a = globalThis.Thread.backtrace(this.context, t);
                a.unshift(o.virtualAddress);
                for (const t of a) {
                  if (t.compare(e.module.base) > 0 && t.compare(e.module.base.add(e.module.size)) < 0) {
                    const e = n(t);
                    if (e) {
                      const o = t.sub(e.virtualAddress);
                      if (o.compare(4095) < 0) {
                        i.buffer.push(`[2m0x${e.relativeVirtualAddress.toString(16).padStart(8, "0")}[0m[2m+0x${o.toString(16).padStart(3, "0")}[0m ${e.class.type.name}::[1m${e.name}[0m`);
                      }
                    }
                  }
                }
                i.flush();
              }
            });
          });
        };
      })(Il2Cpp ||= {});
      (function (e) {
        class t extends NativeStruct {
          static get headerSize() {
            return e.corlib.class("System.Array").instanceSize;
          }
          get elements() {
            const t = e.string("v").object.method("ToCharArray", 0).invoke().handle.offsetOf(e => e.readS16() == 118) ?? raise("couldn't find the elements offset in the native array struct");
            getter(e.Array.prototype, "elements", function () {
              return new e.Pointer(this.handle.add(t), this.elementType);
            }, lazy);
            return this.elements;
          }
          get elementSize() {
            return this.elementType.class.arrayElementSize;
          }
          get elementType() {
            return this.object.class.type.class.baseType;
          }
          get length() {
            return e.exports.arrayGetLength(this);
          }
          get object() {
            return new e.Object(this);
          }
          get(e) {
            if (e < 0 || e >= this.length) {
              raise(`cannot get element at index ${e} as the array length is ${this.length}`);
            }
            return this.elements.get(e);
          }
          set(e, t) {
            if (e < 0 || e >= this.length) {
              raise(`cannot set element at index ${e} as the array length is ${this.length}`);
            }
            this.elements.set(e, t);
          }
          toString() {
            if (this.isNull()) {
              return "null";
            } else {
              return `[${this.elements.read(this.length, 0)}]`;
            }
          }
          *[Symbol.iterator]() {
            for (let e = 0; e < this.length; e++) {
              yield this.elements.get(e);
            }
          }
        }
        __decorate([lazy], t.prototype, "elementSize", null);
        __decorate([lazy], t.prototype, "elementType", null);
        __decorate([lazy], t.prototype, "length", null);
        __decorate([lazy], t.prototype, "object", null);
        __decorate([lazy], t, "headerSize", null);
        e.Array = t;
        e.array = function (t, o) {
          const n = typeof o == "number" ? o : o.length;
          const i = new e.Array(e.exports.arrayNew(t, n));
          if (globalThis.Array.isArray(o)) {
            i.elements.write(o);
          }
          return i;
        };
      })(Il2Cpp ||= {});
      (function (e) {
        let t = class extends NativeStruct {
          get image() {
            if (e.exports.assemblyGetImage.isNull()) {
              const t = this.object.tryMethod("GetType", 1)?.invoke(e.string("<Module>"))?.asNullable()?.tryMethod("get_Module")?.invoke() ?? this.object.tryMethod("GetModules", 1)?.invoke(false)?.get(0) ?? raise(`couldn't find the runtime module object of assembly ${this.name}`);
              return new e.Image(t.field("_impl").value);
            }
            return new e.Image(e.exports.assemblyGetImage(this));
          }
          get name() {
            return this.image.name.replace(".dll", "");
          }
          get object() {
            for (const t of e.domain.object.method("GetAssemblies", 1).invoke(false)) {
              if (t.field("_mono_assembly").value.equals(this)) {
                return t;
              }
            }
            raise("couldn't find the object of the native assembly struct");
          }
        };
        __decorate([lazy], t.prototype, "name", null);
        __decorate([lazy], t.prototype, "object", null);
        t = __decorate([recycle], t);
        e.Assembly = t;
      })(Il2Cpp ||= {});
      (function (e) {
        let t = class extends NativeStruct {
          get actualInstanceSize() {
            const t = e.corlib.class("System.String");
            const o = t.handle.offsetOf(e => e.readInt() == t.instanceSize - 2) ?? raise("couldn't find the actual instance size offset in the native class struct");
            getter(e.Class.prototype, "actualInstanceSize", function () {
              return this.handle.add(o).readS32();
            }, lazy);
            return this.actualInstanceSize;
          }
          get arrayClass() {
            return new e.Class(e.exports.classGetArrayClass(this, 1));
          }
          get arrayElementSize() {
            return e.exports.classGetArrayElementSize(this);
          }
          get assemblyName() {
            return e.exports.classGetAssemblyName(this).readUtf8String().replace(".dll", "");
          }
          get declaringClass() {
            return new e.Class(e.exports.classGetDeclaringType(this)).asNullable();
          }
          get baseType() {
            return new e.Type(e.exports.classGetBaseType(this)).asNullable();
          }
          get elementClass() {
            return new e.Class(e.exports.classGetElementClass(this)).asNullable();
          }
          get fields() {
            return readNativeIterator(t => e.exports.classGetFields(this, t)).map(t => new e.Field(t));
          }
          get flags() {
            return e.exports.classGetFlags(this);
          }
          get fullName() {
            if (this.namespace) {
              return `${this.namespace}.${this.name}`;
            } else {
              return this.name;
            }
          }
          get genericClass() {
            const e = this.image.tryClass(this.fullName)?.asNullable();
            if (e?.equals(this)) {
              return null;
            } else {
              return e ?? null;
            }
          }
          get generics() {
            if (!this.isGeneric && !this.isInflated) {
              return [];
            }
            const t = this.type.object.method("GetGenericArguments").invoke();
            return globalThis.Array.from(t).map(t => new e.Class(e.exports.classFromObject(t)));
          }
          get hasReferences() {
            return !!e.exports.classHasReferences(this);
          }
          get hasStaticConstructor() {
            const e = this.tryMethod(".cctor");
            return e != null && !e.virtualAddress.isNull();
          }
          get image() {
            return new e.Image(e.exports.classGetImage(this));
          }
          get instanceSize() {
            return e.exports.classGetInstanceSize(this);
          }
          get isAbstract() {
            return !!e.exports.classIsAbstract(this);
          }
          get isBlittable() {
            return !!e.exports.classIsBlittable(this);
          }
          get isEnum() {
            return !!e.exports.classIsEnum(this);
          }
          get isGeneric() {
            return !!e.exports.classIsGeneric(this);
          }
          get isInflated() {
            return !!e.exports.classIsInflated(this);
          }
          get isInterface() {
            return !!e.exports.classIsInterface(this);
          }
          get isStruct() {
            return this.isValueType && !this.isEnum;
          }
          get isValueType() {
            return !!e.exports.classIsValueType(this);
          }
          get interfaces() {
            return readNativeIterator(t => e.exports.classGetInterfaces(this, t)).map(t => new e.Class(t));
          }
          get methods() {
            return readNativeIterator(t => e.exports.classGetMethods(this, t)).map(t => new e.Method(t));
          }
          get name() {
            return e.exports.classGetName(this).readUtf8String();
          }
          get namespace() {
            return e.exports.classGetNamespace(this).readUtf8String() || undefined;
          }
          get nestedClasses() {
            return readNativeIterator(t => e.exports.classGetNestedClasses(this, t)).map(t => new e.Class(t));
          }
          get parent() {
            return new e.Class(e.exports.classGetParent(this)).asNullable();
          }
          get pointerClass() {
            return new e.Class(e.exports.classFromObject(this.type.object.method("MakePointerType").invoke()));
          }
          get rank() {
            let e = 0;
            const t = this.name;
            for (let o = this.name.length - 1; o > 0; o--) {
              const n = t[o];
              if (n == "]") {
                e++;
              } else {
                if (n == "[" || e == 0) {
                  break;
                }
                if (n != ",") {
                  break;
                }
                e++;
              }
            }
            return e;
          }
          get staticFieldsData() {
            return e.exports.classGetStaticFieldData(this);
          }
          get valueTypeSize() {
            return e.exports.classGetValueTypeSize(this, NULL);
          }
          get type() {
            return new e.Type(e.exports.classGetType(this));
          }
          alloc() {
            return new e.Object(e.exports.objectNew(this));
          }
          field(e) {
            return this.tryField(e) ?? raise(`couldn't find field ${e} in class ${this.type.name}`);
          }
          *hierarchy(e) {
            let t = e?.includeCurrent ?? 1 ? this : this.parent;
            while (t) {
              yield t;
              t = t.parent;
            }
          }
          inflate(...t) {
            if (!this.isGeneric) {
              raise(`cannot inflate class ${this.type.name} as it has no generic parameters`);
            }
            if (this.generics.length != t.length) {
              raise(`cannot inflate class ${this.type.name} as it needs ${this.generics.length} generic parameter(s), not ${t.length}`);
            }
            const o = t.map(e => e.type.object);
            const n = e.array(e.corlib.class("System.Type"), o);
            const i = this.type.object.method("MakeGenericType", 1).invoke(n);
            return new e.Class(e.exports.classFromObject(i));
          }
          initialize() {
            e.exports.classInitialize(this);
            return this;
          }
          isAssignableFrom(t) {
            return !!e.exports.classIsAssignableFrom(this, t);
          }
          isSubclassOf(t, o) {
            return !!e.exports.classIsSubclassOf(this, t, +o);
          }
          method(e, t = -1) {
            return this.tryMethod(e, t) ?? raise(`couldn't find method ${e} in class ${this.type.name}`);
          }
          nested(e) {
            return this.tryNested(e) ?? raise(`couldn't find nested class ${e} in class ${this.type.name}`);
          }
          new() {
            const t = this.alloc();
            const o = Memory.alloc(Process.pointerSize);
            e.exports.objectInitialize(t, o);
            const n = o.readPointer();
            if (!n.isNull()) {
              raise(new e.Object(n).toString());
            }
            return t;
          }
          tryField(t) {
            return new e.Field(e.exports.classGetFieldFromName(this, Memory.allocUtf8String(t))).asNullable();
          }
          tryMethod(t, o = -1) {
            return new e.Method(e.exports.classGetMethodFromName(this, Memory.allocUtf8String(t), o)).asNullable();
          }
          tryNested(e) {
            return this.nestedClasses.find(t => t.name == e);
          }
          toString() {
            const e = [this.parent].concat(this.interfaces);
            return `${this.isEnum ? "enum" : this.isStruct ? "struct" : this.isInterface ? "interface" : "class"} ${this.type.name}${e ? ` : ${e.map(e => e?.type.name).join(", ")}` : ""}\n{\n    ${this.fields.join("\n    ")}\n    ${this.methods.join("\n    ")}\n}`;
          }
          static enumerate(t) {
            const o = new NativeCallback(o => t(new e.Class(o)), "void", ["pointer", "pointer"]);
            return e.exports.classForEach(o, NULL);
          }
        };
        __decorate([lazy], t.prototype, "arrayClass", null);
        __decorate([lazy], t.prototype, "arrayElementSize", null);
        __decorate([lazy], t.prototype, "assemblyName", null);
        __decorate([lazy], t.prototype, "declaringClass", null);
        __decorate([lazy], t.prototype, "baseType", null);
        __decorate([lazy], t.prototype, "elementClass", null);
        __decorate([lazy], t.prototype, "fields", null);
        __decorate([lazy], t.prototype, "flags", null);
        __decorate([lazy], t.prototype, "fullName", null);
        __decorate([lazy], t.prototype, "generics", null);
        __decorate([lazy], t.prototype, "hasReferences", null);
        __decorate([lazy], t.prototype, "hasStaticConstructor", null);
        __decorate([lazy], t.prototype, "image", null);
        __decorate([lazy], t.prototype, "instanceSize", null);
        __decorate([lazy], t.prototype, "isAbstract", null);
        __decorate([lazy], t.prototype, "isBlittable", null);
        __decorate([lazy], t.prototype, "isEnum", null);
        __decorate([lazy], t.prototype, "isGeneric", null);
        __decorate([lazy], t.prototype, "isInflated", null);
        __decorate([lazy], t.prototype, "isInterface", null);
        __decorate([lazy], t.prototype, "isValueType", null);
        __decorate([lazy], t.prototype, "interfaces", null);
        __decorate([lazy], t.prototype, "methods", null);
        __decorate([lazy], t.prototype, "name", null);
        __decorate([lazy], t.prototype, "namespace", null);
        __decorate([lazy], t.prototype, "nestedClasses", null);
        __decorate([lazy], t.prototype, "parent", null);
        __decorate([lazy], t.prototype, "pointerClass", null);
        __decorate([lazy], t.prototype, "rank", null);
        __decorate([lazy], t.prototype, "staticFieldsData", null);
        __decorate([lazy], t.prototype, "valueTypeSize", null);
        __decorate([lazy], t.prototype, "type", null);
        t = __decorate([recycle], t);
        e.Class = t;
      })(Il2Cpp ||= {});
      (function (e) {
        e.delegate = function (t, o) {
          const n = e.corlib.class("System.Delegate");
          const i = e.corlib.class("System.MulticastDelegate");
          if (!n.isAssignableFrom(t)) {
            raise(`cannot create a delegate for ${t.type.name} as it's a non-delegate class`);
          }
          if (t.equals(n) || t.equals(i)) {
            raise(`cannot create a delegate for neither ${n.type.name} nor ${i.type.name}, use a subclass instead`);
          }
          const a = t.alloc();
          const l = a.handle.toString();
          const r = a.tryMethod("Invoke") ?? raise(`cannot create a delegate for ${t.type.name}, there is no Invoke method`);
          a.method(".ctor").invoke(a, r.handle);
          const s = r.wrap(o);
          a.field("method_ptr").value = s;
          a.field("invoke_impl").value = s;
          e._callbacksToKeepAlive[l] = s;
          return a;
        };
        e._callbacksToKeepAlive = {};
      })(Il2Cpp ||= {});
      (function (e) {
        let t = class extends NativeStruct {
          get assemblies() {
            let t = readNativeList(t => e.exports.domainGetAssemblies(this, t));
            if (t.length == 0) {
              const e = this.object.method("GetAssemblies").overload().invoke();
              t = globalThis.Array.from(e).map(e => e.field("_mono_assembly").value);
            }
            return t.map(t => new e.Assembly(t));
          }
          get object() {
            return e.corlib.class("System.AppDomain").method("get_CurrentDomain").invoke();
          }
          assembly(e) {
            return this.tryAssembly(e) ?? raise(`couldn't find assembly ${e}`);
          }
          attach() {
            return new e.Thread(e.exports.threadAttach(this));
          }
          tryAssembly(t) {
            return new e.Assembly(e.exports.domainGetAssemblyFromName(this, Memory.allocUtf8String(t))).asNullable();
          }
        };
        __decorate([lazy], t.prototype, "assemblies", null);
        __decorate([lazy], t.prototype, "object", null);
        t = __decorate([recycle], t);
        e.Domain = t;
        getter(e, "domain", () => new e.Domain(e.exports.domainGet()), lazy);
      })(Il2Cpp ||= {});
      (function (e) {
        class t extends NativeStruct {
          get class() {
            return new e.Class(e.exports.fieldGetClass(this));
          }
          get flags() {
            return e.exports.fieldGetFlags(this);
          }
          get isLiteral() {
            return !!(this.flags & 64);
          }
          get isStatic() {
            return !!(this.flags & 16);
          }
          get isThreadStatic() {
            const t = e.corlib.class("System.AppDomain").field("type_resolve_in_progress").offset;
            getter(e.Field.prototype, "isThreadStatic", function () {
              return this.offset == t;
            }, lazy);
            return this.isThreadStatic;
          }
          get modifier() {
            switch (this.flags & 7) {
              case 1:
                return "private";
              case 2:
                return "private protected";
              case 3:
                return "internal";
              case 4:
                return "protected";
              case 5:
                return "protected internal";
              case 6:
                return "public";
            }
          }
          get name() {
            return e.exports.fieldGetName(this).readUtf8String();
          }
          get offset() {
            return e.exports.fieldGetOffset(this);
          }
          get type() {
            return new e.Type(e.exports.fieldGetType(this));
          }
          get value() {
            if (!this.isStatic) {
              raise(`cannot access instance field ${this.class.type.name}::${this.name} from a class, use an object instead`);
            }
            const t = Memory.alloc(Process.pointerSize);
            e.exports.fieldGetStaticValue(this.handle, t);
            return e.read(t, this.type);
          }
          set value(t) {
            if (!this.isStatic) {
              raise(`cannot access instance field ${this.class.type.name}::${this.name} from a class, use an object instead`);
            }
            if (this.isThreadStatic || this.isLiteral) {
              raise(`cannot write the value of field ${this.name} as it's thread static or literal`);
            }
            const o = t instanceof e.Object && this.type.class.isValueType ? t.unbox() : t instanceof NativeStruct ? t.handle : t instanceof NativePointer ? t : e.write(Memory.alloc(this.type.class.valueTypeSize), t, this.type);
            e.exports.fieldSetStaticValue(this.handle, o);
          }
          toString() {
            return `${this.isThreadStatic ? "[ThreadStatic] " : ""}${this.isStatic ? "static " : ""}${this.type.name} ${this.name}${this.isLiteral ? ` = ${this.type.class.isEnum ? e.read(this.value.handle, this.type.class.baseType) : this.value}` : ""};${this.isThreadStatic || this.isLiteral ? "" : ` // 0x${this.offset.toString(16)}`}`;
          }
          bind(t) {
            if (this.isStatic) {
              raise(`cannot bind static field ${this.class.type.name}::${this.name} to an instance`);
            }
            const o = this.offset - (t instanceof e.ValueType ? e.Object.headerSize : 0);
            return new Proxy(this, {
              get: (n, i) => i == "value" ? e.read(t.handle.add(o), n.type) : Reflect.get(n, i),
              set: (n, i, a) => i == "value" ? (e.write(t.handle.add(o), a, n.type), true) : Reflect.set(n, i, a)
            });
          }
        }
        __decorate([lazy], t.prototype, "class", null);
        __decorate([lazy], t.prototype, "flags", null);
        __decorate([lazy], t.prototype, "isLiteral", null);
        __decorate([lazy], t.prototype, "isStatic", null);
        __decorate([lazy], t.prototype, "isThreadStatic", null);
        __decorate([lazy], t.prototype, "modifier", null);
        __decorate([lazy], t.prototype, "name", null);
        __decorate([lazy], t.prototype, "offset", null);
        __decorate([lazy], t.prototype, "type", null);
        e.Field = t;
      })(Il2Cpp ||= {});
      (function (e) {
        e.GCHandle = class {
          constructor(e) {
            this.handle = e;
          }
          get target() {
            return new e.Object(e.exports.gcHandleGetTarget(this.handle)).asNullable();
          }
          free() {
            return e.exports.gcHandleFree(this.handle);
          }
        };
      })(Il2Cpp ||= {});
      (function (e) {
        let t = class extends NativeStruct {
          get assembly() {
            return new e.Assembly(e.exports.imageGetAssembly(this));
          }
          get classCount() {
            if (e.unityVersionIsBelow201830) {
              return this.classes.length;
            } else {
              return e.exports.imageGetClassCount(this);
            }
          }
          get classes() {
            if (e.unityVersionIsBelow201830) {
              const t = this.assembly.object.method("GetTypes").invoke(false);
              const o = globalThis.Array.from(t, t => new e.Class(e.exports.classFromObject(t)));
              const n = this.tryClass("<Module>");
              if (n) {
                o.unshift(n);
              }
              return o;
            }
            return globalThis.Array.from(globalThis.Array(this.classCount), (t, o) => new e.Class(e.exports.imageGetClass(this, o)));
          }
          get name() {
            return e.exports.imageGetName(this).readUtf8String();
          }
          class(e) {
            return this.tryClass(e) ?? raise(`couldn't find class ${e} in assembly ${this.name}`);
          }
          tryClass(t) {
            const o = t.lastIndexOf(".");
            const n = Memory.allocUtf8String(o == -1 ? "" : t.slice(0, o));
            const i = Memory.allocUtf8String(t.slice(o + 1));
            return new e.Class(e.exports.classFromName(this, n, i)).asNullable();
          }
        };
        __decorate([lazy], t.prototype, "assembly", null);
        __decorate([lazy], t.prototype, "classCount", null);
        __decorate([lazy], t.prototype, "classes", null);
        __decorate([lazy], t.prototype, "name", null);
        t = __decorate([recycle], t);
        e.Image = t;
        getter(e, "corlib", () => new e.Image(e.exports.getCorlib()), lazy);
      })(Il2Cpp ||= {});
      (function (e) {
        class t extends NativeStruct {
          static capture() {
            return new e.MemorySnapshot();
          }
          constructor(t = e.exports.memorySnapshotCapture()) {
            super(t);
          }
          get classes() {
            return readNativeIterator(t => e.exports.memorySnapshotGetClasses(this, t)).map(t => new e.Class(t));
          }
          get objects() {
            return readNativeList(t => e.exports.memorySnapshotGetObjects(this, t)).filter(e => !e.isNull()).map(t => new e.Object(t));
          }
          free() {
            e.exports.memorySnapshotFree(this);
          }
        }
        __decorate([lazy], t.prototype, "classes", null);
        __decorate([lazy], t.prototype, "objects", null);
        e.MemorySnapshot = t;
        e.memorySnapshot = function (t) {
          const o = e.MemorySnapshot.capture();
          const n = t(o);
          o.free();
          return n;
        };
      })(Il2Cpp ||= {});
      (function (e) {
        class t extends NativeStruct {
          get class() {
            return new e.Class(e.exports.methodGetClass(this));
          }
          get flags() {
            return e.exports.methodGetFlags(this, NULL);
          }
          get implementationFlags() {
            const t = Memory.alloc(Process.pointerSize);
            e.exports.methodGetFlags(this, t);
            return t.readU32();
          }
          get fridaSignature() {
            const t = [];
            for (const e of this.parameters) {
              t.push(e.type.fridaAlias);
            }
            if (!this.isStatic || !!e.unityVersionIsBelow201830) {
              t.unshift("pointer");
            }
            if (this.isInflated) {
              t.push("pointer");
            }
            return t;
          }
          get generics() {
            if (!this.isGeneric && !this.isInflated) {
              return [];
            }
            const t = this.object.method("GetGenericArguments").invoke();
            return globalThis.Array.from(t).map(t => new e.Class(e.exports.classFromObject(t)));
          }
          get isExternal() {
            return !!(this.implementationFlags & 4096);
          }
          get isGeneric() {
            return !!e.exports.methodIsGeneric(this);
          }
          get isInflated() {
            return !!e.exports.methodIsInflated(this);
          }
          get isStatic() {
            return !e.exports.methodIsInstance(this);
          }
          get isSynchronized() {
            return !!(this.implementationFlags & 32);
          }
          get modifier() {
            switch (this.flags & 7) {
              case 1:
                return "private";
              case 2:
                return "private protected";
              case 3:
                return "internal";
              case 4:
                return "protected";
              case 5:
                return "protected internal";
              case 6:
                return "public";
            }
          }
          get name() {
            return e.exports.methodGetName(this).readUtf8String();
          }
          get nativeFunction() {
            return new NativeFunction(this.virtualAddress, this.returnType.fridaAlias, this.fridaSignature);
          }
          get object() {
            return new e.Object(e.exports.methodGetObject(this, NULL));
          }
          get parameterCount() {
            return e.exports.methodGetParameterCount(this);
          }
          get parameters() {
            return globalThis.Array.from(globalThis.Array(this.parameterCount), (t, o) => {
              const n = e.exports.methodGetParameterName(this, o).readUtf8String();
              const i = e.exports.methodGetParameterType(this, o);
              return new e.Parameter(n, o, new e.Type(i));
            });
          }
          get relativeVirtualAddress() {
            return this.virtualAddress.sub(e.module.base);
          }
          get returnType() {
            return new e.Type(e.exports.methodGetReturnType(this));
          }
          get virtualAddress() {
            const t = e.corlib.class("System.Reflection.Module").initialize().field("FilterTypeName").value;
            const o = t.field("method_ptr").value;
            const n = t.field("method").value.offsetOf(e => e.readPointer().equals(o)) ?? raise("couldn't find the virtual address offset in the native method struct");
            getter(e.Method.prototype, "virtualAddress", function () {
              return this.handle.add(n).readPointer();
            }, lazy);
            e.corlib.class("System.Reflection.Module").method(".cctor").invoke();
            return this.virtualAddress;
          }
          set implementation(e) {
            try {
              Interceptor.replace(this.virtualAddress, this.wrap(e));
            } catch (e) {
              switch (e.message) {
                case "access violation accessing 0x0":
                  raise(`couldn't set implementation for method ${this.name} as it has a NULL virtual address`);
                case /unable to intercept function at \w+; please file a bug/.exec(e.message)?.input:
                  warn(`couldn't set implementation for method ${this.name} as it may be a thunk`);
                  break;
                case "already replaced this function":
                  warn(`couldn't set implementation for method ${this.name} as it has already been replaced by a thunk`);
                  break;
                default:
                  throw e;
              }
            }
          }
          inflate(...t) {
            if (!this.isGeneric || this.generics.length != t.length) {
              for (const e of this.overloads()) {
                if (e.isGeneric && e.generics.length == t.length) {
                  return e.inflate(...t);
                }
              }
              raise(`could not find inflatable signature of method ${this.name} with ${t.length} generic parameter(s)`);
            }
            const o = t.map(e => e.type.object);
            const n = e.array(e.corlib.class("System.Type"), o);
            const i = this.object.method("MakeGenericMethod", 1).invoke(n);
            return new e.Method(i.field("mhandle").value);
          }
          invoke(...e) {
            if (!this.isStatic) {
              raise(`cannot invoke non-static method ${this.name} as it must be invoked throught a Il2Cpp.Object, not a Il2Cpp.Class`);
            }
            return this.invokeRaw(NULL, ...e);
          }
          invokeRaw(t, ...o) {
            const n = o.map(e.toFridaValue);
            if (!this.isStatic || !!e.unityVersionIsBelow201830) {
              n.unshift(t);
            }
            if (this.isInflated) {
              n.push(this.handle);
            }
            try {
              const t = this.nativeFunction(...n);
              return e.fromFridaValue(t, this.returnType);
            } catch (e) {
              if (e == null) {
                raise("an unexpected native invocation exception occurred, this is due to parameter types mismatch");
              }
              switch (e.message) {
                case "bad argument count":
                  raise(`couldn't invoke method ${this.name} as it needs ${this.parameterCount} parameter(s), not ${o.length}`);
                case "expected a pointer":
                case "expected number":
                case "expected array with fields":
                  raise(`couldn't invoke method ${this.name} using incorrect parameter types`);
                case "breakpoint triggered":
                case "access violation accessing 0x0":
                  return;
              }
              if (e.message && e.message.startsWith("access violation")) {
                return;
              }
              throw e;
            }
          }
          overload(...t) {
            return this.tryOverload(...t) ?? raise(`couldn't find overloaded method ${this.name}(${t.map(t => t instanceof e.Class ? t.type.name : t)})`);
          }
          *overloads() {
            for (const e of this.class.hierarchy()) {
              for (const t of e.methods) {
                if (this.name == t.name) {
                  yield t;
                }
              }
            }
          }
          parameter(e) {
            return this.tryParameter(e) ?? raise(`couldn't find parameter ${e} in method ${this.name}`);
          }
          revert() {
            Interceptor.revert(this.virtualAddress);
            Interceptor.flush();
          }
          tryOverload(...t) {
            const o = t.length * 1;
            const n = t.length * 2;
            let i;
            e: for (const a of this.overloads()) {
              if (a.parameterCount != t.length) {
                continue;
              }
              let l = 0;
              let r = 0;
              for (const o of a.parameters) {
                const n = t[r];
                if (n instanceof e.Class) {
                  if (o.type.is(n.type)) {
                    l += 2;
                  } else {
                    if (!o.type.class.isAssignableFrom(n)) {
                      continue e;
                    }
                    l += 1;
                  }
                } else {
                  if (o.type.name != n) {
                    continue e;
                  }
                  l += 2;
                }
                r++;
              }
              if (!(l < o)) {
                if (l == n) {
                  return a;
                }
                if (i == null || l > i[0]) {
                  i = [l, a];
                } else if (l == i[0]) {
                  let e = 0;
                  for (const t of i[1].parameters) {
                    if (t.type.class.isAssignableFrom(a.parameters[e].type.class)) {
                      i = [l, a];
                      continue e;
                    }
                    e++;
                  }
                }
              }
            }
            return i?.[1];
          }
          tryParameter(e) {
            return this.parameters.find(t => t.name == e);
          }
          toString() {
            return `${this.isStatic ? "static " : ""}${this.returnType.name} ${this.name}${this.generics.length > 0 ? `<${this.generics.map(e => e.type.name).join(",")}>` : ""}(${this.parameters.join(", ")});${this.virtualAddress.isNull() ? "" : ` // 0x${this.relativeVirtualAddress.toString(16).padStart(8, "0")}`}`;
          }
          bind(t) {
            if (this.isStatic) {
              raise(`cannot bind static method ${this.class.type.name}::${this.name} to an instance`);
            }
            return new Proxy(this, {
              get(n, i, a) {
                switch (i) {
                  case "invoke":
                    const l = t instanceof e.ValueType ? n.class.isValueType ? t.handle.sub(o() ? e.Object.headerSize : 0) : raise(`cannot invoke method ${n.class.type.name}::${n.name} against a value type, you must box it first`) : n.class.isValueType ? t.handle.add(o() ? 0 : e.Object.headerSize) : t.handle;
                    return n.invokeRaw.bind(n, l);
                  case "overloads":
                    return function* () {
                      for (const e of n[i]()) {
                        if (!e.isStatic) {
                          yield e;
                        }
                      }
                    };
                  case "inflate":
                  case "overload":
                  case "tryOverload":
                    const r = Reflect.get(n, i).bind(a);
                    return function (...e) {
                      return r(...e)?.bind(t);
                    };
                }
                return Reflect.get(n, i);
              }
            });
          }
          wrap(t) {
            const n = +!this.isStatic | +e.unityVersionIsBelow201830;
            return new NativeCallback((...i) => {
              const a = this.isStatic ? this.class : this.class.isValueType ? new e.ValueType(i[0].add(o() ? e.Object.headerSize : 0), this.class.type) : new e.Object(i[0]);
              const l = this.parameters.map((t, o) => e.fromFridaValue(i[o + n], t.type));
              const r = t.call(a, ...l);
              return e.toFridaValue(r);
            }, this.returnType.fridaAlias, this.fridaSignature);
          }
        }
        __decorate([lazy], t.prototype, "class", null);
        __decorate([lazy], t.prototype, "flags", null);
        __decorate([lazy], t.prototype, "implementationFlags", null);
        __decorate([lazy], t.prototype, "fridaSignature", null);
        __decorate([lazy], t.prototype, "generics", null);
        __decorate([lazy], t.prototype, "isExternal", null);
        __decorate([lazy], t.prototype, "isGeneric", null);
        __decorate([lazy], t.prototype, "isInflated", null);
        __decorate([lazy], t.prototype, "isStatic", null);
        __decorate([lazy], t.prototype, "isSynchronized", null);
        __decorate([lazy], t.prototype, "modifier", null);
        __decorate([lazy], t.prototype, "name", null);
        __decorate([lazy], t.prototype, "nativeFunction", null);
        __decorate([lazy], t.prototype, "object", null);
        __decorate([lazy], t.prototype, "parameterCount", null);
        __decorate([lazy], t.prototype, "parameters", null);
        __decorate([lazy], t.prototype, "relativeVirtualAddress", null);
        __decorate([lazy], t.prototype, "returnType", null);
        e.Method = t;
        let o = () => {
          const t = e.corlib.class("System.Int64").alloc();
          t.field("m_value").value = 3735928559;
          const n = t.method("Equals", 1).overload(t.class).invokeRaw(t, 3735928559);
          return (o = () => n)();
        };
      })(Il2Cpp ||= {});
      (function (e) {
        class t extends NativeStruct {
          static get headerSize() {
            return e.corlib.class("System.Object").instanceSize;
          }
          get base() {
            if (this.class.parent == null) {
              raise(`class ${this.class.type.name} has no parent`);
            }
            return new Proxy(this, {
              get: (t, o, n) => o == "class" ? Reflect.get(t, o).parent : o == "base" ? Reflect.getOwnPropertyDescriptor(e.Object.prototype, o).get.bind(n)() : Reflect.get(t, o)
            });
          }
          get class() {
            return new e.Class(e.exports.objectGetClass(this));
          }
          get monitor() {
            return new e.Object.Monitor(this);
          }
          get size() {
            return e.exports.objectGetSize(this);
          }
          field(e) {
            return this.tryField(e) ?? raise(`couldn't find non-static field ${e} in hierarchy of class ${this.class.type.name}`);
          }
          method(e, t = -1) {
            return this.tryMethod(e, t) ?? raise(`couldn't find non-static method ${e} in hierarchy of class ${this.class.type.name}`);
          }
          ref(t) {
            return new e.GCHandle(e.exports.gcHandleNew(this, +t));
          }
          virtualMethod(t) {
            return new e.Method(e.exports.objectGetVirtualMethod(this, t)).bind(this);
          }
          tryField(e) {
            const t = this.class.tryField(e);
            if (!t?.isStatic) {
              return t?.bind(this);
            }
            for (const t of this.class.hierarchy({
              includeCurrent: false
            })) {
              for (const o of t.fields) {
                if (o.name == e && !o.isStatic) {
                  return o.bind(this);
                }
              }
            }
          }
          tryMethod(e, t = -1) {
            const o = this.class.tryMethod(e, t);
            if (!o?.isStatic) {
              return o?.bind(this);
            }
            for (const o of this.class.hierarchy()) {
              for (const n of o.methods) {
                if (n.name == e && !n.isStatic && (t < 0 || n.parameterCount == t)) {
                  return n.bind(this);
                }
              }
            }
          }
          toString() {
            if (this.isNull()) {
              return "null";
            } else {
              return this.method("ToString", 0).invoke().content ?? "null";
            }
          }
          unbox() {
            if (this.class.isValueType) {
              return new e.ValueType(e.exports.objectUnbox(this), this.class.type);
            } else {
              return raise(`couldn't unbox instances of ${this.class.type.name} as they are not value types`);
            }
          }
          weakRef(t) {
            return new e.GCHandle(e.exports.gcHandleNewWeakRef(this, +t));
          }
        }
        __decorate([lazy], t.prototype, "class", null);
        __decorate([lazy], t.prototype, "size", null);
        __decorate([lazy], t, "headerSize", null);
        e.Object = t;
        (t = e.Object ||= {}).Monitor = class {
          constructor(e) {
            this.handle = e;
          }
          enter() {
            return e.exports.monitorEnter(this.handle);
          }
          exit() {
            return e.exports.monitorExit(this.handle);
          }
          pulse() {
            return e.exports.monitorPulse(this.handle);
          }
          pulseAll() {
            return e.exports.monitorPulseAll(this.handle);
          }
          tryEnter(t) {
            return !!e.exports.monitorTryEnter(this.handle, t);
          }
          tryWait(t) {
            return !!e.exports.monitorTryWait(this.handle, t);
          }
          wait() {
            return e.exports.monitorWait(this.handle);
          }
        };
      })(Il2Cpp ||= {});
      (function (e) {
        e.Parameter = class {
          constructor(e, t, o) {
            this.name = e;
            this.position = t;
            this.type = o;
          }
          toString() {
            return `${this.type.name} ${this.name}`;
          }
        };
      })(Il2Cpp ||= {});
      (function (e) {
        e.Pointer = class extends NativeStruct {
          constructor(e, t) {
            super(e);
            this.type = t;
          }
          get(t) {
            return e.read(this.handle.add(t * this.type.class.arrayElementSize), this.type);
          }
          read(e, t = 0) {
            const o = new globalThis.Array(e);
            for (let n = 0; n < e; n++) {
              o[n] = this.get(n + t);
            }
            return o;
          }
          set(t, o) {
            e.write(this.handle.add(t * this.type.class.arrayElementSize), o, this.type);
          }
          toString() {
            return this.handle.toString();
          }
          write(e, t = 0) {
            for (let o = 0; o < e.length; o++) {
              this.set(o + t, e[o]);
            }
          }
        };
      })(Il2Cpp ||= {});
      (function (e) {
        e.Reference = class extends NativeStruct {
          constructor(e, t) {
            super(e);
            this.type = t;
          }
          get value() {
            return e.read(this.handle, this.type);
          }
          set value(t) {
            e.write(this.handle, t, this.type);
          }
          toString() {
            if (this.isNull()) {
              return "null";
            } else {
              return `->${this.value}`;
            }
          }
        };
        e.reference = function (t, o) {
          const n = Memory.alloc(Process.pointerSize);
          switch (typeof t) {
            case "boolean":
              return new e.Reference(n.writeS8(+t), e.corlib.class("System.Boolean").type);
            case "number":
              switch (o?.enumValue) {
                case e.Type.Enum.UBYTE:
                  return new e.Reference(n.writeU8(t), o);
                case e.Type.Enum.BYTE:
                  return new e.Reference(n.writeS8(t), o);
                case e.Type.Enum.CHAR:
                case e.Type.Enum.USHORT:
                  return new e.Reference(n.writeU16(t), o);
                case e.Type.Enum.SHORT:
                  return new e.Reference(n.writeS16(t), o);
                case e.Type.Enum.UINT:
                  return new e.Reference(n.writeU32(t), o);
                case e.Type.Enum.INT:
                  return new e.Reference(n.writeS32(t), o);
                case e.Type.Enum.ULONG:
                  return new e.Reference(n.writeU64(t), o);
                case e.Type.Enum.LONG:
                  return new e.Reference(n.writeS64(t), o);
                case e.Type.Enum.FLOAT:
                  return new e.Reference(n.writeFloat(t), o);
                case e.Type.Enum.DOUBLE:
                  return new e.Reference(n.writeDouble(t), o);
              }
            case "object":
              if (t instanceof e.ValueType || t instanceof e.Pointer) {
                return new e.Reference(t.handle, t.type);
              }
              if (t instanceof e.Object) {
                return new e.Reference(n.writePointer(t), t.class.type);
              }
              if (t instanceof e.String || t instanceof e.Array) {
                return new e.Reference(n.writePointer(t), t.object.class.type);
              }
              if (t instanceof NativePointer) {
                switch (o?.enumValue) {
                  case e.Type.Enum.NUINT:
                  case e.Type.Enum.NINT:
                    return new e.Reference(n.writePointer(t), o);
                }
              } else {
                if (t instanceof Int64) {
                  return new e.Reference(n.writeS64(t), e.corlib.class("System.Int64").type);
                }
                if (t instanceof UInt64) {
                  return new e.Reference(n.writeU64(t), e.corlib.class("System.UInt64").type);
                }
              }
            default:
              raise(`couldn't create a reference to ${t} using an unhandled type ${o?.name}`);
          }
        };
      })(Il2Cpp ||= {});
      (function (e) {
        e.String = class extends NativeStruct {
          get content() {
            return e.exports.stringGetChars(this).readUtf16String(this.length);
          }
          set content(t) {
            const o = e.string("vfsfitvnm").handle.offsetOf(e => e.readInt() == 9) ?? raise("couldn't find the length offset in the native string struct");
            globalThis.Object.defineProperty(e.String.prototype, "content", {
              set(t) {
                e.exports.stringGetChars(this).writeUtf16String(t ?? "");
                this.handle.add(o).writeS32(t?.length ?? 0);
              }
            });
            this.content = t;
          }
          get length() {
            return e.exports.stringGetLength(this);
          }
          get object() {
            return new e.Object(this);
          }
          toString() {
            if (this.isNull()) {
              return "null";
            } else {
              return `"${this.content}"`;
            }
          }
        };
        e.string = function (t) {
          return new e.String(e.exports.stringNew(Memory.allocUtf8String(t ?? "")));
        };
      })(Il2Cpp ||= {});
      (function (e) {
        class t extends NativeStruct {
          get id() {
            let t = function () {
              return this.internal.field("thread_id").value.toNumber();
            };
            if (Process.platform != "windows") {
              const o = Process.getCurrentThreadId();
              const n = ptr(t.apply(e.currentThread)).offsetOf(e => e.readS32() == o, 1024) ?? raise("couldn't find the offset for determining the kernel id of a posix thread");
              const i = t;
              t = function () {
                return ptr(i.apply(this)).add(n).readS32();
              };
            }
            getter(e.Thread.prototype, "id", t, lazy);
            return this.id;
          }
          get internal() {
            return this.object.tryField("internal_thread")?.value ?? this.object;
          }
          get isFinalizer() {
            return !e.exports.threadIsVm(this);
          }
          get managedId() {
            return this.object.method("get_ManagedThreadId").invoke();
          }
          get object() {
            return new e.Object(this);
          }
          get staticData() {
            return this.internal.field("static_data").value;
          }
          get synchronizationContext() {
            const t = (this.object.tryMethod("GetMutableExecutionContext") ?? this.object.method("get_ExecutionContext")).invoke();
            const o = t.tryField("_syncContext")?.value ?? t.tryMethod("get_SynchronizationContext")?.invoke() ?? this.tryLocalValue(e.corlib.class("System.Threading.SynchronizationContext"));
            return o?.asNullable() ?? null;
          }
          detach() {
            return e.exports.threadDetach(this);
          }
          schedule(t) {
            const o = this.synchronizationContext?.tryMethod("Post");
            if (o == null) {
              return Process.runOnThread(this.id, t);
            } else {
              return new Promise(n => {
                const i = e.delegate(e.corlib.class("System.Threading.SendOrPostCallback"), () => {
                  const e = t();
                  setImmediate(() => n(e));
                });
                Script.bindWeak(globalThis, () => {
                  i.field("method_ptr").value = i.field("invoke_impl").value = e.exports.domainGet;
                });
                o.invoke(i, NULL);
              });
            }
          }
          tryLocalValue(t) {
            for (let o = 0; o < 16; o++) {
              const n = this.staticData.add(o * Process.pointerSize).readPointer();
              if (!n.isNull()) {
                const o = new e.Object(n.readPointer()).asNullable();
                if (o?.class?.isSubclassOf(t, false)) {
                  return o;
                }
              }
            }
          }
        }
        __decorate([lazy], t.prototype, "internal", null);
        __decorate([lazy], t.prototype, "isFinalizer", null);
        __decorate([lazy], t.prototype, "managedId", null);
        __decorate([lazy], t.prototype, "object", null);
        __decorate([lazy], t.prototype, "staticData", null);
        __decorate([lazy], t.prototype, "synchronizationContext", null);
        e.Thread = t;
        getter(e, "attachedThreads", () => {
          if (e.exports.threadGetAttachedThreads.isNull()) {
            const t = e.currentThread?.handle ?? raise("Current thread is not attached to IL2CPP");
            const o = t.toMatchPattern();
            const n = [];
            for (const i of Process.enumerateRanges("rw-")) {
              if (i.file == null) {
                let a = [];
                try {
                  a = Memory.scanSync(i.base, i.size, o);
                } catch (e) {}
                if (a.length == 1) {
                  while (true) {
                    const o = a[0].address.sub(a[0].size * n.length).readPointer();
                    if (o.isNull() || !o.readPointer().equals(t.readPointer())) {
                      break;
                    }
                    n.unshift(new e.Thread(o));
                  }
                  break;
                }
              }
            }
            return n;
          }
          return readNativeList(e.exports.threadGetAttachedThreads).map(t => new e.Thread(t));
        });
        getter(e, "currentThread", () => new e.Thread(e.exports.threadGetCurrent()).asNullable());
        getter(e, "mainThread", () => e.attachedThreads[0]);
      })(Il2Cpp ||= {});
      (function (e) {
        let t = class extends NativeStruct {
          static get Enum() {
            const t = (t, o = e => e) => o(e.corlib.class(t)).type.enumValue;
            const o = {
              VOID: t("System.Void"),
              BOOLEAN: t("System.Boolean"),
              CHAR: t("System.Char"),
              BYTE: t("System.SByte"),
              UBYTE: t("System.Byte"),
              SHORT: t("System.Int16"),
              USHORT: t("System.UInt16"),
              INT: t("System.Int32"),
              UINT: t("System.UInt32"),
              LONG: t("System.Int64"),
              ULONG: t("System.UInt64"),
              NINT: t("System.IntPtr"),
              NUINT: t("System.UIntPtr"),
              FLOAT: t("System.Single"),
              DOUBLE: t("System.Double"),
              POINTER: t("System.IntPtr", e => e.field("m_value")),
              VALUE_TYPE: t("System.Decimal"),
              OBJECT: t("System.Object"),
              STRING: t("System.String"),
              CLASS: t("System.Array"),
              ARRAY: t("System.Void", e => e.arrayClass),
              NARRAY: t("System.Void", t => new e.Class(e.exports.classGetArrayClass(t, 2))),
              GENERIC_INSTANCE: t("System.Int32", e => e.interfaces.find(e => e.name.endsWith("`1")))
            };
            Reflect.defineProperty(this, "Enum", {
              value: o
            });
            return addFlippedEntries({
              ...o,
              VAR: t("System.Action`1", e => e.generics[0]),
              MVAR: t("System.Array", e => e.method("AsReadOnly", 1).generics[0])
            });
          }
          get class() {
            return new e.Class(e.exports.typeGetClass(this));
          }
          get fridaAlias() {
            function t(e) {
              const t = e.class.fields.filter(e => !e.isStatic);
              if (t.length == 0) {
                return ["char"];
              } else {
                return t.map(e => e.type.fridaAlias);
              }
            }
            if (this.isByReference) {
              return "pointer";
            }
            switch (this.enumValue) {
              case e.Type.Enum.VOID:
                return "void";
              case e.Type.Enum.BOOLEAN:
                return "bool";
              case e.Type.Enum.CHAR:
                return "uchar";
              case e.Type.Enum.BYTE:
                return "int8";
              case e.Type.Enum.UBYTE:
                return "uint8";
              case e.Type.Enum.SHORT:
                return "int16";
              case e.Type.Enum.USHORT:
                return "uint16";
              case e.Type.Enum.INT:
                return "int32";
              case e.Type.Enum.UINT:
                return "uint32";
              case e.Type.Enum.LONG:
                return "int64";
              case e.Type.Enum.ULONG:
                return "uint64";
              case e.Type.Enum.FLOAT:
                return "float";
              case e.Type.Enum.DOUBLE:
                return "double";
              case e.Type.Enum.NINT:
              case e.Type.Enum.NUINT:
              case e.Type.Enum.POINTER:
              case e.Type.Enum.STRING:
              case e.Type.Enum.ARRAY:
              case e.Type.Enum.NARRAY:
                return "pointer";
              case e.Type.Enum.VALUE_TYPE:
                if (this.class.isEnum) {
                  return this.class.baseType.fridaAlias;
                } else {
                  return t(this);
                }
              case e.Type.Enum.CLASS:
              case e.Type.Enum.OBJECT:
              case e.Type.Enum.GENERIC_INSTANCE:
                if (this.class.isStruct) {
                  return t(this);
                } else if (this.class.isEnum) {
                  return this.class.baseType.fridaAlias;
                } else {
                  return "pointer";
                }
              default:
                return "pointer";
            }
          }
          get isByReference() {
            return this.name.endsWith("&");
          }
          get isPrimitive() {
            switch (this.enumValue) {
              case e.Type.Enum.BOOLEAN:
              case e.Type.Enum.CHAR:
              case e.Type.Enum.BYTE:
              case e.Type.Enum.UBYTE:
              case e.Type.Enum.SHORT:
              case e.Type.Enum.USHORT:
              case e.Type.Enum.INT:
              case e.Type.Enum.UINT:
              case e.Type.Enum.LONG:
              case e.Type.Enum.ULONG:
              case e.Type.Enum.FLOAT:
              case e.Type.Enum.DOUBLE:
              case e.Type.Enum.NINT:
              case e.Type.Enum.NUINT:
                return true;
              default:
                return false;
            }
          }
          get name() {
            try {
              const t = e.exports.typeGetName(this);
              try {
                return t.readUtf8String();
              } finally {
                e.free(t);
              }
            } catch {
              return "Error: ToString failed";
            }
          }
          get object() {
            return new e.Object(e.exports.typeGetObject(this));
          }
          get enumValue() {
            return e.exports.typeGetTypeEnum(this);
          }
          is(t) {
            if (e.exports.typeEquals.isNull()) {
              return this.object.method("Equals").invoke(t.object);
            } else {
              return !!e.exports.typeEquals(this, t);
            }
          }
          toString() {
            return this.name;
          }
        };
        __decorate([lazy], t.prototype, "class", null);
        __decorate([lazy], t.prototype, "fridaAlias", null);
        __decorate([lazy], t.prototype, "isByReference", null);
        __decorate([lazy], t.prototype, "isPrimitive", null);
        __decorate([lazy], t.prototype, "name", null);
        __decorate([lazy], t.prototype, "object", null);
        __decorate([lazy], t.prototype, "enumValue", null);
        __decorate([lazy], t, "Enum", null);
        t = __decorate([recycle], t);
        e.Type = t;
      })(Il2Cpp ||= {});
      (function (e) {
        e.ValueType = class extends NativeStruct {
          constructor(e, t) {
            super(e);
            this.type = t;
          }
          box() {
            return new e.Object(e.exports.valueTypeBox(this.type.class, this));
          }
          field(e) {
            return this.tryField(e) ?? raise(`couldn't find non-static field ${e} in hierarchy of class ${this.type.name}`);
          }
          method(e, t = -1) {
            return this.tryMethod(e, t) ?? raise(`couldn't find non-static method ${e} in hierarchy of class ${this.type.name}`);
          }
          tryField(e) {
            const t = this.type.class.tryField(e);
            if (!t?.isStatic) {
              return t?.bind(this);
            }
            for (const t of this.type.class.hierarchy()) {
              for (const o of t.fields) {
                if (o.name == e && !o.isStatic) {
                  return o.bind(this);
                }
              }
            }
          }
          tryMethod(e, t = -1) {
            const o = this.type.class.tryMethod(e, t);
            if (!o?.isStatic) {
              return o?.bind(this);
            }
            for (const o of this.type.class.hierarchy()) {
              for (const n of o.methods) {
                if (n.name == e && !n.isStatic && (t < 0 || n.parameterCount == t)) {
                  return n.bind(this);
                }
              }
            }
          }
          toString() {
            const e = this.method("ToString", 0);
            if (this.isNull()) {
              return "null";
            } else if (e.class.isValueType) {
              return e.invoke().content ?? "null";
            } else {
              return this.box().toString() ?? "null";
            }
          }
        };
      })(Il2Cpp ||= {});
      globalThis.Il2Cpp = Il2Cpp;
      var IL2CPP_EXPORTS_URL = "https://raw.githubusercontent.com/Banban465-tech/ac-symbols/refs/heads/main/symbols.ts";
      function parseRemoteIl2CppExports(e) {
        const t = e.replace(/\r/g, "").trim().split("\n");
        if (t.shift()?.trim() !== "Il2Cpp.$config.exports = {") {
          throw new Error("Unexpected Pastebin header");
        }
        if (t.pop()?.trim() !== "};") {
          throw new Error("Unexpected Pastebin footer");
        }
        const o = {};
        const n = /^(il2cpp_[A-Za-z0-9_]+):\s*\(\)\s*=>\s*Il2Cpp\.module\.findExportByName\("([A-Za-z0-9_$]+)"\),?$/;
        for (const e of t) {
          const t = e.trim();
          if (!t) {
            continue;
          }
          const i = n.exec(t);
          if (!i) {
            throw new Error(`Unexpected Pastebin line: ${t}`);
          }
          const a = i[1];
          const l = i[2];
          o[a] = () => Il2Cpp.module.findExportByName(l);
        }
        const i = Object.keys(o).length;
        if (i === 0) {
          throw new Error("Pastebin export map is empty");
        }
        console.log(`[System] Loaded ${i} Il2Cpp exports.`);
        return o;
      }
      async function fetchRemoteIl2CppExports() {
        if (Process.platform !== "windows") {
          throw new Error(`WinHTTP loader cannot run on ${Process.platform}`);
        }
        const e = Process.findModuleByName("winhttp.dll") ?? Module.load("winhttp.dll");
        const t = Process.getModuleByName("kernel32.dll");
        const o = (t, o, n) => new NativeFunction(e.getExportByName(t), o, n);
        const n = o("WinHttpOpen", "pointer", ["pointer", "uint32", "pointer", "pointer", "uint32"]);
        const i = o("WinHttpConnect", "pointer", ["pointer", "pointer", "uint16", "uint32"]);
        const a = o("WinHttpOpenRequest", "pointer", ["pointer", "pointer", "pointer", "pointer", "pointer", "pointer", "uint32"]);
        const l = o("WinHttpSendRequest", "bool", ["pointer", "pointer", "uint32", "pointer", "uint32", "uint32", "pointer"]);
        const r = o("WinHttpReceiveResponse", "bool", ["pointer", "pointer"]);
        const s = o("WinHttpQueryDataAvailable", "bool", ["pointer", "pointer"]);
        const c = o("WinHttpReadData", "bool", ["pointer", "pointer", "uint32", "pointer"]);
        const d = o("WinHttpCloseHandle", "bool", ["pointer"]);
        const u = new NativeFunction(t.getExportByName("GetLastError"), "uint32", []);
        const m = e => {
          throw new Error(`${e} failed (Win32 error ${u()})`);
        };
        let h = NULL;
        let g = NULL;
        let f = NULL;
        try {
          h = n(Memory.allocUtf16String("solarhp/1.0"), 0, NULL, NULL, 0);
          if (h.isNull()) {
            m("WinHttpOpen");
          }
          g = i(h, Memory.allocUtf16String("pastebin.com"), 443, 0);
          if (g.isNull()) {
            m("WinHttpConnect");
          }
          f = a(g, Memory.allocUtf16String("GET"), Memory.allocUtf16String("/raw/s212As5A"), NULL, NULL, NULL, 8388608);
          if (f.isNull()) {
            m("WinHttpOpenRequest");
          }
          if (!l(f, NULL, 0, NULL, 0, 0, NULL)) {
            m("WinHttpSendRequest");
          }
          if (!r(f, NULL)) {
            m("WinHttpReceiveResponse");
          }
          const e = Memory.alloc(4);
          const t = Memory.alloc(4);
          const o = [];
          while (true) {
            e.writeU32(0);
            if (!s(f, e)) {
              m("WinHttpQueryDataAvailable");
            }
            const n = e.readU32();
            if (n === 0) {
              break;
            }
            const i = Memory.alloc(n + 1);
            t.writeU32(0);
            if (!c(f, i, n, t)) {
              m("WinHttpReadData");
            }
            const a = t.readU32();
            if (a === 0) {
              break;
            }
            i.add(a).writeU8(0);
            o.push(i.readUtf8String(a) ?? "");
          }
          Il2Cpp.$config.exports = parseRemoteIl2CppExports(o.join(""));
        } finally {
          if (!f.isNull()) {
            d(f);
          }
          if (!g.isNull()) {
            d(g);
          }
          if (!h.isNull()) {
            d(h);
          }
        }
      }
      function normalizePlayerToken(e) {
        try {
          if (e == null) {
            return "";
          } else if (typeof e == "string") {
            return e.trim();
          } else if (typeof e == "number" || typeof e == "boolean") {
            return String(e).trim();
          } else if (e.content != null) {
            return String(e.content).trim();
          } else {
            return String(e).trim();
          }
        } catch (e) {
          return "";
        }
      }
      function normalizeWhitelistToken(e) {
        return normalizePlayerToken(e).toLowerCase();
      }
      function getPlayerIdentityInfo(e) {
        const t = [];
        const o = e => {
          const o = normalizePlayerToken(e);
          if (o && o !== "?" && !t.includes(o)) {
            t.push(o);
          }
        };
        try {
          o(e.method("get_playerId").invoke());
        } catch (e) {}
        try {
          o(e.field("_playerId").value);
        } catch (e) {}
        try {
          o(e.method("get_displayName").invoke());
        } catch (e) {}
        try {
          o(e.field("_displayName").value);
        } catch (e) {}
        try {
          o(e.method("get_name").invoke());
        } catch (e) {}
        try {
          o(e.method("ToString").invoke());
        } catch (e) {}
        try {
          if (t.length === 0) {
            const t = e.method("get_gameObject").invoke()?.method("get_name").invoke();
            o(t);
          }
        } catch (e) {}
        const n = t[0] ?? "?";
        return {
          key: n,
          label: t.length > 1 ? t[1] : n,
          aliases: t.length > 0 ? t : ["?"]
        };
      }
      function getPlayerName(e) {
        return getPlayerIdentityInfo(e).label;
      }
      function whitelistHasPlayer(e) {
        try {
          return getPlayerIdentityInfo(e).aliases.some(e => whitelist.includes(normalizeWhitelistToken(e)));
        } catch (e) {
          return false;
        }
      }
      function normalizeSceneObjectHandle(e) {
        try {
          if (!e || e.isNull?.()) {
            return "";
          }
          const t = e.handle;
          if (t) {
            return t.toString();
          } else {
            return "";
          }
        } catch (e) {
          return "";
        }
      }
      function whitelistAddPlayer(e) {
        const t = getPlayerIdentityInfo(e);
        for (const e of t.aliases) {
          const t = normalizeWhitelistToken(e);
          if (t && !whitelist.includes(t)) {
            whitelist.push(t);
          }
        }
        return t.label;
      }
      function whitelistRemovePlayer(e) {
        const t = getPlayerIdentityInfo(e);
        const o = t.aliases.map(e => normalizeWhitelistToken(e));
        whitelist = whitelist.filter(e => !o.includes(normalizeWhitelistToken(e)));
        return t.label;
      }
      var uxSelectedObject = null;
      var uxSelectedName = "none";
      var freecamActive = false;
      var freecamObj = null;
      var freecamPos = null;
      function uxGetTransform(e) {
        try {
          return e.method("get_transform").invoke();
        } catch (e) {
          return null;
        }
      }
      function uxVec3Str(e) {
        try {
          const t = e.field("x").value.toFixed(3);
          const o = e.field("y").value.toFixed(3);
          return `(${t}, ${o}, ${e.field("z").value.toFixed(3)})`;
        } catch (e) {
          return "(?,?,?)";
        }
      }
      function uxGetComponentNames(e) {
        const t = [];
        try {
          const o = e.method("GetComponentsInChildren", 1).inflate(Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Component")).invoke(false);
          if (!o || o.isNull()) {
            return t;
          }
          const n = o.length;
          for (let e = 0; e < Math.min(n, 64); e++) {
            try {
              const n = o.method("get_Item").invoke(e);
              const i = n.method("GetType").invoke().method("get_Name").invoke()?.content ?? "?";
              t.push(i);
            } catch (e) {}
          }
        } catch (e) {}
        return t;
      }
      var itemIDs = ["item_ac_cola", "item_alien_cube", "item_alphablade", "item_ampbattery", "item_ampbattery_mega", "item_anti_gravity_grenade", "item_apple", "item_arena_pistol", "item_arena_shotgun", "item_arrow", "item_arrow_bomb", "item_arrow_heart", "item_arrow_lightbulb", "item_arrow_teleport", "item_axe", "item_backpack", "item_backpack_black", "item_backpack_fish", "item_backpack_green", "item_backpack_large_base", "item_backpack_large_basketball", "item_brick", "item_backpack_large_clover", "item_backpack_monkey", "item_backpack_pink", "item_backpack_realistic", "item_backpack_small_base", "item_backpack_space", "item_backpack_white", "item_backpack_with_flashlight", "item_bait_beetle", "item_bait_fly", "item_bait_glowworm", "item_bait_magmar_ball", "item_bait_mouse_trap", "item_bait_sardine", "item_bait_shell", "item_bait_starfish", "item_bait_wallet", "item_balloon", "item_balloon_heart", "item_bamboo_fishing_rod", "item_banana", "item_banana_chips", "item_baseball_bat", "item_basic_fishing_rod", "item_batterycell_hydra", "item_beans", "item_big_cup", "item_bighead_larva", "item_bloodlust_vial", "item_blox_cube", "item_blox_moon", "item_blox_sphere", "item_blox_star", "item_blox_triangle", "item_boombox", "item_boombox_fishing", "item_boombox_neon", "item_boomerang", "item_box_fan", "item_brain_chunk", "item_brainslug_blue", "item_brainslug_green", "item_brainslug_pink", "item_broccoli_grenade", "item_broccoli_shrink_grenade", "item_broom", "item_broom_halloween", "item_bubble_gun", "item_bubble_staff", "item_burrito", "item_butcherpipe", "item_butcherspear", "item_butchersword", "item_calculator", "item_cardboard_box", "item_cardboard_dragon_body", "item_cardboard_dragon_head", "item_carrot", "item_ceo_plaque", "item_chakra", "item_clapper", "item_cluster_grenade", "item_coconut_shell", "item_cola", "item_cola_large", "item_company_ration", "item_company_ration_heal", "item_cracker", "item_crate", "item_crossbow", "item_crossbow_heart", "item_crowbar", "item_cube_frame", "item_cubetrident", "item_cuboid_anomaly", "item_cutie_dead", "item_d20", "item_deadmans_draw", "item_deadmans_draw_card", "item_deadmans_draw_card_comedy_drama", "item_deadmans_draw_comedy_drama", "item_demon_sword", "item_disc", "item_disposable_camera", "item_dragons_claw", "item_drill", "item_drill_fists", "item_drill_galaxy", "item_drill_neon", "item_dwarven_hammer", "item_dynamite", "item_dynamite_cube", "item_easter_egg", "item_egg", "item_egg_easter_blue", "item_egg_easter_red", "item_egg_easter_yellow", "item_electrical_tape", "item_energy_axe", "item_energy_sword_dual", "item_energy_sword_green", "item_energy_sword_red", "item_eraser", "item_eye_googly", "item_film_reel", "item_finger_board", "item_fish_anglerfish", "item_fish_big_shark", "item_fish_boomfish", "item_fish_boot", "item_fish_bottled_message", "item_fish_carp", "item_fish_chewna", "item_fish_clam_hookshot", "item_fish_cowfish", "item_fish_crappie", "item_fish_crispie", "item_fish_cube", "item_fish_diamond_jade_koi", "item_fish_dollar_bill", "item_fish_dragonfish", "item_fish_fishsword", "item_fish_ghost_sword", "item_fish_gold_fish", "item_fish_hydracarp", "item_fish_irontusk", "item_fish_kissy", "item_fish_license_plate", "item_fish_magma_carp", "item_fish_nebula_fish", "item_fish_nutfish", "item_fish_pufferfish", "item_fish_rainbow_trout", "item_fish_redacted", "item_fish_rotten_fish", "item_fish_salmon", "item_fish_salmonster", "item_fish_scaldfish", "item_fish_seahorse", "item_fish_seamine", "item_fish_shellfish_shield", "item_fish_spicy_salmon", "item_fish_teeth", "item_fish_triclops", "item_fish_tuna", "item_fish_yellowcake", "item_fishing_terminal_bait_button", "item_flamethrower", "item_flamethrower_skull", "item_flamethrower_skull_ruby", "item_flaregun", "item_flashbang", "item_flashlight", "item_flashlight_galaxy", "item_flashlight_mega", "item_flashlight_red", "item_flipflop_realistic", "item_floppy3", "item_floppy5", "item_football", "item_four_leaf_clover", "item_four_leaf_clover_gold", "item_four_leaf_radar", "item_friend_launcher", "item_frying_pan", "item_fungi_blue", "item_fungi_red", "item_gameboy", "item_glitched_banana", "item_glowing_fishing_rod", "item_glowstick", "item_goldbar", "item_goldcoin", "item_goop", "item_goopfish", "item_grappling_hook", "item_great_sword", "item_great_sword_galaxy", "item_grenade", "item_grenade_gold", "item_grenade_launcher", "item_guided_boomerang", "item_gyroid_anomaly", "item_hammer_candy_cane", "item_harddrive", "item_hatchet", "item_hawaiian_drum", "item_heart_chunk", "item_heart_gun", "item_heartchocolatebox", "item_hermes_staff", "item_hh_key", "item_hookshot", "item_hookshot_galaxy", "item_hookshot_sword", "item_hot_cocoa", "item_hoverpad", "item_hoverpad_galaxy", "item_hydra", "item_impulse_grenade", "item_jetpack", "item_joystick", "item_joystick_inv_y", "item_katana_big", "item_katana_medium", "item_keycard", "item_lance", "item_landmine", "item_landmine_bee", "item_lantern_cny", "item_large_banana", "item_lava_fishing_rod", "item_love_grenade", "item_mage_pirate_sword", "item_mannequin_arm", "item_mannequin_arm_left", "item_mannequin_arm_right", "item_mannequin_head", "item_mannequin_leg", "item_mannequin_leg_left", "item_mannequin_leg_right", "item_mannequin_torso", "item_marshmallow_bunny_bomb", "item_megaphone", "item_metal_ball", "item_metal_ball_easter", "item_metal_ball_xmas", "item_metal_plate", "item_metal_plate_small", "item_metal_plate_xmas", "item_metal_rod", "item_metal_rod_curved", "item_metal_rod_easter", "item_metal_rod_small", "item_metal_rod_xmas", "item_metal_triangle", "item_midipad", "item_midipad_animal", "item_mining_laser", "item_mining_laser_orange", "item_module_gun_1", "item_module_gun_2", "item_module_gun_3", "item_module_hull_1", "item_module_hull_2", "item_module_hull_3", "item_module_joust_1", "item_module_joust_2", "item_module_joust_3", "item_module_laser_1", "item_module_laser_2", "item_module_laser_3", "item_module_tether_1", "item_module_tractor_beam_1", "item_momboss_box", "item_moneygun", "item_moonhorror_key", "item_moonrock", "item_moonrock_cheesy", "item_moonrock_friend", "item_motor", "item_mountain_key", "item_mug", "item_needle", "item_nut", "item_nut_drop", "item_ogre_hands", "item_orange", "item_ore_copper_l", "item_ore_copper_m", "item_ore_copper_s", "item_ore_gold_l", "item_ore_gold_m", "item_ore_gold_s", "item_ore_hell", "item_ore_silver_l", "item_ore_silver_m", "item_ore_silver_s", "item_painting_canvas", "item_paperpack", "item_pelican_case", "item_pennant_spring", "item_pickaxe", "item_pickaxe_cny", "item_pickaxe_cube", "item_pickaxe_realistic", "item_pickaxe_realistic_galaxy", "item_pickaxe_spacedwarf", "item_pinata_bat", "item_pineapple", "item_pipe", "item_pistol_dragon", "item_plank", "item_plank_easter", "item_plate_round", "item_plunger", "item_pogostick", "item_police_baton", "item_popcorn", "item_portable_safe_zone", "item_portable_teleporter", "item_prismatic_anomaly", "item_prop_scanner", "item_pumpkin_bomb", "item_pumpkin_pie", "item_pumpkinjack", "item_pumpkinjack_small", "item_pyramidal_anomaly", "item_quest_dball_1", "item_quest_dball_2", "item_quest_dball_3", "item_quest_dball_4", "item_quest_dball_5", "item_quest_dball_6", "item_quest_dball_7", "item_quest_gy_skull", "item_quest_gy_skull_special", "item_quest_hlal_brain", "item_quest_hlal_eyeball", "item_quest_hlal_flesh", "item_quest_hlal_heart", "item_quest_key_graveyard", "item_quest_vhs", "item_quest_vhs_backlots", "item_quest_vhs_basement", "item_quest_vhs_cave", "item_quest_vhs_circus_day", "item_quest_vhs_circus_ext", "item_quest_vhs_circus_fac", "item_quest_vhs_dam_facility", "item_quest_vhs_dam_servers", "item_quest_vhs_dark_forest", "item_quest_vhs_eden", "item_quest_vhs_forest", "item_quest_vhs_foundation", "item_quest_vhs_graveyard", "item_quest_vhs_haunted_house", "item_quest_vhs_hell", "item_quest_vhs_lab", "item_quest_vhs_lake", "item_quest_vhs_lobby", "item_quest_vhs_megalodon", "item_quest_vhs_megalodon_lake", "item_quest_vhs_mines", "item_quest_vhs_moon", "item_quest_vhs_moon_horror_rocket", "item_quest_vhs_mountain", "item_quest_vhs_mountainbot", "item_quest_vhs_mountainshack", "item_quest_vhs_mountainvault", "item_quest_vhs_obsidianhalls", "item_quest_vhs_odd_core", "item_quest_vhs_office", "item_quest_vhs_office_basement", "item_quest_vhs_powerplant_microwave", "item_quest_vhs_powerplant_reactorcore", "item_quest_vhs_powerplant_security", "item_quest_vhs_powerplant_supportfacility", "item_quest_vhs_sandbox", "item_quest_vhs_sewers", "item_quest_vhs_vhs-core", "item_quiver", "item_quiver_heart", "item_radiation_gun", "item_radioactive_broccoli", "item_radioactive_fishing_rod", "item_randombox_mobloot_big", "item_randombox_mobloot_medium", "item_randombox_mobloot_small", "item_randombox_mobloot_weapons", "item_randombox_mobloot_weapons_big", "item_randombox_mobloot_zombie", "item_rare_card", "item_remote_controller", "item_repair_wrench", "item_revolver", "item_revolver_ammo", "item_revolver_gold", "item_ring_buoy", "item_ringmaster_staff", "item_robo_monke", "item_robot_arm_left", "item_robot_arm_left_galaxy", "item_robot_arm_right", "item_robot_arm_right_galaxy", "item_robot_head", "item_rope", "item_rpg", "item_rpg_ammo", "item_rpg_ammo_egg", "item_rpg_ammo_shoe", "item_rpg_ammo_spear", "item_rpg_cny", "item_rpg_easter", "item_rpg_shoe", "item_rpg_smshr", "item_rpg_spear", "item_rubberducky", "item_ruby", "item_saddle", "item_salmoncannon", "item_sawblade", "item_sawblade_launcher", "item_scanner", "item_scissors", "item_server_pad", "item_shadowboss_key", "item_shield", "item_shield_bones", "item_shield_candy_cane", "item_shield_galaxy", "item_shield_police", "item_shield_spartan", "item_shield_viking_1", "item_shield_viking_2", "item_shield_viking_3", "item_shield_viking_4", "item_shotgun", "item_shotgun_ammo", "item_shotgun_gold", "item_shotgun_sawed", "item_shotgun_viper", "item_shovel", "item_shredder", "item_shrinking_broccoli", "item_skipole", "item_skishoe", "item_skishoe_2", "item_skishoe_3", "item_skishoe_4", "item_snail_friend", "item_snowball", "item_snowboard", "item_snowboard_2", "item_snowboard_3", "item_snowboard_4", "item_snowboard_auto", "item_spear_spartan", "item_snowboard_galaxy", "item_spear_candy_cane", "item_special_fishing_rod", "item_special_fishing_rod_radar_part", "item_special_fishing_rod_with_radar", "item_stapler", "item_stash_grenade", "item_steel_beam", "item_steel_beam_xmas", "item_stellarsword_blue", "item_stellarsword_gold", "item_stellate_anomaly", "item_stick_armbones", "item_stick_bone", "item_sticker_dispenser", "item_sticky_dynamite", "item_stinky_cheese", "item_stopwatch", "item_tablet", "item_tapedispenser", "item_tele_grenade", "item_tele_pearl", "item_teleport_dagger", "item_teleport_gun", "item_teleport_gun_galaxy", "item_theremin", "item_timebomb", "item_toilet_paper", "item_toilet_paper_mega", "item_toilet_paper_roll_empty", "item_token_circus", "item_trampoline", "item_treestick", "item_tripwire_explosive", "item_trophy", "item_truss", "item_truss_easter", "item_truss_small", "item_truss_xmas", "item_turkey_leg", "item_turkey_whole", "item_tv_remote", "item_ukulele", "item_ukulele_gold", "item_umbrella", "item_umbrella_clover", "item_umbrella_squirrel", "item_unidentified", "item_upsidedown_loot", "item_uranium_chunk_l", "item_uranium_chunk_m", "item_uranium_chunk_s", "item_viking_hammer", "item_viking_hammer_twilight", "item_war_fan", "item_wheelhandle", "item_wheelhandle_big", "item_whoopie", "item_wireframe_cube", "item_wireframe_gun", "item_wood_log", "item_wood_pallet", "item_wood_pallet_easter", "item_wyrmpiercer", "item_zipline_gun", "item_zombie_meat", "item_squishy_angler", "item_squishy_armstrong", "item_squishy_bighead", "item_squishy_bomber", "item_squishy_chicken", "item_squishy_cutie", "item_squishy_edenzombie", "item_squishy_giantthrower", "item_squishy_gold_angler", "item_squishy_gold_armstrong", "item_squishy_gold_bighead", "item_squishy_gold_bomber", "item_squishy_gold_chicken", "item_squishy_gold_cutie", "item_squishy_gold_edenzombie", "item_squishy_gold_giantthrower", "item_water_massive_super_soaker", "item_water_balloon_blue", "item_water_gun_blue", "item_water_supersoaker_blue", "item_water_balloon_red", "item_water_gun_red", "item_water_supersoaker_red", "item_water_gun", "item_water_balloon", "item_water_soaker", "item_dna_vial", "item_steampunk_clip", "item_steampunk_coils_big", "item_steampunk_coils_small", "item_steampunk_gear_big", "item_steampunk_gear_open_big", "item_steampunk_gear_open_small", "item_steampunk_gear_small", "item_steampunk_gear_spikes_big", "item_steampunk_gear_spikes_small", "item_steampunk_gear_wheels", "item_steampunk_lamp", "item_steampunk_lightbulb", "item_steampunk_redgreen_hand", "item_steampunk_scaffolding", "item_steampunk_smokestack", "item_steampunk_wings", "item_tomato", "item_vuvuzela", "item_backpack_gold", "item_crowbar_gold", "item_hookshot_gold", "item_prune_juice", "item_sticky_dynamite_gold", "item_axe_blood", "item_butcherpipe_blood", "item_crowbar_blood", "item_dwarven_hammer_blood", "item_mining_laser_blood", "item_pickaxe_spacedwarf_blood", "item_shotgun_blood", "item_flamethrower_skull_blood", "item_snowboard_blood", "item_spear_spartan_blood", "item_butchersword_blood", "item_steelchair", "item_backpack_frog", "item_backrooms_floor", "item_backrooms_ceiling", "item_backrooms_chair", "item_backrooms_corner_in", "item_backrooms_lamp", "item_backrooms_corner_out", "item_backrooms_pillar", "item_backrooms_wall_1m", "item_backrooms_wall_2m", "item_quest_vhs_backrooms", "item_quest_vhs_backrooms_horror", "item_c4_explosive", "item_painters_tape", "item_wooden_stool", "item_quest_vhs_toxic_forest", "item_quest_vhs_biolab"];
      var mobIDs = [{
        name: "Angler",
        id: 1
      }, {
        name: "AnglerMad",
        id: 2
      }, {
        name: "Armstrong",
        id: 3
      }, {
        name: "ArmstrongMad",
        id: 4
      }, {
        name: "Banshee",
        id: 5
      }, {
        name: "Bomb",
        id: 6
      }, {
        name: "Bomber",
        id: 7
      }, {
        name: "BomberFlashbang",
        id: 8
      }, {
        name: "BomberMad",
        id: 9
      }, {
        name: "Chicken",
        id: 10
      }, {
        name: "Cyst",
        id: 11
      }, {
        name: "FakeGorilla",
        id: 12
      }, {
        name: "BigHead",
        id: 13
      }, {
        name: "RedGreen",
        id: 14
      }, {
        name: "Phantom",
        id: 15
      }, {
        name: "EvilEye",
        id: 16
      }, {
        name: "GiantThrower",
        id: 17
      }, {
        name: "RedGreenMad",
        id: 18
      }, {
        name: "Spider",
        id: 19
      }, {
        name: "FlyingSwarm",
        id: 20
      }, {
        name: "NextBot",
        id: 21
      }, {
        name: "Segway",
        id: 22
      }, {
        name: "NextBotStatic",
        id: 23
      }, {
        name: "EvilEyePinata",
        id: 24
      }, {
        name: "EvilEyePinataLarge",
        id: 25
      }, {
        name: "Lanky",
        id: 26
      }, {
        name: "Blob",
        id: 27
      }, {
        name: "Cutie",
        id: 28
      }, {
        name: "SpiderCave",
        id: 29
      }, {
        name: "ForestMob",
        id: 30
      }, {
        name: "Mimic",
        id: 31
      }, {
        name: "GraveyardBoss",
        id: 32
      }, {
        name: "Ringmaster",
        id: 33
      }, {
        name: "Puppet",
        id: 34
      }, {
        name: "PolypMass",
        id: 35
      }, {
        name: "RobotDog",
        id: 36
      }, {
        name: "Shadow",
        id: 37
      }, {
        name: "Heart",
        id: 38
      }, {
        name: "Slimey",
        id: 39
      }, {
        name: "ShadowBoss",
        id: 40
      }, {
        name: "BigShark",
        id: 41
      }, {
        name: "EdenZombie",
        id: 42
      }, {
        name: "Skinwalker",
        id: 43
      }, {
        name: "YinWorm",
        id: 44
      }, {
        name: "YangWorm",
        id: 45
      }, {
        name: "ArmstrongSpace",
        id: 46
      }, {
        name: "Smiley",
        id: 47
      }, {
        name: "Uvula",
        id: 48
      }, {
        name: "TubeMonster",
        id: 49
      }, {
        name: "SmileyMad",
        id: 50
      }, {
        name: "MomBossController",
        id: 51
      }];
      var NetworkVFXEntries = [["MuzzleFlash_Shotgun", 0, "network"], ["MuzzleFlash_FlareGun", 1, "network"], ["CrateBreak", 2, "network"], ["MuzzleFlash_SmallGun", 3, "network"], ["MuzzleFlash_GoldRevolver", 4, "network"], ["MuzzleFlash_DragonPistol", 5, "network"], ["MuzzleFlash_ViperShotgun", 6, "network"], ["Explosion_FlareGun", 32, "network"], ["Explosion_Coins", 33, "network"], ["Explosion_Nuts", 34, "network"], ["Explosion_Keys", 35, "network"], ["Explosion_Balloon", 36, "network"], ["Explosion_TeleGrenadeSrc", 37, "network"], ["Player_Touch_Lava", 38, "network"], ["Portal_Teleport", 39, "network"], ["Explosion_Coins_Vertical", 40, "network"], ["Autumn_Leaves_Burst", 41, "network"], ["Explosion_Feathers", 42, "network"], ["Explosion_Popcorn", 43, "network"], ["Electricity_Small", 44, "network"], ["Impact_Flaregun", 64, "network"], ["Impact_Snowball", 65, "network"], ["Impact_GoldRevolver", 66, "network"], ["Impact_MeleeHit", 67, "network"], ["Impact_BigGroundHit", 68, "network"], ["Impact_MeleeHit_CriticalSmall", 69, "network"], ["Impact_MeleeHit_CriticalLarge", 70, "network"], ["Impact_MeleeHit_AoE", 71, "network"], ["Research_ZiplineAttachDetach", 96, "network"], ["Research_Purchase1RP", 97, "network"], ["Research_Purchase5RP", 98, "network"], ["Research_Purchase10RP", 99, "network"], ["Research_PurchaseRPBundle", 100, "network"], ["Rope_ZiplineAttachDetach", 110, "network"], ["MeatExplosion_1", 128, "network"], ["MeatExplosion_2", 129, "network"], ["MeatExplosion_Headshot", 130, "network"], ["ServerRoomSplash_Small", 160, "network"], ["ServerRoomSplash_Big", 161, "network"], ["RAMActivationSparks", 162, "network"], ["GreenBlink", 170, "network"], ["ConfettiBurst", 174, "network"], ["Ethereal_Void", 180, "network"], ["MomBoss_NailBreak", 181, "network"], ["MidAirJump_Fart", 182, "network"], ["FuelExplosion", 183, "network"]];
      var VisualEffectAssetEntries = [["ArenaMatchEndOverlay", 1, "asset"], ["ArenaObjectiveMarkers", 2, "asset"], ["ArenaOreMined", 3, "asset"], ["ArenaTeamOreBreak", 4, "asset"], ["BloodMist", 5, "asset"], ["BubbleStream", 6, "asset"], ["BubbleStreamColored", 7, "asset"], ["BulletTrace", 8, "asset"], ["BulletTrace_B", 9, "asset"], ["CameraColorSample_DoesntWork", 10, "asset"], ["CoinEarn", 11, "asset"], ["DuplicatorAmbience", 12, "asset"], ["ElectricityArc", 13, "asset"], ["ElectricitySurface", 14, "asset"], ["EscherPuzzleSplineParticles", 15, "asset"], ["FartTag", 16, "asset"], ["FishEarn", 17, "asset"], ["FlyingDustParticles", 18, "asset"], ["FlyingDustParticles1", 19, "asset"], ["GhostReviveText", 20, "asset"], ["GhostTargetTrail", 21, "asset"], ["GraveyardAmbience", 22, "asset"], ["HealthBurst", 23, "asset"], ["HeavyRainAmbience", 24, "asset"], ["HellGigaSiphon", 25, "asset"], ["HellSiphon", 26, "asset"], ["MapConnectionSpline", 27, "asset"], ["MirrorBG", 28, "asset"], ["NutEarn", 29, "asset"], ["OreBreak", 30, "asset"], ["OreHit", 31, "asset"], ["PlanetariumDust", 32, "asset"], ["PlanetariumRocks", 33, "asset"], ["PlayerHitTest", 34, "asset"], ["PowerPlugSparks", 35, "asset"], ["ProximityHeal", 36, "asset"], ["QuestMapSpline", 37, "asset"], ["RadiationAmbience", 38, "asset"], ["RAMActivation", 39, "asset"], ["RamBreakdown", 40, "asset"], ["ReactorCoreAmbience", 41, "asset"], ["RPEarn", 42, "asset"], ["RPEarn_Arena", 43, "asset"], ["SecurityVacuumSpline", 44, "asset"], ["SnowstormAmbience", 45, "asset"], ["SpaceDebris", 46, "asset"], ["SwimBubbles", 47, "asset"], ["VentsDust", 48, "asset"], ["VFX_SpiderBlood", 49, "asset"], ["VoxeHit", 50, "asset"], ["VoxelBreak", 51, "asset"], ["WoosterSplash", 52, "asset"], ["XPEarn", 53, "asset"]];
      var NetworkVFXTypes = {
        None: 255
      };
      for (const [e, t] of NetworkVFXEntries) {
        NetworkVFXTypes[e] = t;
      }
      var VisualEffectAssetTypes = {};
      for (const [e, t] of VisualEffectAssetEntries) {
        VisualEffectAssetTypes[e] = t;
      }
      var VFXTypes = {
        ...NetworkVFXTypes,
        ...VisualEffectAssetTypes
      };
      var RPEARN_VFX_ENTRY = {
        label: "RPEarn",
        className: "AnimalCompany.VFX.RPEarn"
      };
      var version = "idk";
      var menuName = "SolarHP Menu";
      var useImguiMenu = false;
      var imguiLoaded = false;
      var solarHpImguiModeKey = "__solarHpImguiMode";
      try {
        if (globalThis[solarHpImguiModeKey] === undefined) {
          globalThis[solarHpImguiModeKey] = false;
        }
      } catch (e) {}
      var menu = null;
      globalThis.roundedMesh = false;
      var reference = null;
      var referenceCollider = null;
      var buttonClickDelay = 0;
      var LerpMenu = true;
      var menuscale = 1;
      var righthand = false;
      var deltaTime = 0;
      var flySmoothRotation = null;
      var _pcSmoothYaw = 0;
      var _pcSmoothPitch = 0;
      var _pcMouseSensitivity = 0.15;
      var _pcFlyAcceleration = 2.5;
      var _pcFlyDeceleration = 1.2;
      var pcMenuDistance = 0.7;
      var menuFollowSmoothness = 15;
      var vrMenuUpOffset = 0.08;
      var vrMenuPalmInset = 0.045;
      var vrMenuRightOffset = 0.025;
      var vrMenuTilt = -8;
      var menuOutlineColor = [0.18, 0.18, 0.195, 1];
      var pageOutlineColor = menuOutlineColor;
      var buttonOutlineColor = pageOutlineColor;
      var menuOutlineThickness = 0.009;
      var controlOutlineThickness = 0.0075;
      var rowOutlineThickness = 0.0065;
      var outlinesEnabled = true;
      var rowButtonHeight = 0.06;
      var rowButtonWidth = 0.61;
      var rowButtonSpacing = 0.032;
      var disconnectButtonWidth = 0.62;
      var pageButtonWidth = 0.135;
      var cornerRoundness = 0.27;
      var titleWidth = 0.6;
      var titleHeight = 0.057;
      function getButtonsPerPage() {
        const e = Math.max(rowButtonSpacing, rowButtonHeight * 0.45);
        return Math.max(4, Math.min(8, Math.floor(0.225 / e) + 1));
      }
      var _pcMode = false;
      var _pcMenuOpen = false;
      var _pcFlyEnabled = false;
      var _pcLastCursorX = -1;
      var _pcLastCursorY = -1;
      var _pcMenuSelector = 0;
      var _pcQWasDown = false;
      var _pcEscWasDown = false;
      var _pcUpWasDown = false;
      var _pcDownWasDown = false;
      var _pcEnterWasDown = false;
      var _pcMinusWasDown = false;
      var _pcPlusWasDown = false;
      var _pcLeftWasDown = false;
      var _pcRightWasDown = false;
      var _pcRmbWasDown = false;
      var _pcLmbMenuWasDown = false;
      var _VK_Q = 81;
      var _VK_ESCAPE = 27;
      var _VK_RBUTTON = 2;
      var _VK_LBUTTON = 1;
      var _VK_W = 87;
      var _VK_A = 65;
      var _VK_S = 83;
      var _VK_D = 68;
      var _VK_SPACE_PC = 32;
      var _VK_SHIFT_PC = 16;
      var _VK_UP_PC = 38;
      var _VK_DOWN_PC = 40;
      var _VK_LEFT_PC = 37;
      var _VK_RIGHT_PC = 39;
      var _VK_OEM_MINUS_PC = 189;
      var _VK_OEM_PLUS_PC = 187;
      var _VK_SUBTRACT_PC = 109;
      var _VK_ADD_PC = 107;
      var _VK_RETURN_PC = 13;
      var _GAKS = null;
      var _GCP = null;
      var _GFW = null;
      var _S2C = null;
      var _GCR = null;
      var _pcPoint = Memory.alloc(8);
      var _pcClientRect = Memory.alloc(16);
      var _pcWin32InitAttempted = false;
      var _pcWin32ReadyLogged = false;
      var _pcYaw = 0;
      var _pcPitch = 0;
      var _pcFlyVel = [0, 0, 0];
      var _pcFlySpeed = 8;
      function lerp(e, t, o) {
        return e + (t - e) * o;
      }
      var shopUnlockEnabled = false;
      var blueprintAbuseEnabled = false;
      var shopHooksInstalled = false;
      var noSpendEnabled = false;
      var noSpendEnabled2 = false;
      var blockRPCEnabled = false;
      var antiKickEnabled = false;
      var time = 0;
      var stashDupeEnabled = false;
      var stashDupe = false;
      var itemIndex = 0;
      var heldItemValue300kLastHandles = ["", ""];
      var gridAmount = 1;
      var gridFrozenItems = [];
      var gridSpacing = 0.45;
      var gridColumns = 0;
      var itemGunDelay = 0;
      var rainbowItemGunDelay = 0;
      var itemIdSelectorGunDelay = 0;
      var mobIndex = 0;
      var mobGunDelay2 = 0;
      var mobSpawnButtonLatched = false;
      var mobSpawnGunTriggerLatched = false;
      var buttonNotifications = true;
      var flySpeed = 20;
      var wasdFlyStarted = false;
      var lastMouseX = 0;
      var lastMouseY = 0;
      var mouseReady = false;
      var yaw = 0;
      var pitch = 0;
      var rotationReady = false;
      var lastLogTime = 0;
      var previousLeftMouse = false;
      var nextComputerClickTime = 0;
      var pcCursorFingerPressUntil = 0;
      var pcCursorFingerPoint = null;
      globalThis.mainText = "HELLO";
      var patternTextCellSize = 0.18;
      var patternTextLetterGap = 1.2;
      var patternTextSpawnQueue = [];
      var patternTextPiecesPerFrame = 4;
      var patternTextGunDelay = 0;
      var bgColor = [0.025, 0.07, 0.1, 0.98];
      var textColor = [0.95, 0.95, 0.95, 1];
      var buttonColor = [0.04, 0.16, 0.23, 0.96];
      var buttonPressedColor = [0.05, 0.38, 0.56, 0.98];
      var menuAnimTime = 0;
      var themeMode = 22;
      var pendingThemeReload = false;
      function selectTheme(e, t) {
        themeMode = e;
        pendingThemeReload = true;
        sendNotification("Theme: " + t, false);
      }
      var rainbowStep = 0;
      var rainbowPresets = [[1, 0, 0], [1, 0.5, 0], [1, 1, 0], [0, 1, 0], [0, 1, 1], [0, 0, 1], [0.5, 0, 1], [1, 0, 1]];
      var currentNotification = "Thanks For Using ItzDaTree's Menu";
      var notifactionResetTime = 0;
      var menuNotifTextComponent = null;
      var hudNotificationRoot = null;
      var hudNotificationCanvas = null;
      var hudNotifications = [];
      var notificationScale = 1;
      var notificationDuration = 4;
      var notificationFadeDuration = 1;
      var notificationScaleValues = [0.75, 0.9, 1, 1.15, 1.3, 1.5];
      var notificationDurationValues = [2, 3, 4, 5, 6, 8];
      var notificationScaleIndex = 2;
      var notificationDurationIndex = 2;
      var spamRainbowAllEnabled = false;
      var currentCategory = 0;
      var currentPage = 0;
      var hueVal = 0;
      var satVal = 0;
      var tagGunDelay = 0;
      var frameCount = 0;
      var menuFpsTextComponent = null;
      var menuFpsSmoothed = 60;
      var menuFpsNextUpdate = 0;
      var menuFontWarned = false;
      var menuClickSoundPath = "C:\\Users\\ItzDa\\Desktop\\TreesMenuPrivate\\menu-clicksounds\\meta-menu-button-sound.wav";
      var menuClickAudioObject = null;
      var menuClickAudioSource = null;
      var menuClickAudioClip = null;
      var menuClickAudioRequest = null;
      var menuClickAudioOperation = null;
      var menuClickLoadStartedAt = 0;
      var menuClickPendingPlay = false;
      var leftPrimary = false;
      var leftSecondary = false;
      var rightPrimary = false;
      var rightSecondary = false;
      var leftGrab = false;
      var rightGrab = false;
      var leftTrigger = false;
      var rightTrigger = false;
      var leftStick = false;
      var rightStick = false;
      var leftStickX = 0;
      var leftStickY = 0;
      var rightStickX = 0;
      var rightStickY = 0;
      var prevLeftGrab = false;
      var prevRightGrab = false;
      var joystickFlyVelocity = [0, 0, 0];
      var fistFlyVelocity = [0, 0, 0];
      var InfAmmo = false;
      var multiShootIndex = 0;
      var multiShootCount = 1;
      var noRecoil = false;
      var spawnMobEnabled = false;
      var spawnMobGunEnabled = false;
      var mobSpawnDelay = 0;
      var objectReference = null;
      var identityQuaternion = null;
      var beforeSpawnDelegate = null;
      var beforeSpawnDelegateClass = null;
      var validatorBypassEnabled = false;
      var _mobSpawnOrigXYZ = null;
      var ArenaItemKilla = false;
      var noShotgunCooldown = false;
      var rapidFireEnabled = false;
      var rapidFirePulseDelay = 0;
      var infDamage = false;
      var launchAxisCache = new Map();
      var ejectDupeAmount = 2;
      var ejectDupeIndex = 0;
      var ejectDupeValues = [1, 2, 5, 10, 20, 30, 50, 100];
      var scaleVal = 0;
      var cachedItems = null;
      var backpackDupe = false;
      var lagGunDelay = 0;
      var mobGunDelay = 0;
      var idGunDelay = 0;
      var rpgHandsDelay = 0;
      var rpcMoneyAllLoopDelay = 0;
      var yeetSpamDelay = 0;
      var giveFlyAllDelay = 0;
      var menuStructurePatched = false;
      var dualRevolverPunchDelay = 0;
      var netPlayerSpawnDelay = 0;
      var deleteAllLobbyItemsDelay = 0;
      var screenshakeAllEnabled = false;
      var itemRainDelay = 0;
      var bringAllItemsGunDelay = 0;
      var jellyAllEnabled = false;
      var yeetAllEnabled = false;
      var banGunEnabled = false;
      var bringAllItemsCursor = 0;
      var goopSpamDelay = 0;
      var itemLauncherSelfDelay = 0;
      var heldItemDuplicateDelay = 0;
      var kickGunDelay = 0;
      var goopfishGunEnabled = true;
      var pendingQuiverFills = [];
      var menuVisible = false;
      var previousVrMenuToggleInput = false;
      var menuOpenAmount = 0;
      var menuTabFadeAmount = 1;
      var menuCanvasGroup = null;
      var menuRestingScale = [0.1, 0.3, 0.3825];
      var menuZoomInDuration = 0.18;
      var menuZoomOutDuration = 0.14;
      var menuTabFadeDuration = 0.16;
      var _lastObjectMat = null;
      var menuInteractionLayer = 30;
      var menuCollisionLayerConfigured = false;
      var muffleAllEnabled = false;
      var squeakyAllEnabled = false;
      var radioactiveAllEnabled = false;
      var duckyRocketAttachments = [];
      var grenadeProjectileLauncherEnabled = false;
      var lastGrenadeProjectileLaunchTime = 0;
      var outlineColor = [1, 0, 0, 1];
      var _snowballDelay = 0;
      var ghostFrozenPos = null;
      var ghostMarker = null;
      var searchQuery = "";
      var roomCodeQuery = "";
      var oreTextQuery = "";
      var oreTextSpawnDelay = 0;
      var oreTextTriggerLatched = false;
      var oreTextGunEnabled = false;
      var oreTextFreezeRefreshTime = 0;
      var oreTextFreezeCursor = 0;
      var oreTextSpawnQueue = [];
      var oreTextPendingObjects = [];
      var oreTextVisualTemplate = null;
      var oreTextVisualTemplates = new Map();
      var oreTextPieceScale = 0.58;
      var oreTextThickness = 0.28;
      var oreTextCellSize = 0.082;
      var oreTextSurfaceOffset = 0.055;
      var oreTextOutline = false;
      var oreTextMirror = false;
      var oreTextVertical = false;
      var oreTextMaxLength = 24;
      var oreTextPiecesPerFrame = 4;
      var oreTextMaterials = [["Hell", "item_ore_hell"], ["Gold", "item_ore_gold_s"], ["Silver", "item_ore_silver_s"], ["Copper", "item_ore_copper_s"], ["Uranium", "item_uranium_chunk_s"]];
      var oreTextMaterialIndex = 0;
      var oreTextLayoutMode = "straight";
      var oreTextFontMode = "block";
      var oreTextAlignment = "center";
      var oreTextSnapMode = "wall";
      var oreTextColorMode = "natural";
      var oreTextBaseHue = 0;
      var oreTextAnimationMode = "none";
      var oreTextFollowMode = "none";
      var oreTextRollDegrees = 0;
      var oreTextCurveStrength = 1;
      var oreTextPreviewEnabled = true;
      var oreTextPreviewRefreshTime = 0;
      var oreTextPreviewObjects = [];
      var oreTextBatches = [];
      var oreTextNextBatchId = 1;
      var oreTextSavedPhrases = ["HELLO", "ITZDATREE", "GG"];
      var oreTextSavedPhraseIndex = 0;
      var oreTextObjectBudget = 350;
      var oreTextSavedStyle = null;
      var oreTextSettingsLoaded = false;
      var ORE_TEXT_SETTINGS_PATH = "C:\\Users\\Paral\\Downloads\\solarhp priv menu\\solarhp_text_presets.json";
      var coolHaloEnabled = false;
      var coolOrbitOrbsEnabled = false;
      var coolHandTrailEnabled = false;
      var coolDiscoEnabled = false;
      var coolHaloObjects = [];
      var coolOrbitObjects = [];
      var coolTrailObjects = [];
      var coolTrailSpawnTime = 0;
      var roomCodeGameMode = 0;
      var roomCodeGameModeNames = ["Adventure", "Arena", "Hardcore", "DevSandbox", "Classic"];
      var jellywowes = 5;
      var lastGunTime = 0;
      var vfxGunEnabled = false;
      var ramVfxGunEnabled = false;
      var rpEarnVfxGunEnabled = false;
      var jellyVal = 0;
      var devEnabled = false;
      var backpackDupeEnabled = false;
      var spamKickInterval = null;
      var spamKickTimer = 0;
      var noLaserOverheat = false;
      var infiniteLaserRange = false;
      var itemLauncherDelay = 0;
      var itemLauncherEnabled = false;
      var pushEveryoneGunDelay = 0;
      var heldPlayer = null;
      var deletePlayerGunDelay = 0;
      var randomAllItemsDelay = 0;
      var allPlayerGunBuffsDelay = 0;
      var lagAllItemsDelay = 0;
      var textSpawnDelay = 0;
      var testMoneySpawnDelay = 0;
      var researchOreSpawnDelay = 0;
      var researchOreFailNotifyDelay = 0;
      var handStealDelay = 0;
      var textSpawnedObjects = [];
      var textBlueprintCache = null;
      var randomAllItemsCursor = 0;
      var wlTargetActionDelay = 0;
      var whitelist = [];
      var netplrs = [];
      var usersSelectedKey = "";
      var usersSelectedLabel = "none";
      var usersSelectedPlayer = null;
      var usersSpectateEnabled = false;
      var usersOrbitCameraEnabled = false;
      var whitelistDisintegrateDelays = {};
      var whitelistGunRe = false;
      var cloneSpawnActive = false;
      var cloneSpawnPhase = 0;
      var cloneSpawnSavedPos = null;
      var cloneSpawnTimer = 0;
      var cloneOrbiters = [];
      var cloneOrbitActive = false;
      var cloneFollowTransforms = [];
      var cloneFollowActive = false;
      var cloneFollowDelay = 0;
      var cloneLineMode = false;
      var cloneSquareMode = false;
      var rigCloneCount = 5;
      var rigSpawnQueueRemaining = 0;
      var rigPendingFormation = null;
      var triggerCloneSpawnActive = false;
      var triggerCloneSpawnCooldown = 0;
      var cloneFrozenTransforms = [];
      var triggerCloneWasPressed = false;
      var rigGrabActive = false;
      var cloneGridCopyActive = false;
      var cloneGridSpawnQueue = 0;
      var cloneGridOffsets = [];
      var sellingmachineSpawns = [];
      var presentSpawns = [];
      var spawnedNetworkPrefabs = [];
      var spawnedPersistentMobs = [];
      var lastMobInterestRefreshTime = 0;
      var movementPlatformLeft = null;
      var movementPlatformRight = null;
      var platformSpawnDelay = 0;
      var menuSnapNextFrame = false;
      var smartError;
      var flingGunEnabled = false;
      var kickGunEnabled = false;
      var gunColor = [0.08, 0.08, 0.08, 0.75];
      var customShredderNuts = 100;
      var teleportGunEnabled = false;
      var colorGunEnabled = false;
      var spazColorGunEnabled = false;
      var prefabGunEnabled = false;
      var spawnGunEnabled = false;
      var applyDevFlag = true;
      var _devApplied = false;
      var telekinesisGunEnabled = false;
      var multiShootEnabled = false;
      var previousGhostKey = false;
      var previousInvisKey = false;
      var previousNoclipKey = false;
      var perviousTeleportKey = false;
      var perviousShootKey = false;
      var perviousSpawnKey = false;
      var previoussaaakey = false;
      var previousASCIIkey = false;
      var wlRpgTarget = null;
      var wlEggTarget = null;
      var wlBoomspearTarget = null;
      var wlRobotDogRPGTarget = null;
      var noBlueprintLimitEnabled;
      var choppableManager = null;
      var isGrabbing = false;
      var giveawayBagMinigunEnabled = false;
      var lastGiveawayBagMinigunTime = 0;
      var giveawayCardID = "item_pelican_case,item_ac_cola,item_taki,item_alien_cube,item_alphablade,item_ampbattery,item_ampbattery_mega,item_anti_gravity_grenade,item_apple,item_arena_pistol,item_arena_shotgun,item_arrow,item_arrow_bomb,item_arrow_heart,item_arrow_lightbulb,item_arrow_teleport,item_axe,item_backpack,item_backpack_black,item_backpack_fish,item_backpack_green,item_backpack_large_base,item_backpack_large_basketball,item_backpack_large_clover,item_backpack_monkey,item_backpack_pink,item_backpack_realistic,item_backpack_small_base,item_backpack_space,item_backpack_white,item_backpack_with_flashlight,item_bait_beetle,item_bait_fly,item_bait_glowworm,item_bait_magmar_ball,item_bait_mouse_trap,item_bait_sardine,item_bait_shell,item_bait_starfish,item_bait_wallet,item_balloon,item_balloon_heart,item_bamboo_fishing_rod,item_banana,item_banana_chips,item_baseball_bat,item_basic_fishing_rod,item_batterycell_hydra,item_beans,item_big_cup,item_bighead_larva,item_bloodlust_vial,item_blox_cube,item_blox_moon,item_blox_sphere,item_blox_star,item_blox_triangle,item_boombox,item_boombox_fishing,item_boombox_neon,item_boomerang,item_box_fan,item_brain_chunk,item_brainslug_blue,item_brainslug_green,item_brainslug_pink,item_broccoli_grenade,item_broccoli_shrink_grenade,item_broom,item_broom_halloween,item_bubble_gun,item_bubble_staff,item_burrito,item_butcherpipe,item_butcherspear,item_butchersword,item_calculator,item_cardboard_box,item_cardboard_dragon_body,item_cardboard_dragon_head,item_carrot,item_ceo_plaque,item_chakra,item_clapper,item_cluster_grenade,item_coconut_shell,item_cola,item_cola_large,item_company_ration,item_company_ration_heal,item_cracker,item_crate,item_crossbow,item_crossbow_heart,item_crowbar,item_cube_frame,item_cubetrident,item_cuboid_anomaly,item_cutie_dead,item_d20,item_deadmans_draw,item_deadmans_draw_card,item_deadmans_draw_card_comedy_drama,item_deadmans_draw_comedy_drama,item_demon_sword,item_disc,item_disposable_camera,item_dragons_claw,item_drill,item_drill_fists,item_drill_galaxy,item_drill_neon,item_dwarven_hammer,item_dynamite,item_dynamite_cube,item_easter_egg,item_egg,item_egg_easter_blue,item_egg_easter_red,item_egg_easter_yellow,item_electrical_tape,item_energy_axe,item_energy_sword_dual,item_energy_sword_green,item_energy_sword_red,item_eraser,item_eye_googly,item_film_reel,item_finger_board,item_fish_anglerfish,item_fish_big_shark,item_fish_boomfish,item_fish_boot,item_fish_bottled_message,item_fish_carp,item_fish_chewna,item_fish_clam_hookshot,item_fish_cowfish,item_fish_crappie,item_fish_crispie,item_fish_cube,item_fish_diamond_jade_koi,item_fish_dollar_bill,item_fish_dragonfish,item_fish_fishsword,item_fish_ghost_sword,item_fish_gold_fish,item_fish_hydracarp,item_fish_irontusk,item_fish_kissy,item_fish_license_plate,item_fish_magma_carp,item_fish_nebula_fish,item_fish_nutfish,item_fish_pufferfish,item_fish_rainbow_trout,item_fish_redacted,item_fish_rotten_fish,item_fish_salmon,item_fish_salmonster,item_fish_scaldfish,item_fish_seahorse,item_fish_seamine,item_fish_shellfish_shield,item_fish_spicy_salmon,item_fish_teeth,item_fish_triclops,item_fish_tuna,item_fish_yellowcake,item_fishing_terminal_bait_button,item_flamethrower,item_flamethrower_skull,item_flamethrower_skull_ruby,item_flaregun,item_flashbang,item_flashlight,item_flashlight_galaxy,item_flashlight_mega,item_flashlight_red,item_flipflop_realistic,item_floppy3,item_floppy5,item_football,item_four_leaf_clover,item_four_leaf_clover_gold,item_four_leaf_radar,item_friend_launcher,item_frying_pan,item_fungi_blue,item_fungi_red,item_gameboy,item_glitched_banana,item_glowing_fishing_rod,item_glowstick,item_goldbar,item_goldcoin,item_goop,item_goopfish,item_grappling_hook,item_great_sword,item_great_sword_galaxy,item_grenade,item_grenade_gold,item_grenade_launcher,item_guided_boomerang,item_gyroid_anomaly,item_hammer_candy_cane,item_harddrive,item_hatchet,item_hawaiian_drum,item_heart_chunk,item_heart_gun,item_heartchocolatebox,item_hh_key,item_hookshot,item_hookshot_galaxy,item_hookshot_sword,item_hot_cocoa,item_hoverpad,item_hoverpad_galaxy,item_hydra,item_impulse_grenade,item_jetpack,item_joystick,item_joystick_inv_y,item_katana_big,item_katana_medium,item_keycard,item_lance,item_landmine,item_landmine_bee,item_lantern_cny,item_large_banana,item_lava_fishing_rod,item_love_grenade,item_mage_pirate_sword,item_mannequin_arm,item_mannequin_arm_left,item_mannequin_arm_right,item_mannequin_head,item_mannequin_leg,item_mannequin_leg_left,item_mannequin_leg_right,item_mannequin_torso,item_marshmallow_bunny_bomb,item_megaphone,item_metal_ball,item_metal_ball_easter,item_metal_ball_xmas,item_metal_plate,item_metal_plate_small,item_metal_plate_xmas,item_metal_rod,item_metal_rod_curved,item_metal_rod_easter,item_metal_rod_small,item_metal_rod_xmas,item_metal_triangle,item_midipad,item_midipad_animal,item_mining_laser,item_mining_laser_orange,item_module_gun_1,item_module_gun_2,item_module_gun_3,item_module_hull_1,item_module_hull_2,item_module_hull_3,item_module_joust_1,item_module_joust_2,item_module_joust_3,item_module_laser_1,item_module_laser_2,item_module_laser_3,item_module_tether_1,item_module_tractor_beam_1,item_momboss_box,item_moneygun,item_moonhorror_key,item_moonrock,item_moonrock_cheesy,item_moonrock_friend,item_motor,item_mountain_key,item_mug,item_needle,item_nut,item_nut_drop,item_ogre_hands,item_orange,item_ore_copper_l,item_ore_copper_m,item_ore_copper_s,item_ore_gold_l,item_ore_gold_m,item_ore_gold_s,item_ore_hell,item_ore_silver_l,item_ore_silver_m,item_ore_silver_s,item_painting_canvas,item_paperpack,item_pennant_spring,item_pickaxe,item_pickaxe_cny,item_pickaxe_cube,item_pickaxe_realistic,item_pickaxe_realistic_galaxy,item_pickaxe_spacedwarf,item_pinata_bat,item_pineapple,item_pipe,item_pistol_dragon,item_plank,item_plank_easter,item_plate_round,item_plunger,item_pogostick,item_police_baton,item_popcorn,item_portable_safe_zone,item_portable_teleporter,item_prismatic_anomaly,item_prop_scanner,item_pumpkin_bomb,item_pumpkin_pie,item_pumpkinjack,item_pumpkinjack_small,item_pyramidal_anomaly,item_quest_gy_skull,item_quest_gy_skull_special,item_quest_hlal_brain,item_quest_hlal_eyeball,item_quest_hlal_flesh,item_quest_hlal_heart,item_quest_key_graveyard,item_quest_vhs,item_quest_vhs_backlots,item_quest_vhs_basement,item_quest_vhs_cave,item_quest_vhs_circus_day,item_quest_vhs_circus_ext,item_quest_vhs_circus_fac,item_quest_vhs_dam_facility,item_quest_vhs_dam_servers,item_quest_vhs_dark_forest,item_quest_vhs_eden,item_quest_vhs_forest,item_quest_vhs_foundation,item_quest_vhs_graveyard,item_quest_vhs_haunted_house,item_quest_vhs_hell,item_quest_vhs_lab,item_quest_vhs_lake,item_quest_vhs_lobby,item_quest_vhs_megalodon,item_quest_vhs_megalodon_lake,item_quest_vhs_mines,item_quest_vhs_moon,item_quest_vhs_moon_horror_rocket,item_quest_vhs_mountain,item_quest_vhs_mountainbot,item_quest_vhs_mountainshack,item_quest_vhs_mountainvault,item_quest_vhs_obsidianhalls,item_quest_vhs_odd_core,item_quest_vhs_office,item_quest_vhs_office_basement,item_quest_vhs_powerplant_microwave,item_quest_vhs_powerplant_reactorcore,item_quest_vhs_powerplant_security,item_quest_vhs_powerplant_supportfacility,item_quest_vhs_sandbox,item_quest_vhs_sewers,item_quest_vhs_vhs-core,item_quiver,item_quiver_heart,item_radiation_gun,item_radioactive_broccoli,item_radioactive_fishing_rod,item_randombox_mobloot_big,item_randombox_mobloot_medium,item_randombox_mobloot_small,item_randombox_mobloot_weapons,item_randombox_mobloot_weapons_big,item_randombox_mobloot_zombie,item_rare_card,item_remote_controller,item_repair_wrench,item_revolver,item_revolver_ammo,item_revolver_gold,item_ring_buoy,item_ringmaster_staff,item_robo_monke,item_robot_arm_left,item_robot_arm_left_galaxy,item_robot_arm_right,item_robot_arm_right_galaxy,item_robot_head,item_rope,item_rpg,item_rpg_ammo,item_rpg_ammo_egg,item_rpg_ammo_shoe,item_rpg_ammo_spear,item_rpg_cny,item_rpg_easter,item_rpg_shoe,item_rpg_smshr,item_rpg_spear,item_rubberducky,item_ruby,item_saddle,item_salmoncannon,item_sawblade,item_sawblade_launcher,item_scanner,item_scissors,item_server_pad,item_shadowboss_key,item_shield,item_shield_bones,item_shield_candy_cane,item_shield_galaxy,item_shield_police,item_shield_viking_1,item_shield_viking_2,item_shield_viking_3,item_shield_viking_4,item_shotgun,item_shotgun_ammo,item_shotgun_gold,item_shotgun_sawed,item_shotgun_viper,item_shovel,item_shredder,item_shrinking_broccoli,item_skipole,item_skishoe,item_skishoe_2,item_skishoe_3,item_skishoe_4,item_snail_friend,item_snowball,item_snowboard,item_snowboard_2,item_snowboard_3,item_snowboard_4,item_snowboard_auto,item_snowboard_galaxy,item_spear_candy_cane,item_special_fishing_rod,item_special_fishing_rod_radar_part,item_special_fishing_rod_with_radar,item_stapler,item_stash_grenade,item_steel_beam,item_steel_beam_xmas,item_stellarsword_blue,item_stellarsword_gold,item_stellate_anomaly,item_stick_armbones,item_stick_bone,item_sticker_dispenser,item_sticky_dynamite,item_stinky_cheese,item_stopwatch,item_tablet,item_tapedispenser,item_tele_grenade,item_tele_pearl,item_teleport_dagger,item_teleport_gun,item_teleport_gun_galaxy,item_theremin,item_timebomb,item_toilet_paper,item_toilet_paper_mega,item_toilet_paper_roll_empty,item_token_circus,item_trampoline,item_treestick,item_tripwire_explosive,item_trophy,item_truss,item_truss_easter,item_truss_small,item_truss_xmas,item_turkey_leg,item_turkey_whole,item_ukulele,item_ukulele_gold,item_umbrella,item_umbrella_clover,item_umbrella_squirrel,item_unidentified,item_upsidedown_loot,item_uranium_chunk_l,item_uranium_chunk_m,item_uranium_chunk_s,item_viking_hammer,item_viking_hammer_twilight,item_war_fan,item_wheelhandle,item_wheelhandle_big,item_whoopie,item_wireframe_cube,item_wireframe_gun,item_wood_log,item_wood_pallet,item_wood_pallet_easter,item_wyrmpiercer,item_zipline_gun,item_zombie_meat,item_apescalibur,item_balloon_smiley,item_grimstaff,item_soccer_ball,item_soccer_goalposts,item_module_minigun_1,item_module_minigun_2,item_module_minigun_3,item_module_gravity_1,item_module_boost_1,item_module_boost_2,item_module_boost_3,item_module_blast_1,item_module_blast_2,item_module_blast_3,item_module_stasis_1,item_quest_vhs_asteroids,item_quest_vhs_derelictvessel,item_quest_vhs_wormhole,item_randombox_modules,item_alienblaster";
      var giveawayCardCount = 21;
      var giveawayBagPending = [];
      var giveawayCardStorePending = [];
      var giveawayBagBackpackIDs = ["item_backpack_monkey", "item_backpack_realistic", "item_backpack_large_base", "item_backpack_large_clover", "item_backpack_large_basketball", "item_backpack_fish"];
      var ProjectilePrefabs = ["FlareGunProjectile", "RPGRocket", "RPGRocketSpear", "RPGRocketEgg", "GrenadeProjectile", "RobotDogRPG", "Landmine", "InflatedBalloon", "InflatedHeartBalloon"];
      var dangerousPrefabs = ["NetPlayer", "NetSpectator"];
      var didInitPrefabs = false;
      var prefabMap = new Map();
      var prefabNameMap = new Map();
      var prefabNames = [];
      var momPrefabDumpDone = false;
      var shakeTargets = [];
      var jellyTargets = [];
      var radioactiveTargets = [];
      var muffleTargets = [];
      var gravityTargets = [];
      var effectGunDelay = 0;
      if (!(time < effectGunDelay)) {
        effectGunDelay = time + 0.15;
        globalThis.autoClean = false;
        var lastCleanTime = 0;
        var orbiters = [];
        var orbitprefabs = [];
        var buggyOrbiters = [];
        var buggyOrbitPrefabs = [];
        var sharkOrbiters = [];
        var sharkOrbitPrefabs = [];
        var sellingTowerOrbiters = [];
        var sellingTowerOrbitPrefabs = [];
        var itemTornadoEntries = [];
        var spazMachineEntries = [];
        var transformForwardAxisCache = new Map();
        var persistentMobEntries = [];
        var wlSellingOrbitEntries = [];
        var wlSellingOrbitPrefabs = [];
        var wlOrbitAllTarget = null;
        var wlOrbitAllPhase = 0;
        var wlPissTarget = null;
        var wlGunBuffTarget = null;
        var wlRightHandDuperTarget = null;
        var wlItemDuperHandTarget = null;
        var wlStashEjectDuperTarget = null;
        var wlItemDuperHandDelay = 0;
        var mobOrbitEntries = [];
        var prefabOrbitEntries = [];
        var heldSellingMachine = null;
        var forceDevModeEnabled = false;
        var mobForceStayEnabled = false;
        var mobSpawnAsyncBroken = false;
        var forceAllStashSlotsEnabled = false;
        var containerFreedomAuraEnabled = false;
        var containerFreedomSweepDelay = 0;
        var playerCagePrefabs = [];
        var playerCageEntries = [];
        var spawnedGoopObjects = [];
        var roundCornersEnabled = false;
        var menuOutlineEnabled = false;
        var GunPointer = null;
        var GunLine = null;
        var gunSmoothStartPosition = null;
        var gunSmoothEndPosition = null;
        var gunSmoothSnapNextFrame = true;
        var gunSmootheningEnabled = true;
        var gunSmootheningSpeed = 14;
        var lastGunLibReturnNotificationTime = -999;
        var lastGunLibReturnNotificationKey = "";
        var fullbirhgtobject = null;
        var lightEnabled = false;
        var vfxTypeEntries = [...NetworkVFXEntries, ...VisualEffectAssetEntries];
        var selectedVfxIndex = Math.max(0, vfxTypeEntries.findIndex(([e]) => e === "ConfettiBurst"));
        var vfxIndex = selectedVfxIndex;
        var _vfxRpc6 = null;
        var _vfxRpcInfoZero = null;
        var _vfxRpcVariantIndex = -1;
        var _vfxRunnerCache = null;
        var _visualEffectAssetMethod = null;
        var _visualEffectAssetClass = null;
        var _visualEffectAssetMethods = null;
        var _visualEffectAssetAnchor = null;
        var _vfxClassCache = new Map();
        var _visualEffectAssetObjectCache = new Map();
        var _visualEffectClassCache = null;
        var _vfxEventAttributeClassCache = null;
        var _vfxPoolPullCache = null;
        var _vfxPoolReceiverCache = undefined;
        var _allVfxDispatchPrewarmed = false;
        var _compiledVisualEffectAssets = new Set();
        var _localNetworkVfxMethodName = null;
        var _localNetworkVfxMethodScanned = false;
        var rewardMethodCache = {};
        var pinnedVfxObjects = [];
        var vfxList = vfxTypeEntries.map(([e]) => e);
        var vfxEnumValues = vfxTypeEntries.map(([e, t]) => t);
        var vfxDispatchUnavailableUntil = 0;
        var vfxNoRunnerLogTime = 0;
        var vfxMethodNamesCache = null;
        var vfxGunWorkingMethodName = null;
        var vfxGunMethodLogTime = 0;
        var nameTagsEnabled = false;
        var arenaEspEnabled = false;
        var multiBuyEnabled = false;
        var multiBuyAmount = 5;
        var multiBuyHookGuard = false;
        var telekinesisEnabled = false;
        var telekinesisHeldPlayer = null;
        var telekinesisHoldDistance = 2;
        var telekinesisMaxGrabDistance = 8;
        var telekinesisLastGrabTime = 0;
        var QUIVER_ITEM_LIMIT = 15;
        var GIVEAWAY_BAG_ITEM_LIMIT = 20;
        var ORE_ITEM_IDS = ["item_upsidedown_loot", "item_uranium_chunk_l", "item_uranium_chunk_m", "item_uranium_chunk_s", "item_ore_copper_l", "item_ore_copper_m", "item_ore_copper_s", "item_ore_gold_l", "item_ore_gold_m", "item_ore_gold_s", "item_ore_hell", "item_ore_silver_l", "item_ore_silver_m", "item_ore_silver_s"];
        var playerNameTagEntries = new Map();
        var playerEspEntries = new Map();
        var prefabIndex = 0;
        var prefabIDs = ["LootSpawnGroup_LootNode_6000", "LootSpawnGroup_LootNode_200", "LootSpawnGroup_LootNode_5500", "LootSpawnGroup_LootNode_200", "LootSpawnGroup_LootNode_10000", "LootSpawnGroup_LootNode_2000", "LootSpawnGroup_LootNode_6000", "LootSpawnGroup_LootNode_3000", "LootSpawnGroup_LootNode_300", "LootSpawnGroup_LootNode_5000", "LootSpawnGroup_LootNode_300", "LootSpawnGroup_LootNode_500", "LootSpawnGroup_LootNode_6000", "LootSpawnGroup_LootNode_200", "LootSpawnGroup_LootNode_1000", "LootSpawnGroup_LootNode_1000", "LootSpawnGroup_LootNode_120", "LootSpawnGroup_LootNode_6000", "LootSpawnGroup_LootNode_300", "LootSpawnGroup_LootNode_200", "LootSpawnGroup_LootNode_800", "LootSpawnGroup_LootNode_15000", "LootSpawnGroup_LootNode_15000", "LootSpawnGroup_LootNode_500", "LootSpawnGroup_LootNode_200", "LootSpawnGroup_LootNode_300", "LootSpawnGroup_LootNode_800", "LootSpawnGroup_LootNode_250", "LootSpawnGroup_LootNode_2500", "LootSpawnGroup_LootNode_300", "NetSessionMainObject", "NetSessionManagers", "VehicleSpawnGroup", "LootSpawnGroup_LootNode_1000", "LootSpawnGroup_LootNode_1500", "LootSpawnGroup_LootNode_15000", "LootSpawnGroup_LootNode_200", "LootSpawnGroup_LootNode_7500", "LootSpawnGroup_LootNode_2000", "LootSpawnGroup_LootNode_5000", "LootSpawnGroup_LootNode_10", "LootSpawnGroup_LootNode_350", "LootSpawnGroup_LootNode_4000", "LootSpawnGroup_LootNode_2500", "LootSpawnGroup_LootNode_500", "LootSpawnGroup_LootNode_1000", "LootSpawnGroup_LootNode_350", "LootSpawnGroup_LootNode_10000", "LootSpawnGroup_LootNode_7000", "LootSpawnGroup_LootNode_800", "LootSpawnGroup_LootNode_800", "LootSpawnGroup_LootNode_200", "LootSpawnGroup_LootNode_1000", "LootSpawnGroup_LootNode_300", "LootSpawnGroup_LootNode_1250", "LootSpawnGroup_LootNode_1500", "LootSpawnGroup_LootNode_400", "LootSpawnGroup_LootNode_6000", "LootSpawnGroup_LootNode_7000", "LootSpawnGroup_LootNode_1000", "VHSQuests_VHSSpawner", "Net", "HandleProxy_2", "Spawner_Key", "LankyController", "SpaceshipTeleporter", "LankyController", "BlobController", "CystController_3", "VHSQuests_VHSSpawner", "ItemSellingMachineController", "CystController", "VHSQuests_VHSSpawner", "OreSpawnManager", "CystController_5", "HandleProxy_3", "CystController_2", "OfficeDoorNetworked", "HandleProxy_1", "CystController_6", "HandleProxy_6", "VHSQuests_VHSSpawner", "CystController_1", "BlobController", "VHSQuests_VHSSpawner", "HandleProxy_5", "BrainPowerPlug", "NetLootSpawnGroup_Cheese", "lobbyVHS_Spawn", "BlobController", "ResearchTerminalFull", "FakeGorillaController", "HandleProxy_4", "GreenscreenNET", "LockedShippingContainer_Quest", "CystController_4", "item_rubberducky", "NetMobSpawnGroup", "AsteroidProtectorController_Medium", "NetworkAnchor_VoxelSector", "NetworkAnchor_VoxelSector", "MurderRabbitController", "TeleportationManager", "NetworkAnchor_VoxelSector", "NetworkedWorldLever_DysonD", "NetworkAnchor_VoxelSector", "NetworkAnchor_VoxelSector", "NetworkAnchor_VoxelSector", "NetworkAnchor_VoxelSector", "VHSQuests_VHSSpawner", "MurderRabbitController", "AnomalySpawner_Cuboid", "AnomalySpawner_Prismatic", "MurderRabbitController", "AsteroidProtectorController", "AnomalySpawner_Gyroid", "NetworkedWorldLever_DysonE", "OreSpawnManager", "AnomalySpawner_Pyramidal", "MineFieldNetObject", "NetworkedWorldLever_DysonB", "NetworkedWorldLever_DysonA", "AsteroidProtectorController_Hard_01", "AsteroidProtectorController_Medium", "NetMobSpawnGroup", "Vehicle_Spacebike", "MineFieldNetObject", "VHSQuests_VHSSpawner", "MurderRabbitController", "AnomalySpawner_Stellate", "NetMobSpawnGroup", "EncounterReset", "NetworkAnchor_VoxelSector", "MineFieldNetObject", "MineFieldNetObject", "NetworkAnchor_VoxelSector", "AsteroidProtectorController_Hard", "AsteroidProtectorController_Hard_04", "Vehicle_Buggy", "NetworkAnchor_VoxelSector", "ItemSellingMachineController", "Vehicle_Spacebike_03", "AsteroidProtectorController_Hard_03", "AsteroidProtectorController_Hard", "MineFieldNetObject", "MurderRabbitController", "Vehicle_Buggy_01", "VHSQuests_VHSSpawner", "MineFieldNetObject", "NetMobSpawnGroup", "NetworkAnchor_VoxelSector", "NetworkAnchor_VoxelSector", "AsteroidProtectorController_Hard_02", "NetMobSpawnGroup", "NetworkedWorldLever_DysonC", "DysonSphere_NetObjects", "RPGRocketShoe", "InflatedSmileyBalloon", "RaceTrack_Circle", "RaceTrack_DAA", "Vehicle_Spacebike_B", "Vehicle_Spaceship_1", "Vehicle_Spaceship_2", "Vehicle_Spaceship_3", "Vehicle_Spaceship_4", "Vehicle_Spaceship_5", "Vehicle_Spaceship_6", "Vehicle_Spaceship_7", "UFO_Easy", "UFO_Medium", "UFO_Hard", "SteamJumpScareManager", "AnomalySpawner"];
        var whitelistEnabled = true;
        var soundFileIndex = 0;
        var previousSoundKey = false;
        var loadedBundles = {};
        var loadedObjects = {};
        var QUEST_PLATFORM = 1;
        var SYMBOLS_URL = "https://raw.githubusercontent.com/Banban465-tech/ac-symbols/refs/heads/main/symbols.ts";
        (async () => {
          await fetchRemoteIl2CppExports();
          await loadQuestServers();
          await Il2Cpp.perform(async () => {
            let domainReady = false;
            while (!domainReady) {
              await new Promise(e => setTimeout(e, 3000));
              try {
                const e = Il2Cpp.domain.assembly("AnimalCompany").image;
                if (e && !e.isNull()) {
                  domainReady = true;
                }
              } catch (e) {}
            }
            const ac = Il2Cpp.domain.assembly("AnimalCompany").image;
            const apputils = ac.class("AnimalCompany.AppUtils");
            const status = ac.class("AnimalCompany.AppUtils").nested("SteamVRHeadsetStatus");
            const xr = apputils.method("GetXRBackend");
            const getstatus = apputils.method("GetSteamVRHeadsetStatus");
            const active = apputils.method("IsSteamVRHeadsetActive");
            function syteLog(e) {
              console.log("[+] " + e);
            }
            function syteError(e) {
              console.log("[-] " + e);
            }
            function syteInfo(e) {
              console.log("[i] " + e);
            }
            function initPcWin32Input() {
              if (_pcWin32InitAttempted) {
                return !!_GAKS;
              }
              _pcWin32InitAttempted = true;
              try {
                let e = null;
                try {
                  e = Process.getModuleByName("user32.dll");
                } catch (t) {
                  try {
                    e = Process.findModuleByName("user32.dll");
                  } catch (e) {}
                }
                if (!e) {
                  try {
                    e = Module.load("user32.dll");
                  } catch (e) {}
                }
                let t = null;
                let o = null;
                let n = null;
                let i = null;
                let a = null;
                try {
                  t = e?.getExportByName ? e.getExportByName("GetAsyncKeyState") : Module.getExportByName("user32.dll", "GetAsyncKeyState");
                } catch (e) {}
                try {
                  o = e?.getExportByName ? e.getExportByName("GetCursorPos") : Module.getExportByName("user32.dll", "GetCursorPos");
                } catch (e) {}
                try {
                  n = e?.getExportByName ? e.getExportByName("GetForegroundWindow") : Module.getExportByName("user32.dll", "GetForegroundWindow");
                  i = e?.getExportByName ? e.getExportByName("ScreenToClient") : Module.getExportByName("user32.dll", "ScreenToClient");
                  a = e?.getExportByName ? e.getExportByName("GetClientRect") : Module.getExportByName("user32.dll", "GetClientRect");
                } catch (e) {}
                if (t) {
                  _GAKS = new NativeFunction(t, "int16", ["int"]);
                  if (o) {
                    _GCP = new NativeFunction(o, "bool", ["pointer"]);
                  }
                  if (n) {
                    _GFW = new NativeFunction(n, "pointer", []);
                  }
                  if (i) {
                    _S2C = new NativeFunction(i, "bool", ["pointer", "pointer"]);
                  }
                  if (a) {
                    _GCR = new NativeFunction(a, "bool", ["pointer", "pointer"]);
                  }
                  if (!_pcWin32ReadyLogged) {
                    _pcWin32ReadyLogged = true;
                    syteLog("[SolarHP] Win32 Ready! Press 'Q' to toggle Menu/Fly.");
                  }
                  return true;
                } else {
                  syteError("[Syte PC] GetAsyncKeyState not found");
                  return false;
                }
              } catch (e) {
                syteError("[SolarHP] Win32 init failed: " + e);
                return false;
              }
            }
            function _pcKeyDown(e) {
              try {
                return (!!_GAKS || !!initPcWin32Input()) && !!(Number(_GAKS(e)) & 32768);
              } catch (e) {
                return false;
              }
            }
            function syteHookReturn(e, t, o, n, i = 0) {
              try {
                const a = e.method(t, i);
                const l = a.nativeFunction;
                a.implementation = function (...e) {
                  if (o()) {
                    return n;
                  } else {
                    return l.call(this, ...e);
                  }
                };
                return true;
              } catch (e) {
                return false;
              }
            }
            function syteInstallShopHooks() {
              if (!shopHooksInstalled) {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.GameplayItemState");
                  for (const t of ["get_isPurchasable", "get_isResearchable", "get_isProduct"]) {
                    syteHookReturn(e, t, () => shopUnlockEnabled, true);
                  }
                  for (const t of ["get_allowSaving", "get_allowBlueprintSaving", "get_canBeSavedToLoadoutTemplate"]) {
                    syteHookReturn(e, t, () => blueprintAbuseEnabled, true);
                  }
                  syteHookReturn(e, "get_maxInBlueprint", () => blueprintAbuseEnabled, 999);
                  const t = AssemblyCSharp.class("AnimalCompany.GameManager");
                  syteHookReturn(t, "TrySpendMoney", () => noSpendEnabled, true, 1);
                  syteHookReturn(t, "TrySpendFishy", () => noSpendEnabled, true, 1);
                  shopHooksInstalled = true;
                } catch (e) {
                  syteError("Shop hooks failed: " + e);
                }
              }
            }
            function syteSafeImage(e) {
              try {
                const t = Il2Cpp.domain.assembly(e);
                if (t && !t.handle.isNull()) {
                  return t.image;
                }
              } catch (e) {}
              syteError("Assembly not found: " + e);
              return null;
            }
            active.implementation = function () {
              return true;
            };
            xr.implementation = function () {
              return 2;
            };
            getstatus.implementation = function () {
              const e = status.new().unbox();
              e.field("activeLoaderPresent").value = true;
              e.field("xrDisplayRunning").value = true;
              e.field("headDeviceValid").value = true;
              e.field("userPresenceKnown").value = true;
              e.field("userPresent").value = true;
              return e;
            };
            const images = {
              AnimalCompany: Il2Cpp.domain.assembly("AnimalCompany").image,
              "UnityEngine.CoreModule": Il2Cpp.domain.assembly("UnityEngine.CoreModule").image,
              "UnityEngine.PhysicsModule": Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image,
              "UnityEngine.UIModule": Il2Cpp.domain.assembly("UnityEngine.UIModule").image,
              "UnityEngine.UI": Il2Cpp.domain.assembly("UnityEngine.UI").image,
              "UnityEngine.TextRenderingModule": Il2Cpp.domain.assembly("UnityEngine.TextRenderingModule").image,
              PhotonFusionNetworking: Il2Cpp.domain.assembly("Fusion.Runtime").image,
              PhotonFusionNetworkingRealtime: Il2Cpp.domain.assembly("Fusion.Realtime").image,
              "Unity.TextMeshPro": Il2Cpp.domain.assembly("Unity.TextMeshPro").image,
              "UnityEngine.XRModule": Il2Cpp.domain.assembly("UnityEngine.XRModule").image,
              "UnityEngine.AudioModule": Il2Cpp.domain.assembly("UnityEngine.AudioModule").image,
              "Oculus.Platform": Il2Cpp.domain.assembly("Oculus.Platform").image
            };
            const acImage = Il2Cpp.domain.assembly("AnimalCompany").image;
            const AssemblyCSharp = images.AnimalCompany;
            const UnityEngineCore = images["UnityEngine.CoreModule"];
            const UnityEnginePhysics = images["UnityEngine.PhysicsModule"];
            const UnityEngineUI = images["UnityEngine.UI"];
            const UnityEngineUIModule = images["UnityEngine.UIModule"];
            const UnityEngineTextRendering = images["UnityEngine.TextRenderingModule"];
            const FusionRuntime = images["Fusion.Runtime"];
            const PhotonFusionNetworking = images.PhotonFusionNetworking;
            const UnityTextMeshPro = images["Unity.TextMeshPro"];
            const UnityEngineXR = images["UnityEngine.XRModule"];
            const UnityEngineAudio = images["UnityEngine.AudioModule"];
            const OculusPlatformSettings = images["Oculus.Platform"].class("Oculus.Platform.PlatformSettings");
            const GTPlayerClass = AssemblyCSharp.class("AnimalCompany.GorillaLocomotion");
            const NetPlayerClass = AssemblyCSharp.class("AnimalCompany.NetPlayer");
            const NetPlayer = AssemblyCSharp.class("AnimalCompany.NetPlayer");
            const ElevatorManager = AssemblyCSharp.class("AnimalCompany.ElevatorManager");
            const ArenaGameManager = AssemblyCSharp.class("AnimalCompany.ArenaGameManager");
            const GrabbableObject = AssemblyCSharp.class("AnimalCompany.GrabbableObject");
            const PickupManager = AssemblyCSharp.class("AnimalCompany.PickupManager");
            const ItemSellingMachineController = AssemblyCSharp.class("AnimalCompany.ItemSellingMachineController");
            const PrefabGen = AssemblyCSharp.class("AnimalCompany.PrefabGenerator");
            const GBIClass = AssemblyCSharp.class("AnimalCompany.GrabbableItem");
            const MeshCollider = UnityEnginePhysics.class("UnityEngine.MeshCollider");
            const Light = UnityEngineCore.class("UnityEngine.Light");
            const BackpackItemClass = AssemblyCSharp.class("AnimalCompany.BackpackItem");
            const QuiverClass = AssemblyCSharp.class("AnimalCompany.Quiver");
            let CrossbowClass = null;
            try {
              CrossbowClass = AssemblyCSharp.class("AnimalCompany.Crossbow");
            } catch (e) {}
            let HeartGunClass = null;
            try {
              HeartGunClass = AssemblyCSharp.class("AnimalCompany.HeartGun");
            } catch (e) {}
            let GrenadeLauncherClass = null;
            try {
              GrenadeLauncherClass = AssemblyCSharp.class("AnimalCompany.GrenadeLauncher");
            } catch (e) {}
            let SalmonCannonClass = null;
            try {
              SalmonCannonClass = AssemblyCSharp.class("AnimalCompany.SalmonCannon");
            } catch (e) {}
            let MobControllerBase = null;
            try {
              MobControllerBase = AssemblyCSharp.class("AnimalCompany.MobController");
            } catch (e) {}
            let NetSessionPrivateRoomManagerClass = null;
            try {
              NetSessionPrivateRoomManagerClass = AssemblyCSharp.class("AnimalCompany.NetSessionPrivateRoomManager");
            } catch (e) {}
            let RoomJoinComputerProgramClass = null;
            try {
              RoomJoinComputerProgramClass = AssemblyCSharp.class("AnimalCompany.RoomJoinComputerProgram");
            } catch (e) {}
            let PlayerWatchDevMenuMediatorClass = null;
            try {
              PlayerWatchDevMenuMediatorClass = AssemblyCSharp.class("AnimalCompany.PlayerWatchDevMenuMediator");
            } catch (e) {}
            let ModerationMenuMediatorClass = null;
            try {
              ModerationMenuMediatorClass = AssemblyCSharp.class("AnimalCompany.ModerationMenuMediator");
            } catch (e) {}
            let PrivateRoomStateClass = null;
            try {
              PrivateRoomStateClass = AssemblyCSharp.class("AnimalCompany.PrivateRoomState");
            } catch (e) {}
            let UserCacheManagerClass = null;
            try {
              UserCacheManagerClass = AssemblyCSharp.class("AnimalCompany.UserCacheManager");
            } catch (e) {}
            let AnimalCompanyApiClass = null;
            try {
              AnimalCompanyApiClass = AssemblyCSharp.class("AnimalCompany.API.AnimalCompanyAPI");
            } catch (e) {}
            let ItemSellingMachineButtonViewClass = null;
            try {
              ItemSellingMachineButtonViewClass = AssemblyCSharp.class("AnimalCompany.ItemSellingMachineButtonView");
            } catch (e) {}
            let GrabbablePurchaseClass = null;
            try {
              GrabbablePurchaseClass = AssemblyCSharp.class("AnimalCompany.GrabbablePurchase");
            } catch (e) {}
            let ArenaGrabbablePurchaseClass = null;
            try {
              ArenaGrabbablePurchaseClass = AssemblyCSharp.class("AnimalCompany.ArenaGrabablePurchase");
            } catch (e) {}
            const AudioClipClass = UnityEngineAudio.class("UnityEngine.AudioClip");
            const PhotonVoiceImage = Il2Cpp.domain.assembly("PhotonVoice").image;
            const VoiceConnectionClass = PhotonVoiceImage.class("Photon.Voice.Unity.VoiceConnection");
            const NetworkSessionManagerClass = AssemblyCSharp.class("AnimalCompany.NetworkSessionManager");
            const DamageSourceInfoClass = AssemblyCSharp.class("AnimalCompany.DamageSourceInfo");
            const GameplayItemStateClass = AssemblyCSharp.class("AnimalCompany.GameplayItemState");
            const RigidbodyClass = UnityEnginePhysics.class("UnityEngine.Rigidbody");
            const PCClass = AssemblyCSharp.class("AnimalCompany.PlayerController");
            const GBOClass = AssemblyCSharp.class("AnimalCompany.GrabbableObject");
            const StickyAnchorClass = AssemblyCSharp.class("AnimalCompany.StickyAnchor");
            const SFXManager = AssemblyCSharp.class("AnimalCompany.SFXManager");
            const NManager = AssemblyCSharp.class("AnimalCompany.NetworkManager");
            const GorillaReportButton = AssemblyCSharp.class("AnimalCompany.ComputerTerminalKey");
            const InputDevices = UnityEngineXR.class("UnityEngine.XR.InputDevices");
            const CommonUsages = UnityEngineXR.class("UnityEngine.XR.CommonUsages");
            const GameObject = UnityEngineCore.class("UnityEngine.GameObject");
            const Application = UnityEngineCore.class("UnityEngine.Application");
            const Object = UnityEngineCore.class("UnityEngine.Object");
            const Vector3 = UnityEngineCore.class("UnityEngine.Vector3");
            const Quaternion = UnityEngineCore.class("UnityEngine.Quaternion");
            const Time = UnityEngineCore.class("UnityEngine.Time");
            const Resources = UnityEngineCore.class("UnityEngine.Resources");
            const Material = UnityEngineCore.class("UnityEngine.Material");
            const Renderer = UnityEngineCore.class("UnityEngine.Renderer");
            const Color = UnityEngineCore.class("UnityEngine.Color");
            const Shader = UnityEngineCore.class("UnityEngine.Shader");
            const Camera = UnityEngineCore.class("UnityEngine.Camera");
            const RectTransform = UnityEngineCore.class("UnityEngine.RectTransform");
            const LineRenderer = UnityEngineCore.class("UnityEngine.LineRenderer");
            const Mesh = UnityEngineCore.class("UnityEngine.Mesh");
            const MeshFilter = UnityEngineCore.class("UnityEngine.MeshFilter");
            const BoxCollider = UnityEnginePhysics.class("UnityEngine.BoxCollider");
            const Collider = UnityEnginePhysics.class("UnityEngine.Collider");
            const Rigidbody = UnityEnginePhysics.class("UnityEngine.Rigidbody");
            const instanceField = GTPlayerClass.fields.find(e => e.name.includes("Instance"));
            const Physics = UnityEnginePhysics.class("UnityEngine.Physics");
            const Component = UnityEngineCore.class("UnityEngine.Component");
            const MonoBehaviour = UnityEngineCore.class("UnityEngine.MonoBehaviour");
            const ParticleManager = AssemblyCSharp.class("AnimalCompany.ParticleManager");
            const Canvas = UnityEngineUIModule.class("UnityEngine.Canvas");
            const CanvasGroup = UnityEngineUIModule.class("UnityEngine.CanvasGroup");
            const CanvasScaler = UnityEngineUI.class("UnityEngine.UI.CanvasScaler");
            const GraphicRaycaster = UnityEngineUI.class("UnityEngine.UI.GraphicRaycaster");
            const Text = UnityEngineUI.class("UnityEngine.UI.Text");
            const ParticleManagerClass = ParticleManager;
            let VFXManagerClass = null;
            try {
              VFXManagerClass = AssemblyCSharp.class("AnimalCompany.VFXManager");
            } catch (e) {}
            let MobDataUtilityClass = null;
            try {
              MobDataUtilityClass = AssemblyCSharp.class("AnimalCompany.MobDataUtility");
            } catch (e) {}
            const Font = UnityEngineTextRendering.class("UnityEngine.Font");
            let TextMesh = null;
            let TextMeshPro3D = null;
            let TMPSettingsClass = null;
            let tmpDefaultFontAsset = null;
            try {
              TextMesh = UnityEngineTextRendering.class("UnityEngine.TextMesh");
            } catch (e) {}
            if (!TextMesh) {
              try {
                TextMesh = UnityEngineCore.class("UnityEngine.TextMesh");
              } catch (e) {}
            }
            try {
              TextMeshPro3D = UnityTextMeshPro.class("TMPro.TextMeshPro");
            } catch (e) {}
            try {
              TMPSettingsClass = UnityTextMeshPro.class("TMPro.TMP_Settings");
            } catch (e) {}
            let GTPlayer = null;
            const UberShader = Shader.method("Find").invoke(Il2Cpp.string("Universal Render Pipeline/Unlit"));
            const TextShader = Shader.method("Find").invoke(Il2Cpp.string("UI/Default"));
            const zeroVector = Vector3.field("zeroVector").value;
            const oneVector = Vector3.field("oneVector").value;
            const identityQuaternion = Quaternion.field("identityQuaternion").value;
            let leftHandTransform = null;
            let rightHandTransform = null;
            let headCollider = null;
            let bodyCollider = null;
            let arial = null;
            let arialRetryAfter = 0;
            function getarialnocrash2() {
              if (arial && !arial.isNull()) {
                return arial;
              }
              const e = Date.now();
              if (e < arialRetryAfter) {
                return null;
              }
              try {
                arial = Resources.method("GetBuiltinResource", 1).inflate(Font).invoke(Il2Cpp.string("Arial.ttf"));
              } catch (t) {
                arial = null;
                arialRetryAfter = e + 2000;
                if (!menuFontWarned) {
                  menuFontWarned = true;
                  console.error("[menu] Arial.ttf builtin font load failed during startup/runtime; text will retry later");
                }
              }
              return arial;
            }
            function gettmpfontnocrash() {
              if (tmpDefaultFontAsset && !tmpDefaultFontAsset.isNull()) {
                return tmpDefaultFontAsset;
              }
              if (!TMPSettingsClass) {
                return null;
              }
              try {
                tmpDefaultFontAsset = TMPSettingsClass.method("get_defaultFontAsset").invoke();
              } catch (e) {}
              if (!tmpDefaultFontAsset || tmpDefaultFontAsset.isNull()) {
                try {
                  const e = TMPSettingsClass.method("get_instance").invoke();
                  if (e && !e.isNull()) {
                    try {
                      tmpDefaultFontAsset = e.method("get_defaultFontAsset").invoke();
                    } catch (e) {}
                    if (!tmpDefaultFontAsset || tmpDefaultFontAsset.isNull()) {
                      try {
                        tmpDefaultFontAsset = e.field("m_defaultFontAsset").value;
                      } catch (e) {}
                    }
                  }
                } catch (e) {}
              }
              if (tmpDefaultFontAsset && !tmpDefaultFontAsset.isNull()) {
                return tmpDefaultFontAsset;
              } else {
                return null;
              }
            }
            const NullMethod = {
              invoke: () => NullObject,
              inflate: () => NullMethod,
              overload: () => NullMethod
            };
            const NullObject = {
              handle: {
                isNull: () => true
              },
              isNull: () => true,
              method: () => NullMethod,
              field: () => ({
                value: NullObject
              }),
              tryField: () => null,
              tryMethod: () => null,
              class: {
                type: {
                  name: "NullObject"
                }
              }
            };
            function isLiveObject(e) {
              try {
                return !!e && !(e.isNull?.() ?? 1);
              } catch (e) {
                return false;
              }
            }
            function refreshRuntimeRefs() {
              try {
                const e = GTPlayerClass.field("<Instance>k__BackingField").value;
                if (isLiveObject(e)) {
                  GTPlayer = e;
                }
              } catch (e) {}
              try {
                if (isLiveObject(GTPlayer)) {
                  try {
                    const e = GTPlayer.field("leftHandTransform").value;
                    if (isLiveObject(e)) {
                      leftHandTransform = e;
                    }
                  } catch (e) {}
                  try {
                    const e = GTPlayer.field("rightHandTransform").value;
                    if (isLiveObject(e)) {
                      rightHandTransform = e;
                    }
                  } catch (e) {}
                  try {
                    const e = GTPlayer.field("headCollider").value;
                    if (isLiveObject(e)) {
                      headCollider = e;
                    }
                  } catch (e) {}
                  try {
                    const e = GTPlayer.field("bodyCollider").value;
                    if (isLiveObject(e)) {
                      bodyCollider = e;
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              return isLiveObject(GTPlayer) && isLiveObject(leftHandTransform) && isLiveObject(rightHandTransform) && isLiveObject(bodyCollider);
            }
            let runtimeRefMissCount = 0;
            function logButtonRuntimeError(e, t) {
              const o = time ?? 0;
              const n = e.__nextErrorLogTime ?? 0;
              const i = (e.__failCount ?? 0) + 1;
              e.__failCount = i;
              if (i === 1 || o >= n) {
                console.error(`[LateUpdate] Error in '${e.buttonText}':`, t);
                e.__nextErrorLogTime = o + 1.5;
              }
            }
            function Destroy(e) {
              try {
                if (!e || e === NullObject) {
                  return;
                }
                if (e.isNull?.()) {
                  return;
                }
                Object.method("Destroy", 1).invoke(e);
              } catch (e) {}
            }
            function getComponent(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  return null;
                } else {
                  return e.method("GetComponent", 1).inflate(t).invoke();
                }
              } catch (e) {
                return null;
              }
            }
            function getComponentInParent(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  return null;
                } else {
                  return e.method("GetComponentInParent", 0).inflate(t).invoke();
                }
              } catch (e) {
                return null;
              }
            }
            function addComponent(e, t) {
              return e.method("AddComponent", 1).inflate(t).invoke();
            }
            function getTransform(e) {
              return e.method("get_transform").invoke();
            }
            function playerIsLocal(e) {
              return e.method("get_IsMine").invoke();
            }
            function setMenuTextScale(e, t, o) {
              try {
                const n = getTransform(t).method("get_localScale").invoke();
                const i = Math.abs(n.field("x").value) || 1;
                const a = Math.abs(n.field("y").value) || 1;
                const l = Math.abs(n.field("z").value) || 1;
                const r = o * 0.42 / i;
                e.method("set_localScale").invoke([-r, o / a, o / l]);
              } catch (t) {
                try {
                  e.method("set_localScale").invoke([o * -0.42, o, o]);
                } catch (e) {}
              }
            }
            function createEmptyObject(e = "", t = null) {
              let o = null;
              try {
                o = GameObject.alloc();
                try {
                  o.method(".ctor", 1).invoke(Il2Cpp.string(e));
                } catch (t) {
                  o.method(".ctor", 0).invoke();
                  try {
                    o.method("set_name").invoke(Il2Cpp.string(e));
                  } catch (e) {}
                }
              } catch (e) {
                o = createObject(zeroVector, identityQuaternion, oneVector, 3, [0, 0, 0, 0], t);
                try {
                  const e = getComponent(o, Collider);
                  if (e && !e.isNull()) {
                    e.method("set_enabled").invoke(false);
                  }
                } catch (e) {}
                return o;
              }
              const n = getTransform(o);
              if (t != null) {
                n.method("SetParent", 2).invoke(t, false);
              }
              return o;
            }
            function invokeInstance(e, t, ...o) {
              return e.invokeRaw(t, ...o);
            }
            function tryMethodName(e, t, o = -1) {
              for (const n of t) {
                try {
                  if (o >= 0) {
                    return e.method(n, o);
                  } else {
                    return e.method(n);
                  }
                } catch (e) {}
              }
              return null;
            }
            function isClassNamed(e, t) {
              try {
                return e && !e.isNull() && e.class.type.name === t;
              } catch (e) {
                return false;
              }
            }
            const NetworkRunner = Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkRunner");
            const NULL = Il2Cpp.reference(Il2Cpp.domain.assembly("mscorlib").image.class("System.Object").alloc());
            function spawnItemAtPos(e, t, o) {
              try {
                const n = String(e || "").replace(/^item_prefab\//, "");
                const i = n.indexOf("item_prefab/") === 0 ? n : "item_prefab/" + n;
                let a = spawnItemAsync(Il2Cpp.string(n), t, o, NULL);
                if (a && !a.isNull?.()) {
                  return a;
                } else {
                  a = spawnItemAsync(Il2Cpp.string(i), t, o, NULL);
                  if (a && !a.isNull?.()) {
                    return a;
                  } else {
                    a = spawnItemFromPrefabID(n, t, o, NULL) || spawnItemFromPrefabID(i, t, o, NULL);
                    if (a && !a.isNull?.()) {
                      return a;
                    } else {
                      return findSpawnedItemNear(n, t, 1.5);
                    }
                  }
                }
              } catch (t) {
                console.error("[spawnItemAtPos] " + e + ": " + t);
                return;
              }
            }
            let itemSpawnSourceDefault = null;
            function getItemSpawnSourceDefault() {
              if (itemSpawnSourceDefault !== null) {
                return itemSpawnSourceDefault;
              }
              const e = AssemblyCSharp.class("AnimalCompany.ItemSpawnSource");
              const t = ["None", "Unknown", "Debug", "Spawned", "Shop", "Chest", "Gift", "Default"];
              for (const o of t) {
                try {
                  itemSpawnSourceDefault = e.field(o).value;
                  return itemSpawnSourceDefault;
                } catch (e) {}
              }
              itemSpawnSourceDefault = 0;
              return itemSpawnSourceDefault;
            }
            function spawnItemAsync(e, t, o, n = NULL) {
              const i = typeof e == "string" ? Il2Cpp.string(e) : e;
              const a = getItemSpawnSourceDefault();
              try {
                const e = PrefabGen.method("SpawnItem", 5).overload("System.String", "UnityEngine.Vector3", "UnityEngine.Quaternion", "OnBeforeSpawned", "AnimalCompany.ItemSpawnSource").invoke(i, t, o, n, a);
                if (e && !e.isNull?.()) {
                  return e;
                }
              } catch (e) {}
              try {
                const e = PrefabGen.method("SpawnItem", 5).invoke(i, t, o, n, a);
                if (e && !e.isNull?.()) {
                  return e;
                }
              } catch (e) {}
              try {
                PrefabGen.method("SpawnItemAsync", 5).overload("System.String", "UnityEngine.Vector3", "UnityEngine.Quaternion", "Fusion.NetworkObjectSpawnDelegate", "AnimalCompany.ItemSpawnSource").invoke(i, t, o, NULL, a);
              } catch (e) {}
              return null;
            }
            function spawnItemFromPrefabID(e, t, o, n = NULL) {
              try {
                const i = PrefabGen.method("GetItemPrefab", 1).invoke(Il2Cpp.string(e));
                if (!i || i.isNull?.()) {
                  return null;
                }
                const a = PrefabGen.method("SpawnItem", 5).overload("AnimalCompany.GrabbableItemPrefab", "UnityEngine.Vector3", "UnityEngine.Quaternion", "OnBeforeSpawned", "AnimalCompany.ItemSpawnSource").invoke(i, t, o, n, getItemSpawnSourceDefault());
                if (a && !a.isNull?.()) {
                  return a;
                }
              } catch (e) {}
              try {
                const i = PrefabGen.method("GetItemPrefab", 1).invoke(Il2Cpp.string(e));
                if (!i || i.isNull?.()) {
                  return null;
                }
                const a = PrefabGen.method("SpawnItem", 5).invoke(i, t, o, n, getItemSpawnSourceDefault());
                if (a && !a.isNull?.()) {
                  return a;
                }
              } catch (e) {}
              return null;
            }
            const fallbackTextBlueprintPieces = [{
              pos: [-4.592, 7.076, -1.16]
            }, {
              pos: [-4.061, 7.081, -1.159]
            }, {
              pos: [-4.207, 7.176, -1.167]
            }, {
              pos: [-4.212, 7.705, -1.21]
            }, {
              pos: [-4.119, 8.235, -1.253]
            }, {
              pos: [-4.124, 8.765, -1.297]
            }, {
              pos: [-4.178, 9.294, -1.34]
            }, {
              pos: [-4.235, 10.351, -1.427]
            }, {
              pos: [-4.239, 10.881, -1.471]
            }, {
              pos: [-4.533, 11.311, -1.507]
            }, {
              pos: [-4.77, 10.828, -1.468]
            }, {
              pos: [-4.717, 10.299, -1.424]
            }, {
              pos: [-4.664, 9.77, -1.381]
            }, {
              pos: [-4.708, 9.241, -1.338]
            }, {
              pos: [-4.703, 8.712, -1.295]
            }, {
              pos: [-4.699, 8.182, -1.251]
            }, {
              pos: [-4.742, 7.653, -1.209]
            }, {
              pos: [-4.243, 11.314, -1.505]
            }, {
              pos: [-3.712, 11.319, -1.504]
            }, {
              pos: [-3.181, 11.324, -1.502]
            }, {
              pos: [-2.744, 11.039, -1.477]
            }, {
              pos: [-3.225, 10.794, -1.459]
            }, {
              pos: [-3.756, 10.837, -1.465]
            }, {
              pos: [-2.892, 11.375, -1.505]
            }, {
              pos: [-2.36, 11.331, -1.499]
            }, {
              pos: [-2.695, 10.894, -1.465]
            }, {
              pos: [-2.405, 10.945, -1.468]
            }, {
              pos: [-3.939, 9.681, -1.37]
            }, {
              pos: [-3.408, 9.637, -1.365]
            }, {
              pos: [-3.742, 9.249, -1.335]
            }, {
              pos: [2.562, 10.682, -1.448]
            }, {
              pos: [3.043, 10.831, -1.458]
            }, {
              pos: [1.02, 11.218, -1.475]
            }, {
              pos: [2.802, 10.925, -1.466]
            }, {
              pos: [1.02, 11.17, -1.471]
            }, {
              pos: [0.775, 11.648, -1.512]
            }, {
              pos: [0.244, 11.692, -1.518]
            }, {
              pos: [-0.287, 11.59, -1.511]
            }, {
              pos: [-0.768, 11.441, -1.501]
            }, {
              pos: [-1.249, 11.197, -1.483]
            }, {
              pos: [-1.534, 10.761, -1.449]
            }, {
              pos: [-1.675, 10.279, -1.41]
            }, {
              pos: [-1.719, 9.749, -1.367]
            }, {
              pos: [-1.715, 9.219, -1.323]
            }, {
              pos: [-1.661, 8.691, -1.28]
            }, {
              pos: [-1.464, 8.212, -1.24]
            }, {
              pos: [-1.17, 7.781, -1.203]
            }, {
              pos: [-0.734, 7.496, -1.178]
            }, {
              pos: [-0.249, 7.357, -1.165]
            }, {
              pos: [0.282, 7.313, -1.159]
            }, {
              pos: [0.813, 7.317, -1.157]
            }, {
              pos: [1.343, 7.419, -1.163]
            }, {
              pos: [1.775, 7.713, -1.185]
            }, {
              pos: [1.243, 7.755, -1.191]
            }, {
              pos: [0.763, 7.559, -1.177]
            }, {
              pos: [0.232, 7.505, -1.175]
            }, {
              pos: [-0.252, 7.645, -1.188]
            }, {
              pos: [-0.737, 7.882, -1.21]
            }, {
              pos: [-1.031, 8.311, -1.246]
            }, {
              pos: [-1.179, 8.84, -1.29]
            }, {
              pos: [-1.281, 9.368, -1.334]
            }, {
              pos: [-1.286, 9.897, -1.377]
            }, {
              pos: [-1.049, 10.381, -1.416]
            }, {
              pos: [-0.763, 10.816, -1.45]
            }, {
              pos: [-0.38, 11.157, -1.477]
            }, {
              pos: [0.151, 11.209, -1.479]
            }, {
              pos: [0.54, 10.877, -1.449]
            }, {
              pos: [4.542, 11.106, -1.451]
            }, {
              pos: [4.01, 11.197, -1.46]
            }, {
              pos: [3.479, 11.143, -1.458]
            }, {
              pos: [3.106, 10.081, -1.373]
            }, {
              pos: [3.447, 9.7, -1.34]
            }, {
              pos: [3.933, 9.415, -1.314]
            }, {
              pos: [4.418, 9.18, -1.293]
            }, {
              pos: [4.855, 8.846, -1.264]
            }, {
              pos: [5.244, 8.513, -1.235]
            }, {
              pos: [5.442, 8.034, -1.195]
            }, {
              pos: [5.447, 7.505, -1.152]
            }, {
              pos: [5.16, 7.069, -1.118]
            }, {
              pos: [4.679, 6.872, -1.103]
            }, {
              pos: [4.149, 6.818, -1.101]
            }, {
              pos: [3.617, 6.863, -1.107]
            }, {
              pos: [3.133, 7.001, -1.12]
            }, {
              pos: [2.792, 7.384, -1.153]
            }, {
              pos: [3.322, 7.437, -1.155]
            }, {
              pos: [3.806, 7.297, -1.142]
            }, {
              pos: [4.288, 7.445, -1.152]
            }, {
              pos: [4.525, 7.93, -1.19]
            }, {
              pos: [4.232, 8.359, -1.228]
            }, {
              pos: [3.794, 8.692, -1.257]
            }, {
              pos: [3.405, 9.026, -1.285]
            }, {
              pos: [3.064, 9.408, -1.318]
            }, {
              pos: [2.722, 9.79, -1.351]
            }, {
              pos: [2.525, 10.268, -1.39]
            }, {
              pos: [2.417, 11.182, -1.466]
            }, {
              pos: [2.751, 11.57, -1.496]
            }, {
              pos: [3.282, 11.671, -1.503]
            }, {
              pos: [3.813, 11.675, -1.5]
            }, {
              pos: [4.344, 11.633, -1.494]
            }, {
              pos: [4.876, 11.492, -1.481]
            }, {
              pos: [-4.628, 10.301, -1.425]
            }];
            const TEXT_SPAWN_BLUEPRINT_PATH = "C:\\Users\\Dawid Jurek\\Downloads\\blueprint.json";
            const TEXT_SPAWN_SCALE = 0.16;
            function readTextBlueprintPieces() {
              if (textBlueprintCache && textBlueprintCache.length > 0) {
                return textBlueprintCache;
              }
              let e = [];
              try {
                let t = "";
                const o = new (0, globalThis.File)(TEXT_SPAWN_BLUEPRINT_PATH, "r");
                try {
                  t = o.readText(1048576);
                } catch (e) {
                  try {
                    let e = null;
                    while ((e = o.readLine()) !== null) {
                      t += String(e) + "\n";
                    }
                  } catch (e) {}
                }
                try {
                  o.close();
                } catch (e) {}
                if (t && t.length > 0) {
                  const o = JSON.parse(t);
                  const n = o?.items ?? [];
                  for (const t of n) {
                    const o = t?.stuckChildren ?? [];
                    for (const t of o) {
                      const o = t?.pos ?? null;
                      if (!o || o.length < 3) {
                        continue;
                      }
                      const n = t?.item ?? {};
                      e.push({
                        itemID: "item_ore_hell",
                        pos: [Number(o[0]) || 0, Number(o[1]) || 0, Number(o[2]) || 0],
                        scaleModifier: Number(n?.scaleModifier ?? 0) || 0,
                        colorHue: Number(n?.colorHue ?? 0) || 0,
                        colorSaturation: Number(n?.colorSaturation ?? 0) || 0
                      });
                    }
                  }
                }
              } catch (e) {
                console.error("[TextSpawn] blueprint read failed, using embedded copy:", e);
              }
              if (e.length === 0) {
                e = fallbackTextBlueprintPieces;
              }
              textBlueprintCache = e;
              return e;
            }
            function applyTextPieceConfig(e, t) {
              try {
                const o = e.method("GetComponent", 1).inflate(GBOClass).invoke();
                if (!o || o.isNull?.()) {
                  return;
                }
                try {
                  o.method("set_scaleModifier").invoke(Number(t?.scaleModifier ?? 0) || 0);
                } catch (e) {}
                try {
                  o.method("set_colorHue").invoke(Number(t?.colorHue ?? 0) || 0);
                } catch (e) {}
                try {
                  o.method("set_colorSaturation").invoke(Number(t?.colorSaturation ?? 0) || 0);
                } catch (e) {}
              } catch (e) {}
            }
            function freezeSpawnedObjectInPlace(e, t = true) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                trySetObjectVelocity(e, [0, 0, 0]);
                let o = getRootLikeObject(e) ?? e;
                try {
                  const t = e.method("get_gameObject").invoke();
                  if (t && !t.isNull?.()) {
                    o = t;
                  }
                } catch (e) {}
                let n = null;
                try {
                  n = getComponent(o, Rigidbody);
                } catch (e) {}
                if (!n || n.isNull?.()) {
                  try {
                    n = o.method("GetComponentInChildren", 0).inflate(Rigidbody).invoke();
                  } catch (e) {}
                }
                if (n && !n.isNull?.()) {
                  try {
                    n.method("set_velocity").invoke([0, 0, 0]);
                  } catch (e) {}
                  try {
                    n.method("set_linearVelocity").invoke([0, 0, 0]);
                  } catch (e) {}
                  try {
                    n.method("set_angularVelocity").invoke([0, 0, 0]);
                  } catch (e) {}
                  try {
                    n.method("set_useGravity").invoke(false);
                  } catch (e) {}
                  try {
                    n.method("set_isKinematic").invoke(true);
                  } catch (e) {}
                  try {
                    n.method("set_constraints").invoke(126);
                  } catch (e) {}
                  try {
                    n.method("set_detectCollisions").invoke(false);
                  } catch (e) {}
                  try {
                    n.method("Sleep").invoke();
                  } catch (e) {}
                }
                if (t) {
                  try {
                    const e = o.method("GetComponentsInChildren", 1).inflate(Collider).invoke(false);
                    if (e && !e.isNull?.()) {
                      for (let t = 0; t < Math.min(e.length, 64); t++) {
                        try {
                          e.method("get_Item").invoke(t).method("set_isTrigger").invoke(true);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              } catch (e) {}
            }
            function freezeGridItemUntilPickedUp(e) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                const t = getRootLikeObject(e) ?? e;
                let o = null;
                try {
                  o = getComponent(t, Rigidbody);
                } catch (e) {}
                if (!o || o.isNull?.()) {
                  try {
                    o = t.method("GetComponentInChildren", 0).inflate(Rigidbody).invoke();
                  } catch (e) {}
                }
                if (o && !o.isNull?.()) {
                  try {
                    o.method("set_velocity").invoke([0, 0, 0]);
                  } catch (e) {}
                  try {
                    o.method("set_linearVelocity").invoke([0, 0, 0]);
                  } catch (e) {}
                  try {
                    o.method("set_angularVelocity").invoke([0, 0, 0]);
                  } catch (e) {}
                  try {
                    o.method("set_useGravity").invoke(false);
                  } catch (e) {}
                  try {
                    o.method("set_isKinematic").invoke(true);
                  } catch (e) {}
                  try {
                    o.method("Sleep").invoke();
                  } catch (e) {}
                }
                const n = normalizeSceneObjectHandle(t);
                if (!gridFrozenItems.some(e => e.rootKey && e.rootKey === n)) {
                  gridFrozenItems.push({
                    object: t,
                    rootKey: n
                  });
                }
              } catch (e) {
                console.error("[Grid mods freeze]", e);
              }
            }
            function unfreezeGridItem(e) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                const t = getRootLikeObject(e) ?? e;
                let o = null;
                try {
                  o = getComponent(t, Rigidbody);
                } catch (e) {}
                if (!o || o.isNull?.()) {
                  try {
                    o = t.method("GetComponentInChildren", 0).inflate(Rigidbody).invoke();
                  } catch (e) {}
                }
                if (o && !o.isNull?.()) {
                  try {
                    o.method("set_isKinematic").invoke(false);
                  } catch (e) {}
                  try {
                    o.method("set_useGravity").invoke(true);
                  } catch (e) {}
                  try {
                    o.method("WakeUp").invoke();
                  } catch (e) {}
                }
              } catch (e) {
                console.error("[Grid mods unfreeze]", e);
              }
            }
            function updateGridFrozenItems() {
              if (gridFrozenItems.length === 0) {
                return;
              }
              const e = new Set();
              try {
                for (const t of getAllNetPlayersList(true)) {
                  for (const o of [0, 1]) {
                    try {
                      const n = getPlayerHeldGrabbable(t, o);
                      if (!n || n.isNull?.()) {
                        continue;
                      }
                      const i = normalizeSceneObjectHandle(getRootLikeObject(n) ?? n);
                      if (i) {
                        e.add(i);
                      }
                    } catch (e) {}
                  }
                }
              } catch (e) {}
              for (let t = gridFrozenItems.length - 1; t >= 0; t--) {
                const o = gridFrozenItems[t];
                try {
                  if (!o || !o.object || o.object.isNull?.()) {
                    gridFrozenItems.splice(t, 1);
                    continue;
                  }
                  if (o.rootKey && e.has(o.rootKey)) {
                    unfreezeGridItem(o.object);
                    gridFrozenItems.splice(t, 1);
                  }
                } catch (e) {
                  gridFrozenItems.splice(t, 1);
                }
              }
            }
            function spawnSelectedItemGrid(e) {
              e = Math.max(1, Math.min(30, Math.round(e)));
              const t = itemIDs[itemIndex];
              if (t) {
                try {
                  const o = headCollider && !headCollider.isNull?.() ? getTransform(headCollider) : rightHandTransform;
                  if (!o || o.isNull?.()) {
                    sendNotification("Player transform is not ready", false);
                    return;
                  }
                  const n = readVec3Components(getLaunchForward(o));
                  let i = [n[0], 0, n[2]];
                  try {
                    if (Vector3.method("Magnitude").invoke(i) < 0.05) {
                      i = [0, 0, 1];
                    }
                    i = Vector3.method("Normalize").invoke(i);
                  } catch (e) {
                    i = [0, 0, 1];
                  }
                  const a = [0, 1, 0];
                  let l = [1, 0, 0];
                  try {
                    l = Vector3.method("Normalize").invoke(Vector3.method("Cross").invoke(a, i));
                  } catch (e) {}
                  let r = o.method("get_position").invoke();
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (e && !e.handle.isNull()) {
                      r = getTransform(e).method("get_position").invoke();
                    }
                  } catch (e) {}
                  const s = gridSpacing;
                  const c = gridColumns > 0 ? Math.min(e, gridColumns) : Math.ceil(Math.sqrt(e));
                  const d = Math.ceil(e / c);
                  const u = Vector3.method("op_Addition").invoke(r, Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(i, 1.7), Vector3.method("op_Multiply", 2).invoke(a, 0.18)));
                  let m = 0;
                  for (let o = 0; o < e; o++) {
                    const n = Math.floor(o / c);
                    const a = (o % c - (Math.min(c, e - n * c) - 1) / 2) * s;
                    const r = (n - (d - 1) / 2) * s;
                    const h = Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(l, a), Vector3.method("op_Multiply", 2).invoke(i, r));
                    const g = spawnItemAtPos(t, Vector3.method("op_Addition").invoke(u, h), identityQuaternion);
                    if (g && !g.isNull?.()) {
                      freezeGridItemUntilPickedUp(g);
                      m++;
                    }
                  }
                  sendNotification("Spawned " + m + "/" + e + " " + t + " in a grid", false, 5);
                } catch (e) {
                  console.error("[Grid mods]", e);
                  sendNotification("Grid spawn failed: " + e, false);
                }
              } else {
                sendNotification("Select an Item ID first", false);
              }
            }
            function clearSpawnedGrid() {
              let e = 0;
              for (const t of gridFrozenItems) {
                try {
                  if (!t?.object || t.object.isNull?.()) {
                    continue;
                  }
                  Object.method("Destroy", 1).invoke(t.object);
                  e++;
                } catch (e) {}
              }
              gridFrozenItems = [];
              sendNotification("Cleared grid items: " + e, false);
            }
            function giveMoneyAllTest() {
              try {
                let e = 0;
                for (const t of getAllNetPlayersList(false)) {
                  try {
                    t.method("RPC_AddPlayerMoney").invoke(10000000);
                    e++;
                  } catch (e) {}
                }
                sendNotification("Test: gave 10M to " + e + " players", false, 4);
              } catch (e) {
                sendNotification("Test money all: " + e, false);
              }
            }
            function removeMoneyAllTest() {
              try {
                let e = 0;
                for (const t of getAllNetPlayersList(false)) {
                  try {
                    t.method("RPC_AddPlayerMoney").invoke(-9999999);
                    e++;
                  } catch (e) {}
                }
                sendNotification("Test: gave -10M to " + e + " players", false, 4);
              } catch (e) {
                sendNotification("Test money all: " + e, false);
              }
            }
            const mineableResearchOrePrefabNames = ["R_CoreVein", "R_CoreVein_1", "Core Ore", "Ore Vein", "Ore Vein_1", "Ore Vein_2", "Ore Vein_3", "HealthOre", "LootOre", "B_CoreVein", "B_CoreVein_1"];
            function findLoadedMineableOreTemplate() {
              try {
                const e = Object.method("FindObjectsByType", 1).inflate(GameObject).invoke(0);
                const t = e ? e.length : 0;
                for (let o = 0; o < t; o++) {
                  try {
                    const t = e.method("get_Item").invoke(o);
                    if (!t || t.isNull?.()) {
                      continue;
                    }
                    const n = t.method("get_name").invoke()?.content ?? "";
                    const i = String(n).replace(/\(Clone\)/g, "").trim().toLowerCase();
                    if (!i) {
                      continue;
                    }
                    if (mineableResearchOrePrefabNames.some(e => i.includes(e.toLowerCase()))) {
                      return t;
                    }
                    if ((i.includes("ore") || i.includes("vein")) && i.includes("core")) {
                      return t;
                    }
                  } catch (e) {}
                }
              } catch (e) {
                console.error("[ResearchOreSpawner find template]", e);
              }
              return null;
            }
            function cloneLoadedMineableOreAt(e, t) {
              try {
                const o = findLoadedMineableOreTemplate();
                if (!o || o.isNull?.()) {
                  return null;
                }
                const n = Object.method("Instantiate", 3).invoke(o, e, t);
                if (!n || n.isNull?.()) {
                  return null;
                }
                try {
                  n.method("SetActive").invoke(true);
                } catch (e) {}
                try {
                  n.method("set_name").invoke(Il2Cpp.string("Spawned_Research_Ore_Vein"));
                } catch (e) {}
                return n;
              } catch (e) {
                console.error("[ResearchOreSpawner clone]", e);
              }
              return null;
            }
            function spawnMineableResearchOreAt(e, t) {
              for (const o of mineableResearchOrePrefabNames) {
                try {
                  const n = spawnNetworkPrefab(o, e, t);
                  if (n && !n.isNull?.()) {
                    return n;
                  }
                } catch (e) {}
              }
              return cloneLoadedMineableOreAt(e, t);
            }
            function spawnResearchOreLoop() {
              if (!(time < researchOreSpawnDelay)) {
                researchOreSpawnDelay = time + 0.65;
                try {
                  const e = rightHandTransform;
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  const t = getLaunchForward(e);
                  const o = getLaunchUp(e, t);
                  const n = Vector3.method("op_Addition").invoke(e.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(t, 0.85), Vector3.method("op_Multiply", 2).invoke(o, -0.18)));
                  const i = spawnMineableResearchOreAt(n, getLaunchRotation(e, t, o));
                  if (!i || i.isNull?.()) {
                    if (time >= researchOreFailNotifyDelay) {
                      researchOreFailNotifyDelay = time + 3;
                      sendNotification("Mineable research ore not found in loaded prefabs", false, 3);
                    }
                    return;
                  }
                  try {
                    console.log("[ResearchOreSpawner] spawned mineable ore vein");
                  } catch (e) {}
                } catch (e) {
                  console.error("[ResearchOreSpawner]", e);
                }
              }
            }
            function spawnMoneyBills500() {
              if (!(time < testMoneySpawnDelay)) {
                testMoneySpawnDelay = time + 1.2;
                try {
                  const e = rightHandTransform;
                  if (!e || e.isNull?.()) {
                    sendNotification("No right hand transform", false);
                    return;
                  }
                  const t = getLaunchForward(e);
                  const o = getLaunchUp(e, t);
                  let n = null;
                  try {
                    n = e.method("get_right").invoke();
                  } catch (e) {
                    n = [1, 0, 0];
                  }
                  const i = e.method("get_position").invoke();
                  const a = Vector3.method("op_Addition").invoke(i, Vector3.method("op_Multiply", 2).invoke(t, 1.1));
                  const l = getLaunchRotation(e, t, o);
                  let r = 0;
                  for (let e = 0; e < 500; e++) {
                    try {
                      const i = e % 25;
                      const s = Math.floor(e / 25) % 10;
                      const c = Math.floor(e / 250);
                      const d = Vector3.method("op_Addition").invoke(Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(n, (i - 12) * 0.055), Vector3.method("op_Multiply", 2).invoke(o, 0.02 + c * 0.07)), Vector3.method("op_Multiply", 2).invoke(t, (s - 4.5) * 0.055));
                      const u = spawnItemAtPos("item_fish_dollar_bill", Vector3.method("op_Addition").invoke(a, d), l);
                      if (!u || u.isNull?.()) {
                        continue;
                      }
                      freezeSpawnedObjectInPlace(u);
                      r++;
                    } catch (e) {}
                  }
                  sendNotification("Spawned money bills: " + r, false, 5);
                } catch (e) {
                  sendNotification("Money bill spawn failed: " + e, false);
                }
              }
            }
            function stealNearestHeldItemToRightHand() {
              if (rightGrab && !(time < handStealDelay)) {
                handStealDelay = time + 0.18;
                try {
                  const e = rightHandTransform.method("get_position").invoke();
                  let t = null;
                  let o = null;
                  let n = 4.5;
                  for (const i of getAllNetPlayersList(false)) {
                    try {
                      for (const a of [0, 1]) {
                        const l = getPlayerHeldGrabbable(i, a);
                        if (!l || l.isNull?.()) {
                          continue;
                        }
                        const r = (() => {
                          try {
                            return l.method("get_gameObject").invoke();
                          } catch (e) {
                            return l;
                          }
                        })();
                        const s = getTransform(r && !r.isNull?.() ? r : l);
                        if (!s || s.isNull?.()) {
                          continue;
                        }
                        const c = Vector3.method("Distance").invoke(e, s.method("get_position").invoke());
                        if (c < n) {
                          t = r && !r.isNull?.() ? r : l;
                          o = i;
                          n = c;
                        }
                      }
                    } catch (e) {}
                  }
                  if (!t || t.isNull?.()) {
                    return;
                  }
                  if (forceMoveLobbyItem(t, Vector3.method("op_Addition").invoke(e, Vector3.method("op_Multiply", 2).invoke(getLaunchForward(rightHandTransform), 0.08)))) {
                    sendNotification("Pulled held item from " + (() => {
                      try {
                        return getPlayerName(o);
                      } catch (e) {
                        return "player";
                      }
                    })(), false, 2);
                  }
                } catch (e) {
                  console.error("[HandSteal]", e);
                }
              }
            }
            function spawnTextBlueprintAsHellOre() {
              if (!(time < textSpawnDelay)) {
                textSpawnDelay = time + 0.8;
                try {
                  const e = readTextBlueprintPieces();
                  if (!e || e.length === 0) {
                    sendNotification("Text blueprint is empty", false);
                    return;
                  }
                  const t = rightHandTransform;
                  if (!t || t.isNull?.()) {
                    sendNotification("No right hand transform", false);
                    return;
                  }
                  let o = 0;
                  let n = 0;
                  let i = 0;
                  for (const t of e) {
                    const e = t.pos;
                    o += e[0];
                    n += e[1];
                    i += e[2];
                  }
                  o /= e.length;
                  n /= e.length;
                  i /= e.length;
                  const a = getLaunchForward(t);
                  const l = getLaunchUp(t, a);
                  let r = null;
                  try {
                    r = t.method("get_right").invoke();
                  } catch (e) {
                    r = [1, 0, 0];
                  }
                  const s = t.method("get_position").invoke();
                  const c = Vector3.method("op_Addition").invoke(s, Vector3.method("op_Multiply", 2).invoke(a, 0.62));
                  const d = getLaunchRotation(t, a, l);
                  let u = 0;
                  for (const t of e) {
                    try {
                      const e = t.pos;
                      const s = (Number(e[0]) - o) * TEXT_SPAWN_SCALE;
                      const m = (Number(e[1]) - n) * TEXT_SPAWN_SCALE;
                      const h = (Number(e[2]) - i) * TEXT_SPAWN_SCALE;
                      const g = Vector3.method("op_Addition").invoke(Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(r, s), Vector3.method("op_Multiply", 2).invoke(l, m)), Vector3.method("op_Multiply", 2).invoke(a, h));
                      const f = spawnItemAtPos("item_ore_hell", Vector3.method("op_Addition").invoke(c, g), d);
                      if (!f || f.isNull?.()) {
                        continue;
                      }
                      applyTextPieceConfig(f, t);
                      freezeSpawnedObjectInPlace(f);
                      textSpawnedObjects.push(f);
                      u++;
                    } catch (e) {}
                  }
                  sendNotification("Spawned Hell Ore text: " + u, false, 4);
                } catch (e) {
                  console.error("[TextSpawn]", e);
                  sendNotification("Text spawn failed: " + e, false);
                }
              }
            }
            function clearSpawnedTextHellOre() {
              oreTextSpawnQueue = [];
              oreTextFreezeCursor = 0;
              clearOreTextPreview();
              let e = 0;
              const t = [];
              const o = textSpawnedObjects.concat(oreTextPendingObjects);
              oreTextPendingObjects = [];
              for (const n of o) {
                try {
                  const t = n?.object ?? n;
                  if (!t || t.isNull?.()) {
                    continue;
                  }
                  let o = t;
                  try {
                    const e = t.method("get_gameObject").invoke();
                    if (e && !e.isNull?.()) {
                      o = e;
                    }
                  } catch (e) {}
                  Object.method("Destroy", 1).invoke(o);
                  e++;
                } catch (e) {
                  t.push(n);
                }
              }
              textSpawnedObjects = t;
              oreTextBatches = [];
              sendNotification("Cleared Hell Ore text: " + e, false);
            }
            function clearOreTextPreview() {
              for (const e of oreTextPreviewObjects) {
                try {
                  if (e && !e.isNull?.()) {
                    Destroy(e);
                  }
                } catch (e) {}
              }
              oreTextPreviewObjects = [];
            }
            function getOreTextActiveCount() {
              return textSpawnedObjects.length + oreTextPendingObjects.length + oreTextSpawnQueue.length;
            }
            function getOreTextMaterial() {
              return oreTextMaterials[oreTextMaterialIndex % oreTextMaterials.length];
            }
            function cycleOreTextMaterial(e) {
              oreTextMaterialIndex = (oreTextMaterialIndex + e + oreTextMaterials.length) % oreTextMaterials.length;
              sendNotification("Text material: " + getOreTextMaterial()[0], false);
            }
            function cycleTextSetting(e, t, o) {
              const n = Math.max(0, t.indexOf(e));
              return t[(n + o + t.length) % t.length];
            }
            function saveCurrentOreTextPhrase() {
              const e = oreTextQuery.trim();
              if (e) {
                if (!oreTextSavedPhrases.includes(e)) {
                  oreTextSavedPhrases.push(e);
                }
                oreTextSavedPhraseIndex = oreTextSavedPhrases.indexOf(e);
                persistOreTextSettings();
                sendNotification("Saved phrase: " + e.replace(/\|/g, " / "), false);
              } else {
                sendNotification("Enter text before saving a phrase", false);
              }
            }
            function cycleSavedOreTextPhrase(e) {
              if (oreTextSavedPhrases.length !== 0) {
                oreTextSavedPhraseIndex = (oreTextSavedPhraseIndex + e + oreTextSavedPhrases.length) % oreTextSavedPhrases.length;
                sendNotification("Phrase: " + oreTextSavedPhrases[oreTextSavedPhraseIndex].replace(/\|/g, " / "), false);
              }
            }
            function loadSavedOreTextPhrase() {
              if (oreTextSavedPhrases.length !== 0) {
                sendNotification("Loaded phrase: " + (oreTextQuery = oreTextSavedPhrases[oreTextSavedPhraseIndex].slice(0, oreTextMaxLength)).replace(/\|/g, " / "), false);
              }
            }
            function deleteSavedOreTextPhrase() {
              if (oreTextSavedPhrases.length === 0) {
                return;
              }
              const e = oreTextSavedPhrases.splice(oreTextSavedPhraseIndex, 1)[0];
              oreTextSavedPhraseIndex = Math.max(0, Math.min(oreTextSavedPhraseIndex, oreTextSavedPhrases.length - 1));
              persistOreTextSettings();
              sendNotification("Deleted phrase: " + e.replace(/\|/g, " / "), false);
            }
            function persistOreTextSettings() {
              try {
                const e = new (0, globalThis.File)(ORE_TEXT_SETTINGS_PATH, "w");
                e.write(JSON.stringify({
                  phrases: oreTextSavedPhrases,
                  savedStyle: oreTextSavedStyle
                }, null, 2));
                try {
                  e.flush();
                } catch (e) {}
                try {
                  e.close();
                } catch (e) {}
              } catch (e) {
                console.error("[OreText] Could not save presets: " + e);
              }
            }
            function loadOreTextSettings() {
              if (!oreTextSettingsLoaded) {
                oreTextSettingsLoaded = true;
                try {
                  const e = new (0, globalThis.File)(ORE_TEXT_SETTINGS_PATH, "r");
                  const t = e.readText(262144);
                  try {
                    e.close();
                  } catch (e) {}
                  const o = JSON.parse(t);
                  if (Array.isArray(o?.phrases)) {
                    oreTextSavedPhrases = o.phrases.map(e => String(e).slice(0, oreTextMaxLength)).filter(Boolean);
                  }
                  if (o?.savedStyle && typeof o.savedStyle == "object") {
                    oreTextSavedStyle = o.savedStyle;
                  }
                  oreTextSavedPhraseIndex = Math.max(0, Math.min(oreTextSavedPhraseIndex, oreTextSavedPhrases.length - 1));
                } catch (e) {}
              }
            }
            function saveOreTextStyle() {
              oreTextSavedStyle = {
                pieceScale: oreTextPieceScale,
                thickness: oreTextThickness,
                cellSize: oreTextCellSize,
                surfaceOffset: oreTextSurfaceOffset,
                materialIndex: oreTextMaterialIndex,
                layout: oreTextLayoutMode,
                font: oreTextFontMode,
                alignment: oreTextAlignment,
                snap: oreTextSnapMode,
                color: oreTextColorMode,
                hue: oreTextBaseHue,
                animation: oreTextAnimationMode,
                follow: oreTextFollowMode,
                roll: oreTextRollDegrees,
                curve: oreTextCurveStrength,
                outline: oreTextOutline,
                mirror: oreTextMirror,
                vertical: oreTextVertical
              };
              persistOreTextSettings();
              sendNotification("Saved current text style", false);
            }
            function syncOreTextToggleButtons() {
              const e = {
                "Outline Text": oreTextOutline,
                "Mirror Text": oreTextMirror,
                "Vertical Text": oreTextVertical,
                "Ghost Preview": oreTextPreviewEnabled
              };
              for (const [t, o] of Object.entries(e)) {
                const e = (buttons[104] || []).find(e => e.buttonText === t);
                if (e) {
                  e.enabled = o;
                }
              }
            }
            function loadOreTextStyle() {
              if (!oreTextSavedStyle) {
                sendNotification("No saved text style", false);
                return;
              }
              const e = oreTextSavedStyle;
              oreTextPieceScale = e.pieceScale;
              oreTextThickness = e.thickness;
              oreTextCellSize = e.cellSize;
              oreTextSurfaceOffset = e.surfaceOffset;
              oreTextMaterialIndex = e.materialIndex;
              oreTextLayoutMode = e.layout;
              oreTextFontMode = e.font;
              oreTextAlignment = e.alignment;
              oreTextSnapMode = e.snap;
              oreTextColorMode = e.color;
              oreTextBaseHue = e.hue;
              oreTextAnimationMode = e.animation;
              oreTextFollowMode = e.follow;
              oreTextRollDegrees = e.roll;
              oreTextCurveStrength = e.curve;
              oreTextOutline = e.outline;
              oreTextMirror = e.mirror;
              oreTextVertical = e.vertical;
              syncOreTextToggleButtons();
              sendNotification("Loaded saved text style", false);
            }
            function getOreTextFollowTarget(e = oreTextFollowMode) {
              try {
                if (e === "selected") {
                  const e = resolveUsersSelectedPlayer();
                  if (e && !e.isNull?.()) {
                    return getTransform(e);
                  }
                }
                if (e === "object" && uxSelectedObject && !uxSelectedObject.isNull?.()) {
                  return getTransform(uxSelectedObject);
                }
                if (e === "local") {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (e && !e.isNull?.()) {
                    return getTransform(e);
                  } else {
                    return getTransform(getGTPlayer());
                  }
                }
              } catch (e) {}
              return null;
            }
            function undoLastOreTextBatch() {
              const e = oreTextBatches.pop();
              if (e) {
                oreTextSpawnQueue = oreTextSpawnQueue.filter(t => t.batchId !== e.id);
                oreTextPendingObjects = oreTextPendingObjects.filter(t => {
                  if (t.batchId !== e.id) {
                    return true;
                  }
                  try {
                    if (t.object && !t.object.isNull?.()) {
                      Destroy(t.object);
                    }
                  } catch (e) {}
                  return false;
                });
                textSpawnedObjects = textSpawnedObjects.filter(t => {
                  if (t?.batchId !== e.id) {
                    return true;
                  }
                  try {
                    const e = t.object ?? t;
                    if (e && !e.isNull?.()) {
                      Destroy(e);
                    }
                  } catch (e) {}
                  return false;
                });
                sendNotification("Undid text: " + e.text, false);
              } else {
                sendNotification("No ore text batch to undo", false);
              }
            }
            function setOreTextVisible(e, t) {
              try {
                const o = e.method("GetComponentsInChildren", 1).inflate(Renderer).invoke(true);
                for (let e = 0; e < o.length; e++) {
                  try {
                    o.get(e).method("set_enabled").invoke(t);
                  } catch (e) {}
                }
              } catch (e) {}
            }
            function getOreTextBatch(e) {
              return oreTextBatches.find(t => t.id === e) ?? null;
            }
            function getOreTextEntryRotation(e) {
              if (e?.snapMode === "floor") {
                try {
                  return Quaternion.method("LookRotation", 2).invoke([0, -1, 0], e.rotationUp ?? [0, 0, 1]);
                } catch (e) {}
              }
              return getLookRotationFromForward(e?.forward ?? [0, 0, 1]);
            }
            function updateFrozenOreText() {
              if (textSpawnedObjects.length === 0) {
                return;
              }
              if (time < oreTextFreezeRefreshTime) {
                return;
              }
              const e = oreTextBatches.some(e => e.animation !== "none" || e.followMode !== "none" || e.colorMode === "rainbow" || e.colorMode === "gradient");
              oreTextFreezeRefreshTime = time + (e ? 0.033 : 0.1);
              const t = Math.min(e ? 48 : 8, textSpawnedObjects.length);
              for (let e = 0; e < t && textSpawnedObjects.length > 0; e++) {
                const e = oreTextFreezeCursor %= textSpawnedObjects.length;
                const t = textSpawnedObjects[e];
                const o = t?.object ?? t;
                try {
                  if (!o || o.isNull?.()) {
                    textSpawnedObjects.splice(e, 1);
                    continue;
                  }
                  if (t?.pos) {
                    const e = getRootLikeObject(o) ?? o;
                    const n = getTransform(e);
                    freezeSpawnedObjectInPlace(o, false);
                    let i = [t.pos[0], t.pos[1], t.pos[2]];
                    const a = getOreTextBatch(t.batchId);
                    if (a) {
                      if (a.followMode !== "none") {
                        try {
                          const e = getOreTextFollowTarget(a.followMode);
                          if (e && !e.isNull?.() && a.anchorStart) {
                            const t = readVec3Components(e.method("get_position").invoke());
                            i[0] += t[0] - a.anchorStart[0];
                            i[1] += t[1] - a.anchorStart[1];
                            i[2] += t[2] - a.anchorStart[2];
                          }
                        } catch (e) {}
                      }
                      const o = Math.max(0, time - a.spawnTime);
                      if (a.animation === "spin" && a.center) {
                        const e = o * 0.8;
                        const t = i[0] - a.center[0];
                        const n = i[2] - a.center[2];
                        i[0] = a.center[0] + t * Math.cos(e) - n * Math.sin(e);
                        i[2] = a.center[2] + t * Math.sin(e) + n * Math.cos(e);
                      } else if (a.animation === "pulse" && t.baseScale) {
                        const e = 1 + Math.sin(o * 4 + (t.pieceIndex ?? 0) * 0.08) * 0.16;
                        try {
                          n.method("set_localScale").invoke(t.baseScale.map(t => t * e));
                        } catch (e) {}
                      }
                      if (a.animation === "dissolve") {
                        setOreTextVisible(e, Math.sin(o * 2.5 + (t.pieceIndex ?? 0) * 0.22) > -0.25);
                      } else if (a.animation === "typewriter") {
                        setOreTextVisible(e, o >= (t.pieceIndex ?? 0) * 0.035);
                      } else {
                        setOreTextVisible(e, true);
                      }
                      if (a.colorMode === "rainbow" || a.colorMode === "gradient") {
                        const n = Math.floor((o * 40 + (t.charIndex ?? 0) * 24) % 255 - 127);
                        try {
                          applyRainbowToSpawnedObject(e, n, 100);
                        } catch (e) {}
                      }
                    }
                    try {
                      n.method("set_position").invoke(i);
                    } catch (e) {}
                    try {
                      n.method("set_rotation").invoke(getOreTextEntryRotation(t));
                    } catch (e) {}
                  } else {
                    freezeSpawnedObjectInPlace(o, false);
                  }
                  oreTextFreezeCursor++;
                } catch (t) {
                  textSpawnedObjects.splice(e, 1);
                }
              }
            }
            function stripOreCloneSimulation(e) {
              try {
                const t = e.method("GetComponentsInChildren", 1).inflate(MonoBehaviour).invoke(true);
                if (t && !t.isNull?.()) {
                  for (let e = 0; e < t.length; e++) {
                    try {
                      t.method("get_Item").invoke(e).method("set_enabled").invoke(false);
                    } catch (e) {}
                  }
                }
              } catch (e) {}
              try {
                const t = e.method("GetComponentsInChildren", 1).inflate(Rigidbody).invoke(true);
                if (t && !t.isNull?.()) {
                  for (let e = 0; e < t.length; e++) {
                    try {
                      const o = t.method("get_Item").invoke(e);
                      o.method("set_velocity").invoke([0, 0, 0]);
                      try {
                        o.method("set_linearVelocity").invoke([0, 0, 0]);
                      } catch (e) {}
                      o.method("set_angularVelocity").invoke([0, 0, 0]);
                      o.method("set_useGravity").invoke(false);
                      o.method("set_isKinematic").invoke(true);
                      o.method("set_constraints").invoke(126);
                      o.method("set_detectCollisions").invoke(false);
                      o.method("Sleep").invoke();
                    } catch (e) {}
                  }
                }
              } catch (e) {}
              try {
                const t = e.method("GetComponentsInChildren", 1).inflate(Collider).invoke(true);
                if (t && !t.isNull?.()) {
                  for (let e = 0; e < t.length; e++) {
                    try {
                      t.method("get_Item").invoke(e).method("set_enabled").invoke(false);
                    } catch (e) {}
                  }
                }
              } catch (e) {}
            }
            function applyOreTextPieceScale(e) {
              try {
                const t = getTransform(e);
                if (!t || t.isNull?.()) {
                  return;
                }
                const o = t.method("get_localScale").invoke();
                const n = Number(o.field("x").value);
                const i = Number(o.field("y").value);
                const a = Number(o.field("z").value);
                t.method("set_localScale").invoke([n * oreTextPieceScale, i * oreTextPieceScale, a * oreTextThickness]);
              } catch (e) {}
            }
            function ensureOreTextVisualTemplate(e, t, o) {
              try {
                const e = oreTextVisualTemplates.get(o);
                if (e && !e.isNull?.()) {
                  oreTextVisualTemplate = e;
                  return true;
                }
              } catch (e) {
                oreTextVisualTemplates.delete(o);
              }
              try {
                const n = spawnItemAtPos(o, e, getLookRotationFromForward(t));
                return !!n && !n.isNull?.() && ((oreTextVisualTemplate = getRootLikeObject(n) ?? n).method("SetActive").invoke(false), oreTextVisualTemplates.set(o, oreTextVisualTemplate), true);
              } catch (e) {
                console.error("[OreText template]", e);
                oreTextVisualTemplate = null;
                return false;
              }
            }
            function processOreTextSpawnQueue() {
              const e = oreTextPiecesPerFrame;
              for (let t = 0; t < e && oreTextSpawnQueue.length > 0; t++) {
                const e = oreTextSpawnQueue.shift();
                if (!e) {
                  break;
                }
                try {
                  if (!ensureOreTextVisualTemplate(e.pos, e.forward, e.materialID ?? "item_ore_hell")) {
                    e.templateAttempts = (e.templateAttempts ?? 0) + 1;
                    if (e.templateAttempts >= 20 && e.materialID !== "item_ore_hell") {
                      e.materialID = "item_ore_hell";
                      e.templateAttempts = 0;
                      sendNotification("Material unavailable; using Hell Ore", false, 2);
                    }
                    if (e.templateAttempts >= 40 && e.materialID === "item_ore_hell") {
                      console.error("[OreText] Could not create a visual template after 40 attempts");
                      continue;
                    }
                    oreTextSpawnQueue.unshift(e);
                    break;
                  }
                  const t = getOreTextEntryRotation(e);
                  const o = Object.method("Instantiate", 3).invoke(oreTextVisualTemplate, e.pos, t);
                  if (!o || o.isNull?.()) {
                    continue;
                  }
                  try {
                    o.method("SetActive").invoke(false);
                  } catch (e) {}
                  stripOreCloneSimulation(o);
                  const n = getTransform(o);
                  try {
                    n.method("set_position").invoke(e.pos);
                  } catch (e) {}
                  try {
                    n.method("set_rotation").invoke(t);
                  } catch (e) {}
                  oreTextPendingObjects.push({
                    ...e,
                    object: o
                  });
                } catch (e) {}
              }
              if (oreTextSpawnQueue.length === 0 && oreTextPendingObjects.length > 0) {
                let e = 0;
                const t = new Set();
                for (const e of oreTextPendingObjects) {
                  t.add(e.batchId);
                }
                for (const e of t) {
                  const t = getOreTextBatch(e);
                  if (t && t.objects.length === 0) {
                    t.spawnTime = time;
                  }
                }
                for (const t of oreTextPendingObjects) {
                  try {
                    if (!t.object || t.object.isNull?.()) {
                      continue;
                    }
                    const o = getRootLikeObject(t.object) ?? t.object;
                    const n = getTransform(o);
                    try {
                      o.method("SetActive").invoke(true);
                    } catch (e) {}
                    applyOreTextPieceScale(o);
                    try {
                      const e = n.method("get_localScale").invoke();
                      t.baseScale = [Number(e.field("x").value), Number(e.field("y").value), Number(e.field("z").value)];
                    } catch (e) {}
                    if (t.colorMode !== "natural") {
                      let e = oreTextBaseHue;
                      if (t.colorMode === "rainbow") {
                        e = (t.charIndex ?? 0) * 37 % 255 - 127;
                      }
                      if (t.colorMode === "gradient") {
                        e = Math.max(-127, Math.min(127, oreTextBaseHue + (t.charIndex ?? 0) * 14));
                      }
                      try {
                        applyRainbowToSpawnedObject(o, e, 100);
                      } catch (e) {}
                    }
                    freezeSpawnedObjectInPlace(t.object, false);
                    n.method("set_position").invoke(t.pos);
                    n.method("set_rotation").invoke(getOreTextEntryRotation(t));
                    textSpawnedObjects.push(t);
                    const i = getOreTextBatch(t.batchId);
                    if (i) {
                      i.objects.push(t);
                    }
                    e++;
                  } catch (e) {}
                }
                oreTextPendingObjects = [];
                sendNotification("Ore text ready: " + e + " pieces", false, 3);
              }
            }
            const ORE_FONT = {
              A: ["01110", "10001", "10001", "11111", "10001", "10001", "10001"],
              B: ["11110", "10001", "10001", "11110", "10001", "10001", "11110"],
              C: ["01111", "10000", "10000", "10000", "10000", "10000", "01111"],
              D: ["11110", "10001", "10001", "10001", "10001", "10001", "11110"],
              E: ["11111", "10000", "10000", "11110", "10000", "10000", "11111"],
              F: ["11111", "10000", "10000", "11110", "10000", "10000", "10000"],
              G: ["01111", "10000", "10000", "10111", "10001", "10001", "01111"],
              H: ["10001", "10001", "10001", "11111", "10001", "10001", "10001"],
              I: ["11111", "00100", "00100", "00100", "00100", "00100", "11111"],
              J: ["00111", "00010", "00010", "00010", "10010", "10010", "01100"],
              K: ["10001", "10010", "10100", "11000", "10100", "10010", "10001"],
              L: ["10000", "10000", "10000", "10000", "10000", "10000", "11111"],
              M: ["10001", "11011", "10101", "10101", "10001", "10001", "10001"],
              N: ["10001", "11001", "10101", "10011", "10001", "10001", "10001"],
              O: ["01110", "10001", "10001", "10001", "10001", "10001", "01110"],
              P: ["11110", "10001", "10001", "11110", "10000", "10000", "10000"],
              Q: ["01110", "10001", "10001", "10001", "10101", "10010", "01101"],
              R: ["11110", "10001", "10001", "11110", "10100", "10010", "10001"],
              S: ["01111", "10000", "10000", "01110", "00001", "00001", "11110"],
              T: ["11111", "00100", "00100", "00100", "00100", "00100", "00100"],
              U: ["10001", "10001", "10001", "10001", "10001", "10001", "01110"],
              V: ["10001", "10001", "10001", "10001", "10001", "01010", "00100"],
              W: ["10001", "10001", "10001", "10101", "10101", "10101", "01010"],
              X: ["10001", "10001", "01010", "00100", "01010", "10001", "10001"],
              Y: ["10001", "10001", "01010", "00100", "00100", "00100", "00100"],
              Z: ["11111", "00001", "00010", "00100", "01000", "10000", "11111"],
              0: ["01110", "10001", "10011", "10101", "11001", "10001", "01110"],
              1: ["00100", "01100", "00100", "00100", "00100", "00100", "01110"],
              2: ["01110", "10001", "00001", "00010", "00100", "01000", "11111"],
              3: ["11110", "00001", "00001", "01110", "00001", "00001", "11110"],
              4: ["00010", "00110", "01010", "10010", "11111", "00010", "00010"],
              5: ["11111", "10000", "10000", "11110", "00001", "00001", "11110"],
              6: ["01110", "10000", "10000", "11110", "10001", "10001", "01110"],
              7: ["11111", "00001", "00010", "00100", "01000", "01000", "01000"],
              8: ["01110", "10001", "10001", "01110", "10001", "10001", "01110"],
              9: ["01110", "10001", "10001", "01111", "00001", "00001", "01110"],
              "!": ["00100", "00100", "00100", "00100", "00100", "00000", "00100"],
              "?": ["01110", "10001", "00001", "00010", "00100", "00000", "00100"]
            };
            function buildOreTextJobs(e, t) {
              const o = oreTextQuery.toUpperCase().slice(0, oreTextMaxLength);
              if (!o.replace(/\|/g, "").trim()) {
                return null;
              }
              const n = readVec3Components(e);
              let i = [n[0], n[1], n[2] - 1];
              try {
                i = readVec3Components((headCollider && !headCollider.isNull?.() ? getTransform(headCollider) : t).method("get_position").invoke());
              } catch (e) {}
              let a = i[0] - n[0];
              let l = i[2] - n[2];
              const r = Math.sqrt(a * a + l * l) || 1;
              a /= r;
              l /= r;
              const s = [a, 0, l];
              const c = [-l, 0, a];
              const d = [n[0] + a * oreTextSurfaceOffset, n[1] + 0.06, n[2] + l * oreTextSurfaceOffset];
              const u = oreTextCellSize;
              const m = o.split("|").slice(0, 4);
              const h = Math.max(...m.map(e => Math.max(0, e.length * 6 - 1)), 1);
              const g = [];
              let f = 0;
              for (let e = 0; e < m.length; e++) {
                const t = m[e];
                const o = Math.max(0, t.length * 6 - 1);
                for (let i = 0; i < t.length; i++, f++) {
                  const a = ORE_FONT[t[i]];
                  if (a) {
                    for (let l = 0; l < 7; l++) {
                      for (let r = 0; r < 5; r++) {
                        if (a[l][r] !== "1") {
                          continue;
                        }
                        if (oreTextOutline || oreTextFontMode === "outline") {
                          const e = (e, t) => e >= 0 && e < 7 && t >= 0 && t < 5 && a[e][t] === "1";
                          if (e(l - 1, r) && e(l + 1, r) && e(l, r - 1) && e(l, r + 1)) {
                            continue;
                          }
                        }
                        if (oreTextFontMode === "stencil" && (l * 5 + r) % 4 == 2) {
                          continue;
                        }
                        let p;
                        let y;
                        if (oreTextVertical) {
                          p = (r - 2) * u;
                          y = ((t.length - 1 - i) * 8 + (6 - l)) * u;
                        } else {
                          let t = -o / 2;
                          if (oreTextAlignment === "left") {
                            t = -h / 2;
                          }
                          if (oreTextAlignment === "right") {
                            t = h / 2 - o;
                          }
                          p = (i * 6 + r + t) * u;
                          y = ((m.length - 1 - e) * 8 + (6 - l)) * u;
                        }
                        if (oreTextMirror) {
                          p = -p;
                        }
                        if (oreTextLayoutMode === "curve") {
                          y += p * p * 0.35 * oreTextCurveStrength;
                        }
                        if (oreTextLayoutMode === "wave") {
                          y += Math.sin(p / Math.max(0.01, u) * 0.8) * u * 1.6 * oreTextCurveStrength;
                        }
                        if (oreTextLayoutMode === "circle") {
                          const e = Math.max(0.45, h * u / (Math.PI * 1.6));
                          const t = p / e;
                          const o = e + y;
                          p = Math.sin(t) * o;
                          y = Math.cos(t) * o;
                        }
                        const b = oreTextRollDegrees * Math.PI / 180;
                        const _ = p * Math.cos(b) - y * Math.sin(b);
                        const v = p * Math.sin(b) + y * Math.cos(b);
                        p = _;
                        y = v;
                        let T;
                        let k = s;
                        if (oreTextSnapMode === "floor") {
                          k = [0, 1, 0];
                          T = [d[0] + c[0] * p - s[0] * y, n[1] + 0.025, d[2] + c[2] * p - s[2] * y];
                        } else {
                          T = [d[0] + c[0] * p, d[1] + y, d[2] + c[2] * p];
                        }
                        g.push({
                          pos: T,
                          forward: k,
                          rotationUp: s,
                          snapMode: oreTextSnapMode,
                          charIndex: f,
                          pieceIndex: g.length
                        });
                      }
                    }
                  }
                }
              }
              return {
                text: o,
                jobs: g,
                center: d
              };
            }
            function spawnOreTextAtPointer(e, t) {
              if (oreTextSpawnQueue.length > 0 || oreTextPendingObjects.length > 0) {
                sendNotification("Wait for the current ore word to finish", false, 2);
                return;
              }
              const o = buildOreTextJobs(e, t);
              if (!o) {
                sendNotification("Enter text first", false);
                return;
              }
              if (getOreTextActiveCount() + o.jobs.length > oreTextObjectBudget) {
                sendNotification("Text budget exceeded (" + oreTextObjectBudget + "). Clear or undo text first.", false, 4);
                return;
              }
              const n = oreTextNextBatchId++;
              const [, i] = getOreTextMaterial();
              const a = getOreTextFollowTarget();
              let l = null;
              try {
                if (a && !a.isNull?.()) {
                  l = readVec3Components(a.method("get_position").invoke());
                }
              } catch (e) {}
              const r = {
                id: n,
                text: o.text,
                center: o.center,
                spawnTime: time,
                animation: oreTextAnimationMode,
                colorMode: oreTextColorMode,
                followMode: oreTextFollowMode,
                anchorStart: l,
                objects: []
              };
              oreTextBatches.push(r);
              for (const e of o.jobs) {
                oreTextSpawnQueue.push({
                  ...e,
                  batchId: n,
                  materialID: i,
                  colorMode: oreTextColorMode
                });
              }
              sendNotification("Queued " + getOreTextMaterial()[0] + " text: " + o.text + " (" + o.jobs.length + ")", false, 4);
            }
            function renderOreTextPreview(e, t) {
              if (!oreTextPreviewEnabled || time < oreTextPreviewRefreshTime) {
                return;
              }
              oreTextPreviewRefreshTime = time + 0.25;
              clearOreTextPreview();
              const o = buildOreTextJobs(e, t);
              if (!o) {
                return;
              }
              const n = o.jobs.slice(0, 40);
              for (const e of n) {
                try {
                  const t = createObject(e.pos, identityQuaternion, [0.025, 0.025, 0.012], 0, [1, 0.35, 0.05, 0.42], null, false);
                  try {
                    Destroy(getComponent(t, Collider));
                  } catch (e) {}
                  oreTextPreviewObjects.push(t);
                } catch (e) {}
              }
            }
            function updateOreTextGun() {
              if (!rightGrab) {
                oreTextTriggerLatched = false;
                clearOreTextPreview();
                return;
              }
              let e = null;
              try {
                e = renderGun();
              } catch (e) {
                return;
              }
              if (!e) {
                return;
              }
              const t = getGunPointerLandingPosition(e);
              if (t) {
                if (rightTrigger) {
                  if (!oreTextTriggerLatched && !(time < oreTextSpawnDelay)) {
                    oreTextTriggerLatched = true;
                    oreTextSpawnDelay = time + 0.8;
                    clearOreTextPreview();
                    spawnOreTextAtPointer(t, rightHandTransform);
                  }
                  return;
                } else {
                  oreTextTriggerLatched = false;
                  renderOreTextPreview(t, rightHandTransform);
                  return;
                }
              } else {
                return undefined;
              }
            }
            function trackPersistentMob(e, t, o, n) {
              try {
                persistentMobEntries.push({
                  mobEntry: e,
                  pos: t,
                  rot: o,
                  object: n ?? null,
                  lastRespawnTime: time
                });
              } catch (e) {}
            }
            function stabilizeMobInstance(e, t = null) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                let o = e;
                try {
                  const t = e.method("get_gameObject").invoke();
                  if (t && !t.isNull?.()) {
                    o = t;
                  }
                } catch (e) {}
                try {
                  o.method("SetActive").invoke(true);
                } catch (e) {}
                try {
                  e.method("set_enabled").invoke(true);
                } catch (e) {}
                try {
                  const e = o.method("GetComponentInChildren", 0).inflate(AssemblyCSharp.class("AnimalCompany.ArenaItemKiller")).invoke();
                  if (e && !e.isNull?.()) {
                    try {
                      e.method("set_enabled").invoke(false);
                    } catch (e) {}
                    try {
                      e.field("_isEnabled").value = false;
                    } catch (e) {}
                  }
                } catch (e) {}
                if (t) {
                  try {
                    const e = getTransform(o);
                    if (e.method("get_position").invoke().field("y").value < -5000) {
                      e.method("set_position").invoke(t);
                    }
                  } catch (e) {}
                }
              } catch (e) {}
            }
            const acMobIdByName = {
              Unidentified: 0,
              Angler: 1,
              AnglerController: 1,
              AnglerMad: 2,
              AnglerMadController: 2,
              Armstrong: 3,
              ArmstrongController: 3,
              ArmstrongMad: 4,
              ArmstrongMadController: 4,
              Banshee: 5,
              BansheeController: 5,
              Bomb: 6,
              BombController: 6,
              Bomber: 7,
              BomberController: 7,
              BomberFlashbang: 8,
              BomberFlashbangController: 8,
              BomberMad: 9,
              BomberMadController: 9,
              Chicken: 10,
              ChickenController: 10,
              Cyst: 11,
              CystController: 11,
              FakeGorilla: 12,
              FakeGorillaController: 12,
              BigHead: 13,
              BigHeadController: 13,
              RedGreen: 14,
              RedGreenController: 14,
              Phantom: 15,
              PhantomController: 15,
              EvilEye: 16,
              EvilEyeController: 16,
              GiantThrower: 17,
              GiantThrowerController: 17,
              RedGreenMad: 18,
              RedGreenMadController: 18,
              Spider: 19,
              SpiderController: 19,
              FlyingSwarm: 20,
              FlyingSwarmController: 20,
              NextBot: 21,
              NextBotController: 21,
              Segway: 22,
              SegwayController: 22,
              NextBotStatic: 23,
              NextBotStaticController: 23,
              EvilEyePinata: 24,
              EvilEyePinataController: 24,
              EvilEyePinataLarge: 25,
              EvilEyePinataLargeController: 25,
              Lanky: 26,
              LankyController: 26,
              Blob: 27,
              BlobController: 27,
              Cutie: 28,
              CutieController: 28,
              SpiderCave: 29,
              SpiderCaveController: 29,
              ForestMob: 30,
              ForestMobController: 30,
              Mimic: 31,
              MimicController: 31,
              GraveyardBoss: 32,
              GraveyardBossController: 32,
              GiantController_GraveyardBoss: 32,
              Ringmaster: 33,
              RingmasterController: 33,
              Puppet: 34,
              PuppetController: 34,
              PolypMass: 35,
              PolypMassController: 35,
              RobotDog: 36,
              RobotDogController: 36,
              Shadow: 37,
              ShadowController: 37,
              Heart: 38,
              HeartController: 38,
              HeartMobController: 38,
              Slimey: 39,
              SlimeyController: 39,
              ShadowBoss: 40,
              ShadowBossController: 40,
              BigShark: 41,
              BigSharkController: 41,
              EdenZombie: 42,
              EdenZombieController: 42,
              Skinwalker: 43,
              SkinwalkerController: 43,
              YinWorm: 44,
              YinWormController: 44,
              YangWorm: 45,
              YangWormController: 45,
              ArmstrongSpace: 46,
              Smiley: 47,
              Uvula: 48,
              TubeMonster: 49,
              SmileyMad: 50,
              MomBossController: 51
            };
            const acMobNameById = {
              0: "Unidentified",
              1: "Angler",
              2: "AnglerMad",
              3: "Armstrong",
              4: "ArmstrongMad",
              5: "Banshee",
              6: "Bomb",
              7: "Bomber",
              8: "BomberFlashbang",
              9: "BomberMad",
              10: "Chicken",
              11: "Cyst",
              12: "FakeGorilla",
              13: "BigHead",
              14: "RedGreen",
              15: "Phantom",
              16: "EvilEye",
              17: "GiantThrower",
              18: "RedGreenMad",
              19: "Spider",
              20: "FlyingSwarm",
              21: "NextBot",
              22: "Segway",
              23: "NextBotStatic",
              24: "EvilEyePinata",
              25: "EvilEyePinataLarge",
              26: "Lanky",
              27: "Blob",
              28: "Cutie",
              29: "SpiderCave",
              30: "ForestMob",
              31: "Mimic",
              32: "GraveyardBoss",
              33: "Ringmaster",
              34: "Puppet",
              35: "PolypMass",
              36: "RobotDog",
              37: "Shadow",
              38: "Heart",
              39: "Slimey",
              40: "ShadowBoss",
              41: "BigShark",
              42: "EdenZombie",
              43: "Skinwalker",
              44: "YinWorm",
              45: "YangWorm",
              46: "ArmstrongSpace",
              47: "Smiley",
              48: "Uvula",
              49: "TubeMonster",
              50: "SmileyMad",
              51: "MomBossController"
            };
            const acMobAliases = {
              GiantController: "GiantThrower",
              GiantGreenController: "GiantThrower",
              Giant_GreenController: "GiantThrower",
              GreenGiantController: "GiantThrower",
              Green_GiantController: "GiantThrower",
              YanWormController: "YangWorm",
              YingWormController: "YinWorm",
              YinYanWormController: "YinWorm",
              YingYangWormController: "YinWorm",
              PrototypeSlenderController: "Shadow"
            };
            let acMobValidatorBypassEnabled = false;
            let acBeforeMobSpawnDelegate = null;
            let acBeforeMobSpawnDelegateClass = null;
            let acNetworkObjectSpawnDelegateRef = null;
            let acMobCompletionDelegates = [];
            function acAnimalCompanyImage() {
              return Il2Cpp.domain.assembly("AnimalCompany").image;
            }
            function acGetMobEnumField(e) {
              try {
                return acAnimalCompanyImage().class("AnimalCompany.MobID").field(e).value;
              } catch (e) {}
              return null;
            }
            function acResolveMobID(e) {
              if (typeof e == "number") {
                const t = acMobNameById[e | 0];
                if (t) {
                  return acGetMobEnumField(t);
                } else {
                  return null;
                }
              }
              const t = String(e || "").replace(/^mob_prefab\//, "");
              const o = t.replace(/Controller$/, "").replace(/_?Controller$/, "");
              const n = [t, acMobAliases[t], o, acMobAliases[o]].filter(Boolean);
              for (const e of n) {
                const t = acGetMobEnumField(e);
                if (t !== null) {
                  return t;
                }
                if (Object.prototype.hasOwnProperty.call(acMobIdByName, e)) {
                  const t = acMobNameById[acMobIdByName[e] | 0];
                  const o = t ? acGetMobEnumField(t) : null;
                  if (o !== null) {
                    return o;
                  }
                }
              }
              return null;
            }
            function acEnableMobValidatorBypass() {
              if (!acMobValidatorBypassEnabled) {
                try {
                  acAnimalCompanyImage().class("AnimalCompany.MobSpawnValidator").method("IsMobAllowed", 1).implementation = () => true;
                  acMobValidatorBypassEnabled = true;
                } catch (e) {
                  console.error("[MobValidatorBypass]", e);
                }
              }
            }
            function acGetBeforeMobSpawnDelegate() {
              if (acBeforeMobSpawnDelegate) {
                return acBeforeMobSpawnDelegate;
              }
              try {
                acBeforeMobSpawnDelegateClass = Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkRunner").tryNested("OnBeforeSpawned");
                const e = acAnimalCompanyImage().class("AnimalCompany.MobSpawnValidator");
                acBeforeMobSpawnDelegate = Il2Cpp.delegate(acBeforeMobSpawnDelegateClass, (t, o) => {
                  try {
                    if (!o || o.handle && o.handle.isNull()) {
                      return;
                    }
                    const t = o.method("get_Id").invoke();
                    e.method("AddAllowMob", 1).invoke(t);
                  } catch (e) {
                    console.error("[BeforeMobSpawn]", e);
                  }
                });
              } catch (e) {
                console.error("[BeforeMobSpawn delegate]", e);
                acBeforeMobSpawnDelegate = null;
              }
              return acBeforeMobSpawnDelegate;
            }
            function acGetNetworkObjectSpawnDelegateRef() {
              if (acNetworkObjectSpawnDelegateRef) {
                return acNetworkObjectSpawnDelegateRef;
              }
              try {
                const e = Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkObjectSpawnDelegate");
                acNetworkObjectSpawnDelegateRef = Il2Cpp.reference(e.alloc());
              } catch (e) {
                console.error("[NetworkObjectSpawnDelegate]", e);
                acNetworkObjectSpawnDelegateRef = NULL;
              }
              return acNetworkObjectSpawnDelegateRef;
            }
            function acIsServerReplicatedMob(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                if (!e.method("get_IsValid").invoke()) {
                  return false;
                }
                if (!e.method("get_IsInSimulation").invoke()) {
                  return false;
                }
                const o = e.method("get_Runner").invoke();
                return !!o && !o.isNull?.() && (!t?.handle || !o?.handle || !!o.handle.equals(t.handle));
              } catch (e) {
                console.error("[MobSpawn replication check]", e);
                return false;
              }
            }
            function acForceMobVisibleToAll(e, t) {
              try {
                if (!e || e.isNull?.() || !t || t.isNull?.()) {
                  return;
                }
                const o = t.method("get_ActivePlayers").invoke();
                if (!o) {
                  return;
                }
                const n = o.method("GetEnumerator").invoke();
                while (n.method("MoveNext").invoke()) {
                  const t = n.method("get_Current").invoke();
                  try {
                    e.method("SetPlayerAlwaysInterested", 2).invoke(t, true);
                  } catch (e) {}
                }
              } catch (e) {
                console.error("[MobSpawn always interested]", e);
              }
            }
            function spawnMobAtPos(e, t, o) {
              try {
                enableMobValidatorBypass();
                acEnableMobValidatorBypass();
                const n = acResolveMobID(e.name) ?? acResolveMobID(e.id);
                if (n === null) {
                  console.error("[MobSpawn] Unknown MobID: " + e.name + " (" + e.id + ")");
                  return null;
                }
                const i = PrefabGen.field("_instance").value;
                if (!i || i.isNull?.()) {
                  console.error("[MobSpawn] PrefabGenerator instance is unavailable");
                  return null;
                }
                const a = i.method("get_runner").invoke();
                if (!a || a.isNull?.()) {
                  console.error("[MobSpawn] NetworkRunner is unavailable; join a room first");
                  return null;
                }
                let l = false;
                let r = NULL;
                try {
                  const n = Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkObjectSpawnDelegate");
                  r = Il2Cpp.delegate(n, n => {
                    try {
                      if (l || !n) {
                        return;
                      }
                      try {
                        if (n.method("get_IsFailed").invoke()) {
                          return;
                        }
                      } catch (e) {}
                      const i = n.method("get_Object").invoke();
                      if (!acIsServerReplicatedMob(i, a)) {
                        console.error("[MobSpawn] Spawn completed without a replicated NetworkObject: " + e.name);
                        return;
                      }
                      try {
                        if (!i.method("get_HasStateAuthority").invoke()) {
                          i.method("RequestStateAuthority").invoke();
                        }
                      } catch (e) {}
                      acForceMobVisibleToAll(i, a);
                      l = true;
                      for (const t of persistentMobEntries) {
                        try {
                          if (!t || t.retired || t.mobEntry !== e) {
                            continue;
                          }
                          const o = t.object;
                          if (!o || !!o.isNull?.() || !o.method("get_IsValid").invoke()) {
                            t.retired = true;
                          }
                        } catch (e) {
                          try {
                            t.retired = true;
                          } catch (e) {}
                        }
                      }
                      stabilizeMobInstance(i, t);
                      spawnedPersistentMobs.push(i);
                      trackPersistentMob(e, t, o || identityQuaternion, i);
                      console.log("[MobSpawn] Network replicated for all players: " + e.name);
                    } catch (e) {
                      console.error("[MobSpawn completion]", e);
                    } finally {
                      const e = acMobCompletionDelegates.indexOf(r);
                      if (e >= 0) {
                        acMobCompletionDelegates.splice(e, 1);
                      }
                    }
                  });
                  acMobCompletionDelegates.push(r);
                } catch (e) {
                  console.error("[MobSpawn delegate]", e);
                  r = NULL;
                }
                const s = [n, t, o || identityQuaternion, getBeforeMobSpawnDelegate(), r, Il2Cpp.string("mod")];
                let c = null;
                try {
                  c = PrefabGen.method("SpawnMobAsyncInternal", 6).overload("AnimalCompany.MobID", "UnityEngine.Vector3", "UnityEngine.Quaternion", "Fusion.NetworkRunner.OnBeforeSpawned", "Fusion.NetworkObjectSpawnDelegate", "System.String").invoke(...s);
                } catch (e) {
                  console.error("[MobSpawn exact internal]", e);
                  try {
                    c = PrefabGen.method("SpawnMobAsyncInternal", 6).invoke(...s);
                  } catch (e) {
                    console.error("[MobSpawn internal]", e);
                    return null;
                  }
                }
                try {
                  if (c && c.method("get_IsFailed").invoke()) {
                    console.error("[MobSpawn] NetworkSpawnOp failed for " + e.name);
                    return null;
                  }
                  if (c && c.method("get_IsSpawned").invoke()) {
                    if (!acIsServerReplicatedMob(c.method("get_Object").invoke(), a)) {
                      console.error("[MobSpawn] Fusion returned a non-replicated object for " + e.name);
                      return null;
                    }
                  }
                } catch (e) {}
                mobSpawnAsyncBroken = false;
                return c ?? true;
              } catch (t) {
                mobSpawnAsyncBroken = true;
                console.error("[MobSpawn] " + e.name + ":", t);
                return null;
              }
            }
            function spawnMob(e, t, o = identityQuaternion) {
              const n = String(e ?? "").replace(/^mob_?prefab\//i, "").replace(/Controller$/i, "");
              const i = acMobIdByName[n] ?? acMobIdByName[String(e ?? "")] ?? -1;
              const a = mobIDs.find(e => e.name.toLowerCase() === n.toLowerCase()) ?? {
                name: n,
                id: i
              };
              return !!a.name && !(a.id < 0) && !!spawnMobAtPos(a, t, o);
            }
            function getDefaultMobSpawnPose() {
              try {
                const e = _pcMode ? getTransform(headCollider) : rightHandTransform;
                if (!e || e.isNull?.()) {
                  return {
                    pos: zeroVector,
                    rot: identityQuaternion
                  };
                }
                const t = getLaunchForward(e, _pcMode ? e : getTransform(headCollider));
                const o = getLaunchUp(e, t);
                return {
                  pos: Vector3.method("op_Addition").invoke(e.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(t, _pcMode ? 3 : 1), Vector3.method("op_Multiply", 2).invoke(o, _pcMode ? -0.35 : -0.1))),
                  rot: getLaunchRotation(e, t, o)
                };
              } catch (e) {
                return {
                  pos: zeroVector,
                  rot: identityQuaternion
                };
              }
            }
            const mobIdValueCache = {};
            function getMobIdValue(e) {
              if (mobIdValueCache[e] !== undefined) {
                return mobIdValueCache[e];
              }
              try {
                const t = Il2Cpp.domain.assembly("AnimalCompany").image;
                const o = t.class("AnimalCompany.MobID").field(e).value;
                mobIdValueCache[e] = o;
                return o;
              } catch (e) {}
              const t = mobIDs.find(t => t.name === e)?.id ?? null;
              mobIdValueCache[e] = t;
              return t;
            }
            function getMobIdLabel(e) {
              const t = getMobIdValue(e.name);
              if (t === null) {
                return e.id;
              } else {
                return t;
              }
            }
            function setItemColorHue(e, t) {
              if (!e || e.isNull?.()) {
                return;
              }
              const o = normalizeItemHue(t);
              try {
                e.method("SetColorHueData").invoke(o);
              } catch (t) {
                try {
                  e.method("set_colorHue").invoke(o);
                } catch (e) {}
              }
              try {
                e.method("HandleColorHueChanged").invoke();
              } catch (e) {}
            }
            function setItemColorSaturation(e, t) {
              if (!e || e.isNull?.()) {
                return;
              }
              const o = normalizeItemSaturation(t);
              try {
                e.method("SetColorSaturationData").invoke(o);
              } catch (t) {
                try {
                  e.method("set_colorSaturation").invoke(o);
                } catch (e) {}
              }
              try {
                e.method("HandleColorSaturationChanged").invoke();
              } catch (e) {}
            }
            function setItemColorSync(e, t, o) {
              setItemColorHue(e, t);
              setItemColorSaturation(e, o);
            }
            function quiverGunRand(e, t) {
              return Math.floor(Math.random() * (t - e + 1)) + e;
            }
            function quiverGunRandomItemID() {
              for (let e = 0; e < 20; e++) {
                const e = itemIDs[quiverGunRand(0, itemIDs.length - 1)];
                if (e && e.indexOf("quiver") === -1) {
                  return e;
                }
              }
              return itemIDs[itemIndex];
            }
            function quiverGunRainbowItem(e) {
              try {
                const t = e.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                if (t && !t.isNull()) {
                  t.method("set_scaleModifier").invoke(quiverGunRand(-128, 127));
                  setItemColorHue(t, quiverGunRand(-127, 127));
                  setItemColorSaturation(t, quiverGunRand(-20, 127));
                }
              } catch (e) {}
            }
            function quiverGunFindNearestQuiver(e) {
              try {
                const t = Object.method("FindObjectsByType", 1).inflate(QuiverClass).invoke(0);
                if (!t || t.isNull?.()) {
                  return null;
                }
                let o = null;
                let n = 999999;
                for (let i = 0; i < t.length; i++) {
                  try {
                    const a = t.get(i);
                    if (!a || a.isNull?.()) {
                      continue;
                    }
                    const l = a.method("get_transform").invoke().method("get_position").invoke();
                    const r = Vector3.method("Distance").invoke(e, l);
                    if (r < n) {
                      n = r;
                      o = a;
                    }
                  } catch (e) {}
                }
                if (n <= 3) {
                  return o;
                } else {
                  return null;
                }
              } catch (e) {
                return null;
              }
            }
            function quiverGunSpawnQuiver(e) {
              try {
                const t = spawnItemAsync(Il2Cpp.string("item_prefab/item_quiver"), e, identityQuaternion, NULL);
                if (t && !t.isNull()) {
                  return t;
                }
              } catch (e) {}
              try {
                return spawnItemAtPos("item_quiver", e, identityQuaternion);
              } catch (e) {}
              return null;
            }
            function quiverGunTryAddItem(e, t, o) {
              try {
                const n = t.method("get_transform").invoke().method("get_position").invoke();
                const i = spawnItemAsync(Il2Cpp.string("item_prefab/" + o), n, identityQuaternion, NULL);
                if (!i || i.isNull()) {
                  return false;
                }
                const a = i.method("get_gameObject").invoke();
                if (!a || a.isNull()) {
                  return false;
                }
                quiverGunRainbowItem(a);
                let l = false;
                try {
                  let t = null;
                  try {
                    t = i.method("GetComponent", 1).inflate(GBIClass).invoke();
                  } catch (e) {}
                  try {
                    if (!t || !!t.isNull?.()) {
                      t = a.method("GetComponent", 1).inflate(GBIClass).invoke();
                    }
                  } catch (e) {}
                  if (t && !t.isNull()) {
                    t.method("AddToBag").invoke(e);
                    l = true;
                  }
                } catch (e) {}
                if (!l) {
                  try {
                    let t = null;
                    try {
                      t = i.method("GetComponent", 1).inflate(GBIClass).invoke();
                    } catch (e) {}
                    try {
                      if (!t || !!t.isNull?.()) {
                        t = a.method("GetComponent", 1).inflate(GBIClass).invoke();
                      }
                    } catch (e) {}
                    if (t && !t.isNull()) {
                      t.method("AddToBagInternal").invoke(e);
                      l = true;
                    }
                  } catch (e) {}
                }
                return l;
              } catch (e) {
                return false;
              }
            }
            function updatePendingQuiverFills() {
              if (pendingQuiverFills && pendingQuiverFills.length !== 0) {
                for (let e = pendingQuiverFills.length - 1; e >= 0; e--) {
                  try {
                    const t = pendingQuiverFills[e];
                    if (!t || !t.quiverGo || t.quiverGo.isNull?.()) {
                      pendingQuiverFills.splice(e, 1);
                      continue;
                    }
                    if (time < t.nextTry) {
                      continue;
                    }
                    let o = null;
                    let n = t.quiverGo;
                    let i = null;
                    try {
                      i = t.quiverGo.method("get_transform").invoke().method("get_position").invoke();
                    } catch (e) {}
                    try {
                      if (i) {
                        o = quiverGunFindNearestQuiver(i);
                      }
                    } catch (e) {}
                    try {
                      if ((!o || !!o.isNull?.()) && !!t.quiverObj && !t.quiverObj.isNull?.()) {
                        o = t.quiverObj.method("GetComponent", 1).inflate(QuiverClass).invoke();
                      }
                    } catch (e) {}
                    try {
                      if (!o || !!o.isNull?.()) {
                        o = t.quiverGo.method("GetComponent", 1).inflate(QuiverClass).invoke();
                      }
                    } catch (e) {}
                    try {
                      if (!o || !!o.isNull?.()) {
                        o = t.quiverGo.method("GetComponentInChildren", 1).inflate(QuiverClass).invoke();
                      }
                    } catch (e) {}
                    try {
                      if (o && !o.isNull?.()) {
                        n = o.method("get_gameObject").invoke();
                      }
                    } catch (e) {}
                    if (!o || o.isNull?.()) {
                      t.attempts++;
                      t.nextTry = time + 0.15;
                      if (t.attempts > 30) {
                        pendingQuiverFills.splice(e, 1);
                      }
                      continue;
                    }
                    let a = 0;
                    while (t.remaining > 0 && a < 2) {
                      if (!quiverGunTryAddItem(o, n, quiverGunRandomItemID())) {
                        t.attempts++;
                        break;
                      }
                      t.remaining--;
                      t.added++;
                      a++;
                    }
                    if (t.remaining <= 0) {
                      pendingQuiverFills.splice(e, 1);
                    } else {
                      t.nextTry = time + 0.08;
                      if (t.attempts > 60) {
                        pendingQuiverFills.splice(e, 1);
                      }
                    }
                  } catch (e) {}
                }
              }
            }
            function normalizeItemHue(e) {
              return (Math.round(Number(e) || 0) % 256 + 256) % 256;
            }
            function normalizeItemSaturation(e) {
              const t = Math.round(Number(e) || 0);
              return Math.max(-128, Math.min(127, t));
            }
            function spawnNetworkPrefab(e, t, o) {
              try {
                const n = PrefabGen.field("_instance").value.method("get_runner").invoke();
                if (!n || n.isNull()) {
                  return null;
                }
                const i = n.field("_config").value.field("PrefabTable").value.field("_sources").value;
                const a = i.method("get_Count").invoke();
                for (let l = 0; l < a; l++) {
                  try {
                    const a = i.method("get_Item").invoke(l);
                    if (a.method("get_Description").invoke().toString().toLowerCase().includes(String(e).toLowerCase())) {
                      const e = a.method("WaitForResult").invoke();
                      if (!e || e.isNull()) {
                        return null;
                      }
                      const i = e => {
                        if (e.class.isEnum || e.isPrimitive) {
                          return 0;
                        }
                        if (!e.class.isValueType) {
                          return NULL;
                        }
                        const t = e.class.fields.filter(e => !e.isStatic);
                        if (t.length === 0) {
                          return 0;
                        } else {
                          return t.map(e => i(e.type));
                        }
                      };
                      const l = (e, t, o) => e.class.fields.filter(e => !e.isStatic).map(e => {
                        const n = e.name.toLowerCase();
                        if (n.includes("hasvalue")) {
                          if (t) {
                            return 1;
                          } else {
                            return 0;
                          }
                        } else if (n === "value" && t) {
                          return o;
                        } else {
                          return i(e.type);
                        }
                      });
                      const r = (e, t) => {
                        if (typeof t == "boolean") {
                          if (t) {
                            return 1;
                          } else {
                            return 0;
                          }
                        }
                        if (t instanceof Il2Cpp.ValueType) {
                          const o = e.class.fields.filter(e => !e.isStatic);
                          if (o.length === 0) {
                            return 0;
                          } else {
                            return o.map(e => r(e.type, e.bind(t).value));
                          }
                        }
                        if (Array.isArray(t)) {
                          return t.map(t => r(e, t));
                        } else {
                          return t;
                        }
                      };
                      const s = (e, t) => l(e, true, r(t.type, t));
                      let c = null;
                      for (const e of n.method("Spawn").overloads()) {
                        if (e.parameterCount !== 6 || e.isGeneric) {
                          continue;
                        }
                        const t = e.parameters;
                        if (t[0].type.name.includes("Fusion.NetworkObject") && t[1].type.name.startsWith("System.Nullable") && t[1].type.name.includes("Vector3") && t[2].type.name.startsWith("System.Nullable") && t[2].type.name.includes("Quaternion") && t[3].type.name.startsWith("System.Nullable") && t[3].type.name.includes("PlayerRef") && t[4].type.name.includes("OnBeforeSpawned") && t[5].type.name.includes("NetworkSpawnFlags")) {
                          c = e;
                          break;
                        }
                      }
                      if (!c) {
                        return null;
                      }
                      const d = s(c.parameters[1].type, t);
                      const u = s(c.parameters[2].type, o);
                      const m = l(c.parameters[3].type, false, i(c.parameters[3].type));
                      const h = c.parameters[4].type.class.isValueType ? i(c.parameters[4].type) : NULL;
                      const g = c.bind(n).invoke(e, d, u, m, h, 0);
                      if (g && !g.isNull()) {
                        spawnedNetworkPrefabs.push(g);
                      }
                      return g;
                    }
                  } catch (e) {}
                }
              } catch (e) {
                console.error("spawnNetworkPrefab error: " + e);
              }
              return null;
            }
            function cleanupDeadTrackedObjects(e) {
              for (let t = e.length - 1; t >= 0; t--) {
                const o = e[t];
                if (!o || !!o.isNull?.()) {
                  e.splice(t, 1);
                }
              }
            }
            function trySetObjectVelocity(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                let o = null;
                try {
                  o = getComponent(e, Rigidbody);
                } catch (e) {}
                if ((!o || o.isNull?.()) && e.method) {
                  try {
                    const t = e.method("get_gameObject").invoke();
                    if (t && !t.isNull()) {
                      o = getComponent(t, Rigidbody);
                    }
                  } catch (e) {}
                }
                if ((!o || o.isNull?.()) && e.method) {
                  try {
                    const t = getTransform(e);
                    if (t && !t.isNull()) {
                      o = getComponentInParent(t.method("get_gameObject").invoke(), Rigidbody);
                    }
                  } catch (e) {}
                }
                if ((!o || o.isNull?.()) && e.method) {
                  try {
                    const t = e.method("get_gameObject").invoke();
                    if (t && !t.isNull()) {
                      o = t.method("GetComponentInChildren", 0).inflate(Rigidbody).invoke();
                    }
                  } catch (e) {}
                }
                if (o && !o.isNull()) {
                  try {
                    o.method("set_isKinematic").invoke(false);
                  } catch (e) {}
                  try {
                    o.method("set_useGravity").invoke(true);
                  } catch (e) {}
                  try {
                    o.method("set_velocity").invoke(t);
                    return true;
                  } catch (e) {}
                  try {
                    o.method("AddForce").invoke(t, 2);
                    return true;
                  } catch (e) {}
                  try {
                    o.method("AddForce").invoke(t);
                    return true;
                  } catch (e) {}
                  try {
                    o.field("_velocity").value = t;
                    return true;
                  } catch (e) {}
                }
              } catch (e) {}
              return false;
            }
            function getLaunchForward(e, t = null) {
              try {
                const o = t && !t.isNull?.() ? t : headCollider && !headCollider.isNull?.() ? getTransform(headCollider) : null;
                const n = o && !o.isNull?.() ? readVec3Components(o.method("get_forward").invoke()) : [0, 0, 1];
                const i = normalizeSceneObjectHandle(e) || String(e);
                const a = launchAxisCache.get(i);
                const l = t => {
                  switch (t) {
                    case "forward":
                    default:
                      return e.method("get_forward").invoke();
                    case "-forward":
                      return Vector3.method("op_Multiply", 2).invoke(e.method("get_forward").invoke(), -1);
                    case "up":
                      return e.method("get_up").invoke();
                    case "-up":
                      return Vector3.method("op_Multiply", 2).invoke(e.method("get_up").invoke(), -1);
                    case "right":
                      return e.method("get_right").invoke();
                    case "-right":
                      return Vector3.method("op_Multiply", 2).invoke(e.method("get_right").invoke(), -1);
                  }
                };
                if (a) {
                  return Vector3.method("Normalize").invoke(l(a));
                }
                const r = ["forward", "-forward"];
                let s = "forward";
                let c = -999999;
                for (const e of r) {
                  try {
                    const t = readVec3Components(l(e));
                    const o = t[0] * n[0] + t[1] * n[1] + t[2] * n[2];
                    if (o > c) {
                      c = o;
                      s = e;
                    }
                  } catch (e) {}
                }
                launchAxisCache.set(i, s);
                return Vector3.method("Normalize").invoke(l(s));
              } catch (t) {
                try {
                  return e.method("get_forward").invoke();
                } catch (e) {
                  return [0, 0, 1];
                }
              }
            }
            function getLaunchUp(e, t) {
              try {
                const o = [0, 1, 0];
                const n = Vector3.method("Dot").invoke(o, t);
                let i = Vector3.method("op_Subtraction", 2).invoke(o, Vector3.method("op_Multiply", 2).invoke(t, n));
                if (Vector3.method("Magnitude").invoke(i) < 0.05) {
                  i = e.method("get_up").invoke();
                }
                return Vector3.method("Normalize").invoke(i);
              } catch (t) {
                try {
                  return e.method("get_up").invoke();
                } catch (e) {
                  return [0, 1, 0];
                }
              }
            }
            function getLaunchRotation(e, t, o) {
              try {
                return Quaternion.method("LookRotation", 2).invoke(t, o);
              } catch (e) {}
              try {
                return Quaternion.method("LookRotation", 1).invoke(t);
              } catch (e) {}
              try {
                return e.method("get_rotation").invoke();
              } catch (e) {
                return identityQuaternion;
              }
            }
            function spawnForwardLaunchedNetworkPrefab(e, t, o = 30, n = 1) {
              try {
                const i = getLaunchForward(t);
                const a = getLaunchUp(t, i);
                const l = Vector3.method("op_Addition").invoke(t.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(i, n), Vector3.method("op_Multiply", 2).invoke(a, -0.06)));
                const r = spawnNetworkPrefab(e, l, getLaunchRotation(t, i, a));
                if (r && !r.isNull()) {
                  if (!trySetObjectVelocity(r, Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(i, o), Vector3.method("op_Multiply", 2).invoke(a, 3)))) {
                    try {
                      getTransform(r).method("set_position").invoke(Vector3.method("op_Addition").invoke(l, Vector3.method("op_Multiply", 2).invoke(i, 2.2)));
                    } catch (e) {}
                  }
                }
                return r;
              } catch (e) {
                return null;
              }
            }
            function spawnForwardLaunchedItem(e, t, o = 24, n = 0.82) {
              try {
                const i = getLaunchForward(t);
                const a = getLaunchUp(t, i);
                const l = Vector3.method("op_Addition").invoke(t.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(i, n), Vector3.method("op_Multiply", 2).invoke(a, -0.05)));
                const r = spawnItemAtPos(e, l, getLaunchRotation(t, i, a));
                if (r && !r.isNull()) {
                  trySetObjectVelocity(r, Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(i, o), Vector3.method("op_Multiply", 2).invoke(a, 1.2)));
                }
                return r;
              } catch (e) {
                return null;
              }
            }
            function getGunAimDirection() {
              if (_pcMode) {
                const e = _pcSmoothYaw * Math.PI / 180;
                const t = _pcSmoothPitch * Math.PI / 180;
                const o = Math.cos(t);
                return Vector3.method("Normalize").invoke([o * Math.sin(e), -Math.sin(t), o * Math.cos(e)]);
              }
              try {
                const e = headCollider && !headCollider.isNull?.() ? getTransform(headCollider) : null;
                return getLaunchForward(rightHandTransform, e);
              } catch (e) {
                return rightHandTransform.method("get_forward").invoke();
              }
            }
            function setUniformLocalScale(e, t, o) {
              try {
                const n = getTransform(t).method("get_localScale").invoke();
                const i = Math.abs(n.field("x").value) || 1;
                const a = Math.abs(n.field("y").value) || 1;
                const l = Math.abs(n.field("z").value) || 1;
                getTransform(e).method("set_localScale").invoke([o / i, o / a, o / l]);
              } catch (t) {
                try {
                  getTransform(e).method("set_localScale").invoke([o, o, o]);
                } catch (e) {}
              }
            }
            function addSurfaceOutline(e, t, o = 0.045) {}
            function addRoundedCorners(e, t, o = 0.022) {}
            let cachedSpawnPrefab = null;
            let cachedSpawnPrefabName = "";
            let prefabTestIndex = 0;
            let lockedItems = [];
            let lastSpawnSearchTime = 0;
            function world2Player(e) {
              e = Vector3.method("op_Subtraction", 2).invoke(e, getTransform(bodyCollider).method("get_position").invoke());
              return e = Vector3.method("op_Addition", 2).invoke(e, getTransform(GTPlayer).method("get_position").invoke());
            }
            function teleportPlayer(e) {
              const t = NetPlayer.method("get_localPlayer").invoke();
              if (t) {
                t.method("RPC_Teleport").invoke(world2Player(e));
              }
            }
            function sendNotification(e = "", t = true, o = 5) {
              notifactionResetTime = getNotificationClock() + o;
              currentNotification = e;
              pushHudNotification(e);
              if (isAlive(menuNotifTextComponent)) {
                try {
                  menuNotifTextComponent.method("set_text").invoke(Il2Cpp.string(e));
                } catch (e) {}
              }
            }
            function createObject(e = zeroVector, t = identityQuaternion, o = oneVector, n = 3, i = [1, 1, 1, 1], a = null, l = false) {
              const r = GameObject.method("CreatePrimitive").invoke(n);
              const s = getComponent(r, Renderer);
              if (i[3] == 0) {
                s.method("set_enabled").invoke(false);
              } else {
                const e = s.method("get_material").invoke();
                e.method("set_shader").invoke(UberShader);
                e.method("set_color").invoke(i);
              }
              const c = getComponent(r, Collider);
              if (c && !c.isNull()) {
                if (l) {
                  c.method("set_enabled").invoke(true);
                  c.method("set_isTrigger").invoke(true);
                } else {
                  c.method("set_isTrigger").invoke(true);
                }
              }
              const d = getTransform(r);
              if (a != null) {
                d.method("SetParent", 2).invoke(a, false);
              }
              d.method("set_position").invoke(e);
              d.method("set_rotation").invoke(t);
              d.method("set_localScale").invoke(o);
              return r;
            }
            function setRendererColorSafe(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                const o = getComponent(e, Renderer);
                if (!o || o.isNull?.()) {
                  return;
                }
                const n = o.method("get_material").invoke();
                if (n && !n.isNull?.()) {
                  n.method("set_color").invoke(t);
                }
              } catch (e) {}
            }
            function setTextColorSafe(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                try {
                  const o = getComponent(e, TextMeshPro3D);
                  if (o && !o.isNull?.()) {
                    try {
                      o.method("set_color").invoke(t);
                    } catch (e) {}
                    return;
                  }
                } catch (e) {}
                try {
                  const o = getComponent(e, TextMesh);
                  if (o && !o.isNull?.()) {
                    try {
                      o.method("set_color").invoke(t);
                    } catch (e) {}
                  }
                } catch (e) {}
              } catch (e) {}
            }
            function updateLiveMenuThemeVisuals() {
              try {
                if (!menu || menu.isNull?.()) {
                  return;
                }
                const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                const t = menu.method("GetComponentsInChildren", 1).inflate(e).invoke(true);
                if (!t || t.isNull?.()) {
                  return;
                }
                for (let e = 0; e < t.length; e++) {
                  try {
                    const o = t.get(e);
                    if (!o || o.isNull?.()) {
                      continue;
                    }
                    const n = o.method("get_gameObject").invoke();
                    if (!n || n.isNull?.()) {
                      continue;
                    }
                    const i = (n.method("get_name").invoke()?.content ?? "").toString();
                    if (i === "MenuBackground") {
                      setRendererColorSafe(n, bgColor);
                    } else if (i.startsWith("@")) {
                      const e = getIndex(i.slice(1));
                      if (themeMode === 10) {
                        setRendererColorSafe(n, i === "@Leave" || i === "@Home" || i === "@OpenSearch" || i === "@GlobalReturn" || i === "@PreviousPage" || i === "@NextPage" ? e?.enabled ? [0.22, 0.22, 0.22, 1] : [0.01, 0.01, 0.01, 0.96] : e?.enabled ? buttonPressedColor : buttonColor);
                      } else if (e) {
                        setRendererColorSafe(n, e.enabled ? buttonPressedColor : buttonColor);
                      }
                    }
                  } catch (e) {}
                }
              } catch (e) {}
            }
            function axisDeadzone(e, t = 0.16) {
              if (Math.abs(e) < t) {
                return 0;
              } else {
                return e;
              }
            }
            function smoothNumber(e, t, o = 0.18) {
              return e + (t - e) * o;
            }
            function smoothVec3(e, t, o = 0.18) {
              e[0] = smoothNumber(e[0], t[0], o);
              e[1] = smoothNumber(e[1], t[1], o);
              e[2] = smoothNumber(e[2], t[2], o);
              return e;
            }
            function readVec3Components(e) {
              return [e.field("x").value || 0, e.field("y").value || 0, e.field("z").value || 0];
            }
            function normalizeXZ(e, t) {
              const o = Math.sqrt(e * e + t * t);
              if (o < 0.0001) {
                return [0, 1];
              } else {
                return [e / o, t / o];
              }
            }
            function getPlayerLaunchTransform(e, t = 1, o = null) {
              try {
                const o = getPlayerHandAnchorTransform(e, t);
                if (o && !o.isNull?.()) {
                  return o;
                }
              } catch (e) {}
              try {
                if (o && !o.isNull?.()) {
                  try {
                    const e = getTransform(o);
                    if (e && !e.isNull?.()) {
                      return e;
                    }
                  } catch (e) {}
                  try {
                    const e = o.method("get_gameObject").invoke();
                    if (e && !e.isNull?.()) {
                      const t = getTransform(e);
                      if (t && !t.isNull?.()) {
                        return t;
                      }
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              return getPlayerProjectileOriginTransform(e);
            }
            function getPlayerHeadTransform(e) {
              try {
                const t = e.field("headCollider").value;
                if (t && !t.isNull?.()) {
                  return getTransform(t);
                }
              } catch (e) {}
              try {
                const t = e.method("get_playerView").invoke();
                if (t && !t.isNull?.()) {
                  try {
                    const e = t.field("_cameraTransform").value;
                    if (e && !e.isNull?.()) {
                      return e;
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              try {
                return getTransform(e);
              } catch (e) {
                return null;
              }
            }
            function collectAllLobbyItemsWithHeld() {
              const e = getAllLobbyItemGameObjects();
              const t = new Set();
              const o = [];
              for (const n of e) {
                try {
                  const e = normalizeSceneObjectHandle(n) || String(n);
                  if (!t.has(e)) {
                    t.add(e);
                    o.push(n);
                  }
                } catch (e) {}
              }
              for (const e of getAllNetPlayersList(true)) {
                for (const n of [0, 1]) {
                  try {
                    const i = getPlayerHeldGrabbable(e, n);
                    if (!i || i.isNull?.()) {
                      continue;
                    }
                    const a = normalizeSceneObjectHandle(i) || String(i);
                    if (!t.has(a)) {
                      t.add(a);
                      o.push(i);
                    }
                  } catch (e) {}
                }
                try {
                  const n = e.method("get_playerView").invoke();
                  if (n && !n.isNull?.()) {
                    const e = n.method("get_gameObject").invoke();
                    if (e && !e.isNull?.()) {
                      try {
                        const n = e.method("GetComponentsInChildren", 0).inflate(GBIClass).invoke(true);
                        if (n && !n.isNull?.()) {
                          for (let e = 0; e < n.length; e++) {
                            try {
                              const i = n.get(e);
                              if (!i || i.isNull?.()) {
                                continue;
                              }
                              const a = normalizeSceneObjectHandle(i) || String(i);
                              if (!t.has(a)) {
                                t.add(a);
                                o.push(i);
                              }
                            } catch (e) {}
                          }
                        }
                      } catch (e) {}
                      try {
                        const n = e.method("GetComponentsInChildren", 0).inflate(GBOClass).invoke(true);
                        if (n && !n.isNull?.()) {
                          for (let e = 0; e < n.length; e++) {
                            try {
                              const i = n.get(e);
                              if (!i || i.isNull?.()) {
                                continue;
                              }
                              const a = normalizeSceneObjectHandle(i) || String(i);
                              if (!t.has(a)) {
                                t.add(a);
                                o.push(i);
                              }
                            } catch (e) {}
                          }
                        }
                      } catch (e) {}
                    }
                  }
                } catch (e) {}
              }
              return o;
            }
            function getRootLikeObject(e) {
              try {
                if (!e || e.isNull?.()) {
                  return null;
                }
                let t = e;
                try {
                  const o = e.method("get_gameObject").invoke();
                  if (o && !o.isNull?.()) {
                    t = o;
                  }
                } catch (e) {}
                try {
                  const e = getTransform(t).method("get_root").invoke();
                  if (e && !e.isNull?.()) {
                    const t = e.method("get_gameObject").invoke();
                    if (t && !t.isNull?.()) {
                      return t;
                    }
                  }
                } catch (e) {}
                return t;
              } catch (t) {
                return e;
              }
            }
            function isItemOccupiedByPlayer(e) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                const t = normalizeSceneObjectHandle(getRootLikeObject(e)) || normalizeSceneObjectHandle(e);
                for (const e of getAllNetPlayersList(true)) {
                  try {
                    for (const o of [0, 1]) {
                      const n = getPlayerHeldGrabbable(e, o);
                      if (!n || n.isNull?.()) {
                        continue;
                      }
                      const i = normalizeSceneObjectHandle(getRootLikeObject(n)) || normalizeSceneObjectHandle(n);
                      if (t && i && t === i) {
                        return true;
                      }
                    }
                  } catch (e) {}
                }
                try {
                  const t = getRootLikeObject(e).method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                  if (t && !t.isNull?.()) {
                    return true;
                  }
                } catch (e) {}
              } catch (e) {}
              return false;
            }
            function tryForceDeveloperMode() {
              try {
                const e = AssemblyCSharp.class("AnimalCompany.App").method("get_state").invoke();
                if (!e || e.isNull?.()) {
                  return;
                }
                try {
                  const t = e.method("get_user").invoke();
                  if (t && !t.isNull?.()) {
                    try {
                      t.field("_isDeveloper").value = true;
                    } catch (e) {}
                    try {
                      t.field("isDeveloper").value = true;
                    } catch (e) {}
                    tryCallNames(t, ["set_isDeveloper"], 1, true);
                  }
                } catch (e) {}
                try {
                  const t = e.method("get_mapMachine").invoke();
                  if (t && !t.isNull?.()) {
                    try {
                      const e = t.method("get_useDevMode").invoke();
                      if (e && !e.isNull?.()) {
                        try {
                          e.method("set_value").invoke(true);
                        } catch (e) {}
                        try {
                          e.field("_value").value = true;
                        } catch (e) {}
                        try {
                          e.field("value").value = true;
                        } catch (e) {}
                      }
                    } catch (e) {}
                    try {
                      t.field("_useDevMode").value = true;
                    } catch (e) {}
                    tryCallNames(t, ["set_useDevMode"], 1, true);
                    tryCallNames(t, ["RefreshDevMode", "Reload", "RefreshButtons", "UpdateButtons", "OpenDevMenu", "OpenDevPanel"], 0);
                  }
                } catch (e) {}
              } catch (e) {}
            }
            function spawnHeldItemDuplicateForPlayer(e, t = 14) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                const o = getPlayerHeldGrabbable(e, 1) ?? getPlayerHeldGrabbable(e, 0);
                if (!o || o.isNull?.()) {
                  return false;
                }
                const n = getGrabbableItemId(o);
                if (!n) {
                  return false;
                }
                const i = getPlayerLaunchTransform(e, getPlayerHeldGrabbable(e, 1) === o ? 1 : 0, o);
                if (!i || i.isNull?.()) {
                  return false;
                }
                const a = getLaunchForward(i, getTransform(e));
                const l = getLaunchUp(i, a);
                const r = getLaunchRotation(i, a, l);
                const s = i.method("get_position").invoke();
                const c = spawnItemAtPos(n, Vector3.method("op_Addition").invoke(s, Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(a, 0.42), Vector3.method("op_Multiply", 2).invoke(l, -0.02))), r);
                if (!c || c.isNull?.()) {
                  return false;
                }
                try {
                  const e = o.method("GetComponent", 1).inflate(GBOClass).invoke();
                  const t = c.method("GetComponent", 1).inflate(GBOClass).invoke();
                  if (e && !e.isNull?.() && t && !t.isNull?.()) {
                    try {
                      t.method("set_scaleModifier").invoke(e.method("get_scaleModifier").invoke());
                    } catch (e) {}
                    try {
                      t.method("set_colorHue").invoke(e.method("get_colorHue").invoke());
                    } catch (e) {}
                    try {
                      t.method("set_colorSaturation").invoke(e.method("get_colorSaturation").invoke());
                    } catch (e) {}
                    try {
                      t.field("_scaleModifier").value = e.field("_scaleModifier").value;
                    } catch (e) {}
                    try {
                      t.field("_colorHue").value = e.field("_colorHue").value;
                    } catch (e) {}
                    try {
                      t.field("_colorSaturation").value = e.field("_colorSaturation").value;
                    } catch (e) {}
                  }
                } catch (e) {}
                trySetObjectVelocity(c, Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(a, t), Vector3.method("op_Multiply", 2).invoke(l, 0.7)));
                return true;
              } catch (e) {
                return false;
              }
            }
            function spawnHeldItemEjectBurstForPlayer(e, t = 3) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                const o = getPlayerHeldGrabbable(e, 1) ?? getPlayerHeldGrabbable(e, 0);
                if (!o || o.isNull?.()) {
                  return false;
                }
                const n = getGrabbableItemId(o);
                if (!n) {
                  return false;
                }
                const i = getPlayerLaunchTransform(e, getPlayerHeldGrabbable(e, 1) === o ? 1 : 0, o);
                if (!i || i.isNull?.()) {
                  return false;
                }
                const a = getPlayerStraightForward(e);
                const l = i.method("get_position").invoke();
                const r = getLookRotationFromForward(a);
                let s = false;
                for (let e = 0; e < t; e++) {
                  try {
                    const t = (e % 2 == 0 ? -1 : 1) * (0.03 + e * 0.015);
                    const i = spawnItemAtPos(n, [l.field("x").value + a[0] * 0.24, l.field("y").value - 0.02 + e * 0.01, l.field("z").value + a[2] * 0.24 + t], r);
                    if (!i || i.isNull?.()) {
                      continue;
                    }
                    s = true;
                    try {
                      const e = o.method("GetComponent", 1).inflate(GBOClass).invoke();
                      const t = i.method("GetComponent", 1).inflate(GBOClass).invoke();
                      if (e && !e.isNull?.() && t && !t.isNull?.()) {
                        try {
                          t.method("set_scaleModifier").invoke(e.method("get_scaleModifier").invoke());
                        } catch (e) {}
                        try {
                          t.method("set_colorHue").invoke(e.method("get_colorHue").invoke());
                        } catch (e) {}
                        try {
                          t.method("set_colorSaturation").invoke(e.method("get_colorSaturation").invoke());
                        } catch (e) {}
                      }
                    } catch (e) {}
                    trySetObjectVelocity(i, [a[0] * (7 + e * 0.45), 0.6 + e * 0.08, a[2] * (7 + e * 0.45)]);
                  } catch (e) {}
                }
                return s;
              } catch (e) {
                return false;
              }
            }
            function pulseWeaponSecondary(e) {
              for (let t = 0; t < 6; t++) {
                tryCallNames(e, ["HandleSecondaryUse", "HandleGripUse", "CockHammer", "PullBackHammer", "HandleSecondaryPress", "PressBButton"], 0);
                tryCallNames(e, ["HandleSecondaryButton", "HandleSecondaryUse", "HandleGripUse", "CockHammer", "PullBackHammer", "HandleSecondaryPress", "PressBButton"], 1, true);
                tryCallNames(e, ["OnSecondaryButtonDown", "OnSecondaryUseDown", "OnBButtonDown", "HandleBButtonDown"], 0);
                tryCallNames(e, ["HandleSecondaryButton", "HandleSecondaryUse", "HandleGripUse", "HandleSecondaryPress", "HandleBButton"], 1, false);
                tryCallNames(e, ["OnSecondaryButtonUp", "OnSecondaryUseUp", "OnBButtonUp", "HandleBButtonUp"], 0);
              }
            }
            function applyGunBuffsToGrabbable(e, t = false) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                if (!grabbableLooksLikeGun(e)) {
                  return false;
                }
                const o = String(e.class?.type?.name ?? "").toLowerCase();
                const n = o.indexOf("revolver") >= 0;
                const i = o.indexOf("rpg") >= 0 || o.indexOf("rocket") >= 0 || o.indexOf("flare") >= 0 || o.indexOf("zipline") >= 0;
                try {
                  e.field("_ammoLoaded").value = 255;
                } catch (e) {}
                try {
                  e.field("ammoLoaded").value = 255;
                } catch (e) {}
                try {
                  e.field("_ammo").value = 255;
                } catch (e) {}
                try {
                  e.field("ammo").value = 255;
                } catch (e) {}
                try {
                  e.field("_isHammerCocked").value = true;
                } catch (e) {}
                try {
                  e.field("_isCocked").value = true;
                } catch (e) {}
                try {
                  e.field("isHammerCocked").value = true;
                } catch (e) {}
                try {
                  e.field("_hasAmmo").value = true;
                } catch (e) {}
                try {
                  e.field("hasAmmo").value = true;
                } catch (e) {}
                try {
                  e.field("_isLoaded").value = true;
                } catch (e) {}
                try {
                  e.field("isLoaded").value = true;
                } catch (e) {}
                try {
                  e.field("_nextUseTime").value = 0;
                } catch (e) {}
                try {
                  e.field("_reloadTimer").value = 0;
                } catch (e) {}
                try {
                  e.field("_reloadCooldown").value = 0;
                } catch (e) {}
                try {
                  e.field("_shotCooldown").value = 0;
                } catch (e) {}
                try {
                  e.field("_timeBetweenShots").value = 0;
                } catch (e) {}
                try {
                  e.field("_useCooldown").value = 0;
                } catch (e) {}
                try {
                  e.field("_secondaryUseCooldown").value = 0;
                } catch (e) {}
                try {
                  e.field("_triggerUseCooldown").value = 0;
                } catch (e) {}
                try {
                  e.field("_hammerPullbackAmount").value = 1;
                } catch (e) {}
                try {
                  e.field("_recoilCooldown").value = 0;
                } catch (e) {}
                try {
                  e.field("recoilForceMag").value = 0;
                } catch (e) {}
                try {
                  e.field("_recoilForceMag").value = 0;
                } catch (e) {}
                try {
                  e.method("set_isHammerCocked").invoke(true);
                } catch (e) {}
                try {
                  e.method("set_ammoLoaded").invoke(255);
                } catch (e) {}
                try {
                  e.method("set_hasAmmo").invoke(true);
                } catch (e) {}
                try {
                  e.method("set_isLoaded").invoke(true);
                } catch (e) {}
                if (i) {
                  try {
                    const t = e.method("get_loadedState").invoke();
                    if (t && !t.isNull?.()) {
                      try {
                        t.field("isLoaded").value = true;
                      } catch (e) {}
                      try {
                        t.field("_isLoaded").value = true;
                      } catch (e) {}
                      try {
                        e.method("set_loadedState").invoke(t);
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
                pulseWeaponSecondary(e);
                if (t) {
                  for (let t = 0; t < (n ? 3 : 2); t++) {
                    try {
                      e.method("HandleTriggerUse").invoke();
                    } catch (e) {}
                  }
                  tryCallNames(e, ["HandlePrimaryUse", "HandlePrimaryButton", "OnPrimaryButtonDown"], 0);
                  tryCallNames(e, ["HandlePrimaryButton"], 1, true);
                  tryCallNames(e, ["HandlePrimaryButton"], 1, false);
                  pulseWeaponSecondary(e);
                }
                try {
                  const t = e.method("get_config").invoke();
                  if (t && !t.isNull?.()) {
                    try {
                      t.field("recoilForceMag").value = 0;
                    } catch (e) {}
                    try {
                      t.field("handRecoilForceMag").value = 0;
                    } catch (e) {}
                    try {
                      t.field("shotSpread").value = 0;
                    } catch (e) {}
                    try {
                      t.field("shotSpreadMin").value = 0;
                    } catch (e) {}
                    try {
                      t.field("shotSpreadMax").value = 0;
                    } catch (e) {}
                    try {
                      t.field("ammoLoaded").value = 255;
                    } catch (e) {}
                    try {
                      t.field("ammo").value = 255;
                    } catch (e) {}
                    try {
                      t.field("hasAmmo").value = true;
                    } catch (e) {}
                    try {
                      t.field("isLoaded").value = true;
                    } catch (e) {}
                    try {
                      t.field("reloadTime").value = 0;
                    } catch (e) {}
                    try {
                      t.field("timeBetweenShots").value = 0;
                    } catch (e) {}
                    try {
                      t.field("useCooldown").value = 0;
                    } catch (e) {}
                    try {
                      t.field("secondaryUseCooldown").value = 0;
                    } catch (e) {}
                  }
                } catch (e) {}
                return true;
              } catch (e) {
                return false;
              }
            }
            function spawnGoopBurstAtTransform(e, t = 18, o = 96, n = 2, i = 2.2, a = 8.8) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                const l = e.method("get_position").invoke();
                const r = readVec3Components(e.method("get_forward").invoke());
                const [s, c] = normalizeXZ(r[0], r[2]);
                for (let e = 0; e < n; e++) {
                  const e = spawnItemAtPos("item_goop", [l.field("x").value + s * 0.14 + (Math.random() * 0.05 - 0.025), l.field("y").value - 0.28 + (Math.random() * 0.02 - 0.01), l.field("z").value + c * 0.14 + (Math.random() * 0.05 - 0.025)], identityQuaternion);
                  if (e && !e.isNull?.()) {
                    spawnedGoopObjects.push({
                      object: e,
                      expireAt: time + i
                    });
                    try {
                      e.method("set_colorHue").invoke(t);
                    } catch (e) {}
                    try {
                      e.method("set_colorSaturation").invoke(o);
                    } catch (e) {}
                    try {
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull?.()) {
                        try {
                          const e = n.method("GetComponent", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull?.()) {
                            try {
                              e.method("set_colorHue").invoke(t);
                            } catch (e) {}
                            try {
                              e.method("set_colorSaturation").invoke(o);
                            } catch (e) {}
                          }
                        } catch (e) {}
                        try {
                          const e = getComponent(n, Renderer);
                          if (e && !e.isNull?.()) {
                            const t = e.method("get_material").invoke();
                            if (t && !t.isNull?.()) {
                              t.method("set_color").invoke([1, 0.95, 0.12, 1]);
                            }
                          }
                        } catch (e) {}
                      }
                    } catch (e) {}
                    trySetObjectVelocity(e, [s * a + (Math.random() * 0.95 - 0.475), Math.random() * 0.35 - 1, c * a + (Math.random() * 0.95 - 0.475)]);
                  }
                }
              } catch (e) {}
            }
            function createSolidPlatform(e, t = [0.35, 0.03, 0.35], o = [1, 0.55, 0.08, 0.9]) {
              const n = createObject(e, identityQuaternion, t, 3, o, null, true);
              try {
                const e = getComponent(n, Collider);
                if (e && !e.isNull()) {
                  e.method("set_enabled").invoke(true);
                  e.method("set_isTrigger").invoke(false);
                }
              } catch (e) {}
              return n;
            }
            function renderMenuText(e, t, o = "", n = [1, 1, 1, 1], i = zeroVector, a = oneVector) {
              if (!TextMesh && !TextMeshPro3D) {
                if (!menuFontWarned) {
                  menuFontWarned = true;
                  console.error("[menu] No TextMesh/TextMeshPro class on this build; skipping menu text");
                }
                return;
              }
              const l = createEmptyObject("MenuText", getTransform(e));
              const r = Math.max(a?.[0] ?? 1, 0.2);
              const s = Math.max((a?.[1] ?? 0.1) / 0.1, 0.6);
              const c = Math.max((o ?? "").trim().length, 1);
              const d = r <= 0.9;
              const u = (d ? 0.034 : 0.022) * s * (d ? Math.max(0.55, Math.min(1, 10.5 / c)) : Math.max(0.55, Math.min(1, 20 / c)));
              if (TextMeshPro3D) {
                const e = addComponent(l, TextMeshPro3D);
                const t = gettmpfontnocrash();
                if (t && !t.isNull()) {
                  try {
                    e.method("set_font").invoke(t);
                  } catch (e) {}
                  try {
                    const e = getComponent(l, Renderer);
                    if (e && !e.isNull()) {
                      e.method("set_enabled").invoke(true);
                      try {
                        e.method("set_sharedMaterial").invoke(t.method("get_material").invoke());
                      } catch (o) {
                        try {
                          e.method("set_material").invoke(t.method("get_material").invoke());
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
                try {
                  e.method("SetText", 1).invoke(Il2Cpp.string(o));
                } catch (t) {
                  try {
                    e.method("set_text").invoke(Il2Cpp.string(o));
                  } catch (e) {}
                }
                try {
                  e.method("set_fontSize").invoke((d ? 6.4 : 4.35) * s);
                } catch (e) {}
                try {
                  e.method("set_color").invoke(n);
                } catch (e) {}
                try {
                  e.method("set_alignment").invoke(514);
                } catch (e) {}
                try {
                  e.method("set_enableWordWrapping").invoke(false);
                } catch (e) {}
                try {
                  e.method("ForceMeshUpdate", 0).invoke();
                } catch (e) {}
              } else {
                const e = getarialnocrash2();
                if (!e || e.isNull()) {
                  if (!menuFontWarned) {
                    menuFontWarned = true;
                    console.error("[menu] Builtin font load failed; skipping menu text to avoid freeze");
                  }
                  return;
                }
                const t = addComponent(l, TextMesh);
                t.method("set_font").invoke(e);
                t.method("set_text").invoke(Il2Cpp.string(o));
                try {
                  t.method("set_fontSize").invoke(d ? 150 : 128);
                } catch (e) {}
                try {
                  t.method("set_characterSize").invoke((d ? 0.041 : 0.031) * s);
                } catch (e) {}
                try {
                  t.method("set_anchor").invoke(4);
                } catch (e) {}
                try {
                  t.method("set_alignment").invoke(1);
                } catch (e) {}
                try {
                  t.method("set_color").invoke(n);
                } catch (e) {}
                try {
                  const t = getComponent(l, Renderer);
                  if (t && !t.isNull()) {
                    t.method("set_enabled").invoke(true);
                    try {
                      t.method("set_sharedMaterial").invoke(e.method("get_material").invoke());
                    } catch (o) {
                      try {
                        t.method("set_material").invoke(e.method("get_material").invoke());
                      } catch (e) {}
                    }
                  }
                } catch (e) {}
              }
              const m = getTransform(l);
              try {
                const e = [(i?.[0] ?? 0) + 0.0125, i?.[1] ?? 0, i?.[2] ?? 0];
                const o = getTransform(t).method("TransformPoint", 1).invoke(e);
                m.method("set_position").invoke(o);
              } catch (e) {
                m.method("set_localPosition").invoke(i);
              }
              try {
                const e = getTransform(t).method("get_rotation").invoke();
                const o = Quaternion.method("op_Multiply").invoke(e, Quaternion.method("Euler").invoke(0, 90, 90));
                m.method("set_rotation").invoke(o);
              } catch (e) {
                m.method("set_localRotation").invoke(Quaternion.method("Euler").invoke(0, 90, 90));
              }
              setMenuTextScale(m, menu, u);
            }
            function renderSyteMenuText(e, t, o, n, i) {
              const a = getarialnocrash2();
              if (!a || a.isNull?.()) {
                return null;
              }
              const l = addComponent(createObject(zeroVector, identityQuaternion, oneVector, 3, [0, 0, 0, 0], getTransform(e)), Text);
              l.method("set_supportRichText").invoke(true);
              l.method("set_text").invoke(Il2Cpp.string(t));
              l.method("set_font").invoke(a);
              l.method("set_fontSize").invoke(1);
              l.method("set_color").invoke(o);
              l.method("set_fontStyle").invoke(1);
              l.method("set_alignment").invoke(4);
              l.method("set_resizeTextForBestFit").invoke(true);
              l.method("set_resizeTextMinSize").invoke(0);
              const r = getComponent(l, RectTransform);
              r.method("set_sizeDelta").invoke(i);
              r.method("set_position").invoke(n);
              r.method("set_rotation").invoke(Quaternion.method("Euler").invoke(180, 90, 90));
              return l;
            }
            function updateMenuFpsText() {
              const e = Math.max(Number(deltaTime) || 0, 0.0001);
              const t = Math.min(999, 1 / e);
              menuFpsSmoothed += (t - menuFpsSmoothed) * Math.min(1, e * 4);
              if (isAlive(menuFpsTextComponent) && !(time < menuFpsNextUpdate)) {
                menuFpsNextUpdate = time + 0.2;
                try {
                  menuFpsTextComponent.method("set_text").invoke(Il2Cpp.string(Math.round(menuFpsSmoothed) + " FPS"));
                } catch (e) {}
              }
            }
            function ensureMenuClickSoundLoaded() {
              if (!isAlive(menuClickAudioClip) && !menuClickAudioRequest) {
                try {
                  menuClickAudioRequest = true;
                  const e = loadWavFile(menuClickSoundPath);
                  if (!e || !isAlive(e.clip)) {
                    throw new Error("WAV parser returned no AudioClip");
                  }
                  menuClickAudioClip = e.clip;
                  const t = UnityEngineAudio.class("UnityEngine.AudioSource");
                  menuClickAudioObject = createEmptyObject("__SolarHPMenuClickSound");
                  try {
                    Object.method("DontDestroyOnLoad", 1).invoke(menuClickAudioObject);
                  } catch (e) {}
                  (menuClickAudioSource = addComponent(menuClickAudioObject, t)).method("set_playOnAwake").invoke(false);
                  menuClickAudioSource.method("set_loop").invoke(false);
                  menuClickAudioSource.method("set_spatialBlend").invoke(0);
                  menuClickAudioSource.method("set_volume").invoke(0.92);
                  menuClickAudioSource.method("set_clip").invoke(menuClickAudioClip);
                  menuClickAudioRequest = null;
                } catch (e) {
                  menuClickAudioRequest = null;
                  menuClickAudioOperation = null;
                }
              }
            }
            function updateMenuClickSoundLoad() {}
            function playMenuClickSound() {
              menuClickPendingPlay = true;
              ensureMenuClickSoundLoaded();
              updateMenuClickSoundLoad();
              try {
                if (!isAlive(menuClickAudioSource) || !isAlive(menuClickAudioClip)) {
                  return;
                }
                menuClickPendingPlay = false;
                try {
                  menuClickAudioSource.method("Stop", 0).invoke();
                } catch (e) {}
                try {
                  menuClickAudioSource.method("set_time").invoke(0);
                } catch (e) {}
                try {
                  menuClickAudioSource.method("PlayOneShot", 2).invoke(menuClickAudioClip, 1);
                  return;
                } catch (e) {}
                menuClickAudioSource.method("Play", 0).invoke();
              } catch (e) {
                console.error("[Menu Click SFX] Playback failed:", e);
              }
            }
            const syteRoundedMeshCache = new Map();
            function getSyteRoundedBoxMesh(e, t, o) {
              const n = Math.max(0.001, Math.min(0.46, o / Math.max(e, 0.001)));
              const i = Math.max(0.001, Math.min(0.46, o / Math.max(t, 0.001)));
              const a = n.toFixed(4) + ":" + i.toFixed(4);
              const l = syteRoundedMeshCache.get(a);
              if (l && !l.isNull?.()) {
                return l;
              }
              const r = [];
              const s = [[0.5 - n, 0.5 - i, 0, Math.PI * 0.5], [-0.5 + n, 0.5 - i, Math.PI * 0.5, Math.PI], [-0.5 + n, -0.5 + i, Math.PI, Math.PI * 1.5], [0.5 - n, -0.5 + i, Math.PI * 1.5, Math.PI * 2]];
              for (const [e, t, o, a] of s) {
                for (let l = 0; l <= 24; l++) {
                  const s = o + (a - o) * (l / 24);
                  r.push([e + Math.cos(s) * n, t + Math.sin(s) * i]);
                }
              }
              const c = [];
              for (const [e, t] of r) {
                c.push([0.5, e, t]);
              }
              for (const [e, t] of r) {
                c.push([-0.5, e, t]);
              }
              const d = r.length;
              const u = c.length;
              c.push([0.5, 0, 0]);
              const m = c.length;
              c.push([-0.5, 0, 0]);
              const h = [];
              for (let e = 0; e < d; e++) {
                const t = (e + 1) % d;
                h.push(u, e, t);
                h.push(m, t + d, e + d);
                h.push(e, e + d, t + d);
                h.push(e, t + d, t);
              }
              const g = Il2Cpp.array(Vector3, c.length);
              for (let e = 0; e < c.length; e++) {
                g.set(e, Il2Cpp.fromFridaValue(c[e], Vector3.type));
              }
              const f = Il2Cpp.array(Il2Cpp.corlib.class("System.Int32"), h);
              const p = Mesh.alloc();
              p.method(".ctor", 0).invoke();
              p.method("set_vertices").invoke(g);
              p.method("set_triangles").invoke(f);
              p.method("RecalculateBounds", 0).invoke();
              p.method("RecalculateNormals", 0).invoke();
              syteRoundedMeshCache.set(a, p);
              return p;
            }
            function createSyteOutlinedRoundedObject(e, t, o, n, i, a = false, l = 0.025) {
              const r = (e, t, o, n, a) => {
                const l = createObject(e, identityQuaternion, t, 3, o, i, n);
                if (roundedMesh) {
                  try {
                    getComponent(l, MeshFilter).method("set_sharedMesh").invoke(getSyteRoundedBoxMesh(t[1], t[2], a));
                  } catch (e) {
                    console.error("[menu] Rounded mesh fallback:", e);
                  }
                }
                if (!n) {
                  Destroy(getComponent(l, Collider));
                }
                return l;
              };
              const s = Math.min(t[1], t[2]);
              const c = Math.min(s * cornerRoundness, 0.075);
              const d = t[1] > 0.68 && t[2] > 0.5;
              r([e[0] - 0.006, e[1] - (d ? 0.009 : 0.006), e[2] - (d ? 0.011 : 0.007)], [t[0], t[1] + 0.014, t[2] + 0.014], [0.005, 0.008, 0.014, 0.72], false, c + 0.009);
              if (outlinesEnabled && l > 0) {
                r([e[0] - 0.001, e[1], e[2]], [t[0], t[1] + l * 2, t[2] + l * 2], n, false, c + l);
              }
              const u = [Math.min(1, o[0] + 0.07), Math.min(1, o[1] + 0.07), Math.min(1, o[2] + 0.09), Math.min(1, o[3])];
              r([e[0] - 0.0035, e[1], e[2]], [t[0], t[1] + 0.004, t[2] + 0.004], u, false, c + 0.003);
              return r(e, t, o, a, c);
            }
            function updateButtonColor(e, t) {
              const o = getComponent(e, Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer"));
              if (!o) {
                return;
              }
              o.method("get_material").invoke().method("set_color").invoke(t.enabled ? buttonPressedColor : buttonColor);
            }
            function menuSmoothStep(e) {
              const t = Math.max(0, Math.min(1, e));
              return t * t * (3 - t * 2);
            }
            function startMenuTabFade() {
              menuTabFadeAmount = 0;
            }
            function updateMenuAnimations(e) {
              const t = Math.max(Number(deltaTime) || 0, 0.0001);
              menuOpenAmount = e ? Math.min(1, menuOpenAmount + t / menuZoomInDuration) : Math.max(0, menuOpenAmount - t / menuZoomOutDuration);
              menuTabFadeAmount = Math.min(1, menuTabFadeAmount + t / menuTabFadeDuration);
              const o = menuSmoothStep(menuOpenAmount);
              const n = 1 - Math.pow(1 - menuTabFadeAmount, 3);
              const i = (0.76 + o * 0.24) * (0.97 + n * 0.03);
              try {
                if (menu != null && !menu.isNull?.()) {
                  getTransform(menu).method("set_localScale").invoke([menuRestingScale[0] * i, menuRestingScale[1] * i, menuRestingScale[2] * i]);
                }
              } catch (e) {}
              try {
                if (menuCanvasGroup != null && !menuCanvasGroup.isNull?.()) {
                  menuCanvasGroup.method("set_alpha").invoke(n);
                }
              } catch (e) {}
            }
            function reloadMenu() {
              try {
                if (menu != null && !menu.isNull?.()) {
                  Object.method("Destroy", 1).invoke(menu);
                  menu = null;
                  menuCanvasGroup = null;
                }
              } catch (e) {}
              menuSnapNextFrame = true;
              try {
                if (menuVisible || righthand && rightSecondary || !righthand && leftSecondary || _pcMode && _pcMenuOpen) {
                  renderMenu();
                }
              } catch (e) {}
            }
            function renderSearchKeyboard(e) {
              const t = buttons[44] ?? [];
              const o = t.find(e => e.buttonText.startsWith("Search: ")) ?? null;
              const n = t.filter(e => !e.buttonText.startsWith("Search: "));
              const i = _pcMode && _pcMenuOpen && n.length > 0 ? n[_pcMenuSelector % n.length] : null;
              const a = createSyteOutlinedRoundedObject([0.105, 0, 0.074], [0.09, 0.61, 0.052], buttonColor, buttonOutlineColor, getTransform(menu), true, rowOutlineThickness);
              try {
                Destroy(getComponent(a, Collider));
              } catch (e) {}
              renderSyteMenuText(e, o?.buttonText ?? "Search: _", textColor, [0.11, 0, 0.074], [0.72, 0.043]);
              const l = (o, n, a, l, r) => {
                c = o;
                const s = t.find(e => e.buttonText === c) ?? null;
                var c;
                if (!s) {
                  return;
                }
                const d = createSyteOutlinedRoundedObject([0.105, a, l], [0.09, r, 0.052], buttonColor, buttonOutlineColor, getTransform(menu), true, rowOutlineThickness);
                d.method("set_name").invoke(Il2Cpp.string("@" + s.buttonText));
                addComponent(d, GorillaReportButton);
                getComponent(d, Collider).method("set_isTrigger").invoke(true);
                renderSyteMenuText(e, s === i ? ">" + n : n, textColor, [0.11, a, l], [Math.max(0.1, r * 1.25), 0.038]);
                updateButtonColor(d, s);
              };
              const r = (e, t, o, n) => {
                let i = (e.length * o + Math.max(0, e.length - 1) * n) / 2;
                for (const a of e) {
                  l(a, a, (i - o / 2) * 0.3, t, o);
                  i -= o + n;
                }
              };
              r(["1", "2", "3", "4", "5", "6", "7", "8", "9", "0"], 0.027, 0.052, 0.007);
              r(["Q", "W", "E", "R", "T", "Y", "U", "I", "O", "P"], -0.014, 0.052, 0.007);
              r(["A", "S", "D", "F", "G", "H", "J", "K", "L"], -0.055, 0.052, 0.007);
              r(["Z", "X", "C", "V", "B", "N", "M"], -0.096, 0.052, 0.007);
              const s = [{
                text: "Exit Search",
                label: "Exit",
                width: 0.1
              }, {
                text: "Clear",
                label: "Clear",
                width: 0.1
              }, {
                text: "Space",
                label: "Space",
                width: 0.16
              }, {
                text: "Backspace",
                label: "Back",
                width: 0.12
              }, {
                text: "Search",
                label: "Search",
                width: 0.12
              }];
              let c = (s.reduce((e, t) => e + t.width, 0) + (s.length - 1) * 0.008) / 2;
              for (const e of s) {
                const t = (c - e.width / 2) * 0.3;
                l(e.text, e.label, t, -0.143, e.width);
                c -= e.width + 0.008;
              }
            }
            function renderRoomCodeKeyboard(e) {
              const t = buttons[80] ?? [];
              const o = t.find(e => e.buttonText.startsWith("Code: ")) ?? null;
              const n = t.filter(e => !e.buttonText.startsWith("Code: "));
              const i = _pcMode && _pcMenuOpen && n.length > 0 ? n[_pcMenuSelector % n.length] : null;
              const a = createSyteOutlinedRoundedObject([0.105, 0, 0.074], [0.09, 0.61, 0.052], buttonColor, buttonOutlineColor, getTransform(menu), true, rowOutlineThickness);
              try {
                Destroy(getComponent(a, Collider));
              } catch (e) {}
              renderSyteMenuText(e, o?.buttonText ?? "Code: _", textColor, [0.11, 0, 0.074], [0.72, 0.043]);
              const l = (o, n, a, l, r) => {
                c = o;
                const s = t.find(e => e.buttonText === c) ?? null;
                var c;
                if (!s) {
                  return;
                }
                const d = createSyteOutlinedRoundedObject([0.105, a, l], [0.09, r, 0.052], buttonColor, buttonOutlineColor, getTransform(menu), true, rowOutlineThickness);
                d.method("set_name").invoke(Il2Cpp.string("@" + s.buttonText));
                addComponent(d, GorillaReportButton);
                getComponent(d, Collider).method("set_isTrigger").invoke(true);
                renderSyteMenuText(e, s === i ? ">" + n : n, textColor, [0.11, a, l], [Math.max(0.1, r * 1.25), 0.038]);
                updateButtonColor(d, s);
              };
              const r = (e, t, o, n) => {
                let i = (e.length * o + Math.max(0, e.length - 1) * n) / 2;
                for (const a of e) {
                  l("Code " + a, a, (i - o / 2) * 0.3, t, o);
                  i -= o + n;
                }
              };
              r(["1", "2", "3", "4", "5", "6", "7", "8", "9", "0"], 0.027, 0.052, 0.007);
              r(["Q", "W", "E", "R", "T", "Y", "U", "I", "O", "P"], -0.014, 0.052, 0.007);
              r(["A", "S", "D", "F", "G", "H", "J", "K", "L"], -0.055, 0.052, 0.007);
              r(["Z", "X", "C", "V", "B", "N", "M"], -0.096, 0.052, 0.007);
              const s = [{
                text: "Exit Room Codes",
                label: "Exit",
                width: 0.13
              }, {
                text: "Clear Code",
                label: "Clear",
                width: 0.13
              }, {
                text: "Backspace Code",
                label: "Back",
                width: 0.15
              }, {
                text: "Join Code",
                label: "Join",
                width: 0.15
              }];
              let c = (s.reduce((e, t) => e + t.width, 0) + (s.length - 1) * 0.008) / 2;
              for (const e of s) {
                const t = (c - e.width / 2) * 0.3;
                l(e.text, e.label, t, -0.143, e.width);
                c -= e.width + 0.008;
              }
            }
            function renderOreTextKeyboard(e) {
              const t = buttons[103] ?? [];
              const o = t.find(e => e.buttonText.startsWith("Text: ")) ?? null;
              const n = t.filter(e => !e.buttonText.startsWith("Text: "));
              const i = _pcMode && _pcMenuOpen && n.length > 0 ? n[_pcMenuSelector % n.length] : null;
              const a = createSyteOutlinedRoundedObject([0.105, 0, 0.074], [0.09, 0.61, 0.052], buttonColor, buttonOutlineColor, getTransform(menu), true, rowOutlineThickness);
              try {
                Destroy(getComponent(a, Collider));
              } catch (e) {}
              renderSyteMenuText(e, o?.buttonText ?? "Text: _", textColor, [0.11, 0, 0.074], [0.72, 0.043]);
              const l = (o, n, a, l, r) => {
                c = o;
                const s = t.find(e => e.buttonText === c) ?? null;
                var c;
                if (!s) {
                  return;
                }
                const d = createSyteOutlinedRoundedObject([0.105, a, l], [0.09, r, 0.052], buttonColor, buttonOutlineColor, getTransform(menu), true, rowOutlineThickness);
                d.method("set_name").invoke(Il2Cpp.string("@" + s.buttonText));
                addComponent(d, GorillaReportButton);
                getComponent(d, Collider).method("set_isTrigger").invoke(true);
                renderSyteMenuText(e, s === i ? ">" + n : n, textColor, [0.11, a, l], [Math.max(0.1, r * 1.25), 0.038]);
                updateButtonColor(d, s);
              };
              const r = (e, t, o, n) => {
                let i = (e.length * o + Math.max(0, e.length - 1) * n) / 2;
                for (const a of e) {
                  l(a, a, (i - o / 2) * 0.3, t, o);
                  i -= o + n;
                }
              };
              r(["1", "2", "3", "4", "5", "6", "7", "8", "9", "0"], 0.027, 0.052, 0.007);
              r(["Q", "W", "E", "R", "T", "Y", "U", "I", "O", "P"], -0.014, 0.052, 0.007);
              r(["A", "S", "D", "F", "G", "H", "J", "K", "L"], -0.055, 0.052, 0.007);
              r(["Z", "X", "C", "V", "B", "N", "M"], -0.096, 0.052, 0.007);
              const s = [{
                text: "Exit Text Mods",
                label: "Exit",
                width: 0.065
              }, {
                text: "Clear Text",
                label: "Clear",
                width: 0.07
              }, {
                text: "Space",
                label: "Space",
                width: 0.075
              }, {
                text: "Backspace",
                label: "Back",
                width: 0.07
              }, {
                text: "Text Options",
                label: "Settings",
                width: 0.085
              }, {
                text: "Ore Text Gun",
                label: "Spawn",
                width: 0.075
              }];
              let c = (s.reduce((e, t) => e + t.width, 0) + (s.length - 1) * 0.01) / 2;
              for (const e of s) {
                const t = (c - e.width / 2) * 0.44;
                l(e.text, e.label, t, -0.143, e.width);
                c -= e.width + 0.01;
              }
            }
            let gunLocked = false;
            let lockTarget = null;
            let GunPointer = null;
            let GunLine = null;
            function renderMenu() {
              applyMenuStructurePatches();
              if (currentCategory === 78 || currentCategory === 79) {
                buildUsersMenuPages();
              }
              if (currentCategory === 80) {
                buildRoomCodeKeyboard();
              }
              if (currentCategory === 103) {
                loadOreTextSettings();
                const e = ["1", "2", "3", "4", "5", "6", "7", "8", "9", "0", "Q", "W", "E", "R", "T", "Y", "U", "I", "O", "P", "A", "S", "D", "F", "G", "H", "J", "K", "L", "Z", "X", "C", "V", "B", "N", "M"];
                buttons[103] = [new ButtonInfo({
                  buttonText: "Text: " + (oreTextQuery || "_"),
                  method: () => {},
                  isTogglable: false
                }), ...e.map(e => new ButtonInfo({
                  buttonText: e,
                  method: () => {
                    if (oreTextQuery.length < oreTextMaxLength) {
                      oreTextQuery += e;
                    }
                    sendNotification("Ore text: " + oreTextQuery + "_", false, 99);
                    reloadMenu();
                  },
                  isTogglable: false
                })), new ButtonInfo({
                  buttonText: "Exit Text Mods",
                  method: () => {
                    currentCategory = 11;
                    currentPage = 0;
                  },
                  isTogglable: false
                }), new ButtonInfo({
                  buttonText: "Clear Text",
                  method: () => {
                    oreTextQuery = "";
                    clearSpawnedTextHellOre();
                    reloadMenu();
                  },
                  isTogglable: false,
                  toolTip: "Clears the typed text, pending spawns, and all spawned text ores."
                }), new ButtonInfo({
                  buttonText: "Space",
                  method: () => {
                    if (oreTextQuery.length < oreTextMaxLength) {
                      oreTextQuery += " ";
                    }
                    reloadMenu();
                  },
                  isTogglable: false
                }), new ButtonInfo({
                  buttonText: "Backspace",
                  method: () => {
                    sendNotification("Ore text: " + ((oreTextQuery = oreTextQuery.slice(0, -1)) || "_"), false, 99);
                    reloadMenu();
                  },
                  isTogglable: false
                }), new ButtonInfo({
                  buttonText: "New Line",
                  method: () => {
                    if (oreTextQuery.length < oreTextMaxLength && !oreTextQuery.endsWith("|")) {
                      oreTextQuery += "|";
                    }
                    reloadMenu();
                  },
                  isTogglable: false,
                  toolTip: "Starts another line. Up to four lines are supported."
                }), new ButtonInfo({
                  buttonText: "Text Options",
                  method: () => {
                    currentCategory = 104;
                    currentPage = 0;
                  },
                  isTogglable: false,
                  toolTip: "Opens ore text size, thickness, spacing, and layout options."
                }), new ButtonInfo({
                  buttonText: "Ore Text Gun",
                  method: () => {
                    updateOreTextGun();
                  },
                  enableMethod: () => {
                    oreTextGunEnabled = true;
                    oreTextTriggerLatched = false;
                    const e = (buttons[103] || []).find(e => e.buttonText === "Ore Text Gun");
                    if (e) {
                      e.enabled = true;
                    }
                  },
                  disableMethod: () => {
                    oreTextGunEnabled = false;
                    oreTextTriggerLatched = false;
                  },
                  isTogglable: true,
                  enabled: oreTextGunEnabled,
                  toolTip: "Hold right grip to aim and pull trigger to spawn frozen Hell Ore text."
                })];
                try {
                  rebuildButtonMap();
                } catch (e) {}
              }
              if (currentCategory === 28) {
                const e = [new ButtonInfo({
                  buttonText: "Exit Enabled Mods",
                  method: () => {
                    currentCategory = 2;
                    currentPage = 0;
                  },
                  isTogglable: false,
                  toolTip: "Returns to the main menu."
                })];
                for (let t = 0; t < buttons.length; t++) {
                  if (t !== 28 && buttons[t]) {
                    for (const o of buttons[t]) {
                      if (o.isTogglable && o.enabled) {
                        const t = o;
                        e.push(new ButtonInfo({
                          buttonText: t.buttonText,
                          method: () => {
                            t.enabled = false;
                            try {
                              t.disableMethod?.();
                            } catch (e) {}
                          },
                          isTogglable: false,
                          toolTip: "Disable: " + t.buttonText
                        }));
                      }
                    }
                  }
                }
                if (e.length === 1) {
                  e.push(new ButtonInfo({
                    buttonText: "None enabled",
                    method: () => {},
                    isTogglable: false,
                    toolTip: "No mods are currently enabled."
                  }));
                }
                buttons[28] = e;
              }
              buttons[44] = [new ButtonInfo({
                buttonText: "Search: " + (searchQuery || "_"),
                method: () => {},
                isTogglable: false,
                toolTip: "Current mod search text."
              }), ...["1", "2", "3", "4", "5", "6", "7", "8", "9", "0", "Q", "W", "E", "R", "T", "Y", "U", "I", "O", "P", "A", "S", "D", "F", "G", "H", "J", "K", "L", "Z", "X", "C", "V", "B", "N", "M"].map(e => new ButtonInfo({
                buttonText: e,
                method: () => {
                  sendNotification("Search: " + (searchQuery += e.toLowerCase()) + "_", false, 99);
                },
                isTogglable: false
              })), new ButtonInfo({
                buttonText: "Exit Search",
                method: () => {
                  currentCategory = 11;
                  currentPage = 0;
                  searchQuery = "";
                },
                isTogglable: false
              }), new ButtonInfo({
                buttonText: "Clear",
                method: () => {
                  searchQuery = "";
                  sendNotification("Search: _", false, 99);
                },
                isTogglable: false
              }), new ButtonInfo({
                buttonText: "Space",
                method: () => {
                  sendNotification("Search: " + (searchQuery += " ") + "_", false, 99);
                },
                isTogglable: false
              }), new ButtonInfo({
                buttonText: "Backspace",
                method: () => {
                  if (searchQuery.length > 0) {
                    searchQuery = searchQuery.slice(0, -1);
                  }
                  sendNotification("Search: " + (searchQuery || "_"), false, 99);
                },
                isTogglable: false
              }), new ButtonInfo({
                buttonText: "Search",
                method: () => {
                  currentCategory = 45;
                  currentPage = 0;
                },
                isTogglable: false
              })];
              if (currentCategory === 45) {
                const e = [new ButtonInfo({
                  buttonText: "Back To Search",
                  method: () => {
                    currentCategory = 44;
                    currentPage = 0;
                    sendNotification("Search: " + searchQuery + "_", false, 99);
                  },
                  isTogglable: false,
                  toolTip: "Returns to search keyboard."
                })];
                if (searchQuery.trim() !== "") {
                  const t = searchQuery.trim().toLowerCase();
                  const o = new Set(["Search", "Exit Search", "Backspace", "Clear", "Space", "Back To Search", "None enabled", "GlobalReturn", "PreviousPage", "NextPage", "Exit Enabled", "Enabled", "Search", ..."ABCDEFGHIJKLMNOPQRSTUVWXYZ"]);
                  const n = new Set();
                  for (let i = 0; i < buttons.length; i++) {
                    if (i !== 44 && i !== 45 && i !== 80 && buttons[i]) {
                      for (const a of buttons[i]) {
                        if (!o.has(a.buttonText) && a.buttonText.toLowerCase().includes(t)) {
                          if (!n.has(a.buttonText)) {
                            n.add(a.buttonText);
                            e.push(a);
                          }
                        }
                      }
                    }
                  }
                }
                if (e.length === 1) {
                  e.push(new ButtonInfo({
                    buttonText: "No results found",
                    method: () => {},
                    isTogglable: false
                  }));
                }
                buttons[45] = e;
              }
              if (currentCategory === 44 || currentCategory === 45) {
                try {
                  rebuildButtonMap();
                } catch (e) {}
              }
              if (!buttons[currentCategory]) {
                currentCategory = 0;
                currentPage = 0;
              }
              const e = currentCategory === 44;
              const t = currentCategory === 80;
              const o = e || t || currentCategory === 103;
              const n = buttons[currentCategory] || buttons[0];
              const i = o ? Math.max(n.length, 1) : getButtonsPerPage();
              const a = Math.max(Math.ceil(n.length / i) - 1, 0);
              if (o) {
                currentPage = 0;
              }
              if (currentPage > a) {
                currentPage = a;
              }
              const l = o ? [] : n.slice(currentPage * i).slice(0, i);
              const r = -0.045;
              menu = createObject(zeroVector, identityQuaternion, [0.1, 0.3, 0.3825], 3, [0, 0, 0, 0]);
              menuSnapNextFrame = true;
              try {
                menu.method("set_name").invoke(Il2Cpp.string("MenuRoot"));
              } catch (e) {}
              Destroy(getComponent(menu, BoxCollider));
              const s = createSyteOutlinedRoundedObject([0.1, 0, -0.03], [0.1, 0.72, 0.76], bgColor, menuOutlineColor, getTransform(menu), false, menuOutlineThickness);
              try {
                s.method("set_name").invoke(Il2Cpp.string("MenuBackground"));
              } catch (e) {}
              const c = createObject(zeroVector, identityQuaternion, oneVector, 3, [0, 0, 0, 0], getTransform(menu));
              const d = addComponent(c, Canvas);
              menuCanvasGroup = addComponent(c, CanvasGroup);
              try {
                const e = 1 - Math.pow(1 - menuTabFadeAmount, 3);
                menuCanvasGroup.method("set_alpha").invoke(e);
              } catch (e) {}
              Destroy(getComponent(c, BoxCollider));
              const u = addComponent(c, CanvasScaler);
              addComponent(c, GraphicRaycaster);
              d.method("set_renderMode").invoke(2);
              u.method("set_dynamicPixelsPerUnit").invoke(1000);
              renderSyteMenuText(c, menuName + " | (" + (currentPage + 1) + ")", textColor, [0.11, 0, 0.103], [titleWidth, titleHeight]);
              const m = createSyteOutlinedRoundedObject([0.1, 0.052, 0.142], [0.09, 0.295, 0.065], buttonColor, pageOutlineColor, getTransform(menu), true, controlOutlineThickness);
              m.method("set_name").invoke(Il2Cpp.string("@Leave"));
              addComponent(m, GorillaReportButton);
              getComponent(m, Collider).method("set_isTrigger").invoke(true);
              const h = _pcMode && _pcMenuOpen && !o && _pcMenuSelector === l.length;
              renderSyteMenuText(c, h ? "> Leave" : "Leave", textColor, [0.11, 0.052, 0.142], [0.28, 0.065]);
              const g = createSyteOutlinedRoundedObject([0.1, -0.052, 0.142], [0.09, 0.295, 0.065], buttonColor, pageOutlineColor, getTransform(menu), true, controlOutlineThickness);
              g.method("set_name").invoke(Il2Cpp.string("@Home"));
              addComponent(g, GorillaReportButton);
              getComponent(g, Collider).method("set_isTrigger").invoke(true);
              const f = _pcMode && _pcMenuOpen && !o && _pcMenuSelector === l.length + 1;
              renderSyteMenuText(c, f ? "> Home" : "Home", textColor, [0.11, -0.052, 0.142], [0.28, 0.065]);
              if (o) {
                if (e) {
                  renderSearchKeyboard(c);
                } else if (t) {
                  renderRoomCodeKeyboard(c);
                } else {
                  renderOreTextKeyboard(c);
                }
              } else {
                const e = createSyteOutlinedRoundedObject([0.1, 0, -0.203], [0.09, disconnectButtonWidth, 0.065], buttonColor, pageOutlineColor, getTransform(menu), true, controlOutlineThickness);
                e.method("set_name").invoke(Il2Cpp.string("@OpenSearch"));
                addComponent(e, GorillaReportButton);
                getComponent(e, Collider).method("set_isTrigger").invoke(true);
                const t = _pcMode && _pcMenuOpen && _pcMenuSelector === l.length + 2;
                renderSyteMenuText(c, t ? "> Search" : "Search", textColor, [0.11, 0, -0.203], [0.6, 0.065]);
                const o = createSyteOutlinedRoundedObject([0.1, 0.14, r], [0.09, pageButtonWidth, 0.64], buttonColor, pageOutlineColor, getTransform(menu), true, controlOutlineThickness);
                o.method("set_name").invoke(Il2Cpp.string("@PreviousPage"));
                addComponent(o, GorillaReportButton);
                getComponent(o, Collider).method("set_isTrigger").invoke(true);
                renderSyteMenuText(c, "<", textColor, [0.11, 0.14, r], [0.25, 0.075]);
                const n = createSyteOutlinedRoundedObject([0.1, -0.14, r], [0.09, pageButtonWidth, 0.64], buttonColor, pageOutlineColor, getTransform(menu), true, controlOutlineThickness);
                n.method("set_name").invoke(Il2Cpp.string("@NextPage"));
                addComponent(n, GorillaReportButton);
                getComponent(n, Collider).method("set_isTrigger").invoke(true);
                renderSyteMenuText(c, ">", textColor, [0.11, -0.14, r], [0.25, 0.075]);
                let i = 0;
                l.forEach(e => {
                  const t = 0.075 - i * rowButtonSpacing;
                  const o = createSyteOutlinedRoundedObject([0.105, 0, t], [0.09, rowButtonWidth, rowButtonHeight], buttonColor, buttonOutlineColor, getTransform(menu), true, rowOutlineThickness);
                  o.method("set_name").invoke(Il2Cpp.string("@" + e.buttonText));
                  addComponent(o, GorillaReportButton);
                  getComponent(o, Collider).method("set_isTrigger").invoke(true);
                  let n = e.getLabel ? e.getLabel() : e.buttonText;
                  if (_pcMode && _pcMenuOpen && i === _pcMenuSelector) {
                    n = "> " + n;
                  }
                  renderSyteMenuText(c, n, textColor, [0.11, 0, t], [0.8, 0.048]);
                  updateButtonColor(o, e);
                  i++;
                });
                menuFpsTextComponent = renderSyteMenuText(c, Math.round(menuFpsSmoothed) + " FPS", [0.15, 1, 0.15, 1], [0.112, -0.09, -0.169], [0.12, 0.03]);
              }
              getTransform(menu).method("set_localScale").invoke(Vector3.method("op_Multiply").invoke(Vector3.method("op_Multiply").invoke(getTransform(menu).method("get_localScale").invoke(), GTPlayer.field("<playerScale>k__BackingField").value), menuscale));
              try {
                menuRestingScale = readVec3Components(getTransform(menu).method("get_localScale").invoke());
              } catch (e) {}
              recenterMenu();
            }
            function renderReference() {
              reference = createObject(zeroVector, identityQuaternion, [0.01, 0.01, 0.01], 0, bgColor, righthand ? leftHandTransform : rightHandTransform);
              referenceCollider = getComponent(reference, Collider);
              getTransform(reference).method("set_localPosition").invoke([0.01, -0.117, 0.05]);
              reference.method("set_layer").invoke(2);
              addComponent(reference, Rigidbody).method("set_isKinematic").invoke(true);
            }
            let physicsRaycastAllVec4 = null;
            let physicsRaycastOutVec4 = null;
            let physicsRaycastOutVec5 = null;
            function renderGun(e = null) {
              let t = rightHandTransform.method("get_position").invoke();
              const o = getGunAimDirection();
              if (_pcMode) {
                try {
                  const e = getTransform(headCollider).method("get_position").invoke();
                  t = Vector3.method("op_Addition").invoke(e, Vector3.method("op_Multiply").invoke(o, 0.14));
                } catch (e) {}
              }
              const n = Vector3.method("op_Addition").invoke(t, Vector3.method("op_Multiply").invoke(o, 0.08));
              const i = e ?? -1;
              let a;
              let l = null;
              try {
                physicsRaycastAllVec4 ||= Physics.method("RaycastAll").overload("UnityEngine.Vector3", "UnityEngine.Vector3", "System.Single", "System.Int32");
                const e = physicsRaycastAllVec4.invoke(n, o, 512, i);
                if (e && !e.isNull() && e.length > 0) {
                  let o = Number.POSITIVE_INFINITY;
                  for (let n = 0; n < e.length; n++) {
                    try {
                      const i = e.get(n);
                      if (!i || i.isNull?.()) {
                        continue;
                      }
                      const a = i.method("get_collider").invoke();
                      if (!a || a.isNull()) {
                        continue;
                      }
                      const r = i.method("get_point").invoke();
                      const s = Vector3.method("Distance").invoke(r, t);
                      if (s < 0.08 || s >= o) {
                        continue;
                      }
                      o = s;
                      l = i;
                    } catch (e) {}
                  }
                }
              } catch (e) {}
              if (!l) {
                try {
                  const a = Il2Cpp.alloc(128);
                  physicsRaycastOutVec4 ||= Physics.method("Raycast").overload("UnityEngine.Vector3", "UnityEngine.Vector3", "UnityEngine.RaycastHit&", "System.Single");
                  physicsRaycastOutVec5 ||= Physics.method("Raycast").overload("UnityEngine.Vector3", "UnityEngine.Vector3", "UnityEngine.RaycastHit&", "System.Single", "System.Int32");
                  if (e == null ? physicsRaycastOutVec4.invoke(n, o, a, 512) : physicsRaycastOutVec5.invoke(n, o, a, 512, i)) {
                    const e = Il2Cpp.reference(a);
                    const o = e.method("get_point").invoke();
                    if (Vector3.method("Distance").invoke(o, t) >= 0.08) {
                      l = e;
                    }
                  }
                } catch (e) {}
              }
              if (l) {
                try {
                  l.method("get_point").invoke();
                } catch (e) {}
              }
              if (gunLocked) {
                a = getTransform(lockTarget).method("get_position").invoke();
              } else if (l && !l.isNull?.()) {
                a = l.method("get_point").invoke();
              } else {
                const e = Vector3.method("op_Multiply").invoke(o, 512);
                a = Vector3.method("op_Addition").invoke(t, e);
              }
              if (Vector3.method("op_Equality").invoke(a, zeroVector)) {
                const e = Vector3.method("op_Multiply").invoke(o, 512);
                a = Vector3.method("op_Addition").invoke(t, e);
              }
              if (GunPointer == null || GunPointer.isNull?.()) {
                GunPointer = createObject(a, identityQuaternion, [0.1, 0.1, 0.1], 0, [1, 1, 1, 1]);
              }
              GunPointer.method("SetActive").invoke(true);
              getTransform(GunPointer).method("set_position").invoke(a);
              const r = getComponent(GunPointer, Renderer).method("get_material").invoke();
              r.method("set_shader").invoke(TextShader);
              const s = gunLocked || rightTrigger ? [0.72, 0.92, 1, 1] : buttonColor;
              r.method("set_color").invoke(s);
              const c = getComponent(GunPointer, Collider);
              if (c != null) {
                Destroy(c);
              }
              if (GunLine == null || GunLine.isNull?.()) {
                const e = createObject(zeroVector, identityQuaternion, oneVector, 0, [0, 0, 0, 0]);
                GunLine = addComponent(e, LineRenderer);
              } else {
                GunLine.method("get_gameObject").invoke().method("SetActive").invoke(true);
              }
              GunLine.method("get_material").invoke().method("set_shader").invoke(TextShader);
              const d = rightTrigger || gunLocked;
              GunLine.method("set_startColor").invoke(d ? [0.88, 0.97, 1, 1] : bgColor);
              GunLine.method("set_endColor").invoke(d ? [0.08, 0.55, 1, 1] : bgColor);
              const u = d ? 0.038 : 0.022;
              GunLine.method("set_startWidth").invoke(u);
              GunLine.method("set_endWidth").invoke(d ? 0.014 : u);
              GunLine.method("set_positionCount").invoke(2);
              GunLine.method("set_useWorldSpace").invoke(true);
              GunLine.method("SetPosition").invoke(0, t);
              GunLine.method("SetPosition").invoke(1, a);
              if (rightTrigger || gunLocked) {
                const e = 30;
                GunLine.method("set_positionCount").invoke(e);
                GunLine.method("SetPosition").invoke(0, t);
                const n = readVec3Components(o);
                const i = Math.sqrt(n[0] * n[0] + n[1] * n[1] + n[2] * n[2]) || 1;
                const l = [n[0] / i, n[1] / i, n[2] / i];
                let r = [0, 1, 0];
                if (Math.abs(l[1]) > 0.92) {
                  r = [1, 0, 0];
                }
                let s = [r[1] * l[2] - r[2] * l[1], r[2] * l[0] - r[0] * l[2], r[0] * l[1] - r[1] * l[0]];
                let c = Math.sqrt(s[0] * s[0] + s[1] * s[1] + s[2] * s[2]) || 1;
                s = [s[0] / c, s[1] / c, s[2] / c];
                let d = [l[1] * s[2] - l[2] * s[1], l[2] * s[0] - l[0] * s[2], l[0] * s[1] - l[1] * s[0]];
                let u = Math.sqrt(d[0] * d[0] + d[1] * d[1] + d[2] * d[2]) || 1;
                d = [d[0] / u, d[1] / u, d[2] / u];
                for (let o = 1; o < e - 1; o++) {
                  const n = o / (e - 1);
                  const i = Vector3.method("Lerp").invoke(t, a, n);
                  const l = Math.floor(time * 30);
                  const r = Math.sin(o * 12.9898 + l * 78.233 + 37.719) * 43758.5453;
                  const c = Math.sin(o * 12.9898 + l * 78.233 + 75.438) * 43758.5453;
                  const u = ((r - Math.floor(r)) * 2 - 1) * 0.075 * Math.sin(n * Math.PI);
                  const m = ((c - Math.floor(c)) * 2 - 1) * 0.075 * Math.sin(n * Math.PI);
                  const h = [s[0] * u + d[0] * m, s[1] * u + d[1] * m, s[2] * u + d[2] * m];
                  const g = Vector3.method("op_Addition").invoke(i, h);
                  GunLine.method("SetPosition").invoke(o, g);
                }
                GunLine.method("SetPosition").invoke(e - 1, a);
              }
              return {
                ray: l ?? NullObject,
                gunPointer: GunPointer,
                endPosition: a
              };
            }
            function getAllNetPlayersList(e = true) {
              const t = [];
              const o = new Set();
              const n = n => {
                try {
                  if (!n || n.isNull?.()) {
                    return;
                  }
                  if (!e && playerIsLocal(n)) {
                    return;
                  }
                  const i = normalizeSceneObjectHandle(n) || getPlayerIdentityInfo(n).key;
                  if (!i || o.has(i)) {
                    return;
                  }
                  o.add(i);
                  t.push(n);
                } catch (e) {}
              };
              try {
                const e = NetPlayer.field("playerIDToNetPlayer").value;
                if (e && !e.isNull?.()) {
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  while (t.method("MoveNext").invoke()) {
                    n(t.method("get_Current").invoke());
                  }
                }
              } catch (e) {}
              try {
                const e = Object.method("FindObjectsOfType").inflate(NetPlayer).invoke();
                if (e && !e.isNull?.()) {
                  for (let t = 0; t < e.length; t++) {
                    n(e.get(t));
                  }
                }
              } catch (e) {}
              if (t.length === 0) {
                try {
                  Il2Cpp.gc.choose(NetPlayer).forEach(n);
                } catch (e) {}
              }
              return t;
            }
            function getUsersDisplayName(e, t = 0) {
              const o = [];
              try {
                o.push(e.method("get_displayName").invoke());
              } catch (e) {}
              try {
                o.push(e.field("_displayName").value);
              } catch (e) {}
              try {
                o.push(e.method("get_playerName").invoke());
              } catch (e) {}
              try {
                o.push(e.field("_playerName").value);
              } catch (e) {}
              try {
                const t = e.method("get_gameObject").invoke();
                o.push(t.method("get_name").invoke());
              } catch (e) {}
              try {
                o.push(getPlayerIdentityInfo(e).label);
              } catch (e) {}
              for (const e of o) {
                const t = normalizePlayerToken(e).replace(/^"|"$/g, "").trim();
                if (t && t !== "?" && t.toLowerCase() !== "netplayer") {
                  return t;
                }
              }
              return "Player " + (t + 1);
            }
            function resolveUsersSelectedPlayer() {
              try {
                if (usersSelectedPlayer && !usersSelectedPlayer.isNull?.()) {
                  return usersSelectedPlayer;
                }
              } catch (e) {
                usersSelectedPlayer = null;
              }
              if (!usersSelectedKey) {
                return null;
              }
              const e = normalizeWhitelistToken(usersSelectedKey);
              for (const t of getAllNetPlayersList(true)) {
                try {
                  if (getPlayerIdentityInfo(t).aliases.some(t => normalizeWhitelistToken(t) === e)) {
                    usersSelectedPlayer = t;
                    return t;
                  }
                } catch (e) {}
              }
              return null;
            }
            function setUsersPlayerMuted(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  sendNotification("Selected player is no longer here.", false);
                  return;
                }
                const o = UnityEngineAudio.class("UnityEngine.AudioSource");
                const n = e.method("get_gameObject").invoke().method("GetComponentsInChildren", 0).inflate(o).invoke();
                let i = 0;
                const a = new Set();
                if (n && !n.isNull?.()) {
                  for (let e = 0; e < n.length; e++) {
                    try {
                      const o = n.get(e);
                      o.method("set_mute").invoke(t);
                      a.add(normalizeSceneObjectHandle(o));
                      i++;
                    } catch (e) {}
                  }
                }
                try {
                  const n = Object.method("FindObjectsOfType").inflate(o).invoke();
                  for (let o = 0; o < n.length; o++) {
                    try {
                      const l = n.get(o);
                      const r = normalizeSceneObjectHandle(l);
                      if (a.has(r)) {
                        continue;
                      }
                      const s = l.method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                      if (!s || s.isNull?.() || !s.handle.equals(e.handle)) {
                        continue;
                      }
                      l.method("set_mute").invoke(t);
                      a.add(r);
                      i++;
                    } catch (e) {}
                  }
                } catch (e) {}
                sendNotification((t ? "Muted " : "Unmuted ") + usersSelectedLabel + " (" + i + " audio sources)", false);
              } catch (e) {
                sendNotification("Player audio: " + e, false);
              }
            }
            function showUsersPlayerInfo(e) {
              try {
                if (!e || e.isNull?.()) {
                  sendNotification("Selected player is no longer here.", false);
                  return;
                }
                const t = getPlayerIdentityInfo(e);
                let o = "?";
                try {
                  const t = NetPlayer.method("get_localPlayer").invoke();
                  o = Vector3.method("Distance").invoke(getTransform(t).method("get_position").invoke(), getTransform(e).method("get_position").invoke()).toFixed(1) + "m";
                } catch (e) {}
                console.log("[Users] " + t.label + " | aliases: " + t.aliases.join(" | ") + " | distance: " + o);
                sendNotification(t.label + " | distance " + o, false, 5);
              } catch (e) {
                sendNotification("Player info: " + e, false);
              }
            }
            function updateUsersCamera() {
              if (usersSpectateEnabled || usersOrbitCameraEnabled) {
                try {
                  const e = resolveUsersSelectedPlayer();
                  if (!e || e.isNull?.()) {
                    usersSpectateEnabled = false;
                    usersOrbitCameraEnabled = false;
                    return;
                  }
                  let t = null;
                  try {
                    t = Camera.method("get_main").invoke();
                  } catch (e) {}
                  if (!t || t.isNull?.()) {
                    try {
                      t = Object.method("FindObjectOfType").inflate(Camera).invoke();
                    } catch (e) {}
                  }
                  if (!t || t.isNull?.()) {
                    return;
                  }
                  const o = getTransform(t);
                  const n = getTransform(e);
                  const i = n.method("get_position").invoke();
                  const a = Vector3.method("op_Addition").invoke(i, [0, 1.15, 0]);
                  let l;
                  if (usersOrbitCameraEnabled) {
                    const e = Time.method("get_time").invoke() * 0.8;
                    l = Vector3.method("op_Addition").invoke(a, [Math.sin(e) * 3, 0.8, Math.cos(e) * 3]);
                  } else {
                    const e = Vector3.method("op_Multiply", 2).invoke(n.method("get_forward").invoke(), -3);
                    l = Vector3.method("op_Addition").invoke(a, Vector3.method("op_Addition").invoke(e, [0, 0.7, 0]));
                  }
                  o.method("set_position").invoke(l);
                  const r = Vector3.method("op_Subtraction").invoke(a, l);
                  o.method("set_rotation").invoke(Quaternion.method("LookRotation", 1).invoke(r));
                } catch (e) {}
              }
            }
            function buildUsersMenuPages() {
              const e = [new ButtonInfo({
                buttonText: "Exit Users",
                method: () => {
                  usersSpectateEnabled = false;
                  usersOrbitCameraEnabled = false;
                  currentCategory = 21;
                  currentPage = 0;
                },
                isTogglable: false,
                toolTip: "Returns to Other Players."
              }), new ButtonInfo({
                buttonText: "Refresh Players",
                method: () => {
                  currentCategory = 78;
                  currentPage = 0;
                },
                isTogglable: false,
                toolTip: "Refreshes the live player list."
              })];
              const t = getAllNetPlayersList(true);
              t.forEach((t, o) => {
                try {
                  const n = getPlayerIdentityInfo(t).key;
                  const i = getUsersDisplayName(t, o);
                  const a = playerIsLocal(t) ? " [You]" : "";
                  e.push(new ButtonInfo({
                    buttonText: o + 1 + ". " + i + a,
                    method: () => {
                      usersSelectedKey = n;
                      usersSelectedLabel = i;
                      usersSelectedPlayer = t;
                      usersSpectateEnabled = false;
                      usersOrbitCameraEnabled = false;
                      currentCategory = 79;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Opens local controls for " + i + "."
                  }));
                } catch (e) {}
              });
              if (t.length === 0) {
                e.push(new ButtonInfo({
                  buttonText: "No players found",
                  method: () => {},
                  isTogglable: false,
                  toolTip: "No network players are currently available."
                }));
              }
              buttons[78] = e;
              const o = resolveUsersSelectedPlayer();
              const n = !!o && !o.isNull?.();
              buttons[79] = [new ButtonInfo({
                buttonText: "Back To Users",
                method: () => {
                  usersSpectateEnabled = false;
                  usersOrbitCameraEnabled = false;
                  currentCategory = 78;
                  currentPage = 0;
                },
                isTogglable: false,
                toolTip: "Returns to the player list."
              }), new ButtonInfo({
                buttonText: "Selected: " + (n ? usersSelectedLabel : "Player left"),
                method: () => {},
                isTogglable: false,
                toolTip: "Currently selected player."
              }), new ButtonInfo({
                buttonText: "Player Info",
                method: () => showUsersPlayerInfo(resolveUsersSelectedPlayer()),
                isTogglable: false,
                toolTip: "Shows the selected player's name and distance."
              }), new ButtonInfo({
                buttonText: "Mute Player",
                method: () => setUsersPlayerMuted(resolveUsersSelectedPlayer(), true),
                isTogglable: false,
                toolTip: "Locally mutes audio sources attached to the selected player."
              }), new ButtonInfo({
                buttonText: "Unmute Player",
                method: () => setUsersPlayerMuted(resolveUsersSelectedPlayer(), false),
                isTogglable: false,
                toolTip: "Locally unmutes audio sources attached to the selected player."
              }), new ButtonInfo({
                buttonText: "Spectate Player",
                method: () => {
                  if (resolveUsersSelectedPlayer()) {
                    usersSpectateEnabled = true;
                    usersOrbitCameraEnabled = false;
                    sendNotification("Spectating " + usersSelectedLabel, false);
                  }
                },
                isTogglable: false,
                toolTip: "Moves only your local camera behind the selected player."
              }), new ButtonInfo({
                buttonText: "Orbit Camera",
                method: () => {
                  if (resolveUsersSelectedPlayer()) {
                    usersSpectateEnabled = false;
                    usersOrbitCameraEnabled = true;
                    sendNotification("Camera orbit: " + usersSelectedLabel, false);
                  }
                },
                isTogglable: false,
                toolTip: "Orbits only your local camera around the selected player."
              }), new ButtonInfo({
                buttonText: "Stop Viewing",
                method: () => {
                  usersSpectateEnabled = false;
                  usersOrbitCameraEnabled = false;
                  sendNotification("Player camera stopped", false);
                },
                isTogglable: false,
                toolTip: "Stops the local spectate/orbit camera."
              })];
              try {
                rebuildButtonMap();
              } catch (e) {}
            }
            function resolveGunTargetPlayer(e, t = 8) {
              if (!e) {
                return null;
              }
              try {
                const t = e.ray;
                if (t && !(t.handle?.isNull?.() ?? 1)) {
                  try {
                    const e = t.method("get_collider").invoke();
                    if (e && !e.isNull()) {
                      try {
                        const t = e.method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                        if (t && !t.isNull()) {
                          return t;
                        }
                      } catch (e) {}
                      try {
                        const t = e.method("get_gameObject").invoke();
                        if (t && !t.isNull()) {
                          const e = t.method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                          if (e && !e.isNull()) {
                            return e;
                          }
                        }
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              try {
                const o = getTransform(e.gunPointer).method("get_position").invoke();
                let n = null;
                let i = t;
                for (const e of getAllNetPlayersList(false)) {
                  try {
                    const t = getTransform(e).method("get_position").invoke();
                    const a = Vector3.method("Distance").invoke(o, t);
                    if (a < i) {
                      i = a;
                      n = e;
                    }
                  } catch (e) {}
                }
                return n;
              } catch (e) {
                return null;
              }
            }
            function getWhitelistGunTarget(e, t = true, o = 8) {
              const n = resolveGunTargetPlayer(e, o);
              if (!n || n.isNull?.() || playerIsLocal(n)) {
                return null;
              }
              return {
                player: n,
                id: getPlayerName(n),
                blocked: false
              };
            }
            function spawnRpgProjectile(e, t, o, n = 30) {
              try {
                const i = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("RpgProjectile"), e, t, NULL);
                if (i && !i.isNull()) {
                  try {
                    const e = i.method("GetComponent", 1).inflate(Rigidbody).invoke();
                    if (e && !e.isNull()) {
                      const t = Vector3.method("op_Multiply", 2).invoke(o, n);
                      e.method("set_velocity").invoke(t);
                    }
                  } catch (e) {}
                }
                return i;
              } catch (e) {
                return null;
              }
            }
            function spawnRpgProjectileAt(e, t = 30, o = null) {
              try {
                const n = getLaunchForward(e, o);
                const i = getLaunchUp(e, n);
                return spawnRpgProjectile(Vector3.method("op_Addition").invoke(e.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(n, 0.62), Vector3.method("op_Multiply", 2).invoke(i, -0.03))), getLaunchRotation(e, n, i), n, t);
              } catch (e) {
                return null;
              }
            }
            function getPlayerProjectileOriginTransform(e) {
              try {
                const t = getPlayerHandAnchorTransform(e, 1);
                if (t && !t.isNull?.()) {
                  return t;
                }
              } catch (e) {}
              try {
                const t = e.field("rightHandTransform").value;
                if (isLiveObject(t)) {
                  return t;
                }
              } catch (e) {}
              try {
                const t = e.method("get_playerView").invoke();
                if (isLiveObject(t)) {
                  try {
                    const e = t.field("_rightHandTransform").value;
                    if (isLiveObject(e)) {
                      return e;
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              try {
                const t = e.method("get_playerView").invoke();
                if (isLiveObject(t)) {
                  try {
                    const e = t.field("_cameraTransform").value;
                    if (isLiveObject(e)) {
                      return e;
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              try {
                const t = e.field("headCollider").value;
                if (isLiveObject(t)) {
                  return getTransform(t);
                }
              } catch (e) {}
              try {
                return getTransform(e);
              } catch (e) {
                return null;
              }
            }
            function getPlayerStraightForward(e) {
              try {
                const t = readVec3Components((getPlayerHeadTransform(e) ?? getTransform(e)).method("get_forward").invoke());
                const o = t[0];
                const n = t[2];
                const i = Math.sqrt(o * o + n * n);
                if (i > 0.0001) {
                  return [o / i, 0, n / i];
                }
              } catch (e) {}
              return [0, 0, 1];
            }
            function getLookRotationFromForward(e) {
              try {
                return Quaternion.method("LookRotation", 2).invoke(e, [0, 1, 0]);
              } catch (e) {}
              try {
                return Quaternion.method("LookRotation", 1).invoke(e);
              } catch (e) {}
              return identityQuaternion;
            }
            function recenterMenu() {
              try {
                if (!menu) {
                  return;
                }
                const e = getTransform(menu);
                let t = zeroVector;
                let o = identityQuaternion;
                if (_pcMode) {
                  const e = getTransform(headCollider);
                  if (e && !e.isNull?.()) {
                    const n = e.method("get_position").invoke();
                    const i = e.method("get_forward").invoke();
                    const a = e.method("get_rotation").invoke();
                    t = Vector3.method("op_Addition").invoke(n, Vector3.method("op_Multiply", 2).invoke(i, pcMenuDistance));
                    o = Quaternion.method("op_Multiply").invoke(a, Quaternion.method("Euler").invoke(-90, 90, 0));
                  }
                } else {
                  const e = righthand ? rightHandTransform : leftHandTransform;
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  const n = e.method("get_position").invoke();
                  const i = e.method("get_right").invoke();
                  const a = Vector3.method("op_Multiply").invoke(i, righthand ? vrMenuPalmInset : -vrMenuPalmInset);
                  let l = zeroVector;
                  try {
                    const e = getTransform(headCollider).method("get_right").invoke();
                    l = Vector3.method("op_Multiply", 2).invoke(e, vrMenuRightOffset);
                  } catch (e) {}
                  t = Vector3.method("op_Addition").invoke(Vector3.method("op_Addition").invoke(Vector3.method("op_Addition").invoke(n, [0, vrMenuUpOffset, 0]), a), l);
                  o = e.method("get_rotation").invoke();
                  if (righthand) {
                    o = Quaternion.method("op_Multiply").invoke(o, Quaternion.method("Euler").invoke(0, 0, 180));
                  }
                  o = Quaternion.method("op_Multiply").invoke(o, Quaternion.method("Euler").invoke(0, vrMenuTilt, 0));
                }
                if (menuSnapNextFrame || !LerpMenu) {
                  e.method("set_position").invoke(t);
                  e.method("set_rotation").invoke(o);
                  menuSnapNextFrame = false;
                  return;
                }
                const n = Math.min(1, Math.max(deltaTime, 0.0001) * Math.max(2, menuFollowSmoothness));
                e.method("set_position").invoke(Vector3.method("Lerp").invoke(e.method("get_position").invoke(), t, n));
                e.method("set_rotation").invoke(Quaternion.method("Slerp").invoke(e.method("get_rotation").invoke(), o, n));
              } catch (e) {}
            }
            function getAllLobbyItemGameObjects() {
              const e = [];
              const t = new Set();
              const o = o => {
                try {
                  if (!o || o.isNull?.()) {
                    return;
                  }
                  let n = normalizeSceneObjectHandle(getRootLikeObject(o)) || normalizeSceneObjectHandle(o);
                  if (!n) {
                    try {
                      n = normalizeSceneObjectHandle(o.method("get_gameObject").invoke());
                    } catch (e) {}
                  }
                  if (!n || t.has(n)) {
                    return;
                  }
                  t.add(n);
                  e.push(o);
                } catch (e) {}
              };
              try {
                const e = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                if (e && !e.isNull()) {
                  for (let t = 0; t < e.length; t++) {
                    try {
                      const n = e.get(t).cast(GBOClass);
                      if (!n || n.isNull?.()) {
                        continue;
                      }
                      o(n);
                    } catch (e) {}
                  }
                }
              } catch (e) {}
              try {
                const e = Object.method("FindObjectsByType", 1).inflate(GBIClass).invoke(0);
                if (e && !e.isNull()) {
                  for (let t = 0; t < e.length; t++) {
                    try {
                      const n = e.get(t).cast(GBIClass);
                      if (!n || n.isNull?.()) {
                        continue;
                      }
                      o(n);
                    } catch (e) {}
                  }
                }
              } catch (e) {}
              try {
                const e = GBIClass.method("get_allLootItems").invoke();
                if (e && !e.isNull()) {
                  const t = e.method("GetEnumerator").invoke();
                  while (t.method("MoveNext").invoke()) {
                    try {
                      const e = t.method("get_Current").invoke();
                      if (!e || e.isNull?.()) {
                        continue;
                      }
                      o(e);
                    } catch (e) {}
                  }
                }
              } catch (e) {}
              try {
                for (const e of getAllNetPlayersList(true)) {
                  try {
                    for (const t of [0, 1]) {
                      const n = getPlayerHeldGrabbable(e, t);
                      if (n && !n.isNull?.()) {
                        o(n);
                      }
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              return e;
            }
            function getPlayerHeldGrabbable(e, t) {
              try {
                const o = e.method("GetHandInteractor", 1).invoke(t);
                if (!o || o.isNull?.()) {
                  return null;
                }
                const n = o.field("_itemAnchor").value;
                if (!n || n.isNull?.()) {
                  return null;
                }
                const i = n.method("get_grabbableObject").invoke();
                if (!i || i.isNull?.()) {
                  return null;
                } else {
                  return i;
                }
              } catch (e) {
                return null;
              }
            }
            function getLocalHeldGrabbable(e = true) {
              try {
                const t = NetPlayer.method("get_localPlayer").invoke();
                if (!t || t.isNull?.()) {
                  return null;
                }
                const o = e ? 0 : 1;
                return getPlayerHeldGrabbable(t, e ? 1 : 0) ?? getPlayerHeldGrabbable(t, o);
              } catch (e) {
                return null;
              }
            }
            function getPlayerOverlayKey(e) {
              try {
                return normalizeSceneObjectHandle(e) || getPlayerName(e) || String(e);
              } catch (t) {
                return String(e);
              }
            }
            function configureOverlayMaterial(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                try {
                  e.method("set_shader").invoke(TextShader);
                } catch (t) {
                  try {
                    e.method("set_shader").invoke(UberShader);
                  } catch (e) {}
                }
                try {
                  e.method("set_color").invoke(t);
                } catch (e) {}
                try {
                  e.method("SetInt").invoke(Il2Cpp.string("_ZTest"), 8);
                } catch (e) {}
                try {
                  e.method("SetInt").invoke(Il2Cpp.string("_ZWrite"), 0);
                } catch (e) {}
                try {
                  e.method("SetInt").invoke(Il2Cpp.string("_Cull"), 0);
                } catch (e) {}
                try {
                  e.method("set_renderQueue").invoke(5000);
                } catch (e) {}
              } catch (e) {}
            }
            function configureOverlayRenderer(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                try {
                  e.method("set_sortingOrder").invoke(32767);
                } catch (e) {}
                try {
                  e.method("set_allowOcclusionWhenDynamic").invoke(false);
                } catch (e) {}
                configureOverlayMaterial(e.method("get_material").invoke(), t);
              } catch (e) {}
            }
            function destroyOverlayEntries(e) {
              try {
                for (const [, t] of e.entries()) {
                  try {
                    if (t?.root && !t.root.isNull?.()) {
                      Destroy(t.root);
                    }
                    if (t?.boxRoot && !t.boxRoot.isNull?.()) {
                      Destroy(t.boxRoot);
                    }
                    if (t?.rayRoot && !t.rayRoot.isNull?.()) {
                      Destroy(t.rayRoot);
                    }
                  } catch (e) {}
                }
                e.clear();
              } catch (e) {}
            }
            function getOverlayViewPosition() {
              try {
                return getTransform(headCollider).method("get_position").invoke();
              } catch (e) {
                return [0, 0, 0];
              }
            }
            function updatePlayerNameTags() {
              if (!nameTagsEnabled) {
                destroyOverlayEntries(playerNameTagEntries);
                return;
              }
              const e = new Set();
              const t = getOverlayViewPosition();
              for (const o of getAllNetPlayersList(true)) {
                try {
                  if (!o || o.isNull?.()) {
                    continue;
                  }
                  const n = getPlayerOverlayKey(o);
                  e.add(n);
                  let i = playerNameTagEntries.get(n);
                  if (!i) {
                    const e = createEmptyObject("PlayerNameTag", null);
                    const t = createEmptyObject("PlayerNameTagText", getTransform(e));
                    let o = null;
                    if (TextMesh) {
                      o = addComponent(t, TextMesh);
                      try {
                        const e = getarialnocrash2();
                        if (e && !e.isNull?.()) {
                          o.method("set_font").invoke(e);
                        }
                      } catch (e) {}
                    } else if (TextMeshPro3D) {
                      o = addComponent(t, TextMeshPro3D);
                      try {
                        const e = gettmpfontnocrash();
                        if (e && !e.isNull?.()) {
                          o.method("set_font").invoke(e);
                        }
                      } catch (e) {}
                    }
                    i = {
                      root: e,
                      textObj: t,
                      comp: o
                    };
                    playerNameTagEntries.set(n, i);
                  }
                  const a = getPlayerHeadTransform(o);
                  if (!a || a.isNull?.()) {
                    continue;
                  }
                  const l = a.method("get_position").invoke();
                  const r = [l.field("x").value, l.field("y").value + 0.34, l.field("z").value];
                  const s = getTransform(i.root);
                  s.method("set_position").invoke(r);
                  const c = Vector3.method("op_Subtraction", 2).invoke(t, r);
                  try {
                    const e = Quaternion.method("LookRotation", 1).invoke(c);
                    s.method("set_rotation").invoke(e);
                  } catch (e) {}
                  try {
                    s.method("Rotate").invoke(0, 180, 0);
                  } catch (e) {}
                  if (TextMeshPro3D && i.comp && !TextMesh) {
                    try {
                      i.comp.method("SetText", 1).invoke(Il2Cpp.string(getPlayerName(o)));
                    } catch (e) {
                      try {
                        i.comp.method("set_text").invoke(Il2Cpp.string(getPlayerName(o)));
                      } catch (e) {}
                    }
                    try {
                      i.comp.method("set_fontSize").invoke(2.8);
                    } catch (e) {}
                    try {
                      i.comp.method("set_color").invoke([0.24, 0.66, 1, 1]);
                    } catch (e) {}
                    try {
                      i.comp.method("set_alignment").invoke(514);
                    } catch (e) {}
                    try {
                      i.comp.method("set_enableWordWrapping").invoke(false);
                    } catch (e) {}
                    try {
                      const e = i.textObj.method("GetComponent").inflate(Renderer).invoke();
                      if (e && !e.isNull?.()) {
                        e.method("set_enabled").invoke(true);
                        const t = e.method("get_material").invoke();
                        if (t && !t.isNull?.()) {
                          try {
                            t.method("set_renderQueue").invoke(5000);
                          } catch (e) {}
                        }
                      }
                    } catch (e) {
                      console.error("NameTag Renderer:", e);
                    }
                    try {
                      getTransform(i.textObj).method("set_localScale").invoke([0.02, 0.02, 0.02]);
                    } catch (e) {}
                  } else if (i.comp) {
                    try {
                      i.comp.method("set_text").invoke(Il2Cpp.string(getPlayerName(o)));
                    } catch (e) {}
                    try {
                      i.comp.method("set_fontSize").invoke(44);
                    } catch (e) {}
                    try {
                      i.comp.method("set_characterSize").invoke(0.032);
                    } catch (e) {}
                    try {
                      getTransform(i.textObj).method("set_localScale").invoke([0.02, 0.02, 0.02]);
                    } catch (e) {}
                    try {
                      i.comp.method("set_anchor").invoke(4);
                    } catch (e) {}
                    try {
                      i.comp.method("set_alignment").invoke(1);
                    } catch (e) {}
                    try {
                      i.comp.method("set_color").invoke([0.24, 0.66, 1, 1]);
                    } catch (e) {}
                    try {
                      const e = i.textObj.method("GetComponent").inflate(Renderer).invoke();
                      if (e && !e.isNull?.()) {
                        e.method("set_enabled").invoke(true);
                        const t = e.method("get_material").invoke();
                        if (t && !t.isNull?.()) {
                          try {
                            t.method("set_renderQueue").invoke(5000);
                          } catch (e) {}
                        }
                      }
                    } catch (e) {
                      console.error("NameTag Renderer:", e);
                    }
                  } else if (i.comp) {
                    try {
                      i.comp.method("set_text").invoke(Il2Cpp.string(getPlayerName(o)));
                    } catch (e) {}
                    try {
                      i.comp.method("set_fontSize").invoke(44);
                    } catch (e) {}
                    try {
                      i.comp.method("set_characterSize").invoke(0.032);
                    } catch (e) {}
                    try {
                      i.comp.method("set_anchor").invoke(4);
                    } catch (e) {}
                    try {
                      i.comp.method("set_alignment").invoke(1);
                    } catch (e) {}
                    try {
                      i.comp.method("set_color").invoke([0.24, 0.66, 1, 1]);
                    } catch (e) {}
                  }
                  try {
                    const e = getComponent(i.textObj ?? i.root, Renderer);
                    if (e && !e.isNull?.()) {
                      try {
                        e.method("set_enabled").invoke(true);
                      } catch (e) {}
                      try {
                        if (TextMesh && i.comp) {
                          const t = getarialnocrash2();
                          if (t && !t.isNull?.()) {
                            try {
                              e.method("set_sharedMaterial").invoke(t.method("get_material").invoke());
                            } catch (o) {
                              try {
                                e.method("set_material").invoke(t.method("get_material").invoke());
                              } catch (e) {}
                            }
                          }
                        }
                      } catch (e) {}
                      configureOverlayRenderer(e, [0.24, 0.66, 1, 1]);
                    }
                  } catch (e) {}
                  try {
                    getTransform(i.textObj ?? i.root).method("set_localScale").invoke([0.055, 0.055, 0.055]);
                  } catch (e) {}
                } catch (e) {}
              }
              for (const [t, o] of Array.from(playerNameTagEntries.entries())) {
                if (!e.has(t)) {
                  try {
                    if (o?.textObj && !o.textObj.isNull?.()) {
                      Destroy(o.textObj);
                    }
                  } catch (e) {}
                  try {
                    if (o?.root && !o.root.isNull?.()) {
                      Destroy(o.root);
                    }
                  } catch (e) {}
                  playerNameTagEntries.delete(t);
                }
              }
            }
            function ensureEspEntry(e) {
              const t = getPlayerOverlayKey(e);
              let o = playerEspEntries.get(t);
              if (o) {
                return o;
              }
              const n = createEmptyObject("PlayerEspRay", null);
              const i = createEmptyObject("PlayerEspBox", null);
              o = {
                rayRoot: n,
                boxRoot: i,
                ray: addComponent(n, LineRenderer),
                box: addComponent(i, LineRenderer)
              };
              playerEspEntries.set(t, o);
              return o;
            }
            function updatePlayerEspOverlays() {
              if (!arenaEspEnabled) {
                destroyOverlayEntries(playerEspEntries);
                return;
              }
              const e = new Set();
              const t = getTransform(headCollider);
              t.method("get_position").invoke();
              const o = readVec3Components(t.method("get_right").invoke());
              const n = readVec3Components(t.method("get_up").invoke());
              const i = rightHandTransform.method("get_position").invoke();
              for (const t of getAllNetPlayersList(false)) {
                try {
                  if (!t || t.isNull?.()) {
                    continue;
                  }
                  const a = getPlayerOverlayKey(t);
                  e.add(a);
                  const l = ensureEspEntry(t);
                  const r = getPlayerHeadTransform(t);
                  const s = getTransform(t);
                  if (!r || r.isNull?.() || !s || s.isNull?.()) {
                    continue;
                  }
                  const c = r.method("get_position").invoke();
                  const d = s.method("get_position").invoke();
                  const u = [d.field("x").value, (d.field("y").value + c.field("y").value) * 0.5, d.field("z").value];
                  const m = Math.max(0.9, c.field("y").value - d.field("y").value + 0.55);
                  const h = Math.max(0.24, m * 0.32);
                  const g = [o[0] * h, o[1] * h, o[2] * h];
                  const f = [n[0] * m * 0.5, n[1] * m * 0.5, n[2] * m * 0.5];
                  const p = [u[0] - g[0] - f[0], u[1] - g[1] - f[1], u[2] - g[2] - f[2]];
                  const y = [u[0] + g[0] - f[0], u[1] + g[1] - f[1], u[2] + g[2] - f[2]];
                  const b = [u[0] + g[0] + f[0], u[1] + g[1] + f[1], u[2] + g[2] + f[2]];
                  const _ = [u[0] - g[0] + f[0], u[1] - g[1] + f[1], u[2] - g[2] + f[2]];
                  const v = l.box;
                  const T = l.ray;
                  const k = [0.18, 0.62, 1, 0.95];
                  const N = [1, 0.58, 0.1, 0.92];
                  try {
                    v.method("set_useWorldSpace").invoke(true);
                    v.method("set_positionCount").invoke(5);
                    v.method("set_startWidth").invoke(0.01);
                    v.method("set_endWidth").invoke(0.01);
                    v.method("set_startColor").invoke(k);
                    v.method("set_endColor").invoke(k);
                    try {
                      v.method("set_numCornerVertices").invoke(8);
                    } catch (e) {}
                    try {
                      v.method("set_numCapVertices").invoke(8);
                    } catch (e) {}
                    v.method("SetPosition").invoke(0, p);
                    v.method("SetPosition").invoke(1, y);
                    v.method("SetPosition").invoke(2, b);
                    v.method("SetPosition").invoke(3, _);
                    v.method("SetPosition").invoke(4, p);
                    configureOverlayMaterial(v.method("get_material").invoke(), k);
                  } catch (e) {}
                  try {
                    T.method("set_useWorldSpace").invoke(true);
                    T.method("set_positionCount").invoke(2);
                    T.method("set_startWidth").invoke(0.006);
                    T.method("set_endWidth").invoke(0.006);
                    T.method("set_startColor").invoke(N);
                    T.method("set_endColor").invoke(N);
                    try {
                      T.method("set_numCornerVertices").invoke(8);
                    } catch (e) {}
                    try {
                      T.method("set_numCapVertices").invoke(8);
                    } catch (e) {}
                    T.method("SetPosition").invoke(0, i);
                    T.method("SetPosition").invoke(1, c);
                    configureOverlayMaterial(T.method("get_material").invoke(), N);
                  } catch (e) {}
                } catch (e) {}
              }
              for (const [t, o] of Array.from(playerEspEntries.entries())) {
                if (!e.has(t)) {
                  try {
                    if (o?.rayRoot && !o.rayRoot.isNull?.()) {
                      Destroy(o.rayRoot);
                    }
                  } catch (e) {}
                  try {
                    if (o?.boxRoot && !o.boxRoot.isNull?.()) {
                      Destroy(o.boxRoot);
                    }
                  } catch (e) {}
                  playerEspEntries.delete(t);
                }
              }
            }
            function getPlayerHandAnchorTransform(e, t = 1) {
              try {
                const o = e.method("GetHandInteractor", 1).invoke(t);
                if (o && !o.isNull?.()) {
                  try {
                    const e = o.field("_itemAnchor").value;
                    if (e && !e.isNull?.()) {
                      try {
                        const t = e.method("get_transform").invoke();
                        if (t && !t.isNull?.()) {
                          return t;
                        }
                      } catch (e) {}
                    }
                  } catch (e) {}
                  try {
                    const e = o.field("_handTransform").value;
                    if (e && !e.isNull?.()) {
                      return e;
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              try {
                const o = t === 1 ? "rightHandTransform" : "leftHandTransform";
                const n = e.field(o).value;
                if (n && !n.isNull?.()) {
                  return n;
                }
              } catch (e) {}
              return null;
            }
            function grabbableLooksLikeRevolver(e) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                if ((e.class?.type?.name ?? "").toLowerCase().indexOf("revolver") >= 0) {
                  return true;
                }
                try {
                  const t = e.method("get_gameObject").invoke();
                  if ((t?.method("get_name").invoke()?.content ?? "").toLowerCase().indexOf("revolver") >= 0) {
                    return true;
                  }
                } catch (e) {}
              } catch (e) {}
              return false;
            }
            function grabbableLooksLikeGun(e) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                const t = [];
                try {
                  t.push((e.class?.type?.name ?? "").toLowerCase());
                } catch (e) {}
                try {
                  const o = e.method("get_gameObject").invoke();
                  t.push((o?.method("get_name").invoke()?.content ?? "").toLowerCase());
                } catch (e) {}
                const o = ["gun", "revolver", "shotgun", "pistol", "rpg", "rocket", "flare", "crossbow", "launcher", "cannon", "bow"];
                return t.some(e => o.some(t => e.indexOf(t) >= 0));
              } catch (e) {
                return false;
              }
            }
            function getSelectedItemNeedles(e) {
              const t = new Set();
              const o = e => {
                const o = (e ?? "").toLowerCase().trim();
                if (o) {
                  t.add(o);
                }
              };
              o(e);
              if ((e ?? "").toLowerCase().startsWith("item_")) {
                o((e ?? "").slice(5));
              }
              return Array.from(t);
            }
            function grabbableMatchesSelectedItem(e, t) {
              for (const o of getSelectedItemNeedles(t)) {
                if (grabbableMatchesItemId(e, o)) {
                  return true;
                }
              }
              return false;
            }
            function getGrabbableItemId(e) {
              try {
                if (!e || e.isNull?.()) {
                  return "";
                }
                try {
                  const t = e.method("GetComponent", 1).inflate(GBIClass).invoke();
                  if (t && !t.isNull()) {
                    const e = t.method("get_itemID").invoke();
                    const o = (e?.content ?? "").trim();
                    if (o) {
                      return o;
                    }
                  }
                } catch (e) {}
                try {
                  const t = e.method("get_gameObject").invoke();
                  const o = (t?.method("get_name").invoke()?.content ?? "").trim();
                  if (o) {
                    return o;
                  }
                } catch (e) {}
                return (e.class?.type?.name ?? "").trim();
              } catch (e) {
                return "";
              }
            }
            function grabbableMatchesItemId(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                const o = (t ?? "").toLowerCase();
                if ((e.class?.type?.name ?? "").toLowerCase().indexOf(o) >= 0) {
                  return true;
                }
                try {
                  const t = e.method("get_gameObject").invoke();
                  if ((t?.method("get_name").invoke()?.content ?? "").toLowerCase().indexOf(o) >= 0) {
                    return true;
                  }
                } catch (e) {}
                try {
                  const t = e.method("GetComponent", 1).inflate(GBIClass).invoke();
                  if (t && !t.isNull()) {
                    const e = t.method("get_itemID").invoke();
                    const n = (e?.content ?? "").toLowerCase();
                    if (n === o || n.endsWith(o)) {
                      return true;
                    }
                  }
                } catch (e) {}
              } catch (e) {}
              return false;
            }
            function getPlayerKickTokens(e) {
              const t = [];
              const o = e => {
                const o = normalizePlayerToken(e);
                if (o && o !== "?" && !t.includes(o)) {
                  t.push(o);
                }
              };
              try {
                const t = getPlayerIdentityInfo(e);
                for (const e of t.aliases) {
                  o(e);
                }
              } catch (e) {}
              try {
                o(e.method("GetUserIDAndRegister").invoke());
              } catch (e) {}
              try {
                o(e.method("get_userId").invoke());
              } catch (e) {}
              try {
                o(e.method("get_userID").invoke());
              } catch (e) {}
              try {
                o(e.method("get_accountId").invoke());
              } catch (e) {}
              try {
                o(e.method("get_accountID").invoke());
              } catch (e) {}
              try {
                o(e.field("_userId").value);
              } catch (e) {}
              try {
                o(e.field("_userID").value);
              } catch (e) {}
              try {
                o(e.field("_accountId").value);
              } catch (e) {}
              try {
                o(e.field("_accountID").value);
              } catch (e) {}
              try {
                const t = e.method("get_playerView").invoke();
                if (t && !t.isNull?.()) {
                  try {
                    o(t.field("_userId").value);
                  } catch (e) {}
                  try {
                    o(t.field("_accountId").value);
                  } catch (e) {}
                  try {
                    o(t.method("get_userId").invoke());
                  } catch (e) {}
                }
              } catch (e) {}
              return t;
            }
            function dumpPlayerInfoToTerminal(e) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                const t = [];
                const o = getPlayerIdentityInfo(e);
                t.push("========================================");
                t.push("[PlayerInfo] " + o.label);
                t.push("aliases: " + o.aliases.join(" | "));
                t.push("kickTokens: " + getPlayerKickTokens(e).join(" | "));
                try {
                  t.push("playerRef: " + normalizePlayerToken(e.method("get_PlayerRef").invoke()));
                } catch (e) {}
                const n = (e, o) => {
                  try {
                    if (!e || e.isNull?.()) {
                      return;
                    }
                    const n = ["name", "display", "playerid", "userid", "account", "auth", "token", "nonce", "session", "photon"];
                    for (const i of e.class?.fields ?? []) {
                      try {
                        const a = String(i.name ?? "");
                        if (!n.some(e => a.toLowerCase().indexOf(e) >= 0)) {
                          continue;
                        }
                        t.push(o + "field " + a + " = " + normalizePlayerToken(e.field(a).value));
                      } catch (e) {}
                    }
                    for (const n of ["get_displayName", "get_name", "get_playerId", "get_userId", "get_userID", "get_accountId", "get_accountID", "get_authToken", "get_token", "get_nonce"]) {
                      try {
                        t.push(o + n + " = " + normalizePlayerToken(e.method(n).invoke()));
                      } catch (e) {}
                    }
                  } catch (e) {}
                };
                n(e, "player.");
                try {
                  const t = e.method("get_playerView").invoke();
                  if (t && !t.isNull?.()) {
                    n(t, "view.");
                  }
                } catch (e) {}
                console.log(t.join("\n"));
              } catch (e) {
                console.error("[PlayerInfo]", e);
              }
            }
            function rankKickToken(e) {
              if (!e) {
                return -9999;
              }
              let t = 0;
              if (e.indexOf(" ") < 0) {
                t += 3;
              }
              if (e.length >= 10) {
                t += 3;
              }
              if (/^[a-z0-9_\-:.]+$/i.test(e)) {
                t += 2;
              }
              if (/[0-9]/.test(e)) {
                t += 1;
              }
              if (e.toLowerCase().indexOf("player") >= 0) {
                t -= 2;
              }
              return t;
            }
            function getSingletonInstanceCandidates(e) {
              const t = [];
              const o = new Set();
              const n = e => {
                try {
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  const n = normalizeSceneObjectHandle(e) || String(e);
                  if (o.has(n)) {
                    return;
                  }
                  o.add(n);
                  t.push(e);
                } catch (e) {}
              };
              if (!e) {
                return t;
              }
              for (const t of ["<Instance>k__BackingField", "_instance", "Instance", "instance", "s_instance", "m_instance"]) {
                try {
                  n(e.field(t).value);
                } catch (e) {}
              }
              for (const t of ["get_Instance", "get_instance"]) {
                try {
                  n(e.method(t).invoke());
                } catch (e) {}
              }
              return t;
            }
            function normalizeRoomCode(e) {
              return String(e ?? "").toUpperCase().replace(/[^A-Z0-9]/g, "").slice(0, 8);
            }
            function buildRoomJoinArg(e, t) {
              try {
                const o = String(e?.name ?? "");
                if (o.indexOf("System.String") >= 0) {
                  return Il2Cpp.string(t);
                }
                if (o.indexOf("Boolean") >= 0) {
                  return false;
                }
                if (/(Byte|SByte|Int16|UInt16|Int32|UInt32|Int64|UInt64|Single|Double)/.test(o)) {
                  return 0;
                }
                try {
                  if (e?.class?.isEnum || e?.class?.isValueType) {
                    return 0;
                  }
                } catch (e) {}
              } catch (e) {}
              return NULL;
            }
            function getRoomJoinInstances(e) {
              const t = getSingletonInstanceCandidates(e);
              const o = new Set(t.map(e => normalizeSceneObjectHandle(e) || String(e)));
              const n = e => {
                try {
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  const n = normalizeSceneObjectHandle(e) || String(e);
                  if (o.has(n)) {
                    return;
                  }
                  o.add(n);
                  t.push(e);
                } catch (e) {}
              };
              try {
                n(Object.method("FindObjectOfType").inflate(e).invoke());
              } catch (e) {}
              if (t.length === 0) {
                try {
                  Il2Cpp.gc.choose(e).slice(0, 4).forEach(n);
                } catch (e) {}
              }
              return t;
            }
            function tryJoinRoomCode(e) {
              const t = normalizeRoomCode(e);
              if (!t) {
                sendNotification("Enter a room code first.", false, 4);
                return false;
              }
              try {
                if (RoomJoinComputerProgramClass) {
                  const e = Il2Cpp.gc.choose(RoomJoinComputerProgramClass);
                  for (const o of e) {
                    try {
                      if (!o || o.isNull?.()) {
                        continue;
                      }
                      o.method("OnSubmitCommand", 1).invoke(Il2Cpp.string(t));
                      roomCodeQuery = t;
                      sendNotification("Joining room " + t + "...", false, 6);
                      console.log("[Codes] RoomJoinComputerProgram.OnSubmitCommand(" + t + ") invoked");
                      return true;
                    } catch (e) {
                      console.error("[Codes] Terminal program submit failed:", e);
                    }
                  }
                }
              } catch (e) {
                console.error("[Codes] RoomJoinComputerProgram lookup failed:", e);
              }
              try {
                const e = NManager.method("get_instance").invoke();
                if (!e || e.isNull?.()) {
                  throw new Error("NetworkManager instance unavailable");
                }
                const o = e.field("_currSessionManager").value;
                if (!o || o.isNull?.()) {
                  sendNotification("Session manager is not ready yet.", false, 5);
                  return false;
                }
                const n = acImage.class("AnimalCompany.SessionProperties").alloc();
                n.method(".ctor", 0).invoke();
                n.field("friendlyFireEnabled").value = false;
                n.field("isPublic").value = false;
                n.field("specialEventEnabled").value = false;
                o.method("Connect", 3).invoke(Il2Cpp.string(t), roomCodeGameMode, n);
                roomCodeQuery = t;
                sendNotification("Joining " + t + " (" + roomCodeGameModeNames[roomCodeGameMode] + ")...", false, 6);
                console.log("[Codes] SessionManager.Connect(" + t + ", " + roomCodeGameMode + ") invoked");
                return true;
              } catch (e) {
                console.error("[Codes] Exact SessionManager.Connect failed:", e);
              }
              const o = new Set(["JoinPrivateRoom", "TryJoinPrivateRoom", "JoinRoom", "TryJoinRoom", "JoinRoomByCode", "JoinCode", "ConnectToRoom", "ConnectToPrivateRoom", "JoinPrivateRoomAsync", "JoinRoomAsync", "JoinRoomWithCode"]);
              const n = [NetSessionPrivateRoomManagerClass, NetworkSessionManagerClass, NManager, PrivateRoomStateClass].filter(Boolean);
              const i = [];
              for (const e of n) {
                try {
                  const n = (e.methods ?? []).filter(e => {
                    const t = String(e.name ?? "");
                    const n = t.toLowerCase();
                    if (!o.has(t) && (!(n.indexOf("join") >= 0) && !(n.indexOf("connect") >= 0) || !(n.indexOf("room") >= 0) && !(n.indexOf("code") >= 0) && !(n.indexOf("private") >= 0))) {
                      return false;
                    }
                    const i = e.parameters ?? [];
                    return i.length >= 1 && i.length <= 5 && i.some(e => String(e.type?.name ?? "").indexOf("System.String") >= 0);
                  }).sort((e, t) => Number(!o.has(String(e.name))) - Number(!o.has(String(t.name))));
                  const a = getRoomJoinInstances(e);
                  for (const o of n) {
                    const n = (o.parameters ?? []).map(e => buildRoomJoinArg(e.type, t));
                    i.push(String(e.name ?? "?") + "." + String(o.name ?? "?"));
                    const l = o.isStatic ? [null] : a;
                    for (const i of l) {
                      try {
                        if ((i == null ? o.invoke(...n) : o.bind(i).invoke(...n)) === false) {
                          continue;
                        }
                        roomCodeQuery = t;
                        sendNotification("Joining code " + t + "...", false, 6);
                        console.log("[Codes] Invoked " + String(e.name ?? "?") + "." + String(o.name ?? "?") + " for " + t);
                        return true;
                      } catch (e) {}
                    }
                  }
                } catch (e) {}
              }
              console.error("[Codes] No compatible room join call succeeded. Candidates: " + i.join(", "));
              sendNotification("Could not join " + t + ". See console for detected join methods.", false, 6);
              return false;
            }
            function buildRoomCodeKeyboard() {
              buttons[80] = [new ButtonInfo({
                buttonText: "Code: " + (roomCodeQuery || "_"),
                method: () => {},
                isTogglable: false,
                toolTip: "Current room code."
              }), ...["1", "2", "3", "4", "5", "6", "7", "8", "9", "0", "Q", "W", "E", "R", "T", "Y", "U", "I", "O", "P", "A", "S", "D", "F", "G", "H", "J", "K", "L", "Z", "X", "C", "V", "B", "N", "M"].map(e => {
                const t = new ButtonInfo({
                  buttonText: "Code " + e,
                  method: () => {
                    sendNotification("Code: " + (roomCodeQuery = normalizeRoomCode(roomCodeQuery + e)) + "_", false, 99);
                  },
                  isTogglable: false
                });
                t.getLabel = () => e;
                return t;
              }), new ButtonInfo({
                buttonText: "Exit Room Codes",
                method: () => {
                  roomCodeQuery = "";
                  currentCategory = 11;
                  currentPage = 0;
                },
                isTogglable: false,
                toolTip: "Returns to Player mods."
              }), new ButtonInfo({
                buttonText: "Clear Code",
                method: () => {
                  roomCodeQuery = "";
                  sendNotification("Code: _", false, 99);
                },
                isTogglable: false
              }), new ButtonInfo({
                buttonText: "Backspace Code",
                method: () => {
                  sendNotification("Code: " + ((roomCodeQuery = roomCodeQuery.slice(0, -1)) || "_"), false, 99);
                },
                isTogglable: false
              }), new ButtonInfo({
                buttonText: "Join Code",
                method: () => {
                  tryJoinRoomCode(roomCodeQuery);
                },
                isTogglable: false,
                toolTip: "Joins the entered private room code."
              })];
              try {
                rebuildButtonMap();
              } catch (e) {}
            }
            function buildModerationArg(e, t, o) {
              try {
                const n = String(e?.name ?? "");
                if (n.indexOf("System.String") >= 0) {
                  return Il2Cpp.string(t);
                }
                if (n.indexOf("AnimalCompany.NetPlayer") >= 0) {
                  return o;
                }
                if (n.indexOf("Fusion.PlayerRef") >= 0) {
                  try {
                    return o.method("get_PlayerRef").invoke();
                  } catch (e) {}
                  try {
                    return o.field("_playerRef").value;
                  } catch (e) {}
                  return 0;
                }
                if (n.indexOf("Boolean") >= 0) {
                  return 0;
                }
                if (/(Byte|SByte|Int16|UInt16|Int32|UInt32|Int64|UInt64|Single|Double)/.test(n)) {
                  return 0;
                }
                try {
                  if (e?.class?.isEnum) {
                    return 0;
                  }
                } catch (e) {}
                try {
                  if (e?.class?.isValueType) {
                    return 0;
                  }
                } catch (e) {}
              } catch (e) {}
              return NULL;
            }
            function tryInvokeModerationMethod(e, t, o, n, i) {
              try {
                const a = e.method(o);
                const l = a.overloads ? a.overloads() : [a];
                for (const o of l) {
                  try {
                    if (o.parameters.length < 1 || o.parameters.length > 4) {
                      continue;
                    }
                    if (!o.parameters.some(e => {
                      const t = String(e.type?.name ?? "");
                      return t.indexOf("System.String") >= 0 || t.indexOf("AnimalCompany.NetPlayer") >= 0 || t.indexOf("Fusion.PlayerRef") >= 0;
                    })) {
                      continue;
                    }
                    const a = [];
                    let l = false;
                    for (const e of o.parameters) {
                      try {
                        if (String(e.type?.name ?? "").indexOf("Boolean") >= 0) {
                          l = true;
                        }
                      } catch (e) {}
                      a.push(buildModerationArg(e.type, n, i));
                    }
                    const r = [a];
                    if (l) {
                      const e = a.slice();
                      for (let t = 0; t < o.parameters.length; t++) {
                        try {
                          if (String(o.parameters[t].type?.name ?? "").indexOf("Boolean") >= 0) {
                            e[t] = true;
                          }
                        } catch (e) {}
                      }
                      r.push(e);
                    }
                    for (const n of r) {
                      if (t) {
                        o.invoke(...n);
                      } else {
                        o.bind(e).invoke(...n);
                      }
                      return true;
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              return false;
            }
            function tryKickPlayer(e) {
              if (!e || e.isNull?.() || playerIsLocal(e)) {
                return false;
              }
              try {
                const t = (() => {
                  try {
                    return e.method("get_PlayerRef").invoke();
                  } catch (e) {}
                  try {
                    return e.field("_playerRef").value;
                  } catch (e) {}
                  return null;
                })();
                for (const o of getRunnerCandidates()) {
                  try {
                    if (!o || o.isNull?.()) {
                      continue;
                    }
                    if (t != null) {
                      if (tryCallNames(o, ["Disconnect", "DisconnectPlayer", "RemovePlayer", "KickPlayer", "CloseConnection", "KickPeer"], 1, t)) {
                        return true;
                      }
                      if (tryCallNames(o, ["Disconnect", "DisconnectPlayer", "RemovePlayer", "KickPlayer", "CloseConnection", "KickPeer"], 2, t, true)) {
                        return true;
                      }
                    }
                    if (tryCallNames(o, ["Disconnect", "DisconnectPlayer", "RemovePlayer", "KickPlayer", "CloseConnection", "KickPeer"], 1, e)) {
                      return true;
                    }
                    if (tryCallNames(o, ["Disconnect", "DisconnectPlayer", "RemovePlayer", "KickPlayer", "CloseConnection", "KickPeer"], 2, e, true)) {
                      return true;
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              const t = getPlayerKickTokens(e).sort((e, t) => rankKickToken(t) - rankKickToken(e));
              const o = ["KickPlayer", "RPC_KickPlayer", "KickPlayerByUserId", "KickPlayerByAccountId", "DisconnectPlayer", "DisconnectUser", "KickUserFromPrivateRoom", "RemovePrivateRoomMember", "KickUser", "BanUserFromPrivateRoom", "RoomBanUserAsync", "KickSelectedPlayer", "ModerateKickUser", "OnKickUserPressed", "KickFromRoom", "RemoveFromRoom", "KickNetPlayer", "BanPlayer", "BlacklistPlayer", "ExpelPlayer", "ModerateBanUser", "OnBanUserPressed"];
              const n = [NetSessionPrivateRoomManagerClass, PlayerWatchDevMenuMediatorClass, ModerationMenuMediatorClass, NetworkSessionManagerClass, NManager, PrivateRoomStateClass, UserCacheManagerClass, AnimalCompanyApiClass];
              for (const i of t) {
                for (const t of n) {
                  if (t) {
                    for (const n of o) {
                      for (const o of getSingletonInstanceCandidates(t)) {
                        if (tryInvokeModerationMethod(o, false, n, i, e)) {
                          return true;
                        }
                      }
                      if (tryInvokeModerationMethod(t, true, n, i, e)) {
                        return true;
                      }
                    }
                  }
                }
              }
              return false;
            }
            function kickPlayer(e) {
              const t = AssemblyCSharp.class("AnimalCompany.NetSessionRPCs");
              const o = t.field("_instance").value;
              if (!o) {
                return null;
              }
              const n = t.method("KickPlayer");
              if (!n) {
                return null;
              }
              const i = o.method("RPC_KickPlayer");
              if (!i) {
                return null;
              }
              const a = e.field("_userID").value;
              if (!a) {
                return null;
              }
              const l = Il2Cpp.corlib.class("System.Guid").method("Parse").invoke(a);
              i.invoke(l);
              n.invoke(a);
            }
            function kickAllPlayers() {
              try {
                let e = 0;
                const t = Object.method("FindObjectsOfType").inflate(NetPlayer).invoke();
                for (let o = 0; o < t.length; o++) {
                  const n = t.get(o);
                  if (!playerIsLocal(n)) {
                    kickPlayer(n);
                    e++;
                  }
                }
                console.log(`[+] Kicked ${e} players`);
                sendNotification(`Kicked ${e} players`, false);
              } catch (e) {
                console.log(`[-] Error in kickAllPlayers: ${e}`);
              }
            }
            function styleActionButton(e, t) {
              return new ButtonInfo({
                buttonText: e,
                method: () => {
                  t();
                  reloadMenu();
                },
                isTogglable: false,
                toolTip: "Menu style: " + e
              });
            }
            function getPlayerField(e) {
              try {
                const t = instanceField?.value;
                if (!t || t.isNull?.()) {
                  return null;
                } else {
                  return t.field(e).value;
                }
              } catch (e) {
                return null;
              }
            }
            function getHudNotificationParent() {
              try {
                const e = getPlayerField("headCollider") || headCollider;
                if (isAlive(e)) {
                  return getTransform(e);
                }
              } catch (e) {}
              return null;
            }
            function getNotificationClock() {
              return Date.now() / 1000;
            }
            function setNotificationScaleIndex(e) {
              notificationScaleIndex = (e % notificationScaleValues.length + notificationScaleValues.length) % notificationScaleValues.length;
              sendNotification("Notification Size: " + (notificationScale = notificationScaleValues[notificationScaleIndex]).toFixed(2) + "x", false);
            }
            function adjustNotificationScale(e) {
              setNotificationScaleIndex(notificationScaleIndex + e);
            }
            function setNotificationDurationIndex(e) {
              notificationDurationIndex = (e % notificationDurationValues.length + notificationDurationValues.length) % notificationDurationValues.length;
              sendNotification("Notification Time: " + (notificationDuration = notificationDurationValues[notificationDurationIndex]).toFixed(2) + "s", false);
            }
            function adjustNotificationDuration(e) {
              setNotificationDurationIndex(notificationDurationIndex + e);
            }
            function destroyHudNotification(e) {
              try {
                if (e?.root && isAlive(e.root)) {
                  Destroy(e.root);
                }
              } catch (e) {}
              try {
                if (e?.textObject && isAlive(e.textObject)) {
                  Destroy(e.textObject);
                }
              } catch (e) {}
            }
            function pushHudNotification(e) {
              if (!e || !ensureHudNotificationCanvas()) {
                return;
              }
              while (hudNotifications.length >= 5) {
                destroyHudNotification(hudNotifications.shift());
              }
              const t = createObject(zeroVector, identityQuaternion, oneVector, 3, [0, 0, 0, 0], getTransform(hudNotificationRoot));
              removeCollider(t);
              t.method("set_layer").invoke(menuInteractionLayer);
              const o = getTransform(t);
              try {
                o.method("set_localRotation").invoke(identityQuaternion);
              } catch (e) {}
              const n = createObject([0, 0, 0.002], identityQuaternion, [0.42, 0.052, 0.01], 3, buttonColor, o);
              n.method("set_layer").invoke(menuInteractionLayer);
              removeCollider(n);
              setRenderQueue(n, 4900);
              const i = _lastObjectMat;
              try {
                getTransform(n).method("set_localPosition").invoke([0, 0, 0.002]);
              } catch (e) {}
              try {
                getTransform(n).method("set_localRotation").invoke(identityQuaternion);
              } catch (e) {}
              const a = createHudNotificationText(e, 0);
              try {
                getTransform(a.textObject).method("set_localPosition").invoke([0, 0, -0.004]);
              } catch (e) {}
              try {
                getTransform(a.textObject).method("set_localRotation").invoke(identityQuaternion);
              } catch (e) {}
              const l = getNotificationClock();
              hudNotifications.unshift({
                root: t,
                bg: i,
                textObject: a.textObject,
                text: a.text,
                createdAt: l,
                hideAt: l + Math.max(0, notificationDuration - notificationFadeDuration),
                expiresAt: l + notificationDuration
              });
              repositionHudNotifications();
            }
            function removeCollider(e) {
              try {
                const t = getComponent(e, Collider);
                if (isAlive(t)) {
                  Destroy(t);
                }
              } catch (e) {}
            }
            let menuTextFont = arial;
            function createHudNotificationText(e, t) {
              const o = createObject(zeroVector, identityQuaternion, oneVector, 3, [0, 0, 0, 0], getTransform(hudNotificationCanvas));
              o.method("set_layer").invoke(menuInteractionLayer);
              removeCollider(o);
              const n = addComponent(o, Text);
              n.method("set_text").invoke(Il2Cpp.string(e));
              n.method("set_font").invoke(fontHasMenuGlyphs(menuTextFont) ? menuTextFont : arial);
              n.method("set_fontSize").invoke(1);
              n.method("set_color").invoke(textColor);
              try {
                const e = Il2Cpp.domain.assembly("UnityEngine").image.class("UnityEngine.Material").method(".ctor", 1).invoke(TextShader);
                e.method("set_color").invoke(textColor);
                try {
                  e.method("SetInt", 2).invoke(Il2Cpp.string("_ZTest"), 8);
                } catch (e) {}
                try {
                  e.method("SetInt", 2).invoke(Il2Cpp.string("unity_GUIZTestMode"), 8);
                } catch (e) {}
                try {
                  e.method("set_renderQueue").invoke(5000);
                } catch (e) {}
                n.method("set_material").invoke(e);
              } catch (e) {}
              n.method("set_fontStyle").invoke(0);
              n.method("set_alignment").invoke(4);
              n.method("set_resizeTextForBestFit").invoke(true);
              n.method("set_resizeTextMinSize").invoke(0);
              const i = getComponent(n, RectTransform);
              i.method("set_sizeDelta").invoke([0.38, 0.04]);
              try {
                i.method("set_localPosition").invoke([0, t, 0]);
              } catch (e) {}
              try {
                i.method("set_localRotation").invoke(identityQuaternion);
              } catch (e) {}
              return {
                textObject: o,
                text: n
              };
            }
            function repositionHudNotifications() {
              const e = getNotificationClock();
              for (let t = 0; t < hudNotifications.length; t++) {
                const o = hudNotifications[t];
                const n = Math.max(0, e - o.createdAt);
                const i = Math.max(0, o.expiresAt - e);
                const a = Math.max(0, o.hideAt - e);
                const l = Math.min(1, n / 0.18);
                const r = a > 0 ? 1 : Math.min(1, i / notificationFadeDuration);
                const s = Math.min(l, r);
                const c = s * s * (3 - s * 2);
                const d = t * 0.058 * notificationScale;
                const u = (1 - c) * 0.24;
                const m = Math.max(0.01, notificationScale * (0.86 + c * 0.14));
                try {
                  getTransform(o.root).method("set_localPosition").invoke([u, d, 0]);
                } catch (e) {}
                try {
                  getTransform(o.root).method("set_localScale").invoke([m, m, m]);
                } catch (e) {}
                try {
                  getTransform(o.textObject).method("set_localPosition").invoke([u, d, -0.004]);
                } catch (e) {}
                try {
                  getTransform(o.textObject).method("set_localScale").invoke([m, m, m]);
                } catch (e) {}
              }
            }
            function fontHasMenuGlyphs(e) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                try {
                  e.method("RequestCharactersInTexture", 1).invoke(Il2Cpp.string("ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789 STRKMenuGuiSettingsLeaveHomeSearch"));
                } catch (e) {}
                const t = e.method("get_material").invoke();
                return !!t && !t.isNull?.() && !!e.method("HasCharacter", 1).invoke(65);
              } catch (e) {
                return false;
              }
            }
            function updateHudNotifications() {
              if (hudNotifications.length === 0) {
                return;
              }
              const e = getNotificationClock();
              const t = [];
              for (const o of hudNotifications) {
                if (e >= o.expiresAt) {
                  destroyHudNotification(o);
                } else {
                  t.push(o);
                  const n = Math.max(0, o.expiresAt - e);
                  const i = e < o.hideAt ? 1 : Math.min(1, n / notificationFadeDuration);
                  const a = i * i * (3 - i * 2);
                  const l = [buttonColor[0], buttonColor[1], buttonColor[2], buttonColor[3] * a];
                  const r = [textColor[0], textColor[1], textColor[2], textColor[3] * a];
                  if (isAlive(o.bg)) {
                    try {
                      setMatColor(o.bg, l);
                    } catch (e) {}
                  }
                  if (isAlive(o.text)) {
                    try {
                      o.text.method("set_color").invoke(r);
                    } catch (e) {}
                  }
                }
              }
              hudNotifications = t;
              repositionHudNotifications();
            }
            function setMatColor(e, t) {
              e.method("set_color").invoke(t);
              try {
                e.method("SetColor", 2).invoke(Il2Cpp.string("_BaseColor"), t);
              } catch (e) {}
            }
            function setRenderQueue(e, t) {
              try {
                const o = getComponent(e, Renderer);
                if (isAlive(o)) {
                  o.method("get_material").invoke().method("set_renderQueue").invoke(t);
                }
              } catch (e) {}
            }
            function ensureHudNotificationCanvas() {
              if (isAlive(hudNotificationRoot) && isAlive(hudNotificationCanvas)) {
                return true;
              }
              const e = getHudNotificationParent();
              if (!isAlive(e)) {
                return false;
              }
              removeCollider(hudNotificationRoot = createObject(zeroVector, identityQuaternion, oneVector, 3, [0, 0, 0, 0], e));
              const t = getTransform(hudNotificationRoot);
              try {
                t.method("set_localPosition").invoke([0.42, -0.23, 0.88]);
              } catch (e) {}
              try {
                t.method("set_localRotation").invoke(identityQuaternion);
              } catch (e) {}
              try {
                t.method("set_localScale").invoke(oneVector);
              } catch (e) {}
              (hudNotificationCanvas = createObject(zeroVector, identityQuaternion, oneVector, 3, [0, 0, 0, 0], t)).method("set_layer").invoke(menuInteractionLayer);
              removeCollider(hudNotificationCanvas);
              const o = getTransform(hudNotificationCanvas);
              try {
                o.method("set_localPosition").invoke(zeroVector);
              } catch (e) {}
              try {
                o.method("set_localRotation").invoke(identityQuaternion);
              } catch (e) {}
              const n = addComponent(hudNotificationCanvas, Canvas);
              const i = addComponent(hudNotificationCanvas, CanvasScaler);
              addComponent(hudNotificationCanvas, GraphicRaycaster);
              n.method("set_renderMode").invoke(2);
              n.method("set_sortingOrder").invoke(80);
              i.method("set_dynamicPixelsPerUnit").invoke(1000);
              return true;
            }
            function isGunLibObjectAlive(e) {
              try {
                return !!e && !e.isNull?.() && (e.method("get_name").invoke(), true);
              } catch (e) {
                return false;
              }
            }
            function safeDestroyGunLib(e) {
              if (e) {
                try {
                  if (!e.isNull?.()) {
                    Destroy(e);
                  }
                } catch (e) {}
              }
            }
            function hideGunLib() {
              gunSmoothStartPosition = null;
              gunSmoothEndPosition = null;
              gunSmoothSnapNextFrame = true;
              if (isGunLibObjectAlive(GunPointer)) {
                try {
                  GunPointer.method("SetActive").invoke(false);
                } catch (e) {
                  safeDestroyGunLib(GunPointer);
                  GunPointer = null;
                }
              } else {
                GunPointer = null;
              }
              if (isGunLibObjectAlive(GunLine)) {
                try {
                  GunLine.method("get_gameObject").invoke().method("SetActive").invoke(false);
                } catch (e) {
                  safeDestroyGunLib(GunLine);
                  GunLine = null;
                }
              } else {
                GunLine = null;
              }
            }
            function enableMobValidatorBypass() {
              if (validatorBypassEnabled) {
                return;
              }
              const e = Il2Cpp.domain.assembly("AnimalCompany").image;
              const t = e.class("AnimalCompany.MobSpawnValidator");
              for (const e of t.methods) {
                try {
                  if (e.returnType?.name === "System.Boolean") {
                    e.implementation = function () {
                      return true;
                    };
                  }
                } catch (e) {}
              }
              try {
                const t = e.class("AnimalCompany.HordeMobLobbyHandler");
                for (const e of t.methods) {
                  try {
                    if (e.returnType?.name === "System.Boolean") {
                      e.implementation = function () {
                        return true;
                      };
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              try {
                const t = e.class("AnimalCompany.SpawnGuardPolicy");
                for (const e of t.methods) {
                  try {
                    const t = e.name;
                    if (t === "IsInNoMobArea" || t === "IsNoMobArea") {
                      e.implementation = function () {
                        return false;
                      };
                    } else if (t === "IsContextLegitimate" || t === "AllowSpawnFromSource") {
                      e.implementation = function () {
                        return true;
                      };
                    } else if (t === "get_EnforceProviderDeny" || t === "IsLocalDespawnerFor") {
                      e.implementation = function () {
                        return false;
                      };
                    } else if (t === "RequestAuthoritativeDespawn") {
                      e.implementation = function () {};
                    }
                  } catch (e) {}
                }
                try {
                  const e = t.field("_noMobAreas").value;
                  if (isAlive(e)) {
                    e.method("Clear").invoke();
                  }
                } catch (e) {}
              } catch (e) {}
              validatorBypassEnabled = true;
            }
            function getBeforeMobSpawnDelegate() {
              if (beforeSpawnDelegate) {
                return beforeSpawnDelegate;
              }
              beforeSpawnDelegateClass = Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkRunner").tryNested("OnBeforeSpawned");
              const e = Il2Cpp.domain.assembly("AnimalCompany").image.class("AnimalCompany.MobSpawnValidator");
              return beforeSpawnDelegate = Il2Cpp.delegate(beforeSpawnDelegateClass, (t, o) => {
                if (!o || o.handle?.isNull()) {
                  return;
                }
                const n = o.method("get_Id").invoke();
                e.method("AddAllowMob", 1).invoke(n);
              });
            }
            function spawnItemAsProjectile(e, t, o) {
              try {
                const n = PrefabGen.field("_instance").value.method("get_runner").invoke();
                if (!n || n.isNull()) {
                  return null;
                }
                const i = PrefabGen.method("GetItemPrefab", 1).invoke(Il2Cpp.string(e));
                if (!i || i.isNull()) {
                  return null;
                }
                const a = e => {
                  if (e.class.isEnum || e.isPrimitive) {
                    return 0;
                  }
                  if (!e.class.isValueType) {
                    return NULL;
                  }
                  const t = e.class.fields.filter(e => !e.isStatic);
                  if (t.length === 0) {
                    return 0;
                  } else {
                    return t.map(e => a(e.type));
                  }
                };
                const l = (e, t, o) => e.class.fields.filter(e => !e.isStatic).map(e => {
                  const n = e.name.toLowerCase();
                  if (n.includes("hasvalue")) {
                    if (t) {
                      return 1;
                    } else {
                      return 0;
                    }
                  } else if (n === "value" && t) {
                    return o;
                  } else {
                    return a(e.type);
                  }
                });
                const r = (e, t) => {
                  if (typeof t == "boolean") {
                    if (t) {
                      return 1;
                    } else {
                      return 0;
                    }
                  }
                  if (t instanceof Il2Cpp.ValueType) {
                    const o = e.class.fields.filter(e => !e.isStatic);
                    if (o.length === 0) {
                      return 0;
                    } else {
                      return o.map(e => r(e.type, e.bind(t).value));
                    }
                  }
                  if (Array.isArray(t)) {
                    return t.map(t => r(e, t));
                  } else {
                    return t;
                  }
                };
                const s = (e, t) => l(e, true, r(t.type, t));
                let c = null;
                for (const e of n.method("Spawn").overloads()) {
                  if (e.parameterCount !== 6 || e.isGeneric) {
                    continue;
                  }
                  const t = e.parameters;
                  if (t[0].type.name.includes("Fusion.NetworkObject") && t[1].type.name.startsWith("System.Nullable") && t[1].type.name.includes("Vector3") && t[2].type.name.startsWith("System.Nullable") && t[2].type.name.includes("Quaternion") && t[3].type.name.startsWith("System.Nullable") && t[3].type.name.includes("PlayerRef") && t[4].type.name.includes("OnBeforeSpawned") && t[5].type.name.includes("NetworkSpawnFlags")) {
                    c = e;
                    break;
                  }
                }
                if (!c) {
                  return null;
                }
                const d = s(c.parameters[1].type, t);
                const u = s(c.parameters[2].type, o);
                const m = n.method("get_LocalPlayer").invoke();
                const h = s(c.parameters[3].type, m);
                const g = c.parameters[4].type.class.isValueType ? a(c.parameters[4].type) : NULL;
                return c.bind(n).invoke(i, d, u, h, g, 0);
              } catch (e) {
                return null;
              }
            }
            function isAlive(e) {
              try {
                return !!e && !e.isNull?.() && !e.handle?.isNull?.();
              } catch (e) {
                return false;
              }
            }
            function setRigidbodyVelocitySafe(e, t) {
              if (!isAlive(e)) {
                return false;
              }
              try {
                e.method("set_linearVelocity").invoke(t);
                return true;
              } catch (e) {}
              try {
                e.method("set_velocity").invoke(t);
                return true;
              } catch (e) {}
              return false;
            }
            function releaseTelekinesisPlayer() {
              telekinesisHeldPlayer = null;
            }
            function findNearestPlayerToHand(e, t) {
              let o = null;
              let n = t;
              try {
                const t = getAllNetPlayersList(false);
                for (let i = 0; i < t.length; i++) {
                  const a = t[i];
                  if (a && !a.handle.isNull() && !playerIsLocal(a)) {
                    try {
                      const t = getTransform(a).method("get_position").invoke();
                      const i = Vector3.method("Distance").invoke(e, t);
                      if (i < n) {
                        n = i;
                        o = a;
                      }
                    } catch (e) {}
                  }
                }
              } catch (e) {}
              return o;
            }
            function getTelekinesisLookOriginAndDir() {
              try {
                if (headCollider && !headCollider.isNull?.()) {
                  const e = getTransform(headCollider);
                  if (e && !e.isNull?.()) {
                    return {
                      origin: e.method("get_position").invoke(),
                      dir: e.method("get_forward").invoke()
                    };
                  }
                }
              } catch (e) {}
              try {
                const e = getGunAimDirection();
                return {
                  origin: rightHandTransform.method("get_position").invoke(),
                  dir: e
                };
              } catch (e) {}
              return null;
            }
            function tryGrabTelekinesisPlayer() {
              if (!(time < telekinesisLastGrabTime + 0.25)) {
                telekinesisLastGrabTime = time;
                try {
                  const e = getTelekinesisLookOriginAndDir();
                  if (!e) {
                    return;
                  }
                  const t = findNearestPlayerToHand(e.origin, telekinesisMaxGrabDistance);
                  if (!t) {
                    sendNotification("Telekinesis: no player nearby", false);
                    return;
                  }
                  telekinesisHeldPlayer = t;
                  let o = "player";
                  try {
                    o = getPlayerName(t);
                  } catch (e) {}
                  sendNotification("Holding: " + o, false);
                } catch (e) {
                  console.error("[Telekinesis grab]", e);
                }
              }
            }
            function updateTelekinesis() {
              if (telekinesisEnabled) {
                if (rightGrab && rightTrigger) {
                  if (!telekinesisHeldPlayer) {
                    tryGrabTelekinesisPlayer();
                  }
                }
                if (!rightGrab && telekinesisHeldPlayer) {
                  releaseTelekinesisPlayer();
                  sendNotification("Released player", false);
                  return;
                }
                if (telekinesisHeldPlayer && !telekinesisHeldPlayer.handle.isNull()) {
                  if (!(time < telekinesisLastGrabTime + 0.03)) {
                    telekinesisLastGrabTime = time;
                    try {
                      const e = getTelekinesisLookOriginAndDir();
                      if (!e) {
                        return;
                      }
                      const t = e.origin.field("x").value;
                      const o = e.origin.field("y").value;
                      const n = e.origin.field("z").value;
                      const i = e.dir.field("x").value;
                      const a = e.dir.field("y").value;
                      const l = e.dir.field("z").value;
                      let r = [t + i * telekinesisHoldDistance, o + a * telekinesisHoldDistance, n + l * telekinesisHoldDistance];
                      try {
                        const e = getTransform(safeField(getInstance(), "bodyCollider")).method("get_position").invoke().field("y").value;
                        const t = e - getTransform(getGTPlayer()).method("get_position").invoke().field("y").value;
                        r = [r[0], r[1] - t, r[2]];
                      } catch (e) {}
                      try {
                        telekinesisHeldPlayer.method("RPC_Teleport").invoke(r);
                      } catch (e) {}
                    } catch (e) {
                      releaseTelekinesisPlayer();
                    }
                  }
                } else {
                  releaseTelekinesisPlayer();
                }
              } else if (telekinesisHeldPlayer) {
                releaseTelekinesisPlayer();
              }
            }
            function getLocalRigViewSafe() {
              try {
                const e = PCClass.method("get_instance").invoke();
                if (isAlive(e)) {
                  const t = e.method("get_playerView").invoke();
                  if (isAlive(t)) {
                    return t;
                  }
                }
              } catch (e) {}
              return null;
            }
            function getLocalRigTransformSafe() {
              const e = getLocalRigViewSafe();
              if (isAlive(e)) {
                try {
                  const t = e.method("get_transform").invoke();
                  if (isAlive(t)) {
                    return t;
                  }
                } catch (e) {}
              }
              try {
                const e = getTransform(getGTPlayer());
                if (isAlive(e)) {
                  return e;
                }
              } catch (e) {}
              return null;
            }
            function setClonePhysicsDisabled(e) {
              if (isAlive(e)) {
                try {
                  const t = e.method("GetComponentsInChildren", 1).inflate(MonoBehaviour).invoke(true);
                  for (let e = 0; e < t.length; e++) {
                    try {
                      t.get(e).method("set_enabled").invoke(false);
                    } catch (e) {}
                  }
                } catch (e) {}
                try {
                  const t = e.method("GetComponentsInChildren", 1).inflate(Collider).invoke(true);
                  for (let e = 0; e < t.length; e++) {
                    try {
                      t.get(e).method("set_enabled").invoke(false);
                    } catch (e) {}
                  }
                } catch (e) {}
                try {
                  const t = e.method("GetComponentsInChildren", 1).inflate(Rigidbody).invoke(true);
                  for (let e = 0; e < t.length; e++) {
                    const o = t.get(e);
                    try {
                      o.method("set_linearVelocity").invoke([0, 0, 0]);
                    } catch (e) {}
                    try {
                      o.method("set_velocity").invoke([0, 0, 0]);
                    } catch (e) {}
                    try {
                      o.method("set_angularVelocity").invoke([0, 0, 0]);
                    } catch (e) {}
                    try {
                      o.method("set_detectCollisions").invoke(false);
                    } catch (e) {}
                    try {
                      o.method("set_isKinematic").invoke(true);
                    } catch (e) {}
                  }
                } catch (e) {}
              }
            }
            function cloneRig() {
              try {
                const e = getLocalRigViewSafe();
                if (!isAlive(e)) {
                  throw new Error("local player view is unavailable");
                }
                const t = e.method("get_gameObject").invoke();
                const o = e.method("get_transform").invoke();
                if (!isAlive(t) || !isAlive(o)) {
                  throw new Error("local rig object is unavailable");
                }
                const n = o.method("get_position").invoke();
                const i = o.method("get_rotation").invoke();
                const a = Object.method("Instantiate", 3).invoke(t, n, i);
                if (!isAlive(a)) {
                  throw new Error("Unity Instantiate returned null");
                }
                try {
                  a.method("set_name").invoke(Il2Cpp.string("SolarHP_RigClone"));
                } catch (e) {}
                setClonePhysicsDisabled(a);
                const l = a.method("get_transform").invoke();
                for (cloneFrozenTransforms.push({
                  gameObject: a,
                  transform: l
                }); cloneFrozenTransforms.length > 100;) {
                  const e = cloneFrozenTransforms.shift();
                  try {
                    if (isAlive(e?.gameObject)) {
                      Destroy(e.gameObject);
                    }
                  } catch (e) {}
                }
                console.log("[RBC] Local rig clone spawned");
                return a;
              } catch (e) {
                console.error("[RBC] cloneRig failed: " + e);
                return null;
              }
            }
            function clearRigClones() {
              rigSpawnQueueRemaining = 0;
              rigPendingFormation = null;
              cloneLineMode = false;
              cloneSquareMode = false;
              for (const e of cloneFrozenTransforms) {
                try {
                  if (isAlive(e?.gameObject)) {
                    Destroy(e.gameObject);
                  }
                } catch (e) {}
              }
              cloneFrozenTransforms = [];
              cloneFollowTransforms = [];
              cloneOrbiters = [];
            }
            function startCloneSpawnSequence() {
              cloneSpawnActive = true;
              cloneRig();
              cloneSpawnActive = false;
            }
            function setRigCloneCount(e) {
              sendNotification("Rig count: " + (rigCloneCount = Math.max(1, Math.min(100, rigCloneCount + e))), false);
            }
            function spawnRigFormation(e, t) {
              clearRigClones();
              rigCloneCount = Math.max(1, Math.min(100, Math.floor(e)));
              rigPendingFormation = t;
              rigSpawnQueueRemaining = rigCloneCount;
              cloneOrbitActive = false;
              cloneFollowActive = false;
              cloneLineMode = t === "line";
              cloneSquareMode = t === "square";
              sendNotification("Spawning " + rigCloneCount + " rigs in a " + t, false);
            }
            function startCloneFollow() {
              cloneOrbitActive = false;
              cloneLineMode = false;
              cloneSquareMode = false;
              cloneFollowActive = true;
            }
            function stopCloneFollow() {
              cloneFollowActive = false;
            }
            function startCloneOrbit() {
              cloneFollowActive = false;
              cloneLineMode = false;
              cloneSquareMode = false;
              cloneOrbitActive = true;
            }
            function stopCloneOrbit() {
              cloneOrbitActive = false;
            }
            function setLocalRigRagdoll(e) {
              try {
                const t = getLocalRigViewSafe();
                if (!isAlive(t)) {
                  return;
                }
                const o = getPcPlayerRigidbody();
                const n = safeField(getInstance(), "headCollider");
                const i = isAlive(n) ? getTransform(n) : getLocalRigTransformSafe();
                if (!isAlive(o) || !isAlive(i)) {
                  return;
                }
                t.method("SetRagdoll", 4).invoke(e, [0, 0, 0], o, i);
              } catch (e) {
                console.error("[RBC] SetRagdoll failed: " + e);
              }
            }
            function updateGrabbedRig() {
              if (rigGrabActive) {
                try {
                  const e = getLocalRigViewSafe();
                  if (!isAlive(e)) {
                    return;
                  }
                  const t = safeField(e, "_cameraTransform");
                  if (!isAlive(t)) {
                    return;
                  }
                  let o = null;
                  if (rightGrab) {
                    o = safeField(getInstance(), "rightHandTransform");
                  } else if (leftGrab) {
                    o = safeField(getInstance(), "leftHandTransform");
                  } else {
                    const e = safeField(getInstance(), "headCollider");
                    o = isAlive(e) ? getTransform(e) : null;
                  }
                  if (!isAlive(o)) {
                    return;
                  }
                  t.method("set_position").invoke(o.method("get_position").invoke());
                  t.method("set_rotation").invoke(o.method("get_rotation").invoke());
                } catch (e) {}
              }
            }
            function resetGrabbedRig() {
              try {
                const e = getLocalRigViewSafe();
                const t = isAlive(e) ? safeField(e, "_cameraTransform") : null;
                const o = safeField(getInstance(), "headCollider");
                const n = isAlive(o) ? getTransform(o) : null;
                if (!isAlive(t) || !isAlive(n)) {
                  return;
                }
                t.method("set_position").invoke(n.method("get_position").invoke());
                t.method("set_rotation").invoke(n.method("get_rotation").invoke());
              } catch (e) {}
            }
            function updateRigMods(e) {
              if (rigSpawnQueueRemaining > 0) {
                const e = Math.min(2, rigSpawnQueueRemaining);
                for (let t = 0; t < e; t++) {
                  cloneRig();
                }
                if ((rigSpawnQueueRemaining -= e) <= 0) {
                  rigPendingFormation = null;
                }
              }
              if (triggerCloneSpawnActive && rightTrigger && !triggerCloneWasPressed && e >= triggerCloneSpawnCooldown) {
                triggerCloneSpawnCooldown = e + 0.2;
                spawnRigWithEX();
              }
              triggerCloneWasPressed = rightTrigger;
              for (let e = cloneFrozenTransforms.length - 1; e >= 0; e--) {
                const t = cloneFrozenTransforms[e];
                if (!isAlive(t?.gameObject) || !isAlive(t?.transform)) {
                  cloneFrozenTransforms.splice(e, 1);
                }
              }
              const t = getLocalRigTransformSafe();
              if (!isAlive(t) || cloneFrozenTransforms.length === 0) {
                updateGrabbedRig();
                return;
              }
              const o = t.method("get_position").invoke();
              const n = t.method("get_rotation").invoke();
              const i = o.field("x").value;
              const a = o.field("y").value;
              const l = o.field("z").value;
              let r = [0, 0, 1];
              let s = [1, 0, 0];
              try {
                const e = t.method("get_forward").invoke();
                r = [e.field("x").value, e.field("y").value, e.field("z").value];
              } catch (e) {}
              try {
                const e = t.method("get_right").invoke();
                s = [e.field("x").value, e.field("y").value, e.field("z").value];
              } catch (e) {}
              if (cloneOrbitActive || cloneFollowActive || cloneLineMode || cloneSquareMode) {
                const t = cloneFrozenTransforms.length;
                for (let o = 0; o < t; o++) {
                  const c = cloneFrozenTransforms[o].transform;
                  let d;
                  if (cloneOrbitActive) {
                    const n = e * 1.25 + o / Math.max(1, t) * Math.PI * 2;
                    d = [i + Math.sin(n) * 2, a, l + Math.cos(n) * 2];
                  } else if (cloneSquareMode) {
                    const e = Math.ceil(Math.sqrt(t));
                    const n = Math.ceil(t / e);
                    const c = (o % e - (e - 1) / 2) * 0.8;
                    const u = (Math.floor(o / e) - (n - 1) / 2) * 0.8 + 2.5;
                    d = [i + s[0] * c - r[0] * u, a, l + s[2] * c - r[2] * u];
                  } else if (cloneLineMode) {
                    const e = (o - (t - 1) / 2) * 0.8;
                    d = [i + s[0] * e - r[0] * 1.5, a, l + s[2] * e - r[2] * 1.5];
                  } else {
                    const e = 1.25 + o * 0.75;
                    d = [i - r[0] * e, a - r[1] * e, l - r[2] * e];
                  }
                  try {
                    c.method("set_position").invoke(d);
                    c.method("set_rotation").invoke(n);
                  } catch (e) {}
                }
              }
              updateGrabbedRig();
            }
            function updateRigMods(e) {
              if (rigSpawnQueueRemaining > 0) {
                const e = Math.min(2, rigSpawnQueueRemaining);
                for (let t = 0; t < e; t++) {
                  cloneRig();
                }
                if ((rigSpawnQueueRemaining -= e) <= 0) {
                  rigPendingFormation = null;
                }
              }
              if (cloneGridSpawnQueue > 0) {
                const e = Math.min(2, cloneGridSpawnQueue);
                for (let t = 0; t < e; t++) {
                  const e = cloneGridOffsets[cloneGridOffsets.length - cloneGridSpawnQueue];
                  spawnOneGridClone(e);
                  cloneGridSpawnQueue--;
                }
                if (cloneGridSpawnQueue <= 0) {
                  sendNotification("5×5 Grid ready!", false);
                }
              }
              if (triggerCloneSpawnActive && rightTrigger && !triggerCloneWasPressed && e >= triggerCloneSpawnCooldown) {
                triggerCloneSpawnCooldown = e + 0.2;
                spawnRigWithEX();
              }
              triggerCloneWasPressed = rightTrigger;
              for (let e = cloneFrozenTransforms.length - 1; e >= 0; e--) {
                const t = cloneFrozenTransforms[e];
                if (!isAlive(t?.gameObject) || !isAlive(t?.transform)) {
                  cloneFrozenTransforms.splice(e, 1);
                }
              }
              const t = getLocalRigTransformSafe();
              if (!isAlive(t) || cloneFrozenTransforms.length === 0) {
                updateGrabbedRig();
                return;
              }
              const o = t.method("get_position").invoke();
              const n = t.method("get_rotation").invoke();
              const i = o.field("x").value;
              const a = o.field("y").value;
              const l = o.field("z").value;
              let r = [0, 0, 1];
              let s = [1, 0, 0];
              try {
                const e = t.method("get_forward").invoke();
                r = [e.field("x").value, e.field("y").value, e.field("z").value];
              } catch (e) {}
              try {
                const e = t.method("get_right").invoke();
                s = [e.field("x").value, e.field("y").value, e.field("z").value];
              } catch (e) {}
              if (cloneGridCopyActive) {
                for (let e = 0; e < cloneFrozenTransforms.length; e++) {
                  const t = cloneFrozenTransforms[e];
                  if (!t || !isAlive(t.transform)) {
                    continue;
                  }
                  const o = t.offset || [0, 0, 0];
                  const r = [i + o[0], a + o[1], l + o[2]];
                  try {
                    t.transform.method("set_position").invoke(r);
                    t.transform.method("set_rotation").invoke(n);
                  } catch (e) {}
                }
              } else if (cloneOrbitActive || cloneFollowActive || cloneLineMode || cloneSquareMode) {
                const t = cloneFrozenTransforms.length;
                for (let o = 0; o < t; o++) {
                  const c = cloneFrozenTransforms[o].transform;
                  let d;
                  if (cloneOrbitActive) {
                    const n = e * 1.25 + o / Math.max(1, t) * Math.PI * 2;
                    d = [i + Math.sin(n) * 2, a, l + Math.cos(n) * 2];
                  } else if (cloneSquareMode) {
                    const e = Math.ceil(Math.sqrt(t));
                    const n = Math.ceil(t / e);
                    const c = (o % e - (e - 1) / 2) * 0.8;
                    const u = (Math.floor(o / e) - (n - 1) / 2) * 0.8 + 2.5;
                    d = [i + s[0] * c - r[0] * u, a, l + s[2] * c - r[2] * u];
                  } else if (cloneLineMode) {
                    const e = (o - (t - 1) / 2) * 0.8;
                    d = [i + s[0] * e - r[0] * 1.5, a, l + s[2] * e - r[2] * 1.5];
                  } else {
                    const e = 1.25 + o * 0.75;
                    d = [i - r[0] * e, a - r[1] * e, l - r[2] * e];
                  }
                  try {
                    c.method("set_position").invoke(d);
                    c.method("set_rotation").invoke(n);
                  } catch (e) {}
                }
              }
              updateGrabbedRig();
            }
            function n5EnableSpawnGuardBypass() {}
            function M(e) {
              const t = String(e || "").replace(/^.*\//, "");
              const o = {
                RadarPartSpawner: "FourLeafRadar_Spawner",
                NetworkedLever_SecretLeft: "NetworkedWorldLever",
                BarrelBeansStatic: "BarrelBeansDynamic",
                BarrelExplodingStatic: "BarrelExplodingDynamic",
                BarrelOilStatic: "BarrelOilDynamic",
                SlenderMonster: "ShadowController",
                ArmstrongSpaceController: "ArmstrongControllerSpace"
              }[t] || t;
              if (!o) {
                return null;
              }
              try {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.E_NetworkObject");
                  const t = AssemblyCSharp.class("AnimalCompany.EnumExtension_NetworkObject");
                  const n = e.field(o).value;
                  const i = t.method("GetObject", 1).invoke(n);
                  if (i && (!i.handle || !i.handle.isNull())) {
                    return i;
                  }
                } catch (e) {}
                const e = AssemblyCSharp.class("AnimalCompany.NetworkPrefabSheet");
                const t = e.method("get_globalRefs").invoke();
                if (t && (!t.handle || !t.handle.isNull())) {
                  const e = [o];
                  if (o === "ArmstrongSpaceController") {
                    e.push("ArmstrongControllerSpace");
                  }
                  if (o === "Smiley") {
                    e.push("SmileyController");
                  }
                  for (const o of e) {
                    try {
                      const e = t.field(o).value;
                      if (e && (!e.handle || !e.handle.isNull())) {
                        return e;
                      }
                    } catch (e) {}
                  }
                  try {
                    for (const e of t.class.fields) {
                      if (e.isStatic) {
                        continue;
                      }
                      const n = String(e.name || "");
                      if (n !== o && !n.toLowerCase().includes(o.toLowerCase())) {
                        continue;
                      }
                      const i = e.bind(t).value;
                      if (i && (!i.handle || !i.handle.isNull())) {
                        return i;
                      }
                    }
                  } catch (e) {}
                }
                const n = e.method("get_instance").invoke();
                if (n && (!n.handle || !n.handle.isNull())) {
                  const e = n.method("GetLength").invoke();
                  for (let t = 0; t < e; t++) {
                    try {
                      const e = n.method("Get", 1).invoke(t);
                      if (!e || e.handle && e.handle.isNull()) {
                        continue;
                      }
                      const i = String(e.method("get_name").invoke());
                      if (i === o || i.toLowerCase().includes(o.toLowerCase())) {
                        return e;
                      }
                    } catch (e) {}
                  }
                }
              } catch (e) {}
              return null;
            }
            function N(e, t, o, n, i = null, a = null, l = 0) {
              if (!e || e.isNull() || !t || t.handle && t.handle.isNull()) {
                return null;
              }
              const r = i || NULL;
              const s = e => {
                if (e.class.isEnum || e.isPrimitive) {
                  return 0;
                }
                if (!e.class.isValueType) {
                  return NULL;
                }
                const t = e.class.fields.filter(e => !e.isStatic);
                if (t.length === 0) {
                  return 0;
                } else {
                  return t.map(e => s(e.type));
                }
              };
              const c = (e, t) => {
                if (typeof t == "boolean") {
                  if (t) {
                    return 1;
                  } else {
                    return 0;
                  }
                }
                if (t instanceof Il2Cpp.ValueType) {
                  const o = e.class.fields.filter(e => !e.isStatic);
                  if (o.length === 0) {
                    return 0;
                  } else {
                    return o.map(e => c(e.type, e.bind(t).value));
                  }
                }
                if (Array.isArray(t)) {
                  return t.map(t => c(e, t));
                } else {
                  return t;
                }
              };
              const d = (e, t, o) => e.class.fields.filter(e => !e.isStatic).map(e => {
                const n = e.name.toLowerCase();
                if (n.includes("hasvalue")) {
                  if (t) {
                    return 1;
                  } else {
                    return 0;
                  }
                } else if (n === "value" && t) {
                  return c(e.type, o);
                } else {
                  return s(e.type);
                }
              });
              try {
                const i = e.method("Spawn", 6).overload("Fusion.NetworkObject", "System.Nullable<UnityEngine.Vector3>", "System.Nullable<UnityEngine.Quaternion>", "System.Nullable<Fusion.PlayerRef>", "Fusion.NetworkRunner.OnBeforeSpawned", "Fusion.NetworkSpawnFlags");
                const s = i.parameters;
                const c = i.bind(e).invoke(t, d(s[1].type, true, o), d(s[2].type, true, n), d(s[3].type, a != null, a), r, l);
                if (c && (!c.handle || !c.handle.isNull())) {
                  return c;
                }
              } catch (e) {}
              const u = e.method("Spawn").overloads();
              for (const i of u) {
                try {
                  if (i.parameterCount !== 6 || i.isGeneric) {
                    continue;
                  }
                  const s = i.parameters;
                  const c = String(s[0].type.name || "");
                  if (c !== "Fusion.NetworkObject" && c !== "NetworkObject" && !c.endsWith(".NetworkObject") || !s[1].type.name.includes("Nullable") || !s[1].type.name.includes("Vector3") || !s[2].type.name.includes("Nullable") || !s[2].type.name.includes("Quaternion") || !s[3].type.name.includes("Nullable") || !s[3].type.name.includes("PlayerRef") || !s[4].type.name.includes("OnBeforeSpawned") || !s[5].type.name.includes("NetworkSpawnFlags")) {
                    continue;
                  }
                  return i.bind(e).invoke(t, d(s[1].type, true, o), d(s[2].type, true, n), d(s[3].type, a != null, a), r, l);
                } catch (e) {}
              }
              return null;
            }
            function O(e, t, o, n = null) {
              const i = String(e || "").replace(/^mob_prefab\//, "");
              try {
                n5EnableSpawnGuardBypass();
                let a = null;
                try {
                  a = NManager.method("get_instance").invoke().method("get_currentRunner").invoke();
                } catch (e) {}
                if (!a || a.isNull()) {
                  try {
                    a = PrefabGen.field("_instance").value.method("get_runner").invoke();
                  } catch (e) {}
                }
                if (!a || a.isNull()) {
                  return null;
                }
                const l = M(e);
                if (l) {
                  let e = null;
                  if (i === "NetPlayer") {
                    try {
                      e = n || a.method("get_LocalPlayer").invoke();
                    } catch (e) {}
                  }
                  const r = N(a, l, t, o, null, e, i === "NetPlayer" ? 4 : 0);
                  if (r) {
                    return r;
                  }
                }
                const r = a.field("_config").value.field("PrefabTable").value.field("_sources").value;
                const s = r.method("get_Count").invoke();
                for (let l = 0; l < s; l++) {
                  try {
                    const s = r.method("get_Item").invoke(l);
                    if (!String(s.method("get_Description").invoke()).includes(e)) {
                      continue;
                    }
                    const c = s.method("WaitForResult").invoke();
                    if (!c || c.isNull()) {
                      return null;
                    }
                    const d = e => {
                      if (e.class.isEnum || e.isPrimitive) {
                        return 0;
                      }
                      if (!e.class.isValueType) {
                        return NULL;
                      }
                      const t = e.class.fields.filter(e => !e.isStatic);
                      if (t.length === 0) {
                        return 0;
                      } else {
                        return t.map(e => d(e.type));
                      }
                    };
                    const u = (e, t, o) => e.class.fields.filter(e => !e.isStatic).map(e => {
                      const n = e.name.toLowerCase();
                      if (n.includes("hasvalue")) {
                        if (t) {
                          return 1;
                        } else {
                          return 0;
                        }
                      } else if (n === "value" && t) {
                        return o;
                      } else {
                        return d(e.type);
                      }
                    });
                    const m = (e, t) => {
                      if (typeof t == "boolean") {
                        if (t) {
                          return 1;
                        } else {
                          return 0;
                        }
                      }
                      if (t instanceof Il2Cpp.ValueType) {
                        const o = e.class.fields.filter(e => !e.isStatic);
                        if (o.length === 0) {
                          return 0;
                        } else {
                          return o.map(e => m(e.type, e.bind(t).value));
                        }
                      }
                      if (Array.isArray(t)) {
                        return t.map(t => m(e, t));
                      } else {
                        return t;
                      }
                    };
                    const h = (e, t) => u(e, true, m(t.type, t));
                    let g = null;
                    for (const e of a.method("Spawn").overloads()) {
                      if (e.parameterCount !== 6 || e.isGeneric) {
                        continue;
                      }
                      const t = e.parameters;
                      if (t[0].type.name.includes("NetworkObject") && t[1].type.name.includes("Nullable") && t[2].type.name.includes("Nullable") && t[3].type.name.includes("Nullable") && t[4].type.name.includes("OnBeforeSpawned")) {
                        g = e;
                        break;
                      }
                    }
                    if (!g) {
                      return null;
                    }
                    let f = null;
                    if (i === "NetPlayer") {
                      try {
                        f = n || a.method("get_LocalPlayer").invoke();
                      } catch (e) {}
                    }
                    return g.bind(a).invoke(c, h(g.parameters[1], t), h(g.parameters[2], o), u(g.parameters[3], f != null, f), NULL, i === "NetPlayer" ? 4 : 0);
                  } catch (e) {}
                }
              } catch (e) {}
              return null;
            }
            function n5NullOrDead(e) {
              try {
                return !e || e.handle && e.handle.isNull && e.handle.isNull() || e.isNull && e.isNull();
              } catch (e) {}
              return !e;
            }
            function isAlive(e) {
              return !n5NullOrDead(e);
            }
            function n5GetLocalNetPlayerSafe() {
              try {
                return NetPlayer.method("get_localPlayer").invoke();
              } catch (e) {}
              try {
                return Il2Cpp.domain.assembly("AnimalCompany").image.class("AnimalCompany.NetPlayer").method("get_localPlayer").invoke();
              } catch (e) {}
              return null;
            }
            function F(e) {
              return e.method("get_transform").invoke();
            }
            function aG(e) {
              if (!e) {
                return null;
              }
              try {
                const t = e.method("get_gameObject").invoke();
                if (t && !t.isNull()) {
                  return t.method("get_transform").invoke();
                }
              } catch (e) {}
              try {
                return e.method("get_transform").invoke();
              } catch (e) {}
              return null;
            }
            function eK(e) {
              try {
                return e.method("get_view").invoke();
              } catch (e) {}
              try {
                return e.method("get_playerView").invoke();
              } catch (e) {}
              return null;
            }
            function eL(e, t) {
              try {
                t.field("_player").value = e;
              } catch (o) {
                try {
                  t.field("player").value = e;
                } catch (e) {}
              }
            }
            function hg(e) {
              try {
                return UnityEngineCore.class(e);
              } catch (e) {
                return null;
              }
            }
            function fG(e, t) {
              if (!e || !t) {
                return null;
              }
              let o = null;
              try {
                o = e.method("get_gameObject").invoke();
              } catch (e) {}
              const n = [e, o];
              for (const e of n) {
                if (!!e && (!e.handle || !e.handle.isNull())) {
                  try {
                    const o = e.method("GetComponent", 1).inflate(t).invoke();
                    if (o && !o.handle.isNull()) {
                      return o;
                    }
                  } catch (e) {}
                  try {
                    const o = e.method("GetComponentInChildren", 1).inflate(t).invoke(true);
                    if (o && !o.handle.isNull()) {
                      return o;
                    }
                  } catch (e) {}
                  try {
                    const o = e.method("GetComponentInParent", 1).inflate(t).invoke();
                    if (o && !o.handle.isNull()) {
                      return o;
                    }
                  } catch (e) {}
                }
              }
              return null;
            }
            function n5VecNum(e, t, o) {
              try {
                let n = e[t];
                if (n == null && e.field) {
                  n = e.field(o).value;
                }
                return Number(n || 0);
              } catch (e) {
                return 0;
              }
            }
            function n5IsGoodPosition(e) {
              try {
                if (!e) {
                  return false;
                }
                const t = n5VecNum(e, 0, "x");
                const o = n5VecNum(e, 1, "y");
                const n = n5VecNum(e, 2, "z");
                return !!isFinite(t) && !!isFinite(o) && !!isFinite(n) && !(Math.abs(t) + Math.abs(o) + Math.abs(n) < 0.01) && !(Math.abs(t) > 100000) && !(Math.abs(o) > 100000) && !(Math.abs(n) > 100000);
              } catch (e) {
                return false;
              }
            }
            function n5GetSafeSelfPosition() {
              try {
                const e = getTransform(safeField(getInstance(), "headCollider")).method("get_position").invoke();
                if (n5IsGoodPosition(e)) {
                  return e;
                }
              } catch (e) {}
              try {
                const e = PCClass.method("get_instance").invoke();
                if (e && !e.handle.isNull()) {
                  const t = getTransform(e).method("get_position").invoke();
                  if (n5IsGoodPosition(t)) {
                    return t;
                  }
                }
              } catch (e) {}
              try {
                const e = n5GetLocalNetPlayerSafe();
                if (e && !e.handle.isNull()) {
                  const t = getTransform(e).method("get_position").invoke();
                  if (n5IsGoodPosition(t)) {
                    return t;
                  }
                }
              } catch (e) {}
              return [0, 0, 0];
            }
            function eH() {
              try {
                const e = n5GetSafeSelfPosition();
                if (n5IsGoodPosition(e)) {
                  return e;
                }
              } catch (e) {}
              try {
                const e = NetPlayer.method("get_localPlayer").invoke();
                if (e && (!e.handle || !e.handle.isNull())) {
                  return F(e).method("get_position").invoke();
                }
              } catch (e) {}
              return zeroVector;
            }
            function eW(e, t) {
              try {
                if (n5NullOrDead(e)) {
                  return false;
                }
                const o = n5GetLocalNetPlayerSafe();
                const n = eK(e);
                let i = null;
                try {
                  i = e.method("get_gameObject").invoke();
                } catch (e) {}
                if (n5NullOrDead(i) && !n5NullOrDead(n)) {
                  try {
                    i = n.method("get_gameObject").invoke();
                  } catch (e) {}
                }
                if (!n5NullOrDead(i)) {
                  try {
                    i.method("SetActive").invoke(true);
                  } catch (e) {}
                }
                const a = aG(e) || (n5NullOrDead(n) ? null : F(n));
                if (a) {
                  try {
                    a.method("set_position").invoke(t);
                  } catch (e) {}
                  try {
                    if (!n5NullOrDead(o)) {
                      a.method("set_rotation").invoke(getTransform(o).method("get_rotation").invoke());
                    }
                  } catch (e) {}
                }
                if (!n5NullOrDead(o) && !n5NullOrDead(n)) {
                  eL(o, n);
                  for (const e of ["HandleAvatarDataChanged", "HandleRigChanged", "UpdateAvatar", "UpdateBodyView", "HandlePlayerScaleChanged", "HandlePlayerColorChanged"]) {
                    try {
                      n.method(e).invoke();
                    } catch (e) {}
                  }
                }
                if (!n5NullOrDead(i)) {
                  for (const e of ["UnityEngine.Renderer", "UnityEngine.MeshRenderer", "UnityEngine.SkinnedMeshRenderer"]) {
                    try {
                      const t = hg(e);
                      if (!t) {
                        continue;
                      }
                      const o = i.method("GetComponentsInChildren", 1).inflate(t).invoke(true);
                      for (let e = 0; e < o.length; e++) {
                        try {
                          const t = o.get(e);
                          if (t && !t.handle.isNull()) {
                            t.method("set_enabled").invoke(true);
                          }
                        } catch (e) {}
                      }
                    } catch (e) {}
                  }
                }
                return true;
              } catch (e) {
                console.error("[N5 Rig Spammer visible]", e);
              }
              return false;
            }
            function spawnRigWithEX() {
              if (rightGrab) {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (n5NullOrDead(e)) {
                    return;
                  }
                  let t = null;
                  try {
                    t = e.method("get_playerRef").invoke();
                  } catch (e) {}
                  const o = O("NetPlayer", zeroVector, identityQuaternion, t);
                  if (n5NullOrDead(o)) {
                    sendNotification("Rig Spammer failed", false);
                    return;
                  }
                  let n = null;
                  try {
                    n = fG(o, NetworkObjectClass);
                  } catch (e) {}
                  if (n5NullOrDead(n)) {
                    n = o;
                  }
                  try {
                    n.method("ReleaseStateAuthority").invoke();
                  } catch (e) {}
                  const i = eH();
                  eW(o, [(i[0] || 0) + (Math.random() - 0.5) * 1.8, (i[1] || 0) + 0.05 + Math.random() * 0.8, (i[2] || 0) + (Math.random() - 0.5) * 1.8]);
                  sendNotification("Rig Spammer spawned", false);
                } catch (e) {
                  console.error("[EX Rig Spammer]", e);
                  sendNotification("Rig Spammer error", false);
                }
              }
            }
            let _vec3MulV3F = null;
            function vec3Mul(e, t) {
              if (!_vec3MulV3F) {
                try {
                  for (const e of Vector3.method("op_Multiply").overloads()) {
                    if (e.isGeneric) {
                      continue;
                    }
                    const t = e.parameters;
                    if (t.length !== 2) {
                      continue;
                    }
                    if (!t[0].type.name.includes("Vector3")) {
                      continue;
                    }
                    const o = t[1].type.name;
                    if (o.includes("Single") || o.includes("Float") || o === "System.Single") {
                      _vec3MulV3F = e;
                      break;
                    }
                  }
                } catch (e) {}
              }
              if (_vec3MulV3F) {
                try {
                  return _vec3MulV3F.invoke(e, t);
                } catch (e) {}
              }
              try {
                const o = e.field("x").value * t;
                const n = e.field("y").value * t;
                return [o, n, e.field("z").value * t];
              } catch (t) {
                return e;
              }
            }
            function applyRainbowToSpawnedObject(e, t, o) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                const n = e => {
                  if (!e || e.isNull?.()) {
                    return false;
                  }
                  try {
                    e.method("set_colorHue").invoke(t);
                  } catch (e) {
                    return false;
                  }
                  try {
                    e.method("set_colorSaturation").invoke(o);
                  } catch (e) {
                    return false;
                  }
                  return true;
                };
                if (n(e)) {
                  return true;
                }
                let i = null;
                try {
                  i = e.method("get_gameObject").invoke();
                } catch (e) {}
                if (!i || i.isNull?.()) {
                  try {
                    const t = e.method("get_transform").invoke();
                    if (t && !t.isNull?.()) {
                      i = t.method("get_gameObject").invoke();
                    }
                  } catch (e) {}
                }
                if (i && !i.isNull?.()) {
                  let e = getComponent(i, GBOClass);
                  if (!e || e.isNull?.()) {
                    try {
                      e = i.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                    } catch (t) {
                      e = null;
                    }
                  }
                  if (e && !e.isNull?.()) {
                    n(e);
                    return true;
                  }
                  try {
                    const e = i.method("GetComponentsInChildren", 1).inflate(GBOClass).invoke(true);
                    if (e) {
                      for (let t = 0; t < e.length; t++) {
                        n(e.get(t));
                      }
                      return true;
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              return false;
            }
            function applyScaleToSpawnedObject(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                const o = e => {
                  if (!e || e.isNull?.()) {
                    return false;
                  }
                  try {
                    e.method("set_scaleModifier").invoke(t);
                  } catch (e) {}
                  try {
                    e.field("_scaleModifier").value = t;
                  } catch (e) {}
                  return true;
                };
                o(e);
                let n = null;
                try {
                  n = e.method("get_gameObject").invoke();
                } catch (e) {}
                if (!n || n.isNull?.()) {
                  try {
                    const t = e.method("get_transform").invoke();
                    if (t && !t.isNull?.()) {
                      n = t.method("get_gameObject").invoke();
                    }
                  } catch (e) {}
                }
                if (n && !n.isNull?.()) {
                  let e = getComponent(n, GBOClass);
                  if (!e || e.isNull()) {
                    try {
                      e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                    } catch (t) {
                      e = null;
                    }
                  }
                  if (e && !e.isNull?.()) {
                    o(e);
                    return true;
                  }
                  try {
                    const e = n.method("GetComponentsInChildren", 1).inflate(GBOClass).invoke(true);
                    if (e) {
                      for (let t = 0; t < e.length; t++) {
                        o(e.get(t));
                      }
                      return true;
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              return false;
            }
            function findLocalBackpack(e) {
              try {
                if (!e || e.isNull?.()) {
                  return null;
                }
                const t = getTransform(e);
                if (t && !t.isNull?.()) {
                  try {
                    const e = t.method("get_gameObject").invoke();
                    if (e && !e.isNull()) {
                      const t = e.method("GetComponentInChildren", 1).inflate(BackpackItemClass).invoke(true);
                      if (t && !t.isNull()) {
                        return t;
                      }
                    }
                  } catch (e) {}
                }
                const o = getTransform(e).method("get_position").invoke();
                const n = Object.method("FindObjectsOfType", 1).inflate(BackpackItemClass).invoke(0);
                if (!n || n.isNull()) {
                  return null;
                }
                let i = null;
                let a = 1000000000000;
                for (let e = 0; e < n.length; e++) {
                  try {
                    const t = n.get(e);
                    if (!t || t.isNull()) {
                      continue;
                    }
                    const l = t.method("get_gameObject").invoke();
                    if (!l || l.isNull()) {
                      continue;
                    }
                    const r = getTransform(l).method("get_position").invoke();
                    const s = Vector3.method("Distance").invoke(o, r);
                    if (typeof s == "number" && s < a) {
                      a = s;
                      i = t;
                    }
                  } catch (e) {}
                }
                return i;
              } catch (e) {
                return null;
              }
            }
            const BackpackDupe = AssemblyCSharp.class("AnimalCompany.BackpackItem").method("RemoveItem");
            BackpackDupe.implementation = function (e) {
              if (backpackDupeEnabled) {
                return false;
              }
              this.method("RemoveItem").invoke(e);
            };
            try {
              const e = AssemblyCSharp.class("AnimalCompany.MiningLaser").method("OnUpdate");
              e.implementation = function () {
                const t = e.invokeRaw(this);
                if (noLaserOverheat) {
                  try {
                    this.field("_overheatPenaltyDelay").value = 0;
                  } catch (e) {}
                  try {
                    this.field("_regenDelay").value = 0;
                  } catch (e) {}
                  try {
                    this.field("_hitInterval").value = 0;
                  } catch (e) {}
                  try {
                    this.field("__didOverheat").value = false;
                  } catch (e) {}
                  try {
                    this.field("__fuel").value = this.field("_maxFuel").value;
                  } catch (e) {}
                  try {
                    this.field("_consumedFuelPerSecond").value = 0;
                  } catch (e) {}
                  try {
                    this.field("_regenFuelPerSecond").value = 999;
                  } catch (e) {}
                }
                if (infiniteLaserRange) {
                  try {
                    this.field("_maxDistance").value = 9999;
                  } catch (e) {}
                }
                return t;
              };
            } catch (e) {}
            function getRightFingers(e) {
              try {
                const t = e.method("get_view").invoke();
                if (!t || t.isNull()) {
                  return null;
                }
                const o = t.field("_fingerViews").value;
                if (!o || o.isNull() || o.length < 2) {
                  return null;
                } else {
                  return o.get(1);
                }
              } catch (e) {
                return null;
              }
            }
            function toggleColliders(e) {
              const t = Object.method("FindObjectsOfType").inflate(MeshCollider).invoke();
              for (let o = 0; o < t.length; o++) {
                t.get(o).method("set_enabled").invoke(e);
              }
            }
            function tryMethod(e, t, o) {
              try {
                if (!e || e.handle.isNull()) {
                  return null;
                } else if (o == null) {
                  return e.method(t);
                } else {
                  return e.method(t, o);
                }
              } catch (e) {
                return null;
              }
            }
            function getNetworkObjectSafe(e) {
              if (!e || e.isNull()) {
                return null;
              }
              try {
                if (e.class && e.class.name === "NetworkObject") {
                  return e;
                }
              } catch (e) {}
              const t = [() => e.method("get_Object").invoke(), () => e.method("get_NetworkObject").invoke(), () => e.method("get_networkObject").invoke(), () => e.method("GetComponent", 1).inflate(Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkObject")).invoke()];
              for (const e of t) {
                try {
                  const t = e();
                  if (t && !t.isNull()) {
                    return t;
                  }
                } catch (e) {}
              }
              try {
                const t = e.method("get_gameObject").invoke();
                if (t && !t.isNull()) {
                  const e = t.method("GetComponent", 1).inflate(Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkObject")).invoke();
                  if (e && !e.isNull()) {
                    return e;
                  }
                }
              } catch (e) {}
              return null;
            }
            function findSpawnedItemNear(e, t, o = 1.5) {
              try {
                const n = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                let i = null;
                let a = o;
                for (let o = 0; o < n.length; o++) {
                  try {
                    const l = n.get ? n.get(o) : n[o];
                    if (!l || l.isNull?.()) {
                      continue;
                    }
                    if (e && !grabbableMatchesItemId(l, e)) {
                      continue;
                    }
                    const r = getTransform(l).method("get_position").invoke();
                    const s = Vector3.method("Distance").invoke(t, r);
                    if (s <= a) {
                      i = l;
                      a = s;
                    }
                  } catch (e) {}
                }
                return i;
              } catch (e) {
                return null;
              }
            }
            function requestStateAuthoritySafe(e) {
              if (e && !e.isNull()) {
                try {
                  const t = getNetworkObjectSafe(e);
                  if (t && !t.isNull()) {
                    t.method("RequestStateAuthority").invoke();
                  }
                } catch (e) {}
              }
            }
            const blockedContainerItemIDs = {
              item_crate: true
            };
            function despawnNetworkThing(e) {
              if (e && !e.isNull()) {
                try {
                  let t = getNetworkObjectSafe(e) || e;
                  const o = t.method("get_Runner").invoke();
                  if (o && !o.isNull()) {
                    o.method("Despawn").invoke(t);
                  }
                } catch (e) {}
              }
            }
            function getContainerComponent(e) {
              if (!e || e.isNull()) {
                return null;
              }
              const t = [QuiverClass, BackpackItemClass, CrossbowClass, HeartGunClass, GrenadeLauncherClass, SalmonCannonClass];
              for (const o of t) {
                if (o) {
                  try {
                    const t = e.method("GetComponent", 1).inflate(o).invoke();
                    if (t && !t.isNull()) {
                      return t;
                    }
                  } catch (e) {}
                  try {
                    const t = e.method("GetComponentInChildren", 1).inflate(o).invoke();
                    if (t && !t.isNull()) {
                      return t;
                    }
                  } catch (e) {}
                }
              }
              return null;
            }
            function tryAddItemToContainer(e, t) {
              if (!e || e.isNull() || !t || t.isNull()) {
                return false;
              }
              requestStateAuthoritySafe(e);
              requestStateAuthoritySafe(t);
              const o = (() => {
                try {
                  return t.method("GetComponent", 1).inflate(GBIClass).invoke();
                } catch (e) {
                  return null;
                }
              })();
              const n = [() => o && !o.isNull() && o.method("AddToBag").invoke(e), () => o && !o.isNull() && o.method("AddToBagInternal").invoke(e), () => o && !o.isNull() && o.method("AddToQuiver").invoke(e), () => e.method("TryAddItem").invoke(t)];
              for (const e of n) {
                try {
                  if (e() !== false) {
                    return true;
                  }
                } catch (e) {}
              }
              return false;
            }
            function getQuiverFillPool() {
              return itemIDs.filter(e => !blockedContainerItemIDs[e]);
            }
            function fillContainerWithItems(e, t, o, n) {
              let i = 0;
              let a = 0;
              while (i < o && a < o * 12) {
                a++;
                const o = n(i, a);
                if (!o) {
                  continue;
                }
                const l = spawnItemPrefab(o, t, identityQuaternion);
                if (l && !l.isNull()) {
                  try {
                    const e = l.method("GetComponent", 1).inflate(GBOClass).invoke();
                    if (e && !e.isNull()) {
                      e.method("set_scaleModifier").invoke(randInt(-50, 50));
                      e.method("set_colorHue").invoke(randInt(-127, 127));
                      e.method("set_colorSaturation").invoke(randInt(-20, 127));
                    }
                  } catch (e) {}
                  if (tryAddItemToContainer(e, l)) {
                    i++;
                  } else {
                    try {
                      despawnNetworkThing(l);
                    } catch (e) {}
                  }
                }
              }
              return i;
            }
            function spawnFilledQuiver(e = null, t = null, o = null) {
              try {
                const n = t || leftHandTransform.method("get_position").invoke();
                const i = o || identityQuaternion;
                const a = ["item_quiver", "item_quiver_heart"];
                const l = spawnItemPrefab(a[randInt(0, a.length - 1)], n, i);
                if (!l || l.isNull()) {
                  return null;
                }
                const r = l.method("GetComponent", 1).inflate(GBOClass).invoke();
                if (r && !r.isNull()) {
                  r.method("set_scaleModifier").invoke(randInt(-50, 50));
                  r.method("set_colorHue").invoke(randInt(-127, 127));
                  r.method("set_colorSaturation").invoke(randInt(-20, 127));
                }
                const s = getContainerComponent(l);
                if (!s || s.isNull()) {
                  return null;
                }
                setContainerCapacity(s, QUIVER_ITEM_LIMIT);
                setContainerOpen(s, true);
                const c = getQuiverFillPool();
                const d = fillContainerWithItems(s, n, QUIVER_ITEM_LIMIT, () => e || c[randInt(0, c.length - 1)]);
                setContainerCapacity(s, QUIVER_ITEM_LIMIT);
                setContainerOpen(s, false);
                return {
                  netObj: l,
                  container: s,
                  insertedCount: d
                };
              } catch (e) {
                console.error("spawnFilledQuiver error: " + e);
                return null;
              }
            }
            function spawnOreQuiver() {
              try {
                const e = leftHandTransform.method("get_position").invoke();
                const t = leftHandTransform.method("get_forward").invoke();
                const o = Vector3.method("op_Multiply", 2).invoke(t, 0.3);
                const n = Vector3.method("op_Addition", 2).invoke(e, o);
                const i = ["item_quiver", "item_quiver_heart"];
                const a = spawnItemPrefab(i[randInt(0, i.length - 1)], n, identityQuaternion);
                if (!a || a.isNull()) {
                  return null;
                }
                const l = a.method("GetComponent", 1).inflate(GBOClass).invoke();
                if (l && !l.isNull()) {
                  l.method("set_scaleModifier").invoke(randInt(-50, 50));
                  l.method("set_colorHue").invoke(randInt(-127, 127));
                  l.method("set_colorSaturation").invoke(randInt(-20, 127));
                }
                const r = getContainerComponent(a);
                if (!r || r.isNull()) {
                  return null;
                }
                setContainerCapacity(r, QUIVER_ITEM_LIMIT);
                setContainerOpen(r, true);
                const s = fillContainerWithItems(r, n, QUIVER_ITEM_LIMIT, () => ORE_ITEM_IDS[randInt(0, ORE_ITEM_IDS.length - 1)]);
                setContainerCapacity(r, QUIVER_ITEM_LIMIT);
                setContainerOpen(r, false);
                return {
                  netObj: a,
                  container: r,
                  insertedCount: s
                };
              } catch (e) {
                console.error("spawnOreQuiver error: " + e);
                return null;
              }
            }
            function spawnGiveawayBag() {
              try {
                const e = leftHandTransform.method("get_position").invoke();
                const t = leftHandTransform.method("get_forward").invoke();
                const o = Vector3.method("op_Multiply", 2).invoke(t, 0.3);
                const n = Vector3.method("op_Addition", 2).invoke(e, o);
                const i = ["item_backpack_large_clover", "item_backpack_large_basketball", "item_backpack_large_base"];
                const a = spawnItemPrefab(i[randInt(0, i.length - 1)], n, identityQuaternion);
                if (!a || a.isNull()) {
                  return null;
                }
                const l = a.method("GetComponent", 1).inflate(GBOClass).invoke();
                if (l && !l.isNull()) {
                  l.method("set_scaleModifier").invoke(randInt(-50, 50));
                  l.method("set_colorHue").invoke(randInt(-127, 127));
                  l.method("set_colorSaturation").invoke(randInt(-20, 127));
                }
                const r = getContainerComponent(a);
                if (!r || r.isNull()) {
                  return null;
                }
                setContainerCapacity(r, GIVEAWAY_BAG_ITEM_LIMIT);
                setContainerOpen(r, true);
                const s = fillContainerWithItems(r, n, GIVEAWAY_BAG_ITEM_LIMIT, () => itemIDs[randInt(0, itemIDs.length - 1)]);
                setContainerCapacity(r, GIVEAWAY_BAG_ITEM_LIMIT);
                setContainerOpen(r, false);
                return {
                  netObj: a,
                  container: r,
                  insertedCount: s
                };
              } catch (e) {
                console.error("spawnGiveawayBag error: " + e);
                return null;
              }
            }
            function spawnItemPrefab(e, t, o) {
              try {
                const n = PrefabGen.field("_instance").value;
                if (n && !n.isNull()) {
                  const i = n.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + e), t, o, NULL);
                  if (i && !i.isNull()) {
                    return i;
                  }
                }
              } catch (e) {}
              try {
                const n = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + e), t, o, NULL);
                if (n && !n.isNull()) {
                  return n;
                }
              } catch (e) {}
              try {
                const n = PrefabGen.field("_instance").value;
                if (n && !n.isNull()) {
                  const i = n.method("SpawnItem", 3).invoke(Il2Cpp.string("item_prefab/" + e), t, o);
                  if (i && !i.isNull()) {
                    return i;
                  }
                }
              } catch (e) {}
              console.error("[SPAWN] All attempts failed for: " + e);
              return null;
            }
            function randInt(e, t) {
              return Math.floor(Math.random() * (t - e + 1)) + e;
            }
            function setContainerCapacity(e, t) {
              if (e && !e.isNull()) {
                try {
                  e.field("_capacity").value = t;
                } catch (e) {}
                try {
                  e.field("capacity").value = t;
                } catch (e) {}
                try {
                  e.method("set_capacity").invoke(t);
                } catch (e) {}
              }
            }
            function setContainerOpen(e, t) {
              if (e && !e.isNull()) {
                try {
                  const o = tryMethod(e, "get_isOpen", 0);
                  if (o) {
                    if (!!o.invoke() == !!t) {
                      return;
                    }
                  }
                } catch (e) {}
                try {
                  const o = tryMethod(e, "set_isOpen", 1) || tryMethod(e, "set_open", 1);
                  if (o) {
                    o.invoke(!!t);
                    return;
                  }
                } catch (e) {}
                try {
                  const o = tryMethod(e, t ? "Open" : "Close", 0);
                  if (o) {
                    o.invoke();
                    return;
                  }
                } catch (e) {}
                try {
                  const t = tryMethod(e, "RPC_ToggleOnOff", 0);
                  if (t) {
                    t.invoke();
                  }
                } catch (e) {}
              }
            }
            function getComponentDeep(e, t) {
              let o = getComponent(e, t);
              if (!o || !!o.handle.isNull()) {
                o = getComponentInParent(e, t);
              }
              if (!o || !!o.handle.isNull()) {
                o = getComponentInChildren(e, t);
              }
              if (!o || o.handle.isNull()) {
                return null;
              } else {
                return o;
              }
            }
            function getComponentInChildren(e, t) {
              return e.method("GetComponentInChildren", 0).inflate(t).invoke();
            }
            let lastSpawnTimesLeft = new Map();
            let lastSpawnTimesRight = new Map();
            function checkOtherPlayerInputs(e) {
              let t = {
                leftTrigger: false,
                leftGrab: false,
                rightTrigger: false,
                rightGrab: false
              };
              try {
                const o = e.method("get_view").invoke();
                if (!o || o.isNull()) {
                  return t;
                }
                const n = o.field("_fingerViews").value;
                if (n && !n.isNull() && n.length >= 2) {
                  const e = n.get(0);
                  const o = n.get(1);
                  if (e && !e.isNull()) {
                    const o = e.field("_indexValue").value;
                    const n = e.field("_middleValue").value;
                    t.leftTrigger = o > 0.4;
                    t.leftGrab = n > 0.4;
                  }
                  if (o && !o.isNull()) {
                    const e = o.field("_indexValue").value;
                    const n = o.field("_middleValue").value;
                    t.rightTrigger = e > 0.4;
                    t.rightGrab = n > 0.4;
                  }
                }
              } catch (e) {}
              return t;
            }
            function isMine(e) {
              try {
                return e.method("get_IsMine").invoke();
              } catch (e) {
                return false;
              }
            }
            function shouldBlock(e) {
              return blockRPCEnabled && isMine(e) && !_selfRPCBypass;
            }
            const NetPlayerCls = AssemblyCSharp.class("AnimalCompany.NetPlayer");
            try {
              NetPlayerCls.method("RPC_AddForce").implementation = function (e) {
                if (!shouldBlock(this)) {
                  return this.method("RPC_AddForce").invoke(e);
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_AddForce:", e);
            }
            try {
              NetPlayerCls.method("RPC_Teleport").implementation = function (e) {
                if (!shouldBlock(this)) {
                  try {
                    if (this.method("get_IsMine").invoke()) {
                      e.field("x").value.toFixed(2);
                      e.field("y").value.toFixed(2);
                      e.field("z").value.toFixed(2);
                    }
                  } catch (e) {}
                  return this.method("RPC_Teleport").invoke(e);
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_Teleport:", e);
            }
            try {
              NetPlayerCls.method("RPC_PlayerStun").implementation = function (e, t, o, n) {
                if (!shouldBlock(this)) {
                  return this.method("RPC_PlayerStun").invoke(e, t, o, n);
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_PlayerStun:", e);
            }
            try {
              NetPlayerCls.method("RPC_PlayerHit", 3).implementation = function (e, t, o) {
                if (!shouldBlock(this)) {
                  return this.method("RPC_PlayerHit", 3).invoke(e, t, o);
                }
              };
            } catch (e) {}
            try {
              NetPlayerCls.method("RPC_PlayerHit", 5).implementation = function (e, t, o, n, i) {
                if (!shouldBlock(this)) {
                  return this.method("RPC_PlayerHit", 5).invoke(e, t, o, n, i);
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_PlayerHit(5):", e);
            }
            try {
              NetPlayerCls.method("RPC_DoPlayerDie").implementation = function (e) {
                if (!shouldBlock(this)) {
                  return this.method("RPC_DoPlayerDie").invoke(e);
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_DoPlayerDie:", e);
            }
            try {
              NetPlayerCls.method("RPC_TagAsStinky").implementation = function () {
                if (!shouldBlock(this)) {
                  return this.method("RPC_TagAsStinky").invoke();
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_TagAsStinky:", e);
            }
            try {
              NetPlayerCls.method("RPC_SetColorHSV").implementation = function (e, t, o, n) {
                if (!shouldBlock(this)) {
                  return this.method("RPC_SetColorHSV").invoke(e, t, o, n);
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_SetColorHSV:", e);
            }
            try {
              NetPlayerCls.method("RPC_ApplyBuff").implementation = function (e) {
                if (!shouldBlock(this)) {
                  return this.method("RPC_ApplyBuff").invoke(e);
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_ApplyBuff:", e);
            }
            try {
              NetPlayerCls.method("RPC_SetTeam").implementation = function (e) {
                if (!shouldBlock(this)) {
                  return this.method("RPC_SetTeam").invoke(e);
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_SetTeam:", e);
            }
            try {
              NetPlayerCls.method("RPC_SetHide").implementation = function (e) {
                if (!shouldBlock(this)) {
                  return this.method("RPC_SetHide").invoke(e);
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_SetHide:", e);
            }
            try {
              NetPlayerCls.method("RPC_AddPlayerMoney").implementation = function (e) {
                if (!shouldBlock(this)) {
                  return this.method("RPC_AddPlayerMoney").invoke(e);
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_AddPlayerMoney:", e);
            }
            try {
              NetPlayerCls.method("RPC_AttachToGiantHand").implementation = function (e, t, o, n) {
                if (!shouldBlock(this)) {
                  return this.method("RPC_AttachToGiantHand").invoke(e, t, o, n);
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_AttachToGiantHand:", e);
            }
            try {
              NetPlayerCls.method("RPC_AwardKill").implementation = function () {
                if (!shouldBlock(this)) {
                  return this.method("RPC_AwardKill").invoke();
                }
              };
            } catch (e) {
              console.error("[blockrpc] RPC_AwardKill:", e);
            }
            try {
              NetPlayerCls.method("RPC_KickPlayer").implementation = function () {
                if (!shouldBlock(this) && (!antiKickEnabled || !isMine(this) || !!_selfRPCBypass)) {
                  return this.method("RPC_KickPlayer").invoke();
                }
              };
            } catch (e) {}
            function getSelectedPrefabID() {
              try {
                if (prefabIDs.length <= 0) {
                  return "";
                } else {
                  prefabIndex = (prefabIndex % prefabIDs.length + prefabIDs.length) % prefabIDs.length;
                  return prefabIDs[prefabIndex] ?? "";
                }
              } catch (e) {
                return "";
              }
            }
            blockrpc();
            const safeField = (e, t) => {
              try {
                if (!e || e.isNull()) {
                  return null;
                }
                const o = e.field(t).value;
                if (o && !o.isNull()) {
                  return o;
                } else {
                  return null;
                }
              } catch (e) {
                return null;
              }
            };
            const getInstance = () => {
              try {
                const e = GTPlayerClass.fields.find(e => e.name.includes("Instance"));
                const t = e ? e.value : null;
                if (t && !t.isNull()) {
                  return t;
                } else {
                  return null;
                }
              } catch (e) {
                return null;
              }
            };
            const getGTPlayer = () => {
              try {
                const e = GTPlayerClass.field("<Instance>k__BackingField").value;
                if (e && !e.isNull()) {
                  return e;
                } else {
                  return null;
                }
              } catch (e) {
                return null;
              }
            };
            let prefabSpamLineDelay = 0;
            let rainSpamDelayIndex = 2;
            let rainPrefabDelay = 0;
            function getRainSpamDelay() {
              return getDelayValue(RAIN_SPAM_DELAY_VALUES, rainSpamDelayIndex);
            }
            function getRigidbodySafe(e) {
              const t = [e, getGameObjectSafe(e)];
              for (const e of t) {
                if (e && !e.isNull()) {
                  try {
                    const t = e.method("GetComponent", 1).inflate(Rigidbody).invoke();
                    if (t && !t.isNull()) {
                      return t;
                    }
                  } catch (e) {}
                  try {
                    const t = e.method("GetComponentInChildren", 1).inflate(Rigidbody).invoke();
                    if (t && !t.isNull()) {
                      return t;
                    }
                  } catch (e) {}
                }
              }
              return null;
            }
            function getDelayValue(e, t) {
              return e[(t % e.length + e.length) % e.length];
            }
            function getGameObjectSafe(e) {
              if (!e || e.isNull()) {
                return null;
              }
              try {
                const t = e.method("get_gameObject").invoke();
                if (t && !t.isNull()) {
                  return t;
                }
              } catch (e) {}
              try {
                const t = getNetworkObjectSafe(e);
                if (t && !t.isNull()) {
                  const e = t.method("get_gameObject").invoke();
                  if (e && !e.isNull()) {
                    return e;
                  }
                }
              } catch (e) {}
              return null;
            }
            function cycleNumericSetting(e, t, o, n = "s") {
              const i = (t + 1) % e.length;
              sendNotification(`${o}: ${e[i]}${n}`, false, 2);
              reloadMenu();
              return i;
            }
            const ITEM_SPAWN_DELAY_VALUES = [0, 0.05, 0.1, 0.2, 0.35, 0.5];
            const MOB_SPAWN_DELAY_VALUES = [0, 0.1, 0.2, 0.3, 0.5, 0.75];
            const PREFAB_SPAWN_DELAY_VALUES = [0, 0.05, 0.15, 0.3, 0.5];
            const RAIN_SPAM_DELAY_VALUES = [0, 0.033, 0.05, 0.067, 0.1, 0.2];
            function initPrefabs() {
              if (didInitPrefabs) {
                return;
              }
              const e = AssemblyCSharp.class("AnimalCompany.PrefabGenerator").field("_instance").value.method("get_runner").invoke();
              if (!e && e.handle.isNull()) {
                return;
              }
              const t = e.method("get_Config").invoke().field("PrefabTable").value;
              const o = t.method("get_Prefabs").invoke().method("get_Count").invoke();
              for (let e = 0; e < o; e++) {
                const o = FusionRuntime.class("Fusion.NetworkPrefabId").method("FromIndex").invoke(e);
                const n = t.method("Load").invoke(o, true);
                const i = n.method("get_gameObject").invoke().method("get_name").invoke().toString().replace(/"/g, "");
                if (!i.toLowerCase().includes("item_")) {
                  prefabMap.set(e, n);
                  prefabNameMap.set(i, e);
                  prefabNames.push(i);
                }
              }
              didInitPrefabs = true;
            }
            function spawnPrefab(e = "Basketball", t = null, o = null, n = null) {
              const i = AssemblyCSharp.class("AnimalCompany.PrefabGenerator").field("_instance").value.method("get_runner").invoke();
              if (!i || i.handle.isNull()) {
                return;
              }
              if (!didInitPrefabs) {
                initPrefabs();
              }
              const a = i.method("Spawn").overload("Fusion.NetworkObject", "System.Nullable<UnityEngine.Vector3>", "System.Nullable<UnityEngine.Quaternion>", "System.Nullable<Fusion.PlayerRef>", "Fusion.NetworkRunner.OnBeforeSpawned", "Fusion.NetworkSpawnFlags");
              let l = t || rightHandTransform.method("get_position").invoke();
              let r = o || rightHandTransform.method("get_rotation").invoke();
              let s = n || NetPlayerClass.method("get_localPlayer").invoke().method("get_playerRef").invoke();
              const c = Il2Cpp.nullable(l);
              const d = Il2Cpp.nullable(r);
              const u = Il2Cpp.nullable(s);
              const m = prefabNameMap.get(e);
              const h = [prefabMap.get(m), c, d, u, NULL, 0];
              return a.invoke(...h);
            }
            function isPlayerNearMe(e, t = 10) {
              try {
                const o = getTransform(headCollider).method("get_position").invoke();
                const n = getTransform(e).method("get_position").invoke();
                return Vector3.method("Distance").invoke(o, n) <= t;
              } catch (e) {
                return false;
              }
            }
            function tryCallNames(e, t, o = -1, ...n) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                for (const i of t) {
                  try {
                    const t = o >= 0 ? e.class.method(i, o) : e.class.method(i);
                    if (t) {
                      invokeInstance(t, e, ...n);
                      return true;
                    }
                  } catch (e) {}
                  try {
                    const t = o >= 0 ? e.method(i, o) : e.method(i);
                    if (t) {
                      t.invoke(...n);
                      return true;
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              return false;
            }
            function tryReleaseHeldItem(e) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                const t = ["ForceDrop", "Drop", "Release", "ReleaseGrabHandle", "ReleaseGrabHandles", "ReleaseHeldObject", "DropHeldObject", "DropItem", "Detach", "DetachFromPlayer", "RPC_Drop", "TryDrop", "TryRelease", "ForceRelease", "Unequip", "UngrabAllHandles", "BreakGrabWithDistance"];
                tryCallNames(e, t, -1);
                tryCallNames(e, t, 0);
                try {
                  const o = e.method("get_gameObject").invoke();
                  if (o && !o.isNull?.()) {
                    tryCallNames(o, t, -1);
                    tryCallNames(o, t, 0);
                  }
                } catch (e) {}
              } catch (e) {}
            }
            function requestAuthorityDeep(e) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                tryCallNames(e, ["RequestStateAuthority", "RequestInputAuthority", "TakeOwnership", "ClaimOwnership"], 0);
                try {
                  const t = tryMethodName(e.class, ["get_Object", "get_NetworkObject"], 0);
                  if (t) {
                    const o = invokeInstance(t, e);
                    if (o && !o.isNull?.()) {
                      tryCallNames(o, ["RequestStateAuthority", "RequestInputAuthority", "TakeOwnership", "ClaimOwnership"], 0);
                    }
                  }
                } catch (e) {}
                try {
                  const t = e.method?.("get_Runner")?.invoke?.();
                  if (t && !t.isNull?.()) {
                    tryCallNames(t, ["RequestStateAuthority", "RequestInputAuthority"], 1, e);
                  }
                } catch (e) {}
              } catch (e) {}
            }
            function forceReleaseItemFromAllPlayers(e) {
              try {
                const t = normalizeSceneObjectHandle(e);
                for (const e of getAllNetPlayersList(true)) {
                  try {
                    tryReleaseHeldItem(e);
                  } catch (e) {}
                  tryCallNames(e, ["ReleaseHeldObject", "DropHeldObject", "DropItem", "DetachFromBack", "SetAttachedToBack", "SetAttachedToHip"], 0);
                  tryCallNames(e, ["SetAttachedToBack", "SetAttachedToHip"], 1, false);
                  for (const o of [0, 1]) {
                    try {
                      const n = e.method("GetHandInteractor", 1).invoke(o);
                      if (n && !n.isNull?.()) {
                        tryReleaseHeldItem(n);
                        tryCallNames(n, ["ReleaseHeldObject", "DropHeldObject", "DropItem", "ForceDrop"], 0);
                      }
                      const i = getPlayerHeldGrabbable(e, o);
                      if (!i || i.isNull?.()) {
                        continue;
                      }
                      const a = normalizeSceneObjectHandle(i);
                      if (t && a && t !== a) {
                        continue;
                      }
                      tryReleaseHeldItem(i);
                      tryReleaseHeldItem(e);
                      tryCallNames(i, ["DetachFromBack", "DetachFromHip", "SetAttachedToBack", "SetAttachedToHip"], 0);
                      tryCallNames(i, ["SetAttachedToBack", "SetAttachedToHip"], 1, false);
                    } catch (e) {}
                  }
                }
              } catch (e) {}
            }
            function forceMoveLobbyItem(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                let o = e;
                let n = e;
                try {
                  const t = e.method("get_gameObject").invoke();
                  if (t && !t.isNull?.()) {
                    n = t;
                  }
                } catch (e) {}
                let i = n;
                try {
                  const e = getTransform(n).method("get_root").invoke();
                  if (e && !e.isNull?.()) {
                    const t = e.method("get_gameObject").invoke();
                    if (t && !t.isNull?.()) {
                      i = t;
                    }
                  }
                } catch (e) {}
                forceReleaseItemFromAllPlayers(o);
                forceReleaseItemFromAllPlayers(n);
                forceReleaseItemFromAllPlayers(i);
                tryReleaseHeldItem(o);
                tryReleaseHeldItem(n);
                tryReleaseHeldItem(i);
                requestAuthorityDeep(o);
                requestAuthorityDeep(n);
                requestAuthorityDeep(i);
                try {
                  let t = null;
                  try {
                    t = e.cast?.(GBOClass);
                  } catch (e) {}
                  if (!t || t.isNull?.()) {
                    try {
                      t = n.method("GetComponent", 1).inflate(GBOClass).invoke();
                    } catch (e) {}
                  }
                  if (t && !t.isNull()) {
                    requestAuthorityDeep(t);
                    try {
                      const e = t.method("get_Object").invoke();
                      if (e && !e.isNull?.()) {
                        try {
                          e.method("RequestStateAuthority").invoke();
                        } catch (e) {}
                        requestAuthorityDeep(e);
                      }
                    } catch (e) {}
                    tryCallNames(t, ["SetIsHidden"], 1, false);
                  }
                } catch (e) {}
                tryCallNames(o, ["Grab", "Release"], 2, NULL, false);
                tryCallNames(o, ["RPC_Teleport", "TeleportTo"], 1, t);
                tryCallNames(n, ["RPC_Teleport", "TeleportTo"], 1, t);
                tryCallNames(i, ["RPC_Teleport", "TeleportTo"], 1, t);
                try {
                  const e = i.method("GetComponentInChildren", 0).inflate(Rigidbody).invoke();
                  if (e && !e.isNull()) {
                    try {
                      e.method("set_isKinematic").invoke(true);
                    } catch (e) {}
                    try {
                      e.method("set_detectCollisions").invoke(false);
                    } catch (e) {}
                    try {
                      e.method("set_velocity").invoke(zeroVector);
                    } catch (e) {}
                    try {
                      e.method("set_angularVelocity").invoke(zeroVector);
                    } catch (e) {}
                  }
                } catch (e) {}
                try {
                  getTransform(o).method("set_position").invoke(t);
                } catch (e) {
                  getTransform(n).method("set_position").invoke(t);
                }
                try {
                  getTransform(n).method("set_position").invoke(t);
                } catch (e) {}
                try {
                  getTransform(i).method("set_position").invoke(t);
                } catch (e) {}
                tryCallNames(o, ["SetWorldPosition", "SetPosition"], 1, t);
                tryCallNames(n, ["SetWorldPosition", "SetPosition"], 1, t);
                tryCallNames(i, ["SetWorldPosition", "SetPosition"], 1, t);
                tryCallNames(o, ["RPC_SetPosition", "RPC_SetWorldPosition"], 1, t);
                tryCallNames(n, ["RPC_SetPosition", "RPC_SetWorldPosition"], 1, t);
                tryCallNames(i, ["RPC_SetPosition", "RPC_SetWorldPosition"], 1, t);
                try {
                  const e = i.method("GetComponentInChildren", 0).inflate(Rigidbody).invoke();
                  if (e && !e.isNull()) {
                    try {
                      e.method("set_velocity").invoke(zeroVector);
                    } catch (e) {}
                    try {
                      e.method("set_angularVelocity").invoke(zeroVector);
                    } catch (e) {}
                  }
                } catch (e) {}
                return true;
              } catch (e) {
                return false;
              }
            }
            function destroyNetworkedObjectDeep(e) {
              try {
                if (!e || e.isNull?.()) {
                  return false;
                }
                const t = getRootLikeObject(e);
                const o = [e, t];
                for (const e of o) {
                  try {
                    requestAuthorityDeep(e);
                  } catch (e) {}
                  try {
                    tryReleaseHeldItem(e);
                  } catch (e) {}
                  try {
                    let t = null;
                    try {
                      t = e.cast?.(GBOClass);
                    } catch (e) {}
                    if (!t || t.isNull?.()) {
                      try {
                        t = e.method("GetComponent", 1).inflate(GBOClass).invoke();
                      } catch (e) {}
                    }
                    if (t && !t.isNull?.()) {
                      try {
                        t.method("set_isHidden").invoke(false);
                      } catch (e) {}
                      try {
                        t.method("Despawn").invoke();
                        return true;
                      } catch (e) {}
                      try {
                        t.method("RPC_Destroy").invoke();
                        return true;
                      } catch (e) {}
                    }
                  } catch (e) {}
                  try {
                    const t = tryMethodName(e.class, ["get_Object", "get_NetworkObject"], 0);
                    if (t) {
                      const o = invokeInstance(t, e);
                      if (o && !o.isNull?.()) {
                        for (const e of getRunnerCandidates()) {
                          try {
                            e.method("Despawn").invoke(o);
                            return true;
                          } catch (e) {}
                        }
                        try {
                          o.method("Despawn").invoke();
                          return true;
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                  try {
                    if (e.method) {
                      e.method("Despawn").invoke();
                      return true;
                    }
                  } catch (e) {}
                  try {
                    if (e.method) {
                      e.method("RPC_Destroy").invoke();
                      return true;
                    }
                  } catch (e) {}
                  try {
                    if (e.method) {
                      e.method("DestroySelf").invoke();
                      return true;
                    }
                  } catch (e) {}
                }
                try {
                  Destroy(t);
                } catch (e) {}
                try {
                  Destroy(e);
                } catch (e) {}
                return true;
              } catch (e) {
                return false;
              }
            }
            function getSelectedVfxEntry() {
              if (selectedVfxIndex < 0 || selectedVfxIndex >= vfxTypeEntries.length) {
                selectedVfxIndex = 0;
              }
              return vfxTypeEntries[selectedVfxIndex];
            }
            function getRunnerCandidates() {
              const e = [];
              const t = new Set();
              const o = o => {
                try {
                  if (!o || o.isNull?.()) {
                    return;
                  }
                  const n = normalizeSceneObjectHandle(o) || String(o);
                  if (t.has(n)) {
                    return;
                  }
                  t.add(n);
                  e.push(o);
                } catch (e) {}
              };
              try {
                const e = SFXManager.field("_instance").value;
                if (e && !e.isNull?.()) {
                  try {
                    o(e.method("get__currentRunner").invoke());
                  } catch (e) {}
                  try {
                    o(e.field("_currentRunner").value);
                  } catch (e) {}
                }
              } catch (e) {}
              try {
                const e = PrefabGen.field("_instance").value;
                if (e && !e.isNull?.()) {
                  try {
                    o(e.method("get_runner").invoke());
                  } catch (e) {}
                }
              } catch (e) {}
              try {
                const e = NManager.field("<Instance>k__BackingField").value;
                if (e && !e.isNull?.()) {
                  try {
                    o(e.method("get_runner").invoke());
                  } catch (e) {}
                }
              } catch (e) {}
              return e;
            }
            function getPrimaryVfxRunner() {
              try {
                const e = SFXManager.field("_instance").value;
                if (e && !e.isNull?.()) {
                  try {
                    const t = e.method("get__currentRunner").invoke();
                    if (t && !t.isNull?.()) {
                      return t;
                    }
                  } catch (e) {}
                  try {
                    const t = e.field("_currentRunner").value;
                    if (t && !t.isNull?.()) {
                      return t;
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              try {
                const e = PrefabGen.field("_instance").value;
                if (e && !e.isNull?.()) {
                  const t = e.method("get_runner").invoke();
                  if (t && !t.isNull?.()) {
                    return t;
                  }
                }
              } catch (e) {}
              try {
                const e = NManager.field("<Instance>k__BackingField").value;
                if (e && !e.isNull?.()) {
                  const t = e.method("get_runner").invoke();
                  if (t && !t.isNull?.()) {
                    return t;
                  }
                }
              } catch (e) {}
              return null;
            }
            function makeZeroValueForType(e) {
              try {
                if (!e || e.class?.isEnum || e.isPrimitive) {
                  return 0;
                }
                if (!e.class?.isValueType) {
                  return null;
                }
                const t = e.class.fields.filter(e => !e.isStatic);
                if (t.length === 0) {
                  return 0;
                } else {
                  return t.map(e => makeZeroValueForType(e.type));
                }
              } catch (e) {
                return 0;
              }
            }
            function getVfxRpcInfoZero(e) {
              if (_vfxRpcInfoZero != null) {
                return _vfxRpcInfoZero;
              }
              try {
                const t = e.parameters[e.parameterCount - 1];
                _vfxRpcInfoZero = makeZeroValueForType(t.type);
              } catch (e) {
                _vfxRpcInfoZero = 0;
              }
              return _vfxRpcInfoZero;
            }
            function getVfxRpc6() {
              if (_vfxRpc6) {
                return _vfxRpc6;
              }
              const e = [ParticleManagerClass, VFXManagerClass, SFXManager].filter(e => e);
              try {
                for (const t of AssemblyCSharp.classes) {
                  try {
                    const o = String(t.name ?? "");
                    if (/vfx|effect|particle/i.test(o) && !e.includes(t)) {
                      e.push(t);
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              for (const t of e) {
                try {
                  for (const e of t.methods) {
                    try {
                      if (e.parameterCount !== 6 || e.isGeneric) {
                        continue;
                      }
                      const o = String(e.name ?? "");
                      const n = e.parameters.map(e => String(e.type?.name ?? "")).join("|");
                      const i = /vfx|effect|particle/i.test(o + "|" + n);
                      const a = n.indexOf("UnityEngine.Vector3") >= 0 && n.indexOf("UnityEngine.Quaternion") >= 0;
                      if (!i || !a) {
                        continue;
                      }
                      _vfxRpc6 = e;
                      console.log("[VFX Gun] cached 6-arg VFX RPC: " + t.name + "." + o + "(" + n + ")");
                      return _vfxRpc6;
                    } catch (e) {}
                  }
                } catch (e) {}
              }
              return null;
            }
            function playLocalNetworkVfxImmediate(e, t, o) {
              const n = _localNetworkVfxMethodName ? [_localNetworkVfxMethodName] : _localNetworkVfxMethodScanned ? [] : ["PlayVFX", "SpawnVFX", "PlayEffect", "SpawnEffect", "PlayParticle", "SpawnParticle", "TriggerVFX"];
              for (const i of n) {
                try {
                  if (tryInvokeVfxMethod(i, o, e, t)) {
                    _localNetworkVfxMethodName = i;
                    _localNetworkVfxMethodScanned = true;
                    return true;
                  }
                } catch (e) {}
              }
              _localNetworkVfxMethodScanned = true;
              return false;
            }
            function playNetworkVFX(e, t, o, n = 3) {
              try {
                const i = getVfxRpc6();
                if (!i) {
                  console.error("[VFX Gun] no 6-arg VFX RPC found");
                  return false;
                }
                let a = _vfxRunnerCache;
                try {
                  if (!a || !!a.isNull?.()) {
                    a = null;
                  }
                } catch (e) {
                  a = null;
                }
                if (!a) {
                  a = getPrimaryVfxRunner() ?? getRunnerCandidates()[0];
                  _vfxRunnerCache = a;
                }
                if (!a || a.isNull?.()) {
                  console.error("[VFX Gun] no runner for VFX RPC");
                  return false;
                }
                const l = playLocalNetworkVfxImmediate(e, t, a);
                const r = getVfxRpcInfoZero(i);
                try {
                  const l = i.parameters.map(i => {
                    const l = String(i.type?.name ?? "");
                    const s = String(i.name ?? "").toLowerCase();
                    if (l.indexOf("NetworkRunner") >= 0) {
                      return a;
                    } else if (l.indexOf("UnityEngine.Vector3") >= 0) {
                      return t;
                    } else if (l.indexOf("UnityEngine.Quaternion") >= 0) {
                      return o;
                    } else if (/RpcInfo/i.test(l)) {
                      return r;
                    } else if (/Single|Double/.test(l) || /duration|time|life/.test(s)) {
                      return n;
                    } else if (i.type?.class?.isEnum || /VFX|Effect|Particle/i.test(l + s)) {
                      return e;
                    } else {
                      return 0;
                    }
                  });
                  i.invoke(...l);
                  return true;
                } catch (e) {}
                const s = [[a, e, t, o, n, r], [e, t, o, n, a, r], [e, t, o, n, r, a], [a, e, t, o, r, n]];
                if (_vfxRpcVariantIndex >= 0 && _vfxRpcVariantIndex < s.length) {
                  try {
                    i.invoke(...s[_vfxRpcVariantIndex]);
                    return true;
                  } catch (e) {
                    _vfxRpcVariantIndex = -1;
                    _vfxRunnerCache = null;
                  }
                }
                let c = null;
                for (let e = 0; e < s.length; e++) {
                  const t = s[e];
                  try {
                    i.invoke(...t);
                    _vfxRpcVariantIndex = e;
                    return true;
                  } catch (e) {
                    c = e;
                  }
                }
                console.error("[VFX Gun] 6-arg VFX RPC invoke failed:", c);
                if (l) {
                  return true;
                }
              } catch (e) {
                console.error("[VFX Gun] playNetworkVFX failed:", e);
              }
              return false;
            }
            function getVisualEffectAssetMethods() {
              if (_visualEffectAssetMethods) {
                return _visualEffectAssetMethods;
              }
              const e = [];
              try {
                for (const t of AssemblyCSharp.classes) {
                  try {
                    for (const o of t.methods) {
                      try {
                        if (o.isGeneric) {
                          continue;
                        }
                        const n = String(o.name ?? "");
                        if (/^(get_|set_)/i.test(n) || n === "Pull") {
                          continue;
                        }
                        const i = o.parameters.map(e => String(e.type?.name ?? ""));
                        if (!i.some(e => /E_?VisualEffectAsset|VisualEffectAsset/i.test(e))) {
                          continue;
                        }
                        const a = i.join("|");
                        if (!(a.indexOf("UnityEngine.Vector3") >= 0) && !(a.indexOf("UnityEngine.Transform") >= 0) && !(a.indexOf("UnityEngine.GameObject") >= 0) && !/play|spawn|create|emit|trigger/i.test(n)) {
                          continue;
                        }
                        let l = 0;
                        if (/play|spawn|create|emit|trigger|effect|vfx/i.test(n)) {
                          l += 20;
                        }
                        if (/manager|visual|vfx|effect|particle/i.test(String(t.name ?? ""))) {
                          l += 10;
                        }
                        if (a.indexOf("UnityEngine.Vector3") >= 0) {
                          l += 8;
                        }
                        if (a.indexOf("UnityEngine.Quaternion") >= 0) {
                          l += 5;
                        }
                        if (a.indexOf("UnityEngine.Transform") >= 0 || a.indexOf("UnityEngine.GameObject") >= 0) {
                          l += 4;
                        }
                        if (a.indexOf("Fusion.NetworkRunner") >= 0) {
                          l += 4;
                        }
                        if (o.parameterCount >= 2 && o.parameterCount <= 7) {
                          l += 3;
                        }
                        e.push({
                          method: o,
                          klass: t,
                          score: l,
                          sig: String(t.name ?? "") + "." + String(o.name ?? "") + "(" + a + ")"
                        });
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
              } catch (e) {
                console.error("[VisualEffectAsset] method scan failed:", e);
              }
              e.sort((e, t) => t.score - e.score);
              _visualEffectAssetMethods = e.map(e => e.method);
              if (e.length > 0) {
                _visualEffectAssetMethod = e[0].method;
                _visualEffectAssetClass = e[0].klass;
                console.log("[VisualEffectAsset] cached methods: " + e.slice(0, 8).map(e => e.sig + " score=" + e.score).join(" | "));
                return _visualEffectAssetMethods;
              }
              try {
                let e = 0;
                for (const t of AssemblyCSharp.classes) {
                  try {
                    const o = String(t.name ?? "");
                    if (!/visual|vfx|effect|asset/i.test(o)) {
                      continue;
                    }
                    for (const n of t.methods) {
                      if (e >= 40) {
                        break;
                      }
                      try {
                        const t = n.parameters.map(e => String(e.type?.name ?? "")).join("|");
                        console.log("[VisualEffectAsset] candidate: " + o + "." + n.name + "(" + t + ")");
                        e++;
                      } catch (e) {}
                    }
                  } catch (e) {}
                  if (e >= 40) {
                    break;
                  }
                }
              } catch (e) {}
              return [];
            }
            function getVisualEffectAssetMethod() {
              return getVisualEffectAssetMethods()[0] ?? null;
            }
            function getVisualEffectAssetAnchor(e, t) {
              try {
                if (!_visualEffectAssetAnchor || !!_visualEffectAssetAnchor.isNull?.()) {
                  _visualEffectAssetAnchor = createEmptyObject("VisualEffectAssetAnchor", null);
                }
                const o = getTransform(_visualEffectAssetAnchor);
                try {
                  o.method("set_position").invoke(e);
                } catch (e) {}
                try {
                  o.method("set_rotation").invoke(t);
                } catch (e) {}
                return _visualEffectAssetAnchor;
              } catch (e) {
                return;
              }
            }
            function buildVisualEffectAssetArgs(e, t, o, n) {
              const i = getPrimaryVfxRunner() ?? getRunnerCandidates()[0];
              const a = getVisualEffectAssetAnchor(o, n);
              const l = a ? getTransform(a) : null;
              return e.parameters.map(e => {
                const r = String(e.type?.name ?? "");
                if (/E_?VisualEffectAsset|VisualEffectAsset/i.test(r)) {
                  return t;
                }
                if (r.indexOf("UnityEngine.Vector3") >= 0) {
                  return o;
                }
                if (r.indexOf("UnityEngine.Quaternion") >= 0) {
                  return n;
                }
                if (r.indexOf("UnityEngine.Transform") >= 0) {
                  return l;
                }
                if (r.indexOf("UnityEngine.GameObject") >= 0) {
                  return a;
                }
                if (r.indexOf("Fusion.NetworkRunner") >= 0) {
                  return i;
                }
                if (r.indexOf("Fusion.RpcInfo") >= 0) {
                  return makeZeroValueForType(e.type);
                }
                if (r.indexOf("System.Boolean") >= 0) {
                  return false;
                }
                if (r.indexOf("System.Single") >= 0) {
                  return 3;
                }
                if (r.indexOf("System.Int32") >= 0) {
                  return 0;
                }
                try {
                  if (e.type?.class?.isEnum) {
                    return 0;
                  }
                } catch (e) {}
                try {
                  if (e.type?.class?.isValueType) {
                    return makeZeroValueForType(e.type);
                  }
                } catch (e) {}
              });
            }
            function getClassByFullName(e) {
              if (_vfxClassCache.has(e)) {
                return _vfxClassCache.get(e);
              }
              for (const t of ["Assembly-CSharp", "AnimalCompany", "Unity.VisualEffectGraph.Runtime", "UnityEngine.VFXModule", "UnityEngine.CoreModule"]) {
                try {
                  const o = Il2Cpp.domain.assembly(t).image.class(e);
                  if (o) {
                    _vfxClassCache.set(e, o);
                    return o;
                  }
                } catch (e) {}
              }
              try {
                for (const t of Il2Cpp.domain.assemblies) {
                  try {
                    const o = t.image.class(e);
                    if (o) {
                      _vfxClassCache.set(e, o);
                      return o;
                    }
                  } catch (e) {}
                }
              } catch (e) {}
              try {
                const t = AssemblyCSharp.class(e);
                if (t) {
                  _vfxClassCache.set(e, t);
                }
                return t;
              } catch (e) {}
              _vfxClassCache.set(e, null);
              return null;
            }
            function getVisualEffectAssetObject(e) {
              const t = _visualEffectAssetObjectCache.get(e);
              try {
                if (t && !t.isNull?.()) {
                  return t;
                }
              } catch (e) {}
              try {
                const t = AssemblyCSharp.class("AnimalCompany.EnumExtension_VisualEffectAsset");
                try {
                  const o = t.method("GetObject", 1).invoke(e);
                  if (o && !o.isNull?.()) {
                    _visualEffectAssetObjectCache.set(e, o);
                    return o;
                  }
                } catch (e) {}
                try {
                  const o = t.method("GetObject").invoke(e);
                  if (o && !o.isNull?.()) {
                    _visualEffectAssetObjectCache.set(e, o);
                    return o;
                  }
                } catch (e) {}
              } catch (e) {
                console.error("[VisualEffectAsset] GetObject failed:", e);
              }
              return null;
            }
            function pinVfxObject(e, t, o, n = 4, i = null) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                pinnedVfxObjects.push({
                  go: e,
                  pos: t,
                  rot: o,
                  until: time + n,
                  vfx: i
                });
                const a = getTransform(e);
                try {
                  a.method("set_parent").invoke(null);
                } catch (e) {}
                try {
                  a.method("set_position").invoke(t);
                } catch (e) {}
                try {
                  a.method("set_rotation").invoke(o);
                } catch (e) {}
              } catch (e) {}
            }
            function stabilizePinnedVfxObjects() {
              try {
                for (let e = pinnedVfxObjects.length - 1; e >= 0; e--) {
                  const t = pinnedVfxObjects[e];
                  try {
                    if (!t || !t.go || t.go.isNull?.() || time >= t.until) {
                      pinnedVfxObjects.splice(e, 1);
                      continue;
                    }
                    const o = getTransform(t.go);
                    try {
                      o.method("set_parent").invoke(null);
                    } catch (e) {}
                    try {
                      o.method("set_position").invoke(t.pos);
                    } catch (e) {}
                    try {
                      o.method("set_rotation").invoke(t.rot);
                    } catch (e) {}
                    try {
                      if (t.vfx && !t.vfx.isNull?.()) {
                        forceVisualEffectPosition(t.vfx, t.pos);
                      }
                    } catch (e) {}
                  } catch (t) {
                    pinnedVfxObjects.splice(e, 1);
                  }
                }
              } catch (e) {}
            }
            function deleteAllSpawnedVfxObjects() {
              let e = 0;
              try {
                for (let t = pinnedVfxObjects.length - 1; t >= 0; t--) {
                  const o = pinnedVfxObjects[t];
                  try {
                    if (o?.vfx && !o.vfx.isNull?.()) {
                      try {
                        o.vfx.method("Stop").invoke();
                      } catch (e) {}
                      try {
                        o.vfx.method("Reinit").invoke();
                      } catch (e) {}
                    }
                    if (o?.go && !o.go.isNull?.()) {
                      Destroy(o.go);
                      e++;
                    }
                  } catch (e) {}
                  pinnedVfxObjects.splice(t, 1);
                }
              } catch (e) {}
              return e;
            }
            function setVisualEffectVector(e, t, o) {
              try {
                e.method("SetVector3", 2).invoke(Il2Cpp.string(t), o);
              } catch (e) {}
              try {
                const n = Shader.method("PropertyToID").invoke(Il2Cpp.string(t));
                e.method("SetVector3", 2).invoke(n, o);
              } catch (e) {}
            }
            function forceVisualEffectPosition(e, t) {
              try {
                const o = ["position", "Position", "POSITION", "pos", "Pos", "spawnPosition", "SpawnPosition", "Spawn Position", "worldPosition", "WorldPosition", "World Position", "targetPosition", "TargetPosition", "Target Position", "origin", "Origin", "center", "Center", "point", "Point"];
                for (const n of o) {
                  setVisualEffectVector(e, n, t);
                }
              } catch (e) {}
            }
            function forceVfxEventAttributePosition(e, t) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                const o = ["position", "Position", "POSITION", "pos", "Pos", "spawnPosition", "SpawnPosition", "Spawn Position", "worldPosition", "WorldPosition", "World Position", "targetPosition", "TargetPosition", "Target Position", "origin", "Origin", "center", "Center", "point", "Point"];
                for (const n of o) {
                  try {
                    e.method("SetVector3", 2).invoke(Il2Cpp.string(n), t);
                  } catch (e) {}
                  try {
                    const o = Shader.method("PropertyToID").invoke(Il2Cpp.string(n));
                    e.method("SetVector3", 2).invoke(o, t);
                  } catch (e) {}
                }
              } catch (e) {}
            }
            function sendVisualEffectPlayEvent(e, t) {
              try {
                const o = e.method("CreateVFXEventAttribute").invoke();
                if (o && !o.isNull?.()) {
                  forceVfxEventAttributePosition(o, t);
                  for (const t of ["OnPlay", "Play", "Spawn", "OnSpawn"]) {
                    try {
                      e.method("SendEvent", 2).invoke(Il2Cpp.string(t), o);
                      return true;
                    } catch (e) {}
                  }
                }
              } catch (e) {}
              try {
                e.method("SendEvent", 1).invoke(Il2Cpp.string("OnPlay"));
                return true;
              } catch (e) {}
              return false;
            }
            function getRefValue(e) {
              try {
                if (e?.value && !e.value.isNull?.()) {
                  return e.value;
                }
              } catch (e) {}
              try {
                if (e?.contents && !e.contents.isNull?.()) {
                  return e.contents;
                }
              } catch (e) {}
              try {
                if (e?.handle && !e.handle.isNull?.()) {
                  return e;
                }
              } catch (e) {}
              return null;
            }
            function setVisualEffectObjectTransform(e, t, o) {
              let n = null;
              try {
                n = e.method("get_gameObject").invoke();
              } catch (e) {}
              try {
                const i = n && !n.isNull?.() ? getTransform(n) : e.method("get_transform").invoke();
                if (i && !i.isNull?.()) {
                  try {
                    i.method("set_parent").invoke(null);
                  } catch (e) {}
                  try {
                    i.method("set_position").invoke(t);
                  } catch (e) {}
                  try {
                    i.method("set_rotation").invoke(o);
                  } catch (e) {}
                }
              } catch (e) {}
              return n;
            }
            function playVisualEffectFromManagerPool(e, t, o, n = 4) {
              try {
                if (!VFXManagerClass) {
                  return false;
                }
                const i = _visualEffectClassCache ??= getClassByFullName("UnityEngine.VFX.VisualEffect");
                const a = _vfxEventAttributeClassCache ??= getClassByFullName("UnityEngine.VFX.VFXEventAttribute");
                if (!i || !a) {
                  return false;
                }
                const l = _vfxPoolPullCache ??= VFXManagerClass.method("Pull", 3);
                const r = Il2Cpp.reference(i.alloc());
                const s = Il2Cpp.reference(a.alloc());
                const c = (i, a) => {
                  try {
                    if (a) {
                      l.invoke(e, r, s);
                    } else {
                      invokeInstance(l, i, e, r, s);
                    }
                    const c = getRefValue(r);
                    const d = getRefValue(s);
                    if (!c || c.isNull?.()) {
                      return false;
                    }
                    const u = setVisualEffectObjectTransform(c, t, o);
                    if (u && !u.isNull?.()) {
                      pinVfxObject(u, t, o, n, c);
                    }
                    forceVisualEffectPosition(c, t);
                    forceVfxEventAttributePosition(d, t);
                    try {
                      c.method("Reinit").invoke();
                    } catch (e) {}
                    forceVisualEffectPosition(c, t);
                    forceVfxEventAttributePosition(d, t);
                    let m = false;
                    for (const e of ["OnPlay", "Play", "Spawn", "OnSpawn"]) {
                      try {
                        if (d && !d.isNull?.()) {
                          c.method("SendEvent", 2).invoke(Il2Cpp.string(e), d);
                        } else {
                          c.method("SendEvent", 1).invoke(Il2Cpp.string(e));
                        }
                        m = true;
                        break;
                      } catch (e) {}
                    }
                    if (!m) {
                      try {
                        c.method("Play").invoke();
                        m = true;
                      } catch (e) {}
                    }
                    forceVisualEffectPosition(c, t);
                    return m;
                  } catch (e) {
                    return false;
                  }
                };
                if (_vfxPoolReceiverCache !== undefined) {
                  if (c(_vfxPoolReceiverCache, _vfxPoolReceiverCache === null)) {
                    return true;
                  }
                  _vfxPoolReceiverCache = undefined;
                }
                if (c(null, true)) {
                  _vfxPoolReceiverCache = null;
                  return true;
                }
                for (const e of getSingletonInstanceCandidates(VFXManagerClass)) {
                  if (c(e, false)) {
                    _vfxPoolReceiverCache = e;
                    return true;
                  }
                }
              } catch (e) {
                console.error("[VisualEffectAsset] VFXManager.Pull failed:", e);
              }
              return false;
            }
            function playVisualEffectComponent(e, t, o, n = 4) {
              let i = null;
              try {
                const a = getClassByFullName("UnityEngine.VFX.VisualEffect");
                if (!a) {
                  console.error("[VisualEffectAsset] UnityEngine.VFX.VisualEffect class not found");
                  return false;
                }
                i = createEmptyObject("RPEarnVisualEffect", null);
                try {
                  getTransform(i).method("set_position").invoke(t);
                } catch (e) {}
                try {
                  getTransform(i).method("set_rotation").invoke(o);
                } catch (e) {}
                const l = addComponent(i, a);
                pinVfxObject(i, t, o, n, l);
                try {
                  l.method("set_visualEffectAsset").invoke(e);
                } catch (t) {
                  try {
                    l.field("visualEffectAsset").value = e;
                  } catch (e) {}
                }
                forceVisualEffectPosition(l, t);
                try {
                  l.method("Reinit").invoke();
                } catch (e) {}
                forceVisualEffectPosition(l, t);
                try {
                  l.method("Play").invoke();
                } catch (e) {
                  sendVisualEffectPlayEvent(l, t);
                }
                forceVisualEffectPosition(l, t);
                sendVisualEffectPlayEvent(l, t);
                try {
                  Object.method("Destroy", 2).invoke(i, n);
                } catch (e) {}
                return true;
              } catch (e) {
                console.error("[VisualEffectAsset] component play failed:", e);
                try {
                  if (i && !i.isNull?.()) {
                    Object.method("Destroy", 2).invoke(i, 0.1);
                  }
                } catch (e) {}
                return false;
              }
            }
            function playVisualEffectAssetDirect(e, t, o, n = 4) {
              const i = getVisualEffectAssetObject(e);
              if (!i || i.isNull?.()) {
                console.error("[VisualEffectAsset] GetObject returned null for id " + e);
                return false;
              } else {
                return !!playVisualEffectFromManagerPool(i, t, o, n) || playVisualEffectComponent(i, t, o, n);
              }
            }
            function playVisualEffectAsset(e, t, o, n = 4) {
              try {
                return playVisualEffectAssetDirect(e, t, o, n);
              } catch (e) {
                console.error("[VisualEffectAsset] play failed:", e);
              }
              return false;
            }
            function compileVisualEffectAssetNow(e) {
              if (!_compiledVisualEffectAssets.has(e)) {
                try {
                  const t = getVisualEffectAssetObject(e);
                  if (!t || t.isNull?.()) {
                    return;
                  }
                  if (playVisualEffectFromManagerPool(t, [0, -10000, 0], identityQuaternion, 0.05)) {
                    _compiledVisualEffectAssets.add(e);
                  }
                } catch (t) {
                  console.error("[VFX compile warmup] id " + e, t);
                }
              }
            }
            function prewarmAllVfxDispatch() {
              if (!_allVfxDispatchPrewarmed) {
                try {
                  getVfxRpc6();
                  _vfxRunnerCache = getPrimaryVfxRunner() ?? getRunnerCandidates()[0] ?? null;
                } catch (e) {}
                try {
                  _visualEffectClassCache = getClassByFullName("UnityEngine.VFX.VisualEffect");
                  _vfxEventAttributeClassCache = getClassByFullName("UnityEngine.VFX.VFXEventAttribute");
                  if (VFXManagerClass) {
                    try {
                      _vfxPoolPullCache = VFXManagerClass.method("Pull", 3);
                    } catch (e) {}
                    const e = getSingletonInstanceCandidates(VFXManagerClass);
                    if (e.length > 0) {
                      _vfxPoolReceiverCache = e[0];
                    }
                  }
                  for (const e of VisualEffectAssetEntries) {
                    getVisualEffectAssetObject(e[1]);
                  }
                  const e = getSelectedVfxEntry();
                  if (e[2] === "asset") {
                    compileVisualEffectAssetNow(e[1]);
                  }
                } catch (e) {
                  console.error("[VFX prewarm]", e);
                }
                _allVfxDispatchPrewarmed = true;
              }
            }
            function buildRewardArgs(e, t, o) {
              return e.parameters.map(e => {
                const n = String(e.type?.name ?? "");
                if (n.indexOf("AnimalCompany.NetPlayer") >= 0) {
                  return t;
                }
                if (n.indexOf("Fusion.PlayerRef") >= 0) {
                  try {
                    return t.method("get_PlayerRef").invoke();
                  } catch (e) {}
                  try {
                    return t.field("_playerRef").value;
                  } catch (e) {}
                  return 0;
                }
                if (n.indexOf("System.String") >= 0) {
                  try {
                    return Il2Cpp.string(String(t.method("get_playerId").invoke()));
                  } catch (e) {}
                  return Il2Cpp.string("");
                }
                if (/(Byte|SByte|Int16|UInt16|Int32|UInt32|Int64|UInt64|Single|Double)/.test(n)) {
                  return o;
                }
                if (n.indexOf("System.Boolean") >= 0) {
                  return true;
                }
                try {
                  if (e.type?.class?.isEnum) {
                    return 0;
                  }
                } catch (e) {}
                try {
                  if (e.type?.class?.isValueType) {
                    return makeZeroValueForType(e.type);
                  }
                } catch (e) {}
              });
            }
            function getRewardMethodCandidates(e) {
              if (rewardMethodCache[e]) {
                return rewardMethodCache[e];
              }
              const t = e === "xp" ? /(xp|experience)/i : e === "research" ? /(research|rp|point)/i : /(coin|money|currency|nut)/i;
              const o = /(rpc|add|award|grant|give|earn|increment|credit|deposit)/i;
              const n = [];
              try {
                for (const e of AssemblyCSharp.classes) {
                  try {
                    const i = String(e.name ?? "");
                    const a = /(player|progress|reward|currency|wallet|research|xp|experience|session|rpc|manager)/i.test(i);
                    for (const l of e.methods) {
                      try {
                        if (l.isGeneric || l.parameterCount < 1 || l.parameterCount > 4) {
                          continue;
                        }
                        const r = String(l.name ?? "");
                        const s = l.parameters.map(e => String(e.type?.name ?? "")).join("|");
                        if (!o.test(r) || !t.test(r + "|" + i)) {
                          continue;
                        }
                        if (!/(Int|Single|Double|Player|String|PlayerRef)/i.test(s)) {
                          continue;
                        }
                        let c = 0;
                        if (t.test(r)) {
                          c += 20;
                        }
                        if (/^RPC_/i.test(r)) {
                          c += 10;
                        }
                        if (/add|award|grant|give/i.test(r)) {
                          c += 8;
                        }
                        if (a) {
                          c += 4;
                        }
                        n.push({
                          method: l,
                          klass: e,
                          score: c,
                          sig: i + "." + r + "(" + s + ")"
                        });
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
              } catch (e) {
                console.error("[Reward] scan failed:", e);
              }
              n.sort((e, t) => t.score - e.score);
              rewardMethodCache[e] = n;
              if (n.length > 0) {
                console.log("[Reward] " + e + " candidates: " + n.slice(0, 10).map(e => e.sig + " score=" + e.score).join(" | "));
              } else {
                console.error("[Reward] no " + e + " candidates found");
              }
              return n;
            }
            function tryRewardMethodOnPlayer(e, t, o) {
              if (e === "coins") {
                try {
                  t.method("RPC_AddPlayerMoney").invoke(o);
                  return true;
                } catch (e) {}
              }
              for (const n of getRewardMethodCandidates(e)) {
                try {
                  const e = buildRewardArgs(n.method, t, o);
                  try {
                    n.method.invoke(...e);
                    return true;
                  } catch (e) {}
                  try {
                    invokeInstance(n.method, t, ...e);
                    return true;
                  } catch (e) {}
                  for (const t of getSingletonInstanceCandidates(n.klass)) {
                    try {
                      invokeInstance(n.method, t, ...e);
                      return true;
                    } catch (e) {}
                  }
                } catch (e) {}
              }
              return false;
            }
            function getRewardVfxPlayerPosition(e, t) {
              const o = (e, t) => {
                try {
                  if (e) {
                    return Vector3.method("op_Addition", 2).invoke(e, t);
                  } else {
                    return null;
                  }
                } catch (o) {
                  try {
                    return Vector3.method("op_Addition").invoke(e, t);
                  } catch (e) {}
                }
                return e;
              };
              const n = (e, t = [0, 0, 0]) => {
                try {
                  if (!e || e.isNull?.()) {
                    return null;
                  }
                  const n = getTransform(e);
                  if (!n || n.isNull?.()) {
                    return null;
                  } else {
                    return o(n.method("get_position").invoke(), t);
                  }
                } catch (e) {
                  return;
                }
              };
              try {
                let t = null;
                try {
                  t = n(e.field("headCollider").value, [0, 0.12, 0]);
                  if (t) {
                    return t;
                  }
                } catch (e) {}
                try {
                  t = n(e.field("bodyCollider").value, [0, 0.45, 0]);
                  if (t) {
                    return t;
                  }
                } catch (e) {}
                try {
                  const n = getPlayerHandAnchorTransform(e, 1);
                  if (n && !n.isNull?.() && (t = o(n.method("get_position").invoke(), [0, 0.1, 0]), t)) {
                    return t;
                  }
                } catch (e) {}
                try {
                  t = n(e.method("get_playerView").invoke(), [0, 0.55, 0]);
                  if (t) {
                    return t;
                  }
                } catch (e) {}
                try {
                  t = n(e.method("get_gameObject").invoke(), [0, 0.75, 0]);
                  if (t) {
                    return t;
                  }
                } catch (e) {}
                try {
                  t = n(e, [0, 0.75, 0]);
                  if (t) {
                    return t;
                  }
                } catch (e) {}
              } catch (e) {}
              return t;
            }
            function grantRewardAll(e, t, o, n, i) {
              let a = 0;
              let l = 0;
              try {
                for (const n of getAllNetPlayersList(true)) {
                  try {
                    if (tryRewardMethodOnPlayer(e, n, t)) {
                      a++;
                    }
                    const r = getRewardVfxPlayerPosition(n, i);
                    if (r && playVisualEffectAsset(o, r, identityQuaternion)) {
                      l++;
                    }
                  } catch (e) {}
                }
                if (l === 0 && i && playVisualEffectAsset(o, i, identityQuaternion)) {
                  l++;
                }
              } catch (e) {
                console.error("[Reward] " + n + " failed:", e);
              }
              return {
                count: a,
                vfx: l
              };
            }
            function rayHitPoint(e) {
              try {
                if (!e || e.isNull?.()) {
                  return null;
                } else {
                  return e.method("get_point").invoke();
                }
              } catch (e) {
                return;
              }
            }
            function getGunPointerLandingPosition(e) {
              try {
                if (e?.gunPointer && !e.gunPointer.isNull?.()) {
                  return getTransform(e.gunPointer).method("get_position").invoke();
                }
              } catch (e) {}
              return e?.endPosition ?? zeroVector;
            }
            function syncVfxCategoryButtons() {
              try {
                for (const e of buttons.flat()) {
                  if (e.buttonText === "VFX Gun") {
                    e.enabled = vfxGunEnabled;
                  }
                  if (e.buttonText === "RAM VFX Gun") {
                    e.enabled = ramVfxGunEnabled;
                  }
                  if (e.buttonText === "RPEarn VFX Gun") {
                    e.enabled = rpEarnVfxGunEnabled;
                  }
                }
              } catch (e) {}
            }
            function fireClientVfxAt(e, t) {
              let o = null;
              try {
                const n = AssemblyCSharp.class(t.className);
                o = createEmptyObject(t.label + "_ClientVFX", null);
                try {
                  getTransform(o).method("set_position").invoke(e);
                } catch (e) {}
                try {
                  getTransform(o).method("set_rotation").invoke(identityQuaternion);
                } catch (e) {}
                pinVfxObject(o, e, identityQuaternion, 2);
                const i = addComponent(o, n);
                const a = ["Play", "Spawn", "Trigger", "Emit", "Start", "Activate", "PlayEffect", "SpawnEffect"];
                if (tryCallNames(i, a, 0)) {
                  return true;
                }
                if (tryCallNames(i, a, 1, e)) {
                  return true;
                }
                if (tryCallNames(i, a, 2, e, identityQuaternion)) {
                  return true;
                }
              } catch (e) {
                console.error("[ClientVFX] " + t.label + " class path failed:", e);
              } finally {
                try {
                  if (o && !o.isNull?.()) {
                    Object.method("Destroy", 2).invoke(o, 2);
                  }
                } catch (e) {}
              }
              return false;
            }
            function tryInvokeVfxMethod(e, t, o, n) {
              const i = [[o, n, identityQuaternion], [n, o, identityQuaternion], [t, o, n, identityQuaternion], [t, n, o, identityQuaternion], [o, n], [n, o], [o], [n]];
              try {
                for (const t of [ParticleManagerClass, VFXManagerClass]) {
                  if (t) {
                    for (const o of i) {
                      try {
                        const n = tryMethodName(t, [e], o.length);
                        if (!n) {
                          continue;
                        }
                        n.invoke(...o);
                        return true;
                      } catch (e) {}
                    }
                  }
                }
              } catch (e) {}
              try {
                const t = [SFXManager, ParticleManagerClass, VFXManagerClass];
                for (const o of t) {
                  if (o) {
                    for (const t of getSingletonInstanceCandidates(o)) {
                      for (const o of i) {
                        try {
                          if (tryCallNames(t, [e], o.length, ...o)) {
                            return true;
                          }
                        } catch (e) {}
                      }
                    }
                  }
                }
              } catch (e) {}
              return false;
            }
            function tryDirectPlayVfx(e, t, o) {
              try {
                if (ParticleManagerClass) {
                  try {
                    ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(e, t, o, identityQuaternion);
                    return true;
                  } catch (e) {}
                  try {
                    ParticleManagerClass.method("RPC_PlayVFXRemoteOnly", 4).invoke(e, t, o, identityQuaternion);
                    return true;
                  } catch (e) {}
                  try {
                    ParticleManagerClass.method("RPC_VFX", 4).invoke(e, t, o, identityQuaternion);
                    return true;
                  } catch (e) {}
                }
              } catch (e) {}
              try {
                if (VFXManagerClass) {
                  try {
                    VFXManagerClass.method("RPC_PlayVFX", 4).invoke(e, t, o, identityQuaternion);
                    return true;
                  } catch (e) {}
                  try {
                    VFXManagerClass.method("RPC_VFX", 4).invoke(e, t, o, identityQuaternion);
                    return true;
                  } catch (e) {}
                }
              } catch (e) {}
              return false;
            }
            function playVfxGunIdAt(e, t) {
              let o = null;
              const n = [];
              const i = e => {
                try {
                  if (!e || e.isNull?.() || n.some(t => normalizeSceneObjectHandle(t) === normalizeSceneObjectHandle(e))) {
                    return;
                  }
                  n.push(e);
                } catch (e) {}
              };
              const a = n => {
                try {
                  return !!n && !n.isNull?.() && (ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(n, e, t, identityQuaternion), true);
                } catch (e) {
                  o = e;
                  return false;
                }
              };
              try {
                try {
                  i(SFXManager.field("_instance").value.method("get__currentRunner").invoke());
                } catch (e) {
                  o = e;
                }
                i(getPrimaryVfxRunner());
                for (const e of getRunnerCandidates()) {
                  i(e);
                }
                for (const e of n) {
                  if (a(e)) {
                    return true;
                  }
                }
              } catch (e) {
                o = e;
              }
              try {
                const o = vfxGunWorkingMethodName ? [vfxGunWorkingMethodName] : getDynamicVfxMethodNames();
                for (const i of n) {
                  for (const n of o) {
                    if (tryInvokeVfxMethod(n, i, e, t)) {
                      vfxGunWorkingMethodName = n;
                      console.log("[VFX Gun] using VFX method: " + n);
                      return true;
                    }
                  }
                }
              } catch (e) {
                o = e;
              }
              if (time >= vfxGunMethodLogTime) {
                vfxGunMethodLogTime = time + 10;
                const t = getDynamicVfxMethodNames().join(", ");
                console.error("[VFX Gun] no usable VFX method for id " + e + ". RPC_PlayVFX error:", o);
                console.log("[VFX Gun] VFX-ish methods found: " + t);
              }
              return false;
            }
            function getDynamicVfxMethodNames() {
              if (vfxMethodNamesCache) {
                return vfxMethodNamesCache;
              }
              const e = new Set(["RPC_PlayVFX", "PlayVFX", "SpawnVFX", "TriggerVFX", "PlayEffect", "SpawnEffect", "RPC_PlayVfx", "PlayVfx", "SpawnVfx", "TriggerVfx", "PlayParticle", "SpawnParticle", "RPC_VFX", "RPC_PlayVFXRemoteOnly", "ExecuteNetworkVFX", "NetworkVFXInternal", "RPC_NetworkVFX0", "RPC_NetworkVFX1", "RPC_NetworkVFX2", "RPC_NetworkVFX3", "RPC_NetworkVFX4", "PlayEffects", "DoEffects"]);
              try {
                for (const t of [ParticleManagerClass, SFXManager, VFXManagerClass]) {
                  if (t) {
                    for (const o of t.methods) {
                      try {
                        const t = String(o.name ?? "");
                        if (/vfx|effect|particle/i.test(t)) {
                          e.add(t);
                        }
                      } catch (e) {}
                    }
                  }
                }
              } catch (e) {}
              return vfxMethodNamesCache = Array.from(e);
            }
            function playSelectedVfxAt(e) {
              try {
                if (time < vfxDispatchUnavailableUntil) {
                  return false;
                }
                const [t, o] = getSelectedVfxEntry();
                const n = [o === VFXTypes.None ? VFXTypes.ConfettiBurst : o, VFXTypes.ConfettiBurst, VFXTypes.GreenBlink, VFXTypes.FuelExplosion].filter((e, t, o) => o.indexOf(e) === t);
                let i = false;
                const a = getPrimaryVfxRunner();
                const l = getDynamicVfxMethodNames();
                if (a && !a.isNull?.()) {
                  for (const t of n) {
                    if (tryDirectPlayVfx(a, t, e) && (i = true, t === n[0])) {
                      return true;
                    }
                    for (const o of l) {
                      if (tryInvokeVfxMethod(o, a, t, e)) {
                        i = true;
                        if (t === n[0]) {
                          return true;
                        }
                        break;
                      }
                    }
                  }
                }
                for (const t of getRunnerCandidates()) {
                  for (const o of n) {
                    if (tryDirectPlayVfx(t, o, e) && (i = true, o === n[0])) {
                      return true;
                    }
                    for (const a of l) {
                      if (tryInvokeVfxMethod(a, t, o, e)) {
                        i = true;
                        if (o === n[0]) {
                          return true;
                        }
                        break;
                      }
                    }
                  }
                }
                if (!i) {
                  vfxDispatchUnavailableUntil = time + 4;
                  if (time >= vfxNoRunnerLogTime) {
                    vfxNoRunnerLogTime = time + 15;
                    console.error("[VFX] no runner accepted VFX", t);
                  }
                }
                return i;
              } catch (e) {
                console.error("[VFX] playSelectedVfxAt failed:", e);
                return false;
              }
            }
            function spawnLocalVfxFallback(e) {
              try {
                const t = readVec3Components(e);
                const o = [[1, 0.55, 0.08, 0.92], [1, 0.82, 0.18, 0.92], [0.96, 0.96, 0.96, 0.88]];
                for (let e = 0; e < 4; e++) {
                  const n = createObject([t[0] + (Math.random() * 0.35 - 0.175), t[1] + (Math.random() * 0.35 - 0.05), t[2] + (Math.random() * 0.35 - 0.175)], identityQuaternion, [0.09 + Math.random() * 0.08, 0.09 + Math.random() * 0.08, 0.09 + Math.random() * 0.08], 1, o[e % o.length], null, false);
                  try {
                    Object.method("Destroy", 2).invoke(n, 0.45);
                  } catch (e) {}
                }
              } catch (e) {}
            }
            class ButtonInfo {
              constructor(e) {
                this.buttonText = e.buttonText;
                this.method = e.method;
                this.enableMethod = e.enableMethod;
                this.disableMethod = e.disableMethod;
                this.isTogglable = e.isTogglable ?? true;
                this.toolTip = e.toolTip ?? null;
                this.enabled = e.enabled ?? false;
                this.getLabel = e.getLabel;
              }
            }
            let isPlayingSound = false;
            let soundEndTime = 0;
            let currentPlayingClip = null;
            let soundPreviewObject = null;
            let soundPreviewSource = null;
            let pendingSoundLoad = null;
            let soundRecorder = null;
            let soundRecorderState = null;
            let retiredSoundClips = [];
            const SOUNDBOARD_MAX_SECONDS = 3600;
            const SOUNDBOARD_OUTPUT_RATE = 48000;
            function getFileNameWithoutExtension(e) {
              const t = e.replace(/\\/g, "/").split("/");
              const o = t[t.length - 1];
              const n = o.lastIndexOf(".");
              if (n === -1) {
                return o;
              } else {
                return o.substring(0, n);
              }
            }
            function getFileExtension(e) {
              const t = e.split("?")[0].split("#")[0];
              const o = t.lastIndexOf(".");
              if (o === -1) {
                return "";
              } else {
                return t.substring(o + 1).toLowerCase();
              }
            }
            function getUnityFileUrl(e) {
              let t = e.replace(/\\/g, "/");
              if (!t.startsWith("/")) {
                t = "/" + t;
              }
              return encodeURI("file://" + t).replace(/#/g, "%23");
            }
            function getRecorder() {
              try {
                const e = NetPlayer.method("get_localPlayer").invoke();
                if (e && !e.isNull?.()) {
                  const t = e.field("_recorder").value;
                  if (t && !t.isNull?.()) {
                    return t;
                  }
                }
              } catch (e) {}
              try {
                const e = Il2Cpp.domain.assembly("PhotonVoice").image.class("Photon.Voice.Unity.VoiceConnection");
                const t = Object.method("FindObjectOfType", 0).inflate(e).invoke();
                if (t && !t.isNull?.()) {
                  const e = t.method("get_PrimaryRecorder").invoke();
                  if (e && !e.isNull?.()) {
                    return e;
                  }
                }
              } catch (e) {}
              try {
                const e = Il2Cpp.domain.assembly("PhotonVoice").image.class("Photon.Voice.Unity.Recorder");
                const t = Object.method("FindObjectOfType", 0).inflate(e).invoke();
                if (t && !t.isNull()) {
                  return t;
                }
              } catch (e) {
                console.error("[Soundboard] getRecorder error:", e);
              }
              return null;
            }
            function trySetRecorderValue(e, t, o) {
              try {
                e.method("set_" + t, 1).invoke(o);
                return true;
              } catch (e) {}
              try {
                e.method("set_" + t).invoke(o);
                return true;
              } catch (e) {}
              return false;
            }
            function tryGetRecorderValue(e, t, o) {
              try {
                return e.method("get_" + t, 0).invoke();
              } catch (e) {}
              try {
                return e.method("get_" + t).invoke();
              } catch (e) {}
              return o;
            }
            function restartRecorder(e) {
              try {
                e.method("RestartRecording", 0).invoke();
                return true;
              } catch (e) {}
              try {
                e.method("RestartRecording").invoke();
                return true;
              } catch (e) {}
              return false;
            }
            function captureRecorderState(e) {
              return {
                sourceType: tryGetRecorderValue(e, "SourceType", 0),
                audioClip: tryGetRecorderValue(e, "AudioClip", NULL),
                loopAudioClip: tryGetRecorderValue(e, "LoopAudioClip", false),
                debugEchoMode: tryGetRecorderValue(e, "DebugEchoMode", false),
                transmitEnabled: tryGetRecorderValue(e, "TransmitEnabled", true),
                recordingEnabled: tryGetRecorderValue(e, "RecordingEnabled", true)
              };
            }
            function restoreSoundRecorder() {
              const e = soundRecorder;
              const t = soundRecorderState;
              soundRecorder = null;
              soundRecorderState = null;
              if (e && !e.isNull?.()) {
                try {
                  const o = t ?? {
                    sourceType: 0,
                    audioClip: NULL,
                    loopAudioClip: false,
                    debugEchoMode: false,
                    transmitEnabled: true,
                    recordingEnabled: true
                  };
                  trySetRecorderValue(e, "SourceType", o.sourceType);
                  trySetRecorderValue(e, "AudioClip", o.audioClip ?? NULL);
                  trySetRecorderValue(e, "LoopAudioClip", o.loopAudioClip);
                  trySetRecorderValue(e, "DebugEchoMode", o.debugEchoMode);
                  trySetRecorderValue(e, "TransmitEnabled", o.transmitEnabled);
                  trySetRecorderValue(e, "RecordingEnabled", o.recordingEnabled);
                  restartRecorder(e);
                  console.log("[Soundboard] Microphone recorder restored.");
                } catch (e) {
                  console.error("[Soundboard] Could not restore microphone recorder:", e);
                }
              }
            }
            function retireSoundClip(e) {
              try {
                if (!e || e.isNull?.()) {
                  return;
                }
                if (retiredSoundClips.some(t => t.clip?.handle?.equals?.(e.handle))) {
                  return;
                }
                retiredSoundClips.push({
                  clip: e,
                  destroyAt: (time || 0) + 2
                });
              } catch (e) {}
            }
            function cleanupRetiredSoundClips() {
              for (let e = retiredSoundClips.length - 1; e >= 0; e--) {
                const t = retiredSoundClips[e];
                if (!((time || 0) < t.destroyAt)) {
                  try {
                    if (t.clip && !t.clip.isNull?.()) {
                      Object.method("Destroy", 1).invoke(t.clip);
                    }
                  } catch (e) {}
                  retiredSoundClips.splice(e, 1);
                }
              }
            }
            function getSoundPreviewSource() {
              try {
                if (soundPreviewSource && !soundPreviewSource.isNull?.()) {
                  return soundPreviewSource;
                }
              } catch (e) {}
              try {
                const e = UnityEngineAudio.class("UnityEngine.AudioSource");
                soundPreviewObject = createEmptyObject("__SoundboardPreview");
                try {
                  Object.method("DontDestroyOnLoad", 1).invoke(soundPreviewObject);
                } catch (e) {}
                soundPreviewSource = addComponent(soundPreviewObject, e);
                try {
                  soundPreviewSource.method("set_playOnAwake").invoke(false);
                } catch (e) {}
                try {
                  soundPreviewSource.method("set_loop").invoke(false);
                } catch (e) {}
                try {
                  soundPreviewSource.method("set_spatialBlend").invoke(0);
                } catch (e) {}
                try {
                  soundPreviewSource.method("set_volume").invoke(1);
                } catch (e) {}
                return soundPreviewSource;
              } catch (e) {
                console.error("[Soundboard] Could not create local preview AudioSource:", e);
                return null;
              }
            }
            function playLocalSoundPreview(e) {
              try {
                const t = getSoundPreviewSource();
                if (!t || t.isNull?.()) {
                  return;
                }
                try {
                  t.method("Stop").invoke();
                } catch (e) {}
                t.method("set_clip").invoke(e);
                try {
                  t.method("set_time").invoke(0);
                } catch (e) {}
                try {
                  t.method("set_mute").invoke(false);
                } catch (e) {}
                try {
                  t.method("set_enabled").invoke(true);
                } catch (e) {}
                try {
                  t.method("Play", 1).invoke(0);
                  return;
                } catch (e) {}
                t.method("Play").invoke();
              } catch (e) {
                console.error("[Soundboard] Local preview failed:", e);
              }
            }
            function stopLocalSoundPreview() {
              try {
                if (soundPreviewSource && !soundPreviewSource.isNull?.()) {
                  try {
                    soundPreviewSource.method("Stop").invoke();
                  } catch (e) {}
                  try {
                    soundPreviewSource.method("set_clip").invoke(NULL);
                  } catch (e) {}
                }
              } catch (e) {}
            }
            function stopSound() {
              const e = currentPlayingClip;
              try {
                if (pendingSoundLoad?.request && !pendingSoundLoad.request.isNull?.()) {
                  try {
                    pendingSoundLoad.request.method("Abort").invoke();
                  } catch (e) {}
                }
              } catch (e) {}
              pendingSoundLoad = null;
              stopLocalSoundPreview();
              restoreSoundRecorder();
              isPlayingSound = false;
              currentPlayingClip = null;
              soundEndTime = 0;
              retireSoundClip(e);
            }
            function managedNumber(e) {
              if (typeof e == "number") {
                return e;
              }
              if (typeof e == "bigint") {
                return Number(e);
              }
              try {
                const t = Number(e?.value ?? e?.toString?.() ?? e);
                if (Number.isFinite(t)) {
                  return t;
                } else {
                  return 0;
                }
              } catch (e) {
                return 0;
              }
            }
            function readManagedStreamChunk(e, t, o, n) {
              if (!e || e.isNull?.() || n <= 0) {
                return null;
              }
              const i = Math.floor(n);
              const a = Il2Cpp.array(t, i);
              try {
                e.method("set_Position", 1).invoke(o);
              } catch (t) {
                e.method("Seek", 2).invoke(o, 0);
              }
              let l = 0;
              while (l < i) {
                const t = managedNumber(e.method("Read", 3).invoke(a, l, i - l));
                if (t <= 0) {
                  break;
                }
                l += t;
              }
              if (l <= 0) {
                return null;
              }
              const r = a.elements.handle.readByteArray(l);
              if (r) {
                return new Uint8Array(r);
              } else {
                return null;
              }
            }
            function loadWavFile(e) {
              let t = null;
              try {
                const o = Il2Cpp.domain.assembly("mscorlib").image;
                const n = o.class("System.IO.File");
                const i = o.class("System.Byte");
                t = n.method("OpenRead", 1).invoke(Il2Cpp.string(e));
                if (!t || t.isNull?.()) {
                  throw new Error("Could not open WAV file.");
                }
                const a = managedNumber(t.method("get_Length", 0).invoke());
                if (a < 44) {
                  throw new Error("WAV file is too small.");
                }
                const l = readManagedStreamChunk(t, i, 0, Math.min(a, 65536));
                if (!l || l.length < 44) {
                  throw new Error("Could not read WAV header.");
                }
                const r = new DataView(l.buffer, l.byteOffset, l.byteLength);
                if (r.getUint32(0, true) !== 1179011410 || r.getUint32(8, true) !== 1163280727) {
                  throw new Error("Not a RIFF/WAVE file.");
                }
                let s = 12;
                let c = 0;
                let d = 0;
                let u = 0;
                let m = 0;
                let h = 0;
                let g = 0;
                let f = 0;
                while (s + 8 <= l.length) {
                  const e = r.getUint32(s, true);
                  const t = r.getUint32(s + 4, true);
                  const o = s + 8;
                  if (e === 544501094) {
                    if (t < 16 || o + 16 > l.length) {
                      throw new Error("Invalid WAV fmt chunk.");
                    }
                    c = r.getUint16(o, true);
                    d = r.getUint16(o + 2, true);
                    u = r.getUint32(o + 4, true);
                    h = r.getUint16(o + 12, true);
                    m = r.getUint16(o + 14, true);
                  } else if (e === 1635017060) {
                    g = o;
                    f = t;
                    break;
                  }
                  const n = o + t + (t & 1);
                  if (n <= s || n > l.length) {
                    break;
                  }
                  s = n;
                }
                if (!g || !f) {
                  throw new Error("WAV data chunk was not found in the first 64 KB.");
                }
                if (c !== 1 && c !== 3) {
                  throw new Error("Only PCM or IEEE-float WAV files are supported.");
                }
                if (d < 1 || d > 8) {
                  throw new Error("Unsupported WAV channel count: " + d);
                }
                if (u < 8000 || u > 192000) {
                  throw new Error("Unsupported WAV sample rate: " + u);
                }
                if (![8, 16, 24, 32].includes(m)) {
                  throw new Error("Unsupported WAV bit depth: " + m);
                }
                if (c === 3 && m !== 32) {
                  throw new Error("IEEE-float WAV must use 32-bit samples.");
                }
                const p = m / 8;
                const y = d * p;
                h ||= y;
                if (h < y) {
                  throw new Error("Invalid WAV block alignment.");
                }
                const b = Math.max(0, Math.min(f, a - g));
                const _ = Math.floor(b / h);
                const v = Math.min(_, Math.floor(u * SOUNDBOARD_MAX_SECONDS));
                if (v <= 0) {
                  throw new Error("WAV contains no readable samples.");
                }
                const T = readManagedStreamChunk(t, i, g, v * h);
                if (!T || T.length < h) {
                  throw new Error("Could not read WAV sample data.");
                }
                try {
                  t.method("Close", 0).invoke();
                } catch (e) {}
                t = null;
                const k = Math.floor(T.length / h);
                const N = Math.max(1, Math.floor(k * SOUNDBOARD_OUTPUT_RATE / u));
                const S = new Float32Array(N);
                const C = new DataView(T.buffer, T.byteOffset, T.byteLength);
                const w = e => {
                  if (c === 3) {
                    return C.getFloat32(e, true);
                  }
                  if (m === 8) {
                    return (C.getUint8(e) - 128) / 128;
                  }
                  if (m === 16) {
                    return C.getInt16(e, true) / 32768;
                  }
                  if (m === 24) {
                    let t = C.getUint8(e) | C.getUint8(e + 1) << 8 | C.getUint8(e + 2) << 16;
                    if (t & 8388608) {
                      t |= 4278190080;
                    }
                    return t / 8388608;
                  }
                  return C.getInt32(e, true) / 2147483648;
                };
                for (let e = 0; e < N; e++) {
                  const t = Math.min(k - 1, Math.floor(e * u / SOUNDBOARD_OUTPUT_RATE)) * h;
                  let o = 0;
                  for (let e = 0; e < d; e++) {
                    o += w(t + e * p);
                  }
                  o /= d;
                  S[e] = Number.isFinite(o) ? Math.max(-1, Math.min(1, o)) : 0;
                }
                const x = Il2Cpp.string(getFileNameWithoutExtension(e) + "_voice");
                const I = AudioClipClass.method("Create", 5).invoke(x, N, 1, SOUNDBOARD_OUTPUT_RATE, false);
                if (!I || I.isNull?.()) {
                  throw new Error("Unity could not create the AudioClip.");
                }
                const P = o.class("System.Single");
                const M = Il2Cpp.array(P, N);
                M.elements.handle.writeByteArray(S.buffer);
                I.method("SetData", 2).invoke(M, 0);
                if (_ > v) {
                  sendNotification("Large sound: broadcasting the first 60 minutes.", false);
                }
                console.log("[Soundboard] Prepared " + N + " mono 48 kHz frames from " + e);
                return {
                  clip: I,
                  duration: N / SOUNDBOARD_OUTPUT_RATE,
                  sampleRate: SOUNDBOARD_OUTPUT_RATE,
                  channels: 1
                };
              } catch (e) {
                return null;
              } finally {
                if (t && !t.isNull?.()) {
                  try {
                    t.method("Close", 0).invoke();
                  } catch (e) {}
                }
              }
            }
            function startMp3Load(e) {
              console.error("[Soundboard] MP3 broadcast is disabled for crash safety: " + e);
              sendNotification("Convert MP3 to WAV first (up to 60 minutes are loaded).", false);
              return true;
            }
            function playLoadedSoundData(e, t) {
              try {
                const o = getRecorder();
                if (!o || o.isNull?.()) {
                  throw new Error("The local Photon Voice recorder was not found.");
                }
                stopLocalSoundPreview();
                soundRecorder = o;
                soundRecorderState = captureRecorderState(o);
                const n = trySetRecorderValue(o, "SourceType", 1);
                const i = trySetRecorderValue(o, "AudioClip", t.clip);
                const a = trySetRecorderValue(o, "LoopAudioClip", false);
                const l = trySetRecorderValue(o, "DebugEchoMode", true);
                const r = trySetRecorderValue(o, "TransmitEnabled", true);
                const s = trySetRecorderValue(o, "RecordingEnabled", true);
                const c = restartRecorder(o);
                if (!n || !i || !c) {
                  throw new Error("Photon Voice rejected the AudioClip source.");
                }
                isPlayingSound = true;
                soundEndTime = time + t.duration + 0.1;
                currentPlayingClip = t.clip;
                console.log("[Soundboard] Broadcasting " + e + " duration=" + t.duration + " source=" + n + " clip=" + i + " loop=" + a + " echo=" + l + " transmit=" + r + " recording=" + s);
                sendNotification("Broadcasting through mic: " + getFileNameWithoutExtension(e), false);
              } catch (e) {
                console.error("[Soundboard] Error playing sound:", e);
                restoreSoundRecorder();
                isPlayingSound = false;
                soundEndTime = 0;
                currentPlayingClip = null;
                retireSoundClip(t?.clip);
                sendNotification("<color=red>Mic broadcast could not start.</color>", false);
              }
            }
            function finishPendingSoundLoad() {
              if (pendingSoundLoad) {
                try {
                  const e = pendingSoundLoad.operation;
                  let t = false;
                  try {
                    t = e.method("get_isDone").invoke();
                  } catch (e) {}
                  if (!t) {
                    if ((time || 0) - pendingSoundLoad.startedAt > 10) {
                      try {
                        pendingSoundLoad.request.method("Abort").invoke();
                      } catch (e) {}
                      console.error("[Soundboard] MP3 decode timed out: " + pendingSoundLoad.filePath);
                      sendNotification("<color=red>MP3 decode timed out.</color>", false);
                      pendingSoundLoad = null;
                    }
                    return;
                  }
                  const o = pendingSoundLoad;
                  pendingSoundLoad = null;
                  try {
                    const e = o.request.method("get_error").invoke();
                    const t = String(e?.content ?? e?.toString?.() ?? "");
                    if (t && t !== "null") {
                      console.error("[Soundboard] MP3 load error: " + t);
                    }
                  } catch (e) {}
                  const n = o.downloadHandlerAudioClipClass.method("GetContent", 1).invoke(o.request);
                  if (!n || n.isNull?.()) {
                    console.error("[Soundboard] Unity returned no MP3 AudioClip: " + o.filePath);
                    sendNotification("<color=red>Error decoding MP3.</color>", false);
                    return;
                  }
                  let i = 0;
                  try {
                    i = n.method("get_length").invoke();
                  } catch (e) {}
                  if (!i || i <= 0) {
                    i = 1;
                  }
                  playLoadedSoundData(o.filePath, {
                    clip: n,
                    duration: i,
                    sampleRate: 0,
                    channels: 0
                  });
                } catch (e) {
                  console.error("[Soundboard] Error finishing sound load:", e);
                  sendNotification("<color=red>Error finishing sound load.</color>", false);
                  pendingSoundLoad = null;
                }
              }
            }
            function playSound(e) {
              if (isPlayingSound || pendingSoundLoad) {
                stopSound();
              }
              const t = getFileExtension(e);
              if (t === "wav") {
                sendNotification("Loading sound...", false);
                const t = loadWavFile(e);
                if (t) {
                  playLoadedSoundData(e, t);
                  return;
                } else {
                  sendNotification("<color=red>Error loading WAV file.</color>", false);
                  return;
                }
              }
              if (t === "mp3") {
                sendNotification("Decoding MP3...", false);
                if (!startMp3Load(e)) {
                  sendNotification("<color=red>Error starting MP3 load.</color>", false);
                }
                return;
              }
              sendNotification("<color=red>Unsupported sound file.</color>", false);
            }
            function getScriptDir() {
              return "C:\\Program Files (x86)\\Steam\\steamapps\\common\\Animal Company";
            }
            function rebuildSoundboardButtons() {
              const e = [new ButtonInfo({
                buttonText: "Exit Soundboard",
                method: () => {
                  currentCategory = 11;
                  currentPage = 0;
                },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
              }), new ButtonInfo({
                buttonText: "Reload Sounds",
                method: () => {
                  rebuildSoundboardButtons();
                  reloadMenu();
                  sendNotification("Sounds reloaded!", false);
                },
                isTogglable: false,
                toolTip: "Rescan the sounds folder for .wav and .mp3 files."
              }), new ButtonInfo({
                buttonText: "Stop All",
                method: () => {
                  stopSound();
                },
                isTogglable: false,
                toolTip: "Stops local soundboard playback."
              })];
              try {
                const t = Il2Cpp.domain.assembly("mscorlib").image.class("System.IO.Directory");
                const o = getScriptDir() + "/sounds";
                if (!t.method("Exists", 1).invoke(Il2Cpp.string(o))) {
                  t.method("CreateDirectory", 1).invoke(Il2Cpp.string(o));
                  console.log("[Soundboard] Created sounds directory at " + o);
                }
                const n = t.method("GetFiles", 2).invoke(Il2Cpp.string(o), Il2Cpp.string("*"));
                const i = [];
                if (n && !n.isNull()) {
                  for (let e = 0; e < n.length; e++) {
                    const t = n.get(e);
                    const o = String(t?.content ?? t?.toString?.() ?? "");
                    const a = getFileExtension(o);
                    if (a === "wav" || a === "mp3") {
                      i.push(o);
                    }
                  }
                }
                console.log("[Soundboard] Scanned " + o + " and found " + i.length + " sound file(s)");
                if (i.length === 0) {
                  e.push(new ButtonInfo({
                    buttonText: "No sounds found",
                    method: () => {
                      sendNotification("Add .wav or .mp3 files to " + o, false);
                    },
                    isTogglable: false,
                    toolTip: "Put your .wav or .mp3 files in the 'sounds' folder!"
                  }));
                } else {
                  for (const t of i) {
                    const o = getFileNameWithoutExtension(t);
                    e.push(new ButtonInfo({
                      buttonText: o,
                      method: () => {
                        playSound(t);
                      },
                      isTogglable: false,
                      toolTip: "Play " + o
                    }));
                  }
                }
              } catch (e) {
                console.error("[Soundboard] Error scanning directory:", e);
              }
              buttons[32] = e;
              rebuildButtonMap();
            }
            let loadedAssetBundles = [];
            let spawnedAssetBundleObjects = [];
            let assetBundleSpawnDelay = 0;
            let assetBundleSpawnTriggerLatched = false;
            let selectedAssetBundle = null;
            let selectedAssetBundlePath = "";
            let selectedAssetBundleAssets = [];
            let pendingAssetBundleLoad = null;
            let pendingAssetBundleAssetsLoad = null;
            function getAssetBundleClass() {
              for (const e of ["UnityEngine.AssetBundleModule", "UnityEngine.CoreModule", "UnityEngine"]) {
                try {
                  const t = Il2Cpp.domain.assembly(e).image.class("UnityEngine.AssetBundle");
                  if (t) {
                    return t;
                  }
                } catch (e) {}
              }
              return null;
            }
            function spawnSelectedBundleAsset(e, t, o = null) {
              try {
                if (!e || e.isNull?.()) {
                  sendNotification("Bundle asset is no longer loaded: " + t, false);
                  return;
                }
                let n = rightHandTransform;
                try {
                  if (headCollider && !headCollider.isNull?.()) {
                    n = getTransform(headCollider);
                  }
                } catch (e) {}
                if (!n || n.isNull?.()) {
                  sendNotification("Player transform is not ready", false);
                  return;
                }
                const i = getLaunchForward(n);
                const a = o ?? Vector3.method("op_Addition").invoke(n.method("get_position").invoke(), Vector3.method("op_Multiply", 2).invoke(i, 1.5));
                const l = Object.method("Instantiate", 3).invoke(e, a, identityQuaternion);
                if (!l || l.isNull?.()) {
                  sendNotification("Instantiate returned null", false);
                  return;
                }
                try {
                  l.method("set_name").invoke(Il2Cpp.string(t));
                } catch (e) {}
                spawnedAssetBundleObjects.push(l);
                sendNotification("Spawned bundle asset: " + t, false, 5);
              } catch (e) {
                console.error("[Asset Bundles] Spawn failed:", e);
                sendNotification("Bundle asset spawn failed: " + e, false);
              }
            }
            function updateBundleAssetSpawnGun(e, t) {
              if (!rightGrab) {
                assetBundleSpawnTriggerLatched = false;
                return;
              }
              let o = null;
              try {
                o = renderGun();
              } catch (e) {
                return;
              }
              if (!o) {
                return;
              }
              if (!rightTrigger) {
                assetBundleSpawnTriggerLatched = false;
                return;
              }
              if (assetBundleSpawnTriggerLatched || time < assetBundleSpawnDelay) {
                return;
              }
              assetBundleSpawnTriggerLatched = true;
              assetBundleSpawnDelay = time + 0.45;
              const n = getGunPointerLandingPosition(o);
              if (n) {
                spawnSelectedBundleAsset(e, t, n);
              }
            }
            function unloadAssetBundles() {
              let e = 0;
              for (const t of spawnedAssetBundleObjects) {
                try {
                  if (!t || t.isNull?.()) {
                    continue;
                  }
                  Object.method("Destroy", 1).invoke(t);
                  e++;
                } catch (e) {}
              }
              spawnedAssetBundleObjects = [];
              let t = 0;
              for (const e of loadedAssetBundles) {
                try {
                  if (!e.bundle || e.bundle.isNull?.()) {
                    continue;
                  }
                  e.bundle.method("Unload", 1).invoke(true);
                  t++;
                } catch (e) {
                  console.error("[Asset Bundles] Unload failed:", e);
                }
              }
              loadedAssetBundles = [];
              selectedAssetBundle = null;
              selectedAssetBundlePath = "";
              selectedAssetBundleAssets = [];
              pendingAssetBundleLoad = null;
              pendingAssetBundleAssetsLoad = null;
              sendNotification("Unloaded " + t + " bundle(s), removed " + e + " spawned object(s)", false, 5);
            }
            function rebuildAssetBundleAssetButtons() {
              const e = [new ButtonInfo({
                buttonText: "Exit Bundle Assets",
                method: () => {
                  currentCategory = 101;
                  currentPage = 0;
                },
                isTogglable: false,
                toolTip: "Returns to the Asset Bundles file list."
              })];
              for (const t of selectedAssetBundleAssets) {
                e.push(new ButtonInfo({
                  buttonText: t.name,
                  method: () => {
                    updateBundleAssetSpawnGun(t.asset, t.name);
                  },
                  enableMethod: () => {
                    assetBundleSpawnTriggerLatched = false;
                  },
                  disableMethod: () => {
                    assetBundleSpawnTriggerLatched = false;
                  },
                  isTogglable: true,
                  toolTip: "Hold right grip to aim, then pull trigger to spawn " + t.name + " at the pointer."
                }));
              }
              if (selectedAssetBundleAssets.length === 0) {
                e.push(new ButtonInfo({
                  buttonText: "No assets found",
                  method: () => {
                    sendNotification("This bundle reported no loadable assets", false);
                  },
                  isTogglable: false,
                  toolTip: "The selected bundle contains no named assets."
                }));
              }
              buttons[102] = e;
              rebuildButtonMap();
            }
            function openAssetBundleAssets(e, t) {
              selectedAssetBundle = t;
              selectedAssetBundlePath = e;
              selectedAssetBundleAssets = [];
              if (pendingAssetBundleAssetsLoad) {
                sendNotification("Bundle assets are already loading", false);
              } else {
                try {
                  const o = t.method("LoadAllAssetsAsync", 1).overload("System.Type").invoke(GameObject.type.object);
                  if (!o || o.isNull?.()) {
                    sendNotification("Could not start prefab loading", false);
                    return;
                  }
                  pendingAssetBundleAssetsLoad = {
                    path: e,
                    bundle: t,
                    request: o,
                    startedAt: time || 0
                  };
                  sendNotification("Loading prefabs from " + getFileNameWithoutExtension(e), false);
                } catch (e) {
                  console.error("[Asset Bundles] LoadAllAssetsAsync failed:", e);
                  sendNotification("Could not load bundle prefabs: " + e, false);
                }
              }
            }
            function finishPendingAssetBundleAssetsLoad() {
              const e = pendingAssetBundleAssetsLoad;
              if (e) {
                try {
                  if (!e.request.method("get_isDone").invoke()) {
                    if ((time || 0) - e.startedAt > 20) {
                      pendingAssetBundleAssetsLoad = null;
                      sendNotification("Bundle prefab load timed out", false);
                    }
                    return;
                  }
                  const t = e.request.method("get_allAssets", 0).invoke();
                  pendingAssetBundleAssetsLoad = null;
                  selectedAssetBundleAssets = [];
                  if (t && !t.isNull?.()) {
                    for (let e = 0; e < t.length; e++) {
                      try {
                        const o = t.get(e);
                        if (!o || o.isNull?.()) {
                          continue;
                        }
                        let n = "Prefab " + (e + 1);
                        try {
                          const e = o.method("get_name", 0).invoke();
                          n = String(e?.content ?? e?.toString?.() ?? n);
                        } catch (e) {}
                        selectedAssetBundleAssets.push({
                          name: n,
                          asset: o
                        });
                      } catch (e) {}
                    }
                  }
                  console.log("[Asset Bundles] Loaded " + selectedAssetBundleAssets.length + " GameObject prefab(s) from " + e.path);
                  rebuildAssetBundleAssetButtons();
                  currentCategory = 102;
                  currentPage = 0;
                  try {
                    reloadMenu();
                  } catch (e) {}
                  sendNotification("Opened " + getFileNameWithoutExtension(e.path) + " (" + selectedAssetBundleAssets.length + " prefabs)", false, 5);
                } catch (e) {
                  pendingAssetBundleAssetsLoad = null;
                  console.error("[Asset Bundles] Prefab completion failed:", e);
                  sendNotification("Bundle prefab completion failed: " + e, false);
                }
              }
            }
            function loadAssetBundleFile(e) {
              try {
                const t = loadedAssetBundles.find(t => t.path.toLowerCase() === e.toLowerCase());
                if (t && t.bundle && !t.bundle.isNull?.()) {
                  openAssetBundleAssets(e, t.bundle);
                  return;
                }
                if (pendingAssetBundleLoad) {
                  sendNotification("Already loading: " + getFileNameWithoutExtension(pendingAssetBundleLoad.path), false);
                  return;
                }
                const o = getAssetBundleClass();
                if (!o) {
                  sendNotification("Unity AssetBundle class was not found", false);
                  return;
                }
                const n = Il2Cpp.string(e);
                let i = null;
                try {
                  i = o.method("LoadFromFileAsync", 1).overload("System.String").invoke(n);
                } catch (e) {
                  console.error("[Asset Bundles] LoadFromFileAsync invocation failed:", e);
                }
                if (!i || i.isNull?.()) {
                  sendNotification("Could not start bundle load: " + getFileNameWithoutExtension(e), false);
                  return;
                }
                pendingAssetBundleLoad = {
                  path: e,
                  request: i,
                  startedAt: time || 0
                };
                sendNotification("Loading bundle: " + getFileNameWithoutExtension(e), false);
              } catch (e) {
                console.error("[Asset Bundles] Load failed:", e);
                sendNotification("AssetBundle load failed: " + e, false);
              }
            }
            function finishPendingAssetBundleLoad() {
              const e = pendingAssetBundleLoad;
              if (e) {
                try {
                  let t = false;
                  try {
                    t = !!e.request.method("get_isDone").invoke();
                  } catch (e) {
                    console.error("[Asset Bundles] Could not read async request state:", e);
                    pendingAssetBundleLoad = null;
                    sendNotification("Bundle request state failed", false);
                    return;
                  }
                  if (!t) {
                    if ((time || 0) - e.startedAt > 20) {
                      console.error("[Asset Bundles] Load timed out: " + e.path);
                      pendingAssetBundleLoad = null;
                      sendNotification("Bundle load timed out: " + getFileNameWithoutExtension(e.path), false);
                    }
                    return;
                  }
                  const o = e.request.method("get_assetBundle", 0).invoke();
                  pendingAssetBundleLoad = null;
                  if (!o || o.isNull?.()) {
                    console.error("[Asset Bundles] Unity completed the request but returned a null bundle: " + e.path);
                    sendNotification("Unity rejected bundle: " + getFileNameWithoutExtension(e.path), false);
                    return;
                  }
                  loadedAssetBundles.push({
                    path: e.path,
                    bundle: o
                  });
                  openAssetBundleAssets(e.path, o);
                  try {
                    reloadMenu();
                  } catch (e) {}
                } catch (e) {
                  pendingAssetBundleLoad = null;
                  console.error("[Asset Bundles] Async completion failed:", e);
                  sendNotification("Bundle completion failed: " + e, false);
                }
              }
            }
            function rebuildAssetBundleButtons() {
              const e = [new ButtonInfo({
                buttonText: "Exit Asset Bundles",
                method: () => {
                  currentCategory = 11;
                  currentPage = 0;
                },
                isTogglable: false,
                toolTip: "Returns to the Stuff page."
              }), new ButtonInfo({
                buttonText: "Reload Asset Bundles",
                method: () => {
                  rebuildAssetBundleButtons();
                  currentPage = 0;
                  reloadMenu();
                  sendNotification("Asset bundle folder rescanned", false);
                },
                isTogglable: false,
                toolTip: "Rescans the assetbundles folder for files."
              }), new ButtonInfo({
                buttonText: "Unload Asset Bundles",
                method: () => {
                  unloadAssetBundles();
                  rebuildAssetBundleButtons();
                  currentPage = 0;
                  reloadMenu();
                },
                isTogglable: false,
                toolTip: "Destroys spawned bundle objects and unloads all loaded AssetBundles."
              })];
              try {
                const t = Il2Cpp.domain.assembly("mscorlib").image.class("System.IO.Directory");
                const o = getScriptDir() + "/assetbundles";
                if (!t.method("Exists", 1).invoke(Il2Cpp.string(o))) {
                  t.method("CreateDirectory", 1).invoke(Il2Cpp.string(o));
                }
                const n = t.method("GetFiles", 2).invoke(Il2Cpp.string(o), Il2Cpp.string("*"));
                const i = [];
                if (n && !n.isNull?.()) {
                  for (let e = 0; e < n.length; e++) {
                    const t = n.get(e);
                    const o = String(t?.content ?? t?.toString?.() ?? "");
                    if (o) {
                      i.push(o);
                    }
                  }
                }
                i.sort((e, t) => e.localeCompare(t));
                for (const t of i) {
                  const o = getFileNameWithoutExtension(t) || t;
                  e.push(new ButtonInfo({
                    buttonText: o,
                    method: () => {
                      loadAssetBundleFile(t);
                    },
                    isTogglable: false,
                    toolTip: "Loads this AssetBundle and opens its spawnable asset list."
                  }));
                }
                if (i.length === 0) {
                  e.push(new ButtonInfo({
                    buttonText: "No bundles found",
                    method: () => {
                      sendNotification("Add bundle files to " + o, false, 6);
                    },
                    isTogglable: false,
                    toolTip: "Place Unity AssetBundle files in the assetbundles folder."
                  }));
                }
                console.log("[Asset Bundles] Scanned " + o + " and found " + i.length + " file(s)");
              } catch (e) {
                console.error("[Asset Bundles] Folder scan failed:", e);
              }
              buttons[101] = e;
              rebuildButtonMap();
            }
            const assetBundlesMenuButton = new ButtonInfo({
              buttonText: "Asset Bundles",
              method: () => {
                rebuildAssetBundleButtons();
                currentCategory = 101;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens the AssetBundle loader page."
            });
            function updateAssetBundles() {
              finishPendingAssetBundleLoad();
              finishPendingAssetBundleAssetsLoad();
            }
            function createCoolVisualOrb(e, t) {
              try {
                const o = createObject(zeroVector, identityQuaternion, [t, t, t], 0, e, null, false);
                try {
                  Destroy(getComponent(o, Collider));
                } catch (e) {}
                return o;
              } catch (e) {
                return null;
              }
            }
            function destroyCoolVisualList(e) {
              for (const t of e) {
                try {
                  const e = t?.object ?? t;
                  if (e && !e.isNull?.()) {
                    Destroy(e);
                  }
                } catch (e) {}
              }
              e.length = 0;
            }
            function clearCoolVisualMods() {
              coolHaloEnabled = false;
              coolOrbitOrbsEnabled = false;
              coolHandTrailEnabled = false;
              coolDiscoEnabled = false;
              destroyCoolVisualList(coolHaloObjects);
              destroyCoolVisualList(coolOrbitObjects);
              destroyCoolVisualList(coolTrailObjects);
              for (const e of ["Personal Halo", "Orbiting Orbs", "Hand Trail", "Disco Visuals"]) {
                const t = (buttons[106] || []).find(t => t.buttonText === e);
                if (t) {
                  t.enabled = false;
                }
              }
              sendNotification("Cleared local visual playground", false);
            }
            function ensureCoolVisualCount(e, t, o) {
              while (e.length < t) {
                const n = e.length / Math.max(1, t);
                const i = createCoolVisualOrb([Math.abs(Math.sin(n * Math.PI * 2)), Math.abs(Math.sin((n + 0.333) * Math.PI * 2)), Math.abs(Math.sin((n + 0.666) * Math.PI * 2)), 0.9], o);
                if (!i) {
                  break;
                }
                e.push(i);
              }
              while (e.length > t) {
                const t = e.pop();
                try {
                  if (t && !t.isNull?.()) {
                    Destroy(t);
                  }
                } catch (e) {}
              }
            }
            function updateCoolVisualMods(e) {
              let t = null;
              try {
                t = getTransform(headCollider);
              } catch (e) {}
              if (!t || t.isNull?.()) {
                return;
              }
              const o = readVec3Components(t.method("get_position").invoke());
              if (coolHaloEnabled) {
                ensureCoolVisualCount(coolHaloObjects, 12, 0.055);
                for (let t = 0; t < coolHaloObjects.length; t++) {
                  const n = t / coolHaloObjects.length * Math.PI * 2 + e * 0.35;
                  const i = [o[0] + Math.cos(n) * 0.34, o[1] + 0.32, o[2] + Math.sin(n) * 0.34];
                  try {
                    getTransform(coolHaloObjects[t]).method("set_position").invoke(i);
                  } catch (e) {}
                }
              } else if (coolHaloObjects.length > 0) {
                destroyCoolVisualList(coolHaloObjects);
              }
              if (coolOrbitOrbsEnabled) {
                ensureCoolVisualCount(coolOrbitObjects, 8, 0.085);
                for (let t = 0; t < coolOrbitObjects.length; t++) {
                  const n = t / coolOrbitObjects.length * Math.PI * 2 + e * 1.2;
                  const i = Math.sin(e * 1.7 + t) * 0.35;
                  const a = [o[0] + Math.cos(n) * 1.15, o[1] - 0.35 + i, o[2] + Math.sin(n) * 1.15];
                  try {
                    getTransform(coolOrbitObjects[t]).method("set_position").invoke(a);
                  } catch (e) {}
                }
              } else if (coolOrbitObjects.length > 0) {
                destroyCoolVisualList(coolOrbitObjects);
              }
              if (coolHandTrailEnabled && e >= coolTrailSpawnTime) {
                coolTrailSpawnTime = e + 0.055;
                try {
                  const o = (rightHandTransform && !rightHandTransform.isNull?.() ? rightHandTransform : t).method("get_position").invoke();
                  const n = e * 0.4 % 1;
                  const i = createCoolVisualOrb([Math.abs(Math.sin(n * Math.PI * 2)), Math.abs(Math.sin((n + 0.333) * Math.PI * 2)), Math.abs(Math.sin((n + 0.666) * Math.PI * 2)), 0.75], 0.045);
                  if (i) {
                    getTransform(i).method("set_position").invoke(o);
                    coolTrailObjects.push({
                      object: i,
                      expires: e + 1.1
                    });
                  }
                } catch (e) {}
              }
              for (let t = coolTrailObjects.length - 1; t >= 0; t--) {
                const o = coolTrailObjects[t];
                if (!coolHandTrailEnabled || e >= o.expires) {
                  try {
                    if (o.object && !o.object.isNull?.()) {
                      Destroy(o.object);
                    }
                  } catch (e) {}
                  coolTrailObjects.splice(t, 1);
                }
              }
              if (coolDiscoEnabled) {
                const t = coolHaloObjects.concat(coolOrbitObjects).concat(coolTrailObjects.map(e => e.object));
                for (let o = 0; o < t.length; o++) {
                  const n = e * 2.2 + o * 0.55;
                  setRendererColorSafe(t[o], [Math.abs(Math.sin(n)), Math.abs(Math.sin(n + 2.094)), Math.abs(Math.sin(n + 4.188)), 0.9]);
                }
              }
            }
            function applyMenuStructurePatches() {
              if (!menuStructurePatched) {
                try {
                  let e = function () {
                    try {
                      const e = AssemblyCSharp.class("AnimalCompany.MomBossController");
                      const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                      if (t && t.length > 0) {
                        for (let e = 0; e < t.length; e++) {
                          const o = t.get(e);
                          if (o && !o.isNull?.()) {
                            return o;
                          }
                        }
                      }
                    } catch (e) {
                      console.error("[MomBoss] find failed:", e);
                    }
                    return null;
                  };
                  let t = function (e = 1.2) {
                    try {
                      const t = rightHandTransform && !rightHandTransform.isNull?.() ? rightHandTransform : getTransform(bodyCollider);
                      const o = t.method("get_position").invoke();
                      const n = t.method("get_forward").invoke();
                      const i = Vector3.method("op_Addition").invoke(o, Vector3.method("op_Multiply", 2).invoke(n, e));
                      return {
                        pos: i,
                        rot: t.method("get_rotation").invoke() || identityQuaternion
                      };
                    } catch (e) {
                      try {
                        return {
                          pos: getTransform(bodyCollider).method("get_position").invoke(),
                          rot: identityQuaternion
                        };
                      } catch (e) {
                        return {
                          pos: zeroVector,
                          rot: identityQuaternion
                        };
                      }
                    }
                  };
                  let o = function (t, ...o) {
                    try {
                      const n = e();
                      if (n) {
                        n.method(t).invoke(...o);
                        sendNotification("MomBoss: " + t, false, 2);
                        return true;
                      } else {
                        sendNotification("Mom Boss not found", false, 2);
                        return false;
                      }
                    } catch (e) {
                      sendNotification("MomBoss " + t + " failed", false, 2);
                      console.error("[MomBoss]", t, e);
                      return false;
                    }
                  };
                  let n = function (t, o) {
                    try {
                      const n = e();
                      if (n) {
                        n.field(t).value = o;
                        sendNotification("Mom " + t + " = " + o, false, 2);
                        return true;
                      } else {
                        sendNotification("Mom Boss not found", false, 2);
                        return false;
                      }
                    } catch (e) {
                      sendNotification("Mom field " + t + " failed", false, 2);
                      console.error("[MomBoss field]", t, e);
                      return false;
                    }
                  };
                  let i = function (e, t = identityQuaternion) {
                    try {
                      if (!momPrefabDumpDone) {
                        momPrefabDumpDone = true;
                        try {
                          if (!didInitPrefabs) {
                            initPrefabs();
                          }
                          console.log("[MomBoss] prefab name candidates:");
                          for (let x = 0; x < prefabNames.length; x++) {
                            const nm = String(prefabNames[x]);
                            if (/mom|boss/i.test(nm)) {
                              console.log("  - " + nm);
                            }
                          }
                        } catch (e) {
                          console.error("[MomBoss] prefab dump failed:", e);
                        }
                      }
                      const o = [{
                        type: "prefab",
                        name: "MomBossController"
                      }, {
                        type: "prefab",
                        name: "mom_pillow"
                      }, {
                        type: "item",
                        name: "item_momboss_box"
                      }, {
                        type: "prefab",
                        name: "MomToyBlockObject"
                      }];
                      for (const n of o) {
                        try {
                          let o = null;
                          if (n.type === "item") {
                            o = spawnItemAtPos(n.name, e, t);
                          } else {
                            if (n.name === "MomBossController") {
                              if (spawnMob("MomBossController", e, t)) {
                                sendNotification("Mom spawn: " + n.name, false, 2);
                                return true;
                              }
                            }
                            o = spawnPrefab(n.name, e, t);
                          }
                          if (o && !o.isNull?.()) {
                            sendNotification("Mom spawn: " + n.name, false, 2);
                            return true;
                          }
                        } catch (e) {}
                      }
                    } catch (e) {
                      console.error("[SpawnMomBossAt]", e);
                    }
                    sendNotification("Mom spawn failed", false, 2);
                    return false;
                  };
                  let a = function (e = 1.2) {
                    try {
                      const o = t(e);
                      return i(o.pos, o.rot);
                    } catch (e) {
                      console.error("[SpawnMomBoss]", e);
                      sendNotification("Mom spawn failed", false, 2);
                      return false;
                    }
                  };
                  let l = function () {
                    let o = 0;
                    try {
                      const t = e();
                      if (t) {
                        let e = null;
                        try {
                          e = t.method("get_itemSpawner").invoke();
                        } catch (e) {}
                        if (e && (!e.handle || !e.handle.isNull())) {
                          try {
                            e.method("SpawnSupplyItems").invoke();
                            o++;
                          } catch (e) {}
                          try {
                            e.method("SpawnItems").invoke(["item_arena_pistol", "item_shotgun_ammo", "item_company_ration_heal"], 5, 5);
                            o++;
                          } catch (e) {}
                        }
                      }
                    } catch (e) {
                      console.error("[MomSupply]", e);
                    }
                    if (!o) {
                      const e = t(1.1);
                      const o = e.pos.field ? e.pos.field("x").value : e.pos[0];
                      const n = e.pos.field ? e.pos.field("y").value : e.pos[1];
                      const i = e.pos.field ? e.pos.field("z").value : e.pos[2];
                      for (const e of ["item_arena_pistol", "item_shotgun_ammo", "item_company_ration_heal", "item_rpg_ammo"]) {
                        for (let t = 0; t < 4; t++) {
                          try {
                            spawnItemAtPos(e, [(o || 0) + (Math.random() - 0.5) * 1.5, (n || 0) + Math.random(), (i || 0) + (Math.random() - 0.5) * 1.5], identityQuaternion);
                          } catch (e) {}
                        }
                      }
                    }
                    sendNotification("Mom supplies: " + (o || "fallback"), false, 2);
                  };
                  let r = function (e, t) {
                    let o = 0;
                    try {
                      const n = AssemblyCSharp.class(e);
                      const i = Object.method("FindObjectsByType", 1).inflate(n).invoke(0);
                      if (!i || i.length === 0) {
                        return 0;
                      }
                      for (let e = 0; e < i.length; e++) {
                        try {
                          const n = i.get(e);
                          if (!n || n.isNull?.()) {
                            continue;
                          }
                          let a = false;
                          for (const e in t) {
                            try {
                              n.field(e).value = t[e];
                              a = true;
                            } catch (e) {}
                          }
                          if (a) {
                            o++;
                          }
                        } catch (e) {}
                      }
                    } catch (t) {
                      console.error("[setFieldsOnClass]", e, t);
                    }
                    return o;
                  };
                  let s = function (e = true) {
                    try {
                      let t = r("AnimalCompany.HordeMobController", {
                        _n_isActive: e,
                        _n_mobCountToSpawn: e ? 64 : 0,
                        _ignoreLootDrop: false
                      });
                      t += r("AnimalCompany.HordeMobSpawner", {
                        _enableTest: e,
                        _testAutoWave: e,
                        _autoKillFromThisPlayer: !e
                      });
                      sendNotification("Horde " + (e ? "on" : "off") + ": " + t, false, 2);
                      return t > 0;
                    } catch (e) {
                      console.error("[MomHorde]", e);
                      sendNotification("Horde toggle failed", false, 2);
                      return false;
                    }
                  };
                  const c = (e, t, o, n = null) => {
                    for (const i of e) {
                      if (i.buttonText === t) {
                        i.buttonText = o;
                        if (n != null) {
                          i.toolTip = n;
                        }
                        return;
                      }
                    }
                  };
                  const d = (e, t) => {
                    const o = e.findIndex(e => e.buttonText === t);
                    if (o >= 0) {
                      e.splice(o, 1);
                    }
                  };
                  const u = (e, t, o) => new ButtonInfo({
                    buttonText: e,
                    method: () => {
                      currentCategory = t;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: o
                  });
                  rebuildSoundboardButtons();
                  const m = buttons[0] ?? [];
                  const h = m.find(e => e.buttonText === "Mods Settings" || e.buttonText === "Settings");
                  if (h) {
                    h.buttonText = "Settings";
                    h.method = () => {
                      currentCategory = 21;
                      currentPage = 0;
                    };
                    h.isTogglable = false;
                    h.toolTip = "Opens settings.";
                  }
                  d(m, "Menu Settings");
                  c(m, "Other player mods", "Whitelist mods", "Opens the whitelist player mods page.");
                  c(m, "Misc", "Over Powered", "Opens the over powered category.");
                  d(m, "Unity Explorer");
                  d(m, "Extra WL");
                  const g = e => {
                    for (const t of buttons) {
                      if (!t) {
                        continue;
                      }
                      const o = t.find(t => t.buttonText === e);
                      if (o) {
                        return o;
                      }
                    }
                    return null;
                  };
                  const f = e => e.map(g).filter(e => e != null);
                  buttons[106] = [u("Exit Visuals", 11, "Returns to the Others page."), new ButtonInfo({
                    buttonText: "Extra Visual Shi",
                    method: () => {
                      currentCategory = 120;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Opens the prefab mods category."
                  }), new ButtonInfo({
                    buttonText: "Name Tags",
                    enableMethod: () => {
                      nameTagsEnabled = true;
                      sendNotification("Blue Name Tags ON", false);
                    },
                    disableMethod: () => {
                      nameTagsEnabled = false;
                      destroyOverlayEntries(playerNameTagEntries);
                      sendNotification("Blue Name Tags OFF", false);
                    },
                    isTogglable: true,
                    method: () => {
                      updatePlayerNameTags();
                    },
                    toolTip: "Shows blue usernames above every player's head and keeps them facing you."
                  }), new ButtonInfo({
                    buttonText: "Player ESP",
                    enableMethod: () => {
                      arenaEspEnabled = true;
                      sendNotification("Arena ESP ON", false);
                    },
                    disableMethod: () => {
                      arenaEspEnabled = false;
                      destroyOverlayEntries(playerEspEntries);
                      sendNotification("Arena ESP OFF", false);
                    },
                    isTogglable: true,
                    method: () => {
                      updatePlayerEspOverlays();
                    },
                    toolTip: "Client-side through-wall player rays and 2D style boxes using always-on-top arena-style overlay materials."
                  })];
                  buttons[107] = [u("Exit Movement Hub", 11, "Returns to the Others page."), ...f(["Fist Fly", "Joystick Fly", "Trigger Fly", "Noclip", "Platforms", "Ghost Monke", "Low Gravity", "High Gravity", "Zero Gravity", "Teleport Gun", "Teleport To Random"])];
                  buttons[120] = [u("Exit Extra Visual shi", 106, "Returns to the Others page."), new ButtonInfo({
                    buttonText: "Personal Halo",
                    enableMethod: () => {
                      coolHaloEnabled = true;
                    },
                    disableMethod: () => {
                      coolHaloEnabled = false;
                    },
                    isTogglable: true,
                    enabled: coolHaloEnabled,
                    toolTip: "Creates a rotating local rainbow halo above your head."
                  }), new ButtonInfo({
                    buttonText: "Orbiting Orbs",
                    enableMethod: () => {
                      coolOrbitOrbsEnabled = true;
                    },
                    disableMethod: () => {
                      coolOrbitOrbsEnabled = false;
                    },
                    isTogglable: true,
                    enabled: coolOrbitOrbsEnabled,
                    toolTip: "Creates eight animated local orbs around you."
                  }), new ButtonInfo({
                    buttonText: "Hand Trail",
                    enableMethod: () => {
                      coolHandTrailEnabled = true;
                    },
                    disableMethod: () => {
                      coolHandTrailEnabled = false;
                    },
                    isTogglable: true,
                    enabled: coolHandTrailEnabled,
                    toolTip: "Adds a short-lived rainbow trail to your right hand."
                  }), new ButtonInfo({
                    buttonText: "Disco Visuals",
                    enableMethod: () => {
                      coolDiscoEnabled = true;
                    },
                    disableMethod: () => {
                      coolDiscoEnabled = false;
                    },
                    isTogglable: true,
                    enabled: coolDiscoEnabled,
                    toolTip: "Animates the colors of your active local visual toys."
                  }), new ButtonInfo({
                    buttonText: "Cosmic Visual Preset",
                    method: () => {
                      coolHaloEnabled = true;
                      coolOrbitOrbsEnabled = true;
                      coolHandTrailEnabled = true;
                      coolDiscoEnabled = true;
                      for (const e of ["Personal Halo", "Orbiting Orbs", "Hand Trail", "Disco Visuals"]) {
                        const t = (buttons[106] || []).find(t => t.buttonText === e);
                        if (t) {
                          t.enabled = true;
                        }
                      }
                      sendNotification("Cosmic visual preset enabled", false);
                    },
                    isTogglable: false,
                    toolTip: "Enables every Visual Playground effect."
                  }), new ButtonInfo({
                    buttonText: "Color Burst",
                    method: () => {
                      try {
                        const e = readVec3Components(getTransform(headCollider).method("get_position").invoke());
                        for (let t = 0; t < 24; t++) {
                          const t = Math.random() * Math.PI * 2;
                          const o = 0.3 + Math.random() * 1.2;
                          const n = createCoolVisualOrb([Math.random(), Math.random(), Math.random(), 0.9], 0.035 + Math.random() * 0.05);
                          if (n) {
                            getTransform(n).method("set_position").invoke([e[0] + Math.cos(t) * o, e[1] - 0.4 + Math.random() * 1.6, e[2] + Math.sin(t) * o]);
                            try {
                              Object.method("Destroy", 2).invoke(n, 2.5);
                            } catch (e) {}
                          }
                        }
                      } catch (e) {}
                    },
                    isTogglable: false,
                    toolTip: "Creates a short local burst of colorful particles."
                  }), new ButtonInfo({
                    buttonText: "Clear Visual Playground",
                    method: () => {
                      clearCoolVisualMods();
                    },
                    isTogglable: false,
                    toolTip: "Removes all local visual toys."
                  })];
                  buttons[108] = [u("Exit World Chaos", 11, "Returns to the Others page."), ...f(["Item Rain", "Item Tornado", "Rain Prefabs", "Prefab Orbit", "Mob Orbit", "Spacebike Orbit", "Tree Forest Orbit", "Moon Car Orbit", "ChristmasBox Orbit", "No Gravity On All Items", "Rainbow All Items"])];
                  buttons[109] = [u("Exit Creator Hub", 11, "Returns to the Others page."), u("Text Studio", 103, "Opens the expanded Text Mods studio."), u("Rig Studio", 77, "Opens rig cloning and formation tools."), u("VFX Studio", 56, "Opens VFX spawning."), u("Grid Builder", 100, "Opens grid building tools."), u("Prefab Workshop", 14, "Opens prefab tools."), assetBundlesMenuButton, u("UX Object Lab", 17, "Opens selected-object tools.")];
                  buttons[110] = [u("Exit Utilities", 11, "Returns to the Others page."), u("Settings", 2, "Opens menu and mod settings."), u("Room Codes", 80, "Opens the room-code keyboard."), u("Other Players", 21, "Opens player selection and player tools."), u("Soundboard", 32, "Opens the soundboard."), u("Saved Text Phrases", 105, "Opens saved Text Mods phrases."), u("Credits", 7, "Opens credits.")];
                  buttons[11];
                  buttons[25] = [new ButtonInfo({
                    buttonText: "Exit Weapon mods",
                    method: () => {
                      currentCategory = 6;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to the main category."
                  }), new ButtonInfo({
                    buttonText: "Ammo mods",
                    method: () => {
                      currentCategory = 67;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "opens ammo mods."
                  }), new ButtonInfo({
                    buttonText: "Rocket hands",
                    method: () => {
                      if (rightGrab) {
                        try {
                          const e = getLaunchForward(rightHandTransform);
                          const t = getLaunchUp(rightHandTransform, e);
                          const o = Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(e, 0.85), Vector3.method("op_Multiply", 2).invoke(t, -0.04)));
                          spawnNetworkPrefab("RPGRocket", o, getLaunchRotation(rightHandTransform, e, t));
                        } catch (e) {
                          console.error("RocketLauncher error: " + e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "hi"
                  }), new ButtonInfo({
                    buttonText: "RPG Shoe hands",
                    method: () => {
                      if (rightGrab) {
                        try {
                          const e = getLaunchForward(rightHandTransform);
                          const t = getLaunchUp(rightHandTransform, e);
                          const o = Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(e, 0.85), Vector3.method("op_Multiply", 2).invoke(t, -0.04)));
                          spawnNetworkPrefab("RPGRocketShoe", o, getLaunchRotation(rightHandTransform, e, t));
                        } catch (e) {
                          console.error("RocketLauncher error: " + e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "tuff"
                  }), new ButtonInfo({
                    buttonText: "flaregun hands",
                    method: () => {
                      if (rightGrab) {
                        try {
                          const e = getLaunchForward(rightHandTransform);
                          const t = getLaunchUp(rightHandTransform, e);
                          const o = Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(e, 0.85), Vector3.method("op_Multiply", 2).invoke(t, -0.04)));
                          spawnNetworkPrefab("FlareGunProjectile", o, getLaunchRotation(rightHandTransform, e, t));
                        } catch (e) {
                          console.error("boomspear error: " + e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "tuff"
                  }), new ButtonInfo({
                    buttonText: "boomspear hands",
                    method: () => {
                      if (rightGrab) {
                        try {
                          const e = getLaunchForward(rightHandTransform);
                          const t = getLaunchUp(rightHandTransform, e);
                          const o = Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(e, 0.85), Vector3.method("op_Multiply", 2).invoke(t, -0.04)));
                          spawnNetworkPrefab("RPGRocketSpear", o, getLaunchRotation(rightHandTransform, e, t));
                        } catch (e) {
                          console.error("boomspear error: " + e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "tuff"
                  }), new ButtonInfo({
                    buttonText: "Ducky Rocket",
                    method: () => {
                      for (let e = duckyRocketAttachments.length - 1; e >= 0; e--) {
                        try {
                          const t = duckyRocketAttachments[e];
                          if (!t || !t.rocket || t.rocket.isNull?.()) {
                            duckyRocketAttachments.splice(e, 1);
                            continue;
                          }
                          const o = getTransform(t.rocket);
                          if (!t.ducky || t.ducky.isNull?.()) {
                            try {
                              t.ducky = findSpawnedItemNear(t.duckyId ?? "item_rubberducky", o.method("get_position").invoke(), 2);
                            } catch (e) {}
                            if (!t.ducky || t.ducky.isNull?.()) {
                              if (time > (t.expireTime ?? 0)) {
                                duckyRocketAttachments.splice(e, 1);
                              }
                              continue;
                            }
                          }
                          const n = getTransform(t.ducky);
                          const i = o.method("TransformPoint").invoke([0, 0, 0.08]);
                          n.method("set_position").invoke(i);
                          n.method("set_rotation").invoke(o.method("get_rotation").invoke());
                          if (n) {
                            try {
                              n.method("set_localScale").invoke([1.45, 1.45, 1.45]);
                            } catch (e) {}
                          }
                          if (t.flare && !t.flare.isNull?.()) {
                            const e = getTransform(t.flare);
                            const n = o.method("TransformPoint").invoke([0, 0, -0.18]);
                            e.method("set_position").invoke(n);
                            e.method("set_rotation").invoke(o.method("get_rotation").invoke());
                            try {
                              const e = t.flare.method("GetComponentInChildren", 1).inflate(Rigidbody).invoke(true);
                              if (e && !e.isNull()) {
                                e.method("set_isKinematic").invoke(true);
                                e.method("set_useGravity").invoke(false);
                                try {
                                  e.method("set_linearVelocity").invoke([0, 0, 0]);
                                } catch (e) {}
                                try {
                                  e.method("set_angularVelocity").invoke([0, 0, 0]);
                                } catch (e) {}
                              }
                            } catch (e) {}
                          }
                          try {
                            const e = t.ducky.method("GetComponentInChildren", 1).inflate(Rigidbody).invoke(true);
                            if (e && !e.isNull()) {
                              e.method("set_isKinematic").invoke(true);
                              e.method("set_useGravity").invoke(false);
                              try {
                                e.method("set_linearVelocity").invoke([0, 0, 0]);
                              } catch (e) {}
                              try {
                                e.method("set_angularVelocity").invoke([0, 0, 0]);
                              } catch (e) {}
                            }
                          } catch (e) {}
                        } catch (t) {
                          duckyRocketAttachments.splice(e, 1);
                        }
                      }
                      if (rightGrab) {
                        try {
                          const e = getLaunchForward(rightHandTransform);
                          const t = getLaunchUp(rightHandTransform, e);
                          const o = Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(e, 0.85), Vector3.method("op_Multiply", 2).invoke(t, -0.04)));
                          const n = getLaunchRotation(rightHandTransform, e, t);
                          const i = spawnNetworkPrefab("RobotDogRPG", o, n);
                          const a = spawnNetworkPrefab("FlareGunProjectile", o, n);
                          const l = spawnItemAtPos("item_rubberducky", o, n);
                          if (i && !i.isNull?.()) {
                            const e = getTransform(i);
                            const t = l && !l.isNull?.() ? getTransform(l) : null;
                            if (a && !a.isNull?.()) {
                              const t = getTransform(a);
                              try {
                                const e = a.method("GetComponentInChildren", 1).inflate(Rigidbody).invoke(true);
                                if (e && !e.isNull()) {
                                  e.method("set_isKinematic").invoke(true);
                                  e.method("set_useGravity").invoke(false);
                                  try {
                                    e.method("set_linearVelocity").invoke([0, 0, 0]);
                                  } catch (e) {}
                                  try {
                                    e.method("set_angularVelocity").invoke([0, 0, 0]);
                                  } catch (e) {}
                                }
                              } catch (e) {}
                              try {
                                t.method("SetParent", 2).invoke(e, false);
                              } catch (o) {
                                t.method("set_parent").invoke(e);
                              }
                              try {
                                t.method("set_localPosition").invoke([0, 0, -0.18]);
                              } catch (e) {}
                              try {
                                t.method("set_localRotation").invoke(identityQuaternion);
                              } catch (e) {}
                            }
                            try {
                              const e = l.method("GetComponentInChildren", 1).inflate(Rigidbody).invoke(true);
                              if (e && !e.isNull()) {
                                e.method("set_isKinematic").invoke(true);
                                e.method("set_useGravity").invoke(false);
                                try {
                                  e.method("set_linearVelocity").invoke([0, 0, 0]);
                                } catch (e) {}
                                try {
                                  e.method("set_angularVelocity").invoke([0, 0, 0]);
                                } catch (e) {}
                              }
                            } catch (e) {}
                            try {
                              const e = l.method("GetComponentInChildren", 1).inflate(Collider).invoke(true);
                              if (e && !e.isNull()) {
                                e.method("set_enabled").invoke(false);
                              }
                            } catch (e) {}
                            if (t) {
                              try {
                                t.method("SetParent", 2).invoke(e, false);
                              } catch (o) {
                                try {
                                  t.method("set_parent").invoke(e);
                                } catch (e) {}
                              }
                            }
                            if (t) {
                              try {
                                t.method("set_localPosition").invoke([0, 0, 0.08]);
                              } catch (e) {}
                            }
                            if (t) {
                              try {
                                t.method("set_localRotation").invoke(identityQuaternion);
                              } catch (e) {}
                            }
                            if (t) {
                              try {
                                t.method("set_localScale").invoke([1.45, 1.45, 1.45]);
                              } catch (e) {}
                            }
                            duckyRocketAttachments.push({
                              rocket: i,
                              ducky: l,
                              duckyId: "item_rubberducky",
                              flare: a,
                              expireTime: time + 2.5
                            });
                          }
                        } catch (e) {
                          console.error("DuckyRocket error: " + e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "solarhp"
                  }), new ButtonInfo({
                    buttonText: "RPG Out Of Head",
                    method: () => {
                      if (rightGrab) {
                        spawnNetworkPrefab("RPGRocket", getTransform(headCollider).method("get_position").invoke(), getTransform(headCollider).method("get_rotation").invoke());
                      }
                    },
                    isTogglable: true,
                    toolTip: "solarhp"
                  }), new ButtonInfo({
                    buttonText: "Rocket + Boomspear Hands",
                    method: () => {
                      if (rightGrab) {
                        try {
                          const e = getLaunchForward(rightHandTransform);
                          const t = getLaunchUp(rightHandTransform, e);
                          const o = Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(e, 0.85), Vector3.method("op_Multiply", 2).invoke(t, -0.04)));
                          const n = getLaunchRotation(rightHandTransform, e, t);
                          const i = Quaternion.method("op_Multiply").invoke(n, Quaternion.method("Euler").invoke(0, -15, 0));
                          const a = Quaternion.method("op_Multiply").invoke(n, Quaternion.method("Euler").invoke(0, 15, 0));
                          spawnNetworkPrefab("RPGRocket", o, i);
                          spawnNetworkPrefab("RPGRocketSpear", o, a);
                        } catch (e) {
                          console.error("RocketLauncher error: " + e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "solarhp"
                  }), new ButtonInfo({
                    buttonText: "Egg + Robot Dog RPG Hands",
                    method: () => {
                      if (rightGrab) {
                        try {
                          const e = getLaunchForward(rightHandTransform);
                          const t = getLaunchUp(rightHandTransform, e);
                          const o = Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(e, 0.85), Vector3.method("op_Multiply", 2).invoke(t, -0.04)));
                          const n = getLaunchRotation(rightHandTransform, e, t);
                          const i = Quaternion.method("op_Multiply").invoke(n, Quaternion.method("Euler").invoke(0, -15, 0));
                          const a = Quaternion.method("op_Multiply").invoke(n, Quaternion.method("Euler").invoke(0, 15, 0));
                          spawnNetworkPrefab("RPGRocketEgg", o, i);
                          spawnNetworkPrefab("RobotDogRPG", o, a);
                        } catch (e) {
                          console.error("RocketLauncher error: " + e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "solarhp"
                  }), new ButtonInfo({
                    buttonText: "Robot Dog RPG hands",
                    method: () => {
                      if (rightGrab) {
                        try {
                          const e = getLaunchForward(rightHandTransform);
                          const t = getLaunchUp(rightHandTransform, e);
                          const o = Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(e, 0.85), Vector3.method("op_Multiply", 2).invoke(t, -0.04)));
                          spawnNetworkPrefab("RobotDogRPG", o, getLaunchRotation(rightHandTransform, e, t));
                        } catch (e) {
                          console.error("RobotDogRPG error: " + e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "tuff"
                  }), new ButtonInfo({
                    buttonText: "egg hands",
                    method: () => {
                      if (rightGrab) {
                        try {
                          const e = getLaunchForward(rightHandTransform);
                          const t = getLaunchUp(rightHandTransform, e);
                          const o = Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(e, 0.85), Vector3.method("op_Multiply", 2).invoke(t, -0.04)));
                          spawnNetworkPrefab("RPGRocketEgg", o, getLaunchRotation(rightHandTransform, e, t));
                        } catch (e) {
                          console.error("boomspear error: " + e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "egg"
                  }), new ButtonInfo({
                    buttonText: "grenade hands",
                    method: () => {
                      if (rightGrab) {
                        try {
                          const e = getLaunchForward(rightHandTransform);
                          const t = getLaunchUp(rightHandTransform, e);
                          const o = Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(e, 0.85), Vector3.method("op_Multiply", 2).invoke(t, -0.04)));
                          spawnNetworkPrefab("GrenadeProjectile", o, getLaunchRotation(rightHandTransform, e, t));
                        } catch (e) {
                          console.error("boomspear error: " + e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "aj likes dih"
                  }), new ButtonInfo({
                    buttonText: "grenade hands [V2]",
                    enableMethod: () => {
                      grenadeProjectileLauncherEnabled = true;
                      lastGrenadeProjectileLaunchTime = 0;
                    },
                    disableMethod: () => {
                      grenadeProjectileLauncherEnabled = false;
                    },
                    method: () => {
                      if (!grenadeProjectileLauncherEnabled) {
                        return;
                      }
                      if (rightGrab && !(time < lastGrenadeProjectileLaunchTime)) {
                        lastGrenadeProjectileLaunchTime = time + 0.03;
                        try {
                          const e = rightHandTransform.method("get_forward").invoke();
                          const t = spawnNetworkPrefab("GrenadeProjectile", Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Multiply", 2).invoke(e, 0.65)), rightHandTransform.method("get_rotation").invoke());
                          if (t && !t.isNull()) {
                            const o = t.method("GetComponent", 1).inflate(Rigidbody).invoke();
                            if (o && !o.isNull()) {
                              setRigidbodyVelocitySafe(o, Vector3.method("op_Multiply", 2).invoke(e, 60));
                            }
                          }
                        } catch (e) {
                          sendNotification("Grenade projectile launcher failed: " + e, false);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "Make a fist with right hand to launch GrenadeProjectile prefabs every 0.03 seconds."
                  }), new ButtonInfo({
                    enabled: true,
                    buttonText: "Allow Arena Items",
                    enableMethod: () => {
                      try {
                        AssemblyCSharp.class("AnimalCompany.ArenaItemKiller").method("DespawnIfNecessary").implementation = function () {};
                      } catch (e) {
                        console.error("something failed:", e);
                      }
                    },
                    disableMethod: () => {
                      try {
                        AssemblyCSharp.class("AnimalCompany.ArenaItemKiller").method("DespawnIfNecessary");
                        return this.method("DespawnIfNecessary").invoke();
                      } catch (e) {
                        console.error("something failed:", e);
                      }
                    },
                    isTogglable: true
                  }), new ButtonInfo({
                    buttonText: "No Laser Overheat",
                    isTogglable: true,
                    enableMethod: () => {
                      noLaserOverheat = true;
                    },
                    disableMethod: () => {
                      noLaserOverheat = false;
                    },
                    method: () => {},
                    toolTip: "Removes all overheat, fuel drain and hit cooldown on the mining laser so it fires continuously."
                  }), new ButtonInfo({
                    buttonText: "Infinite Laser Range",
                    isTogglable: true,
                    enableMethod: () => {
                      infiniteLaserRange = true;
                    },
                    disableMethod: () => {
                      infiniteLaserRange = false;
                    },
                    method: () => {},
                    toolTip: "Removes the 3-4m distance cap on the mining laser so the beam reaches infinitely."
                  })];
                  buttons[16] = [new ButtonInfo({
                    buttonText: "Exit ID Settings",
                    method: () => {
                      currentCategory = 2;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to the Settings tab."
                  }), u("Item IDs", 17, "Shows every item ID as selectable buttons."), u("Mob IDs", 18, "Shows every mob ID as selectable buttons."), u("Prefab IDs", 20, "Shows every network prefab ID as selectable buttons."), u("Other Players", 21, "other players"), u("Ammo Mods", 67, "Ammo Mods")];
                  buttons[17] = [new ButtonInfo({
                    buttonText: "Exit Item IDs",
                    method: () => {
                      currentCategory = 6;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to ID Settings."
                  }), ...itemIDs.map((e, t) => new ButtonInfo({
                    buttonText: String(e),
                    method: () => {
                      sendNotification("Item ID: " + itemIDs[itemIndex = t], false);
                    },
                    isTogglable: false,
                    toolTip: "Selects item ID: " + e
                  }))];
                  buttons[100] = [new ButtonInfo({
                    buttonText: "Exit Grid mods",
                    method: () => {
                      currentCategory = 6;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to Item mods."
                  }), new ButtonInfo({
                    buttonText: "Show selected item",
                    method: () => {
                      sendNotification("Grid item: " + itemIDs[itemIndex], false);
                    },
                    isTogglable: false,
                    toolTip: "Shows the Item ID currently selected on the Item IDs page."
                  }), new ButtonInfo({
                    buttonText: "Grid amount +1",
                    method: () => {
                      sendNotification("Grid amount: " + (gridAmount = Math.min(30, gridAmount + 1)), false);
                    },
                    isTogglable: false,
                    toolTip: "Adds one item to the grid amount (maximum 30)."
                  }), new ButtonInfo({
                    buttonText: "Grid amount -1",
                    method: () => {
                      sendNotification("Grid amount: " + (gridAmount = Math.max(1, gridAmount - 1)), false);
                    },
                    isTogglable: false,
                    toolTip: "Removes one item from the grid amount (minimum 1)."
                  }), new ButtonInfo({
                    buttonText: "Spawn grid",
                    method: () => {
                      spawnSelectedItemGrid(gridAmount);
                    },
                    isTogglable: false,
                    toolTip: "Spawns the selected Item ID in a frozen grid using the chosen amount."
                  }), ...[5, 10, 20, 30].map(e => new ButtonInfo({
                    buttonText: "Set amount " + e,
                    method: () => {
                      sendNotification("Grid amount: " + (gridAmount = e), false);
                    },
                    isTogglable: false,
                    toolTip: "Sets the grid amount to " + e + "."
                  })), new ButtonInfo({
                    buttonText: "Grid spacing +",
                    method: () => {
                      sendNotification("Grid spacing: " + (gridSpacing = Math.min(1.5, gridSpacing + 0.1)).toFixed(2), false);
                    },
                    isTogglable: false,
                    toolTip: "Increases spacing between spawned grid items."
                  }), new ButtonInfo({
                    buttonText: "Grid spacing -",
                    method: () => {
                      sendNotification("Grid spacing: " + (gridSpacing = Math.max(0.15, gridSpacing - 0.1)).toFixed(2), false);
                    },
                    isTogglable: false,
                    toolTip: "Decreases spacing between spawned grid items."
                  }), new ButtonInfo({
                    buttonText: "Grid columns +",
                    method: () => {
                      sendNotification("Grid columns: " + (gridColumns = Math.min(10, gridColumns + 1)), false);
                    },
                    isTogglable: false,
                    toolTip: "Uses a custom column count (1-10)."
                  }), new ButtonInfo({
                    buttonText: "Grid columns auto",
                    method: () => {
                      gridColumns = 0;
                      sendNotification("Grid columns: automatic", false);
                    },
                    isTogglable: false,
                    toolTip: "Returns to automatic square grid sizing."
                  }), new ButtonInfo({
                    buttonText: "Clear spawned grid",
                    method: () => {
                      clearSpawnedGrid();
                    },
                    isTogglable: false,
                    toolTip: "Removes grid items that are still frozen and have not been picked up."
                  })];
                  buttons[103] = [new ButtonInfo({
                    buttonText: "Exit Text Mods",
                    method: () => {
                      currentCategory = 11;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to the Stuff page."
                  }), new ButtonInfo({
                    buttonText: "Text: " + (oreTextQuery || "_"),
                    method: () => {
                      sendNotification("Ore text: " + (oreTextQuery || "_"), false, 99);
                    },
                    isTogglable: false,
                    toolTip: "Shows the text that the ore gun will spawn."
                  }), ..."ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!?".split("").map(e => new ButtonInfo({
                    buttonText: e,
                    method: () => {
                      if (oreTextQuery.length < oreTextMaxLength) {
                        oreTextQuery += e;
                      }
                      sendNotification("Ore text: " + (oreTextQuery || "_"), false, 99);
                    },
                    isTogglable: false,
                    toolTip: "Adds " + e + " to the ore text."
                  })), new ButtonInfo({
                    buttonText: "Space",
                    method: () => {
                      if (oreTextQuery.length < oreTextMaxLength) {
                        oreTextQuery += " ";
                      }
                    },
                    isTogglable: false,
                    toolTip: "Adds a space."
                  }), new ButtonInfo({
                    buttonText: "New Line",
                    method: () => {
                      if (oreTextQuery.length < oreTextMaxLength && !oreTextQuery.endsWith("|")) {
                        oreTextQuery += "|";
                      }
                    },
                    isTogglable: false,
                    toolTip: "Starts a new line."
                  }), new ButtonInfo({
                    buttonText: "Backspace",
                    method: () => {
                      sendNotification("Ore text: " + ((oreTextQuery = oreTextQuery.slice(0, -1)) || "_"), false, 99);
                    },
                    isTogglable: false,
                    toolTip: "Removes the last character."
                  }), new ButtonInfo({
                    buttonText: "Clear Text",
                    method: () => {
                      oreTextQuery = "";
                      clearSpawnedTextHellOre();
                    },
                    isTogglable: false,
                    toolTip: "Clears the entered text, pending spawns, and spawned text ores."
                  }), new ButtonInfo({
                    buttonText: "Ore Text Gun",
                    method: () => {
                      updateOreTextGun();
                    },
                    enableMethod: () => {
                      oreTextTriggerLatched = false;
                    },
                    disableMethod: () => {
                      oreTextTriggerLatched = false;
                    },
                    isTogglable: true,
                    toolTip: "Hold right grip to aim and pull trigger to spawn the entered text from Hell Ore."
                  }), new ButtonInfo({
                    buttonText: "Clear Ore Text",
                    method: () => {
                      clearSpawnedTextHellOre();
                    },
                    isTogglable: false,
                    toolTip: "Removes ore letters spawned by the Text Gun."
                  })];
                  buttons[104] = [new ButtonInfo({
                    buttonText: "Exit Text Options",
                    method: () => {
                      currentCategory = 103;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to the Text Mods keyboard."
                  }), new ButtonInfo({
                    buttonText: "Text Objects",
                    getLabel: () => "Objects: " + getOreTextActiveCount() + "/" + oreTextObjectBudget,
                    method: () => {
                      sendNotification("Text objects: " + getOreTextActiveCount() + "/" + oreTextObjectBudget, false);
                    },
                    isTogglable: false,
                    toolTip: "Shows the current text-object performance budget."
                  }), new ButtonInfo({
                    buttonText: "Object Budget -",
                    method: () => {
                      sendNotification("Text budget: " + (oreTextObjectBudget = Math.max(50, oreTextObjectBudget - 50)), false);
                    },
                    isTogglable: false,
                    toolTip: "Lowers the maximum active text pieces."
                  }), new ButtonInfo({
                    buttonText: "Object Budget +",
                    method: () => {
                      sendNotification("Text budget: " + (oreTextObjectBudget = Math.min(1000, oreTextObjectBudget + 50)), false);
                    },
                    isTogglable: false,
                    toolTip: "Raises the maximum active text pieces. Higher values cost performance."
                  }), new ButtonInfo({
                    buttonText: "Material",
                    getLabel: () => "Material: " + getOreTextMaterial()[0],
                    method: () => {
                      cycleOreTextMaterial(1);
                    },
                    isTogglable: false,
                    toolTip: "Cycles Hell, Gold, Silver, Copper, and Uranium text."
                  }), new ButtonInfo({
                    buttonText: "Material Previous",
                    method: () => {
                      cycleOreTextMaterial(-1);
                    },
                    isTogglable: false,
                    toolTip: "Selects the previous text material."
                  }), new ButtonInfo({
                    buttonText: "Layout",
                    getLabel: () => "Layout: " + oreTextLayoutMode,
                    method: () => {
                      sendNotification("Text layout: " + (oreTextLayoutMode = cycleTextSetting(oreTextLayoutMode, ["straight", "curve", "circle", "wave"], 1)), false);
                    },
                    isTogglable: false,
                    toolTip: "Cycles straight, curved, circular, and wave text."
                  }), new ButtonInfo({
                    buttonText: "Curve Strength -",
                    method: () => {
                      sendNotification("Curve strength: " + (oreTextCurveStrength = Math.max(0.25, oreTextCurveStrength - 0.25)).toFixed(2), false);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Curve Strength +",
                    method: () => {
                      sendNotification("Curve strength: " + (oreTextCurveStrength = Math.min(3, oreTextCurveStrength + 0.25)).toFixed(2), false);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Pixel Font",
                    getLabel: () => "Font: " + oreTextFontMode,
                    method: () => {
                      sendNotification("Pixel font: " + (oreTextFontMode = cycleTextSetting(oreTextFontMode, ["block", "outline", "stencil"], 1)), false);
                    },
                    isTogglable: false,
                    toolTip: "Cycles block, outline, and stencil pixel fonts."
                  }), new ButtonInfo({
                    buttonText: "Alignment",
                    getLabel: () => "Align: " + oreTextAlignment,
                    method: () => {
                      sendNotification("Text alignment: " + (oreTextAlignment = cycleTextSetting(oreTextAlignment, ["left", "center", "right"], 1)), false);
                    },
                    isTogglable: false,
                    toolTip: "Controls multi-line left, center, or right alignment."
                  }), new ButtonInfo({
                    buttonText: "Surface Snap",
                    getLabel: () => "Snap: " + oreTextSnapMode,
                    method: () => {
                      sendNotification("Text snap: " + (oreTextSnapMode = cycleTextSetting(oreTextSnapMode, ["wall", "floor", "billboard"], 1)), false);
                    },
                    isTogglable: false,
                    toolTip: "Cycles wall, floor, and viewer-facing placement."
                  }), new ButtonInfo({
                    buttonText: "Rotation -15",
                    method: () => {
                      sendNotification("Text rotation: " + (oreTextRollDegrees = Math.max(-180, oreTextRollDegrees - 15)) + " degrees", false);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Rotation +15",
                    method: () => {
                      sendNotification("Text rotation: " + (oreTextRollDegrees = Math.min(180, oreTextRollDegrees + 15)) + " degrees", false);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Rotation Reset",
                    method: () => {
                      oreTextRollDegrees = 0;
                      sendNotification("Text rotation reset", false);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Color Mode",
                    getLabel: () => "Color: " + oreTextColorMode,
                    method: () => {
                      sendNotification("Text color: " + (oreTextColorMode = cycleTextSetting(oreTextColorMode, ["natural", "solid", "rainbow", "gradient"], 1)), false);
                    },
                    isTogglable: false,
                    toolTip: "Cycles natural material, solid hue, rainbow, and animated gradient."
                  }), new ButtonInfo({
                    buttonText: "Text Hue -",
                    method: () => {
                      sendNotification("Text hue: " + (oreTextBaseHue = Math.max(-127, oreTextBaseHue - 16)), false);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Text Hue +",
                    method: () => {
                      sendNotification("Text hue: " + (oreTextBaseHue = Math.min(127, oreTextBaseHue + 16)), false);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Animation",
                    getLabel: () => "Animation: " + oreTextAnimationMode,
                    method: () => {
                      sendNotification("Text animation: " + (oreTextAnimationMode = cycleTextSetting(oreTextAnimationMode, ["none", "pulse", "spin", "dissolve", "typewriter"], 1)), false);
                    },
                    isTogglable: false,
                    toolTip: "Cycles pulse, spin, dissolve, and typewriter animations."
                  }), new ButtonInfo({
                    buttonText: "Follow Target",
                    getLabel: () => "Follow: " + oreTextFollowMode,
                    method: () => {
                      sendNotification("Text follow: " + (oreTextFollowMode = cycleTextSetting(oreTextFollowMode, ["none", "local", "selected", "object"], 1)) + (oreTextFollowMode === "selected" ? " (" + usersSelectedLabel + ")" : ""), false);
                    },
                    isTogglable: false,
                    toolTip: "Keeps new text attached to you, the selected player, or the selected UX object."
                  }), new ButtonInfo({
                    buttonText: "Save Style Favorite",
                    method: () => {
                      saveOreTextStyle();
                    },
                    isTogglable: false,
                    toolTip: "Saves all current text appearance, layout, color, and animation settings."
                  }), new ButtonInfo({
                    buttonText: "Load Style Favorite",
                    method: () => {
                      loadOreTextStyle();
                    },
                    isTogglable: false,
                    toolTip: "Restores the saved text style."
                  }), new ButtonInfo({
                    buttonText: "Ghost Preview",
                    enableMethod: () => {
                      oreTextPreviewEnabled = true;
                    },
                    disableMethod: () => {
                      oreTextPreviewEnabled = false;
                      clearOreTextPreview();
                    },
                    isTogglable: true,
                    enabled: oreTextPreviewEnabled,
                    toolTip: "Shows a local orange placement preview while aiming."
                  }), new ButtonInfo({
                    buttonText: "Undo Last Text",
                    method: () => {
                      undoLastOreTextBatch();
                    },
                    isTogglable: false,
                    toolTip: "Removes only the most recently spawned word or phrase."
                  }), new ButtonInfo({
                    buttonText: "Save Phrase",
                    method: () => {
                      saveCurrentOreTextPhrase();
                    },
                    isTogglable: false,
                    toolTip: "Saves the current text as a reusable phrase."
                  }), new ButtonInfo({
                    buttonText: "Saved Phrases",
                    method: () => {
                      currentCategory = 105;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Opens saved phrase controls."
                  }), new ButtonInfo({
                    buttonText: "Piece Size",
                    getLabel: () => "Piece Size: " + oreTextPieceScale.toFixed(2),
                    method: () => {
                      sendNotification("Text piece size: " + oreTextPieceScale.toFixed(2), false);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Piece Size -",
                    method: () => {
                      sendNotification("Piece size: " + (oreTextPieceScale = Math.max(0.25, oreTextPieceScale - 0.05)).toFixed(2), false);
                    },
                    isTogglable: false,
                    toolTip: "Makes each lava-ore text pixel smaller."
                  }), new ButtonInfo({
                    buttonText: "Piece Size +",
                    method: () => {
                      sendNotification("Piece size: " + (oreTextPieceScale = Math.min(1.25, oreTextPieceScale + 0.05)).toFixed(2), false);
                    },
                    isTogglable: false,
                    toolTip: "Makes each lava-ore text pixel larger."
                  }), new ButtonInfo({
                    buttonText: "Thickness",
                    getLabel: () => "Thickness: " + oreTextThickness.toFixed(2),
                    method: () => {
                      sendNotification("Text thickness: " + oreTextThickness.toFixed(2), false);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Thickness -",
                    method: () => {
                      sendNotification("Thickness: " + (oreTextThickness = Math.max(0.08, oreTextThickness - 0.04)).toFixed(2), false);
                    },
                    isTogglable: false,
                    toolTip: "Makes spawned lava ores flatter."
                  }), new ButtonInfo({
                    buttonText: "Thickness +",
                    method: () => {
                      sendNotification("Thickness: " + (oreTextThickness = Math.min(1, oreTextThickness + 0.04)).toFixed(2), false);
                    },
                    isTogglable: false,
                    toolTip: "Makes spawned lava ores deeper."
                  }), new ButtonInfo({
                    buttonText: "Letter Spacing",
                    getLabel: () => "Spacing: " + oreTextCellSize.toFixed(3),
                    method: () => {
                      sendNotification("Text spacing: " + oreTextCellSize.toFixed(3), false);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Letter Spacing -",
                    method: () => {
                      sendNotification("Spacing: " + (oreTextCellSize = Math.max(0.045, oreTextCellSize - 0.005)).toFixed(3), false);
                    },
                    isTogglable: false,
                    toolTip: "Moves text pixels closer together."
                  }), new ButtonInfo({
                    buttonText: "Letter Spacing +",
                    method: () => {
                      sendNotification("Spacing: " + (oreTextCellSize = Math.min(0.16, oreTextCellSize + 0.005)).toFixed(3), false);
                    },
                    isTogglable: false,
                    toolTip: "Moves text pixels farther apart."
                  }), new ButtonInfo({
                    buttonText: "Surface Offset -",
                    method: () => {
                      sendNotification("Surface offset: " + (oreTextSurfaceOffset = Math.max(0.01, oreTextSurfaceOffset - 0.01)).toFixed(2), false);
                    },
                    isTogglable: false,
                    toolTip: "Places text closer to the aimed surface."
                  }), new ButtonInfo({
                    buttonText: "Surface Offset +",
                    method: () => {
                      sendNotification("Surface offset: " + (oreTextSurfaceOffset = Math.min(0.3, oreTextSurfaceOffset + 0.01)).toFixed(2), false);
                    },
                    isTogglable: false,
                    toolTip: "Places text farther in front of the aimed surface."
                  }), new ButtonInfo({
                    buttonText: "Outline Text",
                    enableMethod: () => {
                      oreTextOutline = true;
                    },
                    disableMethod: () => {
                      oreTextOutline = false;
                    },
                    isTogglable: true,
                    enabled: oreTextOutline,
                    toolTip: "Removes fully surrounded pixels for a lighter outline style."
                  }), new ButtonInfo({
                    buttonText: "Mirror Text",
                    enableMethod: () => {
                      oreTextMirror = true;
                    },
                    disableMethod: () => {
                      oreTextMirror = false;
                    },
                    isTogglable: true,
                    enabled: oreTextMirror,
                    toolTip: "Mirrors the spawned word horizontally."
                  }), new ButtonInfo({
                    buttonText: "Vertical Text",
                    enableMethod: () => {
                      oreTextVertical = true;
                    },
                    disableMethod: () => {
                      oreTextVertical = false;
                    },
                    isTogglable: true,
                    enabled: oreTextVertical,
                    toolTip: "Stacks characters vertically."
                  }), new ButtonInfo({
                    buttonText: "Spawn Speed -",
                    method: () => {
                      sendNotification("Text spawn speed: " + (oreTextPiecesPerFrame = Math.max(1, oreTextPiecesPerFrame - 1)), false);
                    },
                    isTogglable: false,
                    toolTip: "Spawns fewer text pieces per frame for smoother performance."
                  }), new ButtonInfo({
                    buttonText: "Spawn Speed +",
                    method: () => {
                      sendNotification("Text spawn speed: " + (oreTextPiecesPerFrame = Math.min(10, oreTextPiecesPerFrame + 1)), false);
                    },
                    isTogglable: false,
                    toolTip: "Spawns more text pieces per frame."
                  }), new ButtonInfo({
                    buttonText: "Thin Text Preset",
                    method: () => {
                      oreTextPieceScale = 0.52;
                      oreTextThickness = 0.18;
                      oreTextCellSize = 0.075;
                      sendNotification("Thin text preset applied", false);
                    },
                    isTogglable: false,
                    toolTip: "Applies small, closely spaced, very flat ore pieces."
                  }), new ButtonInfo({
                    buttonText: "Reset Text Style",
                    method: () => {
                      oreTextPieceScale = 0.58;
                      oreTextThickness = 0.28;
                      oreTextCellSize = 0.082;
                      oreTextSurfaceOffset = 0.055;
                      oreTextOutline = false;
                      oreTextMirror = false;
                      oreTextVertical = false;
                      oreTextPiecesPerFrame = 4;
                      oreTextMaterialIndex = 0;
                      oreTextLayoutMode = "straight";
                      oreTextFontMode = "block";
                      oreTextAlignment = "center";
                      oreTextSnapMode = "wall";
                      oreTextColorMode = "natural";
                      oreTextBaseHue = 0;
                      oreTextAnimationMode = "none";
                      oreTextFollowMode = "none";
                      oreTextRollDegrees = 0;
                      oreTextCurveStrength = 1;
                      oreTextPreviewEnabled = true;
                      syncOreTextToggleButtons();
                      sendNotification("Text style reset", false);
                    },
                    isTogglable: false,
                    toolTip: "Restores the improved default text style."
                  })];
                  buttons[105] = [new ButtonInfo({
                    buttonText: "Exit Saved Phrases",
                    method: () => {
                      currentCategory = 104;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to Text Options."
                  }), new ButtonInfo({
                    buttonText: "Current Saved Phrase",
                    getLabel: () => oreTextSavedPhrases.length > 0 ? "Phrase: " + oreTextSavedPhrases[oreTextSavedPhraseIndex].replace(/\|/g, "/") : "Phrase: none",
                    method: () => {
                      sendNotification(oreTextSavedPhrases.length > 0 ? oreTextSavedPhrases[oreTextSavedPhraseIndex].replace(/\|/g, " / ") : "No saved phrases", false);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Previous Phrase",
                    method: () => {
                      cycleSavedOreTextPhrase(-1);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Next Phrase",
                    method: () => {
                      cycleSavedOreTextPhrase(1);
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Load Phrase",
                    method: () => {
                      loadSavedOreTextPhrase();
                    },
                    isTogglable: false,
                    toolTip: "Loads the selected phrase into the Text Mods keyboard."
                  }), new ButtonInfo({
                    buttonText: "Save Current Phrase",
                    method: () => {
                      saveCurrentOreTextPhrase();
                    },
                    isTogglable: false
                  }), new ButtonInfo({
                    buttonText: "Delete Phrase",
                    method: () => {
                      deleteSavedOreTextPhrase();
                    },
                    isTogglable: false
                  })];
                  buttons[111] = [new ButtonInfo({
                    buttonText: "Exit Telekinesis Mods ",
                    method: () => {
                      currentCategory = 11;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to main page."
                  }), new ButtonInfo({
                    buttonText: "Telekinesis",
                    isTogglable: true,
                    enableMethod: () => {
                      telekinesisEnabled = true;
                      releaseTelekinesisPlayer();
                      sendNotification("Telekinesis ON – nearest player", false);
                    },
                    disableMethod: () => {
                      telekinesisEnabled = false;
                      releaseTelekinesisPlayer();
                      sendNotification("Telekinesis OFF", false);
                    },
                    toolTip: "Hold grip + trigger to grab the nearest player and move them with your hand."
                  }), new ButtonInfo({
                    buttonText: "TK: Hold Dist +",
                    method: () => {
                      sendNotification("Hold distance: " + (telekinesisHoldDistance = Math.min(12, telekinesisHoldDistance + 0.5)).toFixed(1), false);
                    },
                    isTogglable: false,
                    toolTip: "Increases how far the object floats in front of your hand."
                  }), new ButtonInfo({
                    buttonText: "TK: Hold Dist -",
                    method: () => {
                      sendNotification("Hold distance: " + (telekinesisHoldDistance = Math.max(1, telekinesisHoldDistance - 0.5)).toFixed(1), false);
                    },
                    isTogglable: false,
                    toolTip: "Decreases how far the object floats in front of your hand."
                  }), new ButtonInfo({
                    buttonText: "TK: Force +",
                    method: () => {
                      telekinesisForceStrength = Math.min(40, telekinesisForceStrength + 2);
                      sendNotification("Force: " + telekinesisForceStrength.toFixed(1), false);
                    },
                    isTogglable: false,
                    toolTip: "Makes objects follow your hand more aggressively."
                  }), new ButtonInfo({
                    buttonText: "TK: Force -",
                    method: () => {
                      telekinesisForceStrength = Math.max(4, telekinesisForceStrength - 2);
                      sendNotification("Force: " + telekinesisForceStrength.toFixed(1), false);
                    },
                    isTogglable: false,
                    toolTip: "Makes objects follow more gently."
                  }), new ButtonInfo({
                    buttonText: "TK: Drop Object",
                    method: () => {
                      if (telekinesisHeldObject) {
                        releaseTelekinesisObject();
                        sendNotification("Object dropped", false);
                      } else {
                        sendNotification("Nothing held", false);
                      }
                    },
                    isTogglable: false,
                    toolTip: "Force-releases the currently held object."
                  })];
                  buttons[62] = [new ButtonInfo({
                    buttonText: "Exit VFX IDs",
                    method: () => {
                      currentCategory = 56;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to vfx spawning."
                  }), ...vfxTypeEntries.map(([e, t, o], n) => new ButtonInfo({
                    buttonText: e + " (" + t + ")",
                    method: () => {
                      selectedVfxIndex = n;
                      const [e, i] = getSelectedVfxEntry();
                      if (o === "asset") {
                        compileVisualEffectAssetNow(t);
                      }
                      sendNotification("VFX ID: " + e + " (" + i + ")", false);
                    },
                    isTogglable: false,
                    toolTip: "Selects VFX ID " + t + "."
                  }))];
                  buttons[99] = [new ButtonInfo({
                    buttonText: "Exit Room Mods",
                    method: () => {
                      currentCategory = 11;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to main page."
                  }), new ButtonInfo({
                    buttonText: "Leave",
                    method: () => {
                      try {
                        const e = NManager.method("get_instance").invoke();
                        if (!e || e.isNull()) {
                          return;
                        }
                        e.method("TryShutdownAndCancelConnecting").invoke();
                      } catch (e) {
                        console.error("[Leave]", e);
                      }
                    },
                    isTogglable: false,
                    toolTip: "Leaves the room cleanly."
                  })];
                  buttons[18] = [new ButtonInfo({
                    buttonText: "Exit Mob IDs",
                    method: () => {
                      currentCategory = 15;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to ID Settings."
                  }), ...mobIDs.map((e, t) => new ButtonInfo({
                    buttonText: e.name,
                    method: () => {
                      sendNotification("Mob ID: " + mobIDs[mobIndex = t].name + " (" + mobIDs[mobIndex].id + ")", false);
                    },
                    isTogglable: false,
                    toolTip: "Selects mob ID " + e.id + "."
                  }))];
                  buttons[20] = [new ButtonInfo({
                    buttonText: "Exit Prefab IDs",
                    method: () => {
                      currentCategory = 14;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to ID Settings."
                  }), ...prefabIDs.map((e, t) => new ButtonInfo({
                    buttonText: String(e),
                    method: () => {
                      prefabIndex = t;
                      sendNotification("Prefab ID: " + getSelectedPrefabID(), false);
                    },
                    isTogglable: false,
                    toolTip: "Selects prefab ID: " + e
                  }))];
                  buttons[56] = [new ButtonInfo({
                    buttonText: "Exit VFX Spawning",
                    method: () => {
                      currentCategory = 11;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to the Others menu."
                  }), new ButtonInfo({
                    buttonText: "VFX IDS",
                    method: () => {
                      currentCategory = 62;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "enters vfx ids."
                  }), new ButtonInfo({
                    buttonText: "Delete All VFX",
                    method: () => {
                      sendNotification("Deleted VFX: " + deleteAllSpawnedVfxObjects(), false);
                    },
                    isTogglable: false,
                    toolTip: "Removes VFX objects spawned by this menu."
                  }), new ButtonInfo({
                    buttonText: "VFX Gun",
                    enableMethod: () => {
                      prewarmAllVfxDispatch();
                      vfxGunEnabled = true;
                      ramVfxGunEnabled = false;
                      rpEarnVfxGunEnabled = false;
                    },
                    disableMethod: () => {
                      vfxGunEnabled = false;
                      hideGunLib();
                    },
                    method: () => {
                      if (!vfxGunEnabled) {
                        return;
                      }
                      if (!rightGrab) {
                        hideGunLib();
                        return;
                      }
                      const e = renderGun();
                      if (rightTrigger && e.ray && !e.ray.isNull() && !(time < lastGunTime + 0.03)) {
                        lastGunTime = time;
                        try {
                          vfxIndex = selectedVfxIndex;
                          const [t, o, n] = getSelectedVfxEntry();
                          if (!(n === "asset" ? playVisualEffectAsset(o, e.endPosition, identityQuaternion) : playNetworkVFX(o, e.endPosition, identityQuaternion))) {
                            sendNotification("VFX failed: " + t, false);
                          }
                        } catch (e) {
                          console.error("VFX gun error:", e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "Spawns the selected network or asset VFX at the gun pointer."
                  })];
                  buttons[62] = [new ButtonInfo({
                    buttonText: "RAM VFX Gun",
                    enableMethod: () => {
                      ramVfxGunEnabled = true;
                      vfxGunEnabled = false;
                      rpEarnVfxGunEnabled = false;
                      gunSmoothSnapNextFrame = true;
                    },
                    disableMethod: () => {
                      ramVfxGunEnabled = false;
                      hideGunLib();
                    },
                    method: () => {
                      if (!ramVfxGunEnabled) {
                        return;
                      }
                      if (!rightGrab) {
                        hideGunLib();
                        return;
                      }
                      const e = renderGun();
                      if (rightTrigger && e.ray && !e.ray.isNull() && !(time < lastGunTime + 0.03)) {
                        lastGunTime = time;
                        try {
                          let t = 0;
                          if (playVisualEffectAsset(VisualEffectAssetTypes.RAMActivation, e.endPosition, identityQuaternion, 3600)) {
                            t++;
                          }
                          if (playVisualEffectAsset(VisualEffectAssetTypes.RamBreakdown, e.endPosition, identityQuaternion, 3600)) {
                            t++;
                          }
                          if (playNetworkVFX(NetworkVFXTypes.RAMActivationSparks, e.endPosition, identityQuaternion, 3600)) {
                            t++;
                          }
                          if (t === 0) {
                            sendNotification("RAM VFX failed (see console)", false);
                          }
                        } catch (e) {
                          console.error("RAM VFX gun error:", e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "Spawns the long-lived RAM tornado effect at the gun pointer."
                  }), new ButtonInfo({
                    buttonText: "Fake RP Gun",
                    enableMethod: () => {
                      rpEarnVfxGunEnabled = true;
                      vfxGunEnabled = false;
                      ramVfxGunEnabled = false;
                    },
                    disableMethod: () => {
                      rpEarnVfxGunEnabled = false;
                      hideGunLib();
                    },
                    method: () => {
                      if (!rpEarnVfxGunEnabled) {
                        return;
                      }
                      if (!rightGrab) {
                        hideGunLib();
                        return;
                      }
                      const e = renderGun();
                      if (rightTrigger && e.ray && !e.ray.isNull() && !(time < lastGunTime + 0.03)) {
                        lastGunTime = time;
                        try {
                          let t = playVisualEffectAsset(VisualEffectAssetTypes.RPEarn, e.endPosition, identityQuaternion, 3600);
                          t ||= fireClientVfxAt(e.endPosition, RPEARN_VFX_ENTRY);
                          if (!t) {
                            sendNotification("RPEarn VFX failed (see console)", false);
                          }
                        } catch (e) {
                          console.error("RPEarn VFX gun error:", e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "Spawns the long-lived RPEarn effect at the gun pointer."
                  })];
                  buttons[67] = [new ButtonInfo({
                    buttonText: "Exit Ammo Mods",
                    method: () => {
                      currentCategory = 25;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to weapons mods."
                  }), new ButtonInfo({
                    buttonText: "Inf Ammo [W?]",
                    enableMethod: () => {
                      InfAmmo = true;
                    },
                    disableMethod: () => {
                      InfAmmo = false;
                    },
                    isTogglable: true,
                    toolTip: "Infinite ammo"
                  }), new ButtonInfo({
                    buttonText: "No Recoil",
                    enableMethod: () => {
                      noRecoil = true;
                    },
                    disableMethod: () => {
                      noRecoil = false;
                    },
                    isTogglable: true,
                    toolTip: "Removes recoil force and spread on all guns (Revolver, Shotgun, Arena guns)."
                  }), new ButtonInfo({
                    buttonText: "No Shotgun Cooldown",
                    enableMethod: () => {
                      noShotgunCooldown = true;
                    },
                    disableMethod: () => {
                      noShotgunCooldown = false;
                    },
                    isTogglable: true,
                    toolTip: "Removes the reload/cooldown delay between shotgun shots."
                  }), new ButtonInfo({
                    buttonText: "Multi Shoot",
                    method: () => {
                      try {
                        const e = NetPlayer.method("get_localPlayer").invoke();
                        if (!e) {
                          return;
                        }
                        const t = 15;
                        for (let o = 0; o <= 1; o++) {
                          const n = e.method("GetHandInteractor", 1).invoke(o);
                          if (!n) {
                            continue;
                          }
                          const i = n.field("_itemAnchor").value;
                          if (!i) {
                            continue;
                          }
                          const a = i.method("get_grabbableObject").invoke();
                          if (!a) {
                            continue;
                          }
                          if (o === 1 ? rightTrigger : leftTrigger) {
                            for (let e = 0; e < t; e++) {
                              a.method("HandleTriggerUse").invoke();
                            }
                          }
                        }
                      } catch (e) {
                        console.error("Rapid Fire error:", e);
                      }
                    },
                    isTogglable: true,
                    toolTip: "Fires multiple shots per trigger pull from both hands"
                  }), new ButtonInfo({
                    buttonText: "Rapid Fire",
                    enableMethod: () => {
                      rapidFireEnabled = true;
                    },
                    disableMethod: () => {
                      rapidFireEnabled = false;
                    },
                    method: () => {
                      try {
                        const e = (e, t, o, ...n) => {
                          for (const i of t) {
                            try {
                              const t = tryMethodName(e.class, [i], o);
                              if (t) {
                                invokeInstance(t, e, ...n);
                              }
                            } catch (e) {}
                          }
                        };
                        const t = t => {
                          try {
                            e(t, ["HandleSecondaryUse", "HandleGripUse", "CockHammer", "PullBackHammer"], 0);
                            e(t, ["HandleSecondaryButton", "HandleSecondaryUse", "HandleGripUse", "CockHammer", "PullBackHammer"], 1, true);
                            e(t, ["HandleSecondaryButton", "HandleSecondaryUse", "HandleGripUse"], 1, false);
                            e(t, ["OnSecondaryButtonDown", "OnSecondaryUseDown"], 0);
                            e(t, ["OnSecondaryButtonUp", "OnSecondaryUseUp"], 0);
                          } catch (e) {}
                        };
                        const o = (e, o) => {
                          if (e && o && !(time <= rapidFirePulseDelay)) {
                            rapidFirePulseDelay = time + 0.018;
                            try {
                              const o = String(e.class?.type?.name ?? "").toLowerCase();
                              try {
                                applyGunBuffsToGrabbable(e, false);
                              } catch (e) {}
                              if (o.indexOf("revolver") >= 0) {
                                try {
                                  e.field("_isHammerCocked").value = true;
                                } catch (e) {}
                                try {
                                  e.field("isHammerCocked").value = true;
                                } catch (e) {}
                                try {
                                  e.field("_ammoLoaded").value = 255;
                                } catch (e) {}
                                try {
                                  e.field("ammoLoaded").value = 255;
                                } catch (e) {}
                                try {
                                  e.field("_reloadTimer").value = 0;
                                } catch (e) {}
                                try {
                                  e.field("_triggerUseCooldown").value = 0;
                                } catch (e) {}
                                try {
                                  e.field("_secondaryUseCooldown").value = 0;
                                } catch (e) {}
                                try {
                                  e.field("_hammerPullbackAmount").value = 1;
                                } catch (e) {}
                                try {
                                  e.method("set_isHammerCocked").invoke(true);
                                } catch (e) {}
                                t(e);
                              }
                              try {
                                e.method("HandleTriggerUse").invoke();
                              } catch (e) {}
                              if (o.indexOf("revolver") >= 0) {
                                try {
                                  e.field("_triggerUseCooldown").value = 0;
                                } catch (e) {}
                                try {
                                  e.field("_secondaryUseCooldown").value = 0;
                                } catch (e) {}
                                try {
                                  e.method("set_isHammerCocked").invoke(true);
                                } catch (e) {}
                                for (let t = 0; t < 3; t++) {
                                  tryCallNames(e, ["OnBButtonDown", "HandleBButtonDown", "PressBButton"], 0);
                                  tryCallNames(e, ["HandleBButton", "HandleSecondaryButton", "HandleSecondaryUse"], 1, true);
                                  tryCallNames(e, ["HandleBButton", "HandleSecondaryButton", "HandleSecondaryUse"], 1, false);
                                }
                                t(e);
                              }
                            } catch (e) {}
                          }
                        };
                        const n = NetPlayer.method("get_localPlayer").invoke();
                        if (!n) {
                          return;
                        }
                        const i = n.method("GetHandInteractor", 1).invoke(1);
                        let a = null;
                        if (i) {
                          try {
                            const e = i.field("_itemAnchor").value;
                            if (e) {
                              a = e.method("get_grabbableObject").invoke();
                            }
                          } catch (e) {}
                        }
                        const l = n.method("GetHandInteractor", 1).invoke(0);
                        let r = null;
                        if (l) {
                          try {
                            const e = l.field("_itemAnchor").value;
                            if (e) {
                              r = e.method("get_grabbableObject").invoke();
                            }
                          } catch (e) {}
                        }
                        o(a, rightTrigger);
                        o(r, leftTrigger);
                      } catch (e) {
                        console.error(e);
                      }
                    },
                    isTogglable: true,
                    toolTip: "Rapid fires trigger items and also force-cocks revolvers when possible."
                  })];
                  buttons[112] = [new ButtonInfo({
                    buttonText: "Exit Mom Boss Mods",
                    method: () => {
                      currentCategory = 11;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to Mom Boss Mods."
                  }), new ButtonInfo({
                    buttonText: "Spawn Mom Boss",
                    isTogglable: false,
                    toolTip: "spawn mom boss related prefab/item at right hand",
                    method: () => {
                      a(1.2);
                    }
                  }), new ButtonInfo({
                    buttonText: "Spawn Mom At Me",
                    isTogglable: false,
                    toolTip: "spawn mom boss at your position",
                    method: () => {
                      try {
                        const e = getTransform(bodyCollider).method("get_position").invoke();
                        i(e, identityQuaternion);
                      } catch (e) {
                        sendNotification("Mom spawn at me failed", false, 2);
                      }
                    }
                  }), new ButtonInfo({
                    buttonText: "Mom Reset Game",
                    isTogglable: false,
                    toolTip: "calls MomBoss ResetGame/RPC_ResetGame",
                    method: () => {
                      if (!o("ResetGame")) {
                        o("RPC_ResetGame");
                      }
                    }
                  }), new ButtonInfo({
                    buttonText: "Mom Easy Mode",
                    isTogglable: false,
                    toolTip: "sets mom boss easy mode field from dump",
                    method: () => {
                      n("_isEasyMode", true);
                    }
                  }), new ButtonInfo({
                    buttonText: "Mom Always Win",
                    isTogglable: false,
                    toolTip: "sets mom boss always-win field from dump",
                    method: () => {
                      n("_isAlwaysWinMode", true);
                    }
                  }), new ButtonInfo({
                    buttonText: "Mom Toy Block",
                    isTogglable: false,
                    toolTip: "change mom boss game mode to toy block",
                    method: () => {
                      if (!o("SetToyBlockGameState")) {
                        o("TestP2ToyBlock");
                      }
                    }
                  }), new ButtonInfo({
                    buttonText: "Mom Simon Says",
                    isTogglable: false,
                    toolTip: "change mom boss game mode to simon says",
                    method: () => {
                      if (!o("SetSimonSaysGameState")) {
                        o("TestP2SimonSays");
                      }
                    }
                  }), new ButtonInfo({
                    buttonText: "Mom Floor Slap",
                    isTogglable: false,
                    toolTip: "change mom boss game mode to floor slap",
                    method: () => {
                      if (!o("SetFloorSlappingGameState")) {
                        o("TestP3FloorSlap");
                      }
                    }
                  }), new ButtonInfo({
                    buttonText: "Mom Blade Ball",
                    isTogglable: false,
                    toolTip: "change mom boss game mode to blade ball",
                    method: () => {
                      if (!o("SetBladeBallGameState")) {
                        o("TestP4BladeBall");
                      }
                    }
                  }), new ButtonInfo({
                    buttonText: "Mom Summon Zombies",
                    isTogglable: false,
                    toolTip: "calls mom boss SummonZombies",
                    method: () => {
                      if (!o("SummonZombies")) {
                        o("SetSummonZombiesP3GameState");
                      }
                    }
                  }), new ButtonInfo({
                    buttonText: "Mom Supply Burst",
                    isTogglable: false,
                    toolTip: "MomBossItemSpawner.SpawnSupplyItems / fallback supplies",
                    method: () => {
                      l();
                    }
                  }), new ButtonInfo({
                    buttonText: "Horde On",
                    isTogglable: false,
                    toolTip: "turns horde controller/spawner fields on",
                    method: () => {
                      s(true);
                    }
                  }), new ButtonInfo({
                    buttonText: "Horde Off",
                    isTogglable: false,
                    toolTip: "turns horde controller/spawner fields off",
                    method: () => {
                      s(false);
                    }
                  }), new ButtonInfo({
                    buttonText: "Mom Kill Zombies",
                    isTogglable: false,
                    toolTip: "calls mom boss KillAllZombies",
                    method: () => {
                      o("KillAllZombies");
                    }
                  }), new ButtonInfo({
                    buttonText: "Mom Success",
                    isTogglable: false,
                    toolTip: "force mom boss success state",
                    method: () => {
                      if (!o("SetGameSuccessGameState")) {
                        o("TestP2Success");
                      }
                    }
                  }), new ButtonInfo({
                    buttonText: "Mom Death",
                    isTogglable: false,
                    toolTip: "force mom boss death state",
                    method: () => {
                      if (!o("SetDeathGameState")) {
                        o("SetFatalHitGameState");
                      }
                    }
                  })];
                  buttons[77] = [new ButtonInfo({
                    buttonText: "Exit Rig mods",
                    method: () => {
                      currentCategory = 11;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to other players."
                  }), new ButtonInfo({
                    buttonText: "Rig Spammer (Trigger)",
                    isTogglable: true,
                    enableMethod: () => {
                      triggerCloneSpawnActive = true;
                    },
                    disableMethod: () => {
                      triggerCloneSpawnActive = false;
                    },
                    toolTip: "Spawns a networked clone when you pull the right trigger (EX logic)."
                  }), new ButtonInfo({
                    buttonText: "Clear Clones",
                    isTogglable: false,
                    method: () => {
                      clearRigClones();
                    },
                    toolTip: "Removes only clones created by this menu."
                  }), new ButtonInfo({
                    buttonText: "Force Ragdoll",
                    isTogglable: true,
                    enableMethod: () => {
                      setLocalRigRagdoll(true);
                    },
                    disableMethod: () => {
                      setLocalRigRagdoll(false);
                    },
                    toolTip: "Toggles ragdoll on your local rig."
                  }), new ButtonInfo({
                    buttonText: "Grab Rig",
                    isTogglable: true,
                    enableMethod: () => {
                      rigGrabActive = true;
                    },
                    disableMethod: () => {
                      rigGrabActive = false;
                      resetGrabbedRig();
                    },
                    toolTip: "Lets you grab your rig"
                  })];
                  buttons[21] = [new ButtonInfo({
                    buttonText: "Exit other players",
                    method: () => {
                      currentCategory = 11;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Returns to other players."
                  }), new ButtonInfo({
                    buttonText: "Whitelist mods",
                    method: () => {
                      currentCategory = 10;
                      currentPage = 0;
                    },
                    isTogglable: false,
                    toolTip: "Opens the test category."
                  }), new ButtonInfo({
                    buttonText: "TP ALL (gun)",
                    method: () => {
                      if (!rightGrab) {
                        return;
                      }
                      const e = renderGun().ray;
                      if (rightTrigger && e && !e.isNull()) {
                        try {
                          const t = e.method("get_point").invoke();
                          const o = NetPlayer.field("playerIDToNetPlayer").value;
                          const n = o.method("get_Values").invoke().method("GetEnumerator").invoke();
                          while (n.method("MoveNext").invoke()) {
                            const e = n.method("get_Current").invoke();
                            if (e && !e.handle.isNull()) {
                              if (!e.method("get_IsMine").invoke()) {
                                e.method("RPC_Teleport").invoke(t);
                              }
                            }
                          }
                        } catch (e) {
                          console.error("TP ALL Gun error:", e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "Teleports all players to your gun pointer."
                  }), new ButtonInfo({
                    buttonText: "TP All To Me",
                    method: () => {
                      const e = getTransform(GTPlayer).method("get_position").invoke();
                      const t = Object.method("FindObjectsOfType").inflate(NetPlayer).invoke();
                      if (t) {
                        for (let o = 0; o < t.length; o++) {
                          const n = t.get(o);
                          if (!playerIsLocal(n)) {
                            n.method("RPC_Teleport").invoke(e);
                          }
                        }
                      }
                    },
                    isTogglable: false,
                    toolTip: "Teleports all players to your position."
                  }), new ButtonInfo({
                    buttonText: "Teleport To Random",
                    method: () => {
                      const e = NetPlayerClass.field("_spawnedPlayers").value;
                      const t = e.method("get_Count").invoke();
                      teleportPlayer(getTransform(e.method("get_Item").invoke(Math.floor(Math.random() * t))).method("get_position").invoke());
                      Rigidbody.method("set_linearVelocity").invoke(zeroVector);
                    },
                    isTogglable: false,
                    toolTip: "Teleports you to a random player."
                  }), new ButtonInfo({
                    buttonText: "Player Orbit",
                    method: () => {
                      try {
                        const e = getAllNetPlayersList(false);
                        if (e.length === 0) {
                          return;
                        }
                        const t = getTransform(headCollider).method("get_position").invoke();
                        const o = t.field("x").value;
                        const n = t.field("y").value;
                        const i = t.field("z").value;
                        const a = 3.5;
                        const l = time * 1.85;
                        for (let t = 0; t < e.length; t++) {
                          const r = e[t];
                          if (!r || r.handle.isNull()) {
                            continue;
                          }
                          const s = l + t / Math.max(e.length, 1) * Math.PI * 2;
                          const c = [o + Math.cos(s) * a, n + 0.15 + Math.sin(s * 2 + time * 0.4) * 0.25, i + Math.sin(s) * a];
                          try {
                            r.method("RPC_Teleport").invoke(c);
                          } catch (e) {}
                        }
                      } catch (e) {
                        console.error("Orbit Players error:", e);
                      }
                    },
                    isTogglable: true,
                    toolTip: "Continuously teleports all other players into an orbit around you."
                  }), new ButtonInfo({
                    buttonText: "Player Orbit (Fast)",
                    method: () => {
                      try {
                        const e = getAllNetPlayersList(false);
                        if (e.length === 0) {
                          return;
                        }
                        const t = getTransform(headCollider).method("get_position").invoke();
                        const o = t.field("x").value;
                        const n = t.field("y").value;
                        const i = t.field("z").value;
                        const a = 3.5;
                        const l = time * 5.550000000000001;
                        for (let t = 0; t < e.length; t++) {
                          const r = e[t];
                          if (!r || r.handle.isNull()) {
                            continue;
                          }
                          const s = l + t / Math.max(e.length, 1) * Math.PI * 2;
                          const c = [o + Math.cos(s) * a, n + 0.15 + Math.sin(s * 2 + time * 1.2) * 0.25, i + Math.sin(s) * a];
                          try {
                            r.method("RPC_Teleport").invoke(c);
                          } catch (e) {}
                        }
                      } catch (e) {
                        console.error("Orbit Players Fast error:", e);
                      }
                    },
                    isTogglable: true,
                    toolTip: "Continuously teleports all other players into a much faster orbit around you."
                  }), new ButtonInfo({
                    buttonText: "RPG Hands All",
                    enableMethod: () => {
                      rpgHandsDelay = 0;
                      sendNotification("RPG Hands All ON", false);
                    },
                    disableMethod: () => {
                      sendNotification("RPG Hands All OFF", false);
                    },
                    isTogglable: true,
                    method: () => {
                      if (!(time <= rpgHandsDelay)) {
                        rpgHandsDelay = time + 0.035;
                        try {
                          for (const e of getAllNetPlayersList(false)) {
                            try {
                              if (!grabbableLooksLikeRevolver(getPlayerHeldGrabbable(e, 1))) {
                                continue;
                              }
                              const t = getPlayerHandAnchorTransform(e, 1) ?? getPlayerProjectileOriginTransform(e);
                              if (!t || t.isNull?.()) {
                                continue;
                              }
                              const o = t.method("get_position").invoke();
                              const n = getPlayerStraightForward(e);
                              const i = [0, 1, 0];
                              const a = getLookRotationFromForward(n);
                              const l = spawnNetworkPrefab("RPGRocket", Vector3.method("op_Addition").invoke(o, Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(n, 0.72), [0, -0.025, 0])), a);
                              if (l && !l.isNull()) {
                                trySetObjectVelocity(l, Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(n, 96), Vector3.method("op_Multiply", 2).invoke(i, 1)));
                              }
                            } catch (e) {}
                          }
                        } catch (e) {
                          console.error("RPG Hands All:", e);
                        }
                      }
                    },
                    toolTip: "Anyone holding a revolver in their right hand gets fast RPG hands from that right-hand anchor."
                  }), new ButtonInfo({
                    buttonText: "Shake All Screens",
                    enableMethod: () => {
                      screenshakeAllEnabled = true;
                      sendNotification("Screen Shake ON!", false);
                    },
                    disableMethod: () => {
                      screenshakeAllEnabled = false;
                      sendNotification("Screen Shake OFF", false);
                    },
                    method: () => {
                      if (screenshakeAllEnabled && frameCount % 10 == 0) {
                        try {
                          const e = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
                          for (let t = 0; t < e.length; t++) {
                            const o = e.get(t);
                            if (!!o && !o.isNull() && !playerIsLocal(o)) {
                              o.method("RPC_ShakeScreen").invoke(0.25, 0.1, 0.1, 8, 1);
                            }
                          }
                        } catch (e) {}
                      }
                    },
                    isTogglable: true,
                    toolTip: "solarhp"
                  }), new ButtonInfo({
                    buttonText: "Jelly All",
                    enableMethod: () => {
                      jellyAllEnabled = true;
                      sendNotification("Jelly ON", false);
                    },
                    disableMethod: () => {
                      jellyAllEnabled = false;
                      sendNotification("Jelly OFF", false);
                    },
                    method: () => {
                      if (jellyAllEnabled && frameCount % 10 == 0) {
                        try {
                          const e = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
                          for (let t = 0; t < e.length; t++) {
                            const o = e.get(t);
                            if (!!o && !o.isNull() && !playerIsLocal(o)) {
                              o.method("RPC_SetJellyEffect").invoke(2, 2);
                            }
                          }
                        } catch (e) {}
                      }
                    },
                    isTogglable: true,
                    toolTip: "solarhp"
                  }), new ButtonInfo({
                    buttonText: "Radioactive All",
                    enableMethod: () => {
                      radioactiveAllEnabled = true;
                      sendNotification("Radioactive ON", false);
                    },
                    disableMethod: () => {
                      radioactiveAllEnabled = false;
                      sendNotification("Radioactive OFF", false);
                    },
                    method: () => {
                      if (radioactiveAllEnabled && frameCount % 10 == 0) {
                        try {
                          const e = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
                          for (let t = 0; t < e.length; t++) {
                            const o = e.get(t);
                            if (!!o && !o.isNull() && !playerIsLocal(o)) {
                              o.method("RPC_SetRadioActive").invoke(5, true, 999);
                            }
                          }
                        } catch (e) {}
                      }
                    },
                    isTogglable: true,
                    toolTip: "solarhp"
                  }), new ButtonInfo({
                    buttonText: "Remove All Gravity",
                    method: () => {
                      try {
                        const e = NetPlayer.field("playerIDToNetPlayer").value;
                        const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                        let o = 0;
                        while (t.method("MoveNext").invoke()) {
                          const e = t.method("get_Current").invoke();
                          if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                            try {
                              e.method("RPC_ApplyBuff").invoke(1);
                              o++;
                            } catch (e) {}
                          }
                        }
                        sendNotification("Big buff sent to " + o + " players!", false);
                      } catch (e) {
                        sendNotification("Buff All Big: " + e, false);
                      }
                    },
                    isTogglable: false,
                    toolTip: "solarhp"
                  }), new ButtonInfo({
                    buttonText: "Muffle All Voices",
                    enableMethod: () => {
                      muffleAllEnabled = true;
                      sendNotification("Muffled Voices ON", false);
                    },
                    disableMethod: () => {
                      muffleAllEnabled = false;
                      sendNotification("Muffled Voices OFF", false);
                    },
                    method: () => {
                      if (muffleAllEnabled && frameCount % 10 == 0) {
                        try {
                          const e = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
                          for (let t = 0; t < e.length; t++) {
                            const o = e.get(t);
                            if (!!o && !o.isNull() && !playerIsLocal(o)) {
                              o.method("RPC_SetMuffledVoiceEnabled").invoke(true);
                            }
                          }
                        } catch (e) {}
                      }
                    },
                    isTogglable: true,
                    toolTip: "solarhp"
                  }), new ButtonInfo({
                    buttonText: "Shake Gun (Toggle)",
                    method: () => {
                      if (frameCount % 8 == 0) {
                        for (let e = 0; e < shakeTargets.length; e++) {
                          const t = shakeTargets[e];
                          if (t && !t.handle.isNull()) {
                            try {
                              t.method("RPC_ShakeScreen").invoke(0.25, 0.1, 0.1, 8, 1);
                            } catch (e) {}
                          }
                        }
                      }
                      if (!rightGrab) {
                        return;
                      }
                      const e = renderGun().ray;
                      if (rightTrigger && !(time < effectGunDelay) && e && !e.isNull()) {
                        try {
                          const t = e.method("get_collider").invoke();
                          if (!t || t.isNull()) {
                            return;
                          }
                          const o = getComponentInParent(t, NetPlayer);
                          if (o && !o.handle.isNull() && !playerIsLocal(o)) {
                            effectGunDelay = time + 0.25;
                            sendNotification(toggleTarget(shakeTargets, o) ? "Shake ON" : "Shake OFF", false);
                          }
                        } catch (e) {}
                      }
                    },
                    isTogglable: true,
                    toolTip: "Shoot a player to toggle screen shake on/off. Stays until you shoot them again."
                  }), new ButtonInfo({
                    buttonText: "Jelly Gun (Toggle)",
                    method: () => {
                      if (frameCount % 8 == 0) {
                        for (let e = 0; e < jellyTargets.length; e++) {
                          const t = jellyTargets[e];
                          if (t && !t.handle.isNull()) {
                            try {
                              t.method("RPC_SetJellyEffect").invoke(2, 2);
                            } catch (e) {}
                          }
                        }
                      }
                      if (!rightGrab) {
                        return;
                      }
                      const e = renderGun().ray;
                      if (rightTrigger && !(time < effectGunDelay) && e && !e.isNull()) {
                        try {
                          const t = e.method("get_collider").invoke();
                          if (!t || t.isNull()) {
                            return;
                          }
                          const o = getComponentInParent(t, NetPlayer);
                          if (o && !o.handle.isNull() && !playerIsLocal(o)) {
                            effectGunDelay = time + 0.25;
                            sendNotification(toggleTarget(jellyTargets, o) ? "Jelly ON" : "Jelly OFF", false);
                          }
                        } catch (e) {}
                      }
                    },
                    isTogglable: true,
                    toolTip: "Shoot a player to toggle jelly effect. Stays until you shoot them again."
                  }), new ButtonInfo({
                    buttonText: "Radioactive Gun (Toggle)",
                    method: () => {
                      if (frameCount % 8 == 0) {
                        for (let e = 0; e < radioactiveTargets.length; e++) {
                          const t = radioactiveTargets[e];
                          if (t && !t.handle.isNull()) {
                            try {
                              t.method("RPC_SetRadioActive").invoke(5, true, 999);
                            } catch (e) {}
                          }
                        }
                      }
                      if (!rightGrab) {
                        return;
                      }
                      const e = renderGun().ray;
                      if (rightTrigger && !(time < effectGunDelay) && e && !e.isNull()) {
                        try {
                          const t = e.method("get_collider").invoke();
                          if (!t || t.isNull()) {
                            return;
                          }
                          const o = getComponentInParent(t, NetPlayer);
                          if (o && !o.handle.isNull() && !playerIsLocal(o)) {
                            effectGunDelay = time + 0.25;
                            sendNotification(toggleTarget(radioactiveTargets, o) ? "Radioactive ON" : "Radioactive OFF", false);
                          }
                        } catch (e) {}
                      }
                    },
                    isTogglable: true,
                    toolTip: "Shoot a player to toggle radioactive. Stays until you shoot them again."
                  }), new ButtonInfo({
                    buttonText: "Muffle Gun (Toggle)",
                    method: () => {
                      if (frameCount % 8 == 0) {
                        for (let e = 0; e < muffleTargets.length; e++) {
                          const t = muffleTargets[e];
                          if (t && !t.handle.isNull()) {
                            try {
                              t.method("RPC_SetMuffledVoiceEnabled").invoke(true);
                            } catch (e) {}
                          }
                        }
                      }
                      if (!rightGrab) {
                        return;
                      }
                      const e = renderGun().ray;
                      if (rightTrigger && !(time < effectGunDelay) && e && !e.isNull()) {
                        try {
                          const t = e.method("get_collider").invoke();
                          if (!t || t.isNull()) {
                            return;
                          }
                          const o = getComponentInParent(t, NetPlayer);
                          if (o && !o.handle.isNull() && !playerIsLocal(o)) {
                            effectGunDelay = time + 0.25;
                            const e = toggleTarget(muffleTargets, o);
                            if (!e) {
                              try {
                                o.method("RPC_SetMuffledVoiceEnabled").invoke(false);
                              } catch (e) {}
                            }
                            sendNotification(e ? "Muffle ON" : "Muffle OFF", false);
                          }
                        } catch (e) {}
                      }
                    },
                    isTogglable: true,
                    toolTip: "Shoot a player to toggle muffled voice. Stays until you shoot them again."
                  }), new ButtonInfo({
                    buttonText: "Gravity Gun (Toggle)",
                    method: () => {
                      if (!rightGrab) {
                        return;
                      }
                      const e = renderGun().ray;
                      if (rightTrigger && !(time < effectGunDelay) && e && !e.isNull()) {
                        try {
                          const t = e.method("get_collider").invoke();
                          if (!t || t.isNull()) {
                            return;
                          }
                          const o = getComponentInParent(t, NetPlayer);
                          if (o && !o.handle.isNull() && !playerIsLocal(o)) {
                            effectGunDelay = time + 0.25;
                            const e = toggleTarget(gravityTargets, o);
                            if (e) {
                              try {
                                o.method("RPC_ApplyBuff").invoke(1);
                              } catch (e) {}
                            }
                            sendNotification(e ? "No-Gravity ON" : "No-Gravity OFF", false);
                          }
                        } catch (e) {}
                      }
                    },
                    isTogglable: true,
                    toolTip: "Shoot a player to toggle no-gravity. (Buff is applied when turned ON)"
                  }), new ButtonInfo({
                    buttonText: "Ban from code gun [NW]",
                    enableMethod: () => {
                      banGunEnabled = true;
                    },
                    disableMethod: () => {
                      banGunEnabled = false;
                    },
                    method: () => {
                      if (!banGunEnabled) {
                        return;
                      }
                      if (!rightGrab) {
                        if (GunPointer) {
                          GunPointer.method("SetActive").invoke(false);
                        }
                        if (GunLine) {
                          GunLine.method("get_gameObject").invoke().method("SetActive").invoke(false);
                        }
                        return;
                      }
                      const e = renderGun();
                      if (rightTrigger && e.ray && !e.ray.isNull()) {
                        try {
                          const t = e.endPosition;
                          const o = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
                          let n = null;
                          let i = 5;
                          for (let e = 0; e < o.length; e++) {
                            const a = o.get(e);
                            if (!a || a.isNull() || playerIsLocal(a)) {
                              continue;
                            }
                            const l = getTransform(a).method("get_position").invoke();
                            const r = Vector3.method("Distance").invoke(t, l);
                            if (r < i) {
                              i = r;
                              n = a;
                            }
                          }
                          if (n && !n.isNull()) {
                            let e = false;
                            try {
                              e = kickPlayer(n);
                            } catch (e) {
                              console.error("Kick player error:", e);
                            }
                            sendNotification(e ? "Kicked player!" : "Kick failed", !e);
                          }
                        } catch (e) {
                          console.error("Kick Gun error:", e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "solarhp"
                  }), new ButtonInfo({
                    buttonText: "Squeaky All Voices",
                    enableMethod: () => {
                      squeakyAllEnabled = true;
                      sendNotification("Squeaky Voices ON", false);
                    },
                    disableMethod: () => {
                      squeakyAllEnabled = false;
                      sendNotification("Squeaky Voices OFF", false);
                    },
                    method: () => {
                      if (squeakyAllEnabled && frameCount % 10 == 0) {
                        try {
                          const e = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
                          for (let t = 0; t < e.length; t++) {
                            const o = e.get(t);
                            if (!!o && !o.isNull() && !playerIsLocal(o)) {
                              o.method("RPC_SetSqueakyVoiceEnabled").invoke(true);
                            }
                          }
                        } catch (e) {}
                      }
                    },
                    isTogglable: true,
                    toolTip: "solarhp"
                  }), new ButtonInfo({
                    buttonText: "Gay Others",
                    method: () => {
                      try {
                        const e = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
                        let t = 0;
                        for (let o = 0; o < e.length; o++) {
                          const n = e.get(o);
                          if (n && !n.isNull() && !playerIsLocal(n)) {
                            const e = Math.random();
                            n.method("RPC_SetColorHSV").invoke(5, e, 1, 1);
                            t++;
                          }
                        }
                        sendNotification("Rainbowed " + t + " players!", false);
                      } catch (e) {}
                    },
                    isTogglable: false,
                    toolTip: "Changes all other players to random colors."
                  }), new ButtonInfo({
                    buttonText: "Spam Gay Others",
                    enableMethod: () => {
                      spamRainbowAllEnabled = true;
                      sendNotification("Spam Rainbow ON - All players flashing!", false);
                    },
                    disableMethod: () => {
                      spamRainbowAllEnabled = false;
                      sendNotification("Spam Rainbow OFF", false);
                    },
                    method: () => {
                      if (spamRainbowAllEnabled && frameCount % 10 == 0) {
                        try {
                          const e = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
                          for (let t = 0; t < e.length; t++) {
                            const o = e.get(t);
                            if (o && !o.isNull() && !playerIsLocal(o)) {
                              const e = Math.random();
                              o.method("RPC_SetColorHSV").invoke(0.2, e, 1, 1);
                            }
                          }
                        } catch (e) {}
                      }
                    },
                    isTogglable: true,
                    toolTip: "Continuously flashes random colors on all other players."
                  }), new ButtonInfo({
                    buttonText: "Kick gun [fixed]",
                    enableMethod: () => {
                      kickGunEnabled = false;
                      flingGunEnabled = false;
                      teleportGunEnabled = false;
                      colorGunEnabled = false;
                      spazColorGunEnabled = false;
                      prefabGunEnabled = false;
                      spawnGunEnabled = false;
                      telekinesisGunEnabled = false;
                      goopfishGunEnabled = true;
                    },
                    disableMethod: () => {
                      goopfishGunEnabled = false;
                    },
                    method: () => {
                      if (!goopfishGunEnabled) {
                        return;
                      }
                      if (!rightGrab) {
                        if (GunPointer) {
                          GunPointer.method("SetActive").invoke(false);
                        }
                        if (GunLine) {
                          GunLine.method("get_gameObject").invoke().method("SetActive").invoke(false);
                        }
                        return;
                      }
                      const e = renderGun();
                      if (e && rightTrigger && e.ray && !e.ray.isNull()) {
                        try {
                          const t = e.endPosition;
                          const o = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
                          let n = null;
                          let i = 5;
                          for (let e = 0; e < o.length; e++) {
                            const a = o.get(e);
                            if (!a || a.isNull() || playerIsLocal(a)) {
                              continue;
                            }
                            const l = getTransform(a).method("get_position").invoke();
                            const r = Vector3.method("Distance").invoke(t, l);
                            if (r < i) {
                              i = r;
                              n = a;
                            }
                          }
                          if (n && !n.isNull()) {
                            let e = false;
                            for (let t = 0; t < 5; t++) {
                              try {
                                kickPlayer(n);
                                e = true;
                                break;
                              } catch (e) {
                                console.log(`Kick attempt ${t + 1}/5 failed: ${e}`);
                                for (let e = 0; e < 800000; e++);
                              }
                            }
                            if (!e) {
                              try {
                                const e = n.method("get_NetworkPlayer").invoke();
                                if (e && !e.isNull()) {
                                  e.method("Disconnect").invoke();
                                }
                              } catch (e) {}
                            }
                            sendNotification(`KICKED (retries: ${e ? "success" : "fallback"}) + goopfish!`, false);
                            const t = getTransform(n).method("get_position").invoke();
                            const o = 12 + Math.floor(Math.random() * 8);
                            for (let e = 0; e < o; e++) {
                              PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string("item_goopfish"), t, identityQuaternion, NULL, 0);
                            }
                          } else {
                            const e = t;
                            const o = 6 + Math.floor(Math.random() * 4);
                            for (let t = 0; t < o; t++) {
                              PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string("item_goopfish"), e, identityQuaternion, NULL, 0);
                            }
                            sendNotification(`Spawned ${o} goopfish!`, false);
                          }
                        } catch (e) {
                          console.log("Kick + Goopfish Gun CRITICAL error: " + e);
                          sendNotification("Gun error - check console", true);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "tuff"
                  }), new ButtonInfo({
                    buttonText: "Kick All",
                    method: () => {
                      const e = Object.method("FindObjectsOfType").inflate(NetPlayer).invoke();
                      if (e) {
                        for (let t = 0; t < e.length; t++) {
                          const o = e.get(t);
                          if (!playerIsLocal(o)) {
                            kickPlayer(o);
                          }
                        }
                      }
                    },
                    isTogglable: false,
                    toolTip: "Lags whoever your hand desires. May be broken."
                  }), new ButtonInfo({
                    buttonText: "Auto Kick All",
                    method: () => {
                      if ((spamKickTimer -= deltaTime) <= 0) {
                        kickAllPlayers();
                        spamKickTimer = 0.2;
                      }
                    },
                    isTogglable: true,
                    toolTip: "Toggles infinite spam kicks on all players. Enable to start, disable to stop."
                  }), new ButtonInfo({
                    buttonText: "Void Gun",
                    method: () => {
                      if (!rightGrab) {
                        return;
                      }
                      const e = renderGun();
                      if (rightTrigger && !(time <= kickGunDelay)) {
                        kickGunDelay = time + 0.45;
                        try {
                          const t = resolveGunTargetPlayer(e, 10);
                          if (!t || t.isNull?.()) {
                            sendNotification("No player there", false);
                            return;
                          }
                          if (playerIsLocal(t)) {
                            return;
                          }
                          const o = getPlayerName(t);
                          if (tryKickPlayer(t)) {
                            sendNotification("Kick sent to " + o, false);
                          } else {
                            try {
                              t.method("RPC_DoPlayerDie").invoke(true);
                            } catch (e) {}
                            try {
                              t.method("RPC_Teleport").invoke([0, -9999999, 0]);
                            } catch (e) {}
                            try {
                              const e = t.method("get_gameObject").invoke();
                              if (e && !e.isNull?.()) {
                                try {
                                  const t = e.method("GetComponentsInChildren", 1).inflate(Renderer).invoke(true);
                                  if (t && !t.isNull?.()) {
                                    for (let e = 0; e < t.length; e++) {
                                      try {
                                        t.get(e).method("set_enabled").invoke(false);
                                      } catch (e) {}
                                    }
                                  }
                                } catch (e) {}
                                try {
                                  const t = e.method("GetComponentsInChildren", 1).inflate(Collider).invoke(true);
                                  if (t && !t.isNull?.()) {
                                    for (let e = 0; e < t.length; e++) {
                                      try {
                                        t.get(e).method("set_enabled").invoke(false);
                                      } catch (e) {}
                                    }
                                  }
                                } catch (e) {}
                                try {
                                  e.method("SetActive").invoke(false);
                                } catch (e) {}
                              }
                            } catch (e) {}
                            console.error("[Kick Gun] moderation failed, hid target instead", o, getPlayerKickTokens(t));
                            sendNotification("Kick hide on " + o, false);
                          }
                        } catch (e) {
                          console.error("Kick Gun:", e);
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "Best-effort private-room kick gun using the game's moderation methods (hold grip + trigger)."
                  }), new ButtonInfo({
                    buttonText: "Kidnap Gun",
                    method: () => {
                      if (heldPlayer && !heldPlayer.isNull?.()) {
                        const e = rightHandTransform.method("get_position").invoke();
                        const t = [e.field("x").value, e.field("y").value - 1.25, e.field("z").value];
                        try {
                          heldPlayer.method("RPC_Teleport").invoke(t);
                        } catch (e) {}
                      }
                      if (rightSecondary) {
                        heldPlayer = null;
                        sendNotification("Released player", false);
                      }
                      if (!rightGrab) {
                        return;
                      }
                      const e = renderGun();
                      if (rightTrigger && !heldPlayer) {
                        const t = resolveGunTargetPlayer(e, 10);
                        if (!t || t.isNull?.()) {
                          return;
                        }
                        if (playerIsLocal(t)) {
                          return;
                        }
                        heldPlayer = t;
                        sendNotification("Grabbed " + getPlayerName(t), false);
                      }
                    },
                    isTogglable: true,
                    toolTip: "Trigger to grab a player, B to release. Player sticks to your right hand."
                  }), new ButtonInfo({
                    buttonText: "Push Everyone (gun)",
                    isTogglable: true,
                    method: () => {
                      if (!rightGrab) {
                        return;
                      }
                      if (renderGun() && rightTrigger && !(time <= pushEveryoneGunDelay)) {
                        pushEveryoneGunDelay = time + 0.15;
                        try {
                          const e = getGunAimDirection();
                          const t = Vector3.method("op_Multiply", 2).invoke(e, 5000);
                          for (const e of getAllNetPlayersList(false)) {
                            try {
                              if (!e || e.handle.isNull()) {
                                continue;
                              }
                              e.method("RPC_AddForce").invoke(t);
                            } catch (e) {}
                          }
                          sendNotification("Pushed everyone back with the gun!", false);
                        } catch (e) {
                          console.error("Push Everyone Gun error:", e);
                        }
                      }
                    },
                    toolTip: "Hold grip and pull trigger to push all other players back very far."
                  }), new ButtonInfo({
                    buttonText: "Insta Kill Gun",
                    isTogglable: true,
                    method: () => {
                      if (!rightGrab) {
                        return;
                      }
                      const e = renderGun().ray;
                      if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                        lagGunDelay = time + 0.25;
                        try {
                          const t = e.method("get_collider").invoke();
                          if (!t || t.isNull()) {
                            return;
                          }
                          const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                          if (!o || o.isNull()) {
                            sendNotification("No player there", false);
                            return;
                          }
                          if (playerIsLocal(o)) {
                            return;
                          }
                          const n = getTransform(o).method("get_position").invoke();
                          const i = DamageSourceInfoClass.method("get_Null").invoke();
                          o.method("RPC_PlayerHit", 3).invoke(9999, n, i);
                          sendNotification("Insta hit 9999: " + getPlayerName(o), false);
                        } catch (e) {
                          console.error("Insta Kill Gun:", e);
                        }
                      }
                    },
                    toolTip: "Point at any player and pull trigger to deal 9999 damage."
                  }), new ButtonInfo({
                    buttonText: "Insta Kill All",
                    method: () => {
                      try {
                        const e = DamageSourceInfoClass.method("get_Null").invoke();
                        let t = 0;
                        for (const o of getAllNetPlayersList(false)) {
                          try {
                            if (!o || o.handle.isNull()) {
                              continue;
                            }
                            const n = getTransform(o).method("get_position").invoke();
                            o.method("RPC_PlayerHit", 3).invoke(9999, n, e);
                            t++;
                          } catch (e) {}
                        }
                        sendNotification("Insta killed all: " + t, false);
                      } catch (e) {
                        console.error("Insta Kill All:", e);
                      }
                    },
                    isTogglable: false,
                    toolTip: "One click deals 9999 damage to every other player."
                  }), new ButtonInfo({
                    buttonText: "Fling gun [W?]",
                    isTogglable: true,
                    method: () => {
                      if (!rightGrab) {
                        return;
                      }
                      const e = renderGun();
                      if (rightTrigger && e.ray) {
                        try {
                          const t = e.ray.method("get_collider").invoke();
                          const o = [0, 500, 0];
                          const n = getComponentInParent(t, NetPlayer);
                          if (n && !n.isNull()) {
                            n.method("RPC_AddForce").invoke(o);
                            sendNotification("Yeeted Player!", false);
                            return;
                          }
                          const i = getComponentInParent(t, GBOClass);
                          if (i && !i.isNull()) {
                            i.method("RPC_AddExternalForce").invoke(o, 2);
                            sendNotification("Yeeted Item!", false);
                          }
                        } catch (e) {}
                      }
                    },
                    toolTip: "Shoots a massive upward force via RPC. Works on players and items."
                  }), new ButtonInfo({
                    buttonText: "Launch All Down",
                    method: () => {
                      try {
                        const e = NetPlayer.field("playerIDToNetPlayer").value;
                        const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                        let o = 0;
                        while (t.method("MoveNext").invoke()) {
                          const e = t.method("get_Current").invoke();
                          if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                            try {
                              e.method("RPC_AddForce").invoke([0, -1500, 0]);
                              o++;
                            } catch (e) {}
                          }
                        }
                        sendNotification("Slammed " + o + " players down!", false);
                      } catch (e) {
                        sendNotification("Launch Down: " + e, false);
                      }
                    },
                    isTogglable: false,
                    toolTip: "Slams all other players downward."
                  }), new ButtonInfo({
                    buttonText: "Hit All 50",
                    method: () => {
                      try {
                        const e = DamageSourceInfoClass.method("get_Null").invoke();
                        let n = 0;
                        for (const t of getAllNetPlayersList(false)) {
                          try {
                            if (!t || t.handle.isNull()) {
                              continue;
                            }
                            const o = getTransform(t).method("get_position").invoke();
                            t.method("RPC_PlayerHit", 3).invoke(50, o, e);
                            n++;
                          } catch (e) {}
                        }
                        sendNotification("Hit " + n + " players for 50dmg!", false);
                      } catch (e) {
                        sendNotification("Hit All: " + e, false);
                      }
                    },
                    isTogglable: false,
                    toolTip: "Deals 50 damage to all other players."
                  }), new ButtonInfo({
                    buttonText: "Hit All 100",
                    method: () => {
                      try {
                        const e = DamageSourceInfoClass.method("get_Null").invoke();
                        let n = 0;
                        for (const t of getAllNetPlayersList(false)) {
                          try {
                            if (!t || t.handle.isNull()) {
                              continue;
                            }
                            const o = getTransform(t).method("get_position").invoke();
                            t.method("RPC_PlayerHit", 3).invoke(101, o, e);
                            n++;
                          } catch (e) {}
                        }
                        sendNotification("Hit " + n + " players for 100dmg!", false);
                      } catch (e) {
                        sendNotification("Hit All: " + e, false);
                      }
                    },
                    isTogglable: false,
                    toolTip: "Deals 100 damage to all other players."
                  }), new ButtonInfo({
                    buttonText: "Bounce All",
                    method: () => {
                      try {
                        const e = NetPlayer.field("playerIDToNetPlayer").value;
                        const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                        let o = 0;
                        while (t.method("MoveNext").invoke()) {
                          const e = t.method("get_Current").invoke();
                          if (!e || e.handle.isNull() || playerIsLocal(e)) {
                            continue;
                          }
                          const n = (Math.random() - 0.5) * 2000;
                          const i = (Math.random() - 0.5) * 2000;
                          try {
                            e.method("RPC_AddForce").invoke([n, 1200, i]);
                            o++;
                          } catch (e) {}
                        }
                        sendNotification("Bounced " + o + " players!", false);
                      } catch (e) {
                        sendNotification("Bounce All: " + e, false);
                      }
                    },
                    isTogglable: false,
                    toolTip: "Launches all other players in random directions."
                  }), new ButtonInfo({
                    buttonText: "Grab Player",
                    isTogglable: true,
                    method: (() => {
                      let e = null;
                      let t = "rightHandTransform";
                      return () => {
                        try {
                          if (!rightGrab) {
                            e = null;
                            return;
                          }
                          if (!e || e.isNull?.()) {
                            e = null;
                            const o = safeField(getInstance(), "rightHandTransform");
                            const n = safeField(getInstance(), "leftHandTransform");
                            if (!o || !n) {
                              return;
                            }
                            const i = o.method("get_position").invoke();
                            const a = n.method("get_position").invoke();
                            const l = Object.method("FindObjectsOfType").inflate(NetPlayer).invoke();
                            if (!l) {
                              return;
                            }
                            let r = 1.5;
                            for (let o = 0; o < l.length; o++) {
                              const n = l.get(o);
                              if (!n || n.handle.isNull() || playerIsLocal(n)) {
                                continue;
                              }
                              const s = getTransform(n).method("get_position").invoke();
                              const c = Vector3.method("Distance").invoke(i, s);
                              const d = Vector3.method("Distance").invoke(a, s);
                              const u = Math.min(c, d);
                              if (u < r) {
                                r = u;
                                e = n;
                                t = c < d ? "rightHandTransform" : "leftHandTransform";
                              }
                            }
                            if (!e) {
                              return;
                            }
                            sendNotification("Grabbed player!", false);
                          }
                          const o = safeField(getInstance(), t);
                          if (!o || o.isNull?.()) {
                            return;
                          }
                          const n = o.method("get_position").invoke();
                          let i = n;
                          try {
                            const e = getTransform(safeField(getInstance(), "bodyCollider")).method("get_position").invoke().field("y").value;
                            const t = e - getTransform(getGTPlayer()).method("get_position").invoke().field("y").value;
                            const o = n.field("x").value;
                            const a = n.field("y").value;
                            i = [o, a - t, n.field("z").value];
                          } catch (e) {}
                          e.method("RPC_Teleport").invoke(i);
                        } catch (e) {
                          console.error("Grab Player:", e);
                        }
                      };
                    })(),
                    disableMethod: () => {},
                    toolTip: "Hold right grip + right trigger to grab the nearest player to your hand."
                  }), new ButtonInfo({
                    buttonText: "Money All +10M",
                    method: () => {
                      try {
                        const e = Object.method("FindObjectsOfType").inflate(NetPlayer).invoke();
                        if (!e) {
                          return;
                        }
                        let t = 0;
                        for (let o = 0; o < e.length; o++) {
                          const n = e.get(o);
                          if (n && !n.handle.isNull() && !playerIsLocal(n)) {
                            try {
                              n.method("RPC_AddPlayerMoney").invoke(10000000);
                              t++;
                            } catch (e) {}
                          }
                        }
                        sendNotification("Gave 10M to " + t + " players!", false);
                      } catch (e) {
                        sendNotification("Money All: " + e, false);
                      }
                    },
                    isTogglable: false,
                    toolTip: "Gives 10,000,000 money to all other players."
                  }), new ButtonInfo({
                    buttonText: "Drain Money All",
                    method: () => {
                      try {
                        const e = Object.method("FindObjectsOfType").inflate(NetPlayer).invoke();
                        if (!e) {
                          return;
                        }
                        let t = 0;
                        for (let o = 0; o < e.length; o++) {
                          const n = e.get(o);
                          if (n && !n.handle.isNull() && !playerIsLocal(n)) {
                            try {
                              n.method("RPC_AddPlayerMoney").invoke(-10000000);
                              t++;
                            } catch (e) {}
                          }
                        }
                        sendNotification("Drained money from " + t + " players!", false);
                      } catch (e) {
                        sendNotification("Money Drain: " + e, false);
                      }
                    },
                    isTogglable: false,
                    toolTip: "Removes 10,000,000 money from all other players."
                  }), new ButtonInfo({
                    buttonText: "Stinky Gun",
                    method: () => {
                      if (rightGrab) {
                        const e = renderGun().ray;
                        if (rightTrigger && e) {
                          const t = getComponentInParent(e.method("get_collider").invoke(), NetPlayer);
                          if (t && !t.handle.isNull() && time > tagGunDelay) {
                            if (!playerIsLocal(t) && (!whitelistEnabled || !whitelistHasPlayer(t))) {
                              t.method("RPC_TagAsStinky").invoke();
                            }
                          }
                        }
                      }
                    },
                    isTogglable: true,
                    toolTip: "Stinkies whoever your hand desires."
                  }), new ButtonInfo({
                    buttonText: "Stinky All",
                    isTogglable: false,
                    method: () => {
                      const e = Object.method("FindObjectsOfType").inflate(NetPlayer).invoke();
                      if (!e) {
                        return null;
                      }
                      for (let t = 0; t < e.length; t++) {
                        const o = e.get(t);
                        if (!o.method("get_IsMine").invoke()) {
                          o.method("RPC_TagAsStinky").invoke();
                        }
                      }
                    },
                    toolTip: "Stinkies everyone"
                  })];
                  const p = ["Settings", "Name", "Movement mods", "Player mods", "Item mods", "Spawning", "Text Spawn", "Gun mods", "Stuff", "RPC Stuff", "Test", "Whitelist mods", "Monster RPCs", "Credits", "Over Powered"];
                  m.sort((e, t) => {
                    const o = p.indexOf(e.buttonText);
                    const n = p.indexOf(t.buttonText);
                    return (o < 0 ? 999 : o) - (n < 0 ? 999 : n);
                  });
                  c(buttons[9] ?? [], "Exit Misc", "Exit Over Powered");
                  d(buttons[12] ?? [], "Prefab Orbit -> Misc");
                  const y = buttons[5] ?? [];
                  const b = buttons[16] ?? [];
                  if (y.length > 0 && b.length > 1 && !y.some(e => e.buttonText === "WL All Speed Loop")) {
                    y.push(...b.slice(1));
                  }
                  if (b.length > 0) {
                    b.splice(0, b.length, new ButtonInfo({
                      buttonText: "Exit WL",
                      method: () => {
                        currentCategory = 5;
                        currentPage = 0;
                      },
                      isTogglable: false,
                      toolTip: "Returns to the main whitelist page."
                    }));
                  }
                } catch (e) {}
                menuStructurePatched = true;
                try {
                  rebuildButtonMap();
                } catch (e) {}
              }
            }
            function dumpMenuToTextFile() {
              const e = "C:\\Program Files (x86)\\Steam\\steamapps\\common\\Animal Company\\SEModdingMenuDump.txt";
              try {
                applyMenuStructurePatches();
                const t = [];
                t.push("SOLARHP MENU DUMP");
                t.push("version=" + version);
                t.push("themeMode=" + themeMode);
                t.push("time=" + time);
                t.push("");
                t.push("Categories:");
                for (let e = 0; e < buttons.length; e++) {
                  const o = buttons[e] ?? [];
                  t.push("[" + e + "] count=" + o.length);
                  for (let e = 0; e < o.length; e++) {
                    const n = o[e];
                    t.push("  (" + e + ") " + n.buttonText + " | togglable=" + !!n.isTogglable + " | enabled=" + !!n.enabled);
                  }
                }
                t.push("");
                t.push("Item IDs (" + itemIDs.length + "):");
                for (const e of itemIDs) {
                  t.push("  " + e);
                }
                t.push("");
                t.push("Prefab IDs (" + prefabIDs.length + "):");
                for (const e of prefabIDs) {
                  t.push("  " + e);
                }
                t.push("");
                t.push("Mob IDs (" + mobIDs.length + "):");
                for (const e of mobIDs) {
                  t.push("  " + e.name + " | id=" + e.id);
                }
                t.push("");
                t.push("VFX IDs (" + vfxTypeEntries.length + "):");
                for (const [e, o] of vfxTypeEntries) {
                  t.push("  " + e + " = " + o);
                }
                t.push("");
                t.push("Assemblies / Classes / Methods:");
                try {
                  const e = Il2Cpp.domain.assemblies ?? [];
                  for (const o of e) {
                    try {
                      const e = o.image;
                      const n = String(e?.name ?? o?.name ?? "?");
                      t.push("[Assembly] " + n);
                      for (const o of e?.classes ?? []) {
                        try {
                          const e = String(o?.type?.name ?? o?.name ?? "?");
                          t.push("  [Class] " + e);
                          for (const e of o?.methods ?? []) {
                            try {
                              const o = Number(e?.parameterCount ?? e?.parameters?.length ?? 0);
                              t.push("    - " + String(e?.name ?? "?") + "(" + o + ")");
                            } catch (e) {}
                          }
                        } catch (e) {}
                      }
                    } catch (e) {}
                  }
                } catch (e) {}
                const o = new (0, globalThis.File)(e, "w");
                for (const e of t) {
                  o.write(e + "\n");
                }
                o.flush();
                o.close();
                console.log("[DumpMenuTXT] wrote", e);
                return e;
              } catch (e) {
                console.error("[DumpMenuTXT]", e);
                return null;
              }
            }
            function forceGrantAllStashSlots() {
              const e = e => {
                try {
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  for (const t of ["_unlockedStashSlots", "unlockedStashSlots", "_stashSlots", "stashSlots", "_maxStashSlots", "maxStashSlots", "_stashSlotCount", "stashSlotCount", "_stashRowsUnlocked", "stashRowsUnlocked", "_stashColsUnlocked", "stashColsUnlocked"]) {
                    try {
                      e.field(t).value = 999;
                    } catch (e) {}
                  }
                  tryCallNames(e, ["UnlockAllStashSlots", "GrantAllStashSlots", "RefreshStashSlots", "SetUnlockedStashSlots", "SetStashSlots"], 0);
                  tryCallNames(e, ["SetUnlockedStashSlots", "SetStashSlots", "GrantStashSlots"], 1, 999);
                  tryCallNames(e, ["set_stashRowsUnlocked", "set_stashColsUnlocked"], 1, 99);
                } catch (e) {}
              };
              try {
                const t = AssemblyCSharp.class("AnimalCompany.App").method("get_state").invoke();
                if (t && !t.isNull?.()) {
                  e(t);
                  try {
                    e(t.method("get_user").invoke());
                  } catch (e) {}
                  try {
                    e(t.method("get_mapMachine").invoke());
                  } catch (e) {}
                  try {
                    e(t.method("get_stash").invoke());
                  } catch (e) {}
                }
              } catch (e) {}
              try {
                const t = AssemblyCSharp.class("AnimalCompany.UserStashState");
                const o = Object.method("FindObjectsByType", 1).inflate(t).invoke(0);
                if (o && !o.isNull?.()) {
                  for (let t = 0; t < o.length; t++) {
                    try {
                      e(o.get(t));
                    } catch (e) {}
                  }
                }
              } catch (e) {}
              try {
                const t = AssemblyCSharp.class("AnimalCompany.StashMachine.StashMachineStashTabUIView");
                const o = Object.method("FindObjectsByType", 1).inflate(t).invoke(0);
                if (o && !o.isNull?.()) {
                  for (let t = 0; t < o.length; t++) {
                    try {
                      const n = o.get(t);
                      e(n);
                      tryCallNames(n, ["InitializeCommand", "Refresh", "RefreshSlots", "UpdateSlots", "Reload"], 0);
                    } catch (e) {}
                  }
                }
              } catch (e) {}
              try {
                e(NetPlayer.method("get_localPlayer").invoke());
              } catch (e) {}
            }
            function applyContainerFreedomSweep() {
              if (!(time <= containerFreedomSweepDelay)) {
                containerFreedomSweepDelay = time + 0.45;
                try {
                  for (const e of getAllLobbyItemGameObjects()) {
                    try {
                      const t = e.method("GetComponent", 1).inflate(GBIClass).invoke();
                      if (!t || t.isNull?.()) {
                        continue;
                      }
                      try {
                        t.method("set_allowAddToBag").invoke(true);
                      } catch (e) {}
                      try {
                        t.field("_allowAddToBag").value = true;
                      } catch (e) {}
                      try {
                        const e = t.method("get_itemData").invoke();
                        if (e && !e.isNull?.()) {
                          const t = e.method("get_flags").invoke();
                          if (t && !t.isNull?.()) {
                            try {
                              t.method("set_value").invoke(8190);
                            } catch (e) {}
                            try {
                              t.field("_value").value = 8190;
                            } catch (e) {}
                          }
                        }
                      } catch (e) {}
                    } catch (e) {}
                  }
                } catch (e) {}
              }
            }
            let _selfRPCBypass = false;
            function selfRPC(e) {
              _selfRPCBypass = true;
              try {
                e();
              } finally {
                _selfRPCBypass = false;
              }
            }
            const buttons = [[new ButtonInfo({
              buttonText: "Enter Menu",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "enters the menu."
            }), new ButtonInfo({
              buttonText: "Join the Discord",
              method: () => {
                try {
                  Application.method("OpenURL").invoke(Il2Cpp.string("https://discord.gg/GkrXvhPy92"));
                } catch (e) {
                  console.error("[Discord] OpenURL failed:", e);
                }
              },
              isTogglable: false,
              toolTip: "Opens the SE-Modding Discord invite."
            })], [new ButtonInfo({
              buttonText: "Leave",
              method: () => {
                try {
                  const e = NManager.method("get_instance").invoke();
                  if (!e || e.isNull()) {
                    return;
                  }
                  e.method("TryShutdownAndCancelConnecting").invoke();
                } catch (e) {
                  console.error("[Leave]", e);
                }
              },
              isTogglable: false,
              toolTip: "Leaves the room cleanly."
            }), new ButtonInfo({
              buttonText: "Home",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
                _pcMenuSelector = 0;
              },
              isTogglable: false,
              toolTip: "Returns to the home category."
            }), new ButtonInfo({
              buttonText: "OpenSearch",
              method: () => {
                searchQuery = "";
                currentCategory = 44;
                currentPage = 0;
                _pcMenuSelector = 0;
                sendNotification("Search: _", false, 4);
              },
              isTogglable: false,
              toolTip: "Opens the mod search keyboard."
            }), new ButtonInfo({
              buttonText: "PreviousPage",
              method: () => {
                const e = Math.ceil(buttons[currentCategory].length / 8) - 1;
                if (--currentPage < 0) {
                  currentPage = e;
                }
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "NextPage",
              method: () => {
                const e = Math.ceil(buttons[currentCategory].length / 8) - 1;
                currentPage++;
                currentPage %= e + 1;
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "GlobalReturn",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns you back to the main category."
            })], [new ButtonInfo({
              buttonText: "Exit Settings",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns to the Settings tab."
            }), new ButtonInfo({
              buttonText: "Themes",
              method: () => {
                currentCategory = 12;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens themes."
            }), new ButtonInfo({
              buttonText: "Enabled Mods",
              method: () => {
                currentCategory = 28;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens enabled mods!"
            }), new ButtonInfo({
              buttonText: "Right Hand",
              enableMethod: () => {
                righthand = true;
                sendNotification("Menu: Right Hand", false);
                reloadMenu();
              },
              disableMethod: () => {
                righthand = false;
                sendNotification("Menu: Left Hand", false);
                reloadMenu();
              },
              isTogglable: true,
              toolTip: "Moves the menu to your right hand instead of your left hand."
            }), new ButtonInfo({
              buttonText: "Log App ID",
              method: () => {
                try {
                  const e = OculusPlatformSettings.method("get_MobileAppID").invoke();
                  console.log("[AppID] Mobile App ID: " + e);
                  sendNotification("<color=grey>[</color><color=#8000ff>MENU</color><color=grey>]</color> App ID: " + e, false, 10);
                } catch (e) {
                  console.error("[AppID] Failed to get App ID: " + e);
                }
              },
              isTogglable: false,
              toolTip: "Logs the Meta/Oculus App ID to the console."
            })], [new ButtonInfo({
              buttonText: "Exit Movement",
              method: () => {
                currentCategory = 4;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "Trigger Fly",
              method: () => {
                try {
                  const e = getTransform(GTPlayer);
                  let t = null;
                  try {
                    t = getComponent(e.method("get_gameObject").invoke(), RigidbodyClass);
                    if (t && !t.isNull()) {
                      t.method("set_linearVelocity").invoke(Vector3.field("zeroVector").value);
                      t.method("set_useGravity").invoke(false);
                    }
                  } catch (e) {}
                  if (rightTrigger) {
                    const t = rightHandTransform.method("get_forward").invoke();
                    const o = Vector3.method("op_Multiply", 2).invoke(t, deltaTime * 46.75);
                    let n = e.method("get_position").invoke();
                    n = Vector3.method("op_Addition", 2).invoke(n, o);
                    e.method("set_position").invoke(n);
                  }
                } catch (e) {
                  console.error("Hand Fly error:", e);
                }
              },
              isTogglable: true,
              toolTip: "Lets you fly around with your hand while holding the trigger."
            }), new ButtonInfo({
              buttonText: "Fist Fly",
              method: () => {
                const e = rightGrab && rightTrigger;
                const t = leftGrab && leftTrigger;
                if (e || t) {
                  try {
                    const o = NetPlayer.method("get_localPlayer").invoke();
                    if (!o || o.handle.isNull()) {
                      return;
                    }
                    let n = [0, 0, 0];
                    if (e) {
                      const e = readVec3Components(rightHandTransform.method("get_forward").invoke());
                      n[0] += e[0];
                      n[1] += e[1];
                      n[2] += e[2];
                    }
                    if (t) {
                      const e = readVec3Components(leftHandTransform.method("get_forward").invoke());
                      n[0] += e[0];
                      n[1] += e[1];
                      n[2] += e[2];
                    }
                    const i = Math.sqrt(n[0] * n[0] + n[1] * n[1] + n[2] * n[2]);
                    if (i < 0.01) {
                      return;
                    }
                    const a = 1.5;
                    n = [n[0] / i * a, n[1] / i * a, n[2] / i * a];
                    const l = smoothVec3(fistFlyVelocity, n, 0.16);
                    selfRPC(() => o.method("RPC_AddForce").invoke(l));
                  } catch (e) {
                    console.error("Fly:", e);
                  }
                } else {
                  smoothVec3(fistFlyVelocity, [0, 0, 0], 0.2);
                }
              },
              isTogglable: true,
              toolTip: "Make a fist to fly where you point with slower smoother force."
            }), new ButtonInfo({
              buttonText: "Ghost Monke",
              enableMethod: () => {
                sendNotification("Ghost Monke ON-segura Y", false);
              },
              disableMethod: () => {
                ghostFrozenPos = null;
                try {
                  if (ghostMarker && !ghostMarker.isNull?.()) {
                    Destroy(ghostMarker);
                  }
                } catch (e) {}
                ghostMarker = null;
                sendNotification("Ghost Monke OFF", false);
              },
              isTogglable: true,
              method: () => {
                try {
                  if (!leftSecondary) {
                    if (ghostFrozenPos) {
                      ghostFrozenPos = null;
                      try {
                        if (ghostMarker && !ghostMarker.isNull?.()) {
                          Destroy(ghostMarker);
                        }
                      } catch (e) {}
                      ghostMarker = null;
                    }
                    return;
                  }
                  if (!ghostFrozenPos) {
                    ghostFrozenPos = getTransform(bodyCollider).method("get_position").invoke();
                    try {
                      const e = getComponent(ghostMarker = createObject(ghostFrozenPos, identityQuaternion, [0.35, 0.6, 0.35], 3, [0.4, 0.7, 1, 0.45], null, false), Collider);
                      if (e && !e.isNull?.()) {
                        e.method("set_enabled").invoke(false);
                      }
                    } catch (e) {}
                  }
                  const e = PCClass.method("get_instance").invoke();
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  const t = e.method("get_playerView").invoke();
                  if (!t || t.isNull?.()) {
                    return;
                  }
                  const o = t.method("get_gameObject").invoke();
                  if (o && !o.isNull?.()) {
                    getTransform(o).method("set_position").invoke(ghostFrozenPos);
                  }
                } catch (e) {
                  console.error("Ghost Monke:", e);
                }
              },
              toolTip: "Liga o mod e segura Y(mao esquerda)pra travar seu corpo de rede no lugar."
            }), new ButtonInfo({
              buttonText: "Speed Boost",
              method: () => {
                getGTPlayer()?.method("SetJumpVelocityMultiplier").invoke(2);
              },
              toolTip: "Gives you a speed boost."
            }), new ButtonInfo({
              buttonText: "Joystick Fly",
              isTogglable: true,
              disableMethod: () => {
                joystickFlyVelocity = [0, 0, 0];
                try {
                  const e = getPcPlayerRigidbody();
                  if (e) {
                    e.method("set_useGravity").invoke(true);
                  }
                } catch (e) {}
              },
              method: () => {
                try {
                  const e = getPcPlayerRigidbody();
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  const t = getTransform(headCollider);
                  const o = readVec3Components(t.method("get_forward").invoke());
                  const n = readVec3Components(t.method("get_right").invoke());
                  const [i, a] = normalizeXZ(o[0], o[2]);
                  const [l, r] = normalizeXZ(n[0], n[2]);
                  const s = [rightStickX, rightStickY];
                  const c = leftStickY;
                  const d = s[1];
                  const u = s[0];
                  const m = i * d + l * u;
                  const h = a * d + r * u;
                  const g = c * 0.95;
                  const f = Math.sqrt(m * m + g * g + h * h);
                  if (f < 0.025) {
                    joystickFlyVelocity = smoothVec3(joystickFlyVelocity, [0, 0, 0], 0.32);
                    e.method("set_useGravity").invoke(false);
                    setRigidbodyVelocitySafe(e, joystickFlyVelocity);
                    return;
                  }
                  const p = Math.max(3, flySpeed * 0.45);
                  const y = [m / f * p * Math.min(1.25, f * 1.4), g / f * p * Math.min(1.25, f * 1.4), h / f * p * Math.min(1.25, f * 1.4)];
                  const b = smoothVec3(joystickFlyVelocity, y, 0.22);
                  e.method("set_useGravity").invoke(false);
                  setRigidbodyVelocitySafe(e, b);
                } catch (e) {
                  console.error("JoystickFly:", e);
                }
              },
              toolTip: "Right stick moves you horizontally, left stick Y moves you up and down."
            }), new ButtonInfo({
              buttonText: "Platforms",
              enableMethod: () => {
                Destroy(movementPlatformLeft);
                Destroy(movementPlatformRight);
                movementPlatformLeft = null;
                movementPlatformRight = null;
                sendNotification("Platforms ON", false);
              },
              disableMethod: () => {
                Destroy(movementPlatformLeft);
                Destroy(movementPlatformRight);
                movementPlatformLeft = null;
                movementPlatformRight = null;
                sendNotification("Platforms OFF", false);
              },
              isTogglable: true,
              method: () => {
                try {
                  const e = (e, t, o, n) => {
                    if (!t) {
                      Destroy(n === "left" ? movementPlatformLeft : movementPlatformRight);
                      if (n === "left") {
                        movementPlatformLeft = null;
                      } else {
                        movementPlatformRight = null;
                      }
                      return;
                    }
                    if (!o) {
                      const t = e.method("get_position").invoke();
                      const o = createSolidPlatform([t.field("x").value, t.field("y").value - 0.18, t.field("z").value], [0.24, 0.025, 0.24], [0.95, 0.4, 0.03, 0.92]);
                      if (n === "left") {
                        movementPlatformLeft = o;
                      } else {
                        movementPlatformRight = o;
                      }
                    }
                  };
                  e(leftHandTransform, leftGrab, prevLeftGrab, "left");
                  e(rightHandTransform, rightGrab, prevRightGrab, "right");
                } catch (e) {
                  console.error("Platforms:", e);
                }
              },
              toolTip: "Hold either grip to keep a solid platform directly under that hand."
            }), new ButtonInfo({
              buttonText: "Long Arms",
              method: () => {
                getTransform(GTPlayer).method("set_localScale").invoke([1.25, 1.25, 1.25]);
              },
              disableMethod: () => {
                getTransform(GTPlayer).method("set_localScale").invoke(oneVector);
              },
              toolTip: "Gives you longer arms."
            }), new ButtonInfo({
              buttonText: "Teleport Gun",
              method: () => {
                if (rightGrab) {
                  const e = renderGun().gunPointer;
                  if (rightTrigger && !perviousTeleportKey) {
                    teleportPlayer(getTransform(e).method("get_position").invoke());
                    Rigidbody.method("set_linearVelocity").invoke(zeroVector);
                  }
                  perviousTeleportKey = rightTrigger;
                }
              },
              toolTip: "Teleports you to wherever your hand desires."
            }), new ButtonInfo({
              buttonText: "Noclip",
              method: () => {
                if (rightTrigger && !previousNoclipKey) {
                  toggleColliders(false);
                }
                if (!rightTrigger && previousNoclipKey) {
                  toggleColliders(true);
                }
                previousNoclipKey = rightTrigger;
              },
              toolTip: "Lets you clip through objects while holding right trigger."
            }), new ButtonInfo({
              buttonText: "Low Gravity",
              method: () => {
                const e = Vector3.method("op_Multiply", 2).invoke(Vector3.field("upVector").value, deltaTime * (6.66 / deltaTime));
                Rigidbody.method("AddForce", 2).invoke(e, 5);
              },
              toolTip: "Makes gravity lower on your character."
            }), new ButtonInfo({
              buttonText: "Zero Gravity",
              method: () => {
                const e = Vector3.method("op_Multiply", 2).invoke(Vector3.field("upVector").value, deltaTime * (9.81 / deltaTime));
                Rigidbody.method("AddForce", 2).invoke(e, 5);
              },
              toolTip: "Makes gravity lower on your character."
            }), new ButtonInfo({
              buttonText: "High Gravity",
              method: () => {
                const e = Vector3.method("op_Multiply", 2).invoke(Vector3.field("downVector").value, deltaTime * (7.77 / deltaTime));
                Rigidbody.method("AddForce", 2).invoke(e, 5);
              },
              toolTip: "Makes gravity higher on your character."
            })], [new ButtonInfo({
              buttonText: "Exit Player",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "Names",
              method: () => {
                currentCategory = 8;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "Movement",
              method: () => {
                currentCategory = 3;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens the player mods category."
            }), new ButtonInfo({
              buttonText: "RPC Protection",
              enableMethod: () => {
                blockRPCEnabled = true;
                sendNotification("RPC Protection ON", false);
              },
              disableMethod: () => {
                blockRPCEnabled = false;
                sendNotification("RPC Protection OFF", false);
              },
              isTogglable: true,
              toolTip: "Blocks incoming hit, stun, kill, and force RPCs sent to you by other players."
            }), new ButtonInfo({
              buttonText: "Invisible",
              enableMethod: () => {
                const e = PCClass.method("get_instance").invoke().method("get_playerView").invoke();
                if (!e) {
                  return null;
                }
                const t = e.field("_cameraTransform").value;
                if (!t) {
                  return null;
                }
                t.method("set_position").invoke([0, -99999, 0]);
              },
              disableMethod: () => {
                const e = PCClass.method("get_instance").invoke().method("get_playerView").invoke();
                if (!e) {
                  return null;
                }
                const t = e.field("_cameraTransform").value;
                if (!t) {
                  return null;
                }
                t.method("set_position").invoke(getTransform(headCollider).method("get_position").invoke());
              },
              isTogglable: true,
              toolTip: "Turns you invisible."
            }), new ButtonInfo({
              buttonText: "Inf Health",
              enableMethod: () => {
                NetPlayer.method("get_localPlayer").invoke().method("set_isInvincible").invoke(true);
              },
              disableMethod: () => {
                NetPlayer.method("get_localPlayer").invoke().method("set_isInvincible").invoke(false);
              },
              isTogglable: true,
              toolTip: "Makes you have inf health"
            }), new ButtonInfo({
              buttonText: "Dual Revolver Punch Launch",
              isTogglable: true,
              method: () => {
                if (!(time <= dualRevolverPunchDelay)) {
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (!e || e.isNull?.()) {
                      return;
                    }
                    const t = getTransform(headCollider).method("get_position").invoke();
                    const o = getTransform(bodyCollider).method("get_position").invoke();
                    for (const n of getAllNetPlayersList(false)) {
                      try {
                        const i = getPlayerHeldGrabbable(n, 0);
                        const a = getPlayerHeldGrabbable(n, 1);
                        if (!grabbableLooksLikeRevolver(i) || !grabbableLooksLikeRevolver(a)) {
                          continue;
                        }
                        const l = getPlayerHandAnchorTransform(n, 0);
                        const r = getPlayerHandAnchorTransform(n, 1);
                        let s = null;
                        try {
                          if (l && !l.isNull?.()) {
                            const e = l.method("get_position").invoke();
                            if (Vector3.method("Distance").invoke(t, e) <= 0.52 || Vector3.method("Distance").invoke(o, e) <= 0.7) {
                              s = l;
                            }
                          }
                        } catch (e) {}
                        try {
                          if (!s && r && !r.isNull?.()) {
                            const e = r.method("get_position").invoke();
                            if (Vector3.method("Distance").invoke(t, e) <= 0.52 || Vector3.method("Distance").invoke(o, e) <= 0.7) {
                              s = r;
                            }
                          }
                        } catch (e) {}
                        if (!s) {
                          continue;
                        }
                        dualRevolverPunchDelay = time + 0.12;
                        const c = getLaunchForward(s, getTransform(headCollider));
                        const d = Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(c, 3.4), [0, 1.05, 0]);
                        selfRPC(() => e.method("RPC_AddForce").invoke(d));
                        break;
                      } catch (e) {}
                    }
                  } catch (e) {
                    console.error("Dual Revolver Punch Launch:", e);
                  }
                }
              },
              toolTip: "Anyone holding revolvers in both hands can punch-launch you when they get close."
            }), new ButtonInfo({
              buttonText: "Force Dev Mode",
              isTogglable: false,
              method: () => {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.App").method("get_state").invoke();
                  if (!e || e.isNull()) {
                    sendNotification("No app state", false);
                    return;
                  }
                  try {
                    const t = e.method("get_user").invoke();
                    if (t && !t.isNull()) {
                      const e = t.method("get_isDeveloper").invoke();
                      if (e && !e.isNull()) {
                        e.method("set_value").invoke(true);
                      }
                    }
                  } catch (e) {
                    console.error("[GiveDev] isDeveloper:", e);
                  }
                  try {
                    const t = e.method("get_mapMachine").invoke();
                    if (t && !t.isNull()) {
                      const e = t.method("get_useDevMode").invoke();
                      if (e && !e.isNull()) {
                        e.method("set_value").invoke(true);
                      }
                    }
                  } catch (e) {
                    console.error("[GiveDev] useDevMode:", e);
                  }
                  sendNotification("Dev mode enabled! (go to Arena then go back to Adventure)", false);
                } catch (e) {
                  sendNotification("Give Dev: " + e, false);
                  console.error("[GiveDev]", e);
                }
              },
              toolTip: "Dev mode enabled! (go to Arena then go back to Adventure)"
            }), new ButtonInfo({
              buttonText: "Auto Clean Stuck Items",
              enableMethod: () => {
                globalThis.autoClean = true;
                sendNotification("Auto Clean Stuck Items ON", false);
              },
              disableMethod: () => {
                globalThis.autoClean = false;
                sendNotification("Auto Clean Stuck Items OFF", false);
              },
              method: () => {
                if (globalThis.autoClean && !(time - lastCleanTime < 0.4)) {
                  lastCleanTime = time;
                  try {
                    const e = PrefabGen.field("_instance").value.method("get_runner").invoke();
                    if (!e || e.isNull()) {
                      return;
                    }
                    const t = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                    let o = 0;
                    for (let n = 0; n < t.length; n++) {
                      const i = t.get(n);
                      if (i && !i.handle.isNull()) {
                        try {
                          let t = false;
                          try {
                            if ((i.method("get_IsHeld")?.invoke?.() || i.field("isHeld")?.value || i.method("get_IsGrabbed")?.invoke?.()) === true) {
                              t = true;
                            }
                          } catch (e) {}
                          if (t) {
                            continue;
                          }
                          let n = false;
                          try {
                            let e = i.method("get_transform").invoke();
                            while (e && !e.isNull()) {
                              const t = getComponentInParent(e, NetPlayer);
                              if (t && !t.handle.isNull() && playerIsLocal(t)) {
                                n = true;
                                break;
                              }
                              e = e.method("get_parent").invoke();
                            }
                          } catch (e) {}
                          if (!n) {
                            continue;
                          }
                          const a = i.method("get_Object").invoke();
                          if (!a || a.isNull()) {
                            continue;
                          }
                          a.method("RequestStateAuthority").invoke();
                          e.method("Despawn").invoke(a);
                          o++;
                        } catch (e) {}
                      }
                    }
                    if (o > 0) {
                      sendNotification(`Auto-removed ${o} stuck item(s)`, false);
                    }
                  } catch (e) {}
                }
              },
              isTogglable: true,
              toolTip: "Always removes items stuck on your body (goopfish etc.). Leaves held items alone."
            }), new ButtonInfo({
              buttonText: "Fullbright",
              disableMethod: () => {
                lightEnabled = false;
                if (fullbirhgtobject != null) {
                  fullbirhgtobject.method("get_gameObject").invoke().method("SetActive").invoke(false);
                }
              },
              enableMethod: () => {
                lightEnabled = true;
                if (fullbirhgtobject != null) {
                  fullbirhgtobject.method("get_gameObject").invoke().method("SetActive").invoke(true);
                }
              },
              method: () => {
                if (lightEnabled) {
                  if (fullbirhgtobject == null) {
                    Destroy(getComponent(fullbirhgtobject = createObject(getTransform(safeField(getInstance(), "headCollider")).method("get_position").invoke(), getTransform(safeField(getInstance(), "headCollider")).method("get_rotation").invoke(), [0, 0, 0], 3, bgColor), Collider));
                    addComponent(fullbirhgtobject, Light);
                    getTransform(fullbirhgtobject).method("SetParent", 2).invoke(getTransform(safeField(getInstance(), "headCollider")), false);
                  } else {
                    const e = getTransform(fullbirhgtobject);
                    e.method("set_position").invoke(getTransform(safeField(getInstance(), "headCollider")).method("get_position").invoke());
                    e.method("set_rotation").invoke(getTransform(safeField(getInstance(), "headCollider")).method("get_rotation").invoke());
                    const t = getComponent(fullbirhgtobject, Light);
                    if (t != null) {
                      t.method("set_intensity").invoke(5);
                      t.method("set_range").invoke(999);
                    }
                    fullbirhgtobject.method("get_gameObject").invoke().method("SetActive").invoke(true);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Press right joystick to toggle a light on your head."
            }), new ButtonInfo({
              buttonText: "Force Dev mode for ven mashines",
              disableMethod: () => {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.ItemVendingMachineView");
                  if (!e) {
                    return;
                  }
                  if (!devEnabled) {
                    const t = Object.method("FindObjectsOfType").inflate(e).invoke();
                    for (let e = 0; e < t.length; e++) {
                      t.get(e).field("_devModeOn").value = false;
                    }
                    devEnabled = false;
                  }
                } catch (e) {
                  console.error(e);
                }
              },
              method: () => {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.ItemVendingMachineView");
                  if (!e) {
                    return;
                  }
                  if (!devEnabled) {
                    const t = Object.method("FindObjectsOfType").inflate(e).invoke();
                    for (let e = 0; e < t.length; e++) {
                      t.get(e).field("_devModeOn").value = true;
                    }
                    devEnabled = true;
                  }
                } catch (e) {
                  console.error(e);
                }
              },
              isTogglable: true,
              toolTip: "Enables the developer mode for the vending machine so you can buy any item even if it isn't in the base game!"
            }), new ButtonInfo({
              buttonText: "Unlock All",
              isTogglable: false,
              method: () => {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.App").method("get_state").invoke();
                  if (!e || e.isNull()) {
                    sendNotification("No app state", false);
                    return;
                  }
                  try {
                    AssemblyCSharp.class("AnimalCompany.GameplayItemState").method("get_isHidden").implementation = function () {
                      return false;
                    };
                  } catch (e) {}
                  let t = null;
                  try {
                    const o = e.method("get_user").invoke();
                    if (o && !o.isNull()) {
                      const e = o.method("get_inventory").invoke();
                      if (e && !e.isNull()) {
                        t = e.method("get_unlockedGameplayItems").invoke();
                        try {
                          const t = e.method("get_devOwnAllAvatarItemsOverride").invoke();
                          if (t && !t.isNull()) {
                            t.method("set_value").invoke(true);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("[UnlockAll] inventory:", e);
                  }
                  let o = 0;
                  try {
                    const n = e.method("get_gameplayItems").invoke();
                    if (n && !n.isNull()) {
                      const e = n.method("get_all").invoke();
                      if (e && !e.isNull()) {
                        const n = e.method("get_Values").invoke();
                        if (n && !n.isNull()) {
                          const e = n.method("GetEnumerator").invoke();
                          while (e.method("MoveNext").invoke()) {
                            try {
                              const n = e.method("get_Current").invoke();
                              if (!n || n.isNull()) {
                                continue;
                              }
                              try {
                                const e = n.method("get_isUnlocked").invoke();
                                if (e && !e.isNull()) {
                                  e.method("set_value").invoke(true);
                                }
                              } catch (e) {}
                              try {
                                const e = n.method("get_unlockDependenciesSatisfied").invoke();
                                if (e && !e.isNull()) {
                                  e.method("set_value").invoke(true);
                                }
                              } catch (e) {}
                              try {
                                const e = n.method("get_unlockable").invoke();
                                if (e && !e.isNull()) {
                                  e.method("set_value").invoke(true);
                                }
                              } catch (e) {}
                              try {
                                const e = n.method("get_id").invoke();
                                if (e && t && !t.isNull()) {
                                  t.method("Add").invoke(e);
                                }
                              } catch (e) {}
                              o++;
                            } catch (e) {}
                          }
                        }
                      }
                    }
                  } catch (e) {
                    console.error("[UnlockAll] gameplayItems:", e);
                  }
                  let n = 0;
                  try {
                    const t = e.method("get_avatarItems").invoke();
                    if (t && !t.isNull()) {
                      const e = t.method("get_all").invoke();
                      if (e && !e.isNull()) {
                        const t = e.method("get_Values").invoke();
                        if (t && !t.isNull()) {
                          const e = t.method("GetEnumerator").invoke();
                          while (e.method("MoveNext").invoke()) {
                            try {
                              const t = e.method("get_Current").invoke();
                              if (!t || t.isNull()) {
                                continue;
                              }
                              try {
                                const e = t.method("get_isOwned").invoke();
                                if (e && !e.isNull()) {
                                  e.method("set_value").invoke(true);
                                }
                              } catch (e) {}
                              try {
                                const e = t.method("get_canPurchaseDirectly").invoke();
                                if (e && !e.isNull()) {
                                  e.method("set_value").invoke(true);
                                }
                              } catch (e) {}
                              try {
                                const e = t.method("get_isDevItem")?.invoke();
                                if (e && !e.isNull()) {
                                  e.method("set_value").invoke(true);
                                }
                              } catch (e) {}
                              n++;
                            } catch (e) {}
                          }
                        }
                      }
                    }
                  } catch (e) {
                    console.error("[UnlockAll] avatarItems:", e);
                  }
                  sendNotification("Unlocked " + o + " items + " + n + " cosmetics (dev included)!", false);
                } catch (e) {
                  sendNotification("Unlock All: " + e, false);
                  console.error("[UnlockAll]", e);
                }
              },
              toolTip: "Unlocks all gameplay + cosmetic items including hidden/dev items. Hooks get_isHidden=false so dev items show in UI."
            }), new ButtonInfo({
              buttonText: "Inf Farts",
              enableMethod: () => {
                try {
                  GTPlayerClass.field("<Instance>k__BackingField").value.method("set_maxMidAirJumps").invoke(9999);
                } catch (e) {
                  sendNotification("Inf farts are now enabled!: " + e, false);
                }
              },
              disableMethod: () => {
                try {
                  GTPlayerClass.field("<Instance>k__BackingField").value.method("set_maxMidAirJumps").invoke(0);
                } catch (e) {}
              },
              isTogglable: true,
              toolTip: "Inf farts are now enabled!"
            }), new ButtonInfo({
              buttonText: "Normal Farts",
              enableMethod: () => {
                try {
                  GTPlayerClass.field("<Instance>k__BackingField").value.method("set_maxMidAirJumps").invoke(1);
                } catch (e) {
                  sendNotification("normal farts are now enabled!: " + e, false);
                }
              },
              disableMethod: () => {
                try {
                  GTPlayerClass.field("<Instance>k__BackingField").value.method("set_maxMidAirJumps").invoke(0);
                } catch (e) {}
              },
              isTogglable: true,
              toolTip: "normal farts are now enabled!"
            }), new ButtonInfo({
              buttonText: "give self nuts",
              method: () => {
                if (frameCount % 60 == 0) {
                  try {
                    const e = AssemblyCSharp.class("AnimalCompany.LakeJobPartTwo");
                    const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                    for (let e = 0; e < t.length; e++) {
                      t.get(e).method("set_bankedNuts").invoke(999999);
                    }
                    sendNotification("Gave lots of nuts!", false);
                  } catch (e) {
                    console.error(e);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Continuously gives nuts when enabled."
            }), new ButtonInfo({
              buttonText: "give self cc",
              method: () => {
                if (frameCount % 60 == 0) {
                  try {
                    const e = AssemblyCSharp.class("AnimalCompany.App").method("get_state").invoke().method("get_user").invoke();
                    const t = e.method("get_wallet").invoke().method("get_softCurrency").invoke();
                    if (t && !t.isNull()) {
                      t.field("_value").value = 999999;
                    }
                    sendNotification("Gave lots of CC!", false);
                  } catch (e) {
                    console.error(e);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Continuously gives CC when enabled."
            }), new ButtonInfo({
              buttonText: "give self rp",
              method: () => {
                if (frameCount % 60 == 0) {
                  try {
                    const e = AssemblyCSharp.class("AnimalCompany.App").method("get_state").invoke().method("get_user").invoke();
                    const t = e.method("get_wallet").invoke().method("get_researchPoints").invoke();
                    if (t && !t.isNull()) {
                      t.field("_value").value = 999999;
                    }
                    sendNotification("Gave lots of research points!", false);
                  } catch (e) {
                    console.error(e);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Continuously gives research points when enabled."
            }), new ButtonInfo({
              buttonText: "Give CC (server RPC)",
              method: function () {
                try {
                  const user = AssemblyCSharp.class("AnimalCompany.App").method("get_state").invoke().method("get_user").invoke();
                  if (!user || user.isNull?.()) {
                    return;
                  }
                  const walletObj = user.method("get_wallet").invoke();
                  if (!walletObj || walletObj.isNull?.()) {
                    return;
                  }
                  if (!this.__ccDumped) {
                    this.__ccDumped = true;
                    for (const g of ["get_softCurrency", "get_hardCurrency", "get_fishCurrency", "get_researchPoints"]) {
                      try {
                        const p = walletObj.method(g).invoke();
                        if (!p || p.isNull?.()) {
                          continue;
                        }
                        const klass = p.class;
                        console.log("[CC-DUMP] " + g + " -> " + (klass?.name ?? "?"));
                        for (const m of klass?.methods ?? []) {
                          try {
                            console.log("[CC-DUMP]   m " + m.name + "(" + (m.parameters?.map(x => x.type?.name ?? "?").join(",") ?? "") + ")");
                          } catch (e) {}
                        }
                        for (const f of klass?.fields ?? []) {
                          let v = "<err>";
                          try {
                            v = String(f.withHolder(p).value);
                          } catch (e) {}
                          console.log("[CC-DUMP]   f " + f.name + " = " + v);
                        }
                      } catch (e) {}
                    }
                  }
                  const pump = (g, val) => {
                    try {
                      const p = walletObj.method(g).invoke();
                      if (!p || p.isNull?.()) {
                        return "<null>";
                      }
                      p.field("_value").value = val;
                      return p.field("_value").value;
                    } catch (e) {
                      return "<err:" + e + ">";
                    }
                  };
                  const target = 999999999;
                  const rs = [];
                  for (const g of ["get_softCurrency", "get_hardCurrency", "get_fishCurrency", "get_researchPoints"]) {
                    try {
                      rs.push(g + " -> " + pump(g, target));
                    } catch (e) {}
                  }
                  try {
                    const flag = walletObj.method("get_saveSoftCurrencyChanges").invoke();
                    if (flag && !flag.isNull?.()) {
                      flag.field("_value").value = true;
                      rs.push("saveSoftCurrencyChanges -> " + flag.field("_value").value);
                    }
                  } catch (e) {}
                  console.log("[CC] " + rs.join(" | "));
                } catch (e) {
                  console.error("[CC-RPC]", e);
                }
              },
              isTogglable: true,
              toolTip: "Dumps wallet StatePrimitives once, then sets soft/hard/fish/research + saveSoftCurrencyChanges to 999999999 each frame."
            }), new ButtonInfo({
              buttonText: "Restart/Fix Mic",
              disableMethod: () => {
                const e = NetPlayer.method("get_localPlayer").invoke();
                if (!e) {
                  return;
                }
                const t = e.field("_recorder").value;
                if (t) {
                  t.method("set_DebugEchoMode").invoke(false);
                }
              },
              enableMethod: () => {
                const e = NetPlayer.method("get_localPlayer").invoke();
                if (!e) {
                  return;
                }
                const t = e.field("_recorder").value;
                if (t) {
                  t.method("RestartRecording").invoke();
                }
              },
              isTogglable: true
            }), new ButtonInfo({
              buttonText: "No Blueprint Limits",
              isTogglable: true,
              method: () => {
                try {
                  noBlueprintLimitEnabled = !noBlueprintLimitEnabled;
                  const e = AssemblyCSharp.class("AnimalCompany.GrabbableItem");
                  if (!e) {
                    console.error("[Mod Error] Failed to find GrabbableItem class");
                    return;
                  }
                  const t = e.method("ValidateBlueprintItems");
                  if (t) {
                    const e = t;
                    t.implementation = function (t, o) {
                      if (noBlueprintLimitEnabled) {
                        return "";
                      } else {
                        return e.invoke(this, t, o);
                      }
                    };
                  }
                  const o = e.method("ValidateBlueprintItemsInternal");
                  if (o) {
                    const e = o;
                    o.implementation = function (t, o, n, i) {
                      if (noBlueprintLimitEnabled) {
                        return "";
                      } else {
                        return e.invoke(this, t, o, n, i);
                      }
                    };
                  }
                  console.log("[Mod] Blueprint saving limits " + (noBlueprintLimitEnabled ? "disabled (save anything)" : "restored"));
                } catch (e) {
                  console.error("[Mod Error] Failed to remove blueprint limits: ", e);
                }
              }
            }), new ButtonInfo({
              buttonText: "Get Hand Position",
              method: () => {
                if (rightGrab) {
                  console.log(rightHandTransform.method("get_position").invoke());
                }
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "Give Money",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e) {
                    return;
                  }
                  e.method("RPC_AddPlayerMoney").invoke(100000000);
                } catch (e) {
                  console.error("give money failed:", e);
                }
              },
              isTogglable: false
            })], [new ButtonInfo({
              buttonText: "Exit Error",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns you back to the main category."
            })], [new ButtonInfo({
              buttonText: "Exit Item mods",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "Item IDs",
              method: () => {
                currentCategory = 17;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "Grid mods",
              method: () => {
                currentCategory = 100;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Spawns 1 to 30 of the currently selected Item ID in a frozen grid."
            }), new ButtonInfo({
              buttonText: "Weapon mods",
              method: () => {
                currentCategory = 25;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "opened ning ning."
            }), new ButtonInfo({
              buttonText: "item id selector gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger && !(time <= itemIdSelectorGunDelay)) {
                  itemIdSelectorGunDelay = time + 0.3;
                  try {
                    const e = t.method("get_collider").invoke();
                    if (!e || e.isNull?.()) {
                      sendNotification("Aim at an item first", false);
                      return;
                    }
                    let o = getComponentInParent(e, GBIClass);
                    if (!o || o.isNull?.()) {
                      const t = getComponentInParent(e, GBOClass);
                      if (t && !t.isNull?.()) {
                        try {
                          o = t.method("get_gameObject").invoke().method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                        } catch (e) {
                          o = null;
                        }
                      }
                    }
                    if (!o || o.isNull?.()) {
                      sendNotification("Target is not a selectable item", false);
                      return;
                    }
                    const n = o.method("get_itemID").invoke();
                    const i = String(n?.content ?? "").trim();
                    const a = e => e.replace(/\\/g, "/").replace(/\(Clone\)/gi, "").trim().toLowerCase().split("/").pop() ?? "";
                    const l = a(i);
                    const r = itemIDs.findIndex(e => a(String(e)) === l);
                    if (!l || r < 0) {
                      sendNotification("Item ID not in selector list: " + (i || "unknown"), false);
                      return;
                    }
                    sendNotification("Selected Item ID: " + itemIDs[itemIndex = r] + " (index " + itemIndex + ")", false);
                  } catch (e) {
                    console.error("Item ID selector gun:", e);
                    sendNotification("Item ID select failed: " + e, false);
                  }
                }
              },
              toolTip: "Aim at an item, hold right grip, and pull trigger to select its Item ID index."
            }), new ButtonInfo({
              buttonText: "item spawner hand (normal)",
              method: () => {
                const e = rightHandTransform;
                if (rightGrab) {
                  try {
                    const t = getLaunchForward(e);
                    const o = getLaunchUp(e, t);
                    const n = Vector3.method("op_Addition").invoke(e.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(t, 0.38), Vector3.method("op_Multiply", 2).invoke(o, -0.03)));
                    const i = spawnItemAtPos(itemIDs[itemIndex], n, getLaunchRotation(e, t, o));
                    if (!i || i.handle.isNull()) {
                      sendNotification("Spawn returned null: " + itemIDs[itemIndex], false);
                    } else {
                      sendNotification("Spawned: " + itemIDs[itemIndex], false);
                    }
                  } catch (e) {
                    console.error("Hand spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Spawns items in your right hand (hold grip + B)."
            }), new ButtonInfo({
              buttonText: "Item spawner hand (rainbow)",
              method: () => {
                const e = rightHandTransform;
                if (rightGrab) {
                  try {
                    const t = itemIDs[itemIndex];
                    const o = getLaunchForward(e);
                    const n = getLaunchUp(e, o);
                    const i = spawnItemAtPos(t, Vector3.method("op_Addition").invoke(e.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(o, 0.38), Vector3.method("op_Multiply", 2).invoke(n, -0.03))), getLaunchRotation(e, o, n));
                    if (!i || i.handle.isNull()) {
                      sendNotification("Rainbow spawn returned null: " + t, false);
                    } else {
                      try {
                        const e = i.method("get_gameObject").invoke();
                        if (e && !e.isNull()) {
                          const t = e.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (t && !t.isNull()) {
                            t.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(t, Math.floor(Math.random() * 255) - 127);
                            setItemColorSaturation(t, Math.floor(Math.random() * 148) - 20);
                          }
                        }
                      } catch (e) {}
                      sendNotification("Rainbow spawned: " + t, false);
                    }
                  } catch (e) {
                    console.error("Rainbow hand spawn error:", e);
                    sendNotification("Rainbow hand spawn failed: " + e, false);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Spawns the selected item at your right hand with randomized size, hue, and saturation."
            }), new ButtonInfo({
              buttonText: "Bring All Items Gun",
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                const t = e.ray;
                if (rightTrigger && !(time <= bringAllItemsGunDelay)) {
                  bringAllItemsGunDelay = time + 0.05;
                  try {
                    const o = t && !t.isNull?.() ? t.method("get_point").invoke() : getTransform(e.gunPointer).method("get_position").invoke();
                    let n = 0;
                    const i = new Set();
                    const a = collectAllLobbyItemsWithHeld();
                    const l = Math.min(24, a.length);
                    for (let e = 0; e < a.length && n < l; e++) {
                      const t = a[(bringAllItemsCursor + e) % a.length];
                      try {
                        const e = getRootLikeObject(t);
                        const a = normalizeSceneObjectHandle(e) || normalizeSceneObjectHandle(t);
                        if (a && i.has(a)) {
                          continue;
                        }
                        if (a) {
                          i.add(a);
                        }
                        requestAuthorityDeep(t);
                        requestAuthorityDeep(e);
                        forceReleaseItemFromAllPlayers(t);
                        forceReleaseItemFromAllPlayers(e);
                        tryReleaseHeldItem(t);
                        tryReleaseHeldItem(e);
                        if (forceMoveLobbyItem(e, o)) {
                          try {
                            forceMoveLobbyItem(e, o);
                          } catch (e) {}
                          try {
                            forceMoveLobbyItem(e, o);
                          } catch (e) {}
                          try {
                            forceMoveLobbyItem(e, o);
                          } catch (e) {}
                          n++;
                        }
                      } catch (e) {}
                    }
                    bringAllItemsCursor = (bringAllItemsCursor + l) % Math.max(a.length, 1);
                    sendNotification("Brought " + n + " items to pointer!", false);
                  } catch (e) {
                    console.error("Bring All Items Gun:", e);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Teleports all lobby items to your gun pointer (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "Spawn Items gun (normal)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger && time > itemGunDelay) {
                  itemGunDelay = time + 0.1;
                  try {
                    const e = t.method("get_point").invoke();
                    const o = spawnItemAtPos(itemIDs[itemIndex], e, identityQuaternion);
                    if (!o || o.handle.isNull()) {
                      sendNotification("Spawn returned null: " + itemIDs[itemIndex], false);
                    } else {
                      console.log("✓ Spawned item:", itemIDs[itemIndex]);
                      sendNotification("Spawned: " + itemIDs[itemIndex], false);
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "Spawns items where you aim (hold grip, pull trigger)."
            }), new ButtonInfo({
              buttonText: "spawn items gun (rainbow)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger && time > rainbowItemGunDelay) {
                  rainbowItemGunDelay = time + 0.1;
                  try {
                    const e = itemIDs[itemIndex];
                    const o = spawnItemAtPos(e, t.method("get_point").invoke(), identityQuaternion);
                    if (!o || o.handle.isNull()) {
                      sendNotification("Rainbow spawn returned null: " + e, false);
                      return;
                    }
                    try {
                      const e = o.method("get_gameObject").invoke();
                      if (e && !e.isNull()) {
                        const t = e.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                        if (t && !t.isNull()) {
                          t.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                          setItemColorHue(t, Math.floor(Math.random() * 255) - 127);
                          setItemColorSaturation(t, Math.floor(Math.random() * 148) - 20);
                        }
                      }
                    } catch (e) {}
                    console.log("✓ Rainbow spawned item:", e);
                    sendNotification("Rainbow spawned: " + e, false);
                  } catch (e) {
                    console.error("Rainbow item spawn error:", e);
                    sendNotification("Rainbow spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "Spawns the selected item ID with randomized scale, hue, and saturation."
            }), new ButtonInfo({
              buttonText: "Steal Items (gun)",
              method: () => {
                const e = rightHandTransform;
                if (rightSecondary && rightGrab) {
                  PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_" + itemIDs[itemIndex]), Il2Cpp.string("GiantRockObject"), e.method("get_position").invoke(), e.method("get_rotation").invoke(), NULL);
                }
                if (rightGrab) {
                  const e = renderGun().ray;
                  if (rightTrigger && e) {
                    const t = getComponentInParent(e.method("get_collider").invoke(), GBOClass);
                    t.method("RPC_Release", 6).invoke(rightHandTransform.method("get_position").invoke(), rightHandTransform.method("get_rotation").invoke(), zeroVector, zeroVector, true, false);
                    t.method("RPC_Teleport", 2).invoke(rightHandTransform.method("get_position").invoke(), rightHandTransform.method("get_rotation").invoke(), zeroVector, zeroVector, true, false);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Lets you steal items from other players."
            }), new ButtonInfo({
              buttonText: "quiver giveaway",
              method: () => {
                if (!rightSecondary) {
                  return;
                }
                const e = spawnFilledQuiver();
                if (e) {
                  sendNotification("Spawned random " + e.insertedCount + "-item quiver!", false);
                }
              },
              isTogglable: true,
              toolTip: "Spawns a random closed quiver with up to 15 items."
            }), new ButtonInfo({
              buttonText: "giveaway bag [W?]",
              enableMethod: () => {
                giveawayBagMinigunEnabled = true;
                lastGiveawayBagMinigunTime = 0;
              },
              disableMethod: () => {
                giveawayBagMinigunEnabled = false;
                giveawayBagPending = [];
              },
              method: () => {
                if (giveawayBagMinigunEnabled && rightGrab && !(time < lastGiveawayBagMinigunTime)) {
                  lastGiveawayBagMinigunTime = time + 0.001;
                  try {
                    const e = rightHandTransform.method("get_forward").invoke();
                    const t = Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Multiply", 2).invoke(e, 0.5));
                    const o = rightHandTransform.method("get_rotation").invoke();
                    const n = giveawayBagBackpackIDs[Math.floor(Math.random() * giveawayBagBackpackIDs.length)];
                    let i = spawnItemAsProjectile(n, t, o);
                    if (!i || !!i.isNull()) {
                      i = spawnItemAtPos(n, t, o);
                    }
                    if (i && !i.isNull()) {
                      const t = Math.floor(Math.random() * 255) - 127;
                      const o = Math.floor(Math.random() * 255) - 127;
                      const n = Math.floor(Math.random() * 80) + 30;
                      giveawayBagPending.push({
                        item: i,
                        rb: null,
                        forward: e,
                        frames: 120,
                        hue: t,
                        sat: o,
                        scale: n,
                        cardsSpawned: false
                      });
                    }
                  } catch (e) {
                    sendNotification("Giveaway bag failed: " + e, false);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Hold right grip to fire large backpacks in random colors and sizes. Each bag is filled with 23 trading cards."
            }), new ButtonInfo({
              buttonText: "current item giveaway quiver",
              method: () => {
                if (!rightSecondary) {
                  return;
                }
                const e = itemIDs[itemIndex];
                const t = spawnFilledQuiver(e);
                if (t) {
                  sendNotification("Spawned " + e + " quiver with " + t.insertedCount + " items!", false);
                }
              },
              isTogglable: true,
              toolTip: "Fills a closed quiver with 15 of your selected item."
            }), new ButtonInfo({
              buttonText: "Spawn Ore Quiver",
              method: () => {
                if (!rightSecondary) {
                  return;
                }
                const e = spawnOreQuiver();
                if (e) {
                  sendNotification("Spawned ore quiver with " + e.insertedCount + " ores!", false);
                }
              },
              isTogglable: true,
              toolTip: "Spawns a randomized quiver filled with randomized ores."
            }), new ButtonInfo({
              buttonText: "Delete Held Item",
              method: () => {
                try {
                  const e = getLocalHeldGrabbable(true);
                  if (!e || e.isNull?.()) {
                    sendNotification("Not holding anything", false);
                    return;
                  }
                  tryReleaseHeldItem(e);
                  requestAuthorityDeep(e);
                  forceReleaseItemFromAllPlayers(e);
                  let t = false;
                  try {
                    e.method("Despawn").invoke();
                    t = true;
                  } catch (e) {}
                  if (!t) {
                    try {
                      e.method("RPC_Destroy").invoke();
                      t = true;
                    } catch (e) {}
                  }
                  if (!t) {
                    try {
                      const o = e.method("get_gameObject").invoke();
                      if (o && !o.isNull?.()) {
                        Destroy(o);
                      }
                      t = true;
                    } catch (e) {}
                  }
                  sendNotification("Item deleted!", false);
                } catch (e) {
                  sendNotification("Delete item: " + e, false);
                  console.error("Delete item:", e);
                }
              },
              isTogglable: false,
              toolTip: "Despawns whatever you're holding in your right hand."
            }), new ButtonInfo({
              buttonText: "No Gravity On Held Item",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e) {
                    return;
                  }
                  const t = e.method("GetHandInteractor", 1).invoke(0);
                  if (!t) {
                    return;
                  }
                  const o = t.field("_itemAnchor").value;
                  if (!o) {
                    return;
                  }
                  const n = o.method("get_grabbableObject").invoke();
                  if (!n) {
                    return;
                  }
                  n.method("set_useGravity").invoke(false);
                } catch (e) {
                  console.error(e);
                }
              },
              isTogglable: false,
              toolTip: "Disables gravity on the item in your right hand."
            }), new ButtonInfo({
              buttonText: "Add Gravity To Held Item",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e) {
                    return;
                  }
                  const t = e.method("GetHandInteractor", 1).invoke(0);
                  if (!t) {
                    return;
                  }
                  const o = t.field("_itemAnchor").value;
                  if (!o) {
                    return;
                  }
                  const n = o.method("get_grabbableObject").invoke();
                  if (!n) {
                    return;
                  }
                  n.method("set_useGravity").invoke(true);
                } catch (e) {
                  console.error(e);
                }
              },
              isTogglable: false,
              toolTip: "Enables gravity on the item in your right hand."
            }), new ButtonInfo({
              buttonText: "Item Launcher",
              isTogglable: true,
              method: () => {
                try {
                  const e = rightGrab && rightTrigger;
                  if (!e && (!leftGrab || !leftTrigger)) {
                    return;
                  }
                  if (time <= itemLauncherSelfDelay) {
                    return;
                  }
                  itemLauncherSelfDelay = time + 0.055;
                  const t = itemIDs[itemIndex];
                  const o = spawnForwardLaunchedItem(t, e ? rightHandTransform : leftHandTransform, 28, 0.92);
                  if (!o || !!o.isNull?.()) {
                    sendNotification("Item launch failed", false);
                  }
                } catch (e) {
                  console.error("Item Launcher:", e);
                }
              },
              toolTip: "Hold grip + trigger to keep launching the currently selected Item ID from your hand."
            }), new ButtonInfo({
              buttonText: "rainbow item launcher",
              enableMethod: () => {
                itemLauncherEnabled = true;
              },
              disableMethod: () => {
                itemLauncherEnabled = false;
                itemLauncherDelay = 0;
              },
              isTogglable: true,
              method: () => {
                if (itemLauncherEnabled && rightGrab && !(time < itemLauncherDelay)) {
                  itemLauncherDelay = time + 0.05;
                  try {
                    const e = rightHandTransform.method("get_position").invoke();
                    const t = rightHandTransform.method("get_forward").invoke();
                    const o = rightHandTransform.method("get_rotation").invoke();
                    const n = spawnItemAtPos(itemIDs[itemIndex], e, o);
                    if (!n || n.isNull()) {
                      return;
                    }
                    const i = n.method("get_gameObject").invoke();
                    if (!i || i.isNull()) {
                      return;
                    }
                    try {
                      const e = i.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                      if (e && !e.isNull()) {
                        e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                        e.method("set_colorHue").invoke(Math.floor(Math.random() * 255) - 127);
                        e.method("set_colorSaturation").invoke(Math.floor(Math.random() * 148) - 20);
                      }
                    } catch (e) {}
                    const a = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const l = i.method("GetComponentInChildren", 1).inflate(a).invoke();
                    if (!l || l.isNull()) {
                      return;
                    }
                    l.method("set_isKinematic").invoke(false);
                    l.method("set_useGravity").invoke(false);
                    const r = Vector3.method("op_Multiply", 2).invoke(t, 120);
                    l.method("AddForce", 2).invoke(r, 1);
                  } catch (e) {
                    smartError("[ItemMinigun]", e);
                  }
                }
              },
              toolTip: "Hold right grip, and make rainbow items launch out of your hand."
            }), new ButtonInfo({
              buttonText: "Dupe Held Item",
              isTogglable: true,
              toolTip: "",
              method: function () {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.isNull()) {
                    return;
                  }
                  for (let t = 0; t <= 1; t++) {
                    const o = t === 1;
                    if (!(o ? rightSecondary : leftSecondary)) {
                      continue;
                    }
                    const n = e.method("GetHandInteractor", 1).invoke(t);
                    if (!n || n.isNull()) {
                      continue;
                    }
                    const i = n.field("_itemAnchor").value;
                    if (!i || i.isNull()) {
                      continue;
                    }
                    const a = i.method("get_grabbableObject").invoke();
                    if (!a || a.isNull()) {
                      continue;
                    }
                    const l = a.method("GetComponent", 1).inflate(GBIClass).invoke();
                    if (!l || l.isNull()) {
                      continue;
                    }
                    let r = "item_treestick";
                    try {
                      const e = l.method("get_itemID").invoke();
                      if (e && !e.isNull() && e.content) {
                        r = e.content;
                      }
                    } catch (e) {}
                    l.class.name;
                    l.class.name;
                    AssemblyCSharp.class("AnimalCompany.BackpackItem");
                    AssemblyCSharp.class("AnimalCompany.Quiver");
                    Il2Cpp.domain.assembly("Fusion.Runtime").image;
                    const s = o ? rightHandTransform : leftHandTransform;
                    const c = s.method("get_position").invoke();
                    const d = s.method("get_rotation").invoke();
                    const u = AssemblyCSharp.class("AnimalCompany.PrefabGenerator");
                    Quaternion.method("get_identity").invoke();
                    u.method("SpawnItem", 5).invoke(Il2Cpp.string(r), c, d, NULL, 0);
                  }
                } catch (e) {
                  console.error("error", e);
                  sendNotification("Dupe failed", false);
                }
              },
              disableMethod: function () {}
            }), new ButtonInfo({
              buttonText: "Dupe Goop Invention",
              isTogglable: true,
              toolTip: "Duplicates the whole goop invention currently held (held part + all stuck/linked parts). One dupe per B/Y press.",
              method: function () {
                try {
                  const sec = righthand ? leftSecondary : rightSecondary;
                  if (sec === this.__goopSecPrev) {
                    return;
                  }
                  this.__goopSecPrev = sec;
                  if (!sec) {
                    return;
                  }
                  const local = NetPlayer.method("get_localPlayer").invoke();
                  if (!local || local.isNull?.()) {
                    return;
                  }
                  let heldPart = null;
                  for (let t = 0; t <= 1; t++) {
                    if (!(t === 1 ? rightSecondary : leftSecondary)) {
                      continue;
                    }
                    const interactor = local.method("GetHandInteractor", 1).invoke(t);
                    if (!interactor || interactor.isNull?.()) {
                      continue;
                    }
                    const anchor = interactor.field("_itemAnchor").value;
                    if (!anchor || anchor.isNull?.()) {
                      continue;
                    }
                    const grabbable = anchor.method("get_grabbableObject").invoke();
                    if (!grabbable || grabbable.isNull?.()) {
                      continue;
                    }
                    heldPart = grabbable;
                    break;
                  }
                  if (!heldPart) {
                    return;
                  }
                  const isGoop = (() => {
                    try {
                      if (heldPart.method("get_isGoopInvention").invoke() === true) {
                        return true;
                      }
                    } catch (e) {}
                    try {
                      const go = heldPart.method("get_gameObject").invoke();
                      if (!go || go.isNull?.()) {
                        return false;
                      }
                      const child = go.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                      return child && !child.isNull?.() && child.method("get_isGoopInvention").invoke() === true;
                    } catch (e) {}
                    try {
                      let hasStuck = false;
                      try {
                        const r = heldPart.field("_cachedStuckRootGrabbable").value;
                        hasStuck = r && !r.isNull?.();
                      } catch (e) {}
                      if (!hasStuck) {
                        try {
                          const r = heldPart.field("_cachedStuckParentGrabbable").value;
                          hasStuck = r && !r.isNull?.();
                        } catch (e) {}
                      }
                      if (hasStuck) {
                        return true;
                      }
                    } catch (e) {}
                    try {
                      const root = getTransform(heldPart).method("get_root").invoke();
                      const rootGO = root && !root.isNull?.() ? root.method("get_gameObject").invoke() : null;
                      if (rootGO && !rootGO.isNull?.()) {
                        const comps = rootGO.method("GetComponentsInChildren", 1).inflate(GBOClass).invoke(true);
                        if (comps && !comps.isNull?.() && comps.length >= 2) {
                          return true;
                        }
                      }
                    } catch (e) {}
                    return false;
                  })();
                  if (!isGoop) {
                    if (!this.__goopToastAt || !(time < this.__goopToastAt)) {
                      this.__goopToastAt = time + 0.5;
                      sendNotification("Not a goop invention", false);
                    }
                    return;
                  }
                  if (!this.__goopDumped) {
                    this.__goopDumped = true;
                    try {
                      console.log("[GoopDupe] GBOClass schema (goop/stick related):");
                      const klass = GBOClass;
                      for (const m of klass?.methods ?? []) {
                        try {
                          const nm = String(m.name ?? "");
                          if (/goop|stick|stuck|attach|invention|weld|join/i.test(nm)) {
                            console.log("[GoopDupe]   m " + nm + "(" + (m.parameters?.map(p => p.type?.name ?? "?").join(",") ?? "") + ")");
                          }
                        } catch (e) {}
                      }
                      for (const f of klass?.fields ?? []) {
                        try {
                          if (/goop|stick|stuck|attach|invention|weld|join/i.test(String(f.name ?? ""))) {
                            console.log("[GoopDupe]   f " + f.name);
                          }
                        } catch (e) {}
                      }
                      try {
                        const sk = AssemblyCSharp.class("AnimalCompany.StickyAnchor");
                        console.log("[GoopDupe] StickyAnchor class schema:");
                        for (const m of sk?.methods ?? []) {
                          try {
                            const nm = String(m.name ?? "");
                            if (/stick|stuck|set|add|init|weld|attach|anchor|goop/i.test(nm)) {
                              console.log("[GoopDupe]   m " + nm + "(" + (m.parameters?.map(p => p.type?.name ?? "?").join(",") ?? "") + ")");
                            }
                          } catch (e) {}
                        }
                        for (const f of sk?.fields ?? []) {
                          try {
                            console.log("[GoopDupe]   f " + f.name);
                          } catch (e) {}
                        }
                      } catch (e) {}
                      try {
                        const nbi = Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkBehaviourId");
                        console.log("[GoopDupe] NetworkBehaviourId schema:");
                        for (const m of nbi?.methods ?? []) {
                          try {
                            const nm = String(m.name ?? "");
                            console.log("[GoopDupe]   m " + nm + "(" + (m.parameters?.map(p => p.type?.name ?? "?").join(",") ?? "") + ")");
                          } catch (e) {}
                        }
                        for (const f of nbi?.fields ?? []) {
                          try {
                            console.log("[GoopDupe]   f " + f.name + " : " + (f.type?.name ?? "?"));
                          } catch (e) {}
                        }
                      } catch (e) {}
                    } catch (e) {}
                  }
                  let inventionRoot = heldPart;
                  try {
                    const r = heldPart.field("_cachedStuckRootGrabbable").value;
                    if (r && !r.isNull?.()) {
                      inventionRoot = r;
                    }
                  } catch (e) {}
                  let rootGo = heldPart;
                  try {
                    const rt = getTransform(heldPart).method("get_root").invoke();
                    if (rt && !rt.isNull?.()) {
                      const g = rt.method("get_gameObject").invoke();
                      if (g && !g.isNull?.()) {
                        rootGo = g;
                      }
                    }
                  } catch (e) {}
                  const rootKey = normalizeSceneObjectHandle(inventionRoot) || normalizeSceneObjectHandle(rootGo);
                  if (this.__goopSnapKey !== rootKey || !this.__goopSnap) {
                    const parts = [];
                    const seen = new Set();
                    const addPart = o => {
                      try {
                        if (!o || o.isNull?.()) {
                          return;
                        }
                        const k = normalizeSceneObjectHandle(o) || String(o);
                        if (seen.has(k)) {
                          return;
                        }
                        seen.add(k);
                        parts.push(o);
                      } catch (e) {}
                    };
                    addPart(heldPart);
                    try {
                      const childParts = rootGo.method("GetComponentsInChildren", 1).inflate(GBOClass).invoke(true);
                      if (childParts && !childParts.isNull?.()) {
                        for (let e = 0; e < childParts.length; e++) {
                          addPart(childParts.get(e));
                        }
                      }
                    } catch (e) {}
                    try {
                      const sceneParts = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                      if (sceneParts && !sceneParts.isNull?.()) {
                        for (let e = 0; e < sceneParts.length; e++) {
                          const p = sceneParts.get(e);
                          try {
                            if (!p || p.isNull?.()) {
                              continue;
                            }
                            let linked = false;
                            try {
                              const r = p.field("_cachedStuckRootGrabbable").value;
                              linked = r && !r.isNull?.() && rootKey && normalizeSceneObjectHandle(r) === rootKey;
                            } catch (e) {}
                            if (!linked) {
                              try {
                                const r = p.field("_cachedStuckParentGrabbable").value;
                                linked = r && !r.isNull?.() && rootKey && normalizeSceneObjectHandle(r) === rootKey;
                              } catch (e) {}
                            }
                            if (!linked) {
                              try {
                                const r = p.field("_cachedStuckRootGrabbable").value;
                                linked = r && !r.isNull?.() && normalizeSceneObjectHandle(r) === rootKey;
                              } catch (e) {}
                            }
                            if (linked) {
                              addPart(p);
                            }
                          } catch (e) {}
                        }
                      }
                    } catch (e) {}
                    if (parts.length <= 1) {
                      try {
                        const rootTF = getTransform(heldPart).method("get_root").invoke();
                        const rootHandle = rootTF && !rootTF.isNull?.() ? normalizeSceneObjectHandle(rootTF) : null;
                        const sceneParts = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                        if (sceneParts && !sceneParts.isNull?.()) {
                          for (let e = 0; e < sceneParts.length; e++) {
                            try {
                              const p = sceneParts.get(e);
                              if (!p || p.isNull?.()) {
                                continue;
                              }
                              const tf = getTransform(p);
                              const r = tf ? tf.method("get_root").invoke() : null;
                              let isStuck = false;
                              try {
                                const rv = p.field("_cachedStuckRootGrabbable").value;
                                isStuck = rv && !rv.isNull?.();
                              } catch (e) {}
                              if (!isStuck) {
                                try {
                                  const rv = p.field("_cachedStuckParentGrabbable").value;
                                  isStuck = rv && !rv.isNull?.();
                                } catch (e) {}
                              }
                              if (isStuck && r && !r.isNull?.() && rootHandle && normalizeSceneObjectHandle(r) === rootHandle) {
                                addPart(p);
                              }
                            } catch (e) {}
                          }
                        }
                      } catch (e) {}
                    }
                    const refTF = getTransform(heldPart);
                    const refPos = refTF ? refTF.method("get_position").invoke() : null;
                    const entries = [];
                    for (let pi = 0; pi < parts.length; pi++) {
                      const part = parts[pi];
                      const tf = getTransform(part);
                      let dx = 0, dy = 0, dz = 0, qx = 0, qy = 0, qz = 0, qw = 1;
                      try {
                        const pos = tf.method("get_position").invoke();
                        if (pos && refPos) {
                          dx = pos.field("x").value - refPos.field("x").value;
                          dy = pos.field("y").value - refPos.field("y").value;
                          dz = pos.field("z").value - refPos.field("z").value;
                        }
                      } catch (e) {}
                      try {
                        const rot = tf.method("get_rotation").invoke();
                        if (rot) {
                          qx = rot.field("x").value; qy = rot.field("y").value;
                          qz = rot.field("z").value; qw = rot.field("w").value;
                        }
                      } catch (e) {}
                      entries.push({
                        partIndex: pi,
                        part,
                        dx, dy, dz,
                        qx, qy, qz, qw,
                        parentHandle: null,
                        rootHandle: null,
                        isStuck: null,
                        isAnchor: null,
                        anchorId: null,
                        stickPos: null
                      });
                    }
                    const handleToEntry = {};
                    for (const en of entries) {
                      try {
                        const h = normalizeSceneObjectHandle(en.part);
                        if (h) {
                          handleToEntry[h] = en;
                        }
                      } catch (e) {}
                    }
                    for (const en of entries) {
                      try {
                        const r = en.part.field("_cachedStuckParentGrabbable").value;
                        if (r && !r.isNull?.()) {
                          const h = normalizeSceneObjectHandle(r);
                          if (h && handleToEntry[h]) {
                            en.parentHandle = h;
                          }
                        }
                      } catch (e) {}
                      try {
                        const r = en.part.field("_cachedStuckRootGrabbable").value;
                        if (r && !r.isNull?.()) {
                          const h = normalizeSceneObjectHandle(r);
                          if (h && handleToEntry[h]) {
                            en.rootHandle = h;
                          }
                        }
                      } catch (e) {}
                      try {
                        en.isStuck = en.part.method("get_isStuck").invoke() === true;
                      } catch (e) {}
                      try {
                        const sa = en.part.method("get_stuckAnchor").invoke();
                        en.isAnchor = !!(sa && !sa.isNull?.());
                        if (sa && !sa.isNull?.()) {
                          en.anchorH = normalizeSceneObjectHandle(sa);
                          try {
                            en.anchorParentID = String(sa.field("_parentID").value ?? "").trim();
                          } catch (e) {}
                          try {
                            const so = sa.method("get_stuckObject").invoke();
                            if (so && !so.isNull?.()) {
                              en.anchorStuckObjH = normalizeSceneObjectHandle(so);
                              en.anchorStuckObjId = getGrabbableItemId(so);
                            }
                          } catch (e) {}
                          try {
                            const ap = sa.field("_position").value;
                            if (ap) {
                              en.anchorPos = { x: ap.field("x").value, y: ap.field("y").value, z: ap.field("z").value };
                            }
                          } catch (e) {}
                          try {
                            const ar = sa.field("_rotation").value;
                            if (ar) {
                              en.anchorRot = { x: ar.field("x").value, y: ar.field("y").value, z: ar.field("z").value, w: ar.field("w").value };
                            }
                          } catch (e) {}
                        }
                      } catch (e) {}
                      try {
                        const sa2 = en.part.method("get_stuckAnchor").invoke();
                        const ownSticky = en.part.method("GetComponent", 1).inflate(StickyAnchorClass).invoke();
                        en.hasOwnAnchor = !!(ownSticky && !ownSticky.isNull?.());
                      } catch (e) {}
                      try {
                        const aid = en.part.field("_stuckAnchorId").value;
                        en.anchorIdRaw = String(aid ?? "").trim();
                      } catch (e) {}
                      try {
                        const st = en.part.method("get_stickTransform").invoke();
                        if (st && !st.isNull?.()) {
                          const sp = st.method("get_position").invoke();
                          if (sp) {
                            en.stickPos = {
                              x: sp.field("x").value,
                              y: sp.field("y").value,
                              z: sp.field("z").value
                            };
                          }
                        }
                      } catch (e) {}
                      try {
                        const aid = en.part.field("_stuckAnchorId").value;
                        en.anchorIdRaw = String(aid?.toString?.() ?? aid ?? "");
                      } catch (e) {}
                      try {
                        const sa3 = en.part.method("get_stuckAnchor").invoke();
                        if (sa3 && !sa3.isNull?.()) {
                          try {
                            en.anchorNetId = String(sa3.method("get_Id").invoke()?.toString?.() ?? sa3.method("get_id").invoke()?.toString?.() ?? "");
                          } catch (e) {}
                          try {
                            const pg = sa3.field("_parentGrabbable").value;
                            if (pg && !pg.isNull?.()) {
                              en.anchorParentGrabId = getGrabbableItemId(pg);
                            }
                          } catch (e) {}
                          try {
                            const pgt = sa3.field("_subRoot").value;
                            if (pgt && !pgt.isNull?.()) {
                              en.anchorSubRoot = String(pgt.method("get_name").invoke()?.content ?? pgt.toString());
                            }
                          } catch (e) {}
                        }
                      } catch (e) {}
                    }
                    this.__goopSnapKey = rootKey;
                    this.__goopSnap = {
                      refPos,
                      refRot: (() => {
                        try {
                          const rot = refTF ? refTF.method("get_rotation").invoke() : null;
                          if (rot) {
                            return { x: rot.field("x").value, y: rot.field("y").value, z: rot.field("z").value, w: rot.field("w").value };
                          }
                        } catch (e) {}
                        return null;
                      })(),
                      entries
                    };
                    console.log(`[GoopDupe] snapshot parts=${entries.length} ref=${!!refPos} rootKey=${rootKey}`);
                    for (const en of entries) {
                      console.log(`[GoopDupe]   part#${en.partIndex} id=${getGrabbableItemId(en.part)} off=(${en.dx.toFixed(2)},${en.dy.toFixed(2)},${en.dz.toFixed(2)}) parent=${en.parentHandle ? "Y" : "N"} root=${en.rootHandle ? "Y" : "N"} stuck=${en.isStuck ? "Y" : "N"} anchor=${en.isAnchor ? "Y" : "N"} ownSticky=${en.hasOwnAnchor ? "Y" : "N"} aId="${en.anchorIdRaw || ""}" anchorNetId="${en.anchorNetId || ""}"${en.anchorStuckObjId ? ` ->${en.anchorStuckObjId}` : ""}${en.anchorParentGrabId ? ` parentGrab=${en.anchorParentGrabId}` : ""}${en.anchorSubRoot ? ` subRoot=${en.anchorSubRoot}` : ""}${en.stickPos ? ` stick=(${en.stickPos.x.toFixed(2)},${en.stickPos.y.toFixed(2)},${en.stickPos.z.toFixed(2)})` : ""}${en.anchorPos ? ` aPos=(${en.anchorPos.x.toFixed(2)},${en.anchorPos.y.toFixed(2)},${en.anchorPos.z.toFixed(2)})` : ""}`);
                    }
                    if (!this.__goopDeepDumped) {
                      this.__goopDeepDumped = true;
                      for (const en of entries) {
                        try {
                          console.log(`[GoopDupe] DEEP part#${en.partIndex} id=${getGrabbableItemId(en.part)}`);
                          const p = en.part;
                          const gbo = p.method("GetComponent", 1).inflate(GBOClass).invoke();
                          if (!gbo || gbo.isNull?.()) {
                            continue;
                          }
                          try {
                            console.log(`[GoopDupe]   get_stuckAnchorId=${gbo.method("get_stuckAnchorId").invoke()?.toString?.() ?? "ERR"}`);
                          } catch (e) {}
                          try {
                            console.log(`[GoopDupe]   get_attachedAnchorId=${gbo.method("get_attachedAnchorId").invoke()?.toString?.() ?? "ERR"}`);
                          } catch (e) {}
                          try {
                            console.log(`[GoopDupe]   get_isStuck=${gbo.method("get_isStuck").invoke()} get_isAttached=${gbo.method("get_isAttached").invoke()}`);
                          } catch (e) {}
                          try {
                            console.log(`[GoopDupe]   get_stickToGlue=${gbo.method("get_stickToGlue").invoke()} get_isStuckFrozen=${gbo.method("get_isStuckFrozen").invoke()}`);
                          } catch (e) {}
                          const sa = gbo.method("get_stuckAnchor").invoke();
                          if (sa && !sa.isNull?.()) {
                            console.log(`[GoopDupe]   anchor obj=${sa.toString()}`);
                            try {
                              console.log(`[GoopDupe]   anchor.isSetup=${sa.method("get_isSetup").invoke()}`);
                            } catch (e) {}
                            try {
                              console.log(`[GoopDupe]   anchor.Id=${sa.method("get_Id").invoke()?.toString?.() ?? "ERR"} id=${sa.method("get_id").invoke()?.toString?.() ?? "ERR"}`);
                            } catch (e) {}
                            try {
                              console.log(`[GoopDupe]   anchor.parentID=${sa.field("_parentID").value?.toString?.() ?? "ERR"}`);
                            } catch (e) {}
                            try {
                              const so = sa.method("get_stuckObject").invoke();
                              console.log(`[GoopDupe]   anchor.stuckObject=${so && !so.isNull?.() ? getGrabbableItemId(so) : "NULL"}`);
                            } catch (e) {}
                            try {
                              const at = sa.method("get_transform").invoke();
                              if (at && !at.isNull?.()) {
                                const ap = at.method("get_position").invoke();
                                console.log(`[GoopDupe]   anchor.worldPos=(${ap ? ap.field("x").value.toFixed(2) : "?"},${ap ? ap.field("y").value.toFixed(2) : "?"},${ap ? ap.field("z").value.toFixed(2) : "?"}) name=${at.method("get_name").invoke()?.content ?? "?"}`);
                              }
                            } catch (e) {}
                            try {
                              const pgt = sa.field("_stuckAnchorTransform").value;
                              if (pgt && !pgt.isNull?.()) {
                                console.log(`[GoopDupe]   anchor._stuckAnchorTransform=${pgt.method("get_name").invoke()?.content ?? "?"} pos=${pgt.method("get_position").invoke()?.toString?.() ?? "?"}`);
                              }
                            } catch (e) {}
                          } else {
                            console.log(`[GoopDupe]   anchor=NULL`);
                          }
                          try {
                            const st = gbo.method("get_stickTransform").invoke();
                            if (st && !st.isNull?.()) {
                              const parent = st.method("get_parent").invoke();
                              console.log(`[GoopDupe]   stickTransform name=${st.method("get_name").invoke()?.content ?? "?"} parent=${parent && !parent.isNull?.() ? parent.method("get_name").invoke()?.content ?? "?" : "NULL"}`);
                            }
                          } catch (e) {}
                          try {
                            const parent = p.method("get_transform").invoke().method("get_parent").invoke();
                            console.log(`[GoopDupe]   part.transform.parent=${parent && !parent.isNull?.() ? parent.method("get_name").invoke()?.content ?? "?" : "NULL"}`);
                          } catch (e) {}
                          try {
                            const all = gbo.method("GetComponentsInChildren", 1).inflate(StickyAnchorClass).invoke(true);
                            if (all && !all.isNull?.()) {
                              console.log(`[GoopDupe]   GetComponentsInChildren(StickyAnchor) count=${all.length}`);
                              for (let qi = 0; qi < all.length; qi++) {
                                console.log(`[GoopDupe]     [${qi}] ${all.get(qi).toString()}`);
                              }
                            }
                          } catch (e) {}
                        } catch (e) {
                          console.error("[GoopDupe] deep:", e);
                        }
                      }
                    }
                  }
                  const snap = this.__goopSnap;
                  const nowTF = getTransform(heldPart);
                  const nowPos = nowTF ? nowTF.method("get_position").invoke() : null;
                  let fwdX = 0, fwdY = 0, fwdZ = 1;
                  try {
                    const fwd = nowTF.method("get_forward").invoke();
                    if (fwd) {
                      fwdX = fwd.field("x").value; fwdY = fwd.field("y").value; fwdZ = fwd.field("z").value;
                    }
                  } catch (e) {}
                  const clusterOffset = 0.9;
                  const spawnedMap = {};
                  let spawned = 0;
                  for (const en of snap.entries) {
                    try {
                      const id = getGrabbableItemId(en.part);
                      if (!id) {
                        console.log(`[GoopDupe] part ${en.partIndex} NO-ID`);
                        continue;
                      }
                      const baseX = nowPos ? nowPos.field("x").value : 0;
                      const baseY = nowPos ? nowPos.field("y").value : 0;
                      const baseZ = nowPos ? nowPos.field("z").value : 0;
                      const spawnPos = [
                        baseX + fwdX * clusterOffset + en.dx,
                        baseY + fwdY * clusterOffset + en.dy,
                        baseZ + fwdZ * clusterOffset + en.dz
                      ];
                      let rot = identityQuaternion;
                      try {
                        rot = Quaternion.method("Normalize", 1).invoke([en.qx, en.qy, en.qz, en.qw]);
                      } catch (e) {}
                      const spawnedItem = spawnItemAtPos(id, spawnPos, rot);
                      if (!spawnedItem || spawnedItem.isNull?.()) {
                        continue;
                      }
                      let spawnedGBO = spawnedItem;
                      try {
                        const c = spawnedItem.method("GetComponent", 1).inflate(GBOClass).invoke();
                        if (c && !c.isNull?.()) {
                          spawnedGBO = c;
                        }
                      } catch (e) {}
                      console.log(`[GoopDupe]   spawned part#${en.partIndex} id=${id}`);
                      try {
                        const h = normalizeSceneObjectHandle(en.part);
                        if (h) {
                          spawnedMap[h] = spawnedGBO;
                        }
                      } catch (e) {}
                      try {
                        const src = en.part.method("GetComponent", 1).inflate(GBOClass).invoke();
                        const dst = spawnedGBO.method("GetComponent", 1).inflate(GBOClass).invoke();
                        if (src && !src.isNull?.() && dst && !dst.isNull?.()) {
                          try {
                            dst.method("set_scaleModifier").invoke(src.method("get_scaleModifier").invoke());
                          } catch (e) {}
                          try {
                            dst.method("set_colorHue").invoke(src.method("get_colorHue").invoke());
                          } catch (e) {}
                          try {
                            dst.method("set_colorSaturation").invoke(src.method("get_colorSaturation").invoke());
                          } catch (e) {}
                        }
                      } catch (e) {}
                      spawned++;
                    } catch (e) {}
                  }
                  for (const en of snap.entries) {
                    try {
                      const srcH = normalizeSceneObjectHandle(en.part);
                      const dst = srcH && spawnedMap[srcH];
                      if (!dst || dst.isNull?.()) {
                        continue;
                      }
                      if (en.parentHandle) {
                        try {
                          const parentDst = spawnedMap[en.parentHandle];
                          if (parentDst && !parentDst.isNull?.()) {
                            dst.field("_cachedStuckParentGrabbable").value = parentDst;
                          }
                        } catch (e) {}
                      }
                      if (en.rootHandle) {
                        try {
                          const rootDst = spawnedMap[en.rootHandle];
                          if (rootDst && !rootDst.isNull?.()) {
                            dst.field("_cachedStuckRootGrabbable").value = rootDst;
                          }
                        } catch (e) {}
                      } else {
                        try {
                          const rootSrc = en.part.field("_cachedStuckRootGrabbable").value;
                          if (rootSrc && !rootSrc.isNull?.()) {
                            const rootH = normalizeSceneObjectHandle(rootSrc);
                            const rootDst = rootH && spawnedMap[rootH];
                            if (rootDst && !rootDst.isNull?.()) {
                              dst.field("_cachedStuckRootGrabbable").value = rootDst;
                            }
                          }
                        } catch (e) {}
                      }
                    } catch (e) {}
                  }
                  if (!this.__goopSpawnDumped) {
                    this.__goopSpawnDumped = true;
                    for (const en of snap.entries) {
                      try {
                        const srcH = normalizeSceneObjectHandle(en.part);
                        const dst = srcH && spawnedMap[srcH];
                        if (!dst || dst.isNull?.()) {
                          continue;
                        }
                        const did = getGrabbableItemId(dst);
                        console.log(`[GoopDupe] SPAWNED part#${en.partIndex} id=${did}`);
                        try {
                          console.log(`[GoopDupe]   isStuck=${dst.method("get_isStuck").invoke()} stickToGlue=${dst.method("get_stickToGlue").invoke()}`);
                        } catch (e) {}
                        try {
                          console.log(`[GoopDupe]   get_stuckAnchorId=${dst.method("get_stuckAnchorId").invoke()?.toString?.() ?? "ERR"}`);
                        } catch (e) {}
                        try {
                          const sa = dst.method("get_stuckAnchor").invoke();
                          console.log(`[GoopDupe]   get_stuckAnchor=${sa && !sa.isNull?.() ? sa.toString() : "NULL"}`);
                        } catch (e) {}
                        try {
                          const own = dst.method("GetComponent", 1).inflate(StickyAnchorClass).invoke();
                          console.log(`[GoopDupe]   GetComponent(StickyAnchor)=${own && !own.isNull?.() ? own.toString() : "NULL"}`);
                        } catch (e) {}
                        try {
                          const kids = dst.method("GetComponentsInChildren", 1).inflate(StickyAnchorClass).invoke(true);
                          console.log(`[GoopDupe]   GetComponentsInChildren(StickyAnchor) count=${kids && !kids.isNull?.() ? kids.length : "ERR"}`);
                        } catch (e) {}
                      } catch (e) {}
                    }
                  }
                  if (spawned > 0 && (!this.__goopDupeAt || !(time < this.__goopDupeAt))) {
                    this.__goopDupeAt = time + 0.5;
                    sendNotification(`Goop dupe: ${spawned}/${snap.entries.length}`, false);
                  }
                  console.log(`[GoopDupe] parts=${snap.entries.length} spawned=${spawned}`);
                } catch (e) {
                  console.error("[GoopDupe]", e);
                }
              },
              disableMethod: function () {}
            }), new ButtonInfo({
              buttonText: "Backpack Dupe",
              method: () => backpackDupeEnabled = true,
              disableMethod: () => backpackDupeEnabled = false,
              toolTip: "Any item you dump out of a backpack will stay in the bag"
            }), new ButtonInfo({
              buttonText: "Piss Mod (color broke)",
              enableMethod: () => {
                goopSpamDelay = 0;
                sendNotification("Piss Mod ON", false);
              },
              disableMethod: () => {
                sendNotification("Piss Mod OFF", false);
              },
              isTogglable: true,
              method: () => {
                if (!(time <= goopSpamDelay)) {
                  goopSpamDelay = time + 0.055;
                  try {
                    spawnGoopBurstAtTransform(getTransform(bodyCollider), 56, 100, 1, 2.2, 5.2);
                  } catch (e) {
                    console.error("Piss Mod:", e);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Blind Player (gun)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (!t || t.handle.isNull()) {
                  return;
                }
                const o = ["item_goopfish", "item_goopfish", "item_goopfish", "item_goopfish", "item_goopfish", "item_goopfish", "item_goopfish", "item_goopfish", "item_goopfish", "item_goopfish"];
                if (rightTrigger) {
                  const e = getTransform(GunPointer).method("get_position").invoke();
                  const t = 3 + Math.floor(Math.random() * 3);
                  for (let o = 0; o < t; o++) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(""), e, identityQuaternion, NULL, 0);
                  }
                  for (const t of o) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(t), e, identityQuaternion, NULL, 0);
                  }
                }
              },
              toolTip: "Spawns items where you aim (hold grip, pull trigger)."
            }), new ButtonInfo({
              buttonText: "Spawn Prune Juice (gun) ",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  Math.floor(Math.random() * 3);
                  PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string("item_prune_juice"), getTransform(GunPointer).method("get_position").invoke(), identityQuaternion, NULL, 0);
                }
              },
              toolTip: "Spawns items where you aim (hold grip, pull trigger)."
            }), new ButtonInfo({
              buttonText: "Spawn Pumpkins (gun)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (!t || t.handle.isNull()) {
                  return;
                }
                const o = ["item_pumpkinjack", "item_pumpkinjack_small", "item_pumpkinjack", "item_pumpkinjack_small"];
                if (rightTrigger) {
                  const e = getTransform(GunPointer).method("get_position").invoke();
                  const t = 3 + Math.floor(Math.random() * 3);
                  for (let o = 0; o < t; o++) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(""), e, identityQuaternion, NULL, 0);
                  }
                  for (const t of o) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(t), e, identityQuaternion, NULL, 0);
                  }
                }
              },
              toolTip: "Spawns items where you aim (hold grip, pull trigger)."
            }), new ButtonInfo({
              buttonText: "Spawn Suitcases (gun)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (!t || t.handle.isNull()) {
                  return;
                }
                const o = ["item_pelican_case", "item_pelican_case", "item_pelican_case"];
                if (rightTrigger) {
                  const e = getTransform(GunPointer).method("get_position").invoke();
                  const t = 3 + Math.floor(Math.random() * 3);
                  for (let o = 0; o < t; o++) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(""), e, identityQuaternion, NULL, 0);
                  }
                  for (const t of o) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(t), e, identityQuaternion, NULL, 0);
                  }
                }
              },
              toolTip: "Spawns items where you aim (hold grip, pull trigger)."
            }), new ButtonInfo({
              buttonText: "Spawn All Modules (gun)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (!t || t.handle.isNull()) {
                  return;
                }
                const o = ["item_module_gun_1", "item_module_gun_2", "item_module_gun_3", "item_module_hull_1", "item_module_hull_2", "item_module_hull_3", "item_module_joust_1", "item_module_joust_2", "item_module_joust_3", "item_module_laser_1", "item_module_laser_2", "item_module_laser_3", "item_module_tether_1", "item_module_tractor_beam_1", "item_module_stasis_1", "item_module_blast_1", "item_module_blast_2", "item_module_blast_3", "item_module_boost_1", "item_module_boost_2", "item_module_boost_3", "item_module_gravity_1", "item_module_minigun_1", "item_module_minigun_2", "item_module_minigun_3"];
                if (rightTrigger) {
                  const e = getTransform(GunPointer).method("get_position").invoke();
                  const t = 3 + Math.floor(Math.random() * 3);
                  for (let o = 0; o < t; o++) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(""), e, identityQuaternion, NULL, 0);
                  }
                  for (const t of o) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(t), e, identityQuaternion, NULL, 0);
                  }
                }
              },
              toolTip: "Spawns items where you aim (hold grip, pull trigger)."
            }), new ButtonInfo({
              buttonText: "Spawn All Crates (gun)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (!t || t.handle.isNull()) {
                  return;
                }
                const o = ["item_randombox_mobloot_big", "item_randombox_mobloot_medium", "item_randombox_mobloot_small", "item_randombox_mobloot_weapons", "item_randombox_mobloot_weapons_big", "item_randombox_mobloot_zombie", "item_randombox_modules"];
                if (rightTrigger) {
                  const e = getTransform(GunPointer).method("get_position").invoke();
                  const t = 3 + Math.floor(Math.random() * 3);
                  for (let o = 0; o < t; o++) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(""), e, identityQuaternion, NULL, 0);
                  }
                  for (const t of o) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(t), e, identityQuaternion, NULL, 0);
                  }
                }
              },
              toolTip: "Spawns items where you aim (hold grip, pull trigger)."
            }), new ButtonInfo({
              buttonText: "Spawn All OG Items (gun)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (!t || t.handle.isNull()) {
                  return;
                }
                const o = ["item_apple", "item_backpack_black", "item_backpack_green", "item_backpack_pink", "item_backpack_white", "item_backpack_with_flashlight", "item_cracker", "item_heartchocolatebox", "item_pumpkin_pie", "item_pumpkinjack", "item_pumpkinjack_small", "item_randombox_mobloot_weapons", "item_randombox_mobloot_zombie", "item_scanner", "item_timebomb", "item_zombie_meat"];
                if (rightTrigger) {
                  const e = getTransform(GunPointer).method("get_position").invoke();
                  const t = 3 + Math.floor(Math.random() * 3);
                  for (let o = 0; o < t; o++) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(""), e, identityQuaternion, NULL, 0);
                  }
                  for (const t of o) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(t), e, identityQuaternion, NULL, 0);
                  }
                }
              },
              toolTip: "Spawns items where you aim (hold grip, pull trigger)."
            }), new ButtonInfo({
              buttonText: "Spawn All TADC Quest Items (gun)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (!t || t.handle.isNull()) {
                  return;
                }
                const o = ["item_blox_cube", "item_blox_moon", "item_blox_sphere", "item_blox_star", "item_blox_triangle", "item_ringmaster_staff", "item_deadmans_draw_comedy_drama"];
                if (rightTrigger) {
                  const e = getTransform(GunPointer).method("get_position").invoke();
                  const t = 3 + Math.floor(Math.random() * 3);
                  for (let o = 0; o < t; o++) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(""), e, identityQuaternion, NULL, 0);
                  }
                  for (const t of o) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(t), e, identityQuaternion, NULL, 0);
                  }
                }
              },
              toolTip: "Spawns items where you aim (hold grip, pull trigger)."
            }), new ButtonInfo({
              buttonText: "Spawn All OG Bags (gun)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (!t || t.handle.isNull()) {
                  return;
                }
                const o = ["item_backpack_black", "item_backpack_green", "item_backpack_pink", "item_backpack_white", "item_backpack_with_flashlight"];
                if (rightTrigger) {
                  const e = getTransform(GunPointer).method("get_position").invoke();
                  const t = 3 + Math.floor(Math.random() * 3);
                  for (let o = 0; o < t; o++) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(""), e, identityQuaternion, NULL, 0);
                  }
                  for (const t of o) {
                    PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string(t), e, identityQuaternion, NULL, 0);
                  }
                }
              },
              toolTip: "Spawns items where you aim (hold grip, pull trigger)."
            }), new ButtonInfo({
              buttonText: "Set Shredder Nuts",
              method: () => {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.ItemShredder");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                  let o = 0;
                  if (t && !t.isNull()) {
                    for (let e = 0; e < t.length; e++) {
                      try {
                        t.get(e).method("set_shreddedValue").invoke(customShredderNuts);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Set " + o + " shredder(s) to " + customShredderNuts + " nuts", false);
                } catch (e) {
                  sendNotification("Shredder error: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets nut count on all shredders in scene. Adjust customShredderNuts in console."
            }), new ButtonInfo({
              buttonText: "Max out ALL shredders",
              method: () => {
                sendNotification("Shredder nuts: " + (customShredderNuts = Math.min(customShredderNuts + 300000, 300000)), false);
              },
              isTogglable: false,
              toolTip: "Increases shredder nut value by 1000."
            }), new ButtonInfo({
              buttonText: "Spawn Shredders",
              isTogglable: true,
              enableMethod: () => {
                sendNotification("Spawn Full Shredder ON", false);
              },
              disableMethod: () => {
                sendNotification("Spawn Full Shredder OFF", false);
              },
              method: () => {
                try {
                  const e = spawnItemAtPos("item_shredder", Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), [0, 0.3, 0]), identityQuaternion);
                  if (!e || e.isNull()) {
                    sendNotification("Shredder spawn failed", false);
                    return;
                  }
                  const t = AssemblyCSharp.class("AnimalCompany.ItemShredder");
                  const o = e.method("GetComponent", 1).inflate(t).invoke();
                  if (o && !o.isNull()) {
                    const e = t.field("BASE_FILL_CAPACITY").value;
                    try {
                      o.method("set_shreddedValue").invoke(customShredderNuts);
                    } catch (e) {}
                    try {
                      o.field("_currFill").value = e;
                    } catch (e) {}
                    try {
                      o.field("_prevFill").value = e;
                    } catch (e) {}
                    try {
                      o.field("_transitionFill").value = e;
                    } catch (e) {}
                  }
                  sendNotification("Spawned full shredder " + customShredderNuts + " nuts", false);
                } catch (e) {
                  sendNotification("Shredder spawn error: " + e, false);
                  console.error("[FullShredder]", e);
                }
              },
              toolTip: "Spawns a full shredder at your hand. Nut value uses customShredderNuts."
            }), new ButtonInfo({
              buttonText: "Spawn Full Shredder",
              isTogglable: true,
              enableMethod: () => {
                sendNotification("Spawn Full Shredder ON", false);
              },
              disableMethod: () => {
                sendNotification("Spawn Full Shredder OFF", false);
              },
              method: () => {
                try {
                  customShredderNuts = Math.min(customShredderNuts + 300000, 300000);
                  const e = spawnItemAtPos("item_shredder", Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), [0, 0.3, 0]), identityQuaternion);
                  if (!e || e.isNull()) {
                    sendNotification("Shredder spawn failed", false);
                    return;
                  }
                  const t = AssemblyCSharp.class("AnimalCompany.ItemShredder");
                  const o = Object.method("FindObjectsByType", 1).inflate(t).invoke(0);
                  let n = 0;
                  if (o && !o.isNull()) {
                    for (let e = 0; e < o.length; e++) {
                      try {
                        o.get(e).method("set_shreddedValue").invoke(customShredderNuts);
                        n++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Spawned full shredder, set " + n + " shredder(s) to " + customShredderNuts + " nuts", false);
                } catch (e) {
                  sendNotification("Shredder spawn error: " + e, false);
                  console.error("[FullShredder]", e);
                }
              },
              toolTip: "Spawns a shredder, maxes customShredderNuts, and applies it to all shredders in scene."
            }), new ButtonInfo({
              buttonText: "Crossbow Dupe",
              isTogglable: true,
              method: () => {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.Quiver");
                  if (!e) {
                    return;
                  }
                  const t = e.method("TryGetNextItemOut");
                  if (!t) {
                    return;
                  }
                  t.implementation = function (e) {
                    return false;
                  };
                } catch (e) {}
              },
              toolTip: "crossbow dupe"
            }), new ButtonInfo({
              buttonText: "Random Hand Duper",
              method: () => {
                if (rightSecondary) {
                  try {
                    const t = NetPlayer.method("get_localPlayer").invoke();
                    if (!t) {
                      return;
                    }
                    const o = t.method("GetHandInteractor", 1).invoke(0);
                    if (!o) {
                      return;
                    }
                    const n = o.field("_itemAnchor").value;
                    if (!n) {
                      return;
                    }
                    const i = n.method("get_grabbableObject").invoke();
                    if (!i || i.isNull()) {
                      return;
                    }
                    let a = "item_treestick";
                    const l = i.method("get_gameObject").invoke().method("GetComponent", 1).inflate(GBIClass).invoke();
                    if (l && !l.isNull()) {
                      const e = l.method("get_itemID").invoke();
                      if (e && !e.isNull()) {
                        a = e.content;
                      }
                    }
                    const r = leftHandTransform.method("get_position").invoke();
                    const s = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + a), r, identityQuaternion, NULL);
                    if (!s || s.isNull()) {
                      return;
                    }
                    const c = i.method("GetComponent", 1).inflate(GBOClass).invoke();
                    let d = e(-128, 127);
                    let u = e(-127, 127);
                    let m = e(-20, 127);
                    if (c && !c.isNull()) {
                      d = c.method("get_scaleModifier").invoke();
                      u = c.method("get_colorHue").invoke();
                      m = c.method("get_colorSaturation").invoke();
                    }
                    const h = s.method("GetComponent", 1).inflate(GBOClass).invoke();
                    if (h && !h.isNull()) {
                      h.method("set_scaleModifier").invoke(d);
                      h.method("set_colorHue").invoke(u);
                      h.method("set_colorSaturation").invoke(m);
                      const e = getLaunchForward(leftHandTransform);
                      const t = Vector3.method("op_Multiply", 2).invoke(e, 10);
                      h.method("AddExternalForceVelocity", 1).invoke(t);
                    }
                    try {
                      const e = i.method("GetComponent", 1).inflate(BackpackItemClass).invoke();
                      if (e && !e.isNull()) {
                        const t = s.method("GetComponent", 1).inflate(BackpackItemClass).invoke();
                        const o = e.method("GetContainedItems").invoke();
                        if (o && !o.isNull() && t && !t.isNull()) {
                          const e = o.method("get_Count").invoke();
                          const n = leftHandTransform.method("get_position").invoke();
                          for (let i = 0; i < e; i++) {
                            try {
                              const e = o.method("get_Item").invoke(i);
                              if (!e || e.isNull()) {
                                continue;
                              }
                              const a = e.method("get_itemID").invoke();
                              if (!a || a.isNull()) {
                                continue;
                              }
                              let l = 0;
                              let r = 0;
                              let s = 0;
                              try {
                                const t = e.method("GetComponent", 1).inflate(GBOClass).invoke();
                                if (t && !t.isNull()) {
                                  l = t.method("get_scaleModifier").invoke();
                                  r = t.method("get_colorHue").invoke();
                                  s = t.method("get_colorSaturation").invoke();
                                }
                              } catch (e) {}
                              const c = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + a.content), n, identityQuaternion, NULL);
                              if (!c || c.isNull()) {
                                continue;
                              }
                              const d = c.method("GetComponent", 1).inflate(GBOClass).invoke();
                              if (d && !d.isNull()) {
                                d.method("set_scaleModifier").invoke(l);
                                d.method("set_colorHue").invoke(r);
                                d.method("set_colorSaturation").invoke(s);
                              }
                              const u = c.method("GetComponent", 1).inflate(GBIClass).invoke();
                              if (u && !u.isNull()) {
                                u.method("AddToBag").invoke(t);
                              }
                            } catch (e) {}
                          }
                          sendNotification("Duped bag + " + e + " items (colors saved)", false);
                        }
                      }
                    } catch (e) {
                      console.error("Bag children copy error:", e);
                    }
                  } catch (e) {
                    console.error("Random Hand Duper error:", e);
                  }
                }
                function e(e, t) {
                  return Math.floor(Math.random() * (t - e + 1)) + e;
                }
              },
              isTogglable: true,
              toolTip: "Dupe item held in left hand with random config (Hold B)."
            }), new ButtonInfo({
              buttonText: "Bag Inf Capacity",
              enableMethod: () => {
                try {
                  BackpackItemClass.method("get_capacity").implementation = function () {
                    return 255;
                  };
                  BackpackItemClass.method("get_capacitySafe").implementation = function () {
                    return 255;
                  };
                  sendNotification("Bag Inf Capacity ON", false);
                } catch (e) {
                  sendNotification("Bag Cap: " + e, false);
                }
              },
              disableMethod: () => {
                try {
                  BackpackItemClass.method("get_capacity").implementation = null;
                  BackpackItemClass.method("get_capacitySafe").implementation = null;
                  sendNotification("Bag Inf Capacity OFF", false);
                } catch (e) {}
              },
              isTogglable: true,
              toolTip: "Backpacks report 255 capacity. Equivalent to BackpackCapacity skill maxed."
            }), new ButtonInfo({
              buttonText: "Quiver Inf Capacity",
              enableMethod: () => {
                try {
                  QuiverClass.method("get_capacity").implementation = function () {
                    return 255;
                  };
                  sendNotification("Quiver Inf Capacity ON", false);
                } catch (e) {
                  sendNotification("Quiver Cap: " + e, false);
                }
              },
              disableMethod: () => {
                try {
                  QuiverClass.method("get_capacity").implementation = null;
                  sendNotification("Quiver Inf Capacity OFF", false);
                } catch (e) {}
              },
              isTogglable: true,
              toolTip: "Quivers report 255 capacity. Equivalent to QuiverCapacity skill maxed."
            }), new ButtonInfo({
              buttonText: "Bag Bomb",
              method: () => {
                if (rightSecondary) {
                  try {
                    const t = leftHandTransform.method("get_position").invoke();
                    const o = ["item_backpack_large_clover", "item_backpack_large_basketball", "item_backpack_large_base"];
                    const n = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + o[e(0, o.length - 1)]), t, identityQuaternion, NULL);
                    if (!n || n.isNull()) {
                      return;
                    }
                    const i = n.method("GetComponent", 1).inflate(GBOClass).invoke();
                    if (i && !i.isNull()) {
                      i.method("set_scaleModifier").invoke(e(-128, 127));
                      i.method("set_colorHue").invoke(e(-127, 127));
                      i.method("set_colorSaturation").invoke(e(-20, 127));
                    }
                    const a = n.method("GetComponent", 1).inflate(BackpackItemClass).invoke();
                    let l = 0;
                    let r = 0;
                    while (l < 23 && r < 60) {
                      r++;
                      try {
                        const o = itemIDs[e(0, itemIDs.length - 1)];
                        const n = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + o), t, identityQuaternion, NULL);
                        if (!n || n.isNull()) {
                          continue;
                        }
                        const i = n.method("GetComponent", 1).inflate(GBOClass).invoke();
                        if (i && !i.isNull()) {
                          i.method("set_scaleModifier").invoke(e(-128, 127));
                          i.method("set_colorHue").invoke(e(-127, 127));
                          i.method("set_colorSaturation").invoke(e(-20, 127));
                        }
                        if (a && !a.isNull()) {
                          let e = false;
                          try {
                            const t = n.method("GetComponent", 1).inflate(GBIClass).invoke();
                            if (t && !t.isNull()) {
                              t.method("AddToBag").invoke(a);
                              e = true;
                            }
                          } catch (e) {}
                          if (!e) {
                            try {
                              const e = n.method("GetComponent", 1).inflate(GBIClass).invoke();
                              if (e && !e.isNull()) {
                                e.method("AddToBagInternal").invoke(a);
                              }
                            } catch (e) {}
                          }
                        }
                        l++;
                      } catch (e) {}
                    }
                    sendNotification("Spawned bag with " + l + " items!", false);
                  } catch (e) {
                    console.error("Bag Bomb error:", e);
                  }
                }
                function e(e, t) {
                  return Math.floor(Math.random() * (t - e + 1)) + e;
                }
              },
              isTogglable: true,
              toolTip: "Spawns a random colored bag with 23 items (hold B)."
            }), new ButtonInfo({
              buttonText: "Cash Quiver Hand",
              method: () => {
                try {
                  const e = rightHandTransform.method("get_position").invoke();
                  const t = rightHandTransform.method("get_rotation").invoke();
                  const o = spawnItemAtPos("item_quiver", e, t);
                  if (!o || o.isNull?.()) {
                    sendNotification("Quiver spawn failed", false);
                    return;
                  }
                  const n = spawnItemAtPos("item_fish_dollar_bill", e, t);
                  if (n && !n.isNull?.()) {
                    try {
                      const e = o.method("GetComponent", 1).inflate(QuiverClass).invoke();
                      const t = n.method("GetComponent", 1).inflate(GBIClass).invoke();
                      if (e && !e.isNull?.() && t && !t.isNull?.()) {
                        try {
                          t.method("AddToBag").invoke(e);
                        } catch (o) {
                          try {
                            t.method("AddToBagInternal").invoke(e);
                          } catch (e) {}
                        }
                      }
                    } catch (e) {}
                  }
                  sendNotification("Spawned cash quiver", false);
                } catch (e) {
                  console.error("Cash Quiver Hand:", e);
                }
              },
              isTogglable: false,
              toolTip: "Spawns a quiver in your hand position with a cash pile style item inside it."
            }), new ButtonInfo({
              buttonText: "Left Hand → Backpack",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.isNull?.()) {
                    sendNotification("No local player", false);
                    return;
                  }
                  const t = getPlayerHeldGrabbable(e, 0);
                  if (!t || t.isNull?.()) {
                    sendNotification("No left-hand item", false);
                    return;
                  }
                  let o = null;
                  try {
                    o = findLocalBackpack(e);
                  } catch (e) {}
                  if (!o || o.isNull?.()) {
                    try {
                      o = Object.method("FindObjectOfType", 0).inflate(BackpackItemClass).invoke();
                    } catch (e) {}
                  }
                  if (!o || o.isNull?.()) {
                    try {
                      o = Object.method("FindObjectOfType", 0).inflate(QuiverClass).invoke();
                    } catch (e) {}
                  }
                  if (!o || o.isNull?.()) {
                    sendNotification("No backpack or container found", false);
                    return;
                  }
                  let n = false;
                  try {
                    tryReleaseHeldItem(t);
                    const i = e.method("GetHandInteractor", 1).invoke(0);
                    if (i && !i.isNull?.()) {
                      tryReleaseHeldItem(i);
                    }
                    let a = null;
                    try {
                      a = t.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    } catch (e) {}
                    if (!a || a.isNull?.()) {
                      try {
                        a = t.method("GetComponent", 1).inflate(GBIClass).invoke();
                      } catch (e) {}
                    }
                    if (a && !a.isNull()) {
                      try {
                        a.method("AddToBag").invoke(o);
                        n = true;
                      } catch (e) {
                        try {
                          a.method("AddToBagInternal").invoke(o);
                          n = true;
                        } catch (e) {
                          n = false;
                        }
                      }
                    }
                  } catch (e) {
                    n = false;
                  }
                  if (!n) {
                    sendNotification("Failed to add left-hand item", false);
                    return;
                  }
                  tryReleaseHeldItem(t);
                  const i = e.method("GetHandInteractor", 1).invoke(0);
                  if (i && !i.isNull?.()) {
                    tryReleaseHeldItem(i);
                  }
                  tryReleaseHeldItem(e);
                  sendNotification("Moved left-hand item into container", false);
                } catch (e) {
                  console.error("Left Hand → Backpack:", e);
                  sendNotification("Left-hand bag add failed: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Moves the item in your left hand into your backpack, quiver, or nearest container."
            }), new ButtonInfo({
              buttonText: "Held Item Infinite Sell",
              method: () => {
                try {
                  const e = getLocalHeldGrabbable(true);
                  if (!e || e.isNull?.()) {
                    sendNotification("No held item", false);
                    return;
                  }
                  let t = null;
                  try {
                    t = e.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                  } catch (e) {
                    t = null;
                  }
                  if (!t || t.isNull?.()) {
                    try {
                      t = e.method("GetComponent", 1).inflate(GBIClass).invoke();
                    } catch (e) {
                      t = null;
                    }
                  }
                  if (!t || t.isNull?.()) {
                    sendNotification("Held item has no GrabbableItem", false);
                    return;
                  }
                  try {
                    t.method("SetAdditionalSellValue").invoke(9999999);
                  } catch (e) {}
                  try {
                    t.method("set_sellValue").invoke(9999999);
                  } catch (e) {}
                  tryCallNames(t, ["set_sellValue", "SetSellValue", "SetAdditionalSellValue"], 1, 9999999);
                  sendNotification("Held item sell value set to " + t.method("get_sellValue").invoke(), false);
                } catch (e) {
                  console.error("Held Item Infinite Sell:", e);
                  sendNotification("Failed to set held item sell value", false);
                }
              },
              isTogglable: false,
              toolTip: "Sets the current held item to infinite sell value (9999999)."
            }), new ButtonInfo({
              buttonText: "Add to Bag",
              method: () => {
                try {
                  let e = function (e, t) {
                    return Math.floor(Math.random() * (t - e + 1)) + e;
                  };
                  let t = function (t, o) {
                    let n = 0;
                    const i = t.method("get_transform").invoke().method("get_position").invoke();
                    for (let a = 0; a < o; a++) {
                      try {
                        const o = itemIDs[e(0, itemIDs.length - 1)];
                        const a = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + o), i, identityQuaternion, NULL);
                        if (!a || a.isNull()) {
                          continue;
                        }
                        const l = a.method("get_gameObject").invoke();
                        try {
                          const t = l.method("GetComponent", 1).inflate(GBOClass).invoke();
                          if (t && !t.isNull()) {
                            t.method("set_scaleModifier").invoke(e(-128, 127));
                            t.method("set_colorHue").invoke(e(-127, 127));
                            t.method("set_colorSaturation").invoke(e(-20, 127));
                          }
                        } catch (e) {}
                        let r = false;
                        try {
                          const e = l.method("GetComponent", 1).inflate(GBIClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("AddToBag").invoke(t);
                            r = true;
                          }
                        } catch (e) {}
                        if (!r) {
                          try {
                            const e = l.method("GetComponent", 1).inflate(GBIClass).invoke();
                            if (e && !e.isNull()) {
                              e.method("AddToBagInternal").invoke(t);
                            }
                          } catch (e) {}
                        }
                        n++;
                      } catch (e) {}
                    }
                    return n;
                  };
                  let o = 0;
                  try {
                    const e = Object.method("FindObjectsByType", 1).inflate(BackpackItemClass).invoke(0);
                    if (e && !e.isNull()) {
                      for (let n = 0; n < e.length; n++) {
                        try {
                          const i = e.get(n);
                          if (!i || i.isNull()) {
                            continue;
                          }
                          o += t(i, 23);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                  try {
                    const e = Object.method("FindObjectsByType", 1).inflate(QuiverClass).invoke(0);
                    if (e && !e.isNull()) {
                      for (let n = 0; n < e.length; n++) {
                        try {
                          const i = e.get(n);
                          if (!i || i.isNull()) {
                            continue;
                          }
                          o += t(i, 23);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                  if (CrossbowClass) {
                    try {
                      const e = Object.method("FindObjectsByType", 1).inflate(CrossbowClass).invoke(0);
                      if (e && !e.isNull()) {
                        for (let n = 0; n < e.length; n++) {
                          try {
                            const i = e.get(n);
                            if (!i || i.isNull()) {
                              continue;
                            }
                            o += t(i, 5);
                          } catch (e) {}
                        }
                      }
                    } catch (e) {}
                  }
                  try {
                    const e = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                    if (e && !e.isNull()) {
                      for (let n = 0; n < e.length; n++) {
                        try {
                          const i = e.get(n);
                          if (!i || i.isNull()) {
                            continue;
                          }
                          let a = false;
                          try {
                            i.method("AddToBagAck");
                            a = true;
                          } catch (e) {}
                          if (!a) {
                            continue;
                          }
                          let l = false;
                          try {
                            i.method("GetComponent", 1).inflate(BackpackItemClass).invoke();
                            l = true;
                          } catch (e) {}
                          try {
                            i.method("GetComponent", 1).inflate(QuiverClass).invoke();
                            l = true;
                          } catch (e) {}
                          if (!l) {
                            o += t(i, 5);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                  sendNotification("Stuffed " + o + " items into all bags, quivers & containers!", false);
                } catch (e) {
                  sendNotification("Add to Bag: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Fills ALL bags and quivers in scene with random items (random color + size)."
            }), new ButtonInfo({
              buttonText: "Item Rain",
              enableMethod: () => {
                itemRainDelay = 0;
                sendNotification("Item Rain ON", false);
              },
              disableMethod: () => {
                sendNotification("Item Rain OFF", false);
              },
              isTogglable: true,
              method: () => {
                if (!(time <= itemRainDelay)) {
                  itemRainDelay = time + 0.08;
                  try {
                    const e = getTransform(headCollider).method("get_position").invoke();
                    const t = [e.field("x").value + (Math.random() * 8 - 4), e.field("y").value + 9 + Math.random() * 4, e.field("z").value + (Math.random() * 8 - 4)];
                    spawnItemAtPos(itemIDs[itemIndex], t, identityQuaternion);
                  } catch (e) {
                    console.error("Item Rain:", e);
                  }
                }
              },
              toolTip: "Rains the currently selected Item ID above your head while enabled."
            }), new ButtonInfo({
              buttonText: "Item Tornado",
              enableMethod: () => {
                itemTornadoEntries = [];
                sendNotification("Item Tornado ON", false);
              },
              disableMethod: () => {
                for (const e of itemTornadoEntries) {
                  try {
                    const t = e?.object;
                    if (!t || t.isNull?.()) {
                      continue;
                    }
                    let o = t;
                    try {
                      const e = t.method("get_gameObject").invoke();
                      if (e && !e.isNull?.()) {
                        o = e;
                      }
                    } catch (e) {}
                    Destroy(o);
                  } catch (e) {}
                }
                itemTornadoEntries = [];
                sendNotification("Item Tornado OFF", false);
              },
              isTogglable: true,
              method: () => {
                try {
                  const e = getTransform(bodyCollider).method("get_position").invoke();
                  while (itemTornadoEntries.length < 34) {
                    const t = spawnItemAtPos(itemIDs[Math.floor(Math.random() * itemIDs.length)], [e.field("x").value + (Math.random() * 3.2 - 1.6), e.field("y").value + 0.6 + Math.random() * 6.5, e.field("z").value + (Math.random() * 3.2 - 1.6)], identityQuaternion);
                    if (!t || t.isNull?.()) {
                      break;
                    }
                    itemTornadoEntries.push({
                      object: t,
                      angle: Math.random() * Math.PI * 2,
                      radius: 2.2 + Math.random() * 3.8,
                      height: 0.2 + Math.random() * 9.6,
                      spin: 1.6 + Math.random() * 2.6
                    });
                  }
                  for (let t = itemTornadoEntries.length - 1; t >= 0; t--) {
                    const o = itemTornadoEntries[t];
                    if (!o || !o.object || o.object.isNull?.()) {
                      itemTornadoEntries.splice(t, 1);
                      continue;
                    }
                    o.angle += deltaTime * o.spin * 4.2;
                    const n = e.field("y").value;
                    const i = n + 0.4 + (o.height + time * (1.05 + o.spin * 0.18) + t * 0.38) % 10.6;
                    const a = 1 + Math.max(0, i - n) * 0.44 + Math.sin(time * 2.9 + t) * 0.22;
                    const l = e.field("x").value + Math.cos(o.angle) * a;
                    const r = e.field("z").value + Math.sin(o.angle) * a;
                    try {
                      getTransform(o.object).method("set_position").invoke([l, i, r]);
                    } catch (e) {}
                    trySetObjectVelocity(o.object, [Math.cos(o.angle + 0.3) * (4.8 + a * 0.7), 0.55 + Math.sin(time * 1.7 + t) * 0.3, Math.sin(o.angle + 0.3) * (4.8 + a * 0.7)]);
                    try {
                      o.object.method("set_colorHue").invoke(Math.floor(Math.sin(time + t) * 110));
                    } catch (e) {}
                    try {
                      o.object.method("set_colorSaturation").invoke(Math.floor(Math.cos(time * 1.2 + t) * 90));
                    } catch (e) {}
                    try {
                      o.object.method("set_scaleModifier").invoke(Math.floor(Math.sin(time * 0.9 + t) * 45));
                    } catch (e) {}
                  }
                } catch (e) {
                  console.error("Item Tornado:", e);
                }
              },
              toolTip: "Spawns a much larger spinning tornado of colorful items around you."
            }), new ButtonInfo({
              buttonText: "Random ALL Items",
              enableMethod: () => {
                lagAllItemsDelay = 0;
                sendNotification("Lag All Items ON", false);
              },
              disableMethod: () => {
                sendNotification("Lag All Items OFF", false);
              },
              isTogglable: true,
              method: () => {
                if (!(time <= lagAllItemsDelay)) {
                  lagAllItemsDelay = time + 0.06;
                  try {
                    let e = 0;
                    for (const t of collectAllLobbyItemsWithHeld()) {
                      if (!t || t.isNull?.()) {
                        continue;
                      }
                      const o = Math.floor(Math.sin(time * 1.8 + e * 0.73) * 127);
                      const n = Math.floor(45 + Math.sin(time * 1.3 + e * 0.51) * 82);
                      const i = Math.floor(Math.sin(time * 1.1 + e * 0.67) * 70);
                      try {
                        let e = t;
                        try {
                          const o = t.method("GetComponent", 1).inflate(GBOClass).invoke();
                          if (o && !o.isNull?.()) {
                            e = o;
                          }
                        } catch (e) {}
                        e.method("set_colorHue").invoke(o);
                        e.method("set_colorSaturation").invoke(n);
                        e.method("set_scaleModifier").invoke(Math.max(-35, Math.min(95, i)));
                      } catch (e) {}
                      e++;
                    }
                  } catch (e) {
                    console.error("Lag All Items:", e);
                  }
                }
              },
              toolTip: "The old heavy random-all behavior that lags the lobby on purpose."
            }), new ButtonInfo({
              buttonText: "Draw (goop)",
              isTogglable: true,
              method: () => {
                if (rightGrab && rightTrigger && !(time < (exports._lastDrawTime || 0))) {
                  exports._lastDrawTime = time + 0.045;
                  try {
                    const e = safeField(getInstance(), "rightHandTransform");
                    if (!e || e.isNull()) {
                      return;
                    }
                    const t = e.method("get_position").invoke();
                    spawnItemAtPos("item_goop", t, e.method("get_rotation").invoke() || identityQuaternion);
                  } catch (e) {
                    console.error("Draw goop error:", e);
                    sendNotification("Draw error: " + e, false);
                  }
                }
              },
              toolTip: "Hold grip + trigger to draw with goop"
            }), new ButtonInfo({
              buttonText: "<size=6>Delete All Items (click twice)</size>",
              method: () => {
                try {
                  const e = PrefabGen.field("_instance").value.method("get_runner").invoke();
                  if (!e || e.isNull()) {
                    return;
                  }
                  const t = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                  let o = 0;
                  for (let n = 0; n < t.length; n++) {
                    const i = t.get(n);
                    if (i && !i.handle.isNull()) {
                      try {
                        const t = i.method("get_Object").invoke();
                        if (!t || t.isNull()) {
                          continue;
                        }
                        t.method("RequestStateAuthority").invoke();
                        e.method("Despawn").invoke(t);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification(`NUCLEAR CLEANUP: Deleted ${o} items.`, false);
                } catch (e) {
                  console.error("Nuclear Delete Error: " + e);
                }
              },
              isTogglable: true,
              toolTip: "Takes authority and deletes EVERY item, including those in players' hands."
            }), new ButtonInfo({
              buttonText: "Sell All Items",
              method: () => {
                try {
                  let e = 0;
                  const t = Object.method("FindObjectsByType", 1).inflate(ItemSellingMachineController).invoke(0);
                  if (!t || t.isNull()) {
                    sendNotification("No selling machines found!", false);
                    return;
                  }
                  for (let o = 0; o < t.length; o++) {
                    try {
                      const n = t.get(o);
                      if (!n || n.isNull()) {
                        continue;
                      }
                      const i = ["SellAll", "SellAllItems", "Sell", "TrySellAll", "DoSell"];
                      for (const t of i) {
                        try {
                          n.method(t).invoke();
                          e++;
                          break;
                        } catch (e) {}
                      }
                    } catch (e) {}
                  }
                  sendNotification("Sold items from " + e + " machine(s)!", false);
                } catch (e) {
                  sendNotification("Sell All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sells all items in every selling machine in the scene."
            }), new ButtonInfo({
              buttonText: "Jelly Held Item",
              isTogglable: true,
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e) {
                    return;
                  }
                  const t = e.method("GetHandInteractor", 1).invoke(0);
                  if (!t) {
                    return;
                  }
                  const o = t.field("_itemAnchor").value;
                  if (!o) {
                    return;
                  }
                  const n = o.method("get_grabbableObject").invoke();
                  if (!n) {
                    return;
                  }
                  if (rightGrab) {
                    jellyVal = Math.min(255, jellyVal + 5);
                  }
                  if (leftGrab) {
                    jellyVal = Math.max(0, jellyVal - 5);
                  }
                  const i = jellyVal & 255;
                  try {
                    n.method("SetJellyStrengthData").invoke(i);
                  } catch (e) {}
                  try {
                    n.method("RPC_SetJellyStrengthData").invoke(i);
                  } catch (e) {}
                  try {
                    n.method("SetJellyStrength").invoke(i / 255);
                  } catch (e) {}
                  try {
                    n.method("set_jellyStrength").invoke(i);
                  } catch (e) {}
                  try {
                    n.field("_jellyStrength").value = i;
                  } catch (e) {}
                  try {
                    n.method("HandleJellyStrengthChanged").invoke();
                  } catch (e) {}
                  if (time > tagGunDelay) {
                    currentNotification = "Jelly: " + i;
                    notifactionResetTime = getNotificationClock() + 1.5;
                    tagGunDelay = time + 0.25;
                  }
                } catch (e) {
                  console.error("[Jelly Held Item]", e);
                }
              },
              toolTip: "Hold right grip to increase jelly, left grip to decrease."
            }), new ButtonInfo({
              buttonText: "Spawn Rainbow Items",
              method: () => {
                const e = rightHandTransform;
                if (rightSecondary && rightGrab) {
                  try {
                    if ((hueVal += 30) > 127) {
                      hueVal = -127;
                    }
                    if ((satVal += 30) > 127) {
                      satVal = -127;
                    }
                    const t = getLaunchForward(e);
                    const o = getLaunchUp(e, t);
                    const n = Vector3.method("op_Addition").invoke(e.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(t, 0.38), Vector3.method("op_Multiply", 2).invoke(o, -0.03)));
                    const i = spawnItemAtPos(itemIDs[itemIndex], n, getLaunchRotation(e, t, o));
                    if (!i || i.handle.isNull()) {
                      sendNotification("Spawn returned null: " + itemIDs[itemIndex], false);
                    } else {
                      applyRainbowToSpawnedObject(i, hueVal, satVal);
                      sendNotification("Spawned: " + itemIDs[itemIndex], false);
                    }
                  } catch (e) {
                    console.error("Hand spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Spawns items in your right hand with rainbow colors (hold grip + B)."
            }), new ButtonInfo({
              buttonText: "Spawn Tiny Items",
              method: () => {
                const e = rightHandTransform;
                if (rightSecondary && rightGrab) {
                  try {
                    const t = getLaunchForward(e);
                    const o = getLaunchUp(e, t);
                    const n = Vector3.method("op_Addition").invoke(e.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(t, 0.38), Vector3.method("op_Multiply", 2).invoke(o, -0.03)));
                    const i = spawnItemAtPos(itemIDs[itemIndex], n, getLaunchRotation(e, t, o));
                    if (!i || i.handle.isNull()) {
                      sendNotification("Spawn returned null: " + itemIDs[itemIndex], false);
                    } else {
                      try {
                        const e = i.method("get_gameObject").invoke();
                        if (e && !e.isNull()) {
                          let t = getComponent(e, GBOClass);
                          if (!t || t.isNull()) {
                            try {
                              t = e.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                            } catch (e) {
                              t = null;
                            }
                          }
                          if (t && !t.isNull()) {
                            try {
                              t.method("set_scaleModifier").invoke(-110);
                            } catch (e) {}
                          }
                        }
                      } catch (e) {}
                      try {
                        applyScaleToSpawnedObject(i, -110);
                      } catch (e) {}
                      sendNotification("Spawned tiny: " + itemIDs[itemIndex], false);
                    }
                  } catch (e) {
                    console.error("Tiny spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Spawns a tiny version of the selected item in your hand."
            }), new ButtonInfo({
              buttonText: "Spawn Big Items",
              method: () => {
                const e = rightHandTransform;
                if (rightSecondary && rightGrab) {
                  try {
                    const t = getLaunchForward(e);
                    const o = getLaunchUp(e, t);
                    const n = Vector3.method("op_Addition").invoke(e.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(t, 0.38), Vector3.method("op_Multiply", 2).invoke(o, -0.03)));
                    const i = spawnItemAtPos(itemIDs[itemIndex], n, getLaunchRotation(e, t, o));
                    if (!i || i.handle.isNull()) {
                      sendNotification("Spawn returned null: " + itemIDs[itemIndex], false);
                    } else {
                      try {
                        const e = i.method("get_gameObject").invoke();
                        if (e && !e.isNull()) {
                          let t = getComponent(e, GBOClass);
                          if (!t || t.isNull()) {
                            try {
                              t = e.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                            } catch (e) {
                              t = null;
                            }
                          }
                          if (t && !t.isNull()) {
                            try {
                              t.method("set_scaleModifier").invoke(80);
                            } catch (e) {}
                          }
                        }
                      } catch (e) {}
                      try {
                        applyScaleToSpawnedObject(i, 80);
                      } catch (e) {}
                      sendNotification("Spawned big: " + itemIDs[itemIndex], false);
                    }
                  } catch (e) {
                    console.error("Big spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Spawns a big version of the selected item in your hand."
            }), new ButtonInfo({
              buttonText: "Spawn Big Rainbow Items",
              method: () => {
                const e = rightHandTransform;
                if (rightSecondary && rightGrab) {
                  try {
                    if ((hueVal += 30) > 127) {
                      hueVal = -127;
                    }
                    if ((satVal += 30) > 127) {
                      satVal = -127;
                    }
                    const t = getLaunchForward(e);
                    const o = getLaunchUp(e, t);
                    const n = Vector3.method("op_Addition").invoke(e.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(t, 0.38), Vector3.method("op_Multiply", 2).invoke(o, -0.03)));
                    const i = spawnItemAtPos(itemIDs[itemIndex], n, getLaunchRotation(e, t, o));
                    if (!i || i.handle.isNull()) {
                      sendNotification("Spawn returned null: " + itemIDs[itemIndex], false);
                    } else {
                      try {
                        const e = i.method("get_gameObject").invoke();
                        if (e && !e.isNull()) {
                          let t = getComponent(e, GBOClass);
                          if (!t || t.isNull()) {
                            try {
                              t = e.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                            } catch (e) {
                              t = null;
                            }
                          }
                          if (t && !t.isNull()) {
                            try {
                              t.method("set_scaleModifier").invoke(80);
                            } catch (e) {}
                          }
                        }
                      } catch (e) {}
                      try {
                        applyScaleToSpawnedObject(i, 80);
                      } catch (e) {}
                      applyRainbowToSpawnedObject(i, hueVal, satVal);
                      sendNotification("Spawned big rainbow: " + itemIDs[itemIndex], false);
                    }
                  } catch (e) {
                    console.error("Big rainbow spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Spawns a big rainbow-colored item in your hand."
            }), new ButtonInfo({
              buttonText: "Spawn Small Rainbow Items",
              method: () => {
                const e = rightHandTransform;
                if (rightSecondary && rightGrab) {
                  try {
                    if ((hueVal += 30) > 127) {
                      hueVal = -127;
                    }
                    if ((satVal += 30) > 127) {
                      satVal = -127;
                    }
                    const t = getLaunchForward(e);
                    const o = getLaunchUp(e, t);
                    const n = Vector3.method("op_Addition").invoke(e.method("get_position").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(t, 0.38), Vector3.method("op_Multiply", 2).invoke(o, -0.03)));
                    const i = spawnItemAtPos(itemIDs[itemIndex], n, getLaunchRotation(e, t, o));
                    if (!i || i.handle.isNull()) {
                      sendNotification("Spawn returned null: " + itemIDs[itemIndex], false);
                    } else {
                      try {
                        const e = i.method("get_gameObject").invoke();
                        if (e && !e.isNull()) {
                          let t = getComponent(e, GBOClass);
                          if (!t || t.isNull()) {
                            try {
                              t = e.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                            } catch (e) {
                              t = null;
                            }
                          }
                          if (t && !t.isNull()) {
                            try {
                              t.method("set_scaleModifier").invoke(-40);
                            } catch (e) {}
                          }
                        }
                      } catch (e) {}
                      try {
                        applyScaleToSpawnedObject(i, -40);
                      } catch (e) {}
                      applyRainbowToSpawnedObject(i, hueVal, satVal);
                      sendNotification("Spawned small rainbow: " + itemIDs[itemIndex], false);
                    }
                  } catch (e) {
                    console.error("Small rainbow spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Spawns a small rainbow-colored item in your hand."
            }), new ButtonInfo({
              buttonText: "Custom hue Held Item",
              method: () => {
                try {
                  const e = getLocalHeldGrabbable(true);
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  if (rightGrab && ++hueVal > 127) {
                    hueVal = -127;
                  }
                  if (leftGrab && --hueVal < -127) {
                    hueVal = 127;
                  }
                  e.method("set_colorHue").invoke(hueVal);
                } catch (e) {
                  console.error(e);
                }
              },
              isTogglable: true,
              toolTip: "Hold A to increase, grip to decrease scale of held item."
            }), new ButtonInfo({
              buttonText: "Custom saturation Held Item",
              method: () => {
                try {
                  const e = getLocalHeldGrabbable(true);
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  if (rightGrab && ++satVal > 127) {
                    satVal = -127;
                  }
                  if (leftGrab && --satVal < -127) {
                    satVal = 127;
                  }
                  e.method("set_colorSaturation").invoke(satVal);
                } catch (e) {
                  console.error(e);
                }
              },
              isTogglable: true,
              toolTip: "Hold A to increase, grip to decrease scale of held item."
            }), new ButtonInfo({
              buttonText: "Custom Scale Held Item",
              method: () => {
                try {
                  const e = getLocalHeldGrabbable(true);
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  if (rightGrab && ++scaleVal > 127) {
                    scaleVal = -127;
                  }
                  if (leftGrab && --scaleVal < -127) {
                    scaleVal = 127;
                  }
                  e.method("set_scaleModifier").invoke(scaleVal);
                } catch (e) {
                  console.error(e);
                }
              },
              isTogglable: true,
              toolTip: "Hold A to increase, grip to decrease scale of held item."
            }), new ButtonInfo({
              buttonText: "No Gravity On All Items",
              isTogglable: true,
              method: (() => {
                const e = new Map();
                let t = 0;
                const o = t => {
                  try {
                    if (!t || t.isNull()) {
                      return;
                    }
                    let o = t;
                    try {
                      const e = t.field("_rigidbody");
                      if (e && e.value && !e.value.isNull()) {
                        o = e.value;
                      }
                    } catch (e) {}
                    o.method("set_useGravity").invoke(false);
                    const n = normalizeSceneObjectHandle(o) || String(o.handle);
                    if (!e.has(n)) {
                      e.set(n, o);
                    }
                  } catch (e) {}
                };
                const n = () => {
                  try {
                    if (frameCount % 60 == 0 || !cachedItems) {
                      cachedItems = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                    }
                    if (time > t) {
                      t = time + 0.35;
                      try {
                        const e = getAllNetPlayersList(true);
                        for (const t of e) {
                          try {
                            if (!t || t.isNull()) {
                              continue;
                            }
                            for (let e = 0; e < 2; e++) {
                              const n = getPlayerHeldGrabbable(t, e);
                              if (!n || n.isNull()) {
                                continue;
                              }
                              const i = getRootLikeObject(n);
                              requestAuthorityDeep(n);
                              requestAuthorityDeep(i);
                              forceReleaseItemFromAllPlayers(n);
                              forceReleaseItemFromAllPlayers(i);
                              tryReleaseHeldItem(n);
                              tryReleaseHeldItem(i);
                              o(n);
                              o(i);
                            }
                          } catch (e) {}
                        }
                      } catch (e) {}
                    }
                    if (cachedItems) {
                      for (let e = 0; e < cachedItems.length; e++) {
                        const t = cachedItems.get(e);
                        if (t && !t.handle.isNull()) {
                          o(t);
                        }
                      }
                    }
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (e && !e.isNull()) {
                      for (let t = 0; t < 2; t++) {
                        try {
                          const n = e.method("GetHandInteractor", 1).invoke(t);
                          if (n && !n.isNull()) {
                            const e = n.field("_itemAnchor").value;
                            if (e && !e.isNull()) {
                              const t = e.method("get_grabbableObject").invoke();
                              if (t && !t.isNull()) {
                                o(t);
                              }
                            }
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("No gravity all items update error:", e);
                  }
                };
                n._disable = () => {
                  for (const [t, o] of e.entries()) {
                    try {
                      if (o && !o.isNull()) {
                        o.method("set_useGravity").invoke(true);
                      }
                    } catch (e) {}
                  }
                  e.clear();
                };
                return n;
              })(),
              disableMethod: function () {
                const e = buttons.flat().find(e => e.buttonText === "No Gravity On All Items")?.method?._disable;
                if (e) {
                  e();
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Scale all items",
              method: () => {
                try {
                  if ((scaleVal += 15) > 127) {
                    scaleVal = -127;
                  }
                  if (rightGrab) {
                    const e = Object.method("FindObjectsOfType").inflate(GBOClass).invoke();
                    if (e) {
                      for (let t = 0; t < e.length; t++) {
                        e.get(t).method("set_scaleModifier").invoke(scaleVal);
                      }
                    }
                  }
                } catch (e) {
                  console.error(e);
                }
              },
              isTogglable: true,
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Rainbow Held Item",
              method: () => {
                try {
                  if (!NetPlayer.method("get_localPlayer").invoke()) {
                    return;
                  }
                  const e = getLocalHeldGrabbable(true);
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  if (rightGrab) {
                    hueVal++;
                    satVal++;
                    if (hueVal > 127) {
                      hueVal = -127;
                    }
                    if (satVal > 127) {
                      satVal = -127;
                    }
                  }
                  if (leftGrab) {
                    hueVal--;
                    satVal--;
                    if (hueVal < -127) {
                      hueVal = 127;
                    }
                    if (satVal > 127) {
                      satVal = -127;
                    }
                  }
                  e.method("set_colorHue").invoke(hueVal);
                  e.method("set_colorSaturation").invoke(satVal);
                } catch (e) {
                  console.error(e);
                }
              },
              isTogglable: true,
              toolTip: "Hold A to increase, grip to decrease hue of held item."
            }), new ButtonInfo({
              buttonText: "Rainbow All Items",
              method: () => {
                try {
                  if (rightGrab && time > tagGunDelay) {
                    tagGunDelay = time + 0.1;
                    if ((hueVal += 5) > 127) {
                      hueVal = -127;
                    }
                    if ((satVal += 5) > 127) {
                      satVal = -127;
                    }
                  }
                  if (leftGrab && time > tagGunDelay) {
                    tagGunDelay = time + 0.1;
                    if ((hueVal -= 5) > 127) {
                      hueVal = -127;
                    }
                    if ((satVal -= 5) > 127) {
                      satVal = -127;
                    }
                  }
                  if (frameCount % 60 == 0 || !cachedItems) {
                    cachedItems = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                  }
                  if (frameCount % 5 != 0) {
                    return;
                  }
                  if (cachedItems) {
                    for (let e = 0; e < cachedItems.length; e++) {
                      const t = cachedItems.get(e);
                      if (t && !t.handle.isNull()) {
                        t.method("set_colorHue").invoke(hueVal);
                        t.method("set_colorSaturation").invoke(satVal);
                      }
                    }
                  }
                } catch (e) {
                  console.error(e);
                }
              },
              isTogglable: true,
              toolTip: "Hold grip to cycle rainbow on all items."
            }), new ButtonInfo({
              buttonText: "random Held Item",
              method: () => {
                try {
                  const e = getLocalHeldGrabbable(true);
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  if (rightGrab) {
                    hueVal++;
                    satVal++;
                    scaleVal++;
                    if (hueVal > 127) {
                      hueVal = -127;
                    }
                    if (satVal > 127) {
                      satVal = -127;
                    }
                    if (scaleVal > 127) {
                      scaleVal = -127;
                    }
                  }
                  if (leftGrab) {
                    hueVal--;
                    satVal--;
                    scaleVal--;
                    if (hueVal > 127) {
                      hueVal = -127;
                    }
                    if (hueVal > 127) {
                      hueVal = -127;
                    }
                    if (satVal > 127) {
                      satVal = -127;
                    }
                  }
                  e.method("set_colorHue").invoke(hueVal);
                  e.method("set_colorSaturation").invoke(satVal);
                  e.method("set_scaleModifier").invoke(scaleVal);
                } catch (e) {
                  console.error(e);
                }
              },
              isTogglable: true,
              toolTip: "Hold A to increase, grip to decrease hue of held item."
            }), new ButtonInfo({
              buttonText: "random All Items",
              method: () => {
                try {
                  if (time <= randomAllItemsDelay) {
                    return;
                  }
                  randomAllItemsDelay = time + 0.12;
                  const e = getAllLobbyItemGameObjects();
                  if (!e || e.length === 0) {
                    return;
                  }
                  let t = 0;
                  const o = Math.min(12, e.length);
                  for (let n = 0; n < e.length && t < o; n++) {
                    const o = (randomAllItemsCursor + n) % e.length;
                    const i = e[o];
                    if (!i || i.isNull?.()) {
                      continue;
                    }
                    if (isItemOccupiedByPlayer(i)) {
                      continue;
                    }
                    const a = Math.floor(Math.sin(time * 1.3 + o * 0.31) * 127);
                    const l = Math.floor(55 + Math.sin(time * 0.9 + o * 0.27) * 55);
                    const r = Math.floor(Math.sin(time * 0.8 + o * 0.23) * 42);
                    try {
                      let e = i;
                      try {
                        const t = i.method("GetComponent", 1).inflate(GBOClass).invoke();
                        if (t && !t.isNull?.()) {
                          e = t;
                        }
                      } catch (e) {}
                      e.method("set_colorHue").invoke(a);
                      e.method("set_colorSaturation").invoke(l);
                      e.method("set_scaleModifier").invoke(Math.max(-18, Math.min(62, r)));
                    } catch (e) {}
                    t++;
                  }
                  randomAllItemsCursor = (randomAllItemsCursor + o) % Math.max(e.length, 1);
                } catch (e) {
                  console.error(e);
                }
              },
              isTogglable: true,
              toolTip: "Smoothly randomizes free lobby items in batches so it actually works without lagging the lobby."
            }), new ButtonInfo({
              buttonText: "suitcase hand spam",
              method: () => {
                if (rightGrab) {
                  PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string("item_pelican_case"), rightHandTransform.method("get_position").invoke(), identityQuaternion, NULL, 0);
                }
              },
              isTogglable: true,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "rare card hand spam",
              method: () => {
                if (rightGrab) {
                  PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string("item_rare_card"), rightHandTransform.method("get_position").invoke(), identityQuaternion, NULL, 0);
                }
              },
              isTogglable: true,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "goopfish hand spam",
              method: () => {
                if (rightGrab) {
                  PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string("item_goopfish"), rightHandTransform.method("get_position").invoke(), identityQuaternion, NULL, 0);
                }
              },
              isTogglable: true,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "time bomb hand spam",
              method: () => {
                if (rightGrab) {
                  PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string("item_timebomb"), rightHandTransform.method("get_position").invoke(), identityQuaternion, NULL, 0);
                }
              },
              isTogglable: true,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "cutie dead hand spam",
              method: () => {
                if (rightGrab) {
                  PrefabGen.method("SpawnItem", 5).invoke(Il2Cpp.string("item_cutie_dead"), rightHandTransform.method("get_position").invoke(), identityQuaternion, NULL, 0);
                }
              },
              isTogglable: true,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "Snowball minigun [W]",
              isTogglable: true,
              method: () => {
                try {
                  const e = rightGrab && rightTrigger;
                  if (!e && (!leftGrab || !leftTrigger)) {
                    return;
                  }
                  if (time <= itemLauncherSelfDelay) {
                    return;
                  }
                  itemLauncherSelfDelay = time + 0.055;
                  const t = spawnForwardLaunchedItem("item_snowball", e ? rightHandTransform : leftHandTransform, 28, 0.92);
                  if (!t || !!t.isNull?.()) {
                    sendNotification("Item launch failed", false);
                  }
                } catch (e) {
                  console.error("Item Launcher:", e);
                }
              },
              toolTip: "Hold grip + trigger to keep launching the currently selected Item ID from your hand."
            }), new ButtonInfo({
              buttonText: "Rainbow Suitcase Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_pelican_case", "item_pelican_case", "item_pelican_case", "item_pelican_case", "item_pelican_case", "item_pelican_case", "item_pelican_case", "item_pelican_case", "item_pelican_case", "item_pelican_case"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, Math.floor(Math.random() * 255) - 127);
                            setItemColorSaturation(e, Math.floor(Math.random() * 148) - 20);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Rainbow ALL Dball Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_quest_dball_1", "item_quest_dball_2", "item_quest_dball_3", "item_quest_dball_4", "item_quest_dball_5", "item_quest_dball_6", "item_quest_dball_7", "item_quest_dball_1", "item_quest_dball_2", "item_quest_dball_3", "item_quest_dball_4", "item_quest_dball_5", "item_quest_dball_6", "item_quest_dball_7", "item_quest_dball_1", "item_quest_dball_2", "item_quest_dball_3", "item_quest_dball_4", "item_quest_dball_5", "item_quest_dball_6", "item_quest_dball_7", "item_quest_dball_1", "item_quest_dball_2", "item_quest_dball_3", "item_quest_dball_4", "item_quest_dball_5", "item_quest_dball_6", "item_quest_dball_7"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, Math.floor(Math.random() * 255) - 127);
                            setItemColorSaturation(e, Math.floor(Math.random() * 148) - 20);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Rainbow Timebomb Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_timebomb", "item_timebomb", "item_timebomb", "item_timebomb", "item_timebomb", "item_timebomb", "item_timebomb", "item_timebomb", "item_timebomb", "item_timebomb"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, Math.floor(Math.random() * 255) - 127);
                            setItemColorSaturation(e, Math.floor(Math.random() * 148) - 20);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Rainbow Rare Card Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_rare_card", "item_rare_card", "item_rare_card", "item_rare_card", "item_rare_card", "item_rare_card", "item_rare_card", "item_rare_card", "item_rare_card", "item_rare_card"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, Math.floor(Math.random() * 255) - 127);
                            setItemColorSaturation(e, Math.floor(Math.random() * 148) - 20);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Rainbow Cutie Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_cutie_dead", "item_cutie_dead", "item_cutie_dead", "item_cutie_dead", "item_cutie_dead", "item_cutie_dead", "item_cutie_dead", "item_cutie_dead", "item_cutie_dead", "item_cutie_dead"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, Math.floor(Math.random() * 255) - 127);
                            setItemColorSaturation(e, Math.floor(Math.random() * 148) - 20);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Rainbow Arena Guns Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_arena_pistol", "item_arena_pistol", "item_arena_pistol", "item_arena_pistol", "item_arena_pistol", "item_arena_shotgun", "item_arena_shotgun", "item_arena_shotgun", "item_arena_shotgun", "item_arena_shotgun"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, Math.floor(Math.random() * 255) - 127);
                            setItemColorSaturation(e, Math.floor(Math.random() * 148) - 20);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Rainbow plushie Items (gun)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_squishy_smiley", "item_squishy_segway", "item_squishy_redgreen", "item_squishy_puppet", "item_squishy_murderbunny", "item_squishy_edenzombie", "item_squishy_cutie", "item_squishy_chicken", "item_squishy_bomber", "item_squishy_bighead", "item_squishy_armstrong", "item_squishy_angler", "item_squishy_smiley", "item_squishy_segway", "item_squishy_redgreen", "item_squishy_puppet", "item_squishy_murderbunny", "item_squishy_edenzombie", "item_squishy_cutie", "item_squishy_chicken", "item_squishy_bomber", "item_squishy_bighead", "item_squishy_armstrong", "item_squishy_angler", "item_squishy_smiley", "item_squishy_segway", "item_squishy_redgreen", "item_squishy_puppet", "item_squishy_murderbunny", "item_squishy_edenzombie", "item_squishy_cutie", "item_squishy_chicken", "item_squishy_bomber", "item_squishy_bighead", "item_squishy_armstrong", "item_squishy_angler"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, Math.floor(Math.random() * 255) - 127);
                            setItemColorSaturation(e, Math.floor(Math.random() * 148) - 20);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Rainbow golden plushie Items (gun)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_squishy_gold_smiley", "item_squishy_gold_segway", "item_squishy_gold_redgreen", "item_squishy_gold_puppet", "item_squishy_gold_murderbunny", "item_squishy_gold_edenzombie", "item_squishy_gold_cutie", "item_squishy_gold_chicken", "item_squishy_gold_bomber", "item_squishy_gold_bighead", "item_squishy_gold_armstrong", "item_squishy_gold_angler", "item_squishy_gold_smiley", "item_squishy_gold_segway", "item_squishy_gold_redgreen", "item_squishy_gold_puppet", "item_squishy_gold_murderbunny", "item_squishy_gold_edenzombie", "item_squishy_gold_cutie", "item_squishy_gold_chicken", "item_squishy_gold_bomber", "item_squishy_gold_bighead", "item_squishy_gold_armstrong", "item_squishy_gold_angler", "item_squishy_gold_smiley", "item_squishy_gold_segway", "item_squishy_gold_redgreen", "item_squishy_gold_puppet", "item_squishy_gold_murderbunny", "item_squishy_gold_edenzombie", "item_squishy_gold_cutie", "item_squishy_gold_chicken", "item_squishy_gold_bomber", "item_squishy_gold_bighead", "item_squishy_gold_armstrong", "item_squishy_gold_angler"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, Math.floor(Math.random() * 255) - 127);
                            setItemColorSaturation(e, Math.floor(Math.random() * 148) - 20);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Rainbow bloody Items (gun)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_butcherpipe_blood", "item_butcherspear_blood", "item_butchersword_blood", "item_axe_blood", "item_crowbar_blood", "item_pickaxe_spacedwarf_blood", "item_shotgun_blood", "item_snowboard_blood", "item_dwarven_hammer_blood", "item_flamethrower_skull_blood", "item_mining_laser_blood", "item_butcherpipe_blood", "item_butcherspear_blood", "item_butchersword_blood", "item_axe_blood", "item_crowbar_blood", "item_pickaxe_spacedwarf_blood", "item_shotgun_blood", "item_snowboard_blood", "item_dwarven_hammer_blood", "item_flamethrower_skull_blood", "item_mining_laser_blood", "item_butcherpipe_blood", "item_butcherspear_blood", "item_butchersword_blood", "item_axe_blood", "item_crowbar_blood", "item_pickaxe_spacedwarf_blood", "item_shotgun_blood", "item_snowboard_blood", "item_dwarven_hammer_blood", "item_flamethrower_skull_blood", "item_mining_laser_blood", "item_butcherpipe_blood", "item_butcherspear_blood", "item_butchersword_blood"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, Math.floor(Math.random() * 255) - 127);
                            setItemColorSaturation(e, Math.floor(Math.random() * 148) - 20);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Giveaway Gun [V1]",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_moonhorror_key", "item_quest_vhs_lab", "item_egg", "item_mug", "item_pyramidal_anomoly", "item_alienblaster", "item_zombie_meat", "item_cardboard_box", "item_pineapple", "item_landmine", "item_backpack_monkey", "item_wireframe_gun", "item_scissors", "item_revolver_gold", "item_ukelele", "item_goldbar", "item_toilet_paper", "item_cola_large", "item_d20", "item_goldcoin", "item_nut", "item_hh_key", "item_timebomb", "item_mountain_key", "item_shadowboss_key"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, Math.floor(Math.random() * 255) - 127);
                            setItemColorSaturation(e, Math.floor(Math.random() * 148) - 20);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Giveaway Gun [V2]",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_plunger", "item_cutie_dead", "item_randombox_modules", "item_landmine", "item_quest_vhs_wormhole", "item_module_blast_3", "item_zipline_gun", "item_pelican_case", "item_wood_log", "item_ac_cola", "item_apple", "item_backpack_fish", "item_banana_chips", "item_beans", "item_boombox", "item_box_fan", "item_brainslug_green", "item_broom_halloween", "item_bubble_gun", "item_burrito", "item_ceo_plaque", "item_company_ration_heal", "item_cracker", "item_flashlight_mega", "item_floppy_5", "item_floppy_3", "item_gameboy", "item_whoopie"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, Math.floor(Math.random() * 255) - 127);
                            setItemColorSaturation(e, Math.floor(Math.random() * 148) - 20);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Galaxy Giveaway Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_plunger", "item_cutie_dead", "item_randombox_modules", "item_landmine", "item_quest_vhs_wormhole", "item_module_blast_3", "item_zipline_gun", "item_pelican_case", "item_wood_log", "item_ac_cola", "item_apple", "item_backpack_fish", "item_banana_chips", "item_baseball_bat", "item_beans", "item_boombox", "item_box_fan", "item_brainslug_green", "item_broom_halloween", "item_bubble_gun", "item_burrito", "item_ceo_plaque", "item_company_ration_heal", "item_cracker", "item_flashlight_mega", "item_floppy_5", "item_floppy_3", "item_gameboy", "item_whoopie", "item_backpack_large_clover", "item_backpack_monkey", "item_backpack_with_flashlight", "item_banana", "item_boombox_fishing", "item_cola", "item_drill", "item_egg", "item_fish_big_shark", "item_treestick", "item_rpg_cny", "item_goldbar", "item_pickaxe_cny", "item_pumpkin_pie", "item_ukulele_gold", "item_umbrella"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, 161);
                            setItemColorSaturation(e, 120);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Galaxy New Golden Items",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                let e = null;
                try {
                  e = renderGun();
                } catch (e) {
                  return;
                }
                if (!e) {
                  return;
                }
                const t = e.ray;
                if (t && !t.handle.isNull() && rightTrigger) {
                  const e = ["item_backpack_gold", "item_crowbar_gold", "item_hookshot_gold"];
                  try {
                    const o = t.method("get_point").invoke();
                    for (let t of e) {
                      const e = spawnItemAtPos(t, o, identityQuaternion);
                      if (!e || e.isNull()) {
                        continue;
                      }
                      const n = e.method("get_gameObject").invoke();
                      if (n && !n.isNull()) {
                        try {
                          const e = n.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                          if (e && !e.isNull()) {
                            e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                            setItemColorHue(e, 161);
                            setItemColorSaturation(e, 120);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("Item spawn error:", e);
                    sendNotification("Spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Rainbow Item Launcher",
              enableMethod: () => {
                itemLauncherEnabled = true;
              },
              disableMethod: () => {
                itemLauncherEnabled = false;
                itemLauncherSelfDelay = 0;
              },
              isTogglable: true,
              method: () => {
                if (itemLauncherEnabled && rightGrab && !(time < itemLauncherSelfDelay)) {
                  itemLauncherSelfDelay = time + 0;
                  try {
                    const e = rightHandTransform.method("get_position").invoke();
                    const t = rightHandTransform.method("get_forward").invoke();
                    const o = rightHandTransform.method("get_rotation").invoke();
                    const n = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    for (let i = 0; i < 4; i++) {
                      const i = spawnItemAtPos(itemIDs[itemIndex], e, o);
                      if (!i || i.isNull()) {
                        continue;
                      }
                      const a = i.method("get_gameObject").invoke();
                      if (!a || a.isNull()) {
                        continue;
                      }
                      try {
                        const e = a.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                        if (e && !e.isNull()) {
                          e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                          setItemColorHue(e, Math.floor(Math.random() * 255) - 127);
                          setItemColorSaturation(e, Math.floor(Math.random() * 148) - 20);
                        }
                      } catch (e) {}
                      const l = a.method("GetComponentInChildren", 1).inflate(n).invoke();
                      if (!l || l.isNull()) {
                        continue;
                      }
                      l.method("set_isKinematic").invoke(false);
                      l.method("set_useGravity").invoke(false);
                      const r = Vector3.method("op_Multiply", 2).invoke(t, 120);
                      l.method("AddForce", 2).invoke(r, 1);
                    }
                  } catch (e) {
                    console.error("[ItemMinigun]", e);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Galaxy Item Launcher",
              enableMethod: () => {
                itemLauncherEnabled = true;
              },
              disableMethod: () => {
                itemLauncherEnabled = false;
                itemLauncherSelfDelay = 0;
              },
              isTogglable: true,
              method: () => {
                if (itemLauncherEnabled && rightGrab && !(time < itemLauncherSelfDelay)) {
                  itemLauncherSelfDelay = time + 0;
                  try {
                    const e = rightHandTransform.method("get_position").invoke();
                    const t = rightHandTransform.method("get_forward").invoke();
                    const o = rightHandTransform.method("get_rotation").invoke();
                    const n = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    for (let i = 0; i < 4; i++) {
                      const i = spawnItemAtPos(itemIDs[itemIndex], e, o);
                      if (!i || i.isNull()) {
                        continue;
                      }
                      const a = i.method("get_gameObject").invoke();
                      if (!a || a.isNull()) {
                        continue;
                      }
                      try {
                        const e = a.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                        if (e && !e.isNull()) {
                          e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                          setItemColorHue(e, 161);
                          setItemColorSaturation(e, 120);
                        }
                      } catch (e) {}
                      const l = a.method("GetComponentInChildren", 1).inflate(n).invoke();
                      if (!l || l.isNull()) {
                        continue;
                      }
                      l.method("set_isKinematic").invoke(false);
                      l.method("set_useGravity").invoke(false);
                      const r = Vector3.method("op_Multiply", 2).invoke(t, 120);
                      l.method("AddForce", 2).invoke(r, 1);
                    }
                  } catch (e) {
                    console.error("[ItemMinigun]", e);
                  }
                }
              },
              toolTip: "solarhp"
            }), new ButtonInfo({
              buttonText: "Random Galaxy Item Launcher",
              enableMethod: () => {
                itemLauncherEnabled = true;
              },
              disableMethod: () => {
                itemLauncherEnabled = false;
                itemLauncherSelfDelay = 0;
              },
              isTogglable: true,
              method: () => {
                if (itemLauncherEnabled && rightGrab && !(time < itemLauncherSelfDelay)) {
                  itemLauncherSelfDelay = time + 0;
                  try {
                    const e = rightHandTransform.method("get_position").invoke();
                    const t = rightHandTransform.method("get_forward").invoke();
                    const o = rightHandTransform.method("get_rotation").invoke();
                    const n = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const i = Math.floor(Math.random() * itemIDs.length);
                    const a = itemIDs[i];
                    for (let i = 0; i < 4; i++) {
                      const i = spawnItemAtPos(a, e, o);
                      if (!i || i.isNull()) {
                        continue;
                      }
                      const l = i.method("get_gameObject").invoke();
                      if (!l || l.isNull()) {
                        continue;
                      }
                      try {
                        const e = l.method("GetComponentInChildren", 1).inflate(GBOClass).invoke();
                        if (e && !e.isNull()) {
                          e.method("set_scaleModifier").invoke(Math.floor(Math.random() * 256) - 128);
                          setItemColorHue(e, 161);
                          setItemColorSaturation(e, 120);
                        }
                      } catch (e) {}
                      const r = l.method("GetComponentInChildren", 1).inflate(n).invoke();
                      if (!r || r.isNull()) {
                        continue;
                      }
                      r.method("set_isKinematic").invoke(false);
                      r.method("set_useGravity").invoke(false);
                      const s = Vector3.method("op_Multiply", 2).invoke(t, 120);
                      r.method("AddForce", 2).invoke(s, 1);
                    }
                  } catch (e) {
                    console.error("[GalaxyItemLauncher]", e);
                  }
                }
              },
              toolTip: "solarhp"
            })], [new ButtonInfo({
              buttonText: "Exit Credits",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "SolarHP",
              method: () => {
                currentCategory = 7;
                currentPage = 0;
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "Byte",
              method: () => {
                currentCategory = 7;
                currentPage = 0;
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "AJ",
              method: () => {
                currentCategory = 7;
                currentPage = 0;
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "rasp",
              method: () => {
                currentCategory = 7;
                currentPage = 0;
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "ItzDaTree",
              method: () => {
                currentCategory = 7;
                currentPage = 0;
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "MrMcFlipFlopp",
              method: () => {
                currentCategory = 7;
                currentPage = 0;
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "Moony2HP",
              method: () => {
                currentCategory = 7;
                currentPage = 0;
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "Harry",
              method: () => {
                currentCategory = 7;
                currentPage = 0;
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "Codex",
              method: () => {
                currentCategory = 7;
                currentPage = 0;
              },
              isTogglable: false
            })], [new ButtonInfo({
              buttonText: "Exit Names",
              method: () => {
                currentCategory = 4;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "<size=9>Set name as SolarHP</size>",
              method: () => {
                NetPlayer.method("get_localPlayer").invoke().method("set_displayName").invoke(Il2Cpp.string("SolarHP"));
              },
              isTogglable: true,
              toolTip: "name has been set."
            }), new ButtonInfo({
              buttonText: "<size=9>Set name as John Doe</size>",
              method: () => {
                NetPlayer.method("get_localPlayer").invoke().method("set_displayName").invoke(Il2Cpp.string("John Doe"));
              },
              isTogglable: true,
              toolTip: "name has been set."
            }), new ButtonInfo({
              buttonText: "<size=9>Set name as Moony2HP</size>",
              method: () => {
                NetPlayer.method("get_localPlayer").invoke().method("set_displayName").invoke(Il2Cpp.string("Moony2HP"));
              },
              isTogglable: true,
              toolTip: "name has been set."
            }), new ButtonInfo({
              buttonText: "Hide Name",
              method: () => {
                NetPlayer.method("get_localPlayer").invoke().method("set_displayName").invoke(Il2Cpp.string(" "));
              },
              isTogglable: true,
              toolTip: "name has been hidden"
            }), new ButtonInfo({
              buttonText: "<size=11>Set name as the ItzDaTree</size>",
              method: () => {
                NetPlayer.method("get_localPlayer").invoke().method("set_displayName").invoke(Il2Cpp.string("ItzDaTree"));
              },
              isTogglable: true,
              toolTip: "name has been set."
            }), new ButtonInfo({
              buttonText: "<size=11>Cycle Name</size>",
              method: () => {
                const e = ["SolarHP", "", "FIX YOUR GAME", "AntiCheatIsShit", "FixedByTree", "Cant ban me", "IamUsingSolarHP"];
                if (globalThis.nameIndex === undefined) {
                  globalThis.nameIndex = 0;
                }
                if (globalThis.nameInterval === undefined) {
                  globalThis.nameInterval = setInterval(() => {
                    const t = NetPlayer.method("get_localPlayer").invoke();
                    if (t && !t.isNull()) {
                      const o = e[globalThis.nameIndex];
                      t.method("set_displayName").invoke(Il2Cpp.string(o));
                      globalThis.nameIndex = (globalThis.nameIndex + 1) % e.length;
                    }
                  }, 500);
                }
              },
              isTogglable: true,
              toolTip: "Cycles through names automatically"
            })], [new ButtonInfo({
              buttonText: "Exit Exploits",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "Multi Buy",
              enableMethod: () => {
                multiBuyEnabled = true;
                sendNotification("Multi Buy ON x" + multiBuyAmount, false);
              },
              disableMethod: () => {
                multiBuyEnabled = false;
                sendNotification("Multi Buy OFF", false);
              },
              isTogglable: true,
              toolTip: "Repeats purchase-machine buys using the current multiplier."
            }), new ButtonInfo({
              buttonText: "Change Multi Buy",
              method: () => {
                if ((multiBuyAmount += 5) > 50) {
                  multiBuyAmount = 5;
                }
                sendNotification("Multi Buy x" + multiBuyAmount, false);
              },
              isTogglable: false,
              toolTip: "Cycles the multi buy amount in steps of 5 up to 50, then resets."
            }), new ButtonInfo({
              buttonText: "Night Alarm pluh",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e) {
                    e.method("PlayNightAlarm").invoke();
                  }
                } catch (e) {
                  console.error("give money failed:", e);
                }
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "Lag Game",
              isTogglable: true,
              enableMethod: () => {
                sendNotification("lag game", false);
              },
              disableMethod: () => {
                sendNotification("lag game", false);
              },
              method: () => {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.NetSessionRPCs").field("_instance").value;
                  const t = AssemblyCSharp.class("AnimalCompany.NetPlayer").method("get_localPlayer").invoke();
                  if (e && !e.isNull?.() && t && !t.isNull?.()) {
                    const o = t.method("get_playerID").invoke();
                    for (let t = 0; t < 100; t++) {
                      try {
                        e.method("RPC_NotifyYeetStarted").invoke(o, 1);
                      } catch (e) {}
                      try {
                        e.method("RPC_NotifyYeetStarted").invoke(o, 0);
                      } catch (e) {}
                      try {
                        e.method("RPC_NotifyYeetStarted").invoke(o, Math.floor(Math.random() * 100));
                      } catch (e) {}
                      try {
                        e.method("RPC_SendChatMessage").invoke(o, "YEET", 0);
                      } catch (e) {}
                      try {
                        e.method("RPC_PlayerDeath").invoke(o);
                      } catch (e) {}
                      try {
                        e.method("RPC_TriggerAnimation").invoke(o, "yeet");
                      } catch (e) {}
                    }
                  }
                } catch (e) {
                  console.error("lag game", e);
                }
              },
              toolTip: "everyones game is now lagging."
            }), new ButtonInfo({
              buttonText: "Grant All Stash Slots",
              enableMethod: () => {
                forceAllStashSlotsEnabled = true;
                sendNotification("Grant All Stash Slots ON", false);
              },
              disableMethod: () => {
                forceAllStashSlotsEnabled = false;
                sendNotification("Grant All Stash Slots OFF", false);
              },
              isTogglable: true,
              toolTip: "Best-effort force unlocks every stash slot it can find on this build."
            }), new ButtonInfo({
              buttonText: "No Player/stash Limits",
              isTogglable: true,
              method: () => {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.GameplayItemState");
                  if (!e) {
                    return;
                  }
                  let t = false;
                  t = !t;
                  ["get_allowAddToBag", "get_allowAddToQuiver", "get_allowAttachToItem", "get_allowAttachToBack", "get_allowAttachToHip"].forEach(o => {
                    const n = e.method(o);
                    if (n) {
                      const e = n;
                      n.implementation = function () {
                        return !!t || e.invoke(this);
                      };
                    }
                  });
                } catch (e) {}
              }
            }), new ButtonInfo({
              buttonText: "No Container Restrictions",
              enableMethod: () => {
                containerFreedomAuraEnabled = true;
                sendNotification("No Container Restrictions ON", false);
              },
              disableMethod: () => {
                containerFreedomAuraEnabled = false;
                sendNotification("No Container Restrictions OFF", false);
              },
              isTogglable: true,
              toolTip: "Force-applies bag/quiver/back/hip/container flags so items can go anywhere."
            }), new ButtonInfo({
              buttonText: "All Player Gun Buffs",
              enableMethod: () => {
                InfAmmo = true;
                noRecoil = true;
                rapidFireEnabled = true;
              },
              isTogglable: true,
              method: () => {
                if (!(time <= allPlayerGunBuffsDelay)) {
                  allPlayerGunBuffsDelay = time + 0.12;
                  try {
                    for (const e of getAllNetPlayersList(true)) {
                      for (const t of [0, 1]) {
                        try {
                          applyGunBuffsToGrabbable(getPlayerHeldGrabbable(e, t), false);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("All Player Gun Buffs:", e);
                  }
                }
              },
              toolTip: "Best-effort local patch that keeps other players' held guns rapid-fire, high-ammo, and no-recoil."
            }), new ButtonInfo({
              buttonText: "Explode All Machines",
              isTogglable: false,
              method: () => {
                try {
                  const e = Object.method("FindObjectsByType", 1).inflate(ItemSellingMachineController).invoke(0);
                  if (!e) {
                    return;
                  }
                  for (let t = 0; t < e.length; t++) {
                    try {
                      const o = e.get(t);
                      if (!o || o.handle.isNull()) {
                        continue;
                      }
                      o.method("RPC_ExplodeMachine").invoke();
                    } catch (e) {}
                  }
                  sendNotification("Exploded all machines!", false);
                } catch (e) {}
              },
              toolTip: "Explodes every selling machine in the room."
            }), new ButtonInfo({
              buttonText: "Recover All Machines",
              isTogglable: false,
              method: () => {
                try {
                  const e = Object.method("FindObjectsByType", 1).inflate(ItemSellingMachineController).invoke(0);
                  if (!e) {
                    return;
                  }
                  for (let t = 0; t < e.length; t++) {
                    try {
                      const o = e.get(t);
                      if (!o || o.handle.isNull()) {
                        continue;
                      }
                      o.method("RPC_RecoverExplosion").invoke();
                    } catch (e) {}
                  }
                  sendNotification("Recovered all machines!", false);
                } catch (e) {}
              },
              toolTip: "Recovers every exploded selling machine."
            }), new ButtonInfo({
              buttonText: "Money All From Machines",
              isTogglable: false,
              method: () => {
                try {
                  const e = Object.method("FindObjectsByType", 1).inflate(ItemSellingMachineController).invoke(0);
                  if (!e || e.isNull?.() || e.length === 0) {
                    sendNotification("No selling machine found!", false);
                    return;
                  }
                  const t = getAllNetPlayersList(true);
                  let o = 0;
                  for (const e of t) {
                    try {
                      if (!e || e.isNull?.()) {
                        continue;
                      }
                      e.method("RPC_AddPlayerMoney").invoke(300000);
                      o++;
                    } catch (e) {
                      console.error("[Machine 300K Player]", e);
                    }
                  }
                  sendNotification(o > 0 ? "Selling machine gave 300K to " + o + " player(s)!" : "Selling-machine payout failed!", false);
                } catch (e) {
                  console.error("[Money All From Machines]", e);
                  sendNotification("Machine payout failed: " + e, false);
                }
              },
              toolTip: "Uses an active selling machine to give every player 300,000 money."
            }), new ButtonInfo({
              buttonText: "Spam Start Selling",
              isTogglable: true,
              method: () => {
                try {
                  if (time < lagGunDelay) {
                    return;
                  }
                  lagGunDelay = time + 0.5;
                  const e = Object.method("FindObjectsByType", 1).inflate(ItemSellingMachineController).invoke(0);
                  if (!e) {
                    return;
                  }
                  for (let t = 0; t < e.length; t++) {
                    try {
                      const o = e.get(t);
                      if (!o || o.handle.isNull()) {
                        continue;
                      }
                      o.method("RPC_StartItemSelling").invoke(0);
                    } catch (e) {}
                  }
                } catch (e) {}
              },
              toolTip: "Spams start selling on all machines."
            }), new ButtonInfo({
              buttonText: "Cage All Players",
              enableMethod: () => {
                playerCagePrefabs = [];
                playerCageEntries = [];
                sendNotification("Cage All Players ON", false);
              },
              disableMethod: () => {
                for (const e of playerCagePrefabs) {
                  if (e && !e.isNull?.()) {
                    try {
                      const t = e.method("get_Runner").invoke();
                      if (t && !t.isNull()) {
                        t.method("Despawn").invoke(e);
                        continue;
                      }
                    } catch (e) {}
                    try {
                      const t = e.method("get_gameObject").invoke();
                      if (t && !t.isNull()) {
                        Destroy(t);
                      }
                    } catch (e) {}
                  }
                }
                playerCagePrefabs = [];
                playerCageEntries = [];
                sendNotification("Cage All Players OFF", false);
              },
              isTogglable: true,
              method: () => {
                try {
                  const e = getAllNetPlayersList(false);
                  if (e.length === 0) {
                    return;
                  }
                  if (playerCageEntries.length === 0) {
                    const t = [[-1.8, -0.45, -1.8], [0, -0.45, -1.8], [1.8, -0.45, -1.8], [-1.8, -0.45, 1.8], [0, -0.45, 1.8], [1.8, -0.45, 1.8], [-1.8, 0.95, -1.8], [0, 0.95, -1.8], [1.8, 0.95, -1.8], [-1.8, 0.95, 1.8], [0, 0.95, 1.8], [1.8, 0.95, 1.8], [-1.8, 0.25, 0], [1.8, 0.25, 0], [0, 0.25, -1.8], [0, 0.25, 1.8]];
                    for (const o of e) {
                      if (o && !o.isNull?.()) {
                        for (const e of t) {
                          const t = spawnNetworkPrefab("ItemSellingMachineController", zeroVector, identityQuaternion);
                          if (!t || t.isNull?.()) {
                            continue;
                          }
                          playerCagePrefabs.push(t);
                          const n = t.method("get_gameObject").invoke();
                          if (!n || n.isNull()) {
                            continue;
                          }
                          const i = n.method("get_transform").invoke();
                          playerCageEntries.push({
                            player: o,
                            transform: i,
                            offset: e
                          });
                        }
                      }
                    }
                  }
                  for (const e of playerCageEntries) {
                    try {
                      const t = e.player;
                      if (!t || t.isNull?.()) {
                        continue;
                      }
                      const o = getTransform(t).method("get_position").invoke();
                      const n = o.field("x").value + e.offset[0];
                      const i = o.field("y").value + e.offset[1];
                      const a = o.field("z").value + e.offset[2];
                      e.transform.method("set_position").invoke([n, i, a]);
                    } catch (e) {}
                  }
                } catch (e) {
                  console.error("Cage All Players:", e);
                }
              },
              toolTip: "Cages every other player with selling machines and despawns them when disabled."
            }), new ButtonInfo({
              buttonText: "Spawn NetPlayer Trigger",
              enableMethod: () => {
                netPlayerSpawnDelay = 0;
                sendNotification("Spawn NetPlayer Trigger ON", false);
              },
              disableMethod: () => {
                sendNotification("Spawn NetPlayer Trigger OFF", false);
              },
              isTogglable: true,
              method: () => {
                if (rightTrigger && !(time <= netPlayerSpawnDelay)) {
                  netPlayerSpawnDelay = time + 0.12;
                  try {
                    spawnForwardLaunchedNetworkPrefab("NetPlayer", rightHandTransform, 18, 1);
                  } catch (e) {
                    console.error("Spawn NetPlayer Trigger:", e);
                  }
                }
              },
              toolTip: "While enabled, pressing right trigger spawns a NetPlayer prefab forward from your hand."
            }), new ButtonInfo({
              buttonText: "Give Masterclient",
              isTogglable: false,
              method: () => {
                try {
                  let e = null;
                  try {
                    const t = PrefabGen.field("_instance").value;
                    if (t && !t.isNull()) {
                      e = t.method("get_runner").invoke();
                    }
                  } catch (e) {}
                  if (!e || e.isNull?.()) {
                    try {
                      const t = SFXManager.field("_instance").value;
                      if (t && !t.isNull()) {
                        e = t.method("get__currentRunner").invoke();
                      }
                    } catch (e) {}
                  }
                  if (!e || e.isNull?.()) {
                    sendNotification("Runner is null", false);
                    return;
                  }
                  const t = e.method("get_LocalPlayer").invoke();
                  if (!t) {
                    sendNotification("LocalPlayer ref null", false);
                    return;
                  }
                  e.method("SetMasterClient").invoke(t);
                  sendNotification("You are now the master client!", false);
                } catch (e) {
                  sendNotification("Masterclient: " + e, false);
                  console.error("[Masterclient]:", e);
                }
              },
              toolTip: "Promotes you to master client / host using NetworkRunner.SetMasterClient."
            }), new ButtonInfo({
              buttonText: "Stash Dupe",
              method: () => stashDupeEnabled = true,
              disableMethod: () => stashDupeEnabled = false,
              toolTip: "Lets you eject more times using your stash."
            }), new ButtonInfo({
              buttonText: "Change Eject Dupe Amount",
              method: () => {
                ejectDupeIndex++;
                ejectDupeIndex %= ejectDupeValues.length;
                sendNotification("[MENU] New eject dupe amount: " + (ejectDupeAmount = ejectDupeValues[ejectDupeIndex]), false);
              },
              isTogglable: false,
              toolTip: "Cycles through preset dupe amounts."
            }), new ButtonInfo({
              buttonText: "Nut spam",
              isTogglable: true,
              method: () => {
                try {
                  if (!rightGrab) {
                    return;
                  }
                  const e = safeField(getInstance(), "rightHandTransform")?.method("get_position").invoke();
                  safeField(getInstance(), "rightHandTransform")?.method("get_rotation").invoke();
                  sendNotification(PickupManager.method("SpawnPickup", 4).invoke(2, e, 5, true) ? "Spawned: " : "Spawn failed: ", false);
                } catch (e) {
                  console.error(e);
                }
              },
              toolTip: "Spawns selected prefab at right hand with right hand rotation."
            }), new ButtonInfo({
              buttonText: "Ammo spam",
              isTogglable: true,
              method: () => {
                try {
                  if (!rightGrab) {
                    return;
                  }
                  const e = safeField(getInstance(), "rightHandTransform")?.method("get_position").invoke();
                  safeField(getInstance(), "rightHandTransform")?.method("get_rotation").invoke();
                  sendNotification(PickupManager.method("SpawnPickup", 4).invoke(1, e, 5, true) ? "Spawned: " : "Spawn failed: ", false);
                } catch (e) {
                  console.error(e);
                }
              },
              toolTip: "idk."
            }), new ButtonInfo({
              buttonText: "Nut Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0;
                  try {
                    const t = e.method("get_point").invoke();
                    PickupManager.method("SpawnPickup", 4).invoke(2, t, 5, true);
                  } catch (e) {
                    sendNotification("Nut Pickup Gun: " + e, false);
                  }
                }
              },
              toolTip: "Spawns nut pickups where you aim (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "Ammo Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0;
                  try {
                    const t = e.method("get_point").invoke();
                    PickupManager.method("SpawnPickup", 4).invoke(1, t, 5, true);
                  } catch (e) {
                    sendNotification("Ammo Pickup Gun: " + e, false);
                  }
                }
              },
              toolTip: "Spawns ammo pickups where you aim (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "Dump Net Objects",
              method: () => {
                try {
                  const e = PrefabGen.field("_instance").value.method("get_runner").invoke();
                  if (!e || e.isNull()) {
                    sendNotification("No runner — join a room first", false, 4);
                    return;
                  }
                  const t = e.field("_config").value.field("PrefabTable").value.field("_sources").value;
                  const o = t.method("get_Count").invoke();
                  console.log("[DumpNetObj] ═══════════════════════════════════════");
                  console.log("[DumpNetObj] NetworkObject prefab table — " + o + " entries:");
                  const n = [];
                  for (let e = 0; e < o; e++) {
                    try {
                      const o = t.method("get_Item").invoke(e).method("get_Description").invoke().toString();
                      console.log("[DumpNetObj]  [" + e + "] " + o);
                      n.push(o);
                    } catch (t) {
                      console.log("[DumpNetObj]  [" + e + "] <error reading entry>");
                    }
                  }
                  console.log("[DumpNetObj] ═══════════════════════════════════════");
                  sendNotification("Dumped " + o + " net prefabs — see Frida log", false, 5);
                } catch (e) {
                  sendNotification("Dump Net Objects: " + e, false, 5);
                  console.error("[DumpNetObj] Error:", e);
                }
              },
              isTogglable: false,
              toolTip: "Logs every spawnable NetworkObject prefab name from the Fusion PrefabTable to the Frida console."
            }), new ButtonInfo({
              buttonText: "Dump Menu TXT",
              method: () => {
                sendNotification(dumpMenuToTextFile() ? "Dumped menu to C:Program Files (x86)SteamsteamappscommonAnimal Company" : "Dump menu failed", false);
              },
              isTogglable: false,
              toolTip: "Dumps the full runtime menu/button/id layout to a txt file beside this script."
            }), new ButtonInfo({
              buttonText: "Inf Damage",
              enableMethod: () => {
                infDamage = true;
              },
              disableMethod: () => {
                infDamage = false;
              },
              isTogglable: true,
              toolTip: "Makes all weapons deal 999999 damage per shot/hit."
            }), new ButtonInfo({
              buttonText: "Get Player Info Gun",
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && !(time <= idGunDelay)) {
                  idGunDelay = time + 0.25;
                  try {
                    const t = resolveGunTargetPlayer(e, 10);
                    if (!t || t.isNull?.()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    dumpPlayerInfoToTerminal(t);
                    sendNotification("Dumped info for " + getPlayerName(t), false);
                  } catch (e) {
                    console.error("Get Player Info Gun:", e);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Aim at a player and dump their names, IDs, tokens, and related info into the terminal."
            }), new ButtonInfo({
              buttonText: "Start Arena",
              method: () => {
                try {
                  ArenaGameManager.method("StartGame").invoke();
                  sendNotification("Arena started!", false);
                } catch (e) {
                  sendNotification("Start Arena: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Starts the arena game."
            }), new ButtonInfo({
              buttonText: "End Arena",
              method: () => {
                try {
                  ArenaGameManager.method("EndGame").invoke();
                  sendNotification("Arena ended!", false);
                } catch (e) {
                  sendNotification("End Arena: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Ends the arena game."
            }), new ButtonInfo({
              buttonText: "Arena Spammer",
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 1.5;
                  try {
                    ArenaGameManager.method("StartGame").invoke();
                  } catch (e) {}
                  try {
                    ArenaGameManager.method("EndGame").invoke();
                  } catch (e) {}
                  sendNotification("Arena spam tick!", false);
                }
              },
              isTogglable: true,
              toolTip: "Rapidly starts and ends the arena every 1.5 s while toggled."
            }), new ButtonInfo({
              buttonText: "set held item value 300k",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.isNull?.()) {
                    return;
                  }
                  for (let t = 0; t < 2; t++) {
                    try {
                      const o = e.method("GetHandInteractor", 1).invoke(t);
                      if (!o || o.isNull?.()) {
                        heldItemValue300kLastHandles[t] = "";
                        continue;
                      }
                      const n = o.field("_itemAnchor").value;
                      if (!n || n.isNull?.()) {
                        heldItemValue300kLastHandles[t] = "";
                        continue;
                      }
                      const i = n.method("get_grabbableObject").invoke();
                      if (!i || i.isNull?.()) {
                        heldItemValue300kLastHandles[t] = "";
                        continue;
                      }
                      const a = i.handle?.toString() ?? i.toString();
                      if (heldItemValue300kLastHandles[t] === a) {
                        continue;
                      }
                      i.method("RPC_SetAdditionalSellValue").invoke(300000);
                      heldItemValue300kLastHandles[t] = a;
                    } catch (e) {
                      heldItemValue300kLastHandles[t] = "";
                    }
                  }
                } catch (e) {
                  console.error("[Held Item Value 300k]", e);
                }
              },
              disableMethod: () => {
                heldItemValue300kLastHandles = ["", ""];
              },
              isTogglable: true,
              toolTip: "whatever you pick up that item will sell for 300k."
            })], [new ButtonInfo({
              buttonText: "Exit Whitelist mods",
              method: () => {
                currentCategory = 21;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns to Other Players."
            }), new ButtonInfo({
              buttonText: "Toggle Whitelist",
              method: () => {
                sendNotification("Whitelist: " + ((whitelistEnabled = !whitelistEnabled) ? "ON" : "OFF"), false);
                reloadMenu();
              },
              isTogglable: false,
              toolTip: "Enables or disables the whitelist system."
            }), new ButtonInfo({
              buttonText: "Whitelist gun",
              method: () => {
                if (!rightGrab) {
                  gunColor = [0.08, 0.08, 0.08, 0.75];
                  return;
                }
                const e = renderGun().ray;
                let t = null;
                if (e) {
                  const o = getComponentInParent(e.method("get_collider").invoke(), NetPlayer);
                  if (!o || o.handle.isNull() || playerIsLocal(o)) {
                    gunColor = [0.08, 0.08, 0.08, 0.75];
                  } else {
                    t = o;
                    let e = false;
                    for (let o = 0; o < whitelist.length; o++) {
                      if (whitelist[o].field("_userID").value.toString() === t.field("_userID").value.toString()) {
                        e = true;
                        break;
                      }
                    }
                    gunColor = e ? [0.2, 1, 0.3, 1] : [1, 0.2, 0.2, 1];
                  }
                }
                if (rightTrigger && time > idGunDelay && (idGunDelay = time + 0.2, t && !t.isNull())) {
                  let e = [];
                  let o = false;
                  for (let n = 0; n < whitelist.length; n++) {
                    const i = whitelist[n];
                    if (i.field("_userID").value.toString() === t.field("_userID").value.toString()) {
                      o = true;
                    } else {
                      e.push(i);
                    }
                  }
                  if (o) {
                    whitelist = e;
                    sendNotification("Removed from whitelist", false);
                    gunColor = [1, 0.2, 0.2, 1];
                    return;
                  }
                  whitelist.push(t);
                  sendNotification("Added to whitelist", false);
                  gunColor = [0.2, 1, 0.3, 1];
                }
              },
              isTogglable: true,
              toolTip: "Point at player and pull trigger to whitelist/unwhitelist them. Green = whitelisted, Red = not.",
              disableMethod: () => {
                gunColor = [0.08, 0.08, 0.08, 0.75];
              }
            }), new ButtonInfo({
              buttonText: "Unwhitelist ALL",
              method: () => {
                whitelist = [];
                sendNotification("Whitelist cleared!", false);
              },
              isTogglable: false,
              toolTip: "Removes all players from the whitelist."
            }), new ButtonInfo({
              buttonText: "Whitelist All Players",
              method: () => {
                const e = Object.method("FindObjectsOfType").inflate(NetPlayer).invoke();
                if (e) {
                  whitelist = [];
                  for (let t = 0; t < e.length; t++) {
                    const o = e.get(t);
                    if (!playerIsLocal(o)) {
                      if (o && !o.handle.isNull()) {
                        whitelist.push(o);
                      }
                    }
                  }
                  sendNotification(`Whitelisted ${whitelist.length} players`, false);
                  gunColor = [0.2, 1, 0.3, 1];
                }
              },
              isTogglable: false,
              toolTip: "Whitelists every remote player currently in the instance."
            }), new ButtonInfo({
              buttonText: "List Whitelist",
              method: () => {
                if (whitelist.length === 0) {
                  sendNotification("Whitelist is empty", false, 4);
                  return;
                }
                let e = [];
                for (let t = 0; t < whitelist.length; t++) {
                  const o = whitelist[t];
                  if (!o || o.handle.isNull()) {
                    continue;
                  }
                  let n = "Unknown";
                  try {
                    n = o.field("_displayName")?.value?.toString() || o.field("_userID")?.value?.toString() || "Unknown";
                  } catch (e) {}
                  e.push(n);
                }
                sendNotification("WL (" + e.length + "): " + e.join(", "), false, 8);
              },
              isTogglable: false,
              toolTip: "Shows all currently whitelisted players."
            }), new ButtonInfo({
              buttonText: "WL Hand RPG",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.05;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          spawnNetworkPrefab("RPGRocket", e.method("get_position").invoke(), e.method("get_rotation").invoke());
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players can shoot rockets by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "WL Hand Grenade",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.05;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          spawnNetworkPrefab("GrenadeProjectile", e.method("get_position").invoke(), e.method("get_rotation").invoke());
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players can shoot rockets by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "WL Hand RPG Shoe",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.05;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          spawnNetworkPrefab("RPGRocketShoe", e.method("get_position").invoke(), e.method("get_rotation").invoke());
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players can shoot rockets by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "WL Hand Boomspear",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.05;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          spawnNetworkPrefab("RPGRocketSpear", e.method("get_position").invoke(), e.method("get_rotation").invoke());
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players can shoot rockets by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "Whitelist Hand Egg",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.3;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          const o = spawnNetworkPrefab("ExplosiveEgg", e.method("get_position").invoke(), e.method("get_rotation").invoke());
                          if (!o) {
                            continue;
                          }
                          const n = o.method("GetComponent", 1).inflate(Rigidbody).invoke();
                          if (n && !n.isNull()) {
                            n.method("set_isKinematic").invoke(false);
                            n.method("WakeUp").invoke();
                            n.method("set_linearVelocity").invoke(Vector3.method("op_Multiply", 2).invoke(e.method("get_forward").invoke(), 20));
                          }
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players launch explosive eggs by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "WL Hand RobotDog RPG",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.05;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          spawnNetworkPrefab("RobotDogRPG", e.method("get_position").invoke(), e.method("get_rotation").invoke());
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players can shoot rockets by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "Whitelist Hand Flare",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.1;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          const o = spawnNetworkPrefab("FlareGunProjectile", e.method("get_position").invoke(), e.method("get_rotation").invoke());
                          if (!o) {
                            continue;
                          }
                          const n = o.method("GetComponent", 1).inflate(Rigidbody).invoke();
                          if (n && !n.isNull()) {
                            n.method("set_isKinematic").invoke(false);
                            n.method("WakeUp").invoke();
                            n.method("set_linearVelocity").invoke(Vector3.method("op_Multiply", 2).invoke(e.method("get_forward").invoke(), 20));
                          }
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players shoot flares by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "Whitelist Fly",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.02;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value.method("get_forward").invoke();
                          const o = Vector3.method("op_Multiply", 2).invoke(e, flySpeed * 0.08);
                          t.method("RPC_AddForce", 1).invoke(o);
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players can fly by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "WL Spawn UFO Hard",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.05;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          spawnNetworkPrefab("UFO_Hard", e.method("get_position").invoke(), e.method("get_rotation").invoke());
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players can shoot rockets by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "WL Spawn VS7",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.05;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          spawnNetworkPrefab("Vehicle_Spaceship_7", e.method("get_position").invoke(), e.method("get_rotation").invoke());
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players can shoot rockets by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "Whitelist Hand Car",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.5;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          const o = spawnNetworkPrefab("Vehicle_Buggy", e.method("get_position").invoke(), e.method("get_rotation").invoke());
                          if (!o) {
                            continue;
                          }
                          const n = o.method("GetComponent", 1).inflate(Rigidbody).invoke();
                          if (n && !n.isNull()) {
                            n.method("set_isKinematic").invoke(false);
                            n.method("WakeUp").invoke();
                            n.method("set_linearVelocity").invoke(Vector3.method("op_Multiply", 2).invoke(e.method("get_forward").invoke(), 20));
                          }
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players launch cars by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "Whitelist Hand Crate",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.1;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/item_crate"), e.method("get_position").invoke(), e.method("get_rotation").invoke(), NULL);
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players launch crates by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "Whitelist Hand Suitcases",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.1;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_pelican_case"), e.method("get_position").invoke(), e.method("get_rotation").invoke(), NULL);
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "dih."
            }), new ButtonInfo({
              buttonText: "Whitelist Hand Timebomb",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.1;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_timebomb"), e.method("get_position").invoke(), e.method("get_rotation").invoke(), NULL);
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "dih."
            }), new ButtonInfo({
              buttonText: "Whitelist Hand Rare Card",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.1;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_rare_card"), e.method("get_position").invoke(), e.method("get_rotation").invoke(), NULL);
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "dih."
            }), new ButtonInfo({
              buttonText: "Whitelist Hand Bomb",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.5;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          const o = spawnNetworkPrefab("BombController", e.method("get_position").invoke(), e.method("get_rotation").invoke());
                          if (!o) {
                            continue;
                          }
                          const n = o.method("GetComponent", 1).inflate(Rigidbody).invoke();
                          if (n && !n.isNull()) {
                            n.method("set_isKinematic").invoke(false);
                            n.method("WakeUp").invoke();
                            n.method("set_linearVelocity").invoke(Vector3.method("op_Multiply", 2).invoke(e.method("get_forward").invoke(), 20));
                          }
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players launch bombs by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "Whitelist Hand Balloon",
              method: () => {
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.3;
                  for (let e = 0; e < whitelist.length; e++) {
                    const t = whitelist[e];
                    if (t) {
                      try {
                        const e = getRightFingers(t);
                        if (!e || e.isNull()) {
                          continue;
                        }
                        if (e.field("_indexValue").value > 0.8 && e.field("_middleValue").value > 0.8 && e.field("_thumbValue").value > 0.8) {
                          const e = t.field("handRight").value;
                          spawnNetworkPrefab("InflatedBalloon", e.method("get_position").invoke(), e.method("get_rotation").invoke());
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players launch balloons by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "Whitelist Hand Giveaway",
              method: () => {
                function e(e, t) {
                  return Math.floor(Math.random() * (t - e + 1)) + e;
                }
                if (time > tagGunDelay) {
                  tagGunDelay = time + 0.3;
                  for (let t = 0; t < whitelist.length; t++) {
                    const o = whitelist[t];
                    if (o) {
                      try {
                        const t = getRightFingers(o);
                        if (!t || t.isNull()) {
                          continue;
                        }
                        if (t.field("_indexValue").value > 0.8 && t.field("_middleValue").value > 0.8 && t.field("_thumbValue").value > 0.8) {
                          const t = o.field("handRight").value;
                          const n = itemIDs[e(0, itemIDs.length - 1)];
                          PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + n), t.method("get_position").invoke(), t.method("get_rotation").invoke(), NULL);
                        }
                      } catch (e) {}
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Whitelisted players get random items by pointing fist (all fingers down)."
            }), new ButtonInfo({
              buttonText: "WL RPG Gun",
              enableMethod: () => {
                wlRpgTarget = null;
              },
              disableMethod: () => {
                wlRpgTarget = null;
              },
              isTogglable: true,
              method: () => {
                try {
                  if (rightGrab) {
                    const e = renderGun();
                    if (rightTrigger && time > tagGunDelay) {
                      tagGunDelay = time + 0.25;
                      const t = getWhitelistGunTarget(e);
                      if (t?.player) {
                        wlRpgTarget = t.player;
                        sendNotification("RPG target: " + t.id, false);
                      }
                    }
                  }
                  if (!wlRpgTarget) {
                    return;
                  }
                  try {
                    const e = Time.method("get_time").invoke();
                    const t = wlRpgTarget.handle.toString();
                    const o = wlRpgTarget.method("get_view").invoke();
                    if (!o || o.isNull?.()) {
                      wlRpgTarget = null;
                      return;
                    }
                    const n = checkOtherPlayerInputs(wlRpgTarget);
                    if (n.leftGrab && n.leftTrigger) {
                      if (e - (lastSpawnTimesLeft.get(t) || 0) > 0.05) {
                        try {
                          const n = o.field("_handTargetLeft").value;
                          if (n && !n.isNull()) {
                            const o = n.method("get_position").invoke();
                            spawnNetworkPrefab("RPGRocket", o, Quaternion.method("op_Multiply").invoke(n.method("get_rotation").invoke(), Quaternion.method("Euler").invoke(-90, 0, 0)));
                            lastSpawnTimesLeft.set(t, e);
                          }
                        } catch (e) {}
                      }
                    }
                    if (n.rightGrab && n.rightTrigger) {
                      if (e - (lastSpawnTimesRight.get(t) || 0) > 0.05) {
                        try {
                          const n = o.field("_handTargetRight").value;
                          if (n && !n.isNull()) {
                            const o = n.method("get_position").invoke();
                            spawnNetworkPrefab("RPGRocket", o, Quaternion.method("op_Multiply").invoke(n.method("get_rotation").invoke(), Quaternion.method("Euler").invoke(-90, 0, 0)));
                            lastSpawnTimesRight.set(t, e);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    if (e?.message?.includes("access violation")) {
                      wlRpgTarget = null;
                    } else {
                      console.error("WL RPG Gun:", e);
                    }
                  }
                } catch (e) {
                  console.error("WL RPG Gun outer:", e);
                }
              },
              toolTip: "Aim at a WL player + trigger to select them. They fire RPG rockets from their own hands with grip+trigger."
            }), new ButtonInfo({
              buttonText: "WL Egg Gun",
              enableMethod: () => {
                wlEggTarget = null;
              },
              disableMethod: () => {
                wlEggTarget = null;
              },
              isTogglable: true,
              method: () => {
                try {
                  if (rightGrab) {
                    const e = renderGun();
                    if (rightTrigger && time > tagGunDelay) {
                      tagGunDelay = time + 0.25;
                      const t = getWhitelistGunTarget(e);
                      if (t?.player) {
                        wlEggTarget = t.player;
                        sendNotification("Egg target: " + t.id, false);
                      }
                    }
                  }
                  if (!wlEggTarget) {
                    return;
                  }
                  try {
                    const e = Time.method("get_time").invoke();
                    const t = wlEggTarget.handle.toString();
                    const o = wlEggTarget.method("get_view").invoke();
                    if (!o || o.isNull?.()) {
                      wlEggTarget = null;
                      return;
                    }
                    const n = checkOtherPlayerInputs(wlEggTarget);
                    if (n.leftGrab && n.leftTrigger) {
                      if (e - (lastSpawnTimesLeft.get(t) || 0) > 0.05) {
                        try {
                          const n = o.field("_handTargetLeft").value;
                          if (n && !n.isNull()) {
                            const o = n.method("get_position").invoke();
                            spawnNetworkPrefab("RPGRocketEgg", o, Quaternion.method("op_Multiply").invoke(n.method("get_rotation").invoke(), Quaternion.method("Euler").invoke(-90, 0, 0)));
                            lastSpawnTimesLeft.set(t, e);
                          }
                        } catch (e) {}
                      }
                    }
                    if (n.rightGrab && n.rightTrigger) {
                      if (e - (lastSpawnTimesRight.get(t) || 0) > 0.05) {
                        try {
                          const n = o.field("_handTargetRight").value;
                          if (n && !n.isNull()) {
                            const o = n.method("get_position").invoke();
                            spawnNetworkPrefab("RPGRocketEgg", o, Quaternion.method("op_Multiply").invoke(n.method("get_rotation").invoke(), Quaternion.method("Euler").invoke(-90, 0, 0)));
                            lastSpawnTimesRight.set(t, e);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    if (e?.message?.includes("access violation")) {
                      wlEggTarget = null;
                    } else {
                      console.error("WL Egg Gun:", e);
                    }
                  }
                } catch (e) {
                  console.error("WL Egg Gun outer:", e);
                }
              },
              toolTip: "Aim at a WL player + trigger to select them. They fire Eggs from their own hands with grip+trigger."
            }), new ButtonInfo({
              buttonText: "WL BoomSpear Gun",
              enableMethod: () => {
                wlBoomspearTarget = null;
              },
              disableMethod: () => {
                wlBoomspearTarget = null;
              },
              isTogglable: true,
              method: () => {
                try {
                  if (rightGrab) {
                    const e = renderGun();
                    if (rightTrigger && time > tagGunDelay) {
                      tagGunDelay = time + 0.25;
                      const t = getWhitelistGunTarget(e);
                      if (t?.player) {
                        wlBoomspearTarget = t.player;
                        sendNotification("BoomSpear target: " + t.id, false);
                      }
                    }
                  }
                  if (!wlBoomspearTarget) {
                    return;
                  }
                  try {
                    const e = Time.method("get_time").invoke();
                    const t = wlBoomspearTarget.handle.toString();
                    const o = wlBoomspearTarget.method("get_view").invoke();
                    if (!o || o.isNull?.()) {
                      wlBoomspearTarget = null;
                      return;
                    }
                    const n = checkOtherPlayerInputs(wlBoomspearTarget);
                    if (n.leftGrab && n.leftTrigger) {
                      if (e - (lastSpawnTimesLeft.get(t) || 0) > 0.05) {
                        try {
                          const n = o.field("_handTargetLeft").value;
                          if (n && !n.isNull()) {
                            const o = n.method("get_position").invoke();
                            spawnNetworkPrefab("RPGRocketSpear", o, Quaternion.method("op_Multiply").invoke(n.method("get_rotation").invoke(), Quaternion.method("Euler").invoke(-90, 0, 0)));
                            lastSpawnTimesLeft.set(t, e);
                          }
                        } catch (e) {}
                      }
                    }
                    if (n.rightGrab && n.rightTrigger) {
                      if (e - (lastSpawnTimesRight.get(t) || 0) > 0.05) {
                        try {
                          const n = o.field("_handTargetRight").value;
                          if (n && !n.isNull()) {
                            const o = n.method("get_position").invoke();
                            spawnNetworkPrefab("RPGRocketSpear", o, Quaternion.method("op_Multiply").invoke(n.method("get_rotation").invoke(), Quaternion.method("Euler").invoke(-90, 0, 0)));
                            lastSpawnTimesRight.set(t, e);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    if (e?.message?.includes("access violation")) {
                      wlBoomspearTarget = null;
                    } else {
                      console.error("WL BoomSpear Gun:", e);
                    }
                  }
                } catch (e) {
                  console.error("WL BoomSpear Gun outer:", e);
                }
              },
              toolTip: "Aim at a WL player + trigger to select them. They fire BoomSpears from their own hands with grip+trigger."
            }), new ButtonInfo({
              buttonText: "WL Give Fly Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.03;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    const o = getPlayerHandAnchorTransform(t.player, 1) ?? getPlayerProjectileOriginTransform(t.player);
                    const n = o && !o.isNull?.() ? getLaunchForward(o, getTransform(t.player)) : getLaunchForward(getTransform(t.player), getTransform(headCollider));
                    t.player.method("RPC_AddForce").invoke(Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(n, 0.34), [0, 0.03, 0]));
                    sendNotification("Gave fly boost to " + t.id, false);
                  } catch (e) {
                    console.error("Give Fly Gun:", e);
                  }
                }
              },
              toolTip: "Point at a player and give them a smooth forward fly force (hold grip)."
            }), new ButtonInfo({
              buttonText: "Revive WL Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getTransform(e.gunPointer).method("get_position").invoke();
                    const o = NetPlayer.field("playerIDToNetPlayer").value;
                    const n = o.method("get_Values").invoke().method("GetEnumerator").invoke();
                    let i = null;
                    let a = 10;
                    while (n.method("MoveNext").invoke()) {
                      const e = n.method("get_Current").invoke();
                      if (!e || e.handle.isNull() || playerIsLocal(e)) {
                        continue;
                      }
                      if (whitelistEnabled && !whitelistHasPlayer(e)) {
                        continue;
                      }
                      try {
                        if (!e.method("get_isDie").invoke()) {
                          continue;
                        }
                      } catch (e) {
                        continue;
                      }
                      const o = Vector3.method("Distance").invoke(getTransform(e).method("get_position").invoke(), t);
                      if (o < a) {
                        a = o;
                        i = e;
                      }
                    }
                    if (i) {
                      i.method("RPC_DoPlayerDie").invoke(false);
                      sendNotification("Revived whitelisted player!", false);
                    } else {
                      sendNotification("No dead WL player near pointer", false);
                    }
                  } catch (e) {
                    console.error("Revive WL Gun:", e);
                  }
                }
              },
              toolTip: "Point at dead whitelisted player to revive them (hold grip)."
            }), new ButtonInfo({
              buttonText: "WL TP To Me Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    const o = NetPlayer.method("get_localPlayer").invoke();
                    if (!o || o.handle.isNull()) {
                      return;
                    }
                    const n = getTransform(o).method("get_position").invoke();
                    t.player.method("RPC_Teleport").invoke(n);
                    sendNotification("TP'd " + t.id + " to you!", false);
                  } catch (e) {
                    console.error("WL TP To Me:", e);
                  }
                }
              },
              toolTip: "Teleports a whitelisted player to your position (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Launch Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    t.player.method("RPC_AddForce").invoke([0, 1500, 0]);
                    sendNotification("Launched " + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Launch:", e);
                  }
                }
              },
              toolTip: "Launches a whitelisted player upward (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Selling Orbit Gun",
              disableMethod: () => {
                for (const e of wlSellingOrbitPrefabs) {
                  if (e && !e.isNull?.()) {
                    try {
                      const t = e.method("get_Runner").invoke();
                      if (t && !t.isNull()) {
                        t.method("Despawn").invoke(e);
                        continue;
                      }
                    } catch (e) {}
                    try {
                      const t = e.method("get_gameObject").invoke();
                      if (t && !t.isNull()) {
                        Destroy(t);
                      }
                    } catch (e) {}
                  }
                }
                wlSellingOrbitPrefabs = [];
                wlSellingOrbitEntries = [];
              },
              isTogglable: true,
              method: () => {
                try {
                  if (rightGrab) {
                    const e = renderGun();
                    if (rightTrigger && time > lagGunDelay) {
                      lagGunDelay = time + 0.18;
                      const t = getWhitelistGunTarget(e, true, 10);
                      if (t && t.player) {
                        if (!wlSellingOrbitEntries.some(e => e.player && !e.player.isNull?.() && e.player.handle.equals(t.player.handle))) {
                          for (let e = 0; e < 8; e++) {
                            const o = spawnNetworkPrefab("ItemSellingMachineController", zeroVector, identityQuaternion);
                            if (!o || o.isNull?.()) {
                              continue;
                            }
                            wlSellingOrbitPrefabs.push(o);
                            const n = o.method("get_gameObject").invoke();
                            if (!n || n.isNull()) {
                              continue;
                            }
                            const i = n.method("get_transform").invoke();
                            wlSellingOrbitEntries.push({
                              player: t.player,
                              transform: i,
                              angle: Math.PI * 2 / 8 * e
                            });
                          }
                          sendNotification("Selling orbit added to " + t.id, false);
                        }
                      }
                    }
                  }
                  for (const e of wlSellingOrbitEntries) {
                    try {
                      const t = e.player;
                      if (!t || t.isNull?.()) {
                        continue;
                      }
                      const o = getTransform(t).method("get_position").invoke();
                      e.angle += deltaTime * 1.8;
                      const n = o.field("x").value + Math.cos(e.angle) * 2.6;
                      const i = o.field("y").value + 0.35 + Math.sin(e.angle * 2 + time) * 0.18;
                      const a = o.field("z").value + Math.sin(e.angle) * 2.6;
                      e.transform.method("set_position").invoke([n, i, a]);
                    } catch (e) {}
                  }
                } catch (e) {
                  console.error("WL Selling Orbit Gun:", e);
                }
              },
              toolTip: "Touches a WL player with the gun and makes selling machines orbit them."
            }), new ButtonInfo({
              buttonText: "WL Orbit All Target Gun",
              enableMethod: () => {
                wlOrbitAllTarget = null;
                wlOrbitAllPhase = 0;
              },
              disableMethod: () => {
                wlOrbitAllTarget = null;
              },
              isTogglable: true,
              method: () => {
                try {
                  if (rightGrab) {
                    const e = renderGun();
                    if (rightTrigger && time > wlTargetActionDelay) {
                      wlTargetActionDelay = time + 0.25;
                      const t = getWhitelistGunTarget(e, true, 10);
                      if (t?.player) {
                        wlOrbitAllTarget = t.player;
                        sendNotification("Orbit target: " + t.id, false);
                      }
                    }
                  }
                  if (!wlOrbitAllTarget || wlOrbitAllTarget.isNull?.()) {
                    return;
                  }
                  const e = getTransform(wlOrbitAllTarget).method("get_position").invoke();
                  const t = getAllNetPlayersList(false);
                  wlOrbitAllPhase += deltaTime * 3.5;
                  let o = 0;
                  for (const n of t) {
                    try {
                      if (!n || n.isNull?.()) {
                        continue;
                      }
                      if (normalizeSceneObjectHandle(n) === normalizeSceneObjectHandle(wlOrbitAllTarget)) {
                        continue;
                      }
                      const i = wlOrbitAllPhase + Math.PI * 2 * o / Math.max(1, t.length);
                      n.method("RPC_Teleport").invoke([e.field("x").value + Math.cos(i) * 2.9, e.field("y").value + 1.15 + Math.sin(time * 2 + o) * 0.45, e.field("z").value + Math.sin(i) * 2.9]);
                      o++;
                    } catch (e) {}
                  }
                } catch (e) {
                  console.error("WL Orbit All Target Gun:", e);
                }
              },
              toolTip: "Aim a WL player once, then all players orbit them until toggled off."
            }), new ButtonInfo({
              buttonText: "WL Cum Gun",
              enableMethod: () => {
                wlPissTarget = null;
              },
              disableMethod: () => {
                wlPissTarget = null;
              },
              isTogglable: true,
              method: () => {
                try {
                  if (rightGrab) {
                    const e = renderGun();
                    if (rightTrigger && time > wlTargetActionDelay) {
                      wlTargetActionDelay = time + 0.25;
                      const t = getWhitelistGunTarget(e, true, 10);
                      if (t?.player) {
                        wlPissTarget = t.player;
                        sendNotification("Piss target: " + t.id, false);
                      }
                    }
                  }
                  if (!wlPissTarget || wlPissTarget.isNull?.() || time <= goopSpamDelay) {
                    return;
                  }
                  goopSpamDelay = time + 0.055;
                  spawnGoopBurstAtTransform(getTransform(wlPissTarget), 56, 100, 1, 2.2, 5.2);
                } catch (e) {
                  console.error("WL Piss Gun:", e);
                }
              },
              toolTip: "Aim a WL player once and continuously gives them the piss stream."
            }), new ButtonInfo({
              buttonText: "WL Gun Buff Target Gun",
              enableMethod: () => {
                wlGunBuffTarget = null;
              },
              disableMethod: () => {
                wlGunBuffTarget = null;
              },
              isTogglable: true,
              method: () => {
                try {
                  if (rightGrab) {
                    const e = renderGun();
                    if (rightTrigger && time > wlTargetActionDelay) {
                      wlTargetActionDelay = time + 0.25;
                      const t = getWhitelistGunTarget(e, true, 10);
                      if (t?.player) {
                        wlGunBuffTarget = t.player;
                        sendNotification("Gun buff target: " + t.id, false);
                      }
                    }
                  }
                  if (!wlGunBuffTarget || wlGunBuffTarget.isNull?.()) {
                    return;
                  }
                  for (const e of [0, 1]) {
                    try {
                      applyGunBuffsToGrabbable(getPlayerHeldGrabbable(wlGunBuffTarget, e), true);
                    } catch (e) {}
                  }
                } catch (e) {
                  console.error("WL Gun Buff Target Gun:", e);
                }
              },
              toolTip: "Aim a WL player once and keeps any guns they hold buffed."
            }), new ButtonInfo({
              buttonText: "WL Right Hand Duper Gun",
              enableMethod: () => {
                wlRightHandDuperTarget = null;
              },
              disableMethod: () => {
                wlRightHandDuperTarget = null;
              },
              isTogglable: true,
              method: () => {
                try {
                  if (rightGrab) {
                    const e = renderGun();
                    if (rightTrigger && time > wlTargetActionDelay) {
                      wlTargetActionDelay = time + 0.25;
                      const t = getWhitelistGunTarget(e, true, 10);
                      if (t?.player) {
                        wlRightHandDuperTarget = t.player;
                        sendNotification("Right-hand duper: " + t.id, false);
                      }
                    }
                  }
                  if (!wlRightHandDuperTarget || wlRightHandDuperTarget.isNull?.() || time <= heldItemDuplicateDelay) {
                    return;
                  }
                  heldItemDuplicateDelay = time + 0.08;
                  spawnHeldItemDuplicateForPlayer(wlRightHandDuperTarget, 12.5);
                } catch (e) {
                  console.error("WL Right Hand Duper Gun:", e);
                }
              },
              toolTip: "Aim a WL player once and makes them spam-dupe whatever is in their right hand."
            }), new ButtonInfo({
              buttonText: "WL Item Duper Hand Gun",
              enableMethod: () => {
                wlItemDuperHandTarget = null;
                wlItemDuperHandDelay = 0;
              },
              disableMethod: () => {
                wlItemDuperHandTarget = null;
                wlItemDuperHandDelay = 0;
              },
              isTogglable: true,
              method: () => {
                try {
                  if (rightGrab) {
                    const e = renderGun();
                    if (rightTrigger && time > wlTargetActionDelay) {
                      wlTargetActionDelay = time + 0.25;
                      const t = getWhitelistGunTarget(e, true, 10);
                      if (t?.player) {
                        wlItemDuperHandTarget = t.player;
                        wlItemDuperHandDelay = 0;
                        sendNotification("Item duper hand: " + t.id, false);
                      }
                    }
                  }
                  if (!wlItemDuperHandTarget || wlItemDuperHandTarget.isNull?.() || time <= wlItemDuperHandDelay) {
                    return;
                  }
                  wlItemDuperHandDelay = time + 0.075;
                  spawnHeldItemDuplicateForPlayer(wlItemDuperHandTarget, 12.5);
                } catch (e) {
                  console.error("WL Item Duper Hand Gun:", e);
                }
              },
              toolTip: "Aim a WL player once and gives them the right-hand item duper hand effect."
            }), new ButtonInfo({
              buttonText: "WL Stash Eject Duper Gun",
              enableMethod: () => {
                wlStashEjectDuperTarget = null;
              },
              disableMethod: () => {
                wlStashEjectDuperTarget = null;
              },
              isTogglable: true,
              method: () => {
                try {
                  if (rightGrab) {
                    const e = renderGun();
                    if (rightTrigger && time > wlTargetActionDelay) {
                      wlTargetActionDelay = time + 0.25;
                      const t = getWhitelistGunTarget(e, true, 10);
                      if (t?.player) {
                        wlStashEjectDuperTarget = t.player;
                        sendNotification("Stash eject duper: " + t.id, false);
                      }
                    }
                  }
                  if (!wlStashEjectDuperTarget || wlStashEjectDuperTarget.isNull?.() || time <= heldItemDuplicateDelay) {
                    return;
                  }
                  heldItemDuplicateDelay = time + 0.16;
                  try {
                    spawnHeldItemEjectBurstForPlayer(wlStashEjectDuperTarget, Math.max(2, ejectDupeAmount));
                  } catch (e) {}
                } catch (e) {
                  console.error("WL Stash Eject Duper Gun:", e);
                }
              },
              toolTip: "Aim a WL player once and gives them a stash-style eject dupe burst from their right hand."
            }), new ButtonInfo({
              buttonText: "WL Kill Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    t.player.method("RPC_DoPlayerDie").invoke(true);
                    sendNotification("Killed " + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Kill:", e);
                  }
                }
              },
              toolTip: "Kills a whitelisted player (hold grip + trigger). Must be in whitelist."
            }), new ButtonInfo({
              buttonText: "WL Stinky Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    t.player.method("RPC_TagAsStinky").invoke();
                    sendNotification("Stinkied " + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Stinky:", e);
                  }
                }
              },
              toolTip: "Tags a whitelisted player as stinky (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Color Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    const o = time * 0.5 % 1;
                    t.player.method("RPC_SetColorHSV").invoke(99999, o, 1, 1);
                    sendNotification("Rainbow'd " + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Color:", e);
                  }
                }
              },
              toolTip: "Sets a whitelisted player to a rainbow color (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Speed Buff Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    t.player.method("RPC_ApplyBuff").invoke(1);
                    sendNotification("Speed buff -> " + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Speed Buff:", e);
                  }
                }
              },
              toolTip: "Gives speed buff to a whitelisted player (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL AntiGrav Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    t.player.method("RPC_ApplyBuff").invoke(7);
                    sendNotification("AntiGrav buff -> " + t.id + "!", false);
                  } catch (e) {
                    console.error("WL AntiGrav:", e);
                  }
                }
              },
              toolTip: "Gives anti-gravity buff to a whitelisted player (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Scale Big Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    try {
                      t.player.method("set_playerScale").invoke(5);
                    } catch (e) {}
                    try {
                      t.player.field("_playerScale").value = 5;
                    } catch (e) {}
                    sendNotification("Scaled up " + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Scale Big:", e);
                  }
                }
              },
              toolTip: "Sets a whitelisted player to big scale (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Scale Tiny Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    try {
                      t.player.method("set_playerScale").invoke(0.1);
                    } catch (e) {}
                    try {
                      t.player.field("_playerScale").value = 0.1;
                    } catch (e) {}
                    sendNotification("Shrunk " + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Scale Tiny:", e);
                  }
                }
              },
              toolTip: "Sets a whitelisted player to tiny scale (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Stun Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    const o = getTransform(t.player).method("get_position").invoke();
                    t.player.method("RPC_PlayerStun").invoke(o, 5, 5, 1);
                    sendNotification("Stunned " + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Stun:", e);
                  }
                }
              },
              toolTip: "Stuns a whitelisted player for 5s (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Invisible Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    const o = t.player.method("get_isHide").invoke();
                    t.player.method("set_isHide").invoke(!o);
                    sendNotification((o ? "Showed " : "Hid ") + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Invisible:", e);
                  }
                }
              },
              toolTip: "Toggles visibility of a whitelisted player (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Disintegrate Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    const o = getTransform(t.player).method("get_position").invoke();
                    const n = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                    const i = [VFXTypes.MeatExplosion_1, VFXTypes.MeatExplosion_2, VFXTypes.MeatExplosion_Headshot, VFXTypes.Explosion_Coins, VFXTypes.Explosion_Feathers, VFXTypes.ConfettiBurst, VFXTypes.Ethereal_Void, VFXTypes.Electricity_Small, VFXTypes.FuelExplosion];
                    for (const e of i) {
                      try {
                        ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(n, e, o, identityQuaternion);
                      } catch (e) {}
                    }
                    t.player.method("RPC_Teleport").invoke([0, -9999999, 0]);
                    t.player.method("RPC_AddForce").invoke([0, 2000, 0]);
                    sendNotification("Disintegrated WL player: " + t.id, false);
                  } catch (e) {
                    console.error("WL Disintegrate Gun:", e);
                  }
                }
              },
              toolTip: "Disintegrates a whitelisted player with VFX (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Freeze Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    const o = getTransform(t.player).method("get_position").invoke();
                    t.player.method("RPC_PlayerStun").invoke(o, 999, 9999, 0);
                    sendNotification("Froze WL player: " + t.id, false);
                  } catch (e) {
                    console.error("WL Freeze Gun:", e);
                  }
                }
              },
              toolTip: "Freezes a whitelisted player indefinitely (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Hit 50 Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    const o = getTransform(t.player).method("get_position").invoke();
                    const n = DamageSourceInfoClass.method("get_Null").invoke();
                    t.player.method("RPC_PlayerHit", 3).invoke(50, o, n);
                    sendNotification("Hit WL player 50dmg: " + t.id, false);
                  } catch (e) {
                    console.error("WL Hit 50:", e);
                  }
                }
              },
              toolTip: "Deals 50 damage to a whitelisted player (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Void Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    t.player.method("RPC_Teleport").invoke([0, -99999, 0]);
                    sendNotification("Voided WL player: " + t.id, false);
                  } catch (e) {
                    console.error("WL Void Gun:", e);
                  }
                }
              },
              toolTip: "Teleports a whitelisted player to the void (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Revive All WL",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e) && (!whitelistEnabled || whitelistHasPlayer(e))) {
                      try {
                        e.method("RPC_DoPlayerDie").invoke(false);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Revived " + o + " WL players!", false);
                } catch (e) {
                  sendNotification("WL Revive All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Revives all whitelisted players."
            }), new ButtonInfo({
              buttonText: "WL TP All WL 2 Me",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = getTransform(e).method("get_position").invoke();
                  const o = NetPlayer.field("playerIDToNetPlayer").value;
                  const n = o.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let i = 0;
                  while (n.method("MoveNext").invoke()) {
                    const e = n.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e) && (!whitelistEnabled || whitelistHasPlayer(e))) {
                      try {
                        e.method("RPC_Teleport").invoke(t);
                        i++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("TP'd " + i + " WL players to you!", false);
                } catch (e) {
                  sendNotification("WL TP All 2 Me: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Teleports all whitelisted players to your position."
            })], [new ButtonInfo({
              buttonText: "Settings",
              method: () => {
                currentCategory = 2;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens settings."
            }), new ButtonInfo({
              buttonText: "Player",
              method: () => {
                currentCategory = 4;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "player mods."
            }), new ButtonInfo({
              buttonText: "Other Players",
              method: () => {
                currentCategory = 21;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens the other players mods category."
            }), new ButtonInfo({
              buttonText: "Item mods",
              method: () => {
                currentCategory = 6;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens the item mods category."
            }), new ButtonInfo({
              buttonText: "Mob mods",
              method: () => {
                currentCategory = 15;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens the mob mods category."
            }), new ButtonInfo({
              buttonText: "Prefab mods",
              method: () => {
                currentCategory = 14;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens the prefab mods category."
            }), new ButtonInfo({
              buttonText: "Server Codes",
              method: () => {
                roomCodeQuery = "";
                currentCategory = 80;
                currentPage = 0;
                sendNotification("Code: _", false, 99);
              },
              isTogglable: false,
              toolTip: "Opens the A-Z room-code keyboard for PC."
            }), new ButtonInfo({
              buttonText: "Visuals",
              method: () => {
                currentCategory = 106;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens the weird visuals category."
            }), new ButtonInfo({
              buttonText: "Exploits",
              method: () => {
                currentCategory = 9;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens the exploits category."
            }), new ButtonInfo({
              buttonText: "Rig Mods",
              method: () => {
                currentCategory = 77;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens the rig mods category."
            }), new ButtonInfo({
              buttonText: "Text Mods",
              method: () => {
                currentCategory = 103;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Type text and spawn it from Hell Ore with a gun pointer."
            }), new ButtonInfo({
              buttonText: "Telekinesis Mods",
              method: () => {
                currentCategory = 111;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Open Telekinesis mods."
            }), new ButtonInfo({
              buttonText: "Mom Boss Mods",
              method: () => {
                currentCategory = 112;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens the mom boss mods category."
            }), new ButtonInfo({
              buttonText: "Soundboard",
              method: () => {
                currentCategory = 32;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens the soundboard category."
            }), assetBundlesMenuButton, new ButtonInfo({
              buttonText: "Credits",
              method: () => {
                currentCategory = 7;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Opens the credits category."
            })], [new ButtonInfo({
              buttonText: "Exit Themes",
              method: () => {
                currentCategory = 2;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns to main menu."
            }), new ButtonInfo({
              buttonText: "Theme: Royal Blue",
              method: () => selectTheme(0, "Royal Blue"),
              isTogglable: false,
              toolTip: "Animated royal blue pulse theme."
            }), new ButtonInfo({
              buttonText: "Theme: Ocean Blue (default)",
              method: () => selectTheme(22, "Ocean Blue"),
              isTogglable: false,
              toolTip: "ocean blue theme has been set."
            }), new ButtonInfo({
              buttonText: "Theme: Rainbow",
              method: () => selectTheme(1, "Animated Rainbow"),
              isTogglable: false,
              toolTip: "Animated full HSV rainbow cycle - colors change continuously."
            }), new ButtonInfo({
              buttonText: "Theme: Neon Green",
              method: () => selectTheme(2, "Neon Green"),
              isTogglable: false,
              toolTip: "Animated neon green theme."
            }), new ButtonInfo({
              buttonText: "Theme: Black Gray",
              method: () => selectTheme(21, "Black Gray"),
              isTogglable: false,
              toolTip: "Animated black gray theme."
            }), new ButtonInfo({
              buttonText: "Theme: Black Red",
              method: () => selectTheme(3, "Blood Red"),
              isTogglable: false,
              toolTip: "Animated black red theme."
            }), new ButtonInfo({
              buttonText: "Theme: Stupid Orange",
              method: () => selectTheme(14, "Stupid Orange"),
              isTogglable: false,
              toolTip: "Animated bright orange menu theme."
            }), new ButtonInfo({
              buttonText: "Theme: Toxic Lime",
              method: () => selectTheme(5, "Toxic Lime"),
              isTogglable: false,
              toolTip: "Animated toxic lime arcade theme."
            }), new ButtonInfo({
              buttonText: "Theme: Sunset Pink",
              method: () => selectTheme(6, "Sunset Pink"),
              isTogglable: false,
              toolTip: "Animated hot pink and orange sunset theme."
            }), new ButtonInfo({
              buttonText: "Theme: Ice Cyan",
              method: () => selectTheme(15, "Ice Cyan"),
              isTogglable: false,
              toolTip: "Animated frosty cyan theme."
            }), new ButtonInfo({
              buttonText: "Theme: Pizza Menu",
              method: () => selectTheme(19, "Pizza Menu"),
              isTogglable: false,
              toolTip: "Pizza menu theme."
            }), new ButtonInfo({
              buttonText: "Theme: Plasma Purple",
              method: () => selectTheme(9, "Plasma Purple"),
              isTogglable: false,
              toolTip: "Animated neon plasma purple theme."
            }), new ButtonInfo({
              buttonText: "<size=5>Theme: Full Black</size>",
              method: () => selectTheme(10, "Full Black"),
              isTogglable: false,
              toolTip: "theme set."
            }), new ButtonInfo({
              buttonText: "<size=5>Theme: blackout gray</size>",
              method: () => selectTheme(12, "Blackout Gray"),
              isTogglable: false,
              toolTip: "theme set."
            }), new ButtonInfo({
              buttonText: "<size=5>Theme: sum blue</size>",
              method: () => selectTheme(13, "Sum Blue"),
              isTogglable: false,
              toolTip: "theme set."
            }), new ButtonInfo({
              buttonText: "<size=5>Theme: larplol</size>",
              method: () => selectTheme(16, "Larplol"),
              isTogglable: false,
              toolTip: "theme set."
            }), new ButtonInfo({
              buttonText: "Theme: Invinci-Blue",
              method: () => selectTheme(17, "Invinci-Blue"),
              isTogglable: false,
              toolTip: "Animated blue & yellow menu theme."
            }), new ButtonInfo({
              buttonText: "Theme: Ying Yang",
              method: () => selectTheme(18, "Yin Yang"),
              isTogglable: false,
              toolTip: "Animated black and white menu theme."
            })], [new ButtonInfo({
              buttonText: "Exit RPC Stuff",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns to main menu."
            }), new ButtonInfo({
              buttonText: "Launch Me Up",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  selfRPC(() => e.method("RPC_AddForce").invoke([0, 1500, 0]));
                  sendNotification("Launch!", false);
                } catch (e) {
                  sendNotification("Launch: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Launches yourself upward via RPC_AddForce."
            }), new ButtonInfo({
              buttonText: "RPC Stun Me",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = getTransform(e).method("get_position").invoke();
                  selfRPC(() => e.method("RPC_PlayerStun").invoke(t, 5, 3, 1));
                  sendNotification("Stunned for 3s!", false);
                } catch (e) {
                  sendNotification("Stun: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Stuns yourself for 3 seconds."
            }), new ButtonInfo({
              buttonText: "RPC Award Kill",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  selfRPC(() => e.method("RPC_AwardKill").invoke());
                  sendNotification("Kill awarded!", false);
                } catch (e) {
                  sendNotification("Award Kill: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Awards a kill to yourself."
            }), new ButtonInfo({
              buttonText: "RPC Set Red Team",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  selfRPC(() => e.method("RPC_SetTeam").invoke(1));
                  sendNotification("Red team!", false);
                } catch (e) {
                  sendNotification("Set Team: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets your team to Red."
            }), new ButtonInfo({
              buttonText: "RPC Set Blue Team",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  selfRPC(() => e.method("RPC_SetTeam").invoke(2));
                  sendNotification("Blue team!", false);
                } catch (e) {
                  sendNotification("Set Team: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets your team to Blue."
            }), new ButtonInfo({
              buttonText: "RPC Player Hit 50",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = getTransform(e).method("get_position").invoke();
                  const o = DamageSourceInfoClass.method("get_Null").invoke();
                  selfRPC(() => e.method("RPC_PlayerHit", 3).invoke(50, t, o));
                  sendNotification("Hit for 50dmg!", false);
                } catch (e) {
                  sendNotification("Player Hit: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Deals 50 damage to yourself."
            }), new ButtonInfo({
              buttonText: "RPC Player Hit 1",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = getTransform(e).method("get_position").invoke();
                  const o = DamageSourceInfoClass.method("get_Null").invoke();
                  selfRPC(() => e.method("RPC_PlayerHit", 3).invoke(1, t, o));
                  sendNotification("Hit for 1dmg!", false);
                } catch (e) {
                  sendNotification("Player Hit: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Deals 1 damage to yourself."
            }), new ButtonInfo({
              buttonText: "RPC Tag Stinky",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  selfRPC(() => e.method("RPC_TagAsStinky").invoke());
                  sendNotification("Tagged as stinky!", false);
                } catch (e) {
                  sendNotification("Tag Stinky: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Tags yourself as stinky."
            }), new ButtonInfo({
              buttonText: "RPC Teleport Up",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = getTransform(e).method("get_position").invoke();
                  const o = [t.field("x").value, t.field("y").value + 20, t.field("z").value];
                  selfRPC(() => e.method("RPC_Teleport").invoke(o));
                  sendNotification("Teleported up!", false);
                } catch (e) {
                  sendNotification("TP Up: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Teleports you 20 units upward."
            }), new ButtonInfo({
              buttonText: "RPC Color Red",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  selfRPC(() => e.method("RPC_SetColorHSV").invoke(99999, 0, 1, 1));
                  sendNotification("Color: Red!", false);
                } catch (e) {
                  sendNotification("Color: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets your color to red permanently."
            }), new ButtonInfo({
              buttonText: "RPC Color Reset",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  selfRPC(() => e.method("RPC_SetColorHSV").invoke(0, 0, 0, 0));
                  sendNotification("Color reset!", false);
                } catch (e) {
                  sendNotification("Color reset: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Resets your color."
            }), new ButtonInfo({
              buttonText: "RPC End Arena",
              method: () => {
                try {
                  const e = ArenaGameManager.field("_instance").value;
                  if (!e || e.isNull()) {
                    sendNotification("Not in arena", false);
                    return;
                  }
                  e.method("RPC_EndEarly").invoke();
                  sendNotification("Arena ended!", false);
                } catch (e) {
                  sendNotification("End Arena: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Ends the arena early via RPC."
            }), new ButtonInfo({
              buttonText: "RPC Force Ragdoll",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = getTransform(e).method("get_position").invoke();
                  selfRPC(() => e.method("RPC_PlayerStun").invoke(t, 999, 99, 0));
                  sendNotification("Force ragdolled!", false);
                } catch (e) {
                  sendNotification("Ragdoll: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Stuns with huge range/duration to force ragdoll."
            }), new ButtonInfo({
              buttonText: "RPC Buff SpeedBoost",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = AssemblyCSharp.class("AnimalCompany.PlayerBuffSheet").method("get_instance").invoke();
                  if (!t || t.isNull()) {
                    sendNotification("No buff sheet found", false);
                    return;
                  }
                  const o = t.method("GetAll").invoke();
                  if (!o || o.isNull()) {
                    e.method("RPC_ApplyBuff").invoke(1);
                    sendNotification("Speed buff sent (raw ID 1)!", false);
                    return;
                  }
                  let n = false;
                  const i = o.method("GetEnumerator").invoke();
                  while (i.method("MoveNext").invoke()) {
                    try {
                      const t = i.method("get_Current").invoke();
                      if (!t || t.isNull()) {
                        continue;
                      }
                      if (t.field("buff").value === 1) {
                        e.method("ApplyBuff").invoke(t);
                        n = true;
                        break;
                      }
                    } catch (e) {}
                  }
                  if (!n) {
                    e.method("RPC_ApplyBuff").invoke(1);
                  }
                  sendNotification("Speed buff applied!", false);
                } catch (e) {
                  try {
                    NetPlayer.method("get_localPlayer").invoke().method("RPC_ApplyBuff").invoke(1);
                  } catch (e) {}
                  sendNotification("Speed buff sent!", false);
                }
              },
              isTogglable: false,
              toolTip: "Applies SpeedBoost buff to yourself."
            }), new ButtonInfo({
              buttonText: "RPC Buff JumpBoost",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = AssemblyCSharp.class("AnimalCompany.PlayerBuffSheet").method("get_instance").invoke();
                  if (!t || t.isNull()) {
                    e.method("RPC_ApplyBuff").invoke(7);
                    sendNotification("AntiGrav buff sent!", false);
                    return;
                  }
                  const o = t.method("GetAll").invoke();
                  let n = false;
                  if (o && !o.isNull()) {
                    const t = o.method("GetEnumerator").invoke();
                    while (t.method("MoveNext").invoke()) {
                      try {
                        const o = t.method("get_Current").invoke();
                        if (!o || o.isNull()) {
                          continue;
                        }
                        if (o.field("buff").value === 7) {
                          e.method("ApplyBuff").invoke(o);
                          n = true;
                          break;
                        }
                      } catch (e) {}
                    }
                  }
                  if (!n) {
                    e.method("RPC_ApplyBuff").invoke(7);
                  }
                  sendNotification("AntiGravity buff applied!", false);
                } catch (e) {
                  try {
                    NetPlayer.method("get_localPlayer").invoke().method("RPC_ApplyBuff").invoke(7);
                  } catch (e) {}
                  sendNotification("AntiGrav buff sent!", false);
                }
              },
              isTogglable: false,
              toolTip: "Applies AntiGravity buff to yourself."
            }), new ButtonInfo({
              buttonText: "RPC No Team",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  selfRPC(() => e.method("RPC_SetTeam").invoke(0));
                  sendNotification("No team set!", false);
                } catch (e) {
                  sendNotification("Set Team: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Removes you from any team (team 0)."
            }), new ButtonInfo({
              buttonText: "RPC Kill All",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_DoPlayerDie").invoke(true);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Killed " + o + " players!", false);
                } catch (e) {
                  sendNotification("Kill All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Kills all players via RPC_DoPlayerDie."
            }), new ButtonInfo({
              buttonText: "RPC Revive All",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_DoPlayerDie").invoke(false);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Revived " + o + " players!", false);
                } catch (e) {
                  sendNotification("Revive All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Revives all players via RPC_DoPlayerDie."
            }), new ButtonInfo({
              buttonText: "RPC Stun All",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        const t = getTransform(e).method("get_position").invoke();
                        e.method("RPC_PlayerStun").invoke(t, 5, 5, 1);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Stunned " + o + " players!", false);
                } catch (e) {
                  sendNotification("Stun All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Stuns all other players for 5 seconds."
            }), new ButtonInfo({
              buttonText: "RPC Launch All",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_AddForce").invoke([0, 1500, 0]);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Launched " + o + " players!", false);
                } catch (e) {
                  sendNotification("Launch All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Launches all other players upward."
            }), new ButtonInfo({
              buttonText: "RPC TP All 2 Me",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = getTransform(e).method("get_position").invoke();
                  const o = NetPlayer.field("playerIDToNetPlayer").value;
                  const n = o.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let i = 0;
                  while (n.method("MoveNext").invoke()) {
                    const e = n.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_Teleport").invoke(t);
                        i++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("TP'd " + i + " players to you!", false);
                } catch (e) {
                  sendNotification("TP All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Teleports all players to your position."
            }), new ButtonInfo({
              buttonText: "RPC Stinky All",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_TagAsStinky").invoke();
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Stinkied " + o + " players!", false);
                } catch (e) {
                  sendNotification("Stinky All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Tags all other players as stinky."
            }), new ButtonInfo({
              buttonText: "Set Wanted",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = e.method("get_isWanted").invoke();
                  e.method("set_isWanted").invoke(!t);
                  sendNotification("isWanted: " + !t, false);
                } catch (e) {
                  sendNotification("Set Wanted: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Toggles your wanted status."
            }), new ButtonInfo({
              buttonText: "Set Scale Big",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  try {
                    e.method("set_playerScale").invoke(5);
                  } catch (e) {}
                  try {
                    e.field("_playerScale").value = 5;
                  } catch (e) {}
                  try {
                    e.method("SetNormalizedScaleModifier").invoke(5);
                  } catch (e) {}
                  sendNotification("Scale set to big!", false);
                } catch (e) {
                  sendNotification("Set Scale: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets your player scale to big."
            }), new ButtonInfo({
              buttonText: "Set Scale Normal",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  try {
                    e.method("set_playerScale").invoke(1);
                  } catch (e) {}
                  try {
                    e.field("_playerScale").value = 1;
                  } catch (e) {}
                  try {
                    e.method("SetNormalizedScaleModifier").invoke(1);
                  } catch (e) {}
                  sendNotification("Scale reset to normal!", false);
                } catch (e) {
                  sendNotification("Set Scale: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Resets your player scale to normal."
            }), new ButtonInfo({
              buttonText: "Set Scale Tiny",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  try {
                    e.method("set_playerScale").invoke(0.1);
                  } catch (e) {}
                  try {
                    e.field("_playerScale").value = 0.1;
                  } catch (e) {}
                  try {
                    e.method("SetNormalizedScaleModifier").invoke(0.1);
                  } catch (e) {}
                  sendNotification("Scale set to tiny!", false);
                } catch (e) {
                  sendNotification("Set Scale: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets your player scale to tiny."
            }), new ButtonInfo({
              buttonText: "Set Kills +10",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = e.method("get_kills").invoke();
                  e.method("set_kills").invoke(t + 10);
                  sendNotification("Kills: " + (t + 10), false);
                } catch (e) {
                  sendNotification("Set Kills: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Adds 10 kills to your score."
            }), new ButtonInfo({
              buttonText: "Set Deaths 0",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("set_deaths").invoke(0);
                  sendNotification("Deaths reset to 0!", false);
                } catch (e) {
                  sendNotification("Set Deaths: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Resets your death count to 0."
            }), new ButtonInfo({
              buttonText: "Go Invisible",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("set_isHide").invoke(true);
                  sendNotification("Hidden from others!", false);
                } catch (e) {
                  sendNotification("Invisible: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Hides you from other players via set_isHide."
            }), new ButtonInfo({
              buttonText: "Go Visible",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("set_isHide").invoke(false);
                  sendNotification("Visible again!", false);
                } catch (e) {
                  sendNotification("Visible: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Makes you visible again via set_isHide(false)."
            }), new ButtonInfo({
              buttonText: "Color Yellow",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("RPC_SetColorHSV").invoke(99999, 0.17, 1, 1);
                  sendNotification("Color: Yellow!", false);
                } catch (e) {
                  sendNotification("Color: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets your color to yellow via RPC_SetColorHSV."
            }), new ButtonInfo({
              buttonText: "Color Green",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("RPC_SetColorHSV").invoke(99999, 0.33, 1, 1);
                  sendNotification("Color: Green!", false);
                } catch (e) {
                  sendNotification("Color: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets your color to green via RPC_SetColorHSV."
            }), new ButtonInfo({
              buttonText: "Color Blue",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("RPC_SetColorHSV").invoke(99999, 0.67, 1, 1);
                  sendNotification("Color: Blue!", false);
                } catch (e) {
                  sendNotification("Color: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets your color to blue via RPC_SetColorHSV."
            }), new ButtonInfo({
              buttonText: "Color Purple",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("RPC_SetColorHSV").invoke(99999, 0.78, 1, 1);
                  sendNotification("Color: Purple!", false);
                } catch (e) {
                  sendNotification("Color: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets your color to purple via RPC_SetColorHSV."
            }), new ButtonInfo({
              buttonText: "Color Pink",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("RPC_SetColorHSV").invoke(99999, 0.92, 1, 1);
                  sendNotification("Color: Pink!", false);
                } catch (e) {
                  sendNotification("Color: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets your color to pink via RPC_SetColorHSV."
            }), new ButtonInfo({
              buttonText: "Color All Red",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_SetColorHSV").invoke(99999, 0, 1, 1);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Colored " + o + " players red!", false);
                } catch (e) {
                  sendNotification("Color All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all other players' color to red."
            }), new ButtonInfo({
              buttonText: "Scale All Tiny",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("set_playerScale").invoke(0.1);
                        try {
                          e.field("_playerScale").value = 0.1;
                        } catch (e) {}
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Scaled " + o + " players tiny!", false);
                } catch (e) {
                  sendNotification("Scale All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all other players to tiny scale."
            }), new ButtonInfo({
              buttonText: "Scale All Big",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("set_playerScale").invoke(5);
                        try {
                          e.field("_playerScale").value = 5;
                        } catch (e) {}
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Scaled " + o + " players big!", false);
                } catch (e) {
                  sendNotification("Scale All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all other players to big scale."
            }), new ButtonInfo({
              buttonText: "Scale All Normal",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("set_playerScale").invoke(1);
                        try {
                          e.field("_playerScale").value = 1;
                        } catch (e) {}
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Reset " + o + " players to normal scale!", false);
                } catch (e) {
                  sendNotification("Scale All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Resets all other players to normal scale."
            }), new ButtonInfo({
              buttonText: "Hide All Players",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("set_isHide").invoke(true);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Hid " + o + " players!", false);
                } catch (e) {
                  sendNotification("Hide All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Hides all other players from the game."
            }), new ButtonInfo({
              buttonText: "Show All Players",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("set_isHide").invoke(false);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Showed " + o + " players!", false);
                } catch (e) {
                  sendNotification("Show All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Makes all hidden players visible again."
            }), new ButtonInfo({
              buttonText: "Award Kill All",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_AwardKill").invoke();
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Awarded kill to " + o + " players!", false);
                } catch (e) {
                  sendNotification("Award Kill All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Awards a kill to all other players."
            }), new ButtonInfo({
              buttonText: "Freeze All",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        const t = getTransform(e).method("get_position").invoke();
                        e.method("RPC_PlayerStun").invoke(t, 999, 9999, 0);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Froze " + o + " players!", false);
                } catch (e) {
                  sendNotification("Freeze All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Stuns all other players with max duration (freeze effect)."
            }), new ButtonInfo({
              buttonText: "RPC Buff Scale Big",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  try {
                    e.method("RPC_ApplyBuff").invoke(5);
                  } catch (e) {}
                  try {
                    e.method("set_playerScale").invoke(5);
                  } catch (e) {}
                  try {
                    e.field("_playerScale").value = 5;
                  } catch (e) {}
                  sendNotification("Scale buff applied!", false);
                } catch (e) {
                  sendNotification("Scale Buff: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Applies Scale buff (big) to yourself."
            }), new ButtonInfo({
              buttonText: "RPC Buff Stinky",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  try {
                    e.method("RPC_ApplyBuff").invoke(6);
                  } catch (e) {}
                  e.method("RPC_TagAsStinky").invoke();
                  sendNotification("Stinky buff applied!", false);
                } catch (e) {
                  sendNotification("Stinky Buff: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Tags yourself as stinky + applies buff."
            }), new ButtonInfo({
              buttonText: "RPC Buff Damage",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("RPC_ApplyBuff").invoke(3);
                  sendNotification("Damage buff applied!", false);
                } catch (e) {
                  sendNotification("Damage Buff: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Applies Damage buff to yourself."
            }), new ButtonInfo({
              buttonText: "RPC Buff Pink",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("RPC_ApplyBuff").invoke(2);
                  sendNotification("Pink buff applied!", false);
                } catch (e) {
                  sendNotification("Pink Buff: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Applies PinkEffect buff to yourself."
            }), new ButtonInfo({
              buttonText: "RPC Buff All Speed",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_ApplyBuff").invoke(1);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Speed buff sent to " + o + " players!", false);
                } catch (e) {
                  sendNotification("Buff All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sends SpeedBoost buff to all players."
            }), new ButtonInfo({
              buttonText: "RPC Buff All AntiGrav",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_ApplyBuff").invoke(7);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("AntiGrav buff sent to " + o + " players!", false);
                } catch (e) {
                  sendNotification("Buff All AntiGrav: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sends AntiGravity buff to all players."
            }), new ButtonInfo({
              buttonText: "RPC Force All Invisible",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("set_isHide").invoke(true);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Hid " + o + " players!", false);
                } catch (e) {
                  sendNotification("Hide All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Hides all other players."
            }), new ButtonInfo({
              buttonText: "RPC Force All Visible",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("set_isHide").invoke(false);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Unhid " + o + " players!", false);
                } catch (e) {
                  sendNotification("Unhide All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Unhides all other players."
            }), new ButtonInfo({
              buttonText: "RPC Rainbow All",
              isTogglable: true,
              method: () => {
                try {
                  if (frameCount % 10 != 0) {
                    return;
                  }
                  const e = time * 0.5 % 1;
                  const t = NetPlayer.field("playerIDToNetPlayer").value;
                  const o = t.method("get_Values").invoke().method("GetEnumerator").invoke();
                  while (o.method("MoveNext").invoke()) {
                    const t = o.method("get_Current").invoke();
                    if (t && !t.handle.isNull() && !playerIsLocal(t)) {
                      try {
                        t.method("RPC_SetColorHSV").invoke(99999, e, 1, 1);
                      } catch (e) {}
                    }
                  }
                } catch (e) {}
              },
              toolTip: "Cycles all players through rainbow colors."
            }), new ButtonInfo({
              buttonText: "RPC Explode All Items",
              method: () => {
                try {
                  let e = 0;
                  const t = ["AnimalCompany.Bomb", "AnimalCompany.TimeBomb", "AnimalCompany.PumpkinBomb", "AnimalCompany.StashGrenade", "AnimalCompany.BroccoliGrenade", "AnimalCompany.TeleGrenade"];
                  for (const o of t) {
                    try {
                      const t = AssemblyCSharp.class(o);
                      const n = Object.method("FindObjectsByType", 1).inflate(t).invoke(0);
                      if (!n || n.isNull()) {
                        continue;
                      }
                      for (let t = 0; t < n.length; t++) {
                        try {
                          const o = n.get(t);
                          if (!o || o.isNull()) {
                            continue;
                          }
                          o.method("RPC_Explode").invoke();
                          e++;
                        } catch (e) {}
                      }
                    } catch (e) {}
                  }
                  sendNotification("Exploded " + e + " items!", false);
                } catch (e) {
                  sendNotification("Explode All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Explodes all explosives in the scene."
            }), new ButtonInfo({
              buttonText: "RPC Buff All Pink",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_ApplyBuff").invoke(2);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Pink buff -> " + o + " players!", false);
                } catch (e) {
                  sendNotification("Buff All Pink: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Applies PinkEffect buff to all players."
            }), new ButtonInfo({
              buttonText: "RPC Buff All Damage",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_ApplyBuff").invoke(3);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Damage buff -> " + o + " players!", false);
                } catch (e) {
                  sendNotification("Buff All Damage: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Applies Damage buff to all players."
            }), new ButtonInfo({
              buttonText: "RPC Buff All Scale",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_ApplyBuff").invoke(5);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Scale buff -> " + o + " players!", false);
                } catch (e) {
                  sendNotification("Buff All Scale: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Applies Scale buff to all players."
            }), new ButtonInfo({
              buttonText: "RPC Buff All Stinky",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_ApplyBuff").invoke(6);
                        e.method("RPC_TagAsStinky").invoke();
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Stinky buff -> " + o + " players!", false);
                } catch (e) {
                  sendNotification("Buff All Stinky: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Tags all players as stinky + stinky buff."
            }), new ButtonInfo({
              buttonText: "RPC TP All 2 Void",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_Teleport").invoke([0, -99999, 0]);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Voided " + o + " players!", false);
                } catch (e) {
                  sendNotification("TP Void: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Teleports all other players to the void."
            }), new ButtonInfo({
              buttonText: "RPC Kill Me Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (!e || e.handle.isNull()) {
                      return;
                    }
                    e.method("RPC_DoPlayerDie").invoke(true);
                  } catch (e) {}
                }
              },
              toolTip: "Repeatedly kills yourself every 0.5s (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC Force All Red Team",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_SetTeam").invoke(1);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Set " + o + " players to red team!", false);
                } catch (e) {
                  sendNotification("Force Team: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all other players to red team."
            }), new ButtonInfo({
              buttonText: "RPC Force All Blue Team",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_SetTeam").invoke(2);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Set " + o + " players to blue team!", false);
                } catch (e) {
                  sendNotification("Force Team: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all other players to blue team."
            }), new ButtonInfo({
              buttonText: "RPC Color All Rainbow",
              isTogglable: true,
              method: () => {
                if (frameCount % 8 == 0) {
                  try {
                    const e = time * 0.4 % 1;
                    const t = NetPlayer.field("playerIDToNetPlayer").value;
                    const o = t.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (o.method("MoveNext").invoke()) {
                      const t = o.method("get_Current").invoke();
                      if (t && !t.handle.isNull()) {
                        try {
                          t.method("RPC_SetColorHSV").invoke(99999, e, 1, 1);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Cycles all players (incl. you) through rainbow colors continuously."
            }), new ButtonInfo({
              buttonText: "RPC Explode All Bags",
              method: () => {
                try {
                  let e = 0;
                  const t = Object.method("FindObjectsByType", 1).inflate(BackpackItemClass).invoke(0);
                  if (t && !t.isNull()) {
                    for (let o = 0; o < t.length; o++) {
                      try {
                        const n = t.get(o);
                        if (!n || n.isNull()) {
                          continue;
                        }
                        try {
                          n.method("Explode").invoke();
                          e++;
                        } catch (e) {}
                      } catch (e) {}
                    }
                  }
                  sendNotification("Exploded " + e + " bags!", false);
                } catch (e) {
                  sendNotification("Explode Bags: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Explodes all backpacks in the scene."
            }), new ButtonInfo({
              buttonText: "RPC All Wanted",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("set_isWanted").invoke(true);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Marked " + o + " players wanted!", false);
                } catch (e) {
                  sendNotification("All Wanted: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Marks all other players as wanted."
            }), new ButtonInfo({
              buttonText: "RPC Clear Wanted All",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("set_isWanted").invoke(false);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Cleared wanted on " + o + " players!", false);
                } catch (e) {
                  sendNotification("Clear Wanted: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Clears wanted status from all players."
            }), new ButtonInfo({
              buttonText: "RPC Add Kills All",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        const t = e.method("get_kills").invoke();
                        e.method("set_kills").invoke(t + 10);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("+10 kills to " + o + " players!", false);
                } catch (e) {
                  sendNotification("Add Kills: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Adds 10 kills to all players."
            }), new ButtonInfo({
              buttonText: "RPC VFX Spam",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.2;
                  try {
                    const e = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                    const t = getTransform(NetPlayer.method("get_localPlayer").invoke()).method("get_position").invoke();
                    const o = [VFXTypes.ConfettiBurst, VFXTypes.GreenBlink, VFXTypes.Ethereal_Void, VFXTypes.Explosion_Coins, VFXTypes.Explosion_Feathers, VFXTypes.Explosion_Popcorn, VFXTypes.MeatExplosion_1, VFXTypes.MeatExplosion_2, VFXTypes.MidAirJump_Fart, VFXTypes.FuelExplosion, VFXTypes.Electricity_Small];
                    const n = o[Math.floor(Math.random() * o.length)];
                    ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(e, n, t, identityQuaternion);
                  } catch (e) {}
                }
              },
              toolTip: "Spams random VFX effects at your position every 0.2s (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC Kill All Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.8;
                  try {
                    const e = NetPlayer.field("playerIDToNetPlayer").value;
                    const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (t.method("MoveNext").invoke()) {
                      const e = t.method("get_Current").invoke();
                      if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                        try {
                          e.method("RPC_DoPlayerDie").invoke(true);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Repeatedly kills all other players every 0.8s (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC VFX All Spam",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.15;
                  try {
                    const e = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                    const t = NetPlayer.field("playerIDToNetPlayer").value;
                    const o = t.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (o.method("MoveNext").invoke()) {
                      const t = o.method("get_Current").invoke();
                      if (!t || t.handle.isNull() || playerIsLocal(t)) {
                        continue;
                      }
                      const n = getTransform(t).method("get_position").invoke();
                      const i = [VFXTypes.ConfettiBurst, VFXTypes.GreenBlink, VFXTypes.Ethereal_Void, VFXTypes.MeatExplosion_1, VFXTypes.MidAirJump_Fart, VFXTypes.FuelExplosion];
                      const a = i[Math.floor(Math.random() * i.length)];
                      try {
                        ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(e, a, n, identityQuaternion);
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Spams random VFX on all other players every 0.15s (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC Bounce All Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const e = NetPlayer.field("playerIDToNetPlayer").value;
                    const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (t.method("MoveNext").invoke()) {
                      const e = t.method("get_Current").invoke();
                      if (!e || e.handle.isNull() || playerIsLocal(e)) {
                        continue;
                      }
                      const o = (Math.random() - 0.5) * 3000;
                      const n = (Math.random() - 0.5) * 3000;
                      try {
                        e.method("RPC_AddForce").invoke([o, 1500, n]);
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously bounces all other players in random directions (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC Hit All Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.3;
                  try {
                    const e = DamageSourceInfoClass.method("get_Null").invoke();
                    const t = NetPlayer.field("playerIDToNetPlayer").value;
                    const o = t.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (o.method("MoveNext").invoke()) {
                      const t = o.method("get_Current").invoke();
                      if (t && !t.handle.isNull() && !playerIsLocal(t)) {
                        try {
                          const o = getTransform(t).method("get_position").invoke();
                          t.method("RPC_PlayerHit", 3).invoke(10, o, e);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously deals 10 damage to all other players every 0.3s (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC Stun All Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 1;
                  try {
                    const e = NetPlayer.field("playerIDToNetPlayer").value;
                    const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (t.method("MoveNext").invoke()) {
                      const e = t.method("get_Current").invoke();
                      if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                        try {
                          const t = getTransform(e).method("get_position").invoke();
                          e.method("RPC_PlayerStun").invoke(t, 5, 5, 1);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously stuns all other players every 1s (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC All Ragdoll Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 1.5;
                  try {
                    const e = NetPlayer.field("playerIDToNetPlayer").value;
                    const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (t.method("MoveNext").invoke()) {
                      const e = t.method("get_Current").invoke();
                      if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                        try {
                          const t = getTransform(e).method("get_position").invoke();
                          e.method("RPC_PlayerStun").invoke(t, 999, 9999, 0);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously ragdolls all other players (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC Money All +10M",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_AddPlayerMoney").invoke(10000000);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Gave 10M money to " + o + " players!", false);
                } catch (e) {
                  sendNotification("Money All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Gives 10,000,000 money to all other players."
            }), new ButtonInfo({
              buttonText: "RPC Inf Money All +10M",
              enableMethod: () => {
                rpcMoneyAllLoopDelay = 0;
                sendNotification("RPC Inf Money All ON", false);
              },
              disableMethod: () => {
                sendNotification("RPC Inf Money All OFF", false);
              },
              isTogglable: true,
              method: () => {
                if (!(time <= rpcMoneyAllLoopDelay)) {
                  rpcMoneyAllLoopDelay = time + 0.9;
                  try {
                    for (const e of getAllNetPlayersList(false)) {
                      try {
                        e.method("RPC_AddPlayerMoney").invoke(10000000);
                      } catch (e) {}
                    }
                  } catch (e) {
                    console.error("RPC Inf Money All:", e);
                  }
                }
              },
              toolTip: "Continuously gives 10,000,000 money to all other players with a low-frequency loop."
            }), new ButtonInfo({
              buttonText: "RPC Money Drain All",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("RPC_AddPlayerMoney").invoke(-99999);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Drained money from " + o + " players!", false);
                } catch (e) {
                  sendNotification("Money Drain: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Removes 99999 money from all other players."
            }), new ButtonInfo({
              buttonText: "RPC Chaos All Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.6;
                  try {
                    const e = DamageSourceInfoClass.method("get_Null").invoke();
                    const t = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                    const o = NetPlayer.field("playerIDToNetPlayer").value;
                    const n = o.method("get_Values").invoke().method("GetEnumerator").invoke();
                    const i = ["force", "stun", "hit", "vfx", "color", "tp"];
                    const a = [VFXTypes.ConfettiBurst, VFXTypes.Ethereal_Void, VFXTypes.MeatExplosion_1, VFXTypes.FuelExplosion, VFXTypes.Electricity_Small, VFXTypes.MidAirJump_Fart];
                    while (n.method("MoveNext").invoke()) {
                      const o = n.method("get_Current").invoke();
                      if (!o || o.handle.isNull() || playerIsLocal(o)) {
                        continue;
                      }
                      const l = i[Math.floor(Math.random() * i.length)];
                      try {
                        const n = getTransform(o).method("get_position").invoke();
                        if (l === "force") {
                          const e = (Math.random() - 0.5) * 2000;
                          const t = (Math.random() - 0.5) * 2000;
                          o.method("RPC_AddForce").invoke([e, 1000 + Math.random() * 500, t]);
                        } else if (l === "stun") {
                          o.method("RPC_PlayerStun").invoke(n, 5, 3, 1);
                        } else if (l === "hit") {
                          o.method("RPC_PlayerHit", 3).invoke(10, n, e);
                        } else if (l === "vfx") {
                          const e = a[Math.floor(Math.random() * a.length)];
                          ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(t, e, n, identityQuaternion);
                        } else if (l === "color") {
                          const e = Math.random();
                          o.method("RPC_SetColorHSV").invoke(99999, e, 1, 1);
                        } else if (l === "tp") {
                          const e = n.field("x").value + (Math.random() - 0.5) * 20;
                          const t = n.field("z").value + (Math.random() - 0.5) * 20;
                          o.method("RPC_Teleport").invoke([e, n.field("y").value + 5, t]);
                        }
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Randomly applies force/stun/hit/VFX/color/TP to all players every 0.6s (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC Give Me Invincible",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("set_isInvincible").invoke(true);
                  sendNotification("Invincible ON!", false);
                } catch (e) {
                  sendNotification("Invincible: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets yourself to invincible via networked field."
            }), new ButtonInfo({
              buttonText: "RPC Remove Invincible",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("set_isInvincible").invoke(false);
                  sendNotification("Invincible OFF!", false);
                } catch (e) {
                  sendNotification("Invincible: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Removes your invincibility."
            }), new ButtonInfo({
              buttonText: "RPC Give Me Money",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  e.method("RPC_AddPlayerMoney").invoke(9999);
                  sendNotification("Added 9999 money!", false);
                } catch (e) {
                  sendNotification("Give Money: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Gives yourself 9999 money."
            }), new ButtonInfo({
              buttonText: "RPC TP Me To Sky",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = getTransform(e).method("get_position").invoke();
                  selfRPC(() => e.method("RPC_Teleport").invoke([t.field("x").value, t.field("y").value + 200, t.field("z").value]));
                  sendNotification("Teleported to sky!", false);
                } catch (e) {
                  sendNotification("TP Sky: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Teleports you 200 units straight up."
            }), new ButtonInfo({
              buttonText: "RPC Explosive Launch Me",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = (Math.random() - 0.5) * 3000;
                  const o = (Math.random() - 0.5) * 3000;
                  selfRPC(() => e.method("RPC_AddForce").invoke([t, 2500, o]));
                  sendNotification("Explosive launch!", false);
                } catch (e) {
                  sendNotification("Explosive Launch: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Launches yourself in a random explosive direction."
            }), new ButtonInfo({
              buttonText: "RPC All Kill Revive Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const e = NetPlayer.field("playerIDToNetPlayer").value;
                    const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (t.method("MoveNext").invoke()) {
                      const e = t.method("get_Current").invoke();
                      if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                        try {
                          const t = e.method("get_isDie").invoke();
                          e.method("RPC_DoPlayerDie").invoke(!t);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Rapidly toggles all players between alive and dead (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC Force Wanted All Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const e = NetPlayer.field("playerIDToNetPlayer").value;
                    const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (t.method("MoveNext").invoke()) {
                      const e = t.method("get_Current").invoke();
                      if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                        try {
                          e.method("set_isWanted").invoke(true);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously keeps all players wanted (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC Gun Kill",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (playerIsLocal(o)) {
                      return;
                    }
                    o.method("RPC_DoPlayerDie").invoke(true);
                    sendNotification("Killed: " + getPlayerName(o), false);
                  } catch (e) {
                    console.error("RPC Gun Kill:", e);
                  }
                }
              },
              toolTip: "Point at any player, pull trigger to kill them (hold grip)."
            }), new ButtonInfo({
              buttonText: "RPC Gun Revive",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    o.method("RPC_DoPlayerDie").invoke(false);
                    sendNotification("Revived: " + getPlayerName(o), false);
                  } catch (e) {
                    console.error("RPC Gun Revive:", e);
                  }
                }
              },
              toolTip: "Point at any player, pull trigger to revive them (hold grip)."
            }), new ButtonInfo({
              buttonText: "RPC Gun Launch",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (playerIsLocal(o)) {
                      return;
                    }
                    o.method("RPC_AddForce").invoke([0, 1500, 0]);
                    sendNotification("Launched: " + getPlayerName(o), false);
                  } catch (e) {
                    console.error("RPC Gun Launch:", e);
                  }
                }
              },
              toolTip: "Point at any player, pull trigger to launch them (hold grip)."
            }), new ButtonInfo({
              buttonText: "RPC Gun Stun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (playerIsLocal(o)) {
                      return;
                    }
                    const n = getTransform(o).method("get_position").invoke();
                    o.method("RPC_PlayerStun").invoke(n, 5, 5, 1);
                    sendNotification("Stunned: " + getPlayerName(o), false);
                  } catch (e) {
                    console.error("RPC Gun Stun:", e);
                  }
                }
              },
              toolTip: "Point at any player, pull trigger to stun them (hold grip)."
            }), new ButtonInfo({
              buttonText: "RPC Gun Freeze",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (playerIsLocal(o)) {
                      return;
                    }
                    const n = getTransform(o).method("get_position").invoke();
                    o.method("RPC_PlayerStun").invoke(n, 999, 9999, 0);
                    sendNotification("Froze: " + getPlayerName(o), false);
                  } catch (e) {
                    console.error("RPC Gun Freeze:", e);
                  }
                }
              },
              toolTip: "Point at any player, pull trigger to freeze them indefinitely (hold grip)."
            }), new ButtonInfo({
              buttonText: "RPC Gun Color",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.3;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    const n = time * 0.5 % 1;
                    o.method("RPC_SetColorHSV").invoke(99999, n, 1, 1);
                    sendNotification("Rainbow'd: " + getPlayerName(o), false);
                  } catch (e) {
                    console.error("RPC Gun Color:", e);
                  }
                }
              },
              toolTip: "Point at any player, pull trigger to rainbow them (hold grip)."
            }), new ButtonInfo({
              buttonText: "RPC Gun Scale Big",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    try {
                      o.method("set_playerScale").invoke(5);
                    } catch (e) {}
                    try {
                      o.field("_playerScale").value = 5;
                    } catch (e) {}
                    sendNotification("Scaled big: " + getPlayerName(o), false);
                  } catch (e) {
                    console.error("RPC Gun Scale:", e);
                  }
                }
              },
              toolTip: "Point at any player, pull trigger to make them big (hold grip)."
            }), new ButtonInfo({
              buttonText: "RPC Gun Scale Tiny",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    try {
                      o.method("set_playerScale").invoke(0.1);
                    } catch (e) {}
                    try {
                      o.field("_playerScale").value = 0.1;
                    } catch (e) {}
                    sendNotification("Shrunk: " + getPlayerName(o), false);
                  } catch (e) {
                    console.error("RPC Gun Scale Tiny:", e);
                  }
                }
              },
              toolTip: "Point at any player, pull trigger to shrink them (hold grip)."
            }), new ButtonInfo({
              buttonText: "RPC Gun Buff Speed",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    o.method("RPC_ApplyBuff").invoke(1);
                    sendNotification("Speed buff -> " + getPlayerName(o), false);
                  } catch (e) {
                    console.error("RPC Gun Buff Speed:", e);
                  }
                }
              },
              toolTip: "Point at any player, pull trigger to give them SpeedBoost (hold grip)."
            }), new ButtonInfo({
              buttonText: "RPC Gun AntiGrav",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    o.method("RPC_ApplyBuff").invoke(7);
                    sendNotification("AntiGrav -> " + getPlayerName(o), false);
                  } catch (e) {
                    console.error("RPC Gun AntiGrav:", e);
                  }
                }
              },
              toolTip: "Point at any player, pull trigger to give them AntiGravity (hold grip)."
            }), new ButtonInfo({
              buttonText: "RPC Gun Void",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (playerIsLocal(o)) {
                      return;
                    }
                    o.method("RPC_Teleport").invoke([0, -99999, 0]);
                    sendNotification("Voided: " + getPlayerName(o), false);
                  } catch (e) {
                    console.error("RPC Gun Void:", e);
                  }
                }
              },
              toolTip: "Point at any player, pull trigger to void them (hold grip)."
            }), new ButtonInfo({
              buttonText: "RPC Gun Hit 50",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.3;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (playerIsLocal(o)) {
                      return;
                    }
                    const n = getTransform(o).method("get_position").invoke();
                    const i = DamageSourceInfoClass.method("get_Null").invoke();
                    o.method("RPC_PlayerHit", 3).invoke(50, n, i);
                    sendNotification("Hit 50dmg: " + getPlayerName(o), false);
                  } catch (e) {
                    console.error("RPC Gun Hit 50:", e);
                  }
                }
              },
              toolTip: "Point at any player, pull trigger to deal 50 damage (hold grip)."
            }), new ButtonInfo({
              buttonText: "RPC Fake Death Me Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.3;
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (!e || e.handle.isNull()) {
                      return;
                    }
                    const t = e.method("get_isDie").invoke();
                    e.method("set_isDie").invoke(!t);
                  } catch (e) {}
                }
              },
              toolTip: "Rapidly toggles your own death state (ghost effect, toggle)."
            }), new ButtonInfo({
              buttonText: "RPC Self Buff All",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = [1, 2, 3, 5, 7];
                  for (const o of t) {
                    try {
                      e.method("RPC_ApplyBuff").invoke(o);
                    } catch (e) {}
                  }
                  sendNotification("All buffs applied to self!", false);
                } catch (e) {
                  sendNotification("Self Buff All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Applies all known buffs to yourself at once."
            }), new ButtonInfo({
              buttonText: "RPC Heal Me",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  try {
                    e.method("set_healthLost").invoke(0);
                  } catch (e) {}
                  try {
                    e.method("set_isDie").invoke(false);
                  } catch (e) {}
                  sendNotification("Healed to full!", false);
                } catch (e) {
                  sendNotification("Heal Me: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Resets your health (healthLost = 0) and revives if dead."
            }), new ButtonInfo({
              buttonText: "RPC Heal Me Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (!e || e.handle.isNull()) {
                      return;
                    }
                    try {
                      e.method("set_healthLost").invoke(0);
                    } catch (e) {}
                    try {
                      e.method("set_isDie").invoke(false);
                    } catch (e) {}
                  } catch (e) {}
                }
              },
              toolTip: "Continuously heals yourself every 0.5s (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC All Color Rainbow Loop",
              isTogglable: true,
              method: () => {
                if (frameCount % 6 == 0) {
                  try {
                    const e = time * 0.6 % 1;
                    const t = NetPlayer.field("playerIDToNetPlayer").value;
                    const o = t.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (o.method("MoveNext").invoke()) {
                      const t = o.method("get_Current").invoke();
                      if (t && !t.handle.isNull()) {
                        try {
                          t.method("RPC_SetColorHSV").invoke(99999, e, 1, 1);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Cycles ALL players (incl. you) through faster rainbow (toggle)."
            }), new ButtonInfo({
              buttonText: "RPC All Invincible",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("set_isInvincible").invoke(true);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Invincible -> " + o + " players!", false);
                } catch (e) {
                  sendNotification("All Invincible: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Makes all other players invincible."
            }), new ButtonInfo({
              buttonText: "RPC All Remove Invincible",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e)) {
                      try {
                        e.method("set_isInvincible").invoke(false);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Removed invincible from " + o + " players!", false);
                } catch (e) {
                  sendNotification("Remove Invincible: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Removes invincibility from all other players."
            })], [new ButtonInfo({
              buttonText: "Exit Prefab mods",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns to main menu."
            }), new ButtonInfo({
              buttonText: "Prefab IDs",
              method: () => {
                currentCategory = 20;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns you back to the main category."
            }), new ButtonInfo({
              buttonText: "Spawn Prefab",
              method: () => {
                const e = rightHandTransform.method("get_position").invoke();
                const t = rightHandTransform.method("get_rotation").invoke();
                const o = spawnNetworkPrefab(prefabIDs[prefabIndex], e, t);
                if (o && !o.isNull()) {
                  sendNotification("Spawned: " + prefabIDs[prefabIndex], false);
                } else {
                  sendNotification("Failed to spawn! Join a lobby first.", false);
                }
              },
              isTogglable: false,
              toolTip: "Spawns your selected prefab."
            }), new ButtonInfo({
              buttonText: "Prefab Minigun",
              method: () => {
                if (rightGrab && time > lagGunDelay) {
                  lagGunDelay = time + 0.1;
                  const e = spawnPrefab(prefabNames[prefabIndex], rightHandTransform.method("get_position").invoke(), rightHandTransform.method("get_rotation").invoke());
                  if (e) {
                    const t = getComponent(e, Rigidbody);
                    if (t) {
                      if (rightTrigger) {
                        t.method("set_useGravity").invoke(false);
                      }
                      const e = Vector3.method("op_Multiply").invoke(rightHandTransform.method("get_forward").invoke(), 20);
                      t.method("set_linearVelocity").invoke(e);
                    }
                  }
                }
              },
              isTogglable: true,
              toolTip: "Spawns a prefab."
            }), new ButtonInfo({
              buttonText: "Spawn Prefab (gun)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > itemGunDelay) {
                  itemGunDelay = time + 0.3;
                  try {
                    const t = e.method("get_point").invoke();
                    const o = prefabIDs[prefabIndex];
                    const n = spawnNetworkPrefab(o, t, identityQuaternion);
                    if (n && !n.isNull()) {
                      sendNotification("Spawned: " + o, false);
                    } else {
                      sendNotification("Not found in PrefabTable: " + o, false);
                    }
                  } catch (e) {
                    sendNotification("Prefab spawn failed: " + e, false);
                  }
                }
              },
              toolTip: "Spawns selected prefab where you aim via NetworkRunner (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "Spawn Line of Prefab",
              method: () => {
                try {
                  const e = rightHandTransform.method("get_position").invoke();
                  const t = rightHandTransform.method("get_rotation").invoke();
                  const o = PrefabGen.field("_instance").value.method("get_runner").invoke();
                  if (!o || o.isNull()) {
                    console.error("runner null");
                    return;
                  }
                  const n = o.field("_config").value.field("PrefabTable").value.field("_sources").value;
                  const i = n.method("get_Count").invoke();
                  const a = prefabIDs[prefabIndex];
                  let l = false;
                  for (let r = 0; r < i; r++) {
                    try {
                      const i = n.method("get_Item").invoke(r);
                      if (i.method("get_Description").invoke().toString().includes(a)) {
                        const n = i.method("WaitForResult").invoke();
                        if (!n || n.isNull()) {
                          console.error("prefab null");
                          return;
                        }
                        const r = e => {
                          if (e.class.isEnum || e.isPrimitive) {
                            return 0;
                          }
                          if (!e.class.isValueType) {
                            return NULL;
                          }
                          const t = e.class.fields.filter(e => !e.isStatic);
                          if (t.length === 0) {
                            return 0;
                          } else {
                            return t.map(e => r(e.type));
                          }
                        };
                        const s = (e, t, o) => e.class.fields.filter(e => !e.isStatic).map(e => {
                          const n = e.name.toLowerCase();
                          if (n.includes("hasvalue")) {
                            if (t) {
                              return 1;
                            } else {
                              return 0;
                            }
                          } else if (n === "value" && t) {
                            return o;
                          } else {
                            return r(e.type);
                          }
                        });
                        const c = (e, t) => {
                          if (typeof t == "boolean") {
                            if (t) {
                              return 1;
                            } else {
                              return 0;
                            }
                          }
                          if (t instanceof Il2Cpp.ValueType) {
                            const o = e.class.fields.filter(e => !e.isStatic);
                            if (o.length === 0) {
                              return 0;
                            } else {
                              return o.map(e => c(e.type, e.bind(t).value));
                            }
                          }
                          if (Array.isArray(t)) {
                            return t.map(t => c(e, t));
                          } else {
                            return t;
                          }
                        };
                        const d = (e, t) => s(e, true, c(t.type, t));
                        let u = null;
                        for (const e of o.method("Spawn").overloads()) {
                          if (e.parameterCount !== 6 || e.isGeneric) {
                            continue;
                          }
                          const t = e.parameters;
                          if (t[0].type.name.includes("Fusion.NetworkObject") && t[1].type.name.startsWith("System.Nullable") && t[1].type.name.includes("Vector3") && t[2].type.name.startsWith("System.Nullable") && t[2].type.name.includes("Quaternion") && t[3].type.name.startsWith("System.Nullable") && t[3].type.name.includes("PlayerRef") && t[4].type.name.includes("OnBeforeSpawned") && t[5].type.name.includes("NetworkSpawnFlags")) {
                            u = e;
                            break;
                          }
                        }
                        if (!u) {
                          console.error("spawn method not found");
                          return;
                        }
                        for (let i = 0; i < 20; i++) {
                          const a = Vector3.method("op_Multiply").invoke(rightHandTransform.method("get_forward").invoke(), i * 0.5);
                          const l = Vector3.method("op_Addition").invoke(e, a);
                          const c = d(u.parameters[1].type, l);
                          const m = d(u.parameters[2].type, t);
                          const h = s(u.parameters[3].type, false, r(u.parameters[3].type));
                          const g = u.parameters[4].type.class.isValueType ? r(u.parameters[4].type) : NULL;
                          u.bind(o).invoke(n, c, m, h, g, 0);
                        }
                        sendNotification("Spawned 20x: " + a, false);
                        l = true;
                        break;
                      }
                    } catch (e) {}
                  }
                  if (!l) {
                    sendNotification("Prefab not found: " + a, false);
                  }
                } catch (e) {
                  console.error("SpawnLinePrefab error: " + e);
                }
              },
              isTogglable: false,
              toolTip: "Spawns 20 instances of selected prefab in a line."
            }), new ButtonInfo({
              buttonText: "Spawn Long line of prefab",
              method: () => {
                try {
                  const e = rightHandTransform.method("get_position").invoke();
                  const t = rightHandTransform.method("get_rotation").invoke();
                  const o = PrefabGen.field("_instance").value.method("get_runner").invoke();
                  if (!o || o.isNull()) {
                    console.error("runner null");
                    return;
                  }
                  const n = o.field("_config").value.field("PrefabTable").value.field("_sources").value;
                  const i = n.method("get_Count").invoke();
                  const a = prefabIDs[prefabIndex];
                  let l = false;
                  for (let r = 0; r < i; r++) {
                    try {
                      const i = n.method("get_Item").invoke(r);
                      if (i.method("get_Description").invoke().toString().includes(a)) {
                        const n = i.method("WaitForResult").invoke();
                        if (!n || n.isNull()) {
                          console.error("prefab null");
                          return;
                        }
                        const r = e => {
                          if (e.class.isEnum || e.isPrimitive) {
                            return 0;
                          }
                          if (!e.class.isValueType) {
                            return NULL;
                          }
                          const t = e.class.fields.filter(e => !e.isStatic);
                          if (t.length === 0) {
                            return 0;
                          } else {
                            return t.map(e => r(e.type));
                          }
                        };
                        const s = (e, t, o) => e.class.fields.filter(e => !e.isStatic).map(e => {
                          const n = e.name.toLowerCase();
                          if (n.includes("hasvalue")) {
                            if (t) {
                              return 1;
                            } else {
                              return 0;
                            }
                          } else if (n === "value" && t) {
                            return o;
                          } else {
                            return r(e.type);
                          }
                        });
                        const c = (e, t) => {
                          if (typeof t == "boolean") {
                            if (t) {
                              return 1;
                            } else {
                              return 0;
                            }
                          }
                          if (t instanceof Il2Cpp.ValueType) {
                            const o = e.class.fields.filter(e => !e.isStatic);
                            if (o.length === 0) {
                              return 0;
                            } else {
                              return o.map(e => c(e.type, e.bind(t).value));
                            }
                          }
                          if (Array.isArray(t)) {
                            return t.map(t => c(e, t));
                          } else {
                            return t;
                          }
                        };
                        const d = (e, t) => s(e, true, c(t.type, t));
                        let u = null;
                        for (const e of o.method("Spawn").overloads()) {
                          if (e.parameterCount !== 6 || e.isGeneric) {
                            continue;
                          }
                          const t = e.parameters;
                          if (t[0].type.name.includes("Fusion.NetworkObject") && t[1].type.name.startsWith("System.Nullable") && t[1].type.name.includes("Vector3") && t[2].type.name.startsWith("System.Nullable") && t[2].type.name.includes("Quaternion") && t[3].type.name.startsWith("System.Nullable") && t[3].type.name.includes("PlayerRef") && t[4].type.name.includes("OnBeforeSpawned") && t[5].type.name.includes("NetworkSpawnFlags")) {
                            u = e;
                            break;
                          }
                        }
                        if (!u) {
                          console.error("spawn method not found");
                          return;
                        }
                        for (let i = 0; i < 200; i++) {
                          const a = Vector3.method("op_Multiply").invoke(rightHandTransform.method("get_forward").invoke(), i * 0.5);
                          const l = Vector3.method("op_Addition").invoke(e, a);
                          const c = d(u.parameters[1].type, l);
                          const m = d(u.parameters[2].type, t);
                          const h = s(u.parameters[3].type, false, r(u.parameters[3].type));
                          const g = u.parameters[4].type.class.isValueType ? r(u.parameters[4].type) : NULL;
                          u.bind(o).invoke(n, c, m, h, g, 0);
                        }
                        sendNotification("Spawned 200x: " + a, false);
                        l = true;
                        break;
                      }
                    } catch (e) {}
                  }
                  if (!l) {
                    sendNotification("Prefab not found: " + a, false);
                  }
                } catch (e) {
                  console.error("SpawnLongLinePrefab error: " + e);
                }
              },
              isTogglable: false,
              toolTip: "Spawns 200 instances of selected prefab in a long line."
            }), new ButtonInfo({
              buttonText: "Spam Prefabs",
              isTogglable: true,
              method: () => {
                if (rightGrab && rightTrigger && !(time < prefabSpamLineDelay)) {
                  prefabSpamLineDelay = time + 0.05;
                  try {
                    const e = rightHandTransform.method("get_position").invoke();
                    const t = rightHandTransform.method("get_rotation").invoke();
                    const o = PrefabGen.field("_instance").value.method("get_runner").invoke();
                    if (!o || o.isNull()) {
                      console.error("runner null");
                      return;
                    }
                    const n = o.field("_config").value.field("PrefabTable").value.field("_sources").value;
                    const i = n.method("get_Count").invoke();
                    const a = prefabIDs[prefabIndex];
                    for (let l = 0; l < i; l++) {
                      try {
                        const i = n.method("get_Item").invoke(l);
                        if (i.method("get_Description").invoke().toString().includes(a)) {
                          const n = i.method("WaitForResult").invoke();
                          if (!n || n.isNull()) {
                            console.error("prefab null");
                            return;
                          }
                          const a = e => {
                            if (e.class.isEnum || e.isPrimitive) {
                              return 0;
                            }
                            if (!e.class.isValueType) {
                              return NULL;
                            }
                            const t = e.class.fields.filter(e => !e.isStatic);
                            if (t.length === 0) {
                              return 0;
                            } else {
                              return t.map(e => a(e.type));
                            }
                          };
                          const l = (e, t, o) => e.class.fields.filter(e => !e.isStatic).map(e => {
                            const n = e.name.toLowerCase();
                            if (n.includes("hasvalue")) {
                              if (t) {
                                return 1;
                              } else {
                                return 0;
                              }
                            } else if (n === "value" && t) {
                              return o;
                            } else {
                              return a(e.type);
                            }
                          });
                          const r = (e, t) => {
                            if (typeof t == "boolean") {
                              if (t) {
                                return 1;
                              } else {
                                return 0;
                              }
                            }
                            if (t instanceof Il2Cpp.ValueType) {
                              const o = e.class.fields.filter(e => !e.isStatic);
                              if (o.length === 0) {
                                return 0;
                              } else {
                                return o.map(e => r(e.type, e.bind(t).value));
                              }
                            }
                            if (Array.isArray(t)) {
                              return t.map(t => r(e, t));
                            } else {
                              return t;
                            }
                          };
                          const s = (e, t) => l(e, true, r(t.type, t));
                          let c = null;
                          for (const e of o.method("Spawn").overloads()) {
                            if (e.parameterCount !== 6 || e.isGeneric) {
                              continue;
                            }
                            const t = e.parameters;
                            if (t[0].type.name.includes("Fusion.NetworkObject") && t[1].type.name.startsWith("System.Nullable") && t[1].type.name.includes("Vector3") && t[2].type.name.startsWith("System.Nullable") && t[2].type.name.includes("Quaternion") && t[3].type.name.startsWith("System.Nullable") && t[3].type.name.includes("PlayerRef") && t[4].type.name.includes("OnBeforeSpawned") && t[5].type.name.includes("NetworkSpawnFlags")) {
                              c = e;
                              break;
                            }
                          }
                          if (!c) {
                            console.error("spawn method not found");
                            return;
                          }
                          for (let i = 0; i < 15; i++) {
                            const r = Vector3.method("op_Multiply").invoke(rightHandTransform.method("get_forward").invoke(), i * 0.5);
                            const d = Vector3.method("op_Addition").invoke(e, r);
                            const u = s(c.parameters[1].type, d);
                            const m = s(c.parameters[2].type, t);
                            const h = l(c.parameters[3].type, false, a(c.parameters[3].type));
                            const g = c.parameters[4].type.class.isValueType ? a(c.parameters[4].type) : NULL;
                            c.bind(o).invoke(n, u, m, h, g, 0);
                          }
                          break;
                        }
                      } catch (e) {}
                    }
                  } catch (e) {
                    console.error("SpamPrefabs error: " + e);
                  }
                }
              },
              toolTip: "Hold grip + trigger to spam lines of prefabs rapidly."
            }), new ButtonInfo({
              buttonText: "Rain Prefabs",
              isTogglable: true,
              method: () => {
                if (rightGrab && rightTrigger && !(time < rainPrefabDelay)) {
                  rainPrefabDelay = time + getRainSpamDelay();
                  try {
                    const e = rightHandTransform.method("get_position").invoke();
                    const t = 20;
                    const o = (Math.random() - 0.5) * 2 * t;
                    const n = (Math.random() - 0.5) * 2 * t;
                    const i = 10 + Math.random() * 20;
                    const a = Vector3.method("op_Addition").invoke(e, [o, i, n]);
                    const l = Quaternion.method("Euler", 3).invoke(90, 0, 0);
                    const r = spawnNetworkPrefab(prefabIDs[prefabIndex], a, l);
                    if (r && !r.isNull()) {
                      try {
                        const e = getRigidbodySafe(r);
                        if (e && !e.isNull()) {
                          e.method("set_isKinematic").invoke(false);
                          e.method("WakeUp").invoke();
                          e.method("set_linearVelocity").invoke([0, -15, 0]);
                        }
                      } catch (e) {}
                    }
                  } catch (e) {
                    console.error("RainPrefabs error: " + e);
                  }
                }
              },
              toolTip: "Hold grip + trigger to rain prefabs from random heights above (10-30m) with downward velocity."
            }), new ButtonInfo({
              buttonText: "Spam/Rain Rate: " + getRainSpamDelay() + "s",
              method: () => {
                rainSpamDelayIndex = cycleNumericSetting(RAIN_SPAM_DELAY_VALUES, rainSpamDelayIndex, "Spam/Rain rate", "s");
              },
              isTogglable: false,
              toolTip: "Cycles spawn rate for Spam Prefabs and Rain Prefabs. 0=unlimited, 0.033=30/s, 0.05=20/s."
            }), new ButtonInfo({
              buttonText: "Grab Selling Machine",
              enableMethod: () => {
                heldSellingMachine = null;
              },
              disableMethod: () => {
                try {
                  if (heldSellingMachine && !heldSellingMachine.isNull?.()) {
                    try {
                      const e = heldSellingMachine.method("get_Runner").invoke();
                      if (e && !e.isNull?.()) {
                        e.method("Despawn").invoke(heldSellingMachine);
                      }
                    } catch (e) {}
                    try {
                      Destroy(heldSellingMachine.method("get_gameObject").invoke());
                    } catch (e) {}
                  }
                } catch (e) {}
                heldSellingMachine = null;
              },
              isTogglable: true,
              method: () => {
                try {
                  if (!rightGrab) {
                    if (heldSellingMachine && !heldSellingMachine.isNull?.()) {
                      try {
                        heldSellingMachine.method("get_gameObject").invoke().method("SetActive").invoke(false);
                      } catch (e) {}
                    }
                    return;
                  }
                  if ((!heldSellingMachine || heldSellingMachine.isNull?.()) && (!(heldSellingMachine = spawnNetworkPrefab("ItemSellingMachineController", zeroVector, identityQuaternion)) || heldSellingMachine.isNull?.())) {
                    return;
                  }
                  const e = heldSellingMachine.method("get_gameObject").invoke();
                  if (e && !e.isNull?.()) {
                    e.method("SetActive").invoke(true);
                  }
                  const t = rightHandTransform.method("get_position").invoke();
                  const o = getLaunchForward(rightHandTransform, headCollider ? getTransform(headCollider) : null);
                  const n = getLaunchUp(rightHandTransform, o);
                  const i = Vector3.method("op_Addition").invoke(t, Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(o, 0.9), Vector3.method("op_Multiply", 2).invoke(n, -0.08)));
                  const a = getTransform(heldSellingMachine);
                  a.method("set_position").invoke(i);
                  a.method("set_rotation").invoke(getLaunchRotation(rightHandTransform, o, n));
                } catch (e) {
                  console.error("Grab Selling Machine:", e);
                }
              },
              toolTip: "While right grip is held, a selling machine stays pushed out in front of your hand."
            }), new ButtonInfo({
              buttonText: "Blackhole Launcher",
              isTogglable: true,
              method: () => {
                try {
                  const e = rightGrab && rightTrigger;
                  if (!e && (!leftGrab || !leftTrigger)) {
                    return;
                  }
                  if (time <= lagGunDelay) {
                    return;
                  }
                  lagGunDelay = time + 0.05;
                  const t = spawnForwardLaunchedNetworkPrefab("Blackhole", e ? rightHandTransform : leftHandTransform, 155, 1.95);
                  if (!t || !!t.isNull?.()) {
                    sendNotification("Buggy spawn failed", false);
                  }
                } catch (e) {
                  console.error("Buggy Hands:", e);
                }
              },
              toolTip: "Hold grip + trigger to keep launching buggies forward from your hand."
            }), new ButtonInfo({
              buttonText: "Delete ALL Prefabs",
              method: () => {
                let e = 0;
                try {
                  const t = Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkObject");
                  const o = Object.method("FindObjectsByType", 1).inflate(t).invoke(0);
                  for (let t = 0; t < o.length; t++) {
                    const n = o.get(t);
                    if (n && !n.isNull()) {
                      try {
                        const t = n.method("get_gameObject").invoke().method("get_name").invoke().toString().toLowerCase();
                        if (t.includes("netplayer") || t.includes("watch") || t.includes("rig") || t.includes("player")) {
                          continue;
                        }
                        const o = n.method("get_Runner").invoke();
                        if (o && !o.isNull()) {
                          o.method("Despawn").invoke(n);
                          e++;
                        }
                      } catch (e) {}
                    }
                  }
                  orbitprefabs = [];
                  orbiters = [];
                  sendNotification("Deleted " + e + " prefabs from the game!", false);
                } catch (e) {
                  console.log("Delete prefabs error: " + e);
                  sendNotification("Error deleting prefabs", false);
                }
              },
              isTogglable: false,
              toolTip: "Deletes EVERY spawned prefab in the game world (players are safe)."
            }), new ButtonInfo({
              buttonText: "Grab Tree",
              method: () => {
                if (!rightGrab) {
                  isGrabbing = false;
                  choppableManager = null;
                  return;
                }
                if (!isGrabbing || !choppableManager) {
                  const e = Vector3.new().unbox();
                  e.method(".ctor").invoke(0, 1000, 0);
                  const t = spawnPrefab("ChoppableTreeManager", e);
                  console.log("manager:", t);
                  choppableManager = getComponentDeep(t, AssemblyCSharp.class("AnimalCompany.ChoppableTreeManager"));
                  console.log("choppableManager:", choppableManager);
                  isGrabbing = true;
                }
                if (!choppableManager) {
                  return;
                }
                const e = rightHandTransform.method("get_position").invoke();
                const t = rightHandTransform.method("get_rotation").invoke();
                choppableManager.method("RPC_SpawnTree", 5).invoke(2, 90, e, t, 40);
              },
              isTogglable: true,
              toolTip: "Grabs and spawns a tree."
            }), new ButtonInfo({
              buttonText: "Zipline Spam",
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = spawnPrefab("SpawnableZipline", rightHandTransform.method("get_position").invoke(), rightHandTransform.method("get_rotation").invoke());
                let t = [];
                try {
                  t = Physics.method("RaycastAll", 4).invoke(Vector3.method("op_Addition").invoke(rightHandTransform.method("get_position").invoke(), Vector3.method("op_Division").invoke(rightHandTransform.method("get_forward").invoke(), 4)), rightHandTransform.method("get_forward").invoke(), 512, -134221825);
                } catch (e) {
                  console.log(`threw: ${e}`);
                }
                let o = 999999;
                let n = null;
                try {
                  let e = 0;
                  for (const i of t) {
                    e++;
                    try {
                      const e = i.method("get_point").invoke();
                      const t = Vector3.method("Distance").invoke(e, rightHandTransform.method("get_position").invoke());
                      if (typeof t == "number" && !isNaN(t) && t < o) {
                        n = i;
                        o = t;
                      }
                    } catch (e) {}
                  }
                } catch (e) {
                  console.log(`iteration threw: ${e}`);
                }
                getComponentDeep(e, AssemblyCSharp.class("AnimalCompany.SpawnableZipline")).method("Setup").invoke(rightHandTransform.method("get_position").invoke(), n.method("get_point").invoke());
              },
              isTogglable: true,
              toolTip: "Grabs and spawns a tree."
            }), new ButtonInfo({
              buttonText: "Small Mom Block",
              method: () => {
                const e = spawnPrefab("MomToyBlockObject", getTransform(bodyCollider).method("get_position").invoke(), identityQuaternion);
                if (!e || e.handle.isNull()) {
                  console.log("null prefab?");
                  return;
                }
                const t = getComponent(e, AssemblyCSharp.class("AnimalCompany.MomToyBlockObject"));
                if (t || !t.handle.isNull()) {
                  t.method("RPC_SetActive").invoke(true);
                  const e = t.method("get_grabbable").invoke();
                  if (!!e || !e.handle.isNull()) {
                    e.method("set_scaleModifier").invoke(-127);
                  }
                } else {
                  console.log("null comp");
                }
              },
              isTogglable: false,
              toolTip: "Spawns a prefab."
            }), new ButtonInfo({
              buttonText: "Mom Block",
              method: () => {
                const e = spawnPrefab("MomToyBlockObject", getTransform(bodyCollider).method("get_position").invoke(), identityQuaternion);
                if (!e || e.handle.isNull()) {
                  console.log("null prefab?");
                  return;
                }
                const t = getComponent(e, AssemblyCSharp.class("AnimalCompany.MomToyBlockObject"));
                if (t || !t.handle.isNull()) {
                  t.method("RPC_SetActive").invoke(true);
                } else {
                  console.log("null comp");
                }
              },
              isTogglable: false,
              toolTip: "Spawns a prefab."
            }), new ButtonInfo({
              buttonText: "Custom Mom Block",
              method: () => {
                const e = spawnPrefab("MomToyBlockObject", getTransform(bodyCollider).method("get_position").invoke(), identityQuaternion);
                if (!e || e.handle.isNull()) {
                  console.log("null prefab?");
                  return;
                }
                const t = getComponent(e, AssemblyCSharp.class("AnimalCompany.MomToyBlockObject"));
                if (t || !t.handle.isNull()) {
                  t.method("RPC_SetActive").invoke(true);
                  t.method("Drop").invoke(getTransform(bodyCollider).method("get_position").invoke(), 0, 3, 4, 1);
                } else {
                  console.log("null comp");
                }
              },
              isTogglable: false,
              toolTip: "Spawns a prefab."
            }), new ButtonInfo({
              buttonText: "Giant Mom Block",
              method: () => {
                const e = spawnPrefab("MomToyBlockObject", getTransform(bodyCollider).method("get_position").invoke(), identityQuaternion);
                if (!e || e.handle.isNull()) {
                  console.log("null prefab?");
                  return;
                }
                const t = getComponent(e, AssemblyCSharp.class("AnimalCompany.MomToyBlockObject"));
                if (t || !t.handle.isNull()) {
                  t.method("RPC_SetActive").invoke(true);
                  const e = t.method("get_grabbable").invoke();
                  if (!!e || !e.handle.isNull()) {
                    e.method("set_scaleModifier").invoke(127);
                  }
                } else {
                  console.log("null comp");
                }
              },
              isTogglable: false,
              toolTip: "Spawns a prefab."
            }), new ButtonInfo({
              buttonText: "Spawn Teleport Pad",
              method: () => {
                spawnPrefab("TeleportMachine", getTransform(bodyCollider).method("get_position").invoke(), identityQuaternion);
                spawnPrefab("TeleportationManager", getTransform(bodyCollider).method("get_position").invoke(), identityQuaternion);
              },
              isTogglable: false,
              toolTip: "Spawns a prefab."
            }), new ButtonInfo({
              buttonText: "Alien ship spawn",
              method: () => {
                try {
                  const e = leftHandTransform.method("get_position").invoke();
                  spawnNetworkPrefab("Vehicle_Spaceship_7", e, leftHandTransform.method("get_rotation").invoke());
                } catch (e) {}
              },
              isTogglable: false,
              toolTip: "Spawns The spaceships"
            }), new ButtonInfo({
              buttonText: "Alien ship bike spawn",
              method: () => {
                try {
                  const e = leftHandTransform.method("get_position").invoke();
                  spawnNetworkPrefab("Vehicle_Spaceship_1", e, leftHandTransform.method("get_rotation").invoke());
                } catch (e) {}
              },
              isTogglable: false,
              toolTip: "Spawns The spaceships"
            }), new ButtonInfo({
              buttonText: "Moon Car Hands",
              isTogglable: true,
              method: () => {
                try {
                  const e = rightGrab && rightTrigger;
                  if (!e && (!leftGrab || !leftTrigger)) {
                    return;
                  }
                  if (time <= lagGunDelay) {
                    return;
                  }
                  lagGunDelay = time + 0.05;
                  const t = spawnForwardLaunchedNetworkPrefab("Vehicle_Buggy", e ? rightHandTransform : leftHandTransform, 155, 1.95);
                  if (!t || !!t.isNull?.()) {
                    sendNotification("Buggy spawn failed", false);
                  }
                } catch (e) {
                  console.error("Buggy Hands:", e);
                }
              },
              toolTip: "Hold grip + trigger to keep launching buggies forward from your hand."
            }), new ButtonInfo({
              buttonText: "Spacebike Hands",
              isTogglable: true,
              method: () => {
                try {
                  const e = rightGrab && rightTrigger;
                  if (!e && (!leftGrab || !leftTrigger)) {
                    return;
                  }
                  if (time <= lagGunDelay) {
                    return;
                  }
                  lagGunDelay = time + 0.05;
                  const t = spawnForwardLaunchedNetworkPrefab("Vehicle_Spacebike", e ? rightHandTransform : leftHandTransform, 155, 1.95);
                  if (!t || !!t.isNull?.()) {
                    sendNotification("spawn failed", false);
                  }
                } catch (e) {
                  console.error("spacebike Hands:", e);
                }
              },
              toolTip: "Hold grip + trigger to keep launching spacebikes forward from your hand."
            }), new ButtonInfo({
              buttonText: "Prefab Orbit",
              enableMethod: () => {
                prefabOrbitEntries = [];
              },
              disableMethod: () => {
                for (const e of prefabOrbitEntries) {
                  try {
                    const t = e?.object;
                    if (!t || t.isNull?.()) {
                      continue;
                    }
                    let o = t;
                    try {
                      const e = t.method("get_gameObject").invoke();
                      if (e && !e.isNull?.()) {
                        o = e;
                      }
                    } catch (e) {}
                    Destroy(o);
                  } catch (e) {}
                }
                prefabOrbitEntries = [];
              },
              isTogglable: true,
              method: () => {
                try {
                  const e = getTransform(headCollider).method("get_position").invoke();
                  const t = prefabIDs[prefabIndex];
                  while (prefabOrbitEntries.length < 6) {
                    const o = Math.PI * 2 * prefabOrbitEntries.length / 6;
                    const n = spawnNetworkPrefab(t, [e.field("x").value + Math.cos(o) * 3.1, e.field("y").value + 0.8, e.field("z").value + Math.sin(o) * 3.1], identityQuaternion);
                    if (!n || n.isNull?.()) {
                      break;
                    }
                    prefabOrbitEntries.push({
                      object: n,
                      angle: o
                    });
                  }
                  for (const t of prefabOrbitEntries) {
                    try {
                      if (!t.object || t.object.isNull?.()) {
                        continue;
                      }
                      t.angle += deltaTime * 1.6;
                      const o = [e.field("x").value + Math.cos(t.angle) * 3.3, e.field("y").value + 0.8 + Math.sin(t.angle * 2 + time) * 0.25, e.field("z").value + Math.sin(t.angle) * 3.3];
                      getTransform(t.object).method("set_position").invoke(o);
                    } catch (e) {}
                  }
                } catch (e) {
                  console.error("Prefab Orbit:", e);
                }
              },
              toolTip: "Orbits the currently selected Prefab ID around you and deletes them when turned off."
            }), new ButtonInfo({
              buttonText: "ChristmasBox Orbit",
              method: () => {
                const e = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                if (orbitprefabs.length < 4) {
                  (function () {
                    orbiters = [];
                    for (let t = 0; t < 8; t++) {
                      let o = Math.PI * 2 / 8 * t;
                      const n = Vector3.alloc();
                      n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(o) * 6.5, 0, Math.sin(o) * 6.5);
                      let i = e.method("get_position").invoke();
                      let a = spawnNetworkPrefab("ChristmasBox", Vector3.method("op_Addition").invoke(i, [n.field("x").value, n.field("y").value, n.field("z").value]), Quaternion.method("get_identity").invoke());
                      if (!a) {
                        continue;
                      }
                      orbitprefabs.push(a);
                      let l = a.method("get_gameObject").invoke();
                      if (!l) {
                        continue;
                      }
                      let r = l.method("get_transform").invoke();
                      orbiters.push({
                        transform: r,
                        angle: o
                      });
                    }
                  })();
                }
                let t = Time.method("get_deltaTime").invoke();
                let o = e.method("get_position").invoke();
                for (let e of orbiters) {
                  e.angle += t * 1.5;
                  const n = Vector3.alloc();
                  n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(e.angle) * 6.5, 0, Math.sin(e.angle) * 6.5);
                  let i = Vector3.method("op_Addition").invoke(o, [n.field("x").value, n.field("y").value, n.field("z").value]);
                  e.transform.method("set_position").invoke(i);
                }
              },
              disableMethod: () => {
                for (let e = 0; e < orbitprefabs.length; e++) {
                  const t = orbitprefabs[e];
                  if (t && !t.isNull?.()) {
                    try {
                      const e = t.method("get_Runner").invoke();
                      if (e && !e.isNull()) {
                        e.method("Despawn").invoke(t);
                      }
                    } catch (e) {}
                  }
                }
                orbitprefabs = [];
                orbiters = [];
              },
              isTogglable: true,
              toolTip: "Creates 8 selling machines that orbit you."
            }), new ButtonInfo({
              buttonText: "Christmas Present Tower",
              method: () => {
                const e = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                if (orbitprefabs.length < 10) {
                  (function () {
                    orbiters = [];
                    const t = [-6, -4, -2, 0, 2, 4, 6];
                    for (let o = 0; o < t.length; o++) {
                      for (let n = 0; n < 12; n++) {
                        const i = Math.PI * 2 / 12 * n;
                        const a = Vector3.alloc();
                        a.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(i) * 4.8, t[o], Math.sin(i) * 4.8);
                        const l = e.method("get_position").invoke();
                        const r = spawnNetworkPrefab("ChristmasBox", Vector3.method("op_Addition").invoke(l, [a.field("x").value, a.field("y").value, a.field("z").value]), Quaternion.method("get_identity").invoke());
                        if (!r) {
                          continue;
                        }
                        orbitprefabs.push(r);
                        const s = r.method("get_gameObject").invoke();
                        if (!s) {
                          continue;
                        }
                        const c = s.method("get_transform").invoke();
                        orbiters.push({
                          transform: c,
                          angle: i,
                          height: t[o]
                        });
                      }
                    }
                  })();
                }
                const t = Time.method("get_deltaTime").invoke();
                const o = e.method("get_position").invoke();
                for (const e of orbiters) {
                  e.angle += t * 0.85;
                  const n = Vector3.alloc();
                  n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(e.angle) * 4.8, e.height, Math.sin(e.angle) * 4.8);
                  const i = Vector3.method("op_Addition").invoke(o, [n.field("x").value, n.field("y").value, n.field("z").value]);
                  e.transform.method("set_position").invoke(i);
                }
              },
              disableMethod: () => {
                for (let e = 0; e < orbitprefabs.length; e++) {
                  const t = orbitprefabs[e];
                  if (t && !t.isNull?.()) {
                    try {
                      const e = t.method("get_Runner").invoke();
                      if (e && !e.isNull()) {
                        e.method("Despawn").invoke(t);
                      }
                    } catch (e) {}
                  }
                }
                orbitprefabs = [];
                orbiters = [];
              },
              isTogglable: true,
              toolTip: "Creates a 7-story tower of Christmas presents around you."
            }), new ButtonInfo({
              buttonText: "Sellingmachine Orbit self",
              method: () => {
                const e = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                if (orbitprefabs.length < 4) {
                  (function () {
                    orbiters = [];
                    for (let t = 0; t < 8; t++) {
                      let o = Math.PI * 2 / 8 * t;
                      const n = Vector3.alloc();
                      n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(o) * 6.5, 0, Math.sin(o) * 6.5);
                      let i = e.method("get_position").invoke();
                      let a = spawnNetworkPrefab("ItemSellingMachineController", Vector3.method("op_Addition").invoke(i, [n.field("x").value, n.field("y").value, n.field("z").value]), Quaternion.method("get_identity").invoke());
                      if (!a) {
                        continue;
                      }
                      orbitprefabs.push(a);
                      let l = a.method("get_gameObject").invoke();
                      if (!l) {
                        continue;
                      }
                      let r = l.method("get_transform").invoke();
                      orbiters.push({
                        transform: r,
                        angle: o
                      });
                    }
                  })();
                }
                let t = Time.method("get_deltaTime").invoke();
                let o = e.method("get_position").invoke();
                for (let e of orbiters) {
                  e.angle += t * 1.5;
                  const n = Vector3.alloc();
                  n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(e.angle) * 6.5, 0, Math.sin(e.angle) * 6.5);
                  let i = Vector3.method("op_Addition").invoke(o, [n.field("x").value, n.field("y").value, n.field("z").value]);
                  e.transform.method("set_position").invoke(i);
                }
              },
              disableMethod: () => {
                for (let e = 0; e < orbitprefabs.length; e++) {
                  const t = orbitprefabs[e];
                  if (!t) {
                    return;
                  }
                  const o = t.method("get_Runner").invoke();
                  if (o && !o.isNull()) {
                    o.method("Despawn").invoke(t);
                  }
                }
                orbitprefabs = [];
                orbiters = [];
              },
              isTogglable: true,
              toolTip: "Creates 8 selling machines that orbit you."
            }), new ButtonInfo({
              buttonText: "Sellingmachine Tower Orbit",
              method: () => {
                const e = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                if (sellingTowerOrbitPrefabs.length < 10) {
                  (function () {
                    sellingTowerOrbiters = [];
                    const t = [-3.3, 0, 3.3];
                    for (let o = 0; o < t.length; o++) {
                      for (let n = 0; n < 24; n++) {
                        const i = Math.PI * 2 / 24 * n;
                        const a = Vector3.alloc();
                        a.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(i) * 5.6, t[o], Math.sin(i) * 5.6);
                        const l = e.method("get_position").invoke();
                        const r = spawnNetworkPrefab("ItemSellingMachineController", Vector3.method("op_Addition").invoke(l, [a.field("x").value, a.field("y").value, a.field("z").value]), Quaternion.method("get_identity").invoke());
                        if (!r) {
                          continue;
                        }
                        sellingTowerOrbitPrefabs.push(r);
                        const s = r.method("get_gameObject").invoke();
                        if (!s) {
                          continue;
                        }
                        const c = s.method("get_transform").invoke();
                        sellingTowerOrbiters.push({
                          transform: c,
                          angle: i,
                          height: t[o]
                        });
                      }
                    }
                  })();
                }
                const t = Time.method("get_deltaTime").invoke();
                const o = e.method("get_position").invoke();
                for (const e of sellingTowerOrbiters) {
                  e.angle += t * 0.95;
                  const n = Vector3.alloc();
                  n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(e.angle) * 5.6, e.height, Math.sin(e.angle) * 5.6);
                  const i = Vector3.method("op_Addition").invoke(o, [n.field("x").value, n.field("y").value, n.field("z").value]);
                  e.transform.method("set_position").invoke(i);
                }
              },
              disableMethod: () => {
                sellingTowerOrbiters = [];
                sellingTowerOrbitPrefabs = [];
                sendNotification("Selling machine tower frozen in place", false);
              },
              isTogglable: true,
              toolTip: "Creates a dense 3-story selling machine orbit and leaves it in place when disabled."
            }), new ButtonInfo({
              buttonText: "Moon Car Orbit",
              method: () => {
                const e = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                if (buggyOrbitPrefabs.length < 4) {
                  (function () {
                    buggyOrbiters = [];
                    for (let t = 0; t < 8; t++) {
                      let o = Math.PI * 2 / 8 * t;
                      const n = Vector3.alloc();
                      n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(o) * 7, 0.4, Math.sin(o) * 7);
                      let i = e.method("get_position").invoke();
                      let a = spawnNetworkPrefab("Vehicle_Buggy", Vector3.method("op_Addition").invoke(i, [n.field("x").value, n.field("y").value, n.field("z").value]), Quaternion.method("get_identity").invoke());
                      if (!a) {
                        continue;
                      }
                      buggyOrbitPrefabs.push(a);
                      let l = a.method("get_gameObject").invoke();
                      if (!l) {
                        continue;
                      }
                      let r = l.method("get_transform").invoke();
                      buggyOrbiters.push({
                        transform: r,
                        angle: o
                      });
                    }
                  })();
                }
                let t = Time.method("get_deltaTime").invoke();
                let o = e.method("get_position").invoke();
                for (let e of buggyOrbiters) {
                  e.angle += t * 1.15;
                  const n = Vector3.alloc();
                  n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(e.angle) * 7, 0.4, Math.sin(e.angle) * 7);
                  let i = Vector3.method("op_Addition").invoke(o, [n.field("x").value, n.field("y").value, n.field("z").value]);
                  e.transform.method("set_position").invoke(i);
                }
              },
              disableMethod: () => {
                for (let e = 0; e < buggyOrbitPrefabs.length; e++) {
                  const t = buggyOrbitPrefabs[e];
                  if (t && !t.isNull?.()) {
                    try {
                      const e = t.method("get_Runner").invoke();
                      if (e && !e.isNull()) {
                        e.method("Despawn").invoke(t);
                      }
                    } catch (e) {}
                  }
                }
                buggyOrbitPrefabs = [];
                buggyOrbiters = [];
              },
              isTogglable: true,
              toolTip: "Creates 8 buggies that orbit around you."
            }), new ButtonInfo({
              buttonText: "Spacebike Orbit",
              method: () => {
                const e = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                if (buggyOrbitPrefabs.length < 4) {
                  (function () {
                    buggyOrbiters = [];
                    for (let t = 0; t < 8; t++) {
                      let o = Math.PI * 2 / 8 * t;
                      const n = Vector3.alloc();
                      n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(o) * 7, 0.4, Math.sin(o) * 7);
                      let i = e.method("get_position").invoke();
                      let a = spawnNetworkPrefab("Vehicle_Spacebike", Vector3.method("op_Addition").invoke(i, [n.field("x").value, n.field("y").value, n.field("z").value]), Quaternion.method("get_identity").invoke());
                      if (!a) {
                        continue;
                      }
                      buggyOrbitPrefabs.push(a);
                      let l = a.method("get_gameObject").invoke();
                      if (!l) {
                        continue;
                      }
                      let r = l.method("get_transform").invoke();
                      buggyOrbiters.push({
                        transform: r,
                        angle: o
                      });
                    }
                  })();
                }
                let t = Time.method("get_deltaTime").invoke();
                let o = e.method("get_position").invoke();
                for (let e of buggyOrbiters) {
                  e.angle += t * 1.15;
                  const n = Vector3.alloc();
                  n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(e.angle) * 7, 0.4, Math.sin(e.angle) * 7);
                  let i = Vector3.method("op_Addition").invoke(o, [n.field("x").value, n.field("y").value, n.field("z").value]);
                  e.transform.method("set_position").invoke(i);
                }
              },
              disableMethod: () => {
                for (let e = 0; e < buggyOrbitPrefabs.length; e++) {
                  const t = buggyOrbitPrefabs[e];
                  if (t && !t.isNull?.()) {
                    try {
                      const e = t.method("get_Runner").invoke();
                      if (e && !e.isNull()) {
                        e.method("Despawn").invoke(t);
                      }
                    } catch (e) {}
                  }
                }
                buggyOrbitPrefabs = [];
                buggyOrbiters = [];
              },
              isTogglable: true,
              toolTip: "Creates 8 spacebikes that orbit around you."
            }), new ButtonInfo({
              buttonText: "Spacebike Dome",
              method: () => {
                const e = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                if (orbitprefabs.length < 10) {
                  (function () {
                    orbiters = [];
                    const t = [];
                    for (let e = 0; e < 14; e++) {
                      const o = e / 13;
                      const n = Math.cos(o * Math.PI) * 6;
                      t.push(n);
                    }
                    for (let o = 0; o < t.length; o++) {
                      if (!(o < 3)) {
                        for (let n = 0; n < 18; n++) {
                          const i = Math.PI * 2 / 18 * n;
                          const a = o / 13;
                          const l = Math.sin(a * Math.PI) * 6;
                          const r = Vector3.alloc();
                          r.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(i) * l, t[o], Math.sin(i) * l);
                          const s = e.method("get_position").invoke();
                          const c = spawnNetworkPrefab("Vehicle_Spacebike", Vector3.method("op_Addition").invoke(s, [r.field("x").value, r.field("y").value, r.field("z").value]), Quaternion.method("get_identity").invoke());
                          if (!c) {
                            continue;
                          }
                          orbitprefabs.push(c);
                          const d = c.method("get_gameObject").invoke();
                          if (!d) {
                            continue;
                          }
                          const u = d.method("get_transform").invoke();
                          orbiters.push({
                            transform: u,
                            angle: i,
                            height: t[o],
                            radius: l
                          });
                        }
                      }
                    }
                  })();
                }
                const t = Time.method("get_deltaTime").invoke();
                const o = e.method("get_position").invoke();
                for (const e of orbiters) {
                  e.angle += t * 0.85;
                  const n = Vector3.alloc();
                  n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(e.angle) * e.radius, e.height, Math.sin(e.angle) * e.radius);
                  const i = Vector3.method("op_Addition").invoke(o, [n.field("x").value, n.field("y").value, n.field("z").value]);
                  e.transform.method("set_position").invoke(i);
                }
              },
              disableMethod: () => {
                for (let e = 0; e < orbitprefabs.length; e++) {
                  const t = orbitprefabs[e];
                  if (t && !t.isNull?.()) {
                    try {
                      const e = t.method("get_Runner").invoke();
                      if (e && !e.isNull()) {
                        e.method("Despawn").invoke(t);
                      }
                    } catch (e) {}
                  }
                }
                orbitprefabs = [];
                orbiters = [];
              },
              isTogglable: true,
              toolTip: "Creates a Dome of Selling Machines around you."
            }), new ButtonInfo({
              buttonText: "Tree Forest Orbit",
              method: () => {
                const e = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                if (sharkOrbitPrefabs.length < 4) {
                  (function () {
                    sharkOrbiters = [];
                    for (let t = 0; t < 10; t++) {
                      const o = Math.PI * 2 / 10 * t;
                      const n = Vector3.alloc();
                      n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(o) * 6, 0, Math.sin(o) * 6);
                      const i = e.method("get_position").invoke();
                      const a = spawnNetworkPrefab("ChoppableTreeManager", Vector3.method("op_Addition").invoke(i, [n.field("x").value, n.field("y").value, n.field("z").value]), Quaternion.method("get_identity").invoke());
                      if (!a) {
                        continue;
                      }
                      sharkOrbitPrefabs.push(a);
                      const l = a.method("get_gameObject").invoke();
                      if (!l) {
                        continue;
                      }
                      const r = l.method("get_transform").invoke();
                      sharkOrbiters.push({
                        transform: r,
                        angle: o
                      });
                    }
                  })();
                }
                const t = Time.method("get_deltaTime").invoke();
                const o = e.method("get_position").invoke();
                for (const e of sharkOrbiters) {
                  e.angle += t * 1.2;
                  const n = Vector3.alloc();
                  n.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(e.angle) * 6, 0, Math.sin(e.angle) * 6);
                  const i = Vector3.method("op_Addition").invoke(o, [n.field("x").value, n.field("y").value, n.field("z").value]);
                  e.transform.method("set_position").invoke(i);
                }
              },
              disableMethod: () => {
                for (const e of sharkOrbitPrefabs) {
                  if (e && !e.isNull?.()) {
                    try {
                      const t = e.method("get_Runner").invoke();
                      if (t && !t.isNull()) {
                        t.method("Despawn").invoke(e);
                      }
                    } catch (e) {}
                    try {
                      destroyNetworkedObjectDeep(e);
                    } catch (e) {}
                    try {
                      const t = e.method("get_gameObject").invoke();
                      if (t && !t.isNull?.()) {
                        Destroy(t);
                      }
                    } catch (e) {}
                  }
                }
                sharkOrbitPrefabs = [];
                sharkOrbiters = [];
              },
              isTogglable: true,
              toolTip: "Creates a normal orbit of forest trees around you."
            }), new ButtonInfo({
              buttonText: "Teleport All Prefabs",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = getTransform(e).method("get_position").invoke();
                  let o = 0;
                  try {
                    const e = GBIClass.method("get_allLootItems").invoke();
                    if (e && !e.isNull()) {
                      const n = e.method("GetEnumerator").invoke();
                      while (n.method("MoveNext").invoke()) {
                        try {
                          const e = n.method("get_Current").invoke();
                          if (!e || e.isNull()) {
                            continue;
                          }
                          getTransform(e).method("set_position").invoke(t);
                          o++;
                        } catch (e) {}
                      }
                    }
                  } catch (e) {
                    console.error("[TeleportAll] " + e);
                  }
                  try {
                    const e = Object.method("FindObjectOfType", 0).inflate(ItemSellingMachineController).invoke();
                    if (e && !e.isNull()) {
                      getTransform(e).method("set_position").invoke(t);
                    }
                  } catch (e) {}
                  sendNotification("Teleported " + o + " items to you!", false);
                } catch (e) {
                  sendNotification("Teleport all failed: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Teleports all loot items to your position."
            })], [new ButtonInfo({
              buttonText: "Exit Mob mods",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns to main menu."
            }), new ButtonInfo({
              buttonText: "Mob ids",
              method: () => {
                currentCategory = 18;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns to main menu."
            }), new ButtonInfo({
              buttonText: "Spawn Mob",
              method: () => {
                try {
                  const e = mobIDs[mobIndex];
                  const t = getDefaultMobSpawnPose();
                  const o = t.pos;
                  const n = t.rot;
                  const i = e.name;
                  sendNotification(spawnMob(i, o, n) ? "Spawned mob: " + i + " (id=" + getMobIdLabel(e) + ")" : "Spawn failed for: " + i, false);
                } catch (e) {
                  sendNotification("Spawn Mob error: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Spawns the selected mob at your right hand using validator bypass."
            }), new ButtonInfo({
              buttonText: "Spawn Mob at Hand",
              enableMethod: () => {
                spawnMobEnabled = true;
              },
              disableMethod: () => {
                spawnMobEnabled = false;
              },
              method: () => {
                if (!spawnMobEnabled) {
                  return;
                }
                if ((_pcMode ? rightGrab && rightTrigger : rightSecondary && rightGrab) && time > mobSpawnDelay) {
                  mobSpawnDelay = time + 0.35;
                  try {
                    const e = mobIDs[mobIndex];
                    const t = getDefaultMobSpawnPose();
                    const o = t.pos;
                    const n = t.rot;
                    sendNotification(spawnMob(e.name, o, n) ? "Spawning mob: " + e.name + " (id=" + getMobIdLabel(e) + ")" : "Mob spawn failed for: " + e.name, false);
                  } catch (e) {
                    sendNotification("Mob spawn failed: " + e, false);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Hold right grip + B to spawn the selected mob at your right hand."
            }), new ButtonInfo({
              buttonText: "Spawn ALL Mobs (guns)",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger) {
                  const t = e.method("get_point").invoke();
                  const o = [{
                    name: "Angler",
                    id: 1
                  }, {
                    name: "AnglerMad",
                    id: 2
                  }, {
                    name: "Armstrong",
                    id: 3
                  }, {
                    name: "ArmstrongMad",
                    id: 4
                  }, {
                    name: "Banshee",
                    id: 5
                  }, {
                    name: "Bomb",
                    id: 6
                  }, {
                    name: "Bomber",
                    id: 7
                  }, {
                    name: "BomberFlashbang",
                    id: 8
                  }, {
                    name: "BomberMad",
                    id: 9
                  }, {
                    name: "Chicken",
                    id: 10
                  }, {
                    name: "Cyst",
                    id: 11
                  }, {
                    name: "FakeGorilla",
                    id: 12
                  }, {
                    name: "BigHead",
                    id: 13
                  }, {
                    name: "RedGreen",
                    id: 14
                  }, {
                    name: "Phantom",
                    id: 15
                  }, {
                    name: "EvilEye",
                    id: 16
                  }, {
                    name: "GiantThrower",
                    id: 17
                  }, {
                    name: "RedGreenMad",
                    id: 18
                  }, {
                    name: "Spider",
                    id: 19
                  }, {
                    name: "FlyingSwarm",
                    id: 20
                  }, {
                    name: "NextBot",
                    id: 21
                  }, {
                    name: "Segway",
                    id: 22
                  }, {
                    name: "NextBotStatic",
                    id: 23
                  }, {
                    name: "EvilEyePinata",
                    id: 24
                  }, {
                    name: "EvilEyePinataLarge",
                    id: 25
                  }, {
                    name: "Lanky",
                    id: 26
                  }, {
                    name: "Blob",
                    id: 27
                  }, {
                    name: "Cutie",
                    id: 28
                  }, {
                    name: "SpiderCave",
                    id: 29
                  }, {
                    name: "ForestMob",
                    id: 30
                  }, {
                    name: "Mimic",
                    id: 31
                  }, {
                    name: "GraveyardBoss",
                    id: 32
                  }, {
                    name: "Ringmaster",
                    id: 33
                  }, {
                    name: "Puppet",
                    id: 34
                  }, {
                    name: "PolypMass",
                    id: 35
                  }, {
                    name: "RobotDog",
                    id: 36
                  }, {
                    name: "Shadow",
                    id: 37
                  }, {
                    name: "Heart",
                    id: 38
                  }, {
                    name: "Slimey",
                    id: 39
                  }, {
                    name: "ShadowBoss",
                    id: 40
                  }, {
                    name: "BigShark",
                    id: 41
                  }, {
                    name: "EdenZombie",
                    id: 42
                  }, {
                    name: "Skinwalker",
                    id: 43
                  }, {
                    name: "YinWorm",
                    id: 44
                  }, {
                    name: "YangWorm",
                    id: 45
                  }, {
                    name: "ArmstrongSpace",
                    id: 46
                  }, {
                    name: "Smiley",
                    id: 47
                  }, {
                    name: "Uvula",
                    id: 48
                  }, {
                    name: "TubeMonster",
                    id: 49
                  }, {
                    name: "SmileyMad",
                    id: 50
                  }, {
                    name: "MomBossController",
                    id: 51
                  }];
                  for (let e = 0; e < o.length; e++) {
                    const n = o[e];
                    spawnMobAtPos(n, t, identityQuaternion);
                    sendNotification("Spawned: " + n.name + " (id=" + n.id + ")", false);
                  }
                }
              },
              toolTip: "Spawns ALL mobs at your aim point (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "Spawn mob (gun)",
              enableMethod: () => {
                spawnMobGunEnabled = true;
                spawnMobEnabled = false;
                spawnGunEnabled = false;
                prefabGunEnabled = false;
              },
              disableMethod: () => {
                spawnMobGunEnabled = false;
              },
              method: () => {
                if (!spawnMobGunEnabled) {
                  return;
                }
                if (!rightGrab) {
                  hideGunLib();
                  return;
                }
                const e = renderGun();
                if (rightTrigger && e.ray && !e.ray.isNull() && !(time < mobSpawnDelay)) {
                  mobSpawnDelay = time + 0.05;
                  try {
                    const t = e.endPosition;
                    const o = mobIDs[mobIndex];
                    sendNotification(spawnMob(o.name, t, identityQuaternion) ? "Spawning mob: " + o.name + " at laser dot" : "Mob Spawner Gun failed for: " + o.name, false);
                  } catch (e) {
                    sendNotification("Mob gun failed: " + e, false);
                  }
                }
              },
              isTogglable: true,
              toolTip: "Hold right grip to aim, pull trigger to spawn selected mob at laser dot."
            }), new ButtonInfo({
              buttonText: "Force Spawned Mobs Stay",
              enableMethod: () => {
                mobForceStayEnabled = true;
                sendNotification("Force Spawned Mobs Stay ON", false);
              },
              disableMethod: () => {
                mobForceStayEnabled = false;
                sendNotification("Force Spawned Mobs Stay OFF", false);
              },
              isTogglable: true,
              toolTip: "Keeps tracked spawned mobs active and respawns them if they drop out."
            }), new ButtonInfo({
              buttonText: "start zombie",
              method: () => {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.HordeMobLobbyHandler");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                  if (t && t.length > 0) {
                    const e = t.get(0);
                    const o = e.method("get_Object").invoke();
                    if (o) {
                      o.method("RequestStateAuthority").invoke();
                    }
                    e.method("RPC_WaveStart").invoke();
                    sendNotification("ZOMBIE EVENT STARTED!", false);
                    console.log("✓ HordeMobLobbyHandler: RPC_WaveStart invoked.");
                  } else {
                    sendNotification("Horde Handler not found in this map.", false);
                  }
                } catch (e) {
                  console.error("Zombie Event Error: " + e);
                }
              },
              isTogglable: false,
              toolTip: "Uses the HordeMobLobbyHandler to start the official Zombie Wave event."
            }), new ButtonInfo({
              buttonText: "end zombie",
              method: () => {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.HordeMobLobbyHandler");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0).get(0);
                  if (t) {
                    t.method("get_Object").invoke().method("RequestStateAuthority").invoke();
                    if (!t.method("RPC_WaveCancel").invoke()) {
                      t.method("RPC_ResetStartButtonPressed").invoke();
                    }
                    sendNotification("Horde Event Cancelled.", false);
                  }
                } catch (e) {}
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "break mobs",
              method: () => {
                try {
                  const e = AssemblyCSharp.class("AnimalCompany.HordeMobLobbyHandler");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0).get(0);
                  if (t) {
                    t.method("get_Object").invoke().method("RequestStateAuthority").invoke();
                    if (!t.method("RPC_WaveCancel").invoke()) {
                      t.method("RPC_ResetStartButtonPressed").invoke();
                    }
                    sendNotification("Horde Event Cancelled.", false);
                  }
                } catch (e) {}
              },
              isTogglable: false
            }), new ButtonInfo({
              buttonText: "Mob Orbit",
              enableMethod: () => {
                mobOrbitEntries = [];
              },
              disableMethod: () => {
                mobOrbitEntries = [];
              },
              isTogglable: true,
              method: () => {
                try {
                  const e = getTransform(headCollider).method("get_position").invoke();
                  const t = mobIDs[mobIndex];
                  while (mobOrbitEntries.length < 6) {
                    const o = Math.PI * 2 * mobOrbitEntries.length / 6;
                    const n = spawnMobAtPos(t, [e.field("x").value + Math.cos(o) * 3, e.field("y").value + 0.6, e.field("z").value + Math.sin(o) * 3], identityQuaternion);
                    mobOrbitEntries.push({
                      mobEntry: t,
                      object: n ?? null,
                      angle: o
                    });
                  }
                  for (const t of mobOrbitEntries) {
                    try {
                      if (!t.object || t.object.isNull?.()) {
                        continue;
                      }
                      t.angle += deltaTime * 1.8;
                      const o = [e.field("x").value + Math.cos(t.angle) * 3.2, e.field("y").value + 0.7 + Math.sin(t.angle * 2 + time) * 0.35, e.field("z").value + Math.sin(t.angle) * 3.2];
                      stabilizeMobInstance(t.object, o);
                      getTransform(t.object).method("set_position").invoke(o);
                    } catch (e) {}
                  }
                } catch (e) {
                  console.error("Mob Orbit:", e);
                }
              },
              toolTip: "Spawns the current mob selection into an orbit around you and keeps them active."
            }), new ButtonInfo({
              buttonText: "Kill All Mobs",
              method: () => {
                try {
                  let e = 0;
                  for (const t of mobIDs) {
                    try {
                      const o = AssemblyCSharp.class("AnimalCompany." + t.name + "Controller");
                      const n = Object.method("FindObjectsByType", 1).inflate(o).invoke(0);
                      if (!n || n.isNull()) {
                        continue;
                      }
                      for (let t = 0; t < n.length; t++) {
                        try {
                          const o = n.get(t);
                          if (!o || o.isNull()) {
                            continue;
                          }
                          try {
                            o.method("RPC_KillMob").invoke();
                            e++;
                          } catch (t) {
                            try {
                              o.method("KillMob").invoke();
                              e++;
                            } catch (t) {
                              try {
                                o.method("Die").invoke();
                                e++;
                              } catch (e) {}
                            }
                          }
                        } catch (e) {}
                      }
                    } catch (e) {}
                  }
                  sendNotification("Killed " + e + " mobs!", false);
                } catch (e) {
                  sendNotification("Kill Mobs: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Kills all mobs in the scene."
            }), new ButtonInfo({
              buttonText: "Explode All Mobs",
              method: () => {
                try {
                  let e = 0;
                  const t = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                  for (const o of mobIDs) {
                    try {
                      const n = AssemblyCSharp.class("AnimalCompany." + o.name + "Controller");
                      const i = Object.method("FindObjectsByType", 1).inflate(n).invoke(0);
                      if (!i || i.isNull()) {
                        continue;
                      }
                      for (let o = 0; o < i.length; o++) {
                        try {
                          const n = i.get(o);
                          if (!n || n.isNull()) {
                            continue;
                          }
                          const a = getTransform(n).method("get_position").invoke();
                          const l = [VFXTypes.MeatExplosion_1, VFXTypes.MeatExplosion_2, VFXTypes.MeatExplosion_Headshot, VFXTypes.Explosion_Coins, VFXTypes.FuelExplosion, VFXTypes.Electricity_Small];
                          for (const e of l) {
                            try {
                              ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(t, e, a, identityQuaternion);
                            } catch (e) {}
                          }
                          try {
                            n.method("RPC_KillMob").invoke();
                          } catch (e) {
                            try {
                              n.method("KillMob").invoke();
                            } catch (e) {}
                          }
                          e++;
                        } catch (e) {}
                      }
                    } catch (e) {}
                  }
                  sendNotification("Exploded " + e + " mobs!", false);
                } catch (e) {
                  sendNotification("Explode Mobs: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Kills all mobs with explosion VFX."
            }), new ButtonInfo({
              buttonText: "Mob VFX Spam",
              isTogglable: true,
              method: () => {
                if (time > mobGunDelay) {
                  mobGunDelay = time + 0.3;
                  try {
                    const e = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                    for (const t of mobIDs) {
                      try {
                        const o = AssemblyCSharp.class("AnimalCompany." + t.name + "Controller");
                        const n = Object.method("FindObjectsByType", 1).inflate(o).invoke(0);
                        if (!n || n.isNull()) {
                          continue;
                        }
                        for (let t = 0; t < n.length; t++) {
                          try {
                            const o = n.get(t);
                            if (!o || o.isNull()) {
                              continue;
                            }
                            const i = getTransform(o).method("get_position").invoke();
                            const a = [VFXTypes.ConfettiBurst, VFXTypes.Ethereal_Void, VFXTypes.MeatExplosion_1, VFXTypes.Electricity_Small, VFXTypes.FuelExplosion];
                            const l = a[Math.floor(Math.random() * a.length)];
                            try {
                              ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(e, l, i, identityQuaternion);
                            } catch (e) {}
                          } catch (e) {}
                        }
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Spams random VFX on all mobs every 0.3s (toggle)."
            }), new ButtonInfo({
              buttonText: "Mob Kill Loop",
              isTogglable: true,
              method: () => {
                if (time > mobGunDelay2) {
                  mobGunDelay2 = time + 1.5;
                  try {
                    for (const e of mobIDs) {
                      try {
                        const t = AssemblyCSharp.class("AnimalCompany." + e.name + "Controller");
                        const o = Object.method("FindObjectsByType", 1).inflate(t).invoke(0);
                        if (!o || o.isNull()) {
                          continue;
                        }
                        for (let e = 0; e < o.length; e++) {
                          try {
                            const t = o.get(e);
                            if (!t || t.isNull()) {
                              continue;
                            }
                            try {
                              t.method("RPC_KillMob").invoke();
                            } catch (e) {
                              try {
                                t.method("KillMob").invoke();
                              } catch (e) {}
                            }
                          } catch (e) {}
                        }
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously kills all mobs every 1.5s (toggle)."
            }), new ButtonInfo({
              buttonText: "Mob TP To Me",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = getTransform(e).method("get_position").invoke();
                  let o = 0;
                  for (const e of mobIDs) {
                    try {
                      const n = AssemblyCSharp.class("AnimalCompany." + e.name + "Controller");
                      const i = Object.method("FindObjectsByType", 1).inflate(n).invoke(0);
                      if (!i || i.isNull()) {
                        continue;
                      }
                      for (let e = 0; e < i.length; e++) {
                        try {
                          const n = i.get(e);
                          if (!n || n.isNull()) {
                            continue;
                          }
                          getTransform(n).method("set_position").invoke(t);
                          o++;
                        } catch (e) {}
                      }
                    } catch (e) {}
                  }
                  sendNotification("Teleported " + o + " mobs to you!", false);
                } catch (e) {
                  sendNotification("Mob TP: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Teleports all mobs to your position."
            }), new ButtonInfo({
              buttonText: "Mob Scale Big",
              method: () => {
                try {
                  let e = 0;
                  for (const t of mobIDs) {
                    try {
                      const o = AssemblyCSharp.class("AnimalCompany." + t.name + "Controller");
                      const n = Object.method("FindObjectsByType", 1).inflate(o).invoke(0);
                      if (!n || n.isNull()) {
                        continue;
                      }
                      for (let t = 0; t < n.length; t++) {
                        try {
                          const o = n.get(t);
                          if (!o || o.isNull()) {
                            continue;
                          }
                          getTransform(o).method("set_localScale").invoke([5, 5, 5]);
                          e++;
                        } catch (e) {}
                      }
                    } catch (e) {}
                  }
                  sendNotification("Scaled " + e + " mobs big!", false);
                } catch (e) {
                  sendNotification("Mob Scale: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Makes all mobs giant."
            }), new ButtonInfo({
              buttonText: "Mob Scale Tiny",
              method: () => {
                try {
                  let e = 0;
                  for (const t of mobIDs) {
                    try {
                      const o = AssemblyCSharp.class("AnimalCompany." + t.name + "Controller");
                      const n = Object.method("FindObjectsByType", 1).inflate(o).invoke(0);
                      if (!n || n.isNull()) {
                        continue;
                      }
                      for (let t = 0; t < n.length; t++) {
                        try {
                          const o = n.get(t);
                          if (!o || o.isNull()) {
                            continue;
                          }
                          getTransform(o).method("set_localScale").invoke([0.1, 0.1, 0.1]);
                          e++;
                        } catch (e) {}
                      }
                    } catch (e) {}
                  }
                  sendNotification("Shrunk " + e + " mobs!", false);
                } catch (e) {
                  sendNotification("Mob Scale: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Makes all mobs tiny."
            }), new ButtonInfo({
              buttonText: "Mob Freeze",
              isTogglable: true,
              method: () => {
                if (time > mobGunDelay) {
                  mobGunDelay = time + 0.5;
                  try {
                    for (const e of mobIDs) {
                      try {
                        const t = AssemblyCSharp.class("AnimalCompany." + e.name + "Controller");
                        const o = Object.method("FindObjectsByType", 1).inflate(t).invoke(0);
                        if (!o || o.isNull()) {
                          continue;
                        }
                        for (let e = 0; e < o.length; e++) {
                          try {
                            const t = o.get(e);
                            if (!t || t.isNull()) {
                              continue;
                            }
                            try {
                              t.method("RPC_Stun").invoke(999);
                            } catch (e) {}
                            try {
                              t.method("Stun").invoke(999);
                            } catch (e) {}
                            try {
                              t.method("set_isStunned").invoke(true);
                            } catch (e) {}
                            try {
                              const e = getComponent(t.method("get_gameObject").invoke(), Rigidbody);
                              if (e && !e.isNull()) {
                                e.method("set_isKinematic").invoke(true);
                              }
                            } catch (e) {}
                          } catch (e) {}
                        }
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously freezes all mobs (toggle)."
            }), new ButtonInfo({
              buttonText: "Mob Gun Kill",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > mobGunDelay2) {
                  mobGunDelay2 = time + 0.4;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke();
                    for (const e of mobIDs) {
                      try {
                        const t = AssemblyCSharp.class("AnimalCompany." + e.name + "Controller");
                        const n = o.method("GetComponentInParent", 0).inflate(t).invoke();
                        if (!n || n.isNull()) {
                          continue;
                        }
                        try {
                          n.method("RPC_KillMob").invoke();
                        } catch (e) {
                          try {
                            n.method("KillMob").invoke();
                          } catch (e) {}
                        }
                        sendNotification("Killed " + e + "!", false);
                        return;
                      } catch (e) {}
                    }
                    sendNotification("No mob there", false);
                  } catch (e) {
                    console.error("Mob Gun Kill:", e);
                  }
                }
              },
              toolTip: "Point at a mob and pull trigger to kill it (hold grip)."
            }), new ButtonInfo({
              buttonText: "Mob Gun TP To Me",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > mobGunDelay2) {
                  mobGunDelay2 = time + 0.4;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke();
                    const n = NetPlayer.method("get_localPlayer").invoke();
                    if (!n) {
                      return;
                    }
                    const i = getTransform(n).method("get_position").invoke();
                    for (const e of mobIDs) {
                      try {
                        const t = AssemblyCSharp.class("AnimalCompany." + e.name + "Controller");
                        const n = o.method("GetComponentInParent", 0).inflate(t).invoke();
                        if (!n || n.isNull()) {
                          continue;
                        }
                        getTransform(n).method("set_position").invoke(i);
                        sendNotification("Teleported " + e + " to you!", false);
                        return;
                      } catch (e) {}
                    }
                  } catch (e) {
                    console.error("Mob Gun TP:", e);
                  }
                }
              },
              toolTip: "Point at a mob and pull trigger to teleport it to you (hold grip)."
            }), new ButtonInfo({
              buttonText: "Mob Gun Scale Big",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > mobGunDelay2) {
                  mobGunDelay2 = time + 0.4;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke();
                    for (const e of mobIDs) {
                      try {
                        const t = AssemblyCSharp.class("AnimalCompany." + e.name + "Controller");
                        const n = o.method("GetComponentInParent", 0).inflate(t).invoke();
                        if (!n || n.isNull()) {
                          continue;
                        }
                        getTransform(n).method("set_localScale").invoke([5, 5, 5]);
                        sendNotification("Scaled " + e + " big!", false);
                        return;
                      } catch (e) {}
                    }
                  } catch (e) {
                    console.error("Mob Scale Gun:", e);
                  }
                }
              },
              toolTip: "Point at a mob and pull trigger to make it giant (hold grip)."
            }), new ButtonInfo({
              buttonText: "Mob Gun VFX Explode",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > mobGunDelay2) {
                  mobGunDelay2 = time + 0.5;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke();
                    const n = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                    for (const e of mobIDs) {
                      try {
                        const t = AssemblyCSharp.class("AnimalCompany." + e.name + "Controller");
                        const i = o.method("GetComponentInParent", 0).inflate(t).invoke();
                        if (!i || i.isNull()) {
                          continue;
                        }
                        const a = getTransform(i).method("get_position").invoke();
                        const l = [VFXTypes.MeatExplosion_1, VFXTypes.MeatExplosion_2, VFXTypes.MeatExplosion_Headshot, VFXTypes.FuelExplosion, VFXTypes.ConfettiBurst, VFXTypes.Ethereal_Void];
                        for (const e of l) {
                          try {
                            ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(n, e, a, identityQuaternion);
                          } catch (e) {}
                        }
                        try {
                          i.method("RPC_KillMob").invoke();
                        } catch (e) {
                          try {
                            i.method("KillMob").invoke();
                          } catch (e) {}
                        }
                        sendNotification("VFX Exploded " + e + "!", false);
                        return;
                      } catch (e) {}
                    }
                  } catch (e) {
                    console.error("Mob VFX Explode:", e);
                  }
                }
              },
              toolTip: "Point at a mob, trigger to explode it with VFX and kill it (hold grip)."
            }), new ButtonInfo({
              buttonText: "Mob Bounce All",
              method: () => {
                try {
                  let e = 0;
                  for (const t of mobIDs) {
                    try {
                      const o = AssemblyCSharp.class("AnimalCompany." + t.name + "Controller");
                      const n = Object.method("FindObjectsByType", 1).inflate(o).invoke(0);
                      if (!n || n.isNull()) {
                        continue;
                      }
                      for (let t = 0; t < n.length; t++) {
                        try {
                          const o = n.get(t);
                          if (!o || o.isNull()) {
                            continue;
                          }
                          try {
                            const t = getComponent(o.method("get_gameObject").invoke(), Rigidbody);
                            if (t && !t.isNull()) {
                              const o = (Math.random() - 0.5) * 1500;
                              const n = (Math.random() - 0.5) * 1500;
                              t.method("AddForce", 3).invoke([o, 1200, n], 1);
                              e++;
                            }
                          } catch (e) {}
                        } catch (e) {}
                      }
                    } catch (e) {}
                  }
                  sendNotification("Bounced " + e + " mobs!", false);
                } catch (e) {
                  sendNotification("Mob Bounce: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Launches all mobs in random directions."
            }), new ButtonInfo({
              buttonText: "Mob Bounce Loop",
              isTogglable: true,
              method: () => {
                if (time > mobGunDelay) {
                  mobGunDelay = time + 0.8;
                  try {
                    for (const e of mobIDs) {
                      try {
                        const t = AssemblyCSharp.class("AnimalCompany." + e.name + "Controller");
                        const o = Object.method("FindObjectsByType", 1).inflate(t).invoke(0);
                        if (!o || o.isNull()) {
                          continue;
                        }
                        for (let e = 0; e < o.length; e++) {
                          try {
                            const t = o.get(e);
                            if (!t || t.isNull()) {
                              continue;
                            }
                            const n = getComponent(t.method("get_gameObject").invoke(), Rigidbody);
                            if (n && !n.isNull()) {
                              const e = (Math.random() - 0.5) * 2000;
                              const t = (Math.random() - 0.5) * 2000;
                              n.method("AddForce", 3).invoke([e, 1500, t], 1);
                            }
                          } catch (e) {}
                        }
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously bounces all mobs every 0.8s (toggle)."
            }), new ButtonInfo({
              buttonText: "Mob TP Loop",
              isTogglable: true,
              method: () => {
                if (time > mobGunDelay) {
                  mobGunDelay = time + 2;
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (!e || e.handle.isNull()) {
                      return;
                    }
                    const t = getTransform(e).method("get_position").invoke();
                    for (const e of mobIDs) {
                      try {
                        const o = AssemblyCSharp.class("AnimalCompany." + e.name + "Controller");
                        const n = Object.method("FindObjectsByType", 1).inflate(o).invoke(0);
                        if (!n || n.isNull()) {
                          continue;
                        }
                        for (let e = 0; e < n.length; e++) {
                          try {
                            const o = n.get(e);
                            if (!o || o.isNull()) {
                              continue;
                            }
                            getTransform(o).method("set_position").invoke(t);
                          } catch (e) {}
                        }
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously teleports all mobs to you every 2s (toggle)."
            }), new ButtonInfo({
              buttonText: "Mob Mass Scale Loop",
              isTogglable: true,
              method: () => {
                if (time > mobGunDelay) {
                  mobGunDelay = time + 0.3;
                  try {
                    const e = Math.abs(Math.sin(time)) * 8 + 0.5;
                    for (const t of mobIDs) {
                      try {
                        const o = AssemblyCSharp.class("AnimalCompany." + t.name + "Controller");
                        const n = Object.method("FindObjectsByType", 1).inflate(o).invoke(0);
                        if (!n || n.isNull()) {
                          continue;
                        }
                        for (let t = 0; t < n.length; t++) {
                          try {
                            const o = n.get(t);
                            if (!o || o.isNull()) {
                              continue;
                            }
                            getTransform(o).method("set_localScale").invoke([e, e, e]);
                          } catch (e) {}
                        }
                      } catch (e) {}
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously pulses mob scales (toggle)."
            })], [new ButtonInfo({
              buttonText: "Exit Extra WL",
              method: () => {
                currentCategory = 0;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns to main menu."
            }), new ButtonInfo({
              buttonText: "WL All Speed Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 3;
                  try {
                    const e = NetPlayer.field("playerIDToNetPlayer").value;
                    const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (t.method("MoveNext").invoke()) {
                      const e = t.method("get_Current").invoke();
                      if (e && !e.handle.isNull() && !playerIsLocal(e) && (!whitelistEnabled || whitelistHasPlayer(e))) {
                        try {
                          e.method("RPC_ApplyBuff").invoke(1);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously re-applies SpeedBoost to all WL players every 3s (toggle)."
            }), new ButtonInfo({
              buttonText: "WL All AntiGrav Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 3;
                  try {
                    const e = NetPlayer.field("playerIDToNetPlayer").value;
                    const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (t.method("MoveNext").invoke()) {
                      const e = t.method("get_Current").invoke();
                      if (e && !e.handle.isNull() && !playerIsLocal(e) && (!whitelistEnabled || whitelistHasPlayer(e))) {
                        try {
                          e.method("RPC_ApplyBuff").invoke(7);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously re-applies AntiGravity to all WL players every 3s (toggle)."
            }), new ButtonInfo({
              buttonText: "WL Invincible Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const e = NetPlayer.field("playerIDToNetPlayer").value;
                    const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (t.method("MoveNext").invoke()) {
                      const e = t.method("get_Current").invoke();
                      if (e && !e.handle.isNull() && !playerIsLocal(e) && (!whitelistEnabled || whitelistHasPlayer(e))) {
                        try {
                          e.method("set_isInvincible").invoke(true);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Keeps all WL players invincible (toggle)."
            }), new ButtonInfo({
              buttonText: "WL Revive Loop",
              isTogglable: true,
              method: () => {
                if (time > lagGunDelay) {
                  lagGunDelay = time + 1;
                  try {
                    const e = NetPlayer.field("playerIDToNetPlayer").value;
                    const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (t.method("MoveNext").invoke()) {
                      const e = t.method("get_Current").invoke();
                      if (e && !e.handle.isNull() && !playerIsLocal(e) && (!whitelistEnabled || whitelistHasPlayer(e))) {
                        try {
                          if (e.method("get_isDie").invoke()) {
                            e.method("RPC_DoPlayerDie").invoke(false);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Auto-revives WL players when they die (toggle)."
            }), new ButtonInfo({
              buttonText: "WL Rainbow Loop",
              isTogglable: true,
              method: () => {
                if (frameCount % 10 == 0) {
                  try {
                    const e = time * 0.5 % 1;
                    const t = NetPlayer.field("playerIDToNetPlayer").value;
                    const o = t.method("get_Values").invoke().method("GetEnumerator").invoke();
                    while (o.method("MoveNext").invoke()) {
                      const t = o.method("get_Current").invoke();
                      if (t && !t.handle.isNull() && !playerIsLocal(t) && (!whitelistEnabled || whitelistHasPlayer(t))) {
                        try {
                          t.method("RPC_SetColorHSV").invoke(99999, e, 1, 1);
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously rainbow-cycles WL players (toggle)."
            }), new ButtonInfo({
              buttonText: "WL Bounce Gun Loop",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player || t.blocked) {
                      return;
                    }
                    const o = (Math.random() - 0.5) * 2000;
                    const n = (Math.random() - 0.5) * 2000;
                    t.player.method("RPC_AddForce").invoke([o, 1300, n]);
                    sendNotification("Bounced " + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Bounce Gun:", e);
                  }
                }
              },
              toolTip: "Continuously bounces aimed WL player (hold grip)."
            }), new ButtonInfo({
              buttonText: "WL Kill Loop Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.8;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player || t.blocked) {
                      return;
                    }
                    t.player.method("RPC_DoPlayerDie").invoke(true);
                  } catch (e) {
                    console.error("WL Kill Loop:", e);
                  }
                }
              },
              toolTip: "Continuously kills aimed WL player (hold grip). Toggle on to keep killing."
            }), new ButtonInfo({
              buttonText: "WL VFX Spam Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (time > lagGunDelay) {
                  lagGunDelay = time + 0.2;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player || t.blocked) {
                      return;
                    }
                    const o = getTransform(t.player).method("get_position").invoke();
                    const n = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                    const i = [VFXTypes.ConfettiBurst, VFXTypes.GreenBlink, VFXTypes.Ethereal_Void, VFXTypes.Explosion_Coins, VFXTypes.MeatExplosion_1, VFXTypes.MidAirJump_Fart];
                    const a = i[Math.floor(Math.random() * i.length)];
                    try {
                      ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(n, a, o, identityQuaternion);
                    } catch (e) {}
                  } catch (e) {
                    console.error("WL VFX Gun:", e);
                  }
                }
              },
              toolTip: "Spams random VFX on aimed WL player every 0.2s (hold grip)."
            }), new ButtonInfo({
              buttonText: "WL Invincible Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    const o = t.player.method("get_isInvincible").invoke();
                    t.player.method("set_isInvincible").invoke(!o);
                    sendNotification((o ? "Removed invincible from " : "Gave invincible to ") + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Invincible Gun:", e);
                  }
                }
              },
              toolTip: "Toggles invincibility on aimed WL player (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Money Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    t.player.method("RPC_AddPlayerMoney").invoke(9999);
                    sendNotification("Gave money to " + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Money Gun:", e);
                  }
                }
              },
              toolTip: "Gives 9999 money to aimed WL player (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Heal Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    try {
                      t.player.method("set_healthLost").invoke(0);
                    } catch (e) {}
                    try {
                      t.player.method("set_isDie").invoke(false);
                    } catch (e) {}
                    sendNotification("Healed " + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Heal Gun:", e);
                  }
                }
              },
              toolTip: "Fully heals aimed WL player (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Wanted Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun();
                if (rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = getWhitelistGunTarget(e, true, 10);
                    if (!t || !t.player) {
                      sendNotification("No player there", false);
                      return;
                    }
                    if (t.blocked) {
                      sendNotification("Not whitelisted: " + t.id, false);
                      return;
                    }
                    const o = t.player.method("get_isWanted").invoke();
                    t.player.method("set_isWanted").invoke(!o);
                    sendNotification((o ? "Cleared wanted " : "Set wanted ") + t.id + "!", false);
                  } catch (e) {
                    console.error("WL Wanted Gun:", e);
                  }
                }
              },
              toolTip: "Toggles wanted status on aimed WL player (hold grip + trigger)."
            }), new ButtonInfo({
              buttonText: "WL Give All Buff",
              method: () => {
                try {
                  const e = [1, 2, 3, 5, 7];
                  const t = NetPlayer.field("playerIDToNetPlayer").value;
                  const o = t.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let n = 0;
                  while (o.method("MoveNext").invoke()) {
                    const t = o.method("get_Current").invoke();
                    if (t && !t.handle.isNull() && !playerIsLocal(t) && (!whitelistEnabled || whitelistHasPlayer(t))) {
                      for (const o of e) {
                        try {
                          t.method("RPC_ApplyBuff").invoke(o);
                        } catch (e) {}
                      }
                      n++;
                    }
                  }
                  sendNotification("Gave all buffs to " + n + " WL players!", false);
                } catch (e) {
                  sendNotification("WL Give All Buff: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Gives all buffs (Speed, Pink, Damage, Scale, AntiGrav) to all WL players."
            }), new ButtonInfo({
              buttonText: "WL Add Money All",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e) && (!whitelistEnabled || whitelistHasPlayer(e))) {
                      try {
                        e.method("RPC_AddPlayerMoney").invoke(9999);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Gave money to " + o + " WL players!", false);
                } catch (e) {
                  sendNotification("WL Money All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Gives 9999 money to all WL players."
            }), new ButtonInfo({
              buttonText: "WL Set All Teams",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  let n = 1;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e) && (!whitelistEnabled || whitelistHasPlayer(e))) {
                      try {
                        e.method("RPC_SetTeam").invoke(n);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Set team for " + o + " WL players!", false);
                } catch (e) {
                  sendNotification("WL Set Teams: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all WL players to red team."
            }), new ButtonInfo({
              buttonText: "WL Clear Invincible",
              method: () => {
                try {
                  const e = NetPlayer.field("playerIDToNetPlayer").value;
                  const t = e.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let o = 0;
                  while (t.method("MoveNext").invoke()) {
                    const e = t.method("get_Current").invoke();
                    if (e && !e.handle.isNull() && !playerIsLocal(e) && (!whitelistEnabled || whitelistHasPlayer(e))) {
                      try {
                        e.method("set_isInvincible").invoke(false);
                        o++;
                      } catch (e) {}
                    }
                  }
                  sendNotification("Removed invincible from " + o + " WL players!", false);
                } catch (e) {
                  sendNotification("WL Clear Invincible: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Removes invincibility from all WL players."
            }), new ButtonInfo({
              buttonText: "WL Spawn Bag Bomb All",
              method: () => {
                try {
                  let e = function (e, t) {
                    return Math.floor(Math.random() * (t - e + 1)) + e;
                  };
                  const t = ["item_backpack_large_clover", "item_backpack_large_basketball", "item_backpack_large_base"];
                  const o = NetPlayer.field("playerIDToNetPlayer").value;
                  const n = o.method("get_Values").invoke().method("GetEnumerator").invoke();
                  let i = 0;
                  while (n.method("MoveNext").invoke()) {
                    const o = n.method("get_Current").invoke();
                    if (!o || o.handle.isNull() || playerIsLocal(o)) {
                      continue;
                    }
                    if (whitelistEnabled && !whitelistHasPlayer(o)) {
                      continue;
                    }
                    const a = getTransform(o).method("get_position").invoke();
                    try {
                      const o = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + t[e(0, t.length - 1)]), a, identityQuaternion, NULL);
                      if (o && !o.isNull()) {
                        const t = o.method("GetComponent", 1).inflate(BackpackItemClass).invoke();
                        for (let o = 0; o < 10; o++) {
                          try {
                            const o = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + itemIDs[e(0, itemIDs.length - 1)]), a, identityQuaternion, NULL);
                            if (!o || o.isNull()) {
                              continue;
                            }
                            if (t && !t.isNull()) {
                              const e = o.method("GetComponent", 1).inflate(GBIClass).invoke();
                              if (e && !e.isNull()) {
                                try {
                                  e.method("AddToBag").invoke(t);
                                } catch (e) {}
                              }
                            }
                          } catch (e) {}
                        }
                        try {
                          if (t && !t.isNull()) {
                            t.method("Explode").invoke();
                          }
                        } catch (e) {}
                      }
                      i++;
                    } catch (e) {}
                  }
                  sendNotification("Bag bombed " + i + " WL players!", false);
                } catch (e) {
                  sendNotification("WL Bag Bomb All: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Spawns an exploding bag near each WL player."
            })], [new ButtonInfo({
              buttonText: "Exit UX",
              method: () => {
                currentCategory = 11;
                currentPage = 0;
              },
              isTogglable: false,
              toolTip: "Returns to main menu."
            }), new ButtonInfo({
              buttonText: "UX: â–ş Page 1",
              method: () => {
                currentCategory = 17;
                currentPage = 0;
                reloadMenu();
                sendNotification("UX Page 1", false, 2);
              },
              isTogglable: false,
              toolTip: "Jump to Unity Explorer page 1 (Selection, Transform basics)."
            }), new ButtonInfo({
              buttonText: "UX: â–ş Page 2",
              method: () => {
                currentCategory = 17;
                currentPage = 1;
                reloadMenu();
                sendNotification("UX Page 2", false, 2);
              },
              isTogglable: false,
              toolTip: "Jump to Unity Explorer page 2 (Info & Logging)."
            }), new ButtonInfo({
              buttonText: "UX: â–ş Page 3",
              method: () => {
                currentCategory = 17;
                currentPage = 2;
                reloadMenu();
                sendNotification("UX Page 3", false, 2);
              },
              isTogglable: false,
              toolTip: "Jump to Unity Explorer page 3 (Mass & Physics)."
            }), new ButtonInfo({
              buttonText: "UX: â–ş Page 4",
              method: () => {
                currentCategory = 17;
                currentPage = 3;
                reloadMenu();
                sendNotification("UX Page 4", false, 2);
              },
              isTogglable: false,
              toolTip: "Jump to Unity Explorer page 4 (Rendering & Color)."
            }), new ButtonInfo({
              buttonText: "UX: â–ş Page 5",
              method: () => {
                currentCategory = 17;
                currentPage = 4;
                reloadMenu();
                sendNotification("UX Page 5", false, 2);
              },
              isTogglable: false,
              toolTip: "Jump to Unity Explorer page 5 (Parenting & Colliders)."
            }), new ButtonInfo({
              buttonText: "UX: â–ş Page 6",
              method: () => {
                currentCategory = 17;
                currentPage = 5;
                reloadMenu();
                sendNotification("UX Page 6", false, 2);
              },
              isTogglable: false,
              toolTip: "Jump to Unity Explorer page 6 (Grabbable / Interaction)."
            }), new ButtonInfo({
              buttonText: "UX: â–ş Page 7",
              method: () => {
                currentCategory = 17;
                currentPage = 6;
                reloadMenu();
                sendNotification("UX Page 7", false, 2);
              },
              isTogglable: false,
              toolTip: "Jump to Unity Explorer page 7 (Item Flags & Data)."
            }), new ButtonInfo({
              buttonText: "UX: â–ş Page 8",
              method: () => {
                currentCategory = 17;
                currentPage = 7;
                reloadMenu();
                sendNotification("UX Page 8", false, 2);
              },
              isTogglable: false,
              toolTip: "Jump to Unity Explorer page 8 (Edible / Spawn / Scale)."
            }), new ButtonInfo({
              buttonText: "UX: â–ş Page 9",
              method: () => {
                currentCategory = 17;
                currentPage = 8;
                reloadMenu();
                sendNotification("UX Page 9", false, 2);
              },
              isTogglable: false,
              toolTip: "Jump to Unity Explorer page 9 (Player Effects Gun)."
            }), new ButtonInfo({
              buttonText: "UX: â–ş Page 10",
              method: () => {
                currentCategory = 17;
                currentPage = 9;
                reloadMenu();
                sendNotification("UX Page 10", false, 2);
              },
              isTogglable: false,
              toolTip: "Jump to Unity Explorer page 10 (Instantiate & Mob Spawn)."
            }), new ButtonInfo({
              buttonText: "UX: â–ş Last Page",
              method: () => {
                const e = Math.ceil(buttons[17].length / 8) - 1;
                currentCategory = 17;
                currentPage = e;
                reloadMenu();
                sendNotification("UX Last Page (" + (e + 1) + ")", false, 2);
              },
              isTogglable: false,
              toolTip: "Jump directly to the last page of the Unity Explorer."
            }), new ButtonInfo({
              buttonText: "Select Obj Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      sendNotification("Nothing hit", false);
                      return;
                    }
                    const o = t.method("get_gameObject").invoke();
                    if (!o || o.isNull()) {
                      return;
                    }
                    uxSelectedObject = o;
                    sendNotification("UX Selected: " + (uxSelectedName = o.method("get_name").invoke()?.content ?? "unnamed"), false, 4);
                  } catch (e) {
                    console.error("[UX] Select:", e);
                  }
                }
              },
              toolTip: "Hold grip + pull trigger to select any object. Selected object is used by all other UX tools."
            }), new ButtonInfo({
              buttonText: "Inspect Object",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e ? uxVec3Str(e.method("get_position").invoke()) : "?";
                    const o = e ? uxVec3Str(e.method("get_eulerAngles").invoke()) : "?";
                    const n = e ? uxVec3Str(e.method("get_localScale").invoke()) : "?";
                    const i = uxSelectedObject.method("get_activeSelf").invoke();
                    const a = uxSelectedObject.method("get_layer").invoke();
                    const l = uxSelectedObject.method("get_tag").invoke()?.content ?? "?";
                    const r = uxGetComponentNames(uxSelectedObject);
                    console.log("[UX] â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•");
                    console.log("[UX] Name   : " + uxSelectedName);
                    console.log("[UX] Active : " + i);
                    console.log("[UX] Layer  : " + a + "  Tag: " + l);
                    console.log("[UX] Pos    : " + t);
                    console.log("[UX] Rot    : " + o);
                    console.log("[UX] Scale  : " + n);
                    console.log("[UX] Components (" + r.length + "): " + r.join(", "));
                    console.log("[UX] â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•");
                    sendNotification(uxSelectedName + " | " + t + " | " + r.length + " comps â€” see log", false, 6);
                  } catch (e) {
                    sendNotification("Inspect: " + e, false);
                    console.error("[UX] Inspect:", e);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Logs full info (position, rotation, scale, components) of selected object to Frida console."
            }), new ButtonInfo({
              buttonText: "Teleport to Obj",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (!e || e.handle.isNull()) {
                      return;
                    }
                    const t = uxGetTransform(uxSelectedObject);
                    if (!t) {
                      return;
                    }
                    const o = t.method("get_position").invoke();
                    e.method("RPC_Teleport").invoke(o);
                    sendNotification("Teleported to " + uxSelectedName, false);
                  } catch (e) {
                    sendNotification("TP to obj: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Teleports you to the selected object's position."
            }), new ButtonInfo({
              buttonText: "Obj to Me",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (!e || e.handle.isNull()) {
                      return;
                    }
                    const t = getTransform(e).method("get_position").invoke();
                    uxGetTransform(uxSelectedObject).method("set_position").invoke(t);
                    sendNotification("Moved " + uxSelectedName + " to you", false);
                  } catch (e) {
                    sendNotification("Obj to me: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Moves the selected object to your position."
            }), new ButtonInfo({
              buttonText: "Freeze Obj",
              isTogglable: true,
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponent", 1).inflate(e).invoke();
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody on " + uxSelectedName, false);
                      return;
                    }
                    const o = t.method("get_isKinematic").invoke();
                    t.method("set_isKinematic").invoke(!o);
                    sendNotification("Freeze " + uxSelectedName + ": " + !o, false);
                  } catch (e) {
                    sendNotification("Freeze: " + e, false);
                  }
                }
              },
              toolTip: "Toggles isKinematic on the selected object's Rigidbody (freezes/unfreezes physics)."
            }), new ButtonInfo({
              buttonText: "Toggle Active",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("get_activeSelf").invoke();
                    uxSelectedObject.method("SetActive").invoke(!e);
                    sendNotification(uxSelectedName + " active: " + !e, false);
                  } catch (e) {
                    sendNotification("Toggle Active: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Toggles SetActive on the selected object (hides/shows it)."
            }), new ButtonInfo({
              buttonText: "Despawn Obj",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    let e = false;
                    try {
                      const t = uxSelectedObject.method("GetComponentInParent", 0).inflate(GBOClass).invoke();
                      if (t && !t.isNull()) {
                        const o = t.method("get_Object").invoke();
                        if (o && !o.isNull()) {
                          let t = null;
                          try {
                            t = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                          } catch (e) {}
                          if (t && !t.isNull()) {
                            t.method("Despawn").invoke(o);
                            e = true;
                          }
                        }
                      }
                    } catch (e) {}
                    if (!e) {
                      try {
                        const t = Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkObject");
                        const o = uxSelectedObject.method("GetComponentInParent", 0).inflate(t).invoke();
                        if (o && !o.isNull()) {
                          let t = null;
                          try {
                            t = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                          } catch (e) {}
                          if (t && !t.isNull()) {
                            t.method("Despawn").invoke(o);
                            e = true;
                          }
                        }
                      } catch (e) {}
                    }
                    if (e) {
                      sendNotification("Despawned " + uxSelectedName, false);
                      uxSelectedObject = null;
                      uxSelectedName = "none";
                    } else {
                      Destroy(uxSelectedObject);
                      sendNotification("Destroyed (local) " + uxSelectedName, false);
                      uxSelectedObject = null;
                      uxSelectedName = "none";
                    }
                  } catch (e) {
                    sendNotification("Despawn: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Despawns (network removes) the selected object, or destroys it locally if no network object."
            }), new ButtonInfo({
              buttonText: "Scale Obj +25%",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_localScale").invoke();
                    t.field("x").value;
                    t.field("y").value;
                    t.field("z").value;
                    const o = Vector3.method("op_Multiply").invoke(t, 1.25);
                    e.method("set_localScale").invoke(o);
                    sendNotification(uxSelectedName + " scaled up", false);
                  } catch (e) {
                    sendNotification("Scale+: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Scales the selected object up by 25%."
            }), new ButtonInfo({
              buttonText: "Scale Obj -25%",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_localScale").invoke();
                    const o = Vector3.method("op_Multiply").invoke(t, 0.75);
                    e.method("set_localScale").invoke(o);
                    sendNotification(uxSelectedName + " scaled down", false);
                  } catch (e) {
                    sendNotification("Scale-: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Scales the selected object down by 25%."
            }), new ButtonInfo({
              buttonText: "Reset Obj Scale",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = Vector3.method("get_one").invoke();
                    e.method("set_localScale").invoke(t);
                    sendNotification(uxSelectedName + " scale reset", false);
                  } catch (e) {
                    sendNotification("Scale reset: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Resets selected object scale to (1,1,1)."
            }), new ButtonInfo({
              buttonText: "Rotate Obj 90Y",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_eulerAngles").invoke();
                    const o = (t.field("y").value + 90) % 360;
                    const n = t.field("x").value;
                    const i = t.field("z").value;
                    e.method("set_eulerAngles").invoke([n, o, i]);
                    sendNotification(uxSelectedName + " rotated 90Â° Y", false);
                  } catch (e) {
                    sendNotification("Rotate: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Rotates selected object 90 degrees on Y axis."
            }), new ButtonInfo({
              buttonText: "Lock Obj Pos",
              isTogglable: true,
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponent", 1).inflate(e).invoke();
                    if (t && !t.isNull()) {
                      t.method("set_isKinematic").invoke(true);
                      t.method("set_useGravity").invoke(false);
                    }
                    uxGetTransform(uxSelectedObject).method("SetParent", 2).invoke(NULL, true);
                    sendNotification(uxSelectedName + " position locked", false);
                  } catch (e) {
                    sendNotification("Lock pos: " + e, false);
                  }
                }
              },
              toolTip: "Locks selected object's position (kinematic, no gravity, detaches from parent)."
            }), new ButtonInfo({
              buttonText: "Print Fields",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Component");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(e).invoke(false);
                    const o = t ? t.length : 0;
                    console.log("[UX-Fields] Object: " + uxSelectedName + " â”€â”€ " + o + " components");
                    for (let e = 0; e < Math.min(o, 20); e++) {
                      try {
                        const o = t.method("get_Item").invoke(e);
                        const n = o.method("GetType").invoke().method("get_Name").invoke()?.content ?? "?";
                        console.log("[UX-Fields]  â”Ś " + n);
                        try {
                          const e = o.class;
                          if (e) {
                            for (const t of e.fields) {
                              try {
                                const e = t.withHolder(o).value;
                                console.log("[UX-Fields]  â”‚  " + t.name + " = " + e);
                              } catch (e) {
                                console.log("[UX-Fields]  â”‚  " + t.name + " = <unreadable>");
                              }
                            }
                          }
                        } catch (e) {}
                        console.log("[UX-Fields]  â””â”€â”€â”€â”€â”€");
                      } catch (e) {}
                    }
                    sendNotification("Fields dumped to log (" + Math.min(o, 20) + " comps)", false, 4);
                  } catch (e) {
                    sendNotification("Print Fields: " + e, false);
                    console.error("[UX] Fields:", e);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Dumps all Il2Cpp fields of every component on selected object to Frida log."
            }), new ButtonInfo({
              buttonText: "Print Methods",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Component");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(e).invoke(false);
                    const o = t ? t.length : 0;
                    console.log("[UX-Methods] Object: " + uxSelectedName);
                    for (let e = 0; e < Math.min(o, 10); e++) {
                      try {
                        const o = t.method("get_Item").invoke(e);
                        const n = o.method("GetType").invoke().method("get_Name").invoke()?.content ?? "?";
                        console.log("[UX-Methods]  â”Ś " + n);
                        try {
                          const e = o.class;
                          if (e) {
                            for (const t of e.methods) {
                              console.log("[UX-Methods]  â”‚  " + t.name);
                            }
                          }
                        } catch (e) {}
                        console.log("[UX-Methods]  â””â”€â”€â”€â”€â”€");
                      } catch (e) {}
                    }
                    sendNotification("Methods dumped to log", false, 4);
                  } catch (e) {
                    sendNotification("Print Methods: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Dumps all method names on every component to Frida log."
            }), new ButtonInfo({
              buttonText: "Print Children",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    let e = function (t, o) {
                      const n = t.method("get_childCount").invoke();
                      for (let i = 0; i < Math.min(n, 64); i++) {
                        try {
                          const n = t.method("GetChild").invoke(i);
                          const a = n.method("get_gameObject").invoke().method("get_name").invoke()?.content ?? "?";
                          console.log("[UX-Children]" + "  ".repeat(o) + "â”ś " + a);
                          if (o < 4) {
                            e(n, o + 1);
                          }
                        } catch (e) {}
                      }
                    };
                    const t = uxGetTransform(uxSelectedObject);
                    if (!t) {
                      return;
                    }
                    console.log("[UX-Children] Root: " + uxSelectedName);
                    e(t, 1);
                    sendNotification("Children dumped to log", false, 4);
                  } catch (e) {
                    sendNotification("Print Children: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Dumps the full child hierarchy of the selected object to Frida log (4 levels deep)."
            }), new ButtonInfo({
              buttonText: "UX: Select Parent",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    if (!e) {
                      return;
                    }
                    const t = e.method("get_parent").invoke();
                    if (!t || t.isNull()) {
                      sendNotification("No parent", false);
                      return;
                    }
                    uxSelectedObject = t.method("get_gameObject").invoke();
                    sendNotification("UX â†’ Parent: " + (uxSelectedName = uxSelectedObject.method("get_name").invoke()?.content ?? "parent"), false, 3);
                  } catch (e) {
                    sendNotification("Select Parent: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Re-selects the parent GameObject of the current selection."
            }), new ButtonInfo({
              buttonText: "UX: Select Child[0]",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    if (!e) {
                      return;
                    }
                    if (e.method("get_childCount").invoke() === 0) {
                      sendNotification("No children", false);
                      return;
                    }
                    const t = e.method("GetChild").invoke(0);
                    uxSelectedObject = t.method("get_gameObject").invoke();
                    sendNotification("UX â†’ Child[0]: " + (uxSelectedName = uxSelectedObject.method("get_name").invoke()?.content ?? "child"), false, 3);
                  } catch (e) {
                    sendNotification("Select Child: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Navigates into the first child of the current selection."
            }), new ButtonInfo({
              buttonText: "UX: Show Selected",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e ? uxVec3Str(e.method("get_position").invoke()) : "?";
                    const o = uxGetComponentNames(uxSelectedObject);
                    sendNotification("SEL: " + uxSelectedName + "\n" + t + "\n" + o.slice(0, 4).join(", "), false, 6);
                  } catch (e) {
                    sendNotification("Show: " + e, false);
                  }
                } else {
                  sendNotification("UX: nothing selected", false, 4);
                }
              },
              isTogglable: false,
              toolTip: "Re-displays the current selection name and position as a notification."
            }), new ButtonInfo({
              buttonText: "UX: Get Full Path",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = [];
                    let t = uxGetTransform(uxSelectedObject);
                    let o = 0;
                    while (t && !t.isNull() && o < 64) {
                      o++;
                      try {
                        const o = t.method("get_gameObject").invoke();
                        const n = o && !o.isNull() ? o.method("get_name").invoke()?.content ?? "?" : "?";
                        e.unshift(n);
                      } catch (t) {
                        e.unshift("?");
                      }
                      try {
                        const e = t.method("get_parent").invoke();
                        t = e && !e.isNull() ? e : null;
                      } catch (e) {
                        t = null;
                      }
                    }
                    const n = "/" + e.join("/");
                    console.log("[UX] Full path (" + e.length + " levels): " + n);
                    sendNotification(n, false, 9);
                  } catch (e) {
                    sendNotification("Path: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Walks the full transform hierarchy to root and displays the complete scene path."
            }), new ButtonInfo({
              buttonText: "UX: Move +X (1m)",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_position").invoke();
                    t.field("x").value = t.field("x").value + 1;
                    e.method("set_position").invoke(t);
                    sendNotification(uxSelectedName + " +X", false, 2);
                  } catch (e) {
                    sendNotification("Move +X: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Nudges selected object +1 unit on the X axis."
            }), new ButtonInfo({
              buttonText: "UX: Move -X (1m)",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_position").invoke();
                    t.field("x").value = t.field("x").value - 1;
                    e.method("set_position").invoke(t);
                    sendNotification(uxSelectedName + " -X", false, 2);
                  } catch (e) {
                    sendNotification("Move -X: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Nudges selected object -1 unit on the X axis."
            }), new ButtonInfo({
              buttonText: "UX: Move +Y (1m)",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_position").invoke();
                    t.field("y").value = t.field("y").value + 1;
                    e.method("set_position").invoke(t);
                    sendNotification(uxSelectedName + " +Y", false, 2);
                  } catch (e) {
                    sendNotification("Move +Y: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Nudges selected object +1 unit up (Y axis)."
            }), new ButtonInfo({
              buttonText: "UX: Move -Y (1m)",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_position").invoke();
                    t.field("y").value = t.field("y").value - 1;
                    e.method("set_position").invoke(t);
                    sendNotification(uxSelectedName + " -Y", false, 2);
                  } catch (e) {
                    sendNotification("Move -Y: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Nudges selected object -1 unit down (Y axis)."
            }), new ButtonInfo({
              buttonText: "UX: Move +Z (1m)",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_position").invoke();
                    t.field("z").value = t.field("z").value + 1;
                    e.method("set_position").invoke(t);
                    sendNotification(uxSelectedName + " +Z", false, 2);
                  } catch (e) {
                    sendNotification("Move +Z: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Nudges selected object +1 unit on the Z axis."
            }), new ButtonInfo({
              buttonText: "UX: Move -Z (1m)",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_position").invoke();
                    t.field("z").value = t.field("z").value - 1;
                    e.method("set_position").invoke(t);
                    sendNotification(uxSelectedName + " -Z", false, 2);
                  } catch (e) {
                    sendNotification("Move -Z: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Nudges selected object -1 unit on the Z axis."
            }), new ButtonInfo({
              buttonText: "UX: Snap to Ground",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_position").invoke();
                    const o = Vector3.method("get_down").invoke();
                    const n = Physics.method("RaycastAll", 3).invoke(t, o, 100);
                    if (!n || n.isNull()) {
                      sendNotification("No ground found", false);
                      return;
                    }
                    const i = n.length;
                    let a = t.field("y").value;
                    let l = false;
                    for (let e = 0; e < i; e++) {
                      try {
                        const t = n.method("get_Item").invoke(e);
                        if (t.field("collider").value.method("get_gameObject").invoke().method("GetInstanceID").invoke() === uxSelectedObject.method("GetInstanceID").invoke()) {
                          continue;
                        }
                        const o = t.field("point").value.field("y").value;
                        if (!l || o > a) {
                          a = o;
                          l = true;
                        }
                      } catch (e) {}
                    }
                    if (l) {
                      t.field("y").value = a;
                      e.method("set_position").invoke(t);
                      sendNotification(uxSelectedName + " snapped to ground", false);
                    } else {
                      sendNotification("No ground below", false);
                    }
                  } catch (e) {
                    sendNotification("Snap: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Raycasts downward and places selected object on the nearest surface below it."
            }), new ButtonInfo({
              buttonText: "UX: Save Pos",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    globalThis._uxSavedPos = e.method("get_position").invoke();
                    globalThis._uxSavedRot = e.method("get_rotation").invoke();
                    const t = uxVec3Str(globalThis._uxSavedPos);
                    console.log("[UX] Saved pos: " + t + " for " + uxSelectedName);
                    sendNotification("Pos saved: " + t, false, 4);
                  } catch (e) {
                    sendNotification("Save Pos: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Saves the selected object's current position and rotation into a slot."
            }), new ButtonInfo({
              buttonText: "UX: Restore Pos",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  if (globalThis._uxSavedPos) {
                    try {
                      const e = uxGetTransform(uxSelectedObject);
                      e.method("set_position").invoke(globalThis._uxSavedPos);
                      e.method("set_rotation").invoke(globalThis._uxSavedRot);
                      sendNotification("Pos restored on " + uxSelectedName, false);
                    } catch (e) {
                      sendNotification("Restore Pos: " + e, false);
                    }
                  } else {
                    sendNotification("No pos saved", false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Moves the selected object back to the last saved position/rotation."
            }), new ButtonInfo({
              buttonText: "UX: Align to Player",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (!e || e.handle.isNull()) {
                      return;
                    }
                    const t = getTransform(e).method("get_position").invoke();
                    const o = uxGetTransform(uxSelectedObject);
                    const n = getTransform(e).method("get_forward").invoke();
                    const i = t.field("x").value + n.field("x").value * 2;
                    const a = t.field("y").value;
                    const l = t.field("z").value + n.field("z").value * 2;
                    t.field("x").value = i;
                    t.field("y").value = a;
                    t.field("z").value = l;
                    o.method("set_position").invoke(t);
                    sendNotification(uxSelectedName + " aligned to player", false);
                  } catch (e) {
                    sendNotification("Align: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Moves selected object 2m in front of you at your height."
            }), new ButtonInfo({
              buttonText: "UX: Mirror X",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_position").invoke();
                    t.field("x").value = -t.field("x").value;
                    e.method("set_position").invoke(t);
                    sendNotification(uxSelectedName + " mirrored X", false);
                  } catch (e) {
                    sendNotification("Mirror X: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Mirrors the selected object's X position across X=0."
            }), new ButtonInfo({
              buttonText: "UX: Mirror Z",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_position").invoke();
                    t.field("z").value = -t.field("z").value;
                    e.method("set_position").invoke(t);
                    sendNotification(uxSelectedName + " mirrored Z", false);
                  } catch (e) {
                    sendNotification("Mirror Z: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Mirrors the selected object's Z position across Z=0."
            }), new ButtonInfo({
              buttonText: "UX: Rotate +X 90",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_eulerAngles").invoke();
                    t.field("x").value = (t.field("x").value + 90) % 360;
                    e.method("set_eulerAngles").invoke(t);
                    sendNotification(uxSelectedName + " +X 90Â°", false, 2);
                  } catch (e) {
                    sendNotification("Rot +X: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Rotates selected object 90Â° on X axis."
            }), new ButtonInfo({
              buttonText: "UX: Rotate +Z 90",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_eulerAngles").invoke();
                    t.field("z").value = (t.field("z").value + 90) % 360;
                    e.method("set_eulerAngles").invoke(t);
                    sendNotification(uxSelectedName + " +Z 90Â°", false, 2);
                  } catch (e) {
                    sendNotification("Rot +Z: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Rotates selected object 90Â° on Z axis."
            }), new ButtonInfo({
              buttonText: "UX: Reset Rotation",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = Quaternion.method("get_identity").invoke();
                    e.method("set_rotation").invoke(t);
                    sendNotification(uxSelectedName + " rotation reset", false);
                  } catch (e) {
                    sendNotification("Reset Rot: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Resets selected object rotation to identity (0,0,0)."
            }), new ButtonInfo({
              buttonText: "UX: Face Player",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (!e || e.handle.isNull()) {
                      return;
                    }
                    const t = uxGetTransform(uxSelectedObject);
                    t.method("get_position").invoke();
                    const o = getTransform(e).method("get_position").invoke();
                    t.method("LookAt", 1).invoke(o);
                    sendNotification(uxSelectedName + " now faces you", false);
                  } catch (e) {
                    sendNotification("Face Player: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Rotates selected object to face your position."
            }), new ButtonInfo({
              buttonText: "UX: Launch Up",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponent", 1).inflate(e).invoke();
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody on " + uxSelectedName, false);
                      return;
                    }
                    t.method("set_isKinematic").invoke(false);
                    t.method("set_useGravity").invoke(true);
                    const o = Vector3.method("op_Multiply").invoke(Vector3.method("get_up").invoke(), 1200);
                    t.method("AddForce", 1).invoke(o);
                    sendNotification(uxSelectedName + " launched!", false);
                  } catch (e) {
                    sendNotification("Launch: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Adds a large upward force to the selected object's Rigidbody."
            }), new ButtonInfo({
              buttonText: "UX: Zero Velocity",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponent", 1).inflate(e).invoke();
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    const o = Vector3.method("get_zero").invoke();
                    t.method("set_velocity").invoke(o);
                    t.method("set_angularVelocity").invoke(o);
                    sendNotification(uxSelectedName + " velocity zeroed", false);
                  } catch (e) {
                    sendNotification("Zero Vel: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets velocity and angularVelocity to zero on the selected object's Rigidbody."
            }), new ButtonInfo({
              buttonText: "UX: Enable Gravity",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponent", 1).inflate(e).invoke();
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_useGravity").invoke(true);
                    t.method("set_isKinematic").invoke(false);
                    sendNotification(uxSelectedName + " gravity ON", false);
                  } catch (e) {
                    sendNotification("Gravity ON: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Enables gravity and disables kinematic on selected object."
            }), new ButtonInfo({
              buttonText: "UX: Disable Gravity",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponent", 1).inflate(e).invoke();
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_useGravity").invoke(false);
                    sendNotification(uxSelectedName + " gravity OFF", false);
                  } catch (e) {
                    sendNotification("Gravity OFF: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Disables gravity on selected object's Rigidbody."
            }), new ButtonInfo({
              buttonText: "UX: Toggle Renderer",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(e).invoke(false);
                    if (!t || t.isNull()) {
                      sendNotification("No renderers", false);
                      return;
                    }
                    const o = t.length;
                    let n = false;
                    for (let e = 0; e < o; e++) {
                      try {
                        if (t.method("get_Item").invoke(e).method("get_enabled").invoke()) {
                          n = true;
                          break;
                        }
                      } catch (e) {}
                    }
                    for (let e = 0; e < o; e++) {
                      try {
                        t.method("get_Item").invoke(e).method("set_enabled").invoke(!n);
                      } catch (e) {}
                    }
                    sendNotification(uxSelectedName + " renderers: " + !n, false);
                  } catch (e) {
                    sendNotification("Toggle Renderer: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Toggles all Renderer components on the selected object and its children."
            }), new ButtonInfo({
              buttonText: "UX: Color Red",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(e).invoke(false);
                    const o = t ? t.length : 0;
                    const n = [1, 0, 0, 1];
                    for (let e = 0; e < o; e++) {
                      try {
                        t.method("get_Item").invoke(e).method("get_material").invoke().method("set_color").invoke(n);
                      } catch (e) {}
                    }
                    sendNotification(uxSelectedName + " â†’ red", false);
                  } catch (e) {
                    sendNotification("Color Red: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets all materials on the selected object to red."
            }), new ButtonInfo({
              buttonText: "UX: Color Blue",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(e).invoke(false);
                    const o = t ? t.length : 0;
                    const n = [0, 0, 1, 1];
                    for (let e = 0; e < o; e++) {
                      try {
                        t.method("get_Item").invoke(e).method("get_material").invoke().method("set_color").invoke(n);
                      } catch (e) {}
                    }
                    sendNotification(uxSelectedName + " â†’ blue", false);
                  } catch (e) {
                    sendNotification("Color Blue: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets all materials on the selected object to blue."
            }), new ButtonInfo({
              buttonText: "UX: Color Green",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(e).invoke(false);
                    const o = t ? t.length : 0;
                    const n = [0, 1, 0, 1];
                    for (let e = 0; e < o; e++) {
                      try {
                        t.method("get_Item").invoke(e).method("get_material").invoke().method("set_color").invoke(n);
                      } catch (e) {}
                    }
                    sendNotification(uxSelectedName + " â†’ green", false);
                  } catch (e) {
                    sendNotification("Color Green: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets all materials on the selected object to green."
            }), new ButtonInfo({
              buttonText: "UX: Color White",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(e).invoke(false);
                    const o = t ? t.length : 0;
                    const n = [1, 1, 1, 1];
                    for (let e = 0; e < o; e++) {
                      try {
                        t.method("get_Item").invoke(e).method("get_material").invoke().method("set_color").invoke(n);
                      } catch (e) {}
                    }
                    sendNotification(uxSelectedName + " â†’ white", false);
                  } catch (e) {
                    sendNotification("Color White: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Resets all material colors on the selected object to white."
            }), new ButtonInfo({
              buttonText: "UX: Attach to Hand",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (!e || e.handle.isNull()) {
                      return;
                    }
                    const t = e.field("handRight").value;
                    if (!t || t.isNull()) {
                      sendNotification("No right hand transform", false);
                      return;
                    }
                    uxGetTransform(uxSelectedObject).method("SetParent", 2).invoke(t, true);
                    try {
                      const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                      const t = uxSelectedObject.method("GetComponent", 1).inflate(e).invoke();
                      if (t && !t.isNull()) {
                        t.method("set_isKinematic").invoke(true);
                      }
                    } catch (e) {}
                    sendNotification(uxSelectedName + " attached to hand", false);
                  } catch (e) {
                    sendNotification("Attach: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Parents selected object to your right hand transform."
            }), new ButtonInfo({
              buttonText: "UX: Detach Parent",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    uxGetTransform(uxSelectedObject).method("SetParent", 2).invoke(NULL, true);
                    sendNotification(uxSelectedName + " detached", false);
                  } catch (e) {
                    sendNotification("Detach: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Detaches selected object from its parent (SetParent null), keeping world position."
            }), new ButtonInfo({
              buttonText: "UX: Parent to Obj Gun",
              isTogglable: true,
              method: () => {
                if (!uxSelectedObject || uxSelectedObject.isNull()) {
                  sendNotification("Select an object first", false);
                  return;
                }
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke();
                    const n = uxGetTransform(o);
                    uxGetTransform(uxSelectedObject).method("SetParent", 2).invoke(n, true);
                    const i = o.method("get_name").invoke()?.content ?? "?";
                    sendNotification(uxSelectedName + " parented to " + i, false);
                  } catch (e) {
                    sendNotification("Parent to: " + e, false);
                  }
                }
              },
              toolTip: "Hold grip + aim at an object + trigger to parent the selected object to it."
            }), new ButtonInfo({
              buttonText: "UX: Toggle Colliders",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Collider");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(e).invoke(false);
                    const o = t ? t.length : 0;
                    if (o === 0) {
                      sendNotification("No colliders", false);
                      return;
                    }
                    let n = false;
                    for (let e = 0; e < o; e++) {
                      try {
                        if (t.method("get_Item").invoke(e).method("get_enabled").invoke()) {
                          n = true;
                          break;
                        }
                      } catch (e) {}
                    }
                    for (let e = 0; e < o; e++) {
                      try {
                        t.method("get_Item").invoke(e).method("set_enabled").invoke(!n);
                      } catch (e) {}
                    }
                    sendNotification(uxSelectedName + " colliders: " + !n, false);
                  } catch (e) {
                    sendNotification("Toggle Colliders: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Toggles all Collider components on the selected object and its children."
            }), new ButtonInfo({
              buttonText: "UX: Make Trigger",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Collider");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(e).invoke(false);
                    const o = t ? t.length : 0;
                    for (let e = 0; e < o; e++) {
                      try {
                        t.method("get_Item").invoke(e).method("set_isTrigger").invoke(true);
                      } catch (e) {}
                    }
                    sendNotification(uxSelectedName + " â†’ trigger", false);
                  } catch (e) {
                    sendNotification("Make Trigger: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets isTrigger=true on all colliders (walk through the object)."
            }), new ButtonInfo({
              buttonText: "UX: Make Solid",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Collider");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(e).invoke(false);
                    const o = t ? t.length : 0;
                    for (let e = 0; e < o; e++) {
                      try {
                        t.method("get_Item").invoke(e).method("set_isTrigger").invoke(false);
                      } catch (e) {}
                    }
                    sendNotification(uxSelectedName + " â†’ solid", false);
                  } catch (e) {
                    sendNotification("Make Solid: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets isTrigger=false on all colliders (restores collision)."
            }), new ButtonInfo({
              buttonText: "UX: Log Layer+Tag",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("get_layer").invoke();
                    const t = uxSelectedObject.method("get_tag").invoke()?.content ?? "?";
                    const o = uxSelectedObject.method("GetInstanceID").invoke();
                    console.log("[UX] " + uxSelectedName + " | layer=" + e + " tag=" + t + " instanceID=" + o);
                    sendNotification("Layer=" + e + "  Tag=" + t + "  ID=" + o, false, 6);
                  } catch (e) {
                    sendNotification("Layer+Tag: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Logs the layer number, tag, and instanceID of the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Count All Objects",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.GameObject");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                  const o = t ? t.length : 0;
                  console.log("[UX] Total GameObjects in scene: " + o);
                  sendNotification("GameObjects in scene: " + o, false, 5);
                } catch (e) {
                  sendNotification("Count: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Counts and logs all active GameObjects currently in the scene."
            }), new ButtonInfo({
              buttonText: "UX: Dump All Near",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  if (!e || e.handle.isNull()) {
                    return;
                  }
                  const t = getTransform(e).method("get_position").invoke();
                  const o = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.GameObject");
                  const n = Object.method("FindObjectsByType", 1).inflate(o).invoke(0);
                  const i = n ? n.length : 0;
                  let a = 0;
                  console.log("[UX] Objects within 10m:");
                  for (let e = 0; e < i; e++) {
                    try {
                      const o = n.method("get_Item").invoke(e);
                      const i = uxGetTransform(o);
                      if (!i) {
                        continue;
                      }
                      const l = i.method("get_position").invoke();
                      const r = Vector3.method("Distance").invoke(t, l);
                      if (r <= 10) {
                        const e = o.method("get_name").invoke()?.content ?? "?";
                        console.log("[UX]   [" + r.toFixed(1) + "m] " + e);
                        a++;
                      }
                    } catch (e) {}
                  }
                  sendNotification(a + " objects within 10m â€” see log", false, 4);
                } catch (e) {
                  sendNotification("Dump Near: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Logs the name and distance of every GameObject within 10m of you to the Frida console."
            }), new ButtonInfo({
              buttonText: "UX: Full Deep Dump",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    console.log("[UX-DeepDump] â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•");
                    console.log("[UX-DeepDump] Object: " + uxSelectedName);
                    const e = uxGetTransform(uxSelectedObject);
                    if (e) {
                      console.log("[UX-DeepDump] Pos   : " + uxVec3Str(e.method("get_position").invoke()));
                      console.log("[UX-DeepDump] Rot   : " + uxVec3Str(e.method("get_eulerAngles").invoke()));
                      console.log("[UX-DeepDump] Scale : " + uxVec3Str(e.method("get_localScale").invoke()));
                    }
                    console.log("[UX-DeepDump] Layer : " + uxSelectedObject.method("get_layer").invoke());
                    console.log("[UX-DeepDump] Tag   : " + (uxSelectedObject.method("get_tag").invoke()?.content ?? "?"));
                    console.log("[UX-DeepDump] Active: " + uxSelectedObject.method("get_activeSelf").invoke());
                    const t = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Component");
                    const o = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(t).invoke(false);
                    const n = o ? o.length : 0;
                    for (let e = 0; e < Math.min(n, 30); e++) {
                      try {
                        const t = o.method("get_Item").invoke(e);
                        const n = t.method("GetType").invoke().method("get_Name").invoke()?.content ?? "?";
                        console.log("[UX-DeepDump]  â”Śâ”€ " + n);
                        try {
                          for (const e of t.class.fields) {
                            try {
                              console.log("[UX-DeepDump]  â”‚  ." + e.name + " = " + e.withHolder(t).value);
                            } catch (t) {
                              console.log("[UX-DeepDump]  â”‚  ." + e.name + " = <err>");
                            }
                          }
                        } catch (e) {}
                        console.log("[UX-DeepDump]  â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€");
                      } catch (e) {}
                    }
                    console.log("[UX-DeepDump] â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•");
                    sendNotification("Full dump written to log", false, 4);
                  } catch (e) {
                    sendNotification("DeepDump: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Full deep dump: position, rotation, scale, all component fields to Frida log."
            }), new ButtonInfo({
              buttonText: "UX: Get Mass",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody on " + uxSelectedName, false);
                      return;
                    }
                    const o = t.method("get_mass").invoke();
                    const n = t.method("get_linearDamping").invoke();
                    const i = t.method("get_angularDamping").invoke();
                    const a = t.method("get_isKinematic").invoke();
                    const l = t.method("get_useGravity").invoke();
                    console.log("[UX] " + uxSelectedName + " | mass=" + o.toFixed(3) + " drag=" + n.toFixed(3) + " angDrag=" + i.toFixed(3) + " kinematic=" + a + " gravity=" + l);
                    sendNotification("mass=" + o.toFixed(2) + " drag=" + n.toFixed(2) + " kin=" + a, false, 6);
                  } catch (e) {
                    sendNotification("Get Mass: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Reads and displays mass, drag, angularDrag, kinematic and gravity of the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Mass = 0.1",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_mass").invoke(0.1);
                    sendNotification(uxSelectedName + " mass = 0.1 (feather)", false);
                  } catch (e) {
                    sendNotification("Set Mass: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets Rigidbody mass to 0.1 (feather-light, flies on any force)."
            }), new ButtonInfo({
              buttonText: "UX: Mass = 1",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_mass").invoke(1);
                    sendNotification(uxSelectedName + " mass = 1 (default)", false);
                  } catch (e) {
                    sendNotification("Set Mass: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Resets Rigidbody mass to 1 (Unity default)."
            }), new ButtonInfo({
              buttonText: "UX: Mass = 100",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_mass").invoke(100);
                    sendNotification(uxSelectedName + " mass = 100 (boulder)", false);
                  } catch (e) {
                    sendNotification("Set Mass: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets Rigidbody mass to 100 (extremely heavy)."
            }), new ButtonInfo({
              buttonText: "UX: Drag = 0",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_linearDamping").invoke(0);
                    t.method("set_angularDamping").invoke(0);
                    sendNotification(uxSelectedName + " drag = 0 (no air resistance)", false);
                  } catch (e) {
                    sendNotification("Drag=0: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets linear and angular drag to 0 (object glides forever)."
            }), new ButtonInfo({
              buttonText: "UX: Drag = 10",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_linearDamping").invoke(10);
                    t.method("set_angularDamping").invoke(10);
                    sendNotification(uxSelectedName + " drag = 10 (floaty)", false);
                  } catch (e) {
                    sendNotification("Drag=10: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets drag to 10 â€” object floats and slows rapidly."
            }), new ButtonInfo({
              buttonText: "UX: Freeze Position",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_constraints").invoke(14);
                    sendNotification(uxSelectedName + " position frozen", false);
                  } catch (e) {
                    sendNotification("Freeze Pos: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets RigidbodyConstraints.FreezePosition (object stays put but can still rotate)."
            }), new ButtonInfo({
              buttonText: "UX: Freeze Rotation",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_constraints").invoke(112);
                    sendNotification(uxSelectedName + " rotation frozen", false);
                  } catch (e) {
                    sendNotification("Freeze Rot: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets RigidbodyConstraints.FreezeRotation (stops spinning)."
            }), new ButtonInfo({
              buttonText: "UX: Freeze All",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_constraints").invoke(126);
                    sendNotification(uxSelectedName + " ALL physics frozen", false);
                  } catch (e) {
                    sendNotification("Freeze All: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets RigidbodyConstraints.FreezeAll â€” completely locks the object in place."
            }), new ButtonInfo({
              buttonText: "UX: Unfreeze All",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_constraints").invoke(0);
                    t.method("set_isKinematic").invoke(false);
                    sendNotification(uxSelectedName + " fully unfrozen", false);
                  } catch (e) {
                    sendNotification("Unfreeze: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Clears all RigidbodyConstraints and disables kinematic."
            }), new ButtonInfo({
              buttonText: "UX: Add Torque",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_isKinematic").invoke(false);
                    const o = Vector3.method("op_Multiply").invoke(Vector3.method("get_up").invoke(), 500);
                    t.method("AddTorque", 2).invoke(o, 0);
                    sendNotification(uxSelectedName + " spinning!", false);
                  } catch (e) {
                    sendNotification("Torque: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Adds a large upward torque to spin the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Explosion Here",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_isKinematic").invoke(false);
                    const o = uxGetTransform(uxSelectedObject).method("get_position").invoke();
                    t.method("AddExplosionForce", 3).invoke(2000, o, 8);
                    sendNotification(uxSelectedName + " exploded!", false);
                  } catch (e) {
                    sendNotification("Explosion: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Calls AddExplosionForce at the object's own position â€” launches it."
            }), new ButtonInfo({
              buttonText: "UX: Log Velocity",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    const o = t.method("get_linearVelocity").invoke();
                    const n = t.method("get_angularVelocity").invoke();
                    const i = Math.sqrt(Math.pow(o.field("x").value, 2) + Math.pow(o.field("y").value, 2) + Math.pow(o.field("z").value, 2));
                    console.log("[UX] " + uxSelectedName + " vel=" + uxVec3Str(o) + " angVel=" + uxVec3Str(n));
                    sendNotification("speed=" + i.toFixed(2) + " m/s | " + uxVec3Str(o), false, 5);
                  } catch (e) {
                    sendNotification("Log Vel: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Logs and displays the current linear and angular velocity of the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Make Grabbable",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableObject on " + uxSelectedName, false);
                      return;
                    }
                    e.method("set_forceDisableGrab").invoke(false);
                    try {
                      e.field("_allowGrabAnywhere").value = true;
                    } catch (e) {}
                    sendNotification(uxSelectedName + " is now grabbable", false);
                  } catch (e) {
                    sendNotification("Make Grabbable: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Clears forceDisableGrab and enables grab-anywhere on the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Make Ungrabable",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableObject", false);
                      return;
                    }
                    e.method("set_forceDisableGrab").invoke(true);
                    sendNotification(uxSelectedName + " is now ungrabable", false);
                  } catch (e) {
                    sendNotification("Make Ungrabable: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets forceDisableGrab=true so no one can pick up the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Grab Anywhere",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableObject", false);
                      return;
                    }
                    e.field("_allowGrabAnywhere").value = true;
                    e.field("_useDefaultHandGrabOffsets").value = false;
                    sendNotification(uxSelectedName + " can be grabbed anywhere", false);
                  } catch (e) {
                    sendNotification("Grab Anywhere: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets _allowGrabAnywhere=true so the object can be grabbed at any point."
            }), new ButtonInfo({
              buttonText: "UX: Force Grab to Me",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (!e || e.handle.isNull()) {
                      return;
                    }
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                    if (!t || t.isNull()) {
                      const e = rightHandTransform.method("get_position").invoke();
                      uxGetTransform(uxSelectedObject).method("set_position").invoke(e);
                      sendNotification(uxSelectedName + " moved to hand (no GBO)", false);
                      return;
                    }
                    const o = e.method("get_backItemAttachAnchor").invoke();
                    if (o && !o.isNull()) {
                      t.method("Grab").invoke(o, true);
                      sendNotification(uxSelectedName + " grabbed to back anchor", false);
                    } else {
                      const e = rightHandTransform.method("get_position").invoke();
                      uxGetTransform(uxSelectedObject).method("set_position").invoke(e);
                      sendNotification(uxSelectedName + " moved to hand", false);
                    }
                  } catch (e) {
                    sendNotification("Force Grab: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Force-grabs the selected object to your back anchor, or teleports it to your hand as fallback."
            }), new ButtonInfo({
              buttonText: "UX: Force Release",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableObject", false);
                      return;
                    }
                    const t = uxGetTransform(uxSelectedObject).method("get_position").invoke();
                    const o = uxGetTransform(uxSelectedObject).method("get_rotation").invoke();
                    const n = Vector3.method("get_zero").invoke();
                    e.method("Release").invoke(t, o, n, n, false, false);
                    sendNotification(uxSelectedName + " released", false);
                  } catch (e) {
                    sendNotification("Force Release: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Force-releases the selected GrabbableObject with zero velocity."
            }), new ButtonInfo({
              buttonText: "UX: Scale Mod +",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableObject", false);
                      return;
                    }
                    const t = e.method("get_scaleModifier").invoke();
                    const o = Math.min(t + 20, 127);
                    e.method("set_scaleModifier").invoke(o);
                    e.method("HandleScaleChanged").invoke();
                    sendNotification(uxSelectedName + " scaleModifier â†’ " + o, false);
                  } catch (e) {
                    sendNotification("Scale Mod+: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Increases the networked scaleModifier on GrabbableObject by 20 steps."
            }), new ButtonInfo({
              buttonText: "UX: Scale Mod -",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableObject", false);
                      return;
                    }
                    const t = e.method("get_scaleModifier").invoke();
                    const o = Math.max(t - 20, -128);
                    e.method("set_scaleModifier").invoke(o);
                    e.method("HandleScaleChanged").invoke();
                    sendNotification(uxSelectedName + " scaleModifier â†’ " + o, false);
                  } catch (e) {
                    sendNotification("Scale Mod-: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Decreases the networked scaleModifier on GrabbableObject by 20 steps."
            }), new ButtonInfo({
              buttonText: "UX: Set Hue Random",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableObject", false);
                      return;
                    }
                    const t = Math.random();
                    e.method("SetColorHue").invoke(t);
                    e.method("HandleColorHueChanged").invoke();
                    sendNotification(uxSelectedName + " hue â†’ " + (t * 360).toFixed(0) + "Â°", false);
                  } catch (e) {
                    sendNotification("Set Hue: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets a random color hue on the GrabbableObject's networked color."
            }), new ButtonInfo({
              buttonText: "UX: Hide Obj (GBO)",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableObject", false);
                      return;
                    }
                    e.method("SetIsHidden").invoke(true);
                    sendNotification(uxSelectedName + " hidden (networked)", false);
                  } catch (e) {
                    sendNotification("Hide GBO: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Calls SetIsHidden(true) on the GrabbableObject â€” hidden for all players."
            }), new ButtonInfo({
              buttonText: "UX: Unhide Obj (GBO)",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableObject", false);
                      return;
                    }
                    e.method("SetIsHidden").invoke(false);
                    sendNotification(uxSelectedName + " unhidden (networked)", false);
                  } catch (e) {
                    sendNotification("Unhide GBO: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Calls SetIsHidden(false) on the GrabbableObject â€” visible for all players."
            }), new ButtonInfo({
              buttonText: "UX: Reveal All Hidden",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    let e = 0;
                    let t = 0;
                    let o = 0;
                    let n = 0;
                    const i = uxGetTransform(uxSelectedObject);
                    if (!i) {
                      sendNotification("No transform on " + uxSelectedName, false);
                      return;
                    }
                    const a = [i];
                    while (a.length > 0) {
                      const t = a.pop();
                      try {
                        const o = t.method("get_gameObject").invoke();
                        if (o && !o.isNull()) {
                          try {
                            if (!o.method("get_activeSelf").invoke()) {
                              o.method("SetActive").invoke(true);
                              e++;
                            }
                          } catch (e) {}
                        }
                        const n = t.method("get_childCount").invoke();
                        for (let e = 0; e < n; e++) {
                          try {
                            a.push(t.method("GetChild").invoke(e));
                          } catch (e) {}
                        }
                      } catch (e) {}
                    }
                    try {
                      const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                      const o = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(e).invoke(true);
                      const n = o ? o.length : 0;
                      for (let e = 0; e < n; e++) {
                        try {
                          const n = o.method("get_Item").invoke(e);
                          if (!n.method("get_enabled").invoke()) {
                            n.method("set_enabled").invoke(true);
                            t++;
                          }
                        } catch (e) {}
                      }
                    } catch (e) {}
                    try {
                      const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Collider");
                      const t = uxSelectedObject.method("GetComponentsInChildren", 1).inflate(e).invoke(true);
                      const n = t ? t.length : 0;
                      for (let e = 0; e < n; e++) {
                        try {
                          const n = t.method("get_Item").invoke(e);
                          if (!n.method("get_enabled").invoke()) {
                            n.method("set_enabled").invoke(true);
                            o++;
                          }
                        } catch (e) {}
                      }
                    } catch (e) {}
                    try {
                      const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBOClass).invoke(true);
                      if (e && !e.isNull()) {
                        e.method("SetIsHidden").invoke(false);
                        n = 1;
                      }
                    } catch (e) {}
                    const l = "Revealed on " + uxSelectedName + ":\n" + e + " inactive GOs, " + t + " renderers, " + o + " colliders" + (n ? ", GBO unhidden" : "");
                    console.log("[UX] " + l);
                    sendNotification(l, false, 7);
                  } catch (e) {
                    sendNotification("Reveal Hidden: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Makes EVERYTHING on the selected object visible: activates all inactive children, re-enables all disabled Renderers and Colliders, and clears the GBO hidden flag."
            }), new ButtonInfo({
              buttonText: "UX: Sell Value +500",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem", false);
                      return;
                    }
                    const t = e.method("get_additionalSellValue").invoke();
                    e.method("SetAdditionalSellValue").invoke(t + 500);
                    const o = e.method("get_sellValue").invoke();
                    sendNotification(uxSelectedName + " sell value â†’ " + o, false);
                  } catch (e) {
                    sendNotification("Sell Value: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Increases additionalSellValue by 500 (synced via RPC)."
            }), new ButtonInfo({
              buttonText: "UX: Sell Value MAX",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem", false);
                      return;
                    }
                    e.method("SetAdditionalSellValue").invoke(999999);
                    sendNotification(uxSelectedName + " sell value = MAX (999999)", false);
                  } catch (e) {
                    sendNotification("Sell Max: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets additionalSellValue to 999999 â€” sells for a fortune."
            }), new ButtonInfo({
              buttonText: "UX: Sell Value Reset",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem", false);
                      return;
                    }
                    e.method("SetAdditionalSellValue").invoke(0);
                    sendNotification(uxSelectedName + " sell value reset", false);
                  } catch (e) {
                    sendNotification("Sell Reset: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Resets additionalSellValue to 0."
            }), new ButtonInfo({
              buttonText: "UX: Mark Purchased",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem", false);
                      return;
                    }
                    e.method("set_wasPurchased").invoke(true);
                    sendNotification(uxSelectedName + " marked as purchased", false);
                  } catch (e) {
                    sendNotification("Purchased: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets wasPurchased=true on the GrabbableItem."
            }), new ButtonInfo({
              buttonText: "UX: Force Eat Now",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = AssemblyCSharp.class("AnimalCompany.EdibleItem");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("Not edible: " + uxSelectedName, false);
                      return;
                    }
                    t.method("TestOnEaten").invoke();
                    sendNotification(uxSelectedName + " eaten! Nom nom.", false);
                  } catch (e) {
                    sendNotification("Eat: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Calls TestOnEaten() on the EdibleItem component â€” triggers all buff/eat effects."
            }), new ButtonInfo({
              buttonText: "UX: Check Edible",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = AssemblyCSharp.class("AnimalCompany.EdibleItem");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification(uxSelectedName + " is NOT edible", false, 4);
                      console.log("[UX] " + uxSelectedName + " has no EdibleItem component");
                    } else {
                      const e = t.field("_wasEaten").value;
                      sendNotification(uxSelectedName + " IS edible! wasEaten=" + e, false, 4);
                      console.log("[UX] " + uxSelectedName + " EdibleItem found, wasEaten=" + e);
                    }
                  } catch (e) {
                    sendNotification("Check Edible: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Checks if the selected object has an EdibleItem component and logs its state."
            }), new ButtonInfo({
              buttonText: "UX: Print Item Data",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem on " + uxSelectedName, false);
                      return;
                    }
                    const t = e.method("get_itemID").invoke()?.content ?? "?";
                    const o = e.method("get_sellValue").invoke();
                    const n = e.method("get_additionalSellValue").invoke();
                    const i = e.method("get_canAddToBag").invoke();
                    const a = e.method("get_allowAddToBag").invoke();
                    const l = e.method("get_allowAttachToItem").invoke();
                    const r = e.field("_allowAddToBag").value;
                    console.log("[UX-ItemData] â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•");
                    console.log("[UX-ItemData] itemID         : " + t);
                    console.log("[UX-ItemData] sellValue      : " + o + " (+" + n + " bonus)");
                    console.log("[UX-ItemData] canAddToBag    : " + i);
                    console.log("[UX-ItemData] allowAddToBag  : " + a);
                    console.log("[UX-ItemData] allowAttachItem: " + l);
                    console.log("[UX-ItemData] _allowAddToBag : " + r);
                    try {
                      const t = e.method("get_itemData").invoke();
                      if (t && !t.isNull()) {
                        const e = t.method("get_flags").invoke();
                        const o = e ? e.method("get_value").invoke() : "?";
                        console.log("[UX-ItemData] flags          : " + o);
                        console.log("[UX-ItemData] isLoot         : " + t.method("get_isLoot").invoke());
                        console.log("[UX-ItemData] isPurchasable  : " + t.method("get_isPurchasable").invoke());
                        console.log("[UX-ItemData] isBag          : " + t.method("get_isBag").invoke());
                        console.log("[UX-ItemData] isQuiver       : " + t.method("get_isQuiver").invoke());
                        console.log("[UX-ItemData] isWeapon       : " + t.method("get_isWeapon").invoke());
                      }
                    } catch (e) {}
                    console.log("[UX-ItemData] â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•");
                    sendNotification(t + " | sell=" + o + " | see log", false, 5);
                  } catch (e) {
                    sendNotification("Item Data: " + e, false);
                    console.error("[UX] ItemData:", e);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Logs full itemID, sell value, and all GameplayItemFlags to Frida console."
            }), new ButtonInfo({
              buttonText: "UX: Allow in Bag",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem", false);
                      return;
                    }
                    e.method("set_allowAddToBag").invoke(true);
                    try {
                      e.field("_allowAddToBag").value = true;
                    } catch (e) {}
                    sendNotification(uxSelectedName + " can now go in bags", false);
                  } catch (e) {
                    sendNotification("Allow in Bag: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets allowAddToBag=true so the item can be put into backpacks."
            }), new ButtonInfo({
              buttonText: "UX: Deny in Bag",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem", false);
                      return;
                    }
                    e.method("set_allowAddToBag").invoke(false);
                    try {
                      e.field("_allowAddToBag").value = false;
                    } catch (e) {}
                    sendNotification(uxSelectedName + " blocked from bags", false);
                  } catch (e) {
                    sendNotification("Deny in Bag: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets allowAddToBag=false so the item cannot be bagged."
            }), new ButtonInfo({
              buttonText: "UX: Set Loot Flag",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem", false);
                      return;
                    }
                    const t = e.method("get_itemData").invoke();
                    if (!t || t.isNull()) {
                      sendNotification("No itemData", false);
                      return;
                    }
                    const o = t.method("get_flags").invoke();
                    const n = o.method("get_value").invoke();
                    o.method("set_value").invoke(n | 2);
                    sendNotification(uxSelectedName + " flagged as Loot", false);
                  } catch (e) {
                    sendNotification("Loot Flag: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Adds the Loot flag (0x2) to the item's GameplayItemFlags."
            }), new ButtonInfo({
              buttonText: "UX: Set Bag Flag",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem", false);
                      return;
                    }
                    const t = e.method("get_itemData").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_flags").invoke();
                    const n = o.method("get_value").invoke();
                    o.method("set_value").invoke(n | 160);
                    sendNotification(uxSelectedName + " flagged as Bag", false);
                  } catch (e) {
                    sendNotification("Bag Flag: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Adds IsBag(0x20) + AllowAddToBag(0x80) flags to the item."
            }), new ButtonInfo({
              buttonText: "UX: Set Quiver Flag",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem", false);
                      return;
                    }
                    const t = e.method("get_itemData").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_flags").invoke();
                    const n = o.method("get_value").invoke();
                    o.method("set_value").invoke(n | 320);
                    sendNotification(uxSelectedName + " flagged as Quiver", false);
                  } catch (e) {
                    sendNotification("Quiver Flag: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Adds IsQuiver(0x40) + AllowAddToQuiver(0x100) flags to the item."
            }), new ButtonInfo({
              buttonText: "UX: Flag Back+Hip",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem", false);
                      return;
                    }
                    const t = e.method("get_itemData").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_flags").invoke();
                    const n = o.method("get_value").invoke();
                    o.method("set_value").invoke(n | 3072);
                    sendNotification(uxSelectedName + " can attach to back/hip", false);
                  } catch (e) {
                    sendNotification("Back+Hip Flag: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Adds AllowAttachToBack(0x400) + AllowAttachToHip(0x800) flags."
            }), new ButtonInfo({
              buttonText: "UX: Flags = ALL",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem", false);
                      return;
                    }
                    const t = e.method("get_itemData").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    t.method("get_flags").invoke().method("set_value").invoke(8190);
                    e.method("set_allowAddToBag").invoke(true);
                    sendNotification(uxSelectedName + " ALL flags enabled", false);
                  } catch (e) {
                    sendNotification("Flags ALL: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets ALL GameplayItemFlags at once â€” item becomes loot, bag, quiver, attachable everywhere."
            }), new ButtonInfo({
              buttonText: "UX: Flags = CLEAR",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem", false);
                      return;
                    }
                    const t = e.method("get_itemData").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    t.method("get_flags").invoke().method("set_value").invoke(0);
                    sendNotification(uxSelectedName + " flags cleared", false);
                  } catch (e) {
                    sendNotification("Flags Clear: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Clears all GameplayItemFlags to 0 (None)."
            }), new ButtonInfo({
              buttonText: "UX: Buff Player Hit",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.8;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player hit", false);
                      return;
                    }
                    const n = getPlayerName(o);
                    globalThis._uxBuffIdx ||= 1;
                    const i = globalThis._uxBuffIdx;
                    globalThis._uxBuffIdx = i % 7 + 1;
                    o.method("RPC_ApplyBuff").invoke(i);
                    sendNotification("Buffed " + n + " â†’ " + (["?", "Speed", "Pink", "Damage", "Stun", "Scale", "Stinky", "AntiGrav"][i] ?? i), false);
                  } catch (e) {
                    sendNotification("Buff Gun: " + e, false);
                  }
                }
              },
              toolTip: "Aim at a player and pull trigger to cycle through all 7 buffs (Speed/Pink/Damage/Stun/Scale/Stinky/AntiGrav)."
            }), new ButtonInfo({
              buttonText: "UX: Tag Stinky Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player", false);
                      return;
                    }
                    o.method("RPC_TagAsStinky").invoke();
                    sendNotification(getPlayerName(o) + " tagged stinky!", false);
                  } catch (e) {
                    sendNotification("Stinky Gun: " + e, false);
                  }
                }
              },
              toolTip: "Aim at a player and pull trigger to tag them as stinky."
            }), new ButtonInfo({
              buttonText: "UX: Color HSV Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke().method("GetComponentInParent", 0).inflate(NetPlayer).invoke();
                    if (!o || o.isNull()) {
                      sendNotification("No player", false);
                      return;
                    }
                    const n = Math.random();
                    o.method("RPC_SetColorHSV").invoke(30, n, 1, 1);
                    sendNotification(getPlayerName(o) + " â†’ HSV hue " + (n * 360).toFixed(0) + "Â°", false);
                  } catch (e) {
                    sendNotification("HSV Gun: " + e, false);
                  }
                }
              },
              toolTip: "Aim at a player and pull trigger to set a random HSV color effect on them for 30 seconds."
            }), new ButtonInfo({
              buttonText: "UX: Instantiate Prefab",
              method: () => {
                try {
                  const e = prefabIDs[prefabIndex];
                  const t = NetPlayer.method("get_localPlayer").invoke();
                  const o = spawnNetworkPrefab(e, t && !t.handle.isNull() ? getTransform(t).method("get_position").invoke() : Vector3.method("get_zero").invoke(), identityQuaternion);
                  if (o && !o.isNull()) {
                    sendNotification("Spawned (net): " + e, false, 5);
                  } else {
                    sendNotification("Not found in PrefabTable: " + e, false);
                  }
                } catch (e) {
                  sendNotification("Instantiate: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Spawns the selected prefab at your position via NetworkRunner PrefabTable."
            }), new ButtonInfo({
              buttonText: "UX: Spawn At Aim",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const t = e.method("get_point").invoke();
                    const o = prefabIDs[prefabIndex];
                    const n = spawnNetworkPrefab(o, t, identityQuaternion);
                    if (n && !n.isNull()) {
                      sendNotification("Spawned: " + o, false);
                    } else {
                      sendNotification("Not found in PrefabTable: " + o, false);
                    }
                  } catch (e) {
                    sendNotification("Spawn Aim: " + e, false);
                  }
                }
              },
              toolTip: "Hold grip + aim + trigger to spawn the selected prefab exactly where you aim (via NetworkRunner)."
            }), new ButtonInfo({
              buttonText: "UX: Spawn Copy Here",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    const t = e && !e.handle.isNull() ? getTransform(e).method("get_position").invoke() : uxGetTransform(uxSelectedObject).method("get_position").invoke();
                    const o = uxGetTransform(uxSelectedObject).method("get_rotation").invoke();
                    try {
                      const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                      if (e && !e.isNull()) {
                        const n = e.method("get_itemID").invoke()?.content ?? "";
                        if (n) {
                          PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string(n), t, o, NULL);
                          sendNotification("Copied: " + n, false, 5);
                          return;
                        }
                      }
                    } catch (e) {}
                    Object.method("Instantiate", 3).invoke(uxSelectedObject, t, o);
                    sendNotification("Cloned: " + uxSelectedName, false, 5);
                  } catch (e) {
                    sendNotification("Spawn Copy: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Spawns a copy of the selected object at your position. Uses itemID for networked spawn, falls back to Instantiate."
            }), new ButtonInfo({
              buttonText: "UX: Despawn Selected",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkObject");
                    const t = uxSelectedObject.method("GetComponentInParent", 0).inflate(e).invoke();
                    if (t && !t.isNull()) {
                      const e = PrefabGen.method("get_runner").invoke();
                      if (e && !e.isNull()) {
                        e.method("Despawn", 1).invoke(t);
                        sendNotification("Despawned: " + uxSelectedName, false);
                        uxSelectedObject = null;
                        uxSelectedName = "none";
                        return;
                      }
                    }
                    Object.method("Destroy", 1).invoke(uxSelectedObject);
                    sendNotification("Destroyed: " + uxSelectedName, false);
                    uxSelectedObject = null;
                    uxSelectedName = "none";
                  } catch (e) {
                    sendNotification("Despawn: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Despawns the selected object via NetworkRunner, or falls back to Object.Destroy."
            }), new ButtonInfo({
              buttonText: "UX: Copy to Prefab List",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    let e = "";
                    try {
                      const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                      if (t && !t.isNull()) {
                        const o = t.method("get_itemID").invoke()?.content ?? "";
                        if (o && !prefabIDs.includes(o)) {
                          prefabIDs.push(o);
                          e = o;
                        } else if (o) {
                          sendNotification("Already in list: " + o, false);
                          return;
                        }
                      }
                    } catch (e) {}
                    if (!e) {
                      const t = uxSelectedName;
                      if (prefabIDs.includes(t)) {
                        sendNotification("Already in list: " + t, false);
                        return;
                      }
                      prefabIDs.push(t);
                      e = t;
                    }
                    prefabIndex = prefabIDs.length - 1;
                    sendNotification("Added + selected: " + e, false, 5);
                  } catch (e) {
                    sendNotification("Copy Prefab: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Adds the selected object's itemID (or name) to your prefab spawn list and selects it."
            }), new ButtonInfo({
              buttonText: "UX: Make Edible",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = AssemblyCSharp.class("AnimalCompany.EdibleItem");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (t && !t.isNull()) {
                      sendNotification(uxSelectedName + " already has EdibleItem!", false, 4);
                      return;
                    }
                    const o = uxSelectedObject.method("AddComponent", 1).inflate(e).invoke();
                    if (o && !o.isNull()) {
                      sendNotification(uxSelectedName + " â†’ EdibleItem added!", false, 5);
                    } else {
                      sendNotification("AddComponent returned null", false);
                    }
                  } catch (e) {
                    sendNotification("Make Edible: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Adds an EdibleItem component to the selected object so it can be eaten."
            }), new ButtonInfo({
              buttonText: "UX: Trigger Eat",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = AssemblyCSharp.class("AnimalCompany.EdibleItem");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No EdibleItem â€” use Make Edible first", false);
                      return;
                    }
                    t.field("_wasEaten").value = true;
                    let o = false;
                    try {
                      const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                      if (e && !e.isNull()) {
                        e.method("DespawnItem").invoke();
                        o = true;
                      }
                    } catch (e) {}
                    if (!o) {
                      try {
                        Object.method("Destroy", 1).invoke(uxSelectedObject);
                        o = true;
                      } catch (e) {}
                    }
                    sendNotification(uxSelectedName + " â†’ eaten and despawned!", false, 4);
                    uxSelectedObject = null;
                    uxSelectedName = "none";
                  } catch (e) {
                    sendNotification("Trigger Eat: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Triggers the eat sequence: sets _wasEaten=true then despawns the item via DespawnItem or Object.Destroy."
            }), new ButtonInfo({
              buttonText: "UX: Check Edible",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = AssemblyCSharp.class("AnimalCompany.EdibleItem");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification(uxSelectedName + " is NOT edible", false, 4);
                      console.log("[UX] " + uxSelectedName + " â€” no EdibleItem component");
                    } else {
                      const e = t.field("_wasEaten").value;
                      const o = t.field("_isCloseToMouth").value;
                      sendNotification(uxSelectedName + " IS edible | wasEaten=" + e + " nearMouth=" + o, false, 5);
                      console.log("[UX] EdibleItem found on " + uxSelectedName + " | wasEaten=" + e + " isCloseToMouth=" + o);
                    }
                  } catch (e) {
                    sendNotification("Check Edible: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Checks if the selected object has an EdibleItem and logs wasEaten + isCloseToMouth."
            }), new ButtonInfo({
              buttonText: "UX: Reset Eaten",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = AssemblyCSharp.class("AnimalCompany.EdibleItem");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No EdibleItem on " + uxSelectedName, false);
                      return;
                    }
                    t.field("_wasEaten").value = false;
                    t.field("_timerEating").value = 0;
                    sendNotification(uxSelectedName + " â†’ eat state reset (can be eaten again)", false, 4);
                  } catch (e) {
                    sendNotification("Reset Eaten: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Resets _wasEaten=false and _timerEating=0 so the item can be eaten again."
            }), new ButtonInfo({
              buttonText: "UX: Scale x2",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_localScale").invoke();
                    const o = t.field("x").value;
                    const n = t.field("y").value;
                    const i = [o * 2, n * 2, t.field("z").value * 2];
                    e.method("set_localScale").invoke(i);
                    sendNotification(uxSelectedName + " scale x2 â†’ (" + (o * 2).toFixed(2) + ")", false);
                  } catch (e) {
                    sendNotification("Scale x2: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Doubles the localScale of the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Scale x0.5",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_localScale").invoke();
                    const o = t.field("x").value;
                    const n = t.field("y").value;
                    const i = [o * 0.5, n * 0.5, t.field("z").value * 0.5];
                    e.method("set_localScale").invoke(i);
                    sendNotification(uxSelectedName + " scale x0.5 â†’ (" + (o * 0.5).toFixed(2) + ")", false);
                  } catch (e) {
                    sendNotification("Scale x0.5: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Halves the localScale of the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Scale Reset",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = Vector3.method("get_one").invoke();
                    e.method("set_localScale").invoke(t);
                    sendNotification(uxSelectedName + " scale â†’ (1,1,1)", false);
                  } catch (e) {
                    sendNotification("Scale Reset: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Resets localScale of the selected object to (1,1,1)."
            }), new ButtonInfo({
              buttonText: "UX: Scale x10",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_localScale").invoke();
                    const o = t.field("x").value;
                    const n = t.field("y").value;
                    const i = [o * 10, n * 10, t.field("z").value * 10];
                    e.method("set_localScale").invoke(i);
                    sendNotification(uxSelectedName + " scale x10 â†’ (" + (o * 10).toFixed(2) + ")", false);
                  } catch (e) {
                    sendNotification("Scale x10: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Scales the selected object up by 10x."
            }), new ButtonInfo({
              buttonText: "UX: Move Up 1m",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_position").invoke();
                    const o = [t.field("x").value, t.field("y").value + 1, t.field("z").value];
                    e.method("set_position").invoke(o);
                    sendNotification(uxSelectedName + " moved up 1m", false);
                  } catch (e) {
                    sendNotification("Move Up: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Moves the selected object up by 1 metre in world space."
            }), new ButtonInfo({
              buttonText: "UX: Move Down 1m",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_position").invoke();
                    const o = [t.field("x").value, t.field("y").value - 1, t.field("z").value];
                    e.method("set_position").invoke(o);
                    sendNotification(uxSelectedName + " moved down 1m", false);
                  } catch (e) {
                    sendNotification("Move Down: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Moves the selected object down by 1 metre in world space."
            }), new ButtonInfo({
              buttonText: "UX: Rotate Y 90Â°",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_eulerAngles").invoke();
                    const o = t.field("x").value;
                    const n = (t.field("y").value + 90) % 360;
                    const i = t.field("z").value;
                    const a = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Vector3").method(".ctor", 3).invoke(o, n, i);
                    e.method("set_eulerAngles").invoke(a);
                    sendNotification(uxSelectedName + " rotated Y +90Â°", false);
                  } catch (e) {
                    try {
                      const e = uxGetTransform(uxSelectedObject);
                      const t = e.method("get_eulerAngles").invoke();
                      t.field("y").value = (t.field("y").value + 90) % 360;
                      e.method("set_eulerAngles").invoke(t);
                      sendNotification(uxSelectedName + " rotated Y +90Â°", false);
                    } catch (e) {
                      sendNotification("Rotate Y: " + e, false);
                    }
                  }
                }
              },
              isTogglable: false,
              toolTip: "Rotates the selected object 90Â° around the Y axis."
            }), new ButtonInfo({
              buttonText: "UX: Snap to Ground",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_position").invoke();
                    const o = t.field("x").value;
                    t.field("y").value;
                    const n = t.field("z").value;
                    const i = Vector3.method("op_Addition").invoke(t, Vector3.method("op_Multiply").invoke(Vector3.method("get_up").invoke(), 2));
                    const a = Vector3.method("op_Multiply").invoke(Vector3.method("get_up").invoke(), -1);
                    const l = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Physics");
                    Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.RaycastHit");
                    const r = Il2Cpp.alloc(128);
                    if (l.method("Raycast", 4).invoke(i, a, r, 200)) {
                      const t = Il2Cpp.reference(r).method("get_point").invoke().field("y").value;
                      const i = Vector3.method("op_Addition").invoke(Vector3.method("get_zero").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(Vector3.method("get_right").invoke(), o), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(Vector3.method("get_up").invoke(), t), Vector3.method("op_Multiply", 2).invoke(Vector3.method("get_forward").invoke(), n))));
                      e.method("set_position").invoke(i);
                      sendNotification(uxSelectedName + " snapped to y=" + t.toFixed(2), false, 4);
                    } else {
                      const t = Vector3.method("op_Addition").invoke(Vector3.method("get_zero").invoke(), Vector3.method("op_Addition").invoke(Vector3.method("op_Multiply", 2).invoke(Vector3.method("get_right").invoke(), o), Vector3.method("op_Multiply", 2).invoke(Vector3.method("get_forward").invoke(), n)));
                      e.method("set_position").invoke(t);
                      sendNotification(uxSelectedName + " snapped to y=0 (no raycast hit)", false, 4);
                    }
                  } catch (e) {
                    sendNotification("Snap Ground: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Raycasts down from the selected object and snaps it to the ground surface."
            }), new ButtonInfo({
              buttonText: "UX: Get Item ID",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedObject.method("GetComponentInChildren", 1).inflate(GBIClass).invoke(true);
                    if (!e || e.isNull()) {
                      sendNotification("No GrabbableItem on " + uxSelectedName, false);
                      return;
                    }
                    const t = e.method("get_itemID").invoke()?.content ?? "?";
                    const o = e.method("get_sellValue").invoke();
                    console.log("[UX] ItemID: " + t + " | sellValue: " + o);
                    sendNotification("ItemID: " + t + " | sell=" + o, false, 7);
                  } catch (e) {
                    sendNotification("Get ItemID: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Reads and displays the itemID and sell value from the selected GrabbableItem."
            }), new ButtonInfo({
              buttonText: "UX: Log All Components",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetComponentNames(uxSelectedObject);
                    console.log("[UX] Components on " + uxSelectedName + " (" + e.length + "):");
                    e.forEach((e, t) => console.log("[UX]   [" + t + "] " + e));
                    sendNotification(uxSelectedName + ": " + e.length + " comps â€” see log", false, 5);
                  } catch (e) {
                    sendNotification("Log Comps: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Logs every component type name on the selected object to the Frida console."
            }), new ButtonInfo({
              buttonText: "UX: Log Full Hierarchy",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    let e = function (t, o) {
                      const n = t.method("get_name").invoke()?.content ?? "?";
                      const i = "  ".repeat(o) + (o === 0 ? "ROOT: " : "â””â”€ ");
                      console.log("[UX-Hier] " + i + n);
                      const a = uxGetTransform(t);
                      if (!a) {
                        return;
                      }
                      const l = a.method("get_childCount").invoke();
                      for (let t = 0; t < Math.min(l, 20); t++) {
                        try {
                          const n = a.method("GetChild").invoke(t).method("get_gameObject").invoke();
                          e(n, o + 1);
                        } catch (e) {}
                      }
                    };
                    e(uxSelectedObject, 0);
                    sendNotification("Hierarchy logged â€” check Frida console", false, 4);
                  } catch (e) {
                    sendNotification("Hierarchy: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Recursively logs the full child hierarchy of the selected GameObject (max 20 per level)."
            }), new ButtonInfo({
              buttonText: "UX: Select Parent",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject).method("get_parent").invoke();
                    if (!e || e.isNull()) {
                      sendNotification(uxSelectedName + " has no parent", false);
                      return;
                    }
                    const t = e.method("get_gameObject").invoke();
                    uxSelectedObject = t;
                    sendNotification("Now selected: " + (uxSelectedName = t.method("get_name").invoke()?.content ?? "unnamed"), false, 4);
                  } catch (e) {
                    sendNotification("Select Parent: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Navigates up to the parent GameObject and selects it as the new UX target."
            }), new ButtonInfo({
              buttonText: "UX: Select Child[0]",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxGetTransform(uxSelectedObject);
                    const t = e.method("get_childCount").invoke();
                    if (t === 0) {
                      sendNotification(uxSelectedName + " has no children", false);
                      return;
                    }
                    const o = e.method("GetChild").invoke(0).method("get_gameObject").invoke();
                    uxSelectedObject = o;
                    sendNotification("Now selected: " + (uxSelectedName = o.method("get_name").invoke()?.content ?? "child") + " (child 0 of " + t + ")", false, 4);
                  } catch (e) {
                    sendNotification("Select Child: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Selects the first child of the currently selected GameObject."
            }), new ButtonInfo({
              buttonText: "UX: Select by Gun",
              isTogglable: true,
              method: () => {
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.5;
                  try {
                    const t = e.method("get_collider").invoke();
                    if (!t || t.isNull()) {
                      return;
                    }
                    const o = t.method("get_gameObject").invoke();
                    uxSelectedObject = o;
                    sendNotification("UX Selected: " + (uxSelectedName = o.method("get_name").invoke()?.content ?? "unnamed"), false, 4);
                  } catch (e) {
                    sendNotification("Select Gun: " + e, false);
                  }
                }
              },
              toolTip: "Hold grip + aim + trigger to point-select any object in the world as your UX target."
            }), new ButtonInfo({
              buttonText: "UX: Spawn Mob Here",
              method: () => {
                try {
                  const e = NetPlayer.method("get_localPlayer").invoke();
                  const t = e && !e.handle.isNull() ? getTransform(e).method("get_position").invoke() : Vector3.method("get_zero").invoke();
                  const o = [10, 1, 5, 6, 19, 15, 3, 22];
                  const n = ["Chicken", "Angler", "Banshee", "Bomb", "Spider", "Phantom", "Armstrong", "Segway"];
                  globalThis._uxMobIdx ||= 0;
                  if (rightGrab) {
                    globalThis._uxMobIdx = (globalThis._uxMobIdx + 1) % o.length;
                    sendNotification("Mob: " + n[globalThis._uxMobIdx], false, 3);
                    return;
                  }
                  const i = o[globalThis._uxMobIdx];
                  const a = n[globalThis._uxMobIdx];
                  spawnMobAtPos({
                    name: a,
                    id: i
                  }, t, identityQuaternion);
                  sendNotification("Spawned " + a + " at your position!", false, 4);
                } catch (e) {
                  sendNotification("Spawn Mob: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Spawns a mob at your position. Hold RIGHT GRIP to cycle mob type (Chicken/Angler/Banshee/Bomb/Spider/Phantom/Armstrong/Segway), then press without grip to spawn."
            }), new ButtonInfo({
              buttonText: "UX: Kinematic ON",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_isKinematic").invoke(true);
                    sendNotification(uxSelectedName + " â†’ kinematic ON (no physics)", false);
                  } catch (e) {
                    sendNotification("Kinematic ON: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Enables isKinematic on the selected object's Rigidbody (physics won't move it)."
            }), new ButtonInfo({
              buttonText: "UX: Kinematic OFF",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_isKinematic").invoke(false);
                    sendNotification(uxSelectedName + " â†’ kinematic OFF (physics active)", false);
                  } catch (e) {
                    sendNotification("Kinematic OFF: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Disables isKinematic â€” object is now fully simulated by physics."
            }), new ButtonInfo({
              buttonText: "UX: Zero Velocity",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    const o = Vector3.method("get_zero").invoke();
                    t.method("set_linearVelocity").invoke(o);
                    t.method("set_angularVelocity").invoke(o);
                    sendNotification(uxSelectedName + " velocity zeroed", false);
                  } catch (e) {
                    sendNotification("Zero Vel: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Sets both linear and angular velocity to zero on the selected Rigidbody."
            }), new ButtonInfo({
              buttonText: "UX: Launch Up",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image.class("UnityEngine.Rigidbody");
                    const t = uxSelectedObject.method("GetComponentInChildren", 1).inflate(e).invoke(true);
                    if (!t || t.isNull()) {
                      sendNotification("No Rigidbody", false);
                      return;
                    }
                    t.method("set_isKinematic").invoke(false);
                    t.method("set_useGravity").invoke(true);
                    const o = [0, 1200, 0];
                    t.method("AddForce", 2).invoke(o, 1);
                    sendNotification(uxSelectedName + " launched up!", false);
                  } catch (e) {
                    sendNotification("Launch Up: " + e, false);
                  }
                }
              },
              isTogglable: false,
              toolTip: "Applies a large upward impulse force to the selected object's Rigidbody."
            }), new ButtonInfo({
              buttonText: "UX: List Scenes",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.SceneManagement.SceneManager");
                  const t = e.method("get_sceneCountInBuildSettings").invoke();
                  const o = e.method("get_sceneCount").invoke();
                  console.log("[UX-Scene] Build settings: " + t + " scenes, currently loaded: " + o);
                  for (let t = 0; t < o; t++) {
                    try {
                      const o = e.method("GetSceneAt").invoke(t);
                      const n = o.method("get_name").invoke()?.content ?? "?";
                      const i = o.method("get_path").invoke()?.content ?? "?";
                      const a = o.method("get_buildIndex").invoke();
                      console.log("[UX-Scene]   [" + t + "] name=" + n + " buildIndex=" + a + " path=" + i);
                    } catch (e) {}
                  }
                  sendNotification("Scenes: " + t + " in build, " + o + " loaded â€” see log", false, 5);
                } catch (e) {
                  sendNotification("List Scenes: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Logs all loaded scenes and build-settings scene count to Frida console."
            }), new ButtonInfo({
              buttonText: "UX: Load Scene 0",
              method: () => {
                try {
                  Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.SceneManagement.SceneManager").method("LoadScene", 1).invoke(0);
                  sendNotification("Loading scene index 0...", false);
                } catch (e) {
                  sendNotification("Load Scene 0: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Loads build-index scene 0 (usually the main menu/lobby)."
            }), new ButtonInfo({
              buttonText: "UX: Load Scene 1",
              method: () => {
                try {
                  Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.SceneManagement.SceneManager").method("LoadScene", 1).invoke(1);
                  sendNotification("Loading scene index 1...", false);
                } catch (e) {
                  sendNotification("Load Scene 1: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Loads build-index scene 1."
            }), new ButtonInfo({
              buttonText: "UX: Load Scene 2",
              method: () => {
                try {
                  Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.SceneManagement.SceneManager").method("LoadScene", 1).invoke(2);
                  sendNotification("Loading scene index 2...", false);
                } catch (e) {
                  sendNotification("Load Scene 2: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Loads build-index scene 2."
            }), new ButtonInfo({
              buttonText: "UX: Load Scene 3",
              method: () => {
                try {
                  Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.SceneManagement.SceneManager").method("LoadScene", 1).invoke(3);
                  sendNotification("Loading scene index 3...", false);
                } catch (e) {
                  sendNotification("Load Scene 3: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Loads build-index scene 3."
            }), new ButtonInfo({
              buttonText: "UX: Load Scene 4",
              method: () => {
                try {
                  Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.SceneManagement.SceneManager").method("LoadScene", 1).invoke(4);
                  sendNotification("Loading scene index 4...", false);
                } catch (e) {
                  sendNotification("Load Scene 4: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Loads build-index scene 4."
            }), new ButtonInfo({
              buttonText: "UX: Load Scene 5",
              method: () => {
                try {
                  Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.SceneManagement.SceneManager").method("LoadScene", 1).invoke(5);
                  sendNotification("Loading scene index 5...", false);
                } catch (e) {
                  sendNotification("Load Scene 5: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Loads build-index scene 5."
            }), new ButtonInfo({
              buttonText: "UX: Reload Current",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.SceneManagement.SceneManager");
                  const t = e.method("GetActiveScene").invoke();
                  const o = t.method("get_buildIndex").invoke();
                  const n = t.method("get_name").invoke()?.content ?? "?";
                  e.method("LoadScene", 1).invoke(o);
                  sendNotification("Reloading: " + n + " [" + o + "]", false);
                } catch (e) {
                  sendNotification("Reload: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Reloads the currently active scene."
            }), new ButtonInfo({
              buttonText: "UX: Load Scene +1",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.SceneManagement.SceneManager");
                  const t = e.method("GetActiveScene").invoke().method("get_buildIndex").invoke();
                  const o = e.method("get_sceneCountInBuildSettings").invoke();
                  const n = (t + 1) % Math.max(o, 1);
                  e.method("LoadScene", 1).invoke(n);
                  sendNotification("Loading scene " + n + " of " + o, false);
                } catch (e) {
                  sendNotification("Load +1: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Loads the next scene in build order (wraps around)."
            }), new ButtonInfo({
              buttonText: "UX: Load Scene -1",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.SceneManagement.SceneManager");
                  const t = e.method("GetActiveScene").invoke().method("get_buildIndex").invoke();
                  const o = e.method("get_sceneCountInBuildSettings").invoke();
                  const n = (t - 1 + Math.max(o, 1)) % Math.max(o, 1);
                  e.method("LoadScene", 1).invoke(n);
                  sendNotification("Loading scene " + n + " of " + o, false);
                } catch (e) {
                  sendNotification("Load -1: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Loads the previous scene in build order (wraps around)."
            }), new ButtonInfo({
              buttonText: "UX: Log Active Scene",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.SceneManagement.SceneManager").method("GetActiveScene").invoke();
                  const t = e.method("get_name").invoke()?.content ?? "?";
                  const o = e.method("get_path").invoke()?.content ?? "?";
                  const n = e.method("get_buildIndex").invoke();
                  const i = e.method("get_rootCount").invoke();
                  console.log("[UX-Scene] Active: name=" + t + " index=" + n + " rootObjects=" + i);
                  console.log("[UX-Scene] Path: " + o);
                  sendNotification("Scene: " + t + " [" + n + "] roots=" + i, false, 6);
                } catch (e) {
                  sendNotification("Log Scene: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Logs the active scene name, build index, and root object count."
            }), new ButtonInfo({
              buttonText: "UX: Instantiate Obj",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    const t = e && !e.handle.isNull() ? getTransform(e).method("get_position").invoke() : identityQuaternion;
                    const o = Object.method("Instantiate", 3).invoke(uxSelectedObject, t, identityQuaternion);
                    if (!o || o.isNull()) {
                      sendNotification("Instantiate returned null", false);
                      return;
                    }
                    const n = o.method("get_name").invoke()?.content ?? "clone";
                    sendNotification("Instantiated: " + n, false);
                    console.log("[UX] Instantiated: " + n);
                  } catch (e) {
                    sendNotification("Instantiate: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Instantiates a local clone of the selected object at your position."
            }), new ButtonInfo({
              buttonText: "UX: Instantiate Gun",
              isTogglable: true,
              method: () => {
                if (!uxSelectedObject || uxSelectedObject.isNull()) {
                  return;
                }
                if (!rightGrab) {
                  return;
                }
                const e = renderGun().ray;
                if (e && !e.handle.isNull() && rightTrigger && time > lagGunDelay) {
                  lagGunDelay = time + 0.4;
                  try {
                    const t = e.method("get_point").invoke();
                    const o = Object.method("Instantiate", 3).invoke(uxSelectedObject, t, identityQuaternion);
                    if (!o || o.isNull()) {
                      sendNotification("Instantiate returned null", false);
                      return;
                    }
                    sendNotification("Spawned clone at aim point", false);
                  } catch (e) {
                    sendNotification("Instantiate Gun: " + e, false);
                  }
                }
              },
              toolTip: "Hold grip + pull trigger to instantiate the selected object wherever you aim."
            }), new ButtonInfo({
              buttonText: "UX: Clone x5",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    const t = e && !e.handle.isNull() ? getTransform(e).method("get_position").invoke() : zeroVector;
                    let o = 0;
                    for (let e = 0; e < 5; e++) {
                      try {
                        const e = [t.field("x").value + (Math.random() - 0.5) * 4, t.field("y").value, t.field("z").value + (Math.random() - 0.5) * 4];
                        const n = Object.method("Instantiate", 3).invoke(uxSelectedObject, e, identityQuaternion);
                        if (n && !n.isNull()) {
                          o++;
                        }
                      } catch (e) {}
                    }
                    sendNotification("Cloned x" + o + " around you", false);
                  } catch (e) {
                    sendNotification("Clone x5: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Instantiates 5 copies of the selected object scattered around you."
            }), new ButtonInfo({
              buttonText: "UX: Destroy Obj",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = uxSelectedName;
                    let t = false;
                    try {
                      const e = uxSelectedObject.method("GetComponent", 1).inflate(GBOClass).invoke();
                      if (e && !e.isNull()) {
                        const o = e.method("get_Object").invoke();
                        if (o && !o.isNull()) {
                          SFXManager.field("_instance").value.method("get__currentRunner").invoke().method("Despawn").invoke(o);
                          t = true;
                        }
                      }
                    } catch (e) {}
                    if (!t) {
                      Object.method("Destroy", 1).invoke(uxSelectedObject);
                    }
                    uxSelectedObject = null;
                    uxSelectedName = "none";
                    sendNotification("Destroyed: " + e, false);
                  } catch (e) {
                    sendNotification("Destroy: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Destroys the selected object (networked despawn if possible, else local Destroy)."
            }), new ButtonInfo({
              buttonText: "UX: List AudioSources",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                  console.log("[UX-SFX] AudioSources in scene: " + t.length);
                  for (let e = 0; e < Math.min(t.length, 30); e++) {
                    try {
                      const o = t.get(e);
                      if (!o || o.isNull()) {
                        continue;
                      }
                      const n = o.method("get_gameObject").invoke().method("get_name").invoke()?.content ?? "?";
                      let i = "none";
                      try {
                        const e = o.method("get_clip").invoke();
                        if (e && !e.isNull()) {
                          i = e.method("get_name").invoke()?.content ?? "?";
                        }
                      } catch (e) {}
                      const a = o.method("get_isPlaying").invoke();
                      const l = o.method("get_volume").invoke().toFixed(2);
                      console.log("[UX-SFX]  [" + e + "] GO=" + n + " clip=" + i + " playing=" + a + " vol=" + l);
                    } catch (e) {}
                  }
                  sendNotification("AudioSources: " + t.length + " â€” see log", false, 5);
                } catch (e) {
                  sendNotification("List Audio: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Logs all AudioSource components in the scene to Frida console."
            }), new ButtonInfo({
              buttonText: "UX: Mute All SFX",
              isTogglable: true,
              enableMethod: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioListener");
                  try {
                    e.method("set_volume").invoke(0);
                  } catch (e) {}
                  const t = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                  const o = Object.method("FindObjectsByType", 1).inflate(t).invoke(0);
                  for (let e = 0; e < o.length; e++) {
                    try {
                      o.get(e).method("set_volume").invoke(0);
                    } catch (e) {}
                  }
                  sendNotification("All SFX muted", false);
                } catch (e) {
                  sendNotification("Mute SFX: " + e, false);
                }
              },
              disableMethod: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioListener");
                  try {
                    e.method("set_volume").invoke(1);
                  } catch (e) {}
                  const t = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                  const o = Object.method("FindObjectsByType", 1).inflate(t).invoke(0);
                  for (let e = 0; e < o.length; e++) {
                    try {
                      o.get(e).method("set_volume").invoke(1);
                    } catch (e) {}
                  }
                  sendNotification("All SFX unmuted", false);
                } catch (e) {
                  sendNotification("Unmute SFX: " + e, false);
                }
              },
              toolTip: "Mutes / unmutes all AudioSources in the scene."
            }), new ButtonInfo({
              buttonText: "UX: Stop All SFX",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                  let o = 0;
                  for (let e = 0; e < t.length; e++) {
                    try {
                      t.get(e).method("Stop").invoke();
                      o++;
                    } catch (e) {}
                  }
                  sendNotification("Stopped " + o + " AudioSources", false);
                } catch (e) {
                  sendNotification("Stop SFX: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Calls Stop() on every AudioSource in the scene."
            }), new ButtonInfo({
              buttonText: "UX: Play All SFX",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                  let o = 0;
                  for (let e = 0; e < t.length; e++) {
                    try {
                      t.get(e).method("Play").invoke();
                      o++;
                    } catch (e) {}
                  }
                  sendNotification("Playing " + o + " AudioSources", false);
                } catch (e) {
                  sendNotification("Play SFX: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Calls Play() on every AudioSource in the scene."
            }), new ButtonInfo({
              buttonText: "UX: SFX Pitch 0.5x",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                  let o = 0;
                  for (let e = 0; e < t.length; e++) {
                    try {
                      t.get(e).method("set_pitch").invoke(0.5);
                      o++;
                    } catch (e) {}
                  }
                  sendNotification("Pitch â†’ 0.5x on " + o + " sources (slow-mo SFX)", false);
                } catch (e) {
                  sendNotification("Pitch 0.5x: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all AudioSource pitches to 0.5 (slow-motion sound effect)."
            }), new ButtonInfo({
              buttonText: "UX: SFX Pitch 2x",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                  let o = 0;
                  for (let e = 0; e < t.length; e++) {
                    try {
                      t.get(e).method("set_pitch").invoke(2);
                      o++;
                    } catch (e) {}
                  }
                  sendNotification("Pitch â†’ 2x on " + o + " sources (chipmunk SFX)", false);
                } catch (e) {
                  sendNotification("Pitch 2x: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all AudioSource pitches to 2.0 (chipmunk/fast sound)."
            }), new ButtonInfo({
              buttonText: "UX: SFX Pitch Reset",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                  let o = 0;
                  for (let e = 0; e < t.length; e++) {
                    try {
                      t.get(e).method("set_pitch").invoke(1);
                      o++;
                    } catch (e) {}
                  }
                  sendNotification("Pitch reset on " + o + " sources", false);
                } catch (e) {
                  sendNotification("Pitch Reset: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Resets all AudioSource pitches to 1.0 (normal)."
            }), new ButtonInfo({
              buttonText: "UX: Vol All 0.1",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                  for (let e = 0; e < t.length; e++) {
                    try {
                      t.get(e).method("set_volume").invoke(0.1);
                    } catch (e) {}
                  }
                  sendNotification("Volume â†’ 0.1 on all sources (whisper)", false);
                } catch (e) {
                  sendNotification("Vol 0.1: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all AudioSource volumes to 0.1 (nearly silent)."
            }), new ButtonInfo({
              buttonText: "UX: Vol All Max",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                  for (let e = 0; e < t.length; e++) {
                    try {
                      t.get(e).method("set_volume").invoke(1);
                    } catch (e) {}
                  }
                  sendNotification("Volume â†’ 1.0 on all sources", false);
                } catch (e) {
                  sendNotification("Vol Max: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all AudioSource volumes to 1.0 (maximum)."
            }), new ButtonInfo({
              buttonText: "UX: Swap Clip Random",
              method: () => {
                try {
                  const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                  const t = Object.method("FindObjectsByType", 1).inflate(e).invoke(0);
                  if (!t || t.length === 0) {
                    sendNotification("No AudioSources found", false);
                    return;
                  }
                  let o = null;
                  let n = "?";
                  for (let e = 0; e < t.length; e++) {
                    try {
                      const i = t.get(e);
                      if (!i || i.isNull()) {
                        continue;
                      }
                      const a = i.method("get_clip").invoke();
                      if (a && !a.isNull()) {
                        o = a;
                        n = a.method("get_name").invoke()?.content ?? "?";
                        break;
                      }
                    } catch (e) {}
                  }
                  if (!o) {
                    sendNotification("No AudioClip found to copy", false);
                    return;
                  }
                  let i = 0;
                  for (let e = 0; e < t.length; e++) {
                    try {
                      const n = t.get(e);
                      if (!n || n.isNull()) {
                        continue;
                      }
                      n.method("set_clip").invoke(o);
                      n.method("Play").invoke();
                      i++;
                    } catch (e) {}
                  }
                  sendNotification("Swapped all to clip: " + n + " (" + i + " sources)", false);
                } catch (e) {
                  sendNotification("Swap Clip: " + e, false);
                }
              },
              isTogglable: false,
              toolTip: "Copies the first found AudioClip onto all AudioSources and plays them."
            }), new ButtonInfo({
              buttonText: "UX: Log Obj Clips",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(e).invoke();
                    if (!t || t.isNull() || t.length === 0) {
                      sendNotification("No AudioSources on " + uxSelectedName, false);
                      return;
                    }
                    console.log("[UX-SFX] AudioSources on " + uxSelectedName + " (" + t.length + "):");
                    for (let e = 0; e < t.length; e++) {
                      try {
                        const o = t.get(e);
                        if (!o || o.isNull()) {
                          continue;
                        }
                        let n = "none";
                        try {
                          const e = o.method("get_clip").invoke();
                          if (e && !e.isNull()) {
                            n = e.method("get_name").invoke()?.content ?? "?";
                          }
                        } catch (e) {}
                        const i = o.method("get_volume").invoke().toFixed(2);
                        const a = o.method("get_pitch").invoke().toFixed(2);
                        const l = o.method("get_loop").invoke();
                        console.log("[UX-SFX]   [" + e + "] clip=" + n + " vol=" + i + " pitch=" + a + " loop=" + l);
                      } catch (e) {}
                    }
                    sendNotification(t.length + " AudioSources logged â€” see Frida log", false, 4);
                  } catch (e) {
                    sendNotification("Log Clips: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Logs all AudioSource clips/volume/pitch on the selected object to Frida console."
            }), new ButtonInfo({
              buttonText: "UX: Play Obj SFX",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(e).invoke();
                    let o = 0;
                    if (t && !t.isNull()) {
                      for (let e = 0; e < t.length; e++) {
                        try {
                          t.get(e).method("Play").invoke();
                          o++;
                        } catch (e) {}
                      }
                    }
                    sendNotification("Played " + o + " AudioSources on " + uxSelectedName, false);
                  } catch (e) {
                    sendNotification("Play Obj SFX: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Plays all AudioSources on the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Mute Obj SFX",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.AudioModule").image.class("UnityEngine.AudioSource");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(e).invoke();
                    let o = 0;
                    if (t && !t.isNull()) {
                      for (let e = 0; e < t.length; e++) {
                        try {
                          const n = t.get(e);
                          n.method("Stop").invoke();
                          n.method("set_volume").invoke(0);
                          o++;
                        } catch (e) {}
                      }
                    }
                    sendNotification("Muted " + o + " AudioSources on " + uxSelectedName, false);
                  } catch (e) {
                    sendNotification("Mute Obj SFX: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Stops and mutes all AudioSources on the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Log Textures",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(e).invoke();
                    if (!t || t.isNull() || t.length === 0) {
                      sendNotification("No renderers on " + uxSelectedName, false);
                      return;
                    }
                    console.log("[UX-Tex] Renderers on " + uxSelectedName + " (" + t.length + "):");
                    for (let e = 0; e < t.length; e++) {
                      try {
                        const o = t.get(e);
                        if (!o || o.isNull()) {
                          continue;
                        }
                        const n = o.method("get_material").invoke();
                        if (!n || n.isNull()) {
                          continue;
                        }
                        const i = n.method("get_name").invoke()?.content ?? "?";
                        let a = "none";
                        try {
                          const e = n.method("get_mainTexture").invoke();
                          if (e && !e.isNull()) {
                            a = e.method("get_name").invoke()?.content ?? "?";
                          }
                        } catch (e) {}
                        const l = n.method("get_shader").invoke();
                        const r = l ? l.method("get_name").invoke()?.content ?? "?" : "?";
                        console.log("[UX-Tex]   [" + e + "] mat=" + i + " tex=" + a + " shader=" + r);
                      } catch (e) {}
                    }
                    sendNotification(t.length + " renderers logged â€” see Frida log", false, 4);
                  } catch (e) {
                    sendNotification("Log Textures: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Logs all renderer materials, textures, and shaders on the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Tex Invisible",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(e).invoke();
                    if (!t || t.isNull()) {
                      sendNotification("No renderers", false);
                      return;
                    }
                    for (let e = 0; e < t.length; e++) {
                      try {
                        t.get(e).method("set_enabled").invoke(false);
                      } catch (e) {}
                    }
                    sendNotification(uxSelectedName + " â†’ invisible (renderers off)", false);
                  } catch (e) {
                    sendNotification("Tex Invisible: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Disables all renderers on the selected object â€” makes it invisible."
            }), new ButtonInfo({
              buttonText: "UX: Tex Visible",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(e).invoke();
                    if (!t || t.isNull()) {
                      sendNotification("No renderers", false);
                      return;
                    }
                    for (let e = 0; e < t.length; e++) {
                      try {
                        t.get(e).method("set_enabled").invoke(true);
                      } catch (e) {}
                    }
                    sendNotification(uxSelectedName + " â†’ visible (renderers on)", false);
                  } catch (e) {
                    sendNotification("Tex Visible: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Enables all renderers on the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Tex Set Unlit",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Shader.method("Find").invoke(Il2Cpp.string("Universal Render Pipeline/Unlit"));
                    const t = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const o = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(t).invoke();
                    let n = 0;
                    if (o && !o.isNull()) {
                      for (let t = 0; t < o.length; t++) {
                        try {
                          const i = o.get(t).method("get_material").invoke();
                          if (i && !i.isNull()) {
                            i.method("set_shader").invoke(e);
                            n++;
                          }
                        } catch (e) {}
                      }
                    }
                    sendNotification(uxSelectedName + " â†’ Unlit shader (" + n + " mats)", false);
                  } catch (e) {
                    sendNotification("Set Unlit: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Switches all materials on the selected object to the URP Unlit shader."
            }), new ButtonInfo({
              buttonText: "UX: Tex Swap Black",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(e).invoke();
                    let o = 0;
                    if (t && !t.isNull()) {
                      for (let e = 0; e < t.length; e++) {
                        try {
                          const n = t.get(e).method("get_material").invoke();
                          if (n && !n.isNull()) {
                            n.method("set_color").invoke([0, 0, 0, 1]);
                            o++;
                          }
                        } catch (e) {}
                      }
                    }
                    sendNotification(uxSelectedName + " â†’ black (" + o + " mats)", false);
                  } catch (e) {
                    sendNotification("Tex Swap Black: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all material colors to black on the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Tex Swap White",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(e).invoke();
                    let o = 0;
                    if (t && !t.isNull()) {
                      for (let e = 0; e < t.length; e++) {
                        try {
                          const n = t.get(e).method("get_material").invoke();
                          if (n && !n.isNull()) {
                            n.method("set_color").invoke([1, 1, 1, 1]);
                            o++;
                          }
                        } catch (e) {}
                      }
                    }
                    sendNotification(uxSelectedName + " â†’ white (" + o + " mats)", false);
                  } catch (e) {
                    sendNotification("Tex Swap White: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all material colors to white on the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Tex Swap Yellow",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(e).invoke();
                    let o = 0;
                    if (t && !t.isNull()) {
                      for (let e = 0; e < t.length; e++) {
                        try {
                          const n = t.get(e).method("get_material").invoke();
                          if (n && !n.isNull()) {
                            n.method("set_color").invoke([1, 0.92, 0.016, 1]);
                            o++;
                          }
                        } catch (e) {}
                      }
                    }
                    sendNotification(uxSelectedName + " â†’ yellow (" + o + " mats)", false);
                  } catch (e) {
                    sendNotification("Tex Swap Yellow: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Sets all material colors to yellow on the selected object."
            }), new ButtonInfo({
              buttonText: "UX: Tex Rainbow Loop",
              isTogglable: true,
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull() && frameCount % 8 == 0) {
                  try {
                    const e = time * 0.5 % 1;
                    const t = Math.floor(e * 6);
                    const o = e * 6 - t;
                    const n = 1 - o;
                    let i = 1;
                    let a = 1;
                    let l = 1;
                    switch (t % 6) {
                      case 0:
                        i = 1;
                        a = o;
                        l = 0;
                        break;
                      case 1:
                        i = n;
                        a = 1;
                        l = 0;
                        break;
                      case 2:
                        i = 0;
                        a = 1;
                        l = o;
                        break;
                      case 3:
                        i = 0;
                        a = n;
                        l = 1;
                        break;
                      case 4:
                        i = o;
                        a = 0;
                        l = 1;
                        break;
                      case 5:
                        i = 1;
                        a = 0;
                        l = n;
                    }
                    const r = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const s = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(r).invoke();
                    if (s && !s.isNull()) {
                      for (let e = 0; e < s.length; e++) {
                        try {
                          const t = s.get(e).method("get_material").invoke();
                          if (t && !t.isNull()) {
                            t.method("set_color").invoke([i, a, l, 1]);
                          }
                        } catch (e) {}
                      }
                    }
                  } catch (e) {}
                }
              },
              toolTip: "Continuously cycles the selected object's material color through the rainbow (toggle)."
            }), new ButtonInfo({
              buttonText: "UX: Tex Set Emissive",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(e).invoke();
                    let o = 0;
                    if (t && !t.isNull()) {
                      for (let e = 0; e < t.length; e++) {
                        try {
                          const n = t.get(e).method("get_material").invoke();
                          if (!n || n.isNull()) {
                            continue;
                          }
                          try {
                            n.method("EnableKeyword").invoke(Il2Cpp.string("_EMISSION"));
                          } catch (e) {}
                          try {
                            n.method("SetColor").invoke(Il2Cpp.string("_EmissionColor"), [2, 2, 2, 1]);
                          } catch (e) {}
                          o++;
                        } catch (e) {}
                      }
                    }
                    sendNotification(uxSelectedName + " â†’ emissive white (" + o + " mats)", false);
                  } catch (e) {
                    sendNotification("Set Emissive: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Enables emission on all materials of the selected object (glowing white)."
            }), new ButtonInfo({
              buttonText: "UX: Tex Clear Tex",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const t = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(e).invoke();
                    let o = 0;
                    if (t && !t.isNull()) {
                      for (let e = 0; e < t.length; e++) {
                        try {
                          const n = t.get(e).method("get_material").invoke();
                          if (!n || n.isNull()) {
                            continue;
                          }
                          const i = Il2Cpp.reference(Il2Cpp.domain.assembly("mscorlib").image.class("System.Object").alloc());
                          try {
                            n.method("set_mainTexture").invoke(i);
                          } catch (e) {}
                          o++;
                        } catch (e) {}
                      }
                    }
                    sendNotification(uxSelectedName + " textures cleared (" + o + " mats)", false);
                  } catch (e) {
                    sendNotification("Clear Tex: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Clears the mainTexture on all materials â€” shows flat color only."
            }), new ButtonInfo({
              buttonText: "UX: Tex From Near",
              method: () => {
                if (uxSelectedObject && !uxSelectedObject.isNull()) {
                  try {
                    const e = NetPlayer.method("get_localPlayer").invoke();
                    if (!e || e.handle.isNull()) {
                      return;
                    }
                    const t = getTransform(e).method("get_position").invoke();
                    const o = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
                    const n = Object.method("FindObjectsByType", 1).inflate(o).invoke(0);
                    let i = null;
                    let a = "?";
                    for (let e = 0; e < n.length; e++) {
                      try {
                        const o = n.get(e);
                        if (!o || o.isNull()) {
                          continue;
                        }
                        const l = o.method("get_gameObject").invoke();
                        if (l && !l.isNull() && l.handle.equals(uxSelectedObject.handle)) {
                          continue;
                        }
                        if (Vector3.method("Distance").invoke(t, uxGetTransform(l).method("get_position").invoke()) > 8) {
                          continue;
                        }
                        const r = o.method("get_material").invoke();
                        if (!r || r.isNull()) {
                          continue;
                        }
                        const s = r.method("get_mainTexture").invoke();
                        if (s && !s.isNull()) {
                          i = s;
                          a = s.method("get_name").invoke()?.content ?? "?";
                          break;
                        }
                      } catch (e) {}
                    }
                    if (!i) {
                      sendNotification("No texture found within 8m", false);
                      return;
                    }
                    const l = uxSelectedObject.method("GetComponentsInChildren", 0).inflate(o).invoke();
                    let r = 0;
                    if (l && !l.isNull()) {
                      for (let e = 0; e < l.length; e++) {
                        try {
                          const t = l.get(e).method("get_material").invoke();
                          if (t && !t.isNull()) {
                            t.method("set_mainTexture").invoke(i);
                            r++;
                          }
                        } catch (e) {}
                      }
                    }
                    sendNotification("Applied tex '" + a + "' to " + r + " mats on " + uxSelectedName, false);
                  } catch (e) {
                    sendNotification("Tex From Near: " + e, false);
                  }
                } else {
                  sendNotification("No object selected", false);
                }
              },
              isTogglable: false,
              toolTip: "Steals the texture from the nearest object within 8m and applies it to the selected object."
            })]];
            let buttonMap = new Map();
            function rebuildButtonMap() {
              buttonMap = new Map();
              buttons.flat().forEach(e => {
                buttonMap.set(e.buttonText, e);
              });
            }
            function getIndex(e) {
              let t = buttonMap.get(e);
              if (!t) {
                rebuildButtonMap();
                t = buttonMap.get(e);
              }
              return t;
            }
            function getVisibleMenuButton(e) {
              try {
                const t = buttons[currentCategory] || [];
                const o = currentCategory === 44;
                const n = currentCategory === 80;
                const i = (o || n || currentCategory === 103 ? t.filter(e => !e.buttonText.startsWith(o ? "Search: " : n ? "Code: " : "Text: ")) : t.slice(currentPage * getButtonsPerPage(), (currentPage + 1) * getButtonsPerPage())).find(t => t.buttonText === e);
                if (i) {
                  return i;
                }
              } catch (e) {}
              return getIndex(e);
            }
            rebuildButtonMap();
            const ButtonActivation = GorillaReportButton.method("OnTriggerEnter");
            function updateInput() {
              const e = InputDevices.method("GetDeviceAtXRNode", 1).invoke(4);
              const t = InputDevices.method("GetDeviceAtXRNode", 1).invoke(5);
              const o = Il2Cpp.alloc(1);
              const n = Il2Cpp.alloc(8);
              function i(e) {
                try {
                  const t = t => {
                    try {
                      n.writeFloat(0);
                      n.add(4).writeFloat(0);
                      if (e.method("TryGetFeatureValue", 2).invoke(t, n)) {
                        return [axisDeadzone(n.readFloat()), axisDeadzone(n.add(4).readFloat())];
                      }
                    } catch (e) {}
                    return [0, 0];
                  };
                  const o = [];
                  try {
                    o.push(t(CommonUsages.field("primary2DAxis").value));
                  } catch (e) {}
                  try {
                    o.push(t(CommonUsages.field("secondary2DAxis").value));
                  } catch (e) {}
                  try {
                    o.push(t(CommonUsages.field("thumbstick").value));
                  } catch (e) {}
                  let i = [0, 0];
                  let a = 0;
                  for (const e of o) {
                    const t = Math.abs(e[0]) + Math.abs(e[1]);
                    if (t > a) {
                      i = e;
                      a = t;
                    }
                  }
                  return i;
                } catch (e) {}
                return [0, 0];
              }
              function a(e, t) {
                try {
                  o.writeU8(0);
                  return !!e.method("TryGetFeatureValue", 2).invoke(CommonUsages.field(t).value, o) && o.readU8() !== 0;
                } catch (e) {
                  return false;
                }
              }
              leftPrimary = a(e, "primaryButton");
              leftSecondary = a(e, "secondaryButton");
              leftGrab = a(e, "gripButton");
              leftTrigger = a(e, "triggerButton");
              leftStick = a(e, "primary2DAxisClick");
              rightPrimary = a(t, "primaryButton");
              rightSecondary = a(t, "secondaryButton");
              rightTrigger = a(t, "triggerButton");
              rightGrab = a(t, "gripButton");
              rightStick = a(t, "primary2DAxisClick");
              [leftStickX, leftStickY] = i(e);
              [rightStickX, rightStickY] = i(t);
            }
            function getPcPlayerRigidbody() {
              try {
                const e = GTPlayer.field("_playerRigidBody").value;
                if (e && !e.isNull?.()) {
                  return e;
                }
              } catch (e) {}
              try {
                const e = getComponent(GTPlayer.method("get_gameObject").invoke(), Rigidbody);
                if (e && !e.isNull?.()) {
                  return e;
                }
              } catch (e) {}
              return null;
            }
            function activatePcMenuButton(e) {
              try {
                playMenuClickSound();
                const t = currentCategory;
                if (e.isTogglable) {
                  e.enabled = !e.enabled;
                  if (e.enabled) {
                    e.enableMethod?.();
                  } else {
                    e.disableMethod?.();
                  }
                } else {
                  e.method?.();
                }
                if (currentCategory !== t) {
                  startMenuTabFade();
                }
                if (e.toolTip) {
                  sendNotification((e.isTogglable ? e.enabled ? "[ENABLE] " : "[DISABLE] " : "") + e.toolTip, false);
                }
              } catch (t) {
                console.error(`[PC Button] ${e.buttonText}:`, t);
              }
              reloadMenu();
            }
            function getPcMenuButtonUnderCursor() {
              if (!menu || menu.isNull?.() || !_GCP || !_GFW || !_S2C || !_GCR) {
                return null;
              }
              try {
                const e = _GFW();
                if (!e || e.isNull?.()) {
                  return null;
                }
                if (!_GCP(_pcPoint) || !_S2C(e, _pcPoint) || !_GCR(e, _pcClientRect)) {
                  return null;
                }
                const t = _pcPoint.readS32();
                const o = _pcPoint.add(4).readS32();
                const n = _pcClientRect.add(8).readS32();
                const i = _pcClientRect.add(12).readS32();
                if (t < 0 || o < 0 || t >= n || o >= i) {
                  return null;
                }
                const a = Camera.method("get_main").invoke();
                if (!a || a.isNull?.()) {
                  return null;
                }
                const l = a.method("ScreenPointToRay", 1).invoke([t, i - o, 0]);
                const r = l.method("get_origin").invoke();
                const s = l.method("get_direction").invoke();
                const c = Physics.method("RaycastAll").overload("UnityEngine.Vector3", "UnityEngine.Vector3", "System.Single", "System.Int32").invoke(r, s, 32, -1);
                if (!c || c.isNull?.()) {
                  return null;
                }
                const d = getTransform(menu);
                let u = null;
                let m = Number.POSITIVE_INFINITY;
                for (let e = 0; e < c.length; e++) {
                  try {
                    const t = c.get(e);
                    const o = t.method("get_collider").invoke();
                    if (!o || o.isNull?.()) {
                      continue;
                    }
                    const n = o.method("get_transform").invoke();
                    if (!n || n.isNull?.() || !n.method("IsChildOf").invoke(d)) {
                      continue;
                    }
                    const i = o.method("get_gameObject").invoke();
                    const a = (i.method("get_name").invoke()?.content ?? "").toString();
                    if (!a.startsWith("@")) {
                      continue;
                    }
                    const l = t.method("get_distance").invoke();
                    if (l < m) {
                      m = l;
                      u = a.slice(1);
                    }
                  } catch (e) {}
                }
                return u;
              } catch (e) {
                console.error("[PC Menu Mouse]", e);
                return null;
              }
            }
            function pollPcModeHotkeys() {
              initPcWin32Input();
              if (!_GAKS) {
                return false;
              }
              const e = _pcKeyDown(_VK_Q);
              if (e && !_pcQWasDown) {
                if (_pcMode) {
                  _pcMenuOpen = !_pcMenuOpen;
                  _pcMenuSelector = 0;
                  rightGrab = false;
                  rightTrigger = false;
                } else {
                  _pcMode = true;
                  _pcMenuOpen = true;
                  _pcFlyEnabled = true;
                  _pcFlyVel = [0, 0, 0];
                  _pcLastCursorX = -1;
                  syteLog("PC Mode ON  Q=toggle menu  WASD+RMB=fly  Esc=exit");
                }
              }
              _pcQWasDown = e;
              const t = _pcKeyDown(_VK_ESCAPE);
              if (t && !_pcEscWasDown && _pcMode) {
                _pcMode = false;
                _pcMenuOpen = false;
                _pcFlyEnabled = false;
                _pcFlyVel = [0, 0, 0];
                rightGrab = false;
                rightTrigger = false;
                try {
                  const e = getPcPlayerRigidbody();
                  e?.method("set_useGravity").invoke(true);
                } catch (e) {}
                syteLog("PC Mode OFF");
              }
              _pcEscWasDown = t;
              return true;
            }
            function updatePcMode() {
              if (!pollPcModeHotkeys()) {
                return;
              }
              if (!_pcMode) {
                return;
              }
              const e = _pcKeyDown(_VK_RBUTTON);
              const t = _pcKeyDown(_VK_LBUTTON);
              if (!_pcMenuOpen) {
                rightGrab = e;
                rightTrigger = t;
              }
              if (e && _GCP) {
                try {
                  _GCP(_pcPoint);
                  const e = _pcPoint.readS32();
                  const t = _pcPoint.add(4).readS32();
                  if (_pcLastCursorX >= 0) {
                    _pcYaw = (_pcYaw + (e - _pcLastCursorX) * _pcMouseSensitivity) % 360;
                    _pcPitch = Math.max(-89, Math.min(89, _pcPitch - (t - _pcLastCursorY) * _pcMouseSensitivity));
                  }
                  _pcLastCursorX = e;
                  _pcLastCursorY = t;
                } catch (e) {}
              } else {
                _pcLastCursorX = -1;
              }
              _pcSmoothYaw = lerp(_pcSmoothYaw, _pcYaw, Math.min(1, deltaTime * 18));
              _pcSmoothPitch = lerp(_pcSmoothPitch, _pcPitch, Math.min(1, deltaTime * 18));
              try {
                getTransform(headCollider).method("set_rotation").invoke(Quaternion.method("Euler").invoke(_pcSmoothPitch, _pcSmoothYaw, 0));
              } catch (e) {}
              if (_pcFlyEnabled && !_pcMenuOpen) {
                try {
                  const e = _pcSmoothYaw * Math.PI / 180;
                  const t = _pcSmoothPitch * Math.PI / 180;
                  const o = Math.cos(t);
                  const n = [o * Math.sin(e), -Math.sin(t), o * Math.cos(e)];
                  const i = [Math.cos(e), 0, -Math.sin(e)];
                  let a = 0;
                  let l = 0;
                  let r = 0;
                  let s = false;
                  if (_pcKeyDown(_VK_W)) {
                    a += n[0];
                    l += n[1];
                    r += n[2];
                    s = true;
                  }
                  if (_pcKeyDown(_VK_S)) {
                    a -= n[0];
                    l -= n[1];
                    r -= n[2];
                    s = true;
                  }
                  if (_pcKeyDown(_VK_A)) {
                    a -= i[0];
                    r -= i[2];
                    s = true;
                  }
                  if (_pcKeyDown(_VK_D)) {
                    a += i[0];
                    r += i[2];
                    s = true;
                  }
                  if (_pcKeyDown(_VK_SPACE_PC)) {
                    l += 1;
                    s = true;
                  }
                  if (_pcKeyDown(_VK_SHIFT_PC)) {
                    l -= 1;
                    s = true;
                  }
                  const c = Math.sqrt(a * a + l * l + r * r) || 1;
                  const d = s ? [a / c * _pcFlySpeed, l / c * _pcFlySpeed, r / c * _pcFlySpeed] : [0, 0, 0];
                  const u = Math.min(1, deltaTime * (s ? _pcFlyAcceleration : _pcFlyDeceleration));
                  _pcFlyVel = [lerp(_pcFlyVel[0], d[0], u), lerp(_pcFlyVel[1], d[1], u), lerp(_pcFlyVel[2], d[2], u)];
                  const m = getPcPlayerRigidbody();
                  if (m) {
                    m.method("set_useGravity").invoke(false);
                    setRigidbodyVelocitySafe(m, _pcFlyVel);
                  }
                } catch (e) {
                  console.error("[PC Fly]", e);
                }
              } else if (_pcFlyEnabled) {
                _pcFlyVel = [0, 0, 0];
                try {
                  const e = getPcPlayerRigidbody();
                  if (e) {
                    e.method("set_useGravity").invoke(true);
                    setRigidbodyVelocitySafe(e, [0, 0, 0]);
                  }
                } catch (e) {}
              }
              if (_pcMenuOpen) {
                const e = currentCategory === 44;
                const o = currentCategory === 80;
                const n = currentCategory === 103;
                const i = e || o || n;
                const a = e ? (buttons[44] || []).filter(e => !e.buttonText.startsWith("Search: ")) : o ? (buttons[80] || []).filter(e => !e.buttonText.startsWith("Code: ")) : n ? (buttons[103] || []).filter(e => !e.buttonText.startsWith("Text: ")) : [];
                const l = i ? [] : (buttons[currentCategory] || []).slice(currentPage * 8, (currentPage + 1) * 8);
                const r = i ? [] : [getIndex("Leave"), getIndex("Home"), getIndex("OpenSearch")].filter(e => e);
                const s = i ? a : l.concat(r);
                const c = s.length || 1;
                _pcMenuSelector = (_pcMenuSelector % c + c) % c;
                if (t && !_pcLmbMenuWasDown) {
                  const e = getPcMenuButtonUnderCursor();
                  const t = e ? getVisibleMenuButton(e) : null;
                  if (t) {
                    const e = s.indexOf(t);
                    if (e >= 0) {
                      _pcMenuSelector = e;
                    }
                    activatePcMenuButton(t);
                  }
                }
                _pcLmbMenuWasDown = t;
                const d = l.length;
                const u = d;
                const m = d + 1;
                const h = d + 2;
                const g = e => {
                  _pcMenuSelector = d <= 0 ? (_pcMenuSelector + e + c) % c : e < 0 ? _pcMenuSelector < d ? _pcMenuSelector === 0 ? m : _pcMenuSelector - 1 : _pcMenuSelector === m ? u : _pcMenuSelector === u ? h : d - 1 : _pcMenuSelector < d ? _pcMenuSelector === d - 1 ? h : _pcMenuSelector + 1 : _pcMenuSelector === h ? u : _pcMenuSelector === u ? m : 0;
                };
                const f = [{
                  start: 0,
                  length: 10
                }, {
                  start: 10,
                  length: 10
                }, {
                  start: 20,
                  length: 9
                }, {
                  start: 29,
                  length: 7
                }, {
                  start: 36,
                  length: e || n ? 5 : 4
                }];
                const p = () => {
                  let e = f.findIndex(e => _pcMenuSelector >= e.start && _pcMenuSelector < e.start + e.length);
                  if (e < 0) {
                    e = 0;
                  }
                  const t = f[e];
                  return {
                    rowIndex: e,
                    columnIndex: _pcMenuSelector - t.start,
                    row: t
                  };
                };
                const y = e => {
                  const t = p();
                  const o = (t.rowIndex + e + f.length) % f.length;
                  const n = f[o];
                  const i = t.row.length <= 1 || n.length <= 1 ? 0 : Math.round(t.columnIndex * (n.length - 1) / (t.row.length - 1));
                  _pcMenuSelector = n.start + i;
                };
                const b = e => {
                  const t = p();
                  const o = (t.columnIndex + e + t.row.length) % t.row.length;
                  _pcMenuSelector = t.row.start + o;
                };
                const _ = _pcKeyDown(_VK_UP_PC);
                if (_ && !_pcUpWasDown) {
                  if (i) {
                    y(-1);
                  } else {
                    g(-1);
                  }
                  reloadMenu();
                }
                _pcUpWasDown = _;
                const v = _pcKeyDown(_VK_DOWN_PC);
                if (v && !_pcDownWasDown) {
                  if (i) {
                    y(1);
                  } else {
                    g(1);
                  }
                  reloadMenu();
                }
                _pcDownWasDown = v;
                const T = _pcKeyDown(_VK_RETURN_PC) || _pcKeyDown(_VK_SPACE_PC);
                if (T && !_pcEnterWasDown) {
                  const e = s[_pcMenuSelector];
                  if (e) {
                    activatePcMenuButton(e);
                  }
                }
                _pcEnterWasDown = T;
                const k = _pcKeyDown(_VK_LEFT_PC);
                if (k && !_pcLeftWasDown) {
                  if (i) {
                    b(-1);
                  } else if (_pcMenuSelector === u || _pcMenuSelector === m) {
                    _pcMenuSelector = _pcMenuSelector === u ? m : u;
                  } else {
                    const e = Math.max(Math.ceil((buttons[currentCategory] || []).length / 8) - 1, 0);
                    currentPage = currentPage <= 0 ? e : currentPage - 1;
                    _pcMenuSelector = 0;
                  }
                  reloadMenu();
                }
                _pcLeftWasDown = k;
                const N = _pcKeyDown(_VK_RIGHT_PC);
                if (N && !_pcRightWasDown) {
                  if (i) {
                    b(1);
                  } else if (_pcMenuSelector === u || _pcMenuSelector === m) {
                    _pcMenuSelector = _pcMenuSelector === u ? m : u;
                  } else {
                    const e = Math.max(Math.ceil((buttons[currentCategory] || []).length / 8) - 1, 0);
                    currentPage = currentPage >= e ? 0 : currentPage + 1;
                    _pcMenuSelector = 0;
                  }
                  reloadMenu();
                }
                _pcRightWasDown = N;
              } else {
                _pcLmbMenuWasDown = t;
              }
            }
            ButtonActivation.implementation = function (e) {
              const t = this.method("get_name").invoke().toString();
              if (!(t.length > 1) || t[1] != "@") {
                return this.method("OnTriggerEnter").invoke(e);
              }
              if (e.handle.equals(referenceCollider.handle)) {
                const e = t.substring(2, t.length - 1);
                const o = Time.method("get_time").invoke();
                if (o > buttonClickDelay) {
                  buttonClickDelay = o + 0.2;
                  playMenuClickSound();
                  if (e === "Discord" || t.indexOf("Discord") >= 0) {
                    try {
                      Application.method("OpenURL").invoke(Il2Cpp.string("https://discord.gg/GkrXvhPy92"));
                    } catch (e) {
                      console.error("[Discord] OpenURL failed:", e);
                    }
                    return;
                  }
                  const n = getVisibleMenuButton(e);
                  if (n) {
                    if (n.isTogglable) {
                      n.enabled = !n.enabled;
                      reloadMenu();
                      if (n.enabled) {
                        if (n.toolTip) {
                          sendNotification("[ENABLE] " + n.toolTip, false);
                        }
                        try {
                          n.enableMethod?.();
                        } catch (e) {
                          n.enabled = false;
                          console.error(`[Toggle Enable] ${n.buttonText}:`, e);
                        }
                      } else {
                        if (n.toolTip) {
                          sendNotification("[DISABLE] " + n.toolTip, false);
                        }
                        try {
                          n.disableMethod?.();
                        } catch (e) {
                          console.error(`[Toggle Disable] ${n.buttonText}:`, e);
                        }
                      }
                    } else {
                      const e = currentCategory;
                      try {
                        n.method?.();
                      } catch (e) {
                        console.error(`[Button] ${n.buttonText}:`, e);
                      }
                      if (currentCategory !== e) {
                        startMenuTabFade();
                      }
                      reloadMenu();
                      if (n.toolTip) {
                        sendNotification(n.toolTip, false);
                      }
                    }
                  }
                }
              }
            };
            let LateUpdate = null;
            const updateHookCandidates = [{
              klass: PCClass,
              label: "PlayerController",
              names: ["LateUpdate", "Update"]
            }, {
              klass: GTPlayerClass,
              label: "GorillaLocomotion",
              names: ["LateUpdate", "Update"]
            }, {
              klass: NetPlayer,
              label: "NetPlayer",
              names: ["LateUpdate", "Update"]
            }, {
              klass: PCClass,
              label: "PlayerController",
              names: ["FixedUpdate", "OnUpdate", "Tick", "PlayerUpdate", "LocomotionUpdate"]
            }, {
              klass: GTPlayerClass,
              label: "GorillaLocomotion",
              names: ["FixedUpdate", "OnUpdate", "Tick", "PlayerUpdate", "LocomotionUpdate"]
            }, {
              klass: NetPlayer,
              label: "NetPlayer",
              names: ["FixedUpdate", "OnUpdate", "Tick", "PlayerUpdate", "LocomotionUpdate"]
            }];
            for (const e of updateHookCandidates) {
              for (const t of e.names) {
                try {
                  LateUpdate = e.klass.method(t);
                } catch (e) {}
              }
              if (LateUpdate) {
                break;
              }
            }
            if (!LateUpdate) {
              console.error("[hook] No update method found anywhere, menu will not tick");
            }
            if (LateUpdate) {
              LateUpdate.implementation = function () {
                try {
                  pollPcModeHotkeys();
                  if (!refreshRuntimeRefs()) {
                    runtimeRefMissCount++;
                    if (runtimeRefMissCount === 1 || runtimeRefMissCount % 120 == 0) {
                      console.warn("[LateUpdate] Runtime refs unavailable; keeping cached state and skipping this frame");
                    }
                    return LateUpdate.invoke();
                  }
                  runtimeRefMissCount = 0;
                  deltaTime = Time.method("get_deltaTime").invoke();
                  time = Time.method("get_time").invoke();
                  try {
                    finishPendingSoundLoad();
                    ensureMenuClickSoundLoaded();
                    updateMenuClickSoundLoad();
                    updateAssetBundles();
                    if (isPlayingSound && time >= soundEndTime) {
                      stopSound();
                    }
                    cleanupRetiredSoundClips();
                  } catch (e) {
                    console.error("[Soundboard update]", e);
                  }
                  try {
                    updateHudNotifications();
                  } catch (e) {
                    console.error("[HUD Notifications]", e);
                  }
                  stabilizePinnedVfxObjects();
                  if (!buttons[89] || buttons[89].length !== itemIDs.length + 1) {
                    buttons[89] = [new ButtonInfo({
                      buttonText: "Exit Easy Item Spawning",
                      method: () => {
                        currentCategory = 0;
                        currentPage = 0;
                      },
                      isTogglable: false,
                      toolTip: "Returns to main menu."
                    })].concat(itemIDs.map(e => new ButtonInfo({
                      buttonText: e,
                      isTogglable: true,
                      method: () => {
                        try {
                          if (!rightGrab) {
                            return;
                          }
                          if (time < _snowballDelay) {
                            return;
                          }
                          _snowballDelay = time + 0.02;
                          const t = rightHandTransform.method("get_position").invoke();
                          const o = rightHandTransform.method("get_forward").invoke();
                          const n = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string(e), t, identityQuaternion, NULL);
                          if (!n || n.handle.isNull()) {
                            return;
                          }
                          const i = n.method("GetComponent", 1).inflate(GBOClass).invoke();
                          if (i && !i.isNull()) {
                            try {
                              i.method("set_colorHue").invoke(Math.floor(Math.random() * 240) - 120);
                            } catch (e) {}
                            try {
                              i.method("set_colorSaturation").invoke(Math.floor(Math.random() * 254) - 127);
                            } catch (e) {}
                            try {
                              i.method("set_scaleModifier").invoke(127);
                            } catch (e) {}
                            try {
                              const e = vec3Mul(o, 100);
                              i.method("AddExternalForceVelocity", 1).invoke(e);
                            } catch (e) {}
                          }
                        } catch (e) {}
                      },
                      toolTip: "Hold right grip and the trigger buton thing to spawn " + e
                    })));
                  }
                  menuAnimTime += deltaTime;
                  if (themeMode === 0) {
                    const e = (Math.sin(menuAnimTime * 2.5) * 0.5 + 0.5) * 0.12;
                    bgColor = [0.02, 0.05 + e * 0.3, 0.25 + e, 0.85];
                    buttonColor = [0.1 + e * 0.4, 0.18 + e * 0.6, 0.55 + e, 0.85];
                    textColor = [0.8, 0.88 + e * 0.4, 1, 1];
                  } else if (themeMode === 1) {
                    const e = menuAnimTime * 0.25 % 1;
                    const t = Math.abs(Math.sin(e * Math.PI * 2));
                    const o = Math.abs(Math.sin((e + 0.333) * Math.PI * 2));
                    const n = Math.abs(Math.sin((e + 0.666) * Math.PI * 2));
                    const i = (Math.sin(menuAnimTime * 3) * 0.5 + 0.5) * 0.06;
                    bgColor = [t * 0.18 + i * 0.03, o * 0.18 + i * 0.03, n * 0.18 + i * 0.03, 0.92];
                    buttonColor = [t * 0.55 + i, o * 0.55 + i, n * 0.55 + i, 0.88];
                    buttonPressedColor = [t, o, n, 1];
                    textColor = [0.95, 0.95, 0.95, 1];
                  } else if (themeMode === 2) {
                    const e = (Math.sin(menuAnimTime * 3) * 0.5 + 0.5) * 0.2;
                    bgColor = [0, 0.05 + e * 0.2, 0, 0.85];
                    buttonColor = [0, 0.25 + e, 0, 0.85];
                    textColor = [0.2, 1, 0.2, 1];
                  } else if (themeMode === 3) {
                    Math.sin(menuAnimTime * 2);
                    bgColor = [0.01, 0.01, 0.01, 0.96];
                    buttonColor = [0.22, 0.03, 0.03, 0.88];
                    buttonPressedColor = [0.01, 0.01, 0.01, 0.96];
                    textColor = [1, 0.3, 0.3, 1];
                  } else if (themeMode === 4) {
                    const e = (Math.sin(menuAnimTime * 2) * 0.5 + 0.5) * 0.1;
                    bgColor = [0.22 + e * 0.35, 0.08 + e * 0.18, 0, 0.9];
                    buttonColor = [0.9 + e * 0.5, 0.35 + e * 0.35, 0, 0.92];
                    buttonPressedColor = [1, 0.6 + e * 0.25, 0.08, 1];
                    textColor = [1, 0.9, 0.72, 1];
                  } else if (themeMode === 5) {
                    const e = (Math.sin(menuAnimTime * 3.4) * 0.5 + 0.5) * 0.18;
                    bgColor = [0.02, 0.12 + e * 0.18, 0, 0.9];
                    buttonColor = [0.28 + e * 0.35, 0.92, 0.02 + e * 0.14, 0.94];
                    buttonPressedColor = [0.74, 1, 0.18, 1];
                    textColor = [0.95, 1, 0.78, 1];
                  } else if (themeMode === 6) {
                    const e = (Math.sin(menuAnimTime * 2.8) * 0.5 + 0.5) * 0.18;
                    bgColor = [0.24 + e * 0.16, 0.04, 0.12 + e * 0.08, 0.9];
                    buttonColor = [1, 0.28 + e * 0.18, 0.52 + e * 0.18, 0.92];
                    buttonPressedColor = [1, 0.62, 0.26, 1];
                    textColor = [1, 0.92, 0.86, 1];
                  } else if (themeMode === 7) {
                    const e = (Math.sin(menuAnimTime * 3.1) * 0.5 + 0.5) * 0.16;
                    bgColor = [0, 0.1 + e * 0.08, 0.16 + e * 0.18, 0.9];
                    buttonColor = [0.12 + e * 0.12, 0.74 + e * 0.2, 1, 0.92];
                    buttonPressedColor = [0.72, 0.94, 1, 1];
                    textColor = [0.9, 0.99, 1, 1];
                  } else if (themeMode === 8) {
                    const e = (Math.sin(menuAnimTime * 2.1) * 0.5 + 0.5) * 0.12;
                    bgColor = [0.02 + e * 0.04, 0.02 + e * 0.04, 0.02 + e * 0.02, 0.95];
                    buttonColor = [0.66 + e * 0.28, 0.48 + e * 0.22, 0.12, 0.94];
                    buttonPressedColor = [1, 0.86, 0.28, 1];
                    textColor = [1, 0.94, 0.74, 1];
                  } else if (themeMode === 9) {
                    const e = (Math.sin(menuAnimTime * 3.6) * 0.5 + 0.5) * 0.18;
                    bgColor = [0.1 + e * 0.08, 0, 0.16 + e * 0.1, 0.9];
                    buttonColor = [0.74 + e * 0.18, 0.22 + e * 0.12, 1, 0.94];
                    buttonPressedColor = [0.94, 0.56, 1, 1];
                    textColor = [0.98, 0.92, 1, 1];
                  } else if (themeMode === 10) {
                    bgColor = [0.01, 0.01, 0.01, 0.96];
                    buttonColor = [0.01, 0.01, 0.01, 0.96];
                    buttonPressedColor = [0.56, 0.56, 0.56, 1];
                    textColor = [1, 0.94, 0.74, 1];
                  } else if (themeMode === 12) {
                    bgColor = [0.01, 0.01, 0.01, 0.96];
                    buttonColor = [0.56, 0.56, 0.56, 1];
                    buttonPressedColor = [0.01, 0.01, 0.01, 0.96];
                    textColor = [1, 0.94, 0.74, 1];
                  } else if (themeMode === 13) {
                    bgColor = [0.05, 0.05, 0.1, 1];
                    buttonColor = [0.1, 0.1, 0.2, 1];
                    buttonPressedColor = [0.4, 0.6, 1, 1];
                    textColor = [0.6, 0.8, 1, 1];
                  } else if (themeMode === 14) {
                    bgColor = [1, 0.5, 0, 1];
                    buttonColor = [0.666, 0.333, 0, 1];
                    buttonPressedColor = [0.333, 0.15, 0, 1];
                    textColor = [1, 0.7450981, 0.4901961, 1];
                  } else if (themeMode === 15) {
                    bgColor = [0.7, 0.9, 1, 1];
                    buttonColor = [0.5, 0.7, 0.9, 1];
                    buttonPressedColor = [0.2, 0.4, 0.8, 1];
                    textColor = [0.1, 0.2, 0.4, 1];
                  } else if (themeMode === 16) {
                    bgColor = [0.5, 0, 0.5, 1];
                    buttonColor = [0.25, 0, 0.25, 1];
                    buttonPressedColor = [0.7, 0, 0.7, 1];
                    textColor = [1, 0.9, 1, 1];
                  } else if (themeMode === 17) {
                    bgColor = [0.1, 0.45, 1, 0.96];
                    buttonColor = [1, 0.9, 0.1, 0.96];
                    buttonPressedColor = [0.05, 0.3, 0.75, 1];
                    textColor = [0.01, 0.01, 0.01, 0.96];
                  } else if (themeMode === 18) {
                    bgColor = [1, 1, 1, 0.96];
                    buttonColor = [0, 0, 0, 0.96];
                    buttonPressedColor = [0.7, 0.7, 0.7, 1];
                    textColor = [1, 1, 1, 1];
                  } else if (themeMode === 19) {
                    bgColor = [0.08, 0.08, 0.08, 1];
                    buttonColor = [0.12, 0.12, 0.12, 1];
                    buttonPressedColor = [1, 0.25, 0, 1];
                    textColor = [1, 0.45, 0, 1];
                  } else if (themeMode === 20) {
                    bgColor = [0.02, 0.02, 0.05, 0.97];
                    buttonColor = [0.08, 0.08, 0.15, 0.95];
                    buttonPressedColor = [0.4, 0.15, 0.85, 1];
                    textColor = [0.7, 0.85, 1, 1];
                    outlineColor = [0.6, 0.3, 1, 1];
                  } else if (themeMode === 21) {
                    bgColor = [0, 0, 0, 0.96];
                    buttonColor = [0.2, 0.2, 0.2, 0.96];
                    buttonPressedColor = [0, 0, 0, 1];
                    textColor = [1, 1, 1, 1];
                  } else if (themeMode === 22) {
                    bgColor = [0.025, 0.07, 0.1, 0.98];
                    buttonColor = [0.04, 0.16, 0.23, 0.96];
                    buttonPressedColor = [0.05, 0.38, 0.56, 0.98];
                    textColor = [1, 1, 1, 1];
                  }
                  if (pendingThemeReload) {
                    pendingThemeReload = false;
                    reloadMenu();
                  } else {
                    updateLiveMenuThemeVisuals();
                  }
                  for (let e = lockedItems.length - 1; e >= 0; e--) {
                    try {
                      const t = lockedItems[e];
                      if (!t || t.isNull()) {
                        lockedItems.splice(e, 1);
                        continue;
                      }
                      getTransform(t).method("set_position").invoke(t._lockedPos);
                    } catch (t) {
                      lockedItems.splice(e, 1);
                    }
                  }
                  for (let e = spawnedGoopObjects.length - 1; e >= 0; e--) {
                    try {
                      const t = spawnedGoopObjects[e];
                      if (!t || !t.object || t.object.isNull?.() || time >= t.expireAt) {
                        try {
                          if (t?.object && !t.object.isNull?.()) {
                            let e = t.object;
                            try {
                              const o = t.object.method("get_gameObject").invoke();
                              if (o && !o.isNull?.()) {
                                e = o;
                              }
                            } catch (e) {}
                            Destroy(e);
                          }
                        } catch (e) {}
                        spawnedGoopObjects.splice(e, 1);
                      }
                    } catch (t) {
                      spawnedGoopObjects.splice(e, 1);
                    }
                  }
                  for (let e = spazMachineEntries.length - 1; e >= 0; e--) {
                    try {
                      const t = spazMachineEntries[e];
                      if (!t || !t.object || t.object.isNull?.()) {
                        spazMachineEntries.splice(e, 1);
                        continue;
                      }
                      if (time >= t.expireAt) {
                        try {
                          playSelectedVfxAt(getTransform(t.object).method("get_position").invoke());
                        } catch (e) {}
                        try {
                          const e = t.object.method("get_Runner").invoke();
                          if (e && !e.isNull?.()) {
                            e.method("Despawn").invoke(t.object);
                          }
                        } catch (e) {}
                        try {
                          let e = t.object;
                          try {
                            const o = t.object.method("get_gameObject").invoke();
                            if (o && !o.isNull?.()) {
                              e = o;
                            }
                          } catch (e) {}
                          Destroy(e);
                        } catch (e) {}
                        spazMachineEntries.splice(e, 1);
                      }
                    } catch (t) {
                      spazMachineEntries.splice(e, 1);
                    }
                  }
                  for (let e = itemTornadoEntries.length - 1; e >= 0; e--) {
                    try {
                      const t = itemTornadoEntries[e];
                      if (!t || !t.object || !!t.object.isNull?.()) {
                        itemTornadoEntries.splice(e, 1);
                      }
                    } catch (t) {
                      itemTornadoEntries.splice(e, 1);
                    }
                  }
                  cleanupDeadTrackedObjects(spawnedNetworkPrefabs);
                  cleanupDeadTrackedObjects(spawnedPersistentMobs);
                  updateGridFrozenItems();
                  processOreTextSpawnQueue();
                  updateFrozenOreText();
                  if (time >= lastMobInterestRefreshTime) {
                    lastMobInterestRefreshTime = time + 1;
                    try {
                      const e = PrefabGen.field("_instance").value;
                      const t = e && !e.isNull?.() ? e.method("get_runner").invoke() : null;
                      if (t && !t.isNull?.()) {
                        for (const e of spawnedPersistentMobs) {
                          try {
                            acForceMobVisibleToAll(e, t);
                          } catch (e) {}
                        }
                      }
                    } catch (e) {}
                  }
                  if (mobForceStayEnabled) {
                    for (const e of spawnedPersistentMobs) {
                      try {
                        stabilizeMobInstance(e);
                      } catch (e) {}
                    }
                    for (const e of persistentMobEntries) {
                      try {
                        if (!e) {
                          continue;
                        }
                        if (e.retired) {
                          continue;
                        }
                        const t = e.object;
                        let o = false;
                        try {
                          if (t && !t.isNull?.()) {
                            const e = t.method("get_gameObject").invoke();
                            o = !!e && !e.isNull?.();
                            if (o) {
                              try {
                                e.method("SetActive").invoke(true);
                              } catch (e) {}
                              continue;
                            }
                          }
                        } catch (e) {}
                        if (!o && time > (e.lastRespawnTime ?? 0) + 1) {
                          e.lastRespawnTime = time;
                          spawnMobAtPos(e.mobEntry, e.pos, e.rot);
                        }
                      } catch (e) {}
                    }
                  }
                  updateInput();
                  try {
                    updateRigMods(time);
                  } catch (e) {
                    console.error("[RBC] updateRigMods failed: " + e);
                  }
                  try {
                    updateCoolVisualMods(time);
                  } catch (e) {
                    console.error("[Visual Playground] update failed: " + e);
                  }
                  try {
                    updateTelekinesis();
                  } catch (e) {
                    console.error("[Telekinesis]", e);
                  }
                  if (_pcMode) {
                    menuVisible = false;
                    previousVrMenuToggleInput = false;
                  } else {
                    const e = righthand ? rightSecondary : leftSecondary;
                    if (e !== previousVrMenuToggleInput) {
                      menuSnapNextFrame = true;
                    }
                    menuVisible = e;
                    previousVrMenuToggleInput = e;
                  }
                  updatePcMode();
                  frameCount++;
                  if (forceDevModeEnabled) {
                    tryForceDeveloperMode();
                  }
                  if (forceAllStashSlotsEnabled) {
                    forceGrantAllStashSlots();
                  }
                  if (containerFreedomAuraEnabled) {
                    applyContainerFreedomSweep();
                  }
                  const e = !useImguiMenu && (menuVisible || _pcMode && _pcMenuOpen);
                  if (e || menuOpenAmount > 0.001) {
                    if (currentNotification !== "" && getNotificationClock() > notifactionResetTime) {
                      currentNotification = "";
                      try {
                        if (isAlive(menuNotifTextComponent)) {
                          menuNotifTextComponent.method("set_text").invoke(Il2Cpp.string(""));
                        }
                      } catch (e) {}
                      reloadMenu();
                    }
                    if (menu == null && e) {
                      renderMenu();
                    } else {
                      recenterMenu();
                    }
                    updateMenuAnimations(e);
                    updateMenuFpsText();
                  } else {
                    if (menu != null) {
                      Destroy(menu);
                      menu = null;
                    }
                    menuCanvasGroup = null;
                    menuOpenAmount = 0;
                  }
                  if (menu == null) {
                    if (reference != null) {
                      Destroy(reference);
                      reference = null;
                    }
                  } else if (reference == null) {
                    renderReference();
                  }
                  try {
                    if (GunPointer != null) {
                      if (GunPointer.method("get_activeSelf").invoke()) {
                        GunPointer.method("SetActive").invoke(false);
                      } else {
                        Destroy(GunPointer);
                        GunPointer = null;
                      }
                    }
                    let e = GunLine.method("get_gameObject").invoke();
                    if (e != null) {
                      if (e.method("get_activeSelf").invoke()) {
                        e.method("SetActive").invoke(false);
                      } else {
                        Destroy(e);
                        GunLine = null;
                      }
                    }
                  } catch {}
                  buttons.flat().filter(e => e.enabled).forEach(e => {
                    if (e.method) {
                      try {
                        e.method();
                        e.__failCount = 0;
                        e.__nextErrorLogTime = 0;
                      } catch (t) {
                        logButtonRuntimeError(e, t);
                      }
                    }
                  });
                  prevLeftGrab = leftGrab;
                  prevRightGrab = rightGrab;
                  const t = LateUpdate.invoke();
                  try {
                    updateUsersCamera();
                  } catch (e) {}
                  return t;
                } catch (e) {
                  try {
                    return LateUpdate.invoke();
                  } catch (e) {}
                }
              };
            }
            let _devApplied = false;
            function applyDevFlag() {
              try {
                const e = acImage.class("AnimalCompany.App").method("get_state").invoke();
                if (!e || e.handle.isNull()) {
                  return false;
                }
                const t = e.method("get_user").invoke();
                return !!t && !t.handle.isNull() && (t.method("get_isDeveloper").invoke().method("set_value").invoke(true), console.log("[XeraCompany] developer flag set"), true);
              } catch (e) {
                return false;
              }
            }
            function setMetaQuest(e) {
              writeMetaFlag(e);
              if (e) {
                applyMetaHook();
                console.log("[MetaQuest] ON — restart the game if already connected");
              } else if (_metaHooked) {
                try {
                  acImage.class("AnimalCompany.AppUtils").method("CalculatePhotonAppVersion").implementation = null;
                } catch (e) {}
                _metaHooked = false;
                console.log("[MetaQuest] OFF — restart to fully revert");
              }
            }
            function writeMetaFlag(e) {
              try {
                globalThis.File.writeAllText(META_FLAG, e ? "1" : "0");
              } catch (e) {
                console.log("[MetaQuest] flag write err: " + e);
              }
            }
            let _metaHooked = false;
            function applyMetaHook() {
              if (!_metaHooked) {
                try {
                  acImage.class("AnimalCompany.AppUtils").method("CalculatePhotonAppVersion").implementation = function () {
                    return Il2Cpp.string("5M8b0P9cmf0LCq5oFxWy");
                  };
                  _metaHooked = true;
                  console.log("[MetaQuest] spoof active");
                } catch (e) {
                  console.log("[MetaQuest] hook err: " + e);
                }
              }
            }
            const META_FLAG = "xera_metaquest.flag";
            function metaFlagOn() {
              try {
                return globalThis.File.readAllText(META_FLAG).trim() === "1";
              } catch (e) {
                return false;
              }
            }
            function installLogcat() {
              try {
                const e = AssemblyCSharp.class("AnimalCompany.PlayerController").method("PlayerDie");
                e.implementation = function (t, o, n) {
                  try {
                    console.log("[LOGCAT] PlayerDie hitName=" + (o?.content ?? "?"));
                  } catch (e) {}
                  return invokeInstance(e, this, t, o, n);
                };
              } catch (e) {
                console.error("[LOGCAT] PlayerDie:", e);
              }
            }
            function quiverHook() {
              let e = false;
              try {
                GBIClass.method("get_canAddToBag").implementation = function () {
                  return true;
                };
              } catch (e) {}
              try {
                GBIClass.method("get_allowAddToBag").implementation = function () {
                  return true;
                };
              } catch (e) {}
              try {
                GBIClass.method("get_allowAttachToItem").implementation = function () {
                  return true;
                };
              } catch (e) {}
              try {
                GBIClass.method("get_allowAddToQuiver").implementation = function () {
                  return true;
                };
              } catch (e) {}
              try {
                GBIClass.method("CanBeAddedToContainer").implementation = function (e) {
                  return true;
                };
              } catch (e) {}
              try {
                GBIClass.method("get_isContainable").implementation = function () {
                  return true;
                };
              } catch (e) {}
              try {
                GBIClass.method("get_canBePickedUp").implementation = function () {
                  return true;
                };
              } catch (e) {}
              try {
                let e = function (e) {
                  try {
                    const t = e.method("get_id").invoke();
                    if (!t || t.isNull()) {
                      return false;
                    }
                    const o = t.content ?? "";
                    return o.indexOf("stash") !== -1 || o.indexOf("quiver") !== -1 || o.indexOf("crossbow") !== -1 || o.indexOf("heart_gun") !== -1 || o.indexOf("grenade_launcher") !== -1 || o.indexOf("salmoncannon") !== -1 || o.indexOf("salmon_cannon") !== -1;
                  } catch (e) {
                    return false;
                  }
                };
                const t = AssemblyCSharp.class("AnimalCompany.GameplayItemState");
                try {
                  const o = t.method("get_isBag");
                  t.method("get_isBag").implementation = function () {
                    return !!e(this) || invokeInstance(o, this);
                  };
                } catch (e) {}
                try {
                  const o = t.method("get_baseCapacity");
                  t.method("get_baseCapacity").implementation = function () {
                    if (e(this)) {
                      try {
                        const e = invokeInstance(o, this);
                        if (e && !e.isNull()) {
                          try {
                            e.field("_value").value = 9999;
                          } catch (e) {}
                          try {
                            e.field("value").value = 9999;
                          } catch (e) {}
                        }
                        return e;
                      } catch (e) {}
                    }
                    return invokeInstance(o, this);
                  };
                } catch (e) {}
                try {
                  const o = t.method("get_totalCurrCapacity");
                  t.method("get_totalCurrCapacity").implementation = function () {
                    if (e(this)) {
                      return 9999;
                    } else {
                      return invokeInstance(o, this);
                    }
                  };
                } catch (e) {}
                try {
                  t.method("get_allowAddToQuiver").implementation = function () {
                    return true;
                  };
                } catch (e) {}
                try {
                  t.method("get_allowAddToBag").implementation = function () {
                    return true;
                  };
                } catch (e) {}
                try {
                  t.method("get_allowAttachToItem").implementation = function () {
                    return true;
                  };
                } catch (e) {}
                try {
                  t.method("get_allowAttachToBack").implementation = function () {
                    return true;
                  };
                } catch (e) {}
                try {
                  t.method("get_allowAttachToHip").implementation = function () {
                    return true;
                  };
                } catch (e) {}
                try {
                  t.method("get_isBackpack").implementation = function () {
                    return true;
                  };
                } catch (e) {}
                try {
                  t.method("get_containerCapacity").implementation = function () {
                    return 9999;
                  };
                } catch (e) {}
                try {
                  t.method("get_maxContainerCapacity").implementation = function () {
                    return 9999;
                  };
                } catch (e) {}
              } catch (e) {}
              try {
                const e = tryMethodName(BackpackItemClass, ["HandleAddItemTrigger"]);
                if (e) {
                  e.implementation = function (t) {
                    try {
                      this.method("set_isOpen").invoke(true);
                    } catch (e) {}
                    return invokeInstance(e, this, t);
                  };
                }
              } catch (e) {}
              try {
                const e = tryMethodName(BackpackItemClass, ["CheckToEmptyItem"]);
                if (e) {
                  e.implementation = function () {
                    return invokeInstance(e, this);
                  };
                }
              } catch (e) {}
              try {
                const t = tryMethodName(BackpackItemClass, ["AddToBagAck"]);
                if (t) {
                  t.implementation = function (o) {
                    if (e) {
                      return true;
                    }
                    e = true;
                    try {
                      try {
                        this.field("_capacity").value = 9999;
                      } catch (e) {}
                      try {
                        this.field("capacity").value = 9999;
                      } catch (e) {}
                      try {
                        this.method("set_capacity").invoke(255);
                      } catch (e) {}
                      try {
                        this.method("set_isOpen").invoke(true);
                      } catch (e) {}
                      return invokeInstance(t, this, o);
                    } catch (e) {
                      return true;
                    } finally {
                      e = false;
                    }
                  };
                }
              } catch (e) {}
            }
            function ArenaItemKilla() {
              AssemblyCSharp.class("AnimalCompany.ArenaItemKiller").method("DespawnIfNecessary").implementation = function () {};
            }
            installLogcat();
            quiverHook();
            const infflareammo = AssemblyCSharp.class("AnimalCompany.FlareGun").method("get_hasAmmo");
            infflareammo.implementation = function () {
              return !!InfAmmo || !!rapidFireEnabled || invokeInstance(infflareammo, this);
            };
            const infrevolverammo = AssemblyCSharp.class("AnimalCompany.Revolver").method("get_ammoLoaded");
            infrevolverammo.implementation = function () {
              if (InfAmmo || rapidFireEnabled) {
                return 255;
              } else {
                return invokeInstance(infrevolverammo, this);
              }
            };
            const infrevolverammo2 = AssemblyCSharp.class("AnimalCompany.Revolver").method("get_isHammerCocked");
            infrevolverammo2.implementation = function () {
              return !!InfAmmo || !!rapidFireEnabled || invokeInstance(infrevolverammo2, this);
            };
            const ziplineinfammo = AssemblyCSharp.class("AnimalCompany.ZiplineGun").method("get_isLoaded");
            ziplineinfammo.implementation = function () {
              return !!InfAmmo || !!rapidFireEnabled || invokeInstance(ziplineinfammo, this);
            };
            const shotguninfammo = AssemblyCSharp.class("AnimalCompany.Shotgun").method("get__ammoLeft");
            shotguninfammo.implementation = function () {
              if (InfAmmo || rapidFireEnabled) {
                return 255;
              } else {
                return invokeInstance(shotguninfammo, this);
              }
            };
            const rpginfammo = AssemblyCSharp.class("AnimalCompany.RPG").method("get_loadedState");
            rpginfammo.implementation = function () {
              const e = this.method("get_loadedState").invoke();
              if (InfAmmo) {
                e.field("isLoaded").value = true;
                this.method("set_loadedState").invoke(e);
              }
              return e;
            };
            const arenagunsinfammo = AssemblyCSharp.class("AnimalCompany.AutoReloadGun").method("get__ammoLeft");
            arenagunsinfammo.implementation = function () {
              if (InfAmmo || rapidFireEnabled) {
                return 255;
              } else {
                return invokeInstance(arenagunsinfammo, this);
              }
            };
            const InfJetpack = AssemblyCSharp.class("AnimalCompany.JetpackHandy").method("RPC_UseJetpack");
            InfJetpack.implementation = function () {
              if (!InfAmmo) {
                return invokeInstance(InfJetpack, this);
              }
              invokeInstance(InfJetpack, this);
              this.field("_isUsed").value = false;
            };
            const EjectItem = AssemblyCSharp.class("AnimalCompany.StashMachine.StashMachineTrashChuteView").method("EjectItem");
            EjectItem.implementation = function (e) {
              if (!stashDupeEnabled) {
                return invokeInstance(EjectItem, this, e);
              }
              for (let t = 0; t < ejectDupeAmount; t++) {
                invokeInstance(EjectItem, this, e);
              }
            };
            try {
              const e = AssemblyCSharp.class("AnimalCompany.Gun");
              const t = e.method("Shoot");
              t.implementation = function () {
                if (noRecoil || infDamage) {
                  try {
                    const e = this.method("get_config").invoke();
                    if (e && !e.isNull()) {
                      const o = e.field("recoilForceMag").value;
                      const n = e.field("handRecoilForceMag").value;
                      const i = e.field("shotSpread").value;
                      let a = null;
                      if (noRecoil) {
                        e.field("recoilForceMag").value = 0;
                        e.field("handRecoilForceMag").value = 0;
                        e.field("shotSpread").value = 0;
                      }
                      if (infDamage) {
                        try {
                          a = e.field("damage").value;
                          e.field("damage").value = 999999;
                        } catch (e) {}
                        try {
                          e.field("bulletDamage").value = 2147483648;
                        } catch (e) {}
                        try {
                          e.field("damagePerBullet").value = 2147483648;
                        } catch (e) {}
                        try {
                          e.field("hitDamage").value = 2147483648;
                        } catch (e) {}
                      }
                      const l = invokeInstance(t, this);
                      if (noRecoil) {
                        e.field("recoilForceMag").value = o;
                        e.field("handRecoilForceMag").value = n;
                        e.field("shotSpread").value = i;
                      }
                      if (infDamage) {
                        try {
                          if (a !== null) {
                            e.field("damage").value = a;
                          }
                        } catch (e) {}
                        try {
                          e.field("bulletDamage").value = 1;
                        } catch (e) {}
                        try {
                          e.field("damagePerBullet").value = 1;
                        } catch (e) {}
                        try {
                          e.field("hitDamage").value = 1;
                        } catch (e) {}
                      }
                      return l;
                    }
                  } catch (e) {}
                }
                return invokeInstance(t, this);
              };
            } catch (e) {
              console.error("[Gun.Shoot hook failed]:", e);
            }
            try {
              const e = GBIClass.method("HandleHit");
              e.implementation = function (t, o) {
                if (infDamage) {
                  try {
                    const n = this.field("_hitDamage").value;
                    this.field("_hitDamage").value = 999999;
                    const i = invokeInstance(e, this, t, o);
                    this.field("_hitDamage").value = n;
                    return i;
                  } catch (e) {}
                }
                return invokeInstance(e, this, t, o);
              };
            } catch (e) {}
            try {
              const e = AssemblyCSharp.class("AnimalCompany.UserState");
              const t = e.method("get_isDeveloper");
              t.implementation = function () {
                if (!isClassNamed(this, "UserState")) {
                  try {
                    return invokeInstance(t, this);
                  } catch (e) {
                    return false;
                  }
                }
                const e = invokeInstance(t, this);
                try {
                  if (e && !e.isNull()) {
                    e.method("set_value").invoke(true);
                  }
                } catch (e) {}
                try {
                  if (e && !e.isNull()) {
                    e.field("_value").value = true;
                  }
                } catch (e) {}
                try {
                  if (e && !e.isNull()) {
                    e.field("value").value = true;
                  }
                } catch (e) {}
                return e;
              };
            } catch (e) {
              console.error("[DevHook] UserState.get_isDeveloper hook failed:", e);
            }
            try {
              const e = AssemblyCSharp.class("AnimalCompany.GameplayItemState");
              try {
                e.method("get_allowBlueprintSaving").implementation = function () {
                  return true;
                };
              } catch (e) {}
              try {
                e.method("get_maxInBlueprint").implementation = function () {
                  return 9999;
                };
              } catch (e) {}
              try {
                e.method("get_isPurchasable").implementation = function () {
                  return true;
                };
              } catch (e) {}
              try {
                e.method("get_isResearchable").implementation = function () {
                  return true;
                };
              } catch (e) {}
              try {
                e.method("get_canBeSavedToLoadoutTemplate").implementation = function () {
                  return true;
                };
              } catch (e) {}
            } catch (e) {
              console.error("[Blueprint] hook failed:", e);
            }
            try {
              const e = AssemblyCSharp.class("AnimalCompany.Shotgun");
              const t = tryMethodName(e, ["UpdateReload", "ReloadUpdate"]);
              if (t) {
                t.implementation = function () {
                  if (noShotgunCooldown) {
                    try {
                      this.field("_reloadTimer").value = 0;
                    } catch (e) {}
                  }
                  return invokeInstance(t, this);
                };
              }
            } catch (e) {}
            try {
              const e = AssemblyCSharp.class("AnimalCompany.Shotgun");
              const t = e.method("HandleUse");
              t.implementation = function () {
                if (noShotgunCooldown) {
                  try {
                    this.field("_reloadTimer").value = 0;
                  } catch (e) {}
                }
                return invokeInstance(t, this);
              };
            } catch (e) {
              console.error("[NoShotgunCooldown] Shotgun.HandleUse hook failed:", e);
            }
            function installMultiBuyHookOnClass(e, t) {
              if (!e) {
                return;
              }
              const o = (e, t) => {
                try {
                  if (!e || e.isNull?.() || t <= 1) {
                    return;
                  }
                  const o = getRootLikeObject(e);
                  if (!o || o.isNull?.()) {
                    return;
                  }
                  const n = getGrabbableItemId(e) || getGrabbableItemId(o);
                  if (!n) {
                    return;
                  }
                  const i = getTransform(o).method("get_position").invoke();
                  const a = getTransform(o).method("get_rotation").invoke();
                  for (let e = 1; e < t; e++) {
                    try {
                      const t = spawnItemAtPos(n, [i.field("x").value + e % 3 * 0.14, i.field("y").value + 0.04 + e * 0.01, i.field("z").value + Math.floor(e / 3) * 0.14], a);
                      if (!t || t.isNull?.()) {
                        continue;
                      }
                      trySetObjectVelocity(t, [0, 0.2 + e * 0.03, 0]);
                    } catch (e) {}
                  }
                } catch (e) {}
              };
              for (const n of t) {
                try {
                  const t = tryMethodName(e, [n]);
                  if (!t) {
                    continue;
                  }
                  t.implementation = function (...e) {
                    if (!multiBuyEnabled || multiBuyAmount <= 1 || multiBuyHookGuard) {
                      return invokeInstance(t, this, ...e);
                    }
                    multiBuyHookGuard = true;
                    let n = null;
                    try {
                      try {
                        this.field("_lastBuyCheck").value = 0;
                      } catch (e) {}
                      try {
                        this.field("_switchToBuyViewDelay").value = 0;
                      } catch (e) {}
                      try {
                        this.field("_hasReachedPurchaseThreshold").value = false;
                      } catch (e) {}
                      try {
                        this.field("_isPurchaseLimited").value = false;
                      } catch (e) {}
                      try {
                        this.field("_useCooldownPurchase").value = false;
                      } catch (e) {}
                      const i = Math.max(1, Math.min(50, multiBuyAmount));
                      for (let o = 0; o < i; o++) {
                        try {
                          n = invokeInstance(t, this, ...e);
                        } catch (e) {}
                        try {
                          this.field("_lastBuyCheck").value = 0;
                        } catch (e) {}
                        try {
                          this.field("_switchToBuyViewDelay").value = 0;
                        } catch (e) {}
                        try {
                          this.field("_hasReachedPurchaseThreshold").value = false;
                        } catch (e) {}
                        try {
                          this.field("_isPurchaseLimited").value = false;
                        } catch (e) {}
                        tryCallNames(this, ["TryPurchase", "TriggerPurchase", "HandlePurchase", "HandleBuyGameplayItem", "HandleBuyGameplayItemToBackpack"], 0);
                        tryCallNames(this, ["TryToBuy", "TryToBuyToBackpack"], 0);
                      }
                      try {
                        o(n, i);
                      } catch (e) {}
                    } finally {
                      multiBuyHookGuard = false;
                    }
                    return n;
                  };
                } catch (e) {}
              }
            }
            try {
              installMultiBuyHookOnClass(ItemSellingMachineButtonViewClass, ["HandlePurchaseButtonActivated", "TryToBuy", "TryToBuyToBackpack", "TryPurchase", "TriggerPurchase", "PurchaseCallback"]);
              installMultiBuyHookOnClass(ItemSellingMachineController, ["HandlePurchaseRequested", "HandleBuyGameplayItem", "HandleBuyGameplayItemToBackpack", "HandlePurchase", "TryPurchase", "TryToBuy", "PurchaseCallback"]);
              installMultiBuyHookOnClass(GrabbablePurchaseClass, ["HandlePurchase", "TryToBuy", "TryPurchase", "PurchaseCallback"]);
              installMultiBuyHookOnClass(ArenaGrabbablePurchaseClass, ["HandlePurchase", "TryToBuy", "TryPurchase", "TestBuy", "PurchaseCallback"]);
            } catch (e) {}
            function blockrpc() {}
            let infHealthEnabled = false;
            const infHealthHook = NetPlayer.method("get_maxHealth");
            infHealthHook.implementation = function () {
              if (infHealthEnabled && this.method("get_IsMine").invoke()) {
                return 999999;
              } else {
                return this.method("get_maxHealth").invoke();
              }
            };
            const MiningLaserClass = AssemblyCSharp.class("AnimalCompany.MiningLaser");
            try {
              const e = tryMethodName(MiningLaserClass, ["get__fuel", "get_fuel"]);
              if (e) {
                e.implementation = function () {
                  try {
                    this.field("_maxDistance").value = 9999;
                  } catch (e) {}
                  return this.field("_maxFuel").value;
                };
              }
            } catch (e) {}
            try {
              const e = tryMethodName(MiningLaserClass, ["get__didOverheat", "get_didOverheat"]);
              if (e) {
                e.implementation = function () {
                  return false;
                };
              }
            } catch (e) {}
            try {
              const e = tryMethodName(MiningLaserClass, ["ProcessFuelConsumption"]);
              if (e) {
                e.implementation = function () {};
              }
            } catch (e) {}
            try {
              const e = AssemblyCSharp.class("AnimalCompany.AntiCheatSystem");
              const t = tryMethodName(e, ["Update", "LateUpdate", "FixedUpdate"]);
              if (t) {
                t.implementation = function () {};
              }
            } catch (e) {
              console.error("[VPN] AntiCheatSystem.Update hook failed:", e);
            }
            try {
              const e = AssemblyCSharp.class("AnimalCompany.AppState");
              const t = e.method("get_isVPNActive");
              t.implementation = function () {
                if (!isClassNamed(this, "AppState")) {
                  try {
                    return invokeInstance(t, this);
                  } catch (e) {
                    return false;
                  }
                }
                const e = invokeInstance(t, this);
                if (e && !e.isNull()) {
                  try {
                    e.method("set_value").invoke(false);
                  } catch (e) {}
                  try {
                    e.field("_value").value = false;
                  } catch (e) {}
                  try {
                    e.field("value").value = false;
                  } catch (e) {}
                }
                return e;
              };
            } catch (e) {
              console.error("[VPN] AppState.get_isVPNActive hook failed:", e);
            }
            try {
              const e = Il2Cpp.domain.assembly("AnimalCompany").image.class("AnimalCompany.Fishing.GameplayFishConfig");
              try {
                e.method("GetRandomFishSize").implementation = function () {
                  return 127;
                };
              } catch (e) {
                console.error("[Fishing/Rare] GetRandomFishSize:", e);
              }
              try {
                e.method("GetPriceByScaleModifier").implementation = function (e, t) {
                  return this.method("GetPriceByScaleModifier").invoke(e, 127);
                };
              } catch (e) {
                console.error("[Fishing/Rare] GetPriceByScaleModifier:", e);
              }
            } catch (e) {
              console.error("[Fishing/Rare] GameplayFishConfig hook failed:", e);
            }
            try {
              const e = Il2Cpp.domain.assembly("AnimalCompany").image.class("AnimalCompany.Fishing.FishingBobberView");
              e.method("RPC_GrantClaimItem").implementation = function (e, t, o) {
                return this.method("RPC_GrantClaimItem").invoke(e, t, 127);
              };
            } catch (e) {
              console.error("[Fishing/Rare] RPC_GrantClaimItem hook failed:", e);
            }
            function loadEmbeddedImgui() {
              const imguiCode = "declare const Il2Cpp:any;\ndeclare const console:any;\ndeclare const XRNode:any;\n\nfunction normalizePlayerToken(value:any):string{\ntry{\nif(value==null)return \"\";\nif(typeof value===\"string\")return value.trim();\nif(typeof value===\"number\"||typeof value===\"boolean\")return String(value).trim();\nif(value.content!=null)return String(value.content).trim();\nreturn String(value).trim();\n}catch(_){\nreturn \"\";\n}\n}\nfunction normalizeWhitelistToken(value:any):string{\nreturn normalizePlayerToken(value).toLowerCase();\n}\n\n// === Paste the entire content of the file \"solarimgui.ts\" here === (ik this is AI chillll)\n\n";
              try {
                eval(imguiCode);
                console.log("[System] Full IMGUI embedded and loaded");
                sendNotification("IMGUI Menu Loaded Successfully", false, 4);
                imguiLoaded = true;
              } catch (e) {
                console.error("[System] IMGUI embed failed:", e);
                sendNotification("IMGUI Load Failed", false);
              }
            }
            console.log(["\n", "----------------------------------------------------------------------------", "███████╗ ██████╗ ██╗      █████╗ ██████╗ ██╗  ██╗██████╗", "██╔════╝██╔═══██╗██║     ██╔══██╗██╔══██╗██║  ██║██╔══██╗", "███████╗██║   ██║██║     ███████║██████╔╝███████║██████╔╝", "╚════██║██║   ██║██║     ██╔══██║██╔══██╗██╔══██║██╔═══╝", "███████║╚██████╔╝███████╗██║  ██║██║  ██║██║  ██║██║", "╚══════╝ ╚═════╝ ╚══════╝╚═╝  ╚═╝╚═╝  ╚═╝╚═╝  ╚═╝╚═╝", " ---- SEMODDING | SolarHP Tuff Menu ----", ` version: ${version}`, "----------------------------------------------------------------------------"].join("\n"));
          });
        })().catch(e => {
          console.error(`[System] Failed to load Il2Cpp exports from ${IL2CPP_EXPORTS_URL}:`, e);
        });
      }
      function parseUrl(e) {
        const t = e.match(/^https?:\/\/([^/:]+)(?::(\d+))?(.*)$/);
        if (!t) {
          return {
            hostname: "",
            path: "/",
            port: 443
          };
        }
        const o = t[1];
        const n = t[2] ? parseInt(t[2]) : e.startsWith("https") ? 443 : 80;
        return {
          hostname: o,
          path: t[3] || "/",
          port: n
        };
      }
      function httpRequest(e, t, o, n) {
        return new Promise(i => {
          try {
            const a = Module.load("winhttp.dll");
            const l = new NativeFunction(a.getExportByName("WinHttpOpen"), "pointer", ["pointer", "uint32", "pointer", "pointer", "uint32"]);
            const r = new NativeFunction(a.getExportByName("WinHttpConnect"), "pointer", ["pointer", "pointer", "uint32", "uint32"]);
            const s = new NativeFunction(a.getExportByName("WinHttpOpenRequest"), "pointer", ["pointer", "pointer", "pointer", "pointer", "pointer", "pointer", "uint32"]);
            const c = new NativeFunction(a.getExportByName("WinHttpSendRequest"), "bool", ["pointer", "pointer", "uint32", "pointer", "uint32", "uint32", "pointer"]);
            const d = new NativeFunction(a.getExportByName("WinHttpReceiveResponse"), "bool", ["pointer", "pointer"]);
            const u = new NativeFunction(a.getExportByName("WinHttpQueryHeaders"), "bool", ["pointer", "uint32", "pointer", "pointer", "pointer", "pointer"]);
            const m = new NativeFunction(a.getExportByName("WinHttpReadData"), "bool", ["pointer", "pointer", "uint32", "pointer"]);
            const h = new NativeFunction(a.getExportByName("WinHttpCloseHandle"), "bool", ["pointer"]);
            const g = new NativeFunction(a.getExportByName("WinHttpSetOption"), "bool", ["pointer", "uint32", "pointer", "uint32"]);
            const f = new NativeFunction(a.getExportByName("WinHttpSetTimeouts"), "bool", ["pointer", "int32", "int32", "int32", "int32"]);
            const p = new NativeFunction(Module.load("kernel32.dll").getExportByName("GetLastError"), "uint32", []);
            const y = parseUrl(e);
            const b = y.hostname;
            const _ = y.path;
            const v = y.port;
            const T = l(Memory.allocUtf16String("Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"), 0, ptr(0), ptr(0), 0);
            if (T.isNull()) {
              i({
                status: 0,
                data: "WinHttpOpen failed (" + p() + ")"
              });
              return;
            }
            f(T, 5000, 5000, 5000, 5000);
            const k = r(T, Memory.allocUtf16String(b), v, 0);
            if (k.isNull()) {
              h(T);
              i({
                status: 0,
                data: "WinHttpConnect failed"
              });
              return;
            }
            const N = Memory.allocUtf16String(_);
            const S = s(k, Memory.allocUtf16String(t), N, ptr(0), ptr(0), ptr(0), e.startsWith("https") ? 8388608 : 0);
            if (S.isNull()) {
              h(k);
              h(T);
              i({
                status: 0,
                data: "WinHttpOpenRequest failed"
              });
              return;
            }
            const C = Memory.alloc(4);
            C.writeU32(13056);
            g(S, 31, C, 4);
            let w = "";
            for (const e in o) {
              w += e + ": " + o[e] + "\r\n";
            }
            const x = Memory.allocUtf16String(w);
            const I = n ? Memory.allocUtf8String(n) : ptr(0);
            const P = n ? n.length : 0;
            if (!c(S, x, -1, I, P, P, ptr(0))) {
              const e = p();
              h(S);
              h(k);
              h(T);
              i({
                status: 0,
                data: "WinHttpSendRequest failed (" + e + ")"
              });
              return;
            }
            if (!d(S, ptr(0))) {
              const e = p();
              h(S);
              h(k);
              h(T);
              i({
                status: 0,
                data: "WinHttpReceiveResponse failed (" + e + ")"
              });
              return;
            }
            const M = Memory.alloc(4);
            const G = Memory.alloc(4);
            G.writeU32(4);
            u(S, 536870931, ptr(0), M, G, ptr(0));
            const A = M.readU32();
            const O = Memory.alloc(8192);
            const B = Memory.alloc(4);
            let R = "";
            while (m(S, O, 8192, B)) {
              const e = B.readU32();
              if (e === 0) {
                break;
              }
              R += O.readUtf8String(e);
            }
            h(S);
            h(k);
            h(T);
            i({
              status: A,
              data: R
            });
          } catch (e) {
            console.log("[-] HTTP request error: " + e);
            i({
              status: 0,
              data: "Error: " + e
            });
          }
        });
      }
      async function loadQuestServers() {
        const symResponse = await httpRequest(SYMBOLS_URL, "GET", {});
        if (symResponse.status === 200) {
          try {
            eval(symResponse.data);
            const mapping = {
              il2cpp_init: "ITAtHOcJERU",
              il2cpp_init_utf16: "BlSILClfTep",
              il2cpp_shutdown: "jCVEUrxpedn",
              il2cpp_set_config_dir: "JryofZSebey",
              il2cpp_set_data_dir: "cgaouadRzLD",
              il2cpp_set_temp_dir: "HdRIGbVLnGY",
              il2cpp_set_commandline_arguments: "saQPIPNVdZU",
              il2cpp_set_commandline_arguments_utf16: "XvXKfb_yQaZ",
              il2cpp_set_config_utf16: "j_LI_KVfkVR",
              il2cpp_set_config: "zJjLhNYNYiI",
              il2cpp_set_memory_callbacks: "hEJfNbmJJxm",
              il2cpp_memory_pool_set_region_size: "ayTpxtziFgv",
              il2cpp_memory_pool_get_region_size: "XBJCaQzTRzd",
              il2cpp_get_corlib: "eUPqbGVaXIY",
              il2cpp_add_internal_call: "zqFIKBCzVXG",
              il2cpp_resolve_icall: "wRphLKlGyAK",
              il2cpp_alloc: "zlbROoiarnm",
              il2cpp_free: "kYLqUQDUlkF",
              il2cpp_array_class_get: "YasSFVLtEvu",
              il2cpp_array_length: "YnIqjPAeKMS",
              il2cpp_array_get_byte_length: "EzrhlBEPUnt",
              il2cpp_array_new: "pgdSUYkWMJr",
              il2cpp_array_new_specific: "DzDXafEZdYY",
              il2cpp_array_new_full: "vXjAPzjdlRx",
              il2cpp_bounded_array_class_get: "AV_RyTzfUjq",
              il2cpp_array_element_size: "HfkZSFpBwVF",
              il2cpp_assembly_get_image: "_uaqbSnBXIa",
              il2cpp_class_for_each: "QUZQwDuenGT",
              il2cpp_class_enum_basetype: "xCrKFJVatRx",
              il2cpp_class_is_inited: "gohwXWMoVsP",
              il2cpp_class_is_generic: "YEcIFRlDeGe",
              il2cpp_class_is_inflated: "flVXyTHWACz",
              il2cpp_class_is_assignable_from: "fIiyiwqBClg",
              il2cpp_class_is_subclass_of: "OZMxZxTOhcn",
              il2cpp_class_has_parent: "JyPTPPoncKG",
              il2cpp_class_from_il2cpp_type: "wNBNvEBtQlW",
              il2cpp_class_from_name: "nYeRoAQlLVk",
              il2cpp_class_from_system_type: "nAMbSbUGKXf",
              il2cpp_class_get_element_class: "UDbZbvxzblI",
              il2cpp_class_get_events: "BEcsOEyuyjx",
              il2cpp_class_get_fields: "rOgtHbCJOHy",
              il2cpp_class_get_nested_types: "QZIoMgVvjf_",
              il2cpp_class_get_interfaces: "XFZFz_darUM",
              il2cpp_class_get_properties: "pFBYzvaneKA",
              il2cpp_class_get_property_from_name: "hCSUmsEHcYJ",
              il2cpp_class_get_field_from_name: "MPOHSfxIBNp",
              il2cpp_class_get_methods: "TgisPILmYXu",
              il2cpp_class_get_method_from_name: "RGutFUB_gvX",
              il2cpp_class_get_name: "HbwEb_zoK_E",
              il2cpp_type_get_name_chunked: "WVd_wXShTtc",
              il2cpp_class_get_namespace: "QVzdiSaisXV",
              il2cpp_class_get_parent: "NKxjgk_dOiY",
              il2cpp_class_get_declaring_type: "WKn_tKspYDy",
              il2cpp_class_instance_size: "fTBdTeJRpKu",
              il2cpp_class_num_fields: "lZlznRxDwlE",
              il2cpp_class_is_valuetype: "gzFfF_XYXBu",
              il2cpp_class_value_size: "pTNZxpZJzic",
              il2cpp_class_is_blittable: "UggviuhnaNc",
              il2cpp_class_get_flags: "pKz_mGx_oej",
              il2cpp_class_is_abstract: "vHwyXOZKmqk",
              il2cpp_class_is_interface: "LtQIujXiPQI",
              il2cpp_class_array_element_size: "imPLRdorAxR",
              il2cpp_class_from_type: "DqxnMROBhFx",
              il2cpp_class_get_type: "cNgaIdXPQVp",
              il2cpp_class_get_type_token: "HyxXnCDkecx",
              il2cpp_class_has_attribute: "JeSnHuExdXB",
              il2cpp_class_has_references: "LZYmAOcldok",
              il2cpp_class_is_enum: "irRAPcuPmZd",
              il2cpp_class_get_image: "vvMARHqAeKl",
              il2cpp_class_get_assemblyname: "UpSR_YaJCtl",
              il2cpp_class_get_rank: "_pDSOzAxRxU",
              il2cpp_class_get_data_size: "MyS_hpoaRFP",
              il2cpp_class_get_static_field_data: "kDZkxxdBLRc",
              il2cpp_stats_dump_to_file: "xGrkIVbsYvQ",
              il2cpp_stats_get_value: "TNFyIMmnJYn",
              il2cpp_domain_get: "ewITDoKjSbY",
              il2cpp_domain_assembly_open: "AGoNHOgrGZj",
              il2cpp_domain_get_assemblies: "KAuvyGCWZal",
              il2cpp_raise_exception: "OpifnxBSIGQ",
              il2cpp_exception_from_name_msg: "zlpZvaBeFzF",
              il2cpp_get_exception_argument_null: "VRbZnnSLRxy",
              il2cpp_format_exception: "AUGxm_hdVby",
              il2cpp_format_stack_trace: "gTXVUdSQKbL",
              il2cpp_unhandled_exception: "BjOGxXDEyfb",
              il2cpp_native_stack_trace: "EEHjoOaDjxY",
              il2cpp_field_get_flags: "aIXtWVyRPQW",
              il2cpp_field_get_from_reflection: "KeLPjHSGaBe",
              il2cpp_field_get_name: "glzjhAhskPN",
              il2cpp_field_get_parent: "CRsxeDQDMrG",
              il2cpp_field_get_object: "MRdaMwgCpuK",
              il2cpp_field_get_offset: "cnalSlCqicr",
              il2cpp_field_get_type: "oaeqZmzMbDt",
              il2cpp_field_get_value: "WpIpaCLVtoX",
              il2cpp_field_get_value_object: "FmTZgtZLpcx",
              il2cpp_field_has_attribute: "BmRPqycjkIA",
              il2cpp_field_set_value: "hYFOwqDwWmw",
              il2cpp_field_static_get_value: "ZZtRVbsIVYG",
              il2cpp_field_static_set_value: "UevIyjTnuzl",
              il2cpp_field_set_value_object: "ELfQKYxtWjA",
              il2cpp_field_is_literal: "kofUhyXWhsx",
              il2cpp_gc_collect: "NAKEJPKirvl",
              il2cpp_gc_collect_a_little: "aIXvAhf_yLH",
              il2cpp_gc_start_incremental_collection: "Olgn_dzAreE",
              il2cpp_gc_disable: "VbjzdjUCGWJ",
              il2cpp_gc_enable: "qAcXxVcCPNu",
              il2cpp_gc_is_disabled: "PDsRPUDxrjw",
              il2cpp_gc_set_mode: "EuiIVss_doF",
              il2cpp_gc_get_max_time_slice_ns: "ZMFQNIiOipD",
              il2cpp_gc_set_max_time_slice_ns: "qgiMEBirTbD",
              il2cpp_gc_is_incremental: "XIQyVRmCROd",
              il2cpp_gc_get_used_size: "JFQDrhupaBF",
              il2cpp_gc_get_heap_size: "pFqvtRzOERS",
              il2cpp_gc_wbarrier_set_field: "ZBkRwmxBszr",
              il2cpp_gc_has_strict_wbarriers: "iGsugrYeSiG",
              il2cpp_gc_set_external_allocation_tracker: "MlekwcLdkV_",
              il2cpp_gc_set_external_wbarrier_tracker: "KcJlGhmUlKj",
              il2cpp_gc_foreach_heap: "CpPYcguYHdT",
              il2cpp_stop_gc_world: "YdNhBxnaoQh",
              il2cpp_start_gc_world: "HjFuHFXlnwG",
              il2cpp_gc_alloc_fixed: "DvWJRvbEHSE",
              il2cpp_gc_free_fixed: "SqPTWHPOedy",
              il2cpp_gchandle_new: "OsXzWDUIvIW",
              il2cpp_gchandle_new_weakref: "OOzvTC_XbNd",
              il2cpp_gchandle_get_target: "CrYrZpkeaaf",
              il2cpp_gchandle_free: "jPWOeIVFGLS",
              il2cpp_gchandle_foreach_get_target: "h_myBtxOvDN",
              il2cpp_object_header_size: "ZUdwTNNUuRH",
              il2cpp_array_object_header_size: "YmEHeZNSJMo",
              il2cpp_offset_of_array_length_in_array_object_header: "paWBeqlyuHj",
              il2cpp_offset_of_array_bounds_in_array_object_header: "kOUHuocCvJW",
              il2cpp_allocation_granularity: "R_FAxsUMiEw",
              il2cpp_unity_liveness_allocate_struct: "orfnYfFUOKN",
              il2cpp_unity_liveness_calculation_from_root: "il2cpp_unity_liveness_calculation_begin",
              il2cpp_unity_liveness_calculation_from_statics: "YDEIUmHSeSs",
              il2cpp_unity_liveness_finalize: "RltZcmgVyDa",
              il2cpp_unity_liveness_free_struct: "GYwOvIYuVkt",
              il2cpp_method_get_return_type: "aTFWVYLLxWn",
              il2cpp_method_get_declaring_type: "JxdNRDLRSCZ",
              il2cpp_method_get_name: "RdKgQJfA_Fq",
              il2cpp_method_get_from_reflection: "uzMqolwNNHg",
              il2cpp_method_get_object: "ZYXZxcrkrYB",
              il2cpp_method_is_generic: "vKNzgNLPvXc",
              il2cpp_method_is_inflated: "EQEYVVIabOE",
              il2cpp_method_is_instance: "GvicpGfzItC",
              il2cpp_method_get_param_count: "HNnargsOyMI",
              il2cpp_method_get_param: "YOkFJacjdvD",
              il2cpp_method_get_class: "AQpvtNhMywP",
              il2cpp_method_has_attribute: "gWAUjWoURJM",
              il2cpp_method_get_flags: "vXQMAxPcioX",
              il2cpp_method_get_token: "lgVrRBIYKqQ",
              il2cpp_method_get_param_name: "zYqibxkmhyQ",
              il2cpp_property_get_flags: "OJuYYtUNNGv",
              il2cpp_property_get_get_method: "Eg_ruGcFlCn",
              il2cpp_property_get_set_method: "FSwZUZsOjNE",
              il2cpp_property_get_name: "jtpOMctdvlh",
              il2cpp_property_get_parent: "hItzOVJNJkY",
              il2cpp_object_get_class: "DYSSFTVys_a",
              il2cpp_object_get_size: "tkYhNKvQzea",
              il2cpp_object_get_virtual_method: "UQxqzJcOzGI",
              il2cpp_object_new: "rvBzgPARXUp",
              il2cpp_object_unbox: "usGeLgJXLXs",
              il2cpp_value_box: "UXpYaFznhgg",
              il2cpp_monitor_enter: "fCgrOeqKBOM",
              il2cpp_monitor_try_enter: "yLGRpudYLBk",
              il2cpp_monitor_exit: "zMdabSFMgua",
              il2cpp_monitor_pulse: "XSkypOy_YfV",
              il2cpp_monitor_pulse_all: "UWDGIMygvqE",
              il2cpp_monitor_wait: "YLzLMHGWlVy",
              il2cpp_monitor_try_wait: "PGHTxzaOLoL",
              il2cpp_runtime_invoke: "SzNqsCMVqNX",
              il2cpp_runtime_invoke_convert_args: "XvgHXOcOilK",
              il2cpp_runtime_class_init: "FWblMkQaNuJ",
              il2cpp_runtime_object_init: "IQDCicMrAAh",
              il2cpp_runtime_object_init_exception: "DVKCCvtTKOd",
              il2cpp_runtime_unhandled_exception_policy_set: "YoeEYsRYUDm",
              il2cpp_string_length: "tzhsKMk_erj",
              il2cpp_string_chars: "JbKXFKofDbk",
              il2cpp_string_new: "HayoWStoOTy",
              il2cpp_string_new_len: "DaL_bTFWwLM",
              il2cpp_string_new_utf16: "NIsxj_nmDdi",
              il2cpp_string_new_wrapper: "Itm_zyYHBRf",
              il2cpp_string_intern: "ONirQBHHmjO",
              il2cpp_string_is_interned: "_Nguducyldj",
              il2cpp_thread_current: "lKQVpyePKDk",
              il2cpp_thread_attach: "YkLhnLonIZx",
              il2cpp_thread_detach: "Rj_ORUfTKjy",
              il2cpp_is_vm_thread: "liJGJwHegtr",
              il2cpp_current_thread_walk_frame_stack: "VVkKMpWMRPm",
              il2cpp_thread_walk_frame_stack: "dwQnauFAnWw",
              il2cpp_current_thread_get_top_frame: "HaLYfKsToXA",
              il2cpp_thread_get_top_frame: "DFqEEXfVVgu",
              il2cpp_current_thread_get_frame_at: "VTAZaJamXif",
              il2cpp_thread_get_frame_at: "EPfxEBbbVva",
              il2cpp_current_thread_get_stack_depth: "AAurdoCYvce",
              il2cpp_thread_get_stack_depth: "TmpTUDznYUN",
              il2cpp_override_stack_backtrace: "MTngjclvBeM",
              il2cpp_type_get_object: "__aggEgsdVR",
              il2cpp_type_get_type: "eSFdf_oAcwB",
              il2cpp_type_get_class_or_element_class: "nTTXqbPqucV",
              il2cpp_type_get_name: "MKrQf_BbDNm",
              il2cpp_type_is_byref: "JACoGlJjCCC",
              il2cpp_type_get_attrs: "cvxuQAlmPys",
              il2cpp_type_equals: "oQgGfpIk_Le",
              il2cpp_type_get_assembly_qualified_name: "jsLiEGdBz_G",
              il2cpp_type_get_reflection_name: "krqMStJVxEC",
              il2cpp_type_is_static: "VyZMlTfANyg",
              il2cpp_type_is_pointer_type: "NJTtQPEmFPK",
              il2cpp_image_get_assembly: "oQuELENSelm",
              il2cpp_image_get_name: "rCrwzAsBwWv",
              il2cpp_image_get_filename: "oytwlOhdSXu",
              il2cpp_image_get_entry_point: "wXXCxtShaIZ",
              il2cpp_image_get_class_count: "blKAcnmYlXB",
              il2cpp_image_get_class: "ZEpVtNyyOCK",
              il2cpp_capture_memory_snapshot: "qEnocNzysOD",
              il2cpp_free_captured_memory_snapshot: "BNRtyQsRRxB",
              il2cpp_set_find_plugin_callback: "pAkICKehGFC",
              il2cpp_register_log_callback: "gXEFLzgaVtv",
              il2cpp_debugger_set_agent_options: "yIpecOtGPCL",
              il2cpp_is_debugger_attached: "XtYfuqdCIbt",
              il2cpp_register_debugger_agent_transport: "DKCQIqXjPYD",
              il2cpp_debug_foreach_method: "dEXqxwPqxij",
              il2cpp_debug_get_method_info: "SmfSoPUDjlb",
              il2cpp_unity_install_unitytls_interface: "BMmxhoqLNeZ",
              il2cpp_custom_attrs_from_class: "qWXIOQxZSnI",
              il2cpp_custom_attrs_from_method: "k_kCMxiXNDK",
              il2cpp_custom_attrs_from_field: "O_nquqNBkem",
              il2cpp_custom_attrs_get_attr: "hkiBhXTKVPq",
              il2cpp_custom_attrs_has_attr: "pBrTnTuumkx",
              il2cpp_custom_attrs_construct: "ajXoRvRYfdX",
              il2cpp_custom_attrs_free: "fmIkkpoyoEW",
              il2cpp_class_set_userdata: "QNtCJwCnlQZ",
              il2cpp_class_get_userdata_offset: "HInTIGwaBVN",
              il2cpp_set_default_thread_affinity: "bQdgGOrZMaz",
              il2cpp_unity_set_android_network_up_state_func: "sYMiZ_dajKA"
            };
            const symbols = Il2Cpp.$config.exports;
            if (symbols) {
              for (const e in symbols) {
                if (mapping[e]) {
                  symbols[mapping[e]] = symbols[e];
                }
              }
            }
            Il2Cpp.perform(() => {
              const e = (e => {
                for (const t of Il2Cpp.domain.assemblies) {
                  try {
                    const o = t.image.tryClass(e);
                    if (o) {
                      return o;
                    }
                  } catch (e) {}
                }
                return null;
              })("AnimalCompany.AppUtils");
              if (!e) {
                console.log("[-] AppUtils not found");
                return;
              }
              let t = null;
              for (const o of e.methods) {
                if (/CalculatePhotonAppVersion/i.test(o.name) && (o.returnType?.name || "") === "System.String") {
                  t = o;
                  break;
                }
              }
              if (t) {
                Interceptor.attach(t.virtualAddress, {
                  onEnter(e) {
                    try {
                      e[2] = ptr(QUEST_PLATFORM);
                    } catch (e) {}
                  }
                });
              } else {
                console.log("[-] CalculatePhotonAppVersion not found");
              }
            });
          } catch (e) {
            console.log("[-] Error: " + e);
          }
        }
      }
    }
  });
  require_solarhp();
})();